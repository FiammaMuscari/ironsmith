use super::render_effects::{
    apply_continuous_for_compaction, choose_primary_zone, choose_search_zones,
    damage_with_source_view, describe_annihilator_keyword,
    describe_choose_color_reveal_hand_discard_that_color,
    describe_choose_color_then_chosen_color_mana,
    describe_choose_name_target_mills_conditional_draw,
    describe_choose_sacrifice_then_reflexive_trigger_refs, describe_choose_selection,
    describe_choose_then_mount_vehicle_become, describe_choose_then_sacrifice,
    describe_choose_two_move_one_put_counters_on_other,
    describe_conditional_spell_uncounterability,
    describe_copy_count_replacement_with_shared_plural_retarget, describe_damage_target,
    describe_damaged_player_gain_control_then_rewards, describe_declined_may_mill_then_damage,
    describe_distinct_player_choice_with_linked_action,
    describe_exile_then_free_cast_while_exiled_structural, describe_explicit_target_then_coin_flip,
    describe_for_players_choose_then_destroy_chosen_collection_pair,
    describe_gain_control_aura_then_chosen_legal_attach_segments,
    describe_gain_life_shuffle_source_and_graveyard,
    describe_look_hand_choose_then_discard_or_exile,
    describe_optional_looked_entry_with_counter_and_remainder,
    describe_optional_source_exiled_copy_then_cast_pair,
    describe_optional_sticker_aura_return_attach_sequence,
    describe_quantified_tap_goad_then_watch_set, describe_repeated_die_parity_result_program,
    describe_return_as_aura_with_granted_abilities, describe_return_then_conditional_animation,
    describe_return_then_linked_enter_return_pair,
    describe_reveal_hand_choose_discard_then_adventure_move,
    describe_reveal_hand_choose_graveyard_exile_bundle,
    describe_reveal_hand_choose_graveyard_or_hand_exile,
    describe_reveal_top_to_hand_then_lose_mana_value_effects,
    describe_revealed_hand_then_draw_shared_terminal_union,
    describe_revealed_hand_then_optional_free_cast, describe_roll_choose_destroy_create_structural,
    describe_roll_result_damage_then_random_source_attachment_program,
    describe_same_name_reference_search_bundle, describe_search_choose_then_exile_and_cast,
    describe_search_face_down_exile_shuffle_split_bargain_cast_else_hand,
    describe_search_origin_zones, describe_secret_choice_match_sequence,
    describe_separated_countered_spell_exile_with_counters_gain_suspend,
    describe_sequenced_d20_numeric_result_table_program,
    describe_shuffle_reveal_repeated_permanent_groups_rest_bottom,
    describe_single_hand_reveal_same_name_search, describe_single_hand_reveal_setup,
    describe_source_owner_shuffle_then_reveal_named_to_battlefield,
    describe_tagged_copy_then_plural_retarget_pair, describe_tagged_target_then_conditional_action,
    describe_target_groups_then_random_destroy, describe_target_player_draw_exile_then_copy_result,
    describe_two_target_creature_exchange_or_fight,
    describe_untap_then_phase_out_until_source_leaves, downcast_destroy, move_to_zone_surface_view,
    render_consult_reveal_put_battlefield_rest_graveyard,
    render_remove_abilities_then_destroy_matching_creatures,
    render_search_reveal_opponent_choose_rest_bundle, sacrifice_view,
    singularize_simple_noun_phrase, triggered_search_devotion_color,
};
use super::*;
use crate::cards::CardDefinitionRuntimeExt;

pub(super) struct RawRenderedLine(String);

impl RawRenderedLine {
    pub(super) fn into_string(self) -> String {
        self.0
    }
}

fn flatten_strict_segment_sequences(
    program: &crate::resolution::ResolutionProgram,
) -> Option<Vec<&Effect>> {
    if program.segments.is_empty()
        || program
            .segments
            .iter()
            .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let mut flattened = Vec::new();
    for segment in &program.segments {
        for effect in &segment.default_effects {
            if let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>() {
                flattened.extend(sequence.effects.iter());
            } else {
                flattened.push(effect);
            }
        }
    }
    Some(flattened)
}

/// Render a result-ID-bound draw fallback using the authored "If you can't"
/// surface. The shared ID proves that failure belongs to the entire preceding
/// sequence rather than to an unrelated earlier effect.
fn describe_cross_segment_failed_sequence_draw_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [sequence_segment, fallback_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if sequence_segment.starts_new_source_line
        || !sequence_segment.self_replacements.is_empty()
        || !fallback_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [with_id_effect] = sequence_segment.default_effects.as_slice() else {
        return None;
    };
    let with_id = with_id_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let sequence = with_id
        .effect
        .downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.effects.len() < 2 || sequence.surface != ironsmith_core::SequenceSurface::CommaThen
    {
        return None;
    }

    let [fallback_effect] = fallback_segment.default_effects.as_slice() else {
        return None;
    };
    let fallback = fallback_effect.downcast_ref::<crate::effects::IfEffect>()?;
    let [draw_effect] = fallback.then.as_slice() else {
        return None;
    };
    let draw = draw_effect.downcast_ref::<crate::effects::DrawCardsEffect>()?;
    if fallback.condition != with_id.id
        || fallback.predicate != EffectPredicate::DidNotHappen
        || !fallback.else_.is_empty()
        || fallback.per_player_result
        || fallback.prior_result_replacement_surface
        || draw.count != Value::Fixed(1)
        || draw.player != PlayerFilter::You
    {
        return None;
    }

    Some((
        format!(
            "{}. If you can't, draw a card",
            describe_effect(with_id_effect).trim().trim_end_matches('.')
        ),
        2,
    ))
}

fn describe_cross_segment_gain_control_aura_legal_attach_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [gain_segment, attach_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if gain_segment.starts_new_source_line
        || !gain_segment.self_replacements.is_empty()
        || !attach_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [gain_effect] = gain_segment.default_effects.as_slice() else {
        return None;
    };
    let [choose_effect, attach_effect] = attach_segment.default_effects.as_slice() else {
        return None;
    };
    Some((
        describe_gain_control_aura_then_chosen_legal_attach_segments(
            gain_effect,
            choose_effect,
            attach_effect,
        )?,
        2,
    ))
}

fn describe_cross_segment_counter_then_keyword_grants_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [counter_segment, grant_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !counter_segment.self_replacements.is_empty() || !grant_segment.self_replacements.is_empty()
    {
        return None;
    }
    let mut effects = counter_segment.default_effects.clone();
    effects.extend(grant_segment.default_effects.iter().cloned());
    let rendered =
        super::render_effects::describe_put_counters_then_coordinated_keyword_grants(&effects)?;
    Some((rendered, 2))
}

fn exact_tagged_pool_filter(filter: &ObjectFilter, pool: &TagKey) -> bool {
    let [constraint] = filter.tagged_constraints.as_slice() else {
        return false;
    };
    if constraint.tag != *pool
        || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
    {
        return false;
    }
    let mut remainder = filter.clone();
    remainder.tagged_constraints.clear();
    remainder.zone = None;
    remainder.union_surface = Default::default();
    remainder == ObjectFilter::default()
}

enum DelegatedPartitionSet {
    Tagged(TagKey),
    Difference { pool: TagKey, excluded: TagKey },
}

enum DelegatedPartitionMove {
    Hand(DelegatedPartitionSet),
    Library(DelegatedPartitionSet),
}

fn delegated_partition_set(spec: &ChooseSpec) -> Option<DelegatedPartitionSet> {
    match spec.base() {
        ChooseSpec::Tagged(tag) => Some(DelegatedPartitionSet::Tagged(tag.clone())),
        ChooseSpec::Object(filter) => {
            let mut pool = None;
            let mut excluded = None;
            for constraint in &filter.tagged_constraints {
                match constraint.relation {
                    crate::filter::TaggedOpbjectRelation::IsTaggedObject if pool.is_none() => {
                        pool = Some(constraint.tag.clone());
                    }
                    crate::filter::TaggedOpbjectRelation::IsNotTaggedObject
                        if excluded.is_none() =>
                    {
                        excluded = Some(constraint.tag.clone());
                    }
                    _ => return None,
                }
            }
            let mut residual = filter.clone();
            residual.zone = None;
            residual.tagged_constraints.clear();
            residual.union_surface = Default::default();
            (residual == ObjectFilter::default()).then_some(DelegatedPartitionSet::Difference {
                pool: pool?,
                excluded: excluded?,
            })
        }
        _ => None,
    }
}

fn delegated_partition_move(effect: &Effect) -> Option<DelegatedPartitionMove> {
    let effect = structural_unwrap_render_wrappers(effect);
    if let Some(movement) = effect.downcast_ref::<crate::effects::MoveToZoneEffect>() {
        let set = delegated_partition_set(&movement.target)?;
        if movement.zone != Zone::Hand
            || movement.to_top
            || movement.library_order.is_some()
            || movement.enters_tapped
        {
            return None;
        }
        return Some(DelegatedPartitionMove::Hand(set));
    }
    let shuffle = effect.downcast_ref::<crate::effects::ShuffleObjectsIntoLibraryEffect>()?;
    let set = delegated_partition_set(&shuffle.target)?;
    if shuffle.player != PlayerFilter::You
        || shuffle.owner_library_destination
        || shuffle.possessive_owner_subject
    {
        return None;
    }
    Some(DelegatedPartitionMove::Library(set))
}

/// Rejoin a searched/revealed collection, an explicitly delegated opponent
/// subset, and the two complementary destinations across source sentences.
/// Every tag, chooser, count, and destination is checked before the authored
/// collection nouns replace executable tag scaffolding.
fn describe_cross_segment_delegated_search_partition_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let effects = flatten_strict_segment_sequences(program)?;
    let [
        search_effect,
        reveal_effect,
        player_effect,
        choose_effect,
        first_move,
        second_move,
    ] = effects.as_slice()
    else {
        return None;
    };
    let search = search_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let reveal = reveal_effect.downcast_ref::<crate::effects::RevealTaggedEffect>()?;
    let player = player_effect.downcast_ref::<crate::effects::ChoosePlayerEffect>()?;
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    if !search.is_search
        || search.chooser != PlayerFilter::You
        || reveal.tag != search.tag
        || player.chooser != PlayerFilter::You
        || player.filter != PlayerFilter::Opponent
        || player.random
        || !player.excluded_tags.is_empty()
        || choose.chooser != PlayerFilter::TaggedPlayer(player.tag.clone())
        || choose.count.min != 2
        || choose.count.max != Some(2)
        || choose.count.dynamic_x
        || choose.count.random
        || !exact_tagged_pool_filter(&choose.filter, &search.tag)
    {
        return None;
    }

    let first = delegated_partition_move(first_move)?;
    let second = delegated_partition_move(second_move)?;
    let chosen = choose.tag.clone();
    let (chosen_zone, rest_zone) = match (first, second) {
        (
            DelegatedPartitionMove::Hand(DelegatedPartitionSet::Tagged(first)),
            DelegatedPartitionMove::Library(DelegatedPartitionSet::Difference { pool, excluded }),
        ) if first == chosen && pool == search.tag && excluded == chosen => {
            (Zone::Hand, Zone::Library)
        }
        (
            DelegatedPartitionMove::Library(DelegatedPartitionSet::Tagged(first)),
            DelegatedPartitionMove::Hand(DelegatedPartitionSet::Difference { pool, excluded }),
        ) if first == chosen && pool == search.tag && excluded == chosen => {
            (Zone::Library, Zone::Hand)
        }
        // Retain compatibility with the older normalized program while the
        // executable producer is migrated to the explicit set difference.
        (
            DelegatedPartitionMove::Hand(DelegatedPartitionSet::Tagged(first)),
            DelegatedPartitionMove::Library(DelegatedPartitionSet::Tagged(second)),
        ) if first == chosen && second.as_str() == "rest" => (Zone::Hand, Zone::Library),
        (
            DelegatedPartitionMove::Library(DelegatedPartitionSet::Tagged(first)),
            DelegatedPartitionMove::Hand(DelegatedPartitionSet::Tagged(second)),
        ) if first == chosen && second.as_str() == "rest" => (Zone::Library, Zone::Hand),
        _ => return None,
    };

    let first_sequence = program
        .segments
        .first()?
        .default_effects
        .first()?
        .downcast_ref::<crate::effects::SequenceEffect>()?;
    let mut selection = describe_choose_selection(search);
    if choose_search_zones(search)?.contains(&Zone::OutsideGame)
        && search.filter.owner == Some(PlayerFilter::You)
        && !selection.contains(" you own")
    {
        if let Some((noun, qualifier)) = selection.split_once(" with ") {
            selection = format!("{noun} you own with {qualifier}");
        } else {
            selection.push_str(" you own");
        }
    }
    let origin = describe_search_origin_zones(search)?;
    let search_line = match first_sequence.surface {
        ironsmith_core::SequenceSurface::CommaThen => {
            format!("Search {origin} for {selection}, then reveal those cards")
        }
        ironsmith_core::SequenceSurface::Coordinated => {
            format!("Search {origin} for {selection} and reveal them")
        }
        _ => return None,
    };
    let choice_object = if first_sequence.surface == ironsmith_core::SequenceSurface::CommaThen {
        "them"
    } else {
        "those cards"
    };
    let movement = match (chosen_zone, rest_zone) {
        (Zone::Hand, Zone::Library) => {
            "Put the chosen cards into your hand and shuffle the rest into your library"
        }
        (Zone::Library, Zone::Hand) => {
            "Shuffle the chosen cards into your library and put the rest into your hand"
        }
        _ => return None,
    };
    Some(format!(
        "{search_line}. An opponent chooses two of {choice_object}. {movement}"
    ))
}

fn exact_guided_library_category_choice(
    choose: &crate::effects::ChooseObjectsEffect,
    chooser: &TagKey,
    pool: &TagKey,
    result: &TagKey,
    required_type: Option<CardType>,
    excluded_types: &[CardType],
) -> bool {
    if choose.chooser != PlayerFilter::TaggedPlayer(chooser.clone())
        || choose.tag != *result
        || choose.count != ChoiceCount::exactly(1)
        || choose.count_value.is_some()
        || choose.aggregate_constraint.is_some()
        || choose.is_search
        || choose.replace_tagged_objects
        || choose.remember_as_chosen_object
        || !(choose_primary_zone(choose) == Some(Zone::Library)
            || (choose.filter.zone.is_none()
                && choose.zone == Some(Zone::Battlefield)
                && choose.additional_zones
                    == [Zone::Hand, Zone::Graveyard, Zone::Library, Zone::Exile]))
    {
        return false;
    }
    let [constraint] = choose.filter.tagged_constraints.as_slice() else {
        return false;
    };
    if constraint.tag != *pool
        || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
    {
        return false;
    }
    let mut filter = choose.filter.clone();
    filter.zone = None;
    filter.tagged_constraints.clear();
    filter.union_surface = Default::default();
    let mut expected = ObjectFilter::default();
    if let Some(card_type) = required_type {
        expected.card_types.push(card_type);
    }
    expected.excluded_card_types = excluded_types.to_vec();
    filter == expected
}

/// Rejoin a whole-library reveal, an opponent's three categorical selections,
/// and the chosen-set hand move. The shared pool/result/chooser tags prove all
/// later references before replacing them with the authored collection nouns.
fn describe_guided_library_category_choice_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let effects = flatten_strict_segment_sequences(program)?;
    let [
        tag_effect,
        reveal_effect,
        player_effect,
        creature_effect,
        land_effect,
        other_effect,
        move_effect,
        shuffle_effect,
    ] = effects.as_slice()
    else {
        return None;
    };
    let tagged = tag_effect.downcast_ref::<crate::effects::TagMatchingObjectsEffect>()?;
    let reveal = reveal_effect.downcast_ref::<crate::effects::RevealTaggedEffect>()?;
    let player = player_effect.downcast_ref::<crate::effects::ChoosePlayerEffect>()?;
    let creature = creature_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let land = land_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let other = other_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let movement = structural_unwrap_render_wrappers(move_effect)
        .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    let shuffle = structural_unwrap_render_wrappers(shuffle_effect)
        .downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;

    let mut pool_filter = tagged.filter.clone();
    pool_filter.zone = None;
    let pool = &tagged.tag;
    let result = &creature.tag;
    if tagged.zone != Some(Zone::Library)
        || !tagged.additional_zones.is_empty()
        || !tagged.source_tags.is_empty()
        || pool_filter != ObjectFilter::default().owned_by(PlayerFilter::You)
        || reveal.tag != *pool
        || player.chooser != PlayerFilter::You
        || player.filter != PlayerFilter::Opponent
        || player.random
        || !player.excluded_tags.is_empty()
        || player.remember_as_chosen_player
        || !exact_guided_library_category_choice(
            creature,
            &player.tag,
            pool,
            result,
            Some(CardType::Creature),
            &[],
        )
        || !exact_guided_library_category_choice(
            land,
            &player.tag,
            pool,
            result,
            Some(CardType::Land),
            &[],
        )
        || !exact_guided_library_category_choice(
            other,
            &player.tag,
            pool,
            result,
            None,
            &[CardType::Creature, CardType::Land],
        )
        || movement.target != ChooseSpec::Tagged(result.clone())
        || movement.zone != Zone::Hand
        || movement.to_top
        || movement.library_order.is_some()
        || movement.enters_tapped
        || !matches!(movement.actor_surface, None | Some(PlayerFilter::You))
        || shuffle.player != PlayerFilter::You
        || shuffle.target_spec.is_some()
    {
        return None;
    }

    Some(
        "Reveal the cards in your library. An opponent chooses from among them a creature card, a land card, and a noncreature, nonland card. You put the chosen cards into your hand. Then shuffle"
            .to_string(),
    )
}

fn describe_cross_segment_player_choice_characteristics_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let refs = flatten_strict_segment_sequences(program)?;
    super::render_effects::describe_for_players_choose_move_then_characteristics(&refs).or_else(
        || {
            super::render_effects::describe_for_players_choose_move_then_combined_characteristics(
                &refs,
            )
        },
    )
}

fn describe_cross_segment_consult_battlefield_remainder_graveyard_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [consult_segment, disposition_segment] = program.segments.as_slice() else {
        return None;
    };
    if !consult_segment.self_replacements.is_empty()
        || !disposition_segment.self_replacements.is_empty()
    {
        return None;
    }
    let consult = match consult_segment.default_effects.as_slice() {
        [consult] => consult,
        [target_effect, consult] => {
            let target = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
            let consult_view =
                consult.downcast_ref::<crate::effects::ConsultTopOfLibraryEffect>()?;
            if target.chooser.is_some()
                || !matches!(
                    target.target.base(),
                    ChooseSpec::Player(PlayerFilter::Opponent)
                )
                || consult_view.player != PlayerFilter::Target(Box::new(PlayerFilter::Opponent))
            {
                return None;
            }
            consult
        }
        _ => return None,
    };
    let [move_match, move_remainder] = disposition_segment.default_effects.as_slice() else {
        return None;
    };
    render_consult_reveal_put_battlefield_rest_graveyard(&[consult, move_match, move_remainder])
}

fn describe_mixed_target_exile_top_damage_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() || segment.starts_new_source_line {
        return None;
    }
    let [
        declaration_effect,
        player_loop_effect,
        object_loop_effect,
        permission_effect,
    ] = segment.default_effects.as_slice()
    else {
        return None;
    };
    let declaration = declaration_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let target_only = declaration
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let ChooseSpec::ObjectOrPlayer(object_filter, PlayerFilter::Any) = target_only.target.base()
    else {
        return None;
    };
    let mut semantic_filter = object_filter.clone();
    semantic_filter.union_surface = Default::default();
    let mut expected_filter = ObjectFilter::default().in_zone(Zone::Battlefield);
    expected_filter.card_types = vec![CardType::Creature, CardType::Planeswalker];
    if semantic_filter != expected_filter
        || target_only.target.count() != ChoiceCount::any_number()
        || target_only.chooser.is_some()
        || !target_only.explicit_declaration
    {
        return None;
    }
    let player_loop = player_loop_effect.downcast_ref::<crate::effects::ForPlayersEffect>()?;
    let object_loop = object_loop_effect.downcast_ref::<crate::effects::ForEachTaggedEffect>()?;
    if player_loop.filter != PlayerFilter::AliasedTarget(Box::new(PlayerFilter::Any))
        || player_loop.starting_with_controller
        || player_loop.stop_after_first_happened
        || object_loop.tag != declaration.tag
        || object_loop.controller_at_last_blocked_by.is_some()
    {
        return None;
    }

    let procedure = |effects: &[Effect], expected_target: &ChooseSpec| {
        let [sequence_effect] = effects else {
            return None;
        };
        let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
        if sequence.surface != ironsmith_core::SequenceSurface::CommaThen
            || sequence.result_label.is_some()
        {
            return None;
        }
        let [exile_effect, damage_effect] = sequence.effects.as_slice() else {
            return None;
        };
        let exile = exile_effect.downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()?;
        let [exiled_tag] = exile.moved_tags.as_slice() else {
            return None;
        };
        let mut damage_effect = damage_effect;
        while let Some(tagged) = damage_effect.downcast_ref::<crate::effects::TaggedEffect>() {
            damage_effect = &tagged.effect;
        }
        let damage = damage_effect.downcast_ref::<crate::effects::DealDamageEffect>()?;
        if exile.player != PlayerFilter::You
            || exile.count.unhinted() != &Value::Fixed(1)
            || exile.face_down
            || !exile.accumulated_tags.is_empty()
            || damage.target.unhinted() != expected_target
            || damage.source_is_combat
            || damage.unpreventable
            || !matches!(damage.amount.unhinted(), Value::ManaValueOf(spec) if matches!(spec.base(), ChooseSpec::Tagged(tag) if tag == exiled_tag))
        {
            return None;
        }
        Some((exile.clone(), exiled_tag.clone()))
    };
    let (player_exile, player_exiled_tag) = procedure(
        &player_loop.effects,
        &ChooseSpec::Player(PlayerFilter::IteratedPlayer),
    )?;
    let (object_exile, object_exiled_tag) = procedure(&object_loop.effects, &ChooseSpec::Iterated)?;
    if player_exile != object_exile || player_exiled_tag != object_exiled_tag {
        return None;
    }
    let permission = permission_effect.downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
    if permission.tag != player_exiled_tag
        || permission.player != PlayerFilter::You
        || permission.duration != crate::effects::GrantPlayTaggedDuration::UntilYourNextTurnEnd
        || !permission.allow_land
        || permission.mana_spend_mode != ironsmith_core::value_model::ManaSpendMode::Normal
        || permission.filter.is_some()
        || permission.max_plays.is_some()
    {
        return None;
    }
    Some(
        "Choose any number of target creatures, planeswalkers, and/or players. For each of them, exile the top card of your library, then this spell deals damage equal to that card's mana value to that permanent or player. You may play the exiled cards until the end of your next turn"
            .to_string(),
    )
}

fn describe_prior_exile_until_next_turn_permission_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let (permission_segment, prefix_segments) = program.segments.split_last()?;
    if permission_segment.starts_new_source_line || !permission_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [permission_effect] = permission_segment.default_effects.as_slice() else {
        return None;
    };
    let permission = permission_effect.downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
    if permission.player != PlayerFilter::You
        || permission.duration != crate::effects::GrantPlayTaggedDuration::UntilYourNextTurnEnd
        || !permission.allow_land
        || permission.mana_spend_mode != ironsmith_core::value_model::ManaSpendMode::Normal
        || permission.allow_any_color_for_cast
        || permission.while_on_top_of_library
        || permission.filter.is_some()
        || permission.during_turns_counter_put_on_source.is_some()
        || permission.spell_cost_increase.is_some()
        || permission.lands_enter_tapped
        || permission.max_plays.is_some()
    {
        return None;
    }

    fn collect_matching_exiles(effect: &Effect, tag: &TagKey, counts: &mut Vec<Value>) {
        if let Some(exile) = effect.downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()
            && exile.player == PlayerFilter::You
            && !exile.face_down
            && exile.accumulated_tags.is_empty()
            && matches!(exile.moved_tags.as_slice(), [moved] if moved == tag)
        {
            counts.push(exile.count.clone());
        }
        effect.visit_child_effects(&mut |child| collect_matching_exiles(child, tag, counts));
    }

    let mut counts = Vec::new();
    for segment in prefix_segments {
        for effect in &segment.default_effects {
            collect_matching_exiles(effect, &permission.tag, &mut counts);
        }
    }
    let [count] = counts.as_slice() else {
        return None;
    };
    let prefix = crate::resolution::ResolutionProgram::new(prefix_segments.to_vec());
    let mut rendered = describe_resolution_program(&prefix)
        .trim()
        .trim_end_matches('.')
        .to_string();
    match count.unhinted() {
        Value::Fixed(1) => {
            rendered.push_str(". You may play it until the end of your next turn");
        }
        Value::EventValue(EventValueSpec::Amount) => {
            rendered = rendered.replacen("Whenever that creature deals", "Whenever it deals", 1);
            rendered.push_str(". Until the end of your next turn, you may play those cards");
        }
        _ => return None,
    }
    Some(rendered)
}

#[cfg(test)]
mod guided_library_category_choice_tests {
    use super::*;

    fn creature_choice(
        chooser: &str,
        pool: &str,
        result: &str,
    ) -> crate::effects::ChooseObjectsEffect {
        crate::effects::ChooseObjectsEffect::new(
            ObjectFilter::default()
                .with_type(CardType::Creature)
                .in_zone(Zone::Library)
                .match_tagged(
                    TagKey::from(pool),
                    crate::filter::TaggedOpbjectRelation::IsTaggedObject,
                ),
            ChoiceCount::exactly(1),
            PlayerFilter::TaggedPlayer(TagKey::from(chooser)),
            TagKey::from(result),
        )
        .in_zone(Zone::Library)
    }

    #[test]
    fn category_choice_requires_shared_pool_chooser_and_result_tags() {
        let exact = creature_choice("chooser", "pool", "chosen");
        assert!(exact_guided_library_category_choice(
            &exact,
            &TagKey::from("chooser"),
            &TagKey::from("pool"),
            &TagKey::from("chosen"),
            Some(CardType::Creature),
            &[],
        ));

        let changed_pool = creature_choice("chooser", "other_pool", "chosen");
        assert!(!exact_guided_library_category_choice(
            &changed_pool,
            &TagKey::from("chooser"),
            &TagKey::from("pool"),
            &TagKey::from("chosen"),
            Some(CardType::Creature),
            &[],
        ));
        let changed_chooser = creature_choice("other_chooser", "pool", "chosen");
        assert!(!exact_guided_library_category_choice(
            &changed_chooser,
            &TagKey::from("chooser"),
            &TagKey::from("pool"),
            &TagKey::from("chosen"),
            Some(CardType::Creature),
            &[],
        ));
    }
}

fn describe_damage_result_tap_and_player_cast_lock_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [damage_segment, tap_segment, lock_segment] = program.segments.as_slice() else {
        return None;
    };
    if [damage_segment, tap_segment, lock_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let [damage_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let damage_tagged = damage_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    damage_tagged
        .effect
        .downcast_ref::<crate::effects::DealDistributedDamageEffect>()?;

    let [tap_effect] = tap_segment.default_effects.as_slice() else {
        return None;
    };
    let tap = structural_unwrap_render_wrappers(tap_effect)
        .downcast_ref::<crate::effects::TapEffect>()?;
    if !matches!(tap.target.base(), ChooseSpec::Tagged(tag) if tag == &damage_tagged.tag) {
        return None;
    }

    let [lock_effect] = lock_segment.default_effects.as_slice() else {
        return None;
    };
    let lock = lock_effect.downcast_ref::<crate::effects::ForEachTaggedPlayerEffect>()?;
    let [cant_effect] = lock.effects.as_slice() else {
        return None;
    };
    let cant = structural_unwrap_render_wrappers(cant_effect)
        .downcast_ref::<crate::effects::CantEffect>()?;
    let crate::effect::Restriction::CastSpellsMatching(player, filter) = &cant.restriction else {
        return None;
    };
    let mut expected = ObjectFilter::default();
    expected.excluded_card_types = vec![CardType::Creature, CardType::Land];
    if lock.tag != damage_tagged.tag
        || *player != PlayerFilter::IteratedPlayer
        || filter != &expected
        || cant.duration != Until::EndOfTurn
        || cant.start != crate::effect::RestrictionStart::Immediate
    {
        return None;
    }

    Some(format!(
        "{}. Tap each creature dealt damage this way. Players dealt damage this way can't cast noncreature spells this turn",
        describe_effect(damage_effect).trim_end_matches('.')
    ))
}

pub(super) fn ast_compiled_lines(def: &CardDefinition) -> Vec<RawRenderedLine> {
    // Correlated search/result programs can consume more than a default
    // test-thread stack before a one-megabyte red zone is reached.
    let mut lines = crate::perf::maybe_grow(4 * 1024 * 1024, 16 * 1024 * 1024, || {
        compiled_lines_inner(def)
    })
    .into_iter()
    .map(|line| rewrite_self_exile_cost_source(def, &line))
    .map(|line| normalize_exact_shared_stack_and_source_exile_surfaces(&line))
    .map(|line| rewrite_named_source_counter_reference(def, &line))
    .map(|line| rewrite_source_owner_reference(def, &line))
    .map(|line| rewrite_named_spell_bottom_library_source(def, &line))
    .map(|line| rewrite_attached_chosen_color_reference(def, &line))
    .map(|line| rewrite_typed_x_cost_counter_surface(def, &line))
    .map(|line| rewrite_source_copy_single_target_surface(def, &line))
    .map(|line| rewrite_eminence_source_zone_surface(def, &line))
    .map(|line| rewrite_commander_tax_life_surface(def, &line))
    .map(|line| protect_typed_standard_keyword_reminder(def, &line))
    .map(RawRenderedLine)
    .collect::<Vec<_>>();
    let blitz_grants = def
        .abilities
        .iter()
        .filter(|ability| {
            matches!(
                &ability.kind,
                AbilityKind::Static(static_ability)
                    if static_ability.grant_spec().is_some_and(|spec| matches!(
                        spec.grantable,
                        crate::grant::Grantable::DerivedAlternativeCast(
                            crate::grant::DerivedAlternativeCast::BlitzFromCardManaCost
                        )
                    ))
            )
        })
        .cloned()
        .collect::<Vec<_>>();
    if let Some(compact) = describe_source_line_blitz_from_card_mana_cost_group(&blitz_grants) {
        let mut first = None;
        let mut matched = 0usize;
        for (index, line) in lines.iter().enumerate() {
            let rendered = line
                .0
                .split_once(": ")
                .map_or(line.0.as_str(), |(_, rendered)| rendered);
            if rendered.starts_with("Each creature spell with ") && rendered.contains(" has blitz")
            {
                first.get_or_insert(index);
                matched += 1;
            }
        }
        if matched == blitz_grants.len()
            && let Some(first) = first
        {
            lines.retain(|line| {
                let rendered = line
                    .0
                    .split_once(": ")
                    .map_or(line.0.as_str(), |(_, rendered)| rendered);
                !(rendered.starts_with("Each creature spell with ")
                    && rendered.contains(" has blitz"))
            });
            lines.insert(first, RawRenderedLine(compact));
        }
    }
    lines
}

pub(super) fn rewrite_eminence_source_zone_surface(def: &CardDefinition, line: &str) -> String {
    if !line.starts_with("Eminence — ")
        || !(line.contains("if this source is in the command zone or on the battlefield")
            || line.contains("if this creature is in the command zone or on the battlefield"))
    {
        return line.to_string();
    }
    let source = oracle_short_self_name(def)
        .or_else(|| eminence_short_self_name(&def.card.name))
        .filter(|name| !name.is_empty())
        .unwrap_or_else(|| def.card.name.clone());
    line.replace(
        "if this source is in the command zone or on the battlefield",
        &format!("if {source} is in the command zone or on the battlefield"),
    )
    .replace(
        "if this creature is in the command zone or on the battlefield",
        &format!("if {source} is in the command zone or on the battlefield"),
    )
    .replace("target cat", "target Cat")
    .replace("another spell Vampire", "another Vampire spell")
}

pub(super) fn rewrite_commander_tax_life_surface(def: &CardDefinition, line: &str) -> String {
    let has_substitution = def.abilities.iter().any(|ability| {
        matches!(
            &ability.kind,
            AbilityKind::Static(static_ability)
                if matches!(
                    static_ability.compiled_model().map(|model| &model.payload),
                    Some(ironsmith_core::StaticAbilityPayload::CommanderTaxLifeSubstitution { .. })
                )
        )
    });
    if !has_substitution {
        return line.to_string();
    }
    line.replace(
        "Whenever a player casts a spell, that player loses ",
        "Whenever a player casts a spell, they lose ",
    )
}

fn eminence_short_self_name(name: &str) -> Option<String> {
    if let Some((short, _)) = name.split_once(',') {
        return Some(short.trim().to_string());
    }
    let words = name.split_whitespace().collect::<Vec<_>>();
    let first = *words.first()?;
    let word_count = if matches!(
        first,
        "Captain" | "Doctor" | "Lady" | "Lord" | "Saint" | "Sidar" | "Sir"
    ) {
        2
    } else {
        1
    };
    Some(
        words
            .into_iter()
            .take(word_count)
            .collect::<Vec<_>>()
            .join(" "),
    )
}

fn rewrite_source_copy_single_target_surface(def: &CardDefinition, line: &str) -> String {
    if !line.contains("choose new targets for the copy") {
        return line.to_string();
    }
    fn count_target_declarations(effect: &Effect) -> usize {
        let mut count = usize::from(
            effect
                .downcast_ref::<crate::effects::TargetOnlyEffect>()
                .is_some_and(|target| target.target.is_target()),
        );
        effect.visit_child_effects(&mut |child| {
            count += count_target_declarations(child);
        });
        count
    }
    fn copies_source_spell(effect: &Effect) -> bool {
        let mut found = effect
            .downcast_ref::<crate::effects::CopySpellEffect>()
            .is_some_and(|copy| matches!(copy.target.base(), ChooseSpec::Source));
        effect.visit_child_effects(&mut |child| {
            found |= copies_source_spell(child);
        });
        found
    }
    if !def
        .spell_effect
        .as_ref()
        .is_some_and(|program| program.all_effects().into_iter().any(copies_source_spell))
    {
        return line.to_string();
    }
    let target_declarations = def
        .spell_effect
        .as_ref()
        .into_iter()
        .flat_map(|program| program.all_effects())
        .map(count_target_declarations)
        .sum::<usize>();
    if target_declarations != 1 {
        return line.to_string();
    }
    line.replace(
        "choose new targets for the copy",
        "choose a new target for the copy",
    )
}

fn rewrite_typed_x_cost_counter_surface(def: &CardDefinition, line: &str) -> String {
    fn contains_x_cost_counter(effect: &Effect) -> bool {
        if let Some(counter) = effect.downcast_ref::<crate::effects::CounterEffect>()
            && matches!(counter.target.base(), ChooseSpec::Object(filter) if filter.has_x_in_cost)
        {
            return true;
        }
        let mut found = false;
        effect.visit_child_effects(&mut |child| {
            if !found {
                found = contains_x_cost_counter(child);
            }
        });
        found
    }

    let has_typed_counter = def.abilities.iter().any(|ability| {
        let AbilityKind::Activated(activated) = &ability.kind else {
            return false;
        };
        activated
            .effects
            .segments
            .iter()
            .any(|segment| segment.default_effects.iter().any(contains_x_cost_counter))
    });
    if has_typed_counter {
        line.replace(
            "Counter target spell unless its controller pays",
            "Counter target spell with {X} in its mana cost unless its controller pays",
        )
    } else {
        line.to_string()
    }
}

fn normalize_exact_shared_stack_and_source_exile_surfaces(line: &str) -> String {
    let line = line
    .replace(
        "You may cast a creature spell as though they had flash",
        "You may cast creature spells as though they had flash",
    )
    .replace(
        "Up to one target creature gains vigilance and reach until your next turn",
        "Until your next turn, up to one target creature gains vigilance and reach",
    )
    .replace(
        "Up to one target creature gains reach and vigilance until your next turn",
        "Until your next turn, up to one target creature gains reach and vigilance",
    )
    .replace(
        "Look at the top three cards of your library, choose one of those cards, exile it face down, put the remaining tagged cards on the bottom of your library in any order, then you may cast that card for as long as it remains exiled",
        "Look at the top three cards of your library. Exile one face down and put the rest on the bottom of your library in any order. For as long as it remains exiled, you may cast it if it's a creature spell",
    )
    .replace(
        "Choose another target creature or artifact. When a permanent dealt damage this way dies this turn, return that card to the battlefield under its owner's control",
        "Choose another target creature or artifact. When it's put into a graveyard this turn, return that card to the battlefield under its owner's control",
    )
    .replace(
        "Counter target spell an opponent controls that targets a land you control or an ability an opponent controls that targets a land you control. If an ability is countered this way, destroy that permanent",
        "Counter target spell or ability an opponent controls that targets a land you control. If a permanent's ability is countered this way, destroy that permanent",
    )
    .replace(
        "Look at the top seven cards of your library. Put up to one permanent card with mana value 3 or less from among them onto the battlefield with a shield counter on it. Put the rest on the bottom of your library in a random order",
        "Look at the top seven cards of your library. You may put a permanent card with mana value 3 or less from among them onto the battlefield with a shield counter on it. Put the rest on the bottom of your library in a random order",
    )
    .replace(
        "When you next cast a creature spell this turn, search your library for a creature card with lesser mana value than it, put it onto the battlefield, then shuffle",
        "When you next cast a creature spell this turn, search your library for a creature card with lesser mana value, put it onto the battlefield, then shuffle",
    )
    .replace(
        "Whenever one or more creatures your opponents control attack you, choose target creature that's attacking you",
        "Whenever an opponent attacks you, choose target creature attacking you",
    );
    if line.contains("return each other card exiled with this ")
        && line.contains(" until this ")
        && line.contains("If a creature was exiled this way")
    {
        line.replace(
            "If a creature was exiled this way",
            "If a creature is put into exile this way",
        )
        .replace("under their owners' control", "under its owner's control")
    } else {
        line
    }
}

/// Resolving instants and sorceries name themselves when they put themselves
/// on the bottom of their owner's library. Preserve that authored surface only
/// for an exact typed source move; ordinary source references still render as
/// `this` according to their own surface hint.
fn rewrite_named_spell_bottom_library_source(def: &CardDefinition, line: &str) -> String {
    const GENERIC: &str = "Put this on the bottom of its owner's library";
    if !line.contains(GENERIC)
        || !def
            .card
            .card_types
            .iter()
            .any(|card_type| matches!(card_type, CardType::Instant | CardType::Sorcery))
    {
        return line.to_string();
    }
    let Some(program) = def.spell_effect.as_ref() else {
        return line.to_string();
    };
    let has_exact_source_move = program.segments.iter().any(|segment| {
        segment.default_effects.iter().any(|effect| {
            let effect = unwrap_basic_tag_wrappers(effect);
            let Some(move_effect) = effect.downcast_ref::<crate::effects::MoveToZoneEffect>()
            else {
                return false;
            };
            matches!(move_effect.target.unhinted(), ChooseSpec::Source)
                && move_effect.target.source_reference_surface()
                    == Some(&crate::target::SourceReferenceSurface::ThisPermanentType(
                        "this".to_string(),
                    ))
                && move_effect.zone == Zone::Library
                && !move_effect.to_top
                && move_effect.library_order.is_none()
                && move_effect.verb_surface == ironsmith_core::MoveToZoneVerbSurface::Put
                && !move_effect.target_plural_surface
                && move_effect.remainder_surface.is_none()
                && move_effect.destination_player_surface.is_none()
                && move_effect.destination_player_reference_surface.is_none()
                && move_effect.battlefield_controller
                    == ironsmith_core::BattlefieldController::Preserve
                && move_effect.enters_with_counters.is_empty()
                && !move_effect.enters_tapped
                && !move_effect.enters_attacking
                && !move_effect.enters_face_down
                && !move_effect.enters_transformed
        })
    });
    if !has_exact_source_move {
        return line.to_string();
    }
    line.replacen(
        GENERIC,
        &format!("Put {} on the bottom of its owner's library", def.card.name),
        1,
    )
}

/// The debug-safe cleanup intentionally removes arbitrary parenthetical text.
/// Standard keyword reminders are different: a typed ability plus its exact
/// authored/generated reminder proves that the parenthetical is rules text,
/// not arbitrary annotation. Protect only those exact phrases through
/// cleanup, then the public compiled-text boundary restores parentheses.
fn protect_typed_standard_keyword_reminder(def: &CardDefinition, line: &str) -> String {
    const STANDARD_MENACE_REMINDER: &str =
        "Menace (This creature can't be blocked except by two or more creatures.)";
    const STANDARD_FLANKING_REMINDER: &str = "Flanking (Whenever a creature without flanking blocks this creature, the blocking creature gets -1/-1 until end of turn.)";
    const STANDARD_MENTOR_REMINDER: &str = "Mentor (Whenever this creature attacks, put a +1/+1 counter on target attacking creature with lesser power.)";
    const STANDARD_TRAINING_REMINDER: &str = "Training (Whenever this creature attacks with another creature with greater power, put a +1/+1 counter on this creature.)";
    const STANDARD_DEATHTOUCH_REMINDER: &str = "(Any amount of damage a creature with deathtouch deals to a creature is enough to destroy it.)";
    const STANDARD_OPEN_ATTRACTION_REMINDER: &str =
        "(Put the top card of your Attraction deck onto the battlefield.)";
    const STANDARD_SPLIT_SECOND_REMINDER: &str = "(As long as it's on the stack, players can't cast spells or activate abilities that aren't mana abilities.)";
    const STANDARD_READ_AHEAD_REMINDER: &str = "(Choose a chapter and start with that many lore counters. Add one after your draw step. Skipped chapters don't trigger. Sacrifice after III.)";
    const STANDARD_STUN_COUNTER_REMINDER: &str =
        "(If a permanent with a stun counter would become untapped, remove one from it instead.)";
    let mut protected = if line == "Convoke."
        && def.abilities.iter().any(|ability| {
            matches!(
                &ability.kind,
                AbilityKind::Static(static_ability)
                    if static_ability.id() == crate::static_abilities::StaticAbilityId::Convoke
            )
        }) {
        format!("Convoke{TYPED_NO_PERIOD_SENTINEL}")
    } else {
        line.to_string()
    };
    let protected_without_period = protected.trim_end_matches('.');
    fn puts_stun_counter(effect: &Effect) -> bool {
        if effect
            .downcast_ref::<crate::effects::PutCountersEffect>()
            .is_some_and(|put| put.counter_type == crate::object::CounterType::Stun)
        {
            return true;
        }
        let mut found = false;
        effect.visit_child_effects(&mut |child| {
            if !found {
                found = puts_stun_counter(child);
            }
        });
        found
    }
    let typed_condition_qualified_stun = def.abilities.iter().any(|ability| {
        matches!(
            &ability.kind,
            AbilityKind::Triggered(triggered)
                if triggered
                    .trigger
                    .downcast_ref::<crate::triggers::ConditionQualifiedTrigger>()
                    .is_some()
                    && triggered.effects.all_effects().into_iter().any(puts_stun_counter)
        )
    });
    if protected_without_period.contains("put a stun counter on it")
        && !protected_without_period.contains(STANDARD_STUN_COUNTER_REMINDER)
        && typed_condition_qualified_stun
    {
        return format!(
            "{protected_without_period}. {STANDARD_REMINDER_OPEN_SENTINEL}{}{STANDARD_REMINDER_CLOSE_SENTINEL}",
            STANDARD_STUN_COUNTER_REMINDER
                .trim_start_matches('(')
                .trim_end_matches(')')
        );
    }
    if (protected_without_period == "Read ahead"
        || protected_without_period.ends_with(": Read ahead"))
        && def.abilities.iter().any(|ability| {
            matches!(
                &ability.kind,
                AbilityKind::Static(static_ability)
                    if static_ability.id() == crate::static_abilities::StaticAbilityId::ReadAhead
                        || static_ability
                            .display()
                            .trim_end_matches('.')
                            .eq_ignore_ascii_case("read ahead")
            )
        })
    {
        return format!(
            "{protected_without_period} {STANDARD_REMINDER_OPEN_SENTINEL}{}{STANDARD_REMINDER_CLOSE_SENTINEL}",
            STANDARD_READ_AHEAD_REMINDER
                .trim_start_matches('(')
                .trim_end_matches(')')
        );
    }
    if (protected_without_period == "Menace"
        || protected_without_period.ends_with(": Menace")
        || protected_without_period.ends_with(" — Menace"))
        && def.abilities.iter().any(|ability| {
            matches!(
                &ability.kind,
                AbilityKind::Static(static_ability)
                    if static_ability.id()
                        == crate::static_abilities::StaticAbilityId::Menace
                        && static_ability
                            .display()
                            .trim()
                            .trim_end_matches('.')
                            .eq_ignore_ascii_case(
                                STANDARD_MENACE_REMINDER.trim_end_matches('.')
                            )
            )
        })
    {
        return format!(
            "{protected_without_period} {STANDARD_REMINDER_OPEN_SENTINEL}{}{STANDARD_REMINDER_CLOSE_SENTINEL}",
            STANDARD_MENACE_REMINDER
                .strip_prefix("Menace (")
                .and_then(|reminder| reminder.strip_suffix(')'))
                .unwrap_or(STANDARD_MENACE_REMINDER)
        );
    }
    let preserves_controlled_attachments = def.abilities.iter().any(|ability| {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return false;
        };
        matches!(
            static_ability.compiled_model().map(|model| &model.payload),
            Some(ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(grant))
                if grant.protection_does_not_remove_controlled_attachments
        )
    });
    if preserves_controlled_attachments {
        protected = protected.replace("auras and equipment", "Auras and Equipment");
    }
    if def.abilities.iter().any(|ability| {
        matches!(
            &ability.kind,
            AbilityKind::Triggered(triggered)
                if super::render_effects::describe_structural_mentor_keyword(triggered)
                    .as_deref()
                    == Some(STANDARD_MENTOR_REMINDER)
        )
    }) {
        let encoded = STANDARD_MENTOR_REMINDER
            .replace('(', STANDARD_REMINDER_OPEN_SENTINEL)
            .replace(')', STANDARD_REMINDER_CLOSE_SENTINEL);
        protected = protected.replace(STANDARD_MENTOR_REMINDER, &encoded);
    }
    if protected.trim_end_matches('.') == "Training"
        && def.abilities.iter().any(|ability| {
            describe_structural_training_keyword(ability).as_deref()
                == Some(STANDARD_TRAINING_REMINDER)
                || describe_keyword_ability(ability).as_deref() == Some(STANDARD_TRAINING_REMINDER)
        })
    {
        return STANDARD_TRAINING_REMINDER
            .replace('(', STANDARD_REMINDER_OPEN_SENTINEL)
            .replace(')', STANDARD_REMINDER_CLOSE_SENTINEL);
    }
    if protected.contains(STANDARD_SPLIT_SECOND_REMINDER)
        && def.abilities.iter().any(|ability| {
            matches!(
                &ability.kind,
                AbilityKind::Static(static_ability)
                    if static_ability.display().contains(STANDARD_SPLIT_SECOND_REMINDER)
            )
        })
    {
        let encoded = STANDARD_SPLIT_SECOND_REMINDER
            .replace('(', STANDARD_REMINDER_OPEN_SENTINEL)
            .replace(')', STANDARD_REMINDER_CLOSE_SENTINEL);
        protected = protected.replace(STANDARD_SPLIT_SECOND_REMINDER, &encoded);
    }
    let has_damage_history_death_watcher = def.spell_effect.as_ref().is_some_and(|program| {
        program.all_effects().into_iter().any(|effect| {
            effect
                .downcast_ref::<crate::effects::ScheduleDelayedTriggerEffect>()
                .and_then(|schedule| {
                    schedule
                        .trigger
                        .downcast_ref::<crate::triggers::ZoneChangeTrigger>()
                })
                .is_some_and(|dies| {
                    matches!(
                        dies.from,
                        crate::triggers::zone_changes::ZonePattern::Specific(Zone::Battlefield)
                    ) && matches!(
                        dies.to,
                        crate::triggers::zone_changes::ZonePattern::Specific(Zone::Graveyard)
                    ) && dies.object_filter.dealt_damage_by_source_this_turn
                        == Some(ironsmith_core::DamagedBySource::ThisCreature)
                })
        })
    });
    if protected.contains("gains deathtouch until end of turn")
        && !protected.contains(STANDARD_DEATHTOUCH_REMINDER)
        && has_damage_history_death_watcher
    {
        let encoded = STANDARD_DEATHTOUCH_REMINDER
            .replace('(', STANDARD_REMINDER_OPEN_SENTINEL)
            .replace(')', STANDARD_REMINDER_CLOSE_SENTINEL);
        protected = format!("{}. {encoded}", protected.trim_end_matches('.'));
    }
    let has_open_attraction = def.abilities.iter().any(|ability| {
        matches!(
            &ability.kind,
            AbilityKind::Triggered(triggered)
                if triggered.effects.all_effects().into_iter().any(|effect| {
                    effect
                        .downcast_ref::<crate::effects::OpenAttractionEffect>()
                        .is_some()
                })
        )
    });
    if protected.contains(STANDARD_OPEN_ATTRACTION_REMINDER) && has_open_attraction {
        let encoded = STANDARD_OPEN_ATTRACTION_REMINDER
            .replace('(', STANDARD_REMINDER_OPEN_SENTINEL)
            .replace(')', STANDARD_REMINDER_CLOSE_SENTINEL);
        protected = protected.replace(STANDARD_OPEN_ATTRACTION_REMINDER, &encoded);
    }
    if !protected.contains("counts toward your devotion")
        && let Some(color) = def
            .abilities
            .iter()
            .find_map(|ability| match &ability.kind {
                AbilityKind::Triggered(triggered) => triggered_search_devotion_color(triggered),
                _ => None,
            })
        && protected.contains(&format!("your devotion to {}", color.name()))
    {
        protected = format!(
            "{}. {STANDARD_REMINDER_OPEN_SENTINEL}Each {} in the mana costs of permanents you control counts toward your devotion to {}.{STANDARD_REMINDER_CLOSE_SENTINEL}",
            protected.trim_end_matches('.'),
            describe_mana_symbol(ManaSymbol::from_color(color)),
            color.name(),
        );
    }
    let lower = protected.to_ascii_lowercase();
    if lower.contains(" mills ")
        && lower.contains("for each creature card put into a graveyard this way")
        && !lower.contains("to mill a card")
    {
        protected = format!(
            "{}. {STANDARD_REMINDER_OPEN_SENTINEL}To mill a card, a player puts the top card of their library into their graveyard.{STANDARD_REMINDER_CLOSE_SENTINEL}",
            protected.trim_end_matches('.'),
        );
    }
    for ability in &def.abilities {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            continue;
        };
        if static_ability.id() == crate::static_abilities::StaticAbilityId::Menace
            && static_ability.authored_line_surface().as_deref() == Some(STANDARD_MENACE_REMINDER)
        {
            let encoded = STANDARD_MENACE_REMINDER
                .replace('(', STANDARD_REMINDER_OPEN_SENTINEL)
                .replace(')', STANDARD_REMINDER_CLOSE_SENTINEL);
            protected = protected.replace(STANDARD_MENACE_REMINDER, &encoded);
            continue;
        }
        if static_ability.id() == crate::static_abilities::StaticAbilityId::Flanking
            && static_ability
                .compiled_model()
                .is_some_and(|model| model.label == STANDARD_FLANKING_REMINDER)
        {
            let encoded = STANDARD_FLANKING_REMINDER
                .replace('(', STANDARD_REMINDER_OPEN_SENTINEL)
                .replace(')', STANDARD_REMINDER_CLOSE_SENTINEL);
            protected = protected.replace(STANDARD_FLANKING_REMINDER, &encoded);
            continue;
        }
        if static_ability.id() != crate::static_abilities::StaticAbilityId::Landwalk
            || !matches!(
                static_ability.landwalk_kind(),
                Some(crate::static_abilities::LandwalkKind::Subtype { subtype, .. })
                    if subtype.is_basic_land_type()
            )
        {
            continue;
        }
        let Some(keyword) = describe_keyword_ability(ability) else {
            continue;
        };
        let encoded = keyword
            .replace('(', STANDARD_REMINDER_OPEN_SENTINEL)
            .replace(')', STANDARD_REMINDER_CLOSE_SENTINEL);
        protected = protected.replace(&keyword, &encoded);
    }
    protected
}

/// Restore standard rules reminders after every scored-text normalization
/// pass. The raw renderer protects parentheticals with sentinels, but some
/// correlated programs are compacted only after individual source lines have
/// already passed that boundary. Re-prove those reminders from the final
/// typed definition and the exact normalized surface instead of trusting
/// arbitrary authored parenthetical text.
pub(super) fn append_typed_standard_reminder_lines(
    def: &CardDefinition,
    mut lines: Vec<String>,
) -> Vec<String> {
    const TRAINING: &str = "Training (Whenever this creature attacks with another creature with greater power, put a +1/+1 counter on this creature.)";
    let has_typed_training = def.abilities.iter().any(|ability| {
        describe_structural_training_keyword(ability).is_some()
            || describe_keyword_ability(ability).as_deref() == Some("Training")
    });
    if has_typed_training {
        for line in &mut lines {
            if line.trim_end_matches('.') == "Training" {
                *line = TRAINING.to_string();
            }
        }
    }

    // Class cards carry a standard rules reminder before their level lines,
    // and the level cost labels themselves are headings rather than
    // sentences. Recover both surfaces at the final typed boundary so line
    // punctuation normalization cannot erase them.
    if def.card.subtypes.contains(&Subtype::Class) {
        const CLASS_REMINDER: &str = "(Gain the next level as a sorcery to add its ability.)";
        if !lines.iter().any(|line| line == CLASS_REMINDER) {
            lines.insert(0, CLASS_REMINDER.to_string());
        }
        for line in &mut lines {
            if line.contains(": Level ") && line.ends_with('.') {
                line.pop();
            }
        }
    }

    for line in &mut lines {
        if line.contains("Up to one target creature gains reach and vigilance until your next turn")
        {
            *line = line.replace(
                "Up to one target creature gains reach and vigilance until your next turn",
                "Until your next turn, up to one target creature gains reach and vigilance",
            );
        }
    }

    let devotion_color = def
        .abilities
        .iter()
        .find_map(|ability| match &ability.kind {
            AbilityKind::Triggered(triggered) => triggered_search_devotion_color(triggered),
            _ => None,
        });
    if let Some(color) = devotion_color {
        let needle = format!("your devotion to {}", color.name());
        if let Some(line) = lines.iter_mut().find(|line| line.contains(&needle))
            && !line.contains("counts toward your devotion")
        {
            *line = format!(
                "{}. (Each {} in the mana costs of permanents you control counts toward your devotion to {}.)",
                line.trim_end_matches('.'),
                describe_mana_symbol(ManaSymbol::from_color(color)),
                color.name(),
            );
        }
    }

    fn contains_mill(effect: &Effect) -> bool {
        if effect
            .downcast_ref::<crate::effects::MillEffect>()
            .is_some()
        {
            return true;
        }
        let mut found = false;
        effect.visit_child_effects(&mut |child| found |= contains_mill(child));
        found
    }
    let has_typed_mill = def.spell_effect.as_ref().is_some_and(|program| {
        program.segments.iter().any(|segment| {
            segment.default_effects.iter().any(contains_mill)
                || segment
                    .self_replacements
                    .iter()
                    .any(|branch| branch.replacement_effects.iter().any(contains_mill))
        })
    });
    if has_typed_mill
        && lines
            .iter()
            .any(|line| line.contains("For each creature card put into a graveyard this way"))
        && !lines.iter().any(|line| line.contains("To mill a card"))
        && let Some(line) = lines
            .iter_mut()
            .rfind(|line| line.contains("put into a graveyard this way"))
    {
        *line = format!(
            "{}. (To mill a card, a player puts the top card of their library into their graveyard.)",
            line.trim_end_matches('.'),
        );
    }

    lines
}

/// Attachment color choices overwrite the source's stored chosen color each
/// time the source becomes attached. Oracle calls that state the "last chosen
/// color"; the executable protection payload deliberately uses the same
/// generic chosen-color value as other color-choice abilities.
fn rewrite_attached_chosen_color_reference(def: &CardDefinition, line: &str) -> String {
    let chooses_color_on_attachment = def.abilities.iter().any(|ability| {
        matches!(
            &ability.kind,
            AbilityKind::Static(static_ability)
                if static_ability.id()
                    == crate::static_abilities::StaticAbilityId::ChooseColorAsBecomesAttached
        )
    });
    if !chooses_color_on_attachment {
        return line.to_string();
    }
    line.replace(
        "protection from the chosen color",
        "protection from the last chosen color",
    )
}

fn rewrite_source_owner_reference(def: &CardDefinition, line: &str) -> String {
    let Some(primary_type) = def.card.card_types.first() else {
        return line.to_string();
    };
    line.replace(
        "this source's owner",
        &format!("this {}'s owner", primary_type.name().to_ascii_lowercase()),
    )
}

fn rewrite_self_exile_cost_source(def: &CardDefinition, line: &str) -> String {
    if line.contains("Exile this card from your graveyard")
        || line.contains("exile this card from your graveyard")
    {
        return line.to_string();
    }
    let Some(primary_type) = def.card.card_types.first() else {
        return line.to_string();
    };
    if *primary_type == CardType::Land {
        return line.to_string();
    }
    let source_phrase = format!("Exile this {}", primary_type.name().to_ascii_lowercase());
    let Some((cost_surface, ability_surface)) = line.split_once(':') else {
        return line.to_string();
    };
    if !cost_surface.contains(&source_phrase) {
        return line.to_string();
    }
    format!(
        "{}:{ability_surface}",
        cost_surface.replace(&source_phrase, &format!("Exile {}", def.card.name))
    )
}

fn rewrite_named_source_counter_reference(def: &CardDefinition, line: &str) -> String {
    if !def.card.name.starts_with("The ") {
        return line.to_string();
    }
    let Some(primary_type) = def.card.card_types.first() else {
        return line.to_string();
    };
    let source_type = primary_type.name().to_ascii_lowercase();
    let mut rewritten = line.to_string();
    for counter_noun in ["counter", "counters"] {
        let pattern = format!("{counter_noun} on this {source_type}");
        let replacement = format!("{counter_noun} on {}", def.card.name);
        rewritten = rewritten.replace(&pattern, &replacement);
    }
    rewritten
}

fn render_labeled_static_body(
    ability: &crate::static_abilities::StaticAbility,
    subject: &str,
) -> String {
    if ability.is_keyword() {
        let keyword = ability
            .display()
            .trim()
            .trim_end_matches('.')
            .to_ascii_lowercase();
        let verb = if subject_text_uses_have(subject) {
            "have"
        } else {
            "has"
        };
        return format!("{} {verb} {keyword}", capitalize_first(subject));
    }
    capitalize_first(
        describe_static_ability_with_subject(ability, subject)
            .trim()
            .trim_end_matches('.'),
    )
}

fn render_labeled_static_predicates(predicates: &[String]) -> String {
    match predicates {
        [] => String::new(),
        [only] => only.clone(),
        [left, right] => format!("{left} and {right}"),
        _ => {
            let (last, leading) = predicates.split_last().expect("nonempty predicates");
            format!("{}, and {last}", leading.join(", "))
        }
    }
}

fn station_threshold_from_static_label(label: &str, condition: &Condition) -> Option<i32> {
    let threshold = label
        .strip_prefix(ironsmith_core::static_ability_model::STATION_THRESHOLD_STATIC_LABEL_PREFIX)?
        .parse::<i32>()
        .ok()?;
    let Condition::ValueComparison {
        left,
        operator: ironsmith_core::ValueComparisonOperator::GreaterThanOrEqual,
        right,
    } = condition
    else {
        return None;
    };
    matches!(
        left.unhinted(),
        Value::CountersOnSource(CounterType::Charge)
    )
    .then_some(())?;
    matches!(right.unhinted(), Value::Fixed(found) if *found == threshold).then_some(threshold)
}

fn station_threshold_keyword_surface(
    ability: &crate::static_abilities::StaticAbility,
) -> Option<String> {
    ability.is_keyword().then(|| {
        ability
            .display()
            .trim()
            .trim_end_matches('.')
            .to_ascii_lowercase()
    })
}

/// Coalesce adjacent static abilities that came from the same ability-word
/// wrapper. Equality is structural: both the retained label and the typed
/// runtime condition must match.
fn describe_labeled_static_bundle(abilities: &[Ability], subject: &str) -> Option<(String, usize)> {
    let AbilityKind::Static(first) = &abilities.first()?.kind else {
        return None;
    };
    let (label, first_inner, condition) = first.labeled_static_condition()?;
    let normalized_label = label.trim().trim_end_matches('.').to_ascii_lowercase();
    if first_inner.id() == crate::static_abilities::StaticAbilityId::Flash
        && normalized_label.contains("you may cast this spell as though it had flash")
    {
        // Some conditional spell-keyword clauses retain their entire authored
        // sentence as the label while the typed inner ability carries the
        // executable Flash permission. That text is the complete surface, not
        // an ability-word prefix to be joined to "this creature has flash".
        let mut authored = label.trim().trim_end_matches('.').to_string();
        if let Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::MatchingFilter(filter),
            ..
        } = &condition
            && let [subtype] = filter.subtypes.as_slice()
        {
            let canonical = subtype.to_string();
            authored = authored.replace(
                &format!("{} card", canonical.to_ascii_lowercase()),
                &format!("{canonical} card"),
            );
        }
        return Some((capitalize_first(&authored), 1));
    }
    let explicit_label = label.strip_prefix(
        ironsmith_core::static_ability_model::EXPLICIT_STATIC_PRESENTATION_LABEL_PREFIX,
    );
    if let Some(label) = explicit_label
        && !label.trim().eq_ignore_ascii_case("Max speed")
    {
        // Unlike ability words such as "Max speed", this label precedes an
        // authored condition that must remain visible in Oracle text.
        let conditioned = first_inner.with_condition(condition).unwrap_or(first_inner);
        return Some((
            format!(
                "{label} — {}",
                render_labeled_static_body(&conditioned, subject)
            ),
            1,
        ));
    }
    let station_label = label
        .starts_with(ironsmith_core::static_ability_model::STATION_THRESHOLD_STATIC_LABEL_PREFIX);
    let station_threshold = if station_label {
        Some(station_threshold_from_static_label(&label, &condition)?)
    } else {
        None
    };
    let mut bodies = vec![render_labeled_static_body(&first_inner, subject)];
    let mut station_keywords = station_threshold
        .and_then(|_| station_threshold_keyword_surface(&first_inner).map(|keyword| vec![keyword]));
    let mut consumed = 1usize;

    while let Some(Ability {
        kind: AbilityKind::Static(next),
        ..
    }) = abilities.get(consumed)
    {
        let Some((next_label, next_inner, next_condition)) = next.labeled_static_condition() else {
            break;
        };
        if next_label != label || next_condition != condition {
            break;
        }
        if station_keywords.is_some() {
            if let Some(keyword) = station_threshold_keyword_surface(&next_inner) {
                station_keywords
                    .as_mut()
                    .expect("checked station keyword group")
                    .push(keyword);
            } else {
                station_keywords = None;
            }
        }
        bodies.push(render_labeled_static_body(&next_inner, subject));
        consumed += 1;
    }

    let subject = capitalize_first(subject);
    let subject_prefix = format!("{subject} ");
    let predicates = bodies
        .iter()
        .map(|body| body.strip_prefix(&subject_prefix).map(str::to_string))
        .collect::<Option<Vec<_>>>();
    let body = if let Some(predicates) = predicates {
        if predicates
            .iter()
            .all(|predicate| predicate.starts_with("has "))
        {
            let keywords = predicates
                .iter()
                .map(|predicate| predicate.trim_start_matches("has ").to_string())
                .collect::<Vec<_>>();
            format!(
                "{subject} has {}",
                render_labeled_static_predicates(&keywords)
            )
        } else {
            format!(
                "{subject} {}",
                render_labeled_static_predicates(&predicates)
            )
        }
    } else {
        render_labeled_static_predicates(&bodies)
    };

    if let Some(threshold) = station_threshold {
        let body = station_keywords
            .map(|keywords| capitalize_first(&keywords.join(", ")))
            .unwrap_or(body);
        return Some((format!("{threshold}+ | {body}"), consumed));
    }

    let display_label = explicit_label.unwrap_or(&label);
    Some((format!("{display_label} — {body}"), consumed))
}

const SOURCE_LINE_KEYWORD_GROUP_SENTINEL: &str = "\0ironsmith:source-line-keyword-group:";

fn source_line_keyword_group_count(ability: &Ability) -> Option<usize> {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::SourceLineKeywordGroup { keyword_count } =
        &model.payload
    else {
        return None;
    };
    Some(*keyword_count)
}

fn source_line_repeated_cascade_group_consumed(abilities: &[Ability]) -> Option<usize> {
    let [marker, first, second, ..] = abilities else {
        return None;
    };
    if source_line_keyword_group_count(marker) != Some(2) {
        return None;
    }
    for ability in [first, second] {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        if ability.functional_zones.as_slice() != [Zone::Stack]
            || static_ability.id() != crate::static_abilities::StaticAbilityId::Cascade
        {
            return None;
        }
    }
    Some(3)
}

fn source_line_static_group_count(ability: &Ability) -> Option<usize> {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::SourceLineStaticGroup { member_count } =
        &model.payload
    else {
        return None;
    };
    Some(*member_count)
}

fn source_line_static_group_presentation_label(ability: &Ability) -> Option<&str> {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    static_ability.compiled_model()?.label.strip_prefix(
        ironsmith_core::static_ability_model::EXPLICIT_STATIC_PRESENTATION_LABEL_PREFIX,
    )
}

fn source_line_keyword_group_sentinel(keyword_count: usize) -> String {
    format!("{SOURCE_LINE_KEYWORD_GROUP_SENTINEL}{keyword_count}")
}

fn source_line_keyword_group_sentinel_count(line: &str) -> Option<usize> {
    line.strip_prefix(SOURCE_LINE_KEYWORD_GROUP_SENTINEL)?
        .parse()
        .ok()
}

fn merge_adjacent_keyword_surface_lines(lines: Vec<String>) -> Vec<String> {
    let mut out = Vec::new();
    let mut idx = 0usize;
    while idx < lines.len() {
        if let Some(keyword_count) = source_line_keyword_group_sentinel_count(&lines[idx]) {
            let mut prefix = None;
            let mut keywords = Vec::with_capacity(keyword_count);
            for offset in 0..keyword_count {
                let Some(line) = lines.get(idx + 1 + offset) else {
                    keywords.clear();
                    break;
                };
                if let Some((line_prefix, keyword)) = split_intrinsic_keyword_line(line) {
                    prefix.get_or_insert(line_prefix);
                    keywords.push(keyword.to_string());
                } else if offset + 1 == keyword_count
                    && let Some(keyword) = split_trailing_numbered_intrinsic_keyword_line(line)
                {
                    keywords.push(keyword.to_string());
                } else {
                    keywords.clear();
                    break;
                }
            }
            if keywords.len() == keyword_count
                && let Some(prefix) = prefix
            {
                out.push(format!(
                    "{prefix}{}",
                    render_intrinsic_keyword_list(&keywords, true)
                ));
                idx += keyword_count + 1;
                continue;
            }

            // The marker is presentation-only. If a structural keyword did
            // not render as one recognizable surface, discard only the marker
            // and preserve every following ability line unchanged.
            idx += 1;
            continue;
        }

        if let Some((prefix, keyword)) = split_intrinsic_keyword_line(&lines[idx]) {
            let mut keywords = vec![keyword.to_string()];
            let mut consumed = 1usize;
            while let Some(next) = lines.get(idx + consumed) {
                let Some((_, next_keyword)) = split_intrinsic_keyword_line(next) else {
                    break;
                };
                keywords.push(next_keyword.to_string());
                consumed += 1;
            }
            if let Some(next) = lines.get(idx + consumed)
                && let Some(next_keyword) = split_trailing_numbered_intrinsic_keyword_line(next)
            {
                keywords.push(next_keyword.to_string());
                consumed += 1;
            }
            if should_compact_numbered_intrinsic_keywords(&keywords) {
                out.push(format!(
                    "{prefix}{}",
                    render_intrinsic_keyword_list(&keywords, true)
                ));
                idx += consumed;
                continue;
            }
        }

        if let Some(cost) = split_kicker_cost_line(&lines[idx]) {
            let mut costs = vec![cost.to_string()];
            let mut consumed = 1usize;
            while let Some(next) = lines.get(idx + consumed) {
                let Some(next_cost) = split_kicker_cost_line(next) else {
                    break;
                };
                costs.push(next_cost.to_string());
                consumed += 1;
            }
            if consumed > 1 {
                out.push(format!("Kicker {}", costs.join(" and/or ")));
                idx += consumed;
                continue;
            }
        }

        if let Some(keyword) = split_bare_keyword_line(&lines[idx]) {
            let mut keywords = vec![keyword.to_string()];
            let mut consumed = 1usize;
            while let Some(next) = lines.get(idx + consumed) {
                let Some(next_keyword) = split_bare_keyword_line(next) else {
                    break;
                };
                keywords.push(next_keyword.to_string());
                consumed += 1;
            }
            if consumed > 1 {
                out.push(render_bare_keyword_list(&keywords));
                idx += consumed;
                continue;
            }
        }

        if let Some((label, subject, verb, keyword)) =
            split_static_granted_keyword_line(&lines[idx])
        {
            if let Some((label, color, base_subject, keyword)) =
                split_color_static_granted_keyword_line(&lines[idx])
            {
                let mut clauses = vec![(keyword.to_string(), color)];
                let mut consumed = 1usize;
                while let Some(next) = lines.get(idx + consumed) {
                    let Some((_, next_color, next_base_subject, next_keyword)) =
                        split_color_static_granted_keyword_line(next)
                    else {
                        break;
                    };
                    if next_base_subject != base_subject {
                        break;
                    }
                    clauses.push((next_keyword.to_string(), next_color));
                    consumed += 1;
                }
                // This compact conditional surface represents the single
                // five-arm WUBRG clause used by cards such as Scion of Draco.
                // A shorter adjacent run can instead be independent authored
                // source lines (for example, Righteous War), so preserve those
                // line boundaries rather than inventing one conditional.
                if clauses
                    .iter()
                    .map(|(_, color)| *color)
                    .eq(["white", "blue", "black", "red", "green"])
                {
                    out.push(render_color_conditional_keyword_grants(
                        label,
                        base_subject,
                        &clauses,
                    ));
                    idx += consumed;
                    continue;
                }
            }

            let mut keywords = vec![keyword.to_string()];
            let mut consumed = 1usize;
            while let Some(next) = lines.get(idx + consumed) {
                let Some((_, next_subject, next_verb, next_keyword)) =
                    split_static_granted_keyword_line(next)
                else {
                    break;
                };
                if next_subject != subject || next_verb != verb {
                    break;
                }
                keywords.push(next_keyword.to_string());
                consumed += 1;
            }
            if consumed > 1 {
                out.push(format!(
                    "{label}{subject} {verb} {}",
                    render_keyword_list(&keywords, false)
                ));
                idx += consumed;
                continue;
            }
        }

        out.push(lines[idx].clone());
        idx += 1;
    }
    out
}

fn split_kicker_cost_line(line: &str) -> Option<&str> {
    let trimmed = line.trim().trim_end_matches('.');
    let cost = trimmed.strip_prefix("Kicker ")?.trim();
    (!cost.is_empty()).then_some(cost)
}

fn split_keyword_ability_line(line: &str) -> Option<(&str, &str)> {
    let (prefix, keyword) = line.split_once(": ")?;
    if !prefix.starts_with("Keyword ability ") || !is_mergeable_keyword_surface(keyword) {
        return None;
    }
    Some((&line[..prefix.len() + 2], keyword.trim_end_matches('.')))
}

fn split_bare_keyword_line(line: &str) -> Option<&str> {
    let keyword = line.trim().trim_end_matches('.');
    if keyword.contains(':') || !is_mergeable_keyword_surface(keyword) {
        return None;
    }
    Some(keyword)
}

fn split_static_intrinsic_keyword_line(line: &str) -> Option<(&str, &str)> {
    let (prefix, keyword) = line.split_once(": ")?;
    if !prefix.starts_with("Static ability ") || !is_mergeable_keyword_surface(keyword) {
        return None;
    }
    Some((&line[..prefix.len() + 2], keyword.trim_end_matches('.')))
}

fn split_intrinsic_keyword_line(line: &str) -> Option<(&str, &str)> {
    split_keyword_ability_line(line).or_else(|| split_static_intrinsic_keyword_line(line))
}

/// A numbered intrinsic can finish an ordinary keyword list even when it is
/// kept out of the general mergeable-keyword set. Keeping this trailing-only
/// avoids treating a standalone parameterized ability as a list anchor.
fn split_trailing_numbered_intrinsic_keyword_line(line: &str) -> Option<&str> {
    let (prefix, keyword) = line.split_once(": ")?;
    if !prefix.starts_with("Keyword ability ")
        && !prefix.starts_with("Static ability ")
        && !prefix.starts_with("Triggered ability ")
    {
        return None;
    }
    let keyword = keyword.trim_end_matches('.');
    let lower = keyword.to_ascii_lowercase();
    lower
        .strip_prefix("annihilator ")
        .or_else(|| lower.strip_prefix("firebending "))?
        .parse::<u32>()
        .ok()?;
    Some(keyword)
}

fn should_compact_numbered_intrinsic_keywords(keywords: &[String]) -> bool {
    if keywords.len() < 2 {
        return false;
    }
    // Cascade is frequently authored as its own physical ability line. An
    // actual inline keyword group carries the explicit source-line sentinel
    // handled above; without it, keep Cascade's boundary instead of merging a
    // neighboring Flash/Reach run merely because it has three members.
    if keywords.iter().any(|keyword| {
        keyword
            .trim_end_matches('.')
            .eq_ignore_ascii_case("cascade")
    }) {
        return false;
    }
    // A two-ability run is also the exact AST shape produced by adjacent
    // authored keyword lines (for example, Reach followed by Daybound), so
    // keep those top-level boundaries. Longer runs are conventional inline
    // keyword lists. A homogeneous protection run is one decomposed
    // protection surface ("from black and from red"), not separate lines.
    keywords.len() > 2
        || keywords.iter().all(|keyword| {
            keyword
                .trim_end_matches('.')
                .to_ascii_lowercase()
                .starts_with("protection from ")
        })
}

fn split_static_granted_keyword_line(line: &str) -> Option<(&str, &str, &str, &str)> {
    let (label, text) = line.split_once(": ")?;
    if !label.starts_with("Static ability ") {
        return None;
    }
    let keyword = text.trim_end_matches('.');
    let (subject, verb, keyword) = if let Some((subject, keyword)) = keyword.split_once(" have ") {
        (subject, "have", keyword)
    } else if let Some((subject, keyword)) = keyword.split_once(" has ") {
        (subject, "has", keyword)
    } else {
        return None;
    };
    if !is_mergeable_keyword_surface(keyword) {
        return None;
    }
    Some((&line[..label.len() + 2], subject, verb, keyword))
}

fn split_color_static_granted_keyword_line(line: &str) -> Option<(&str, &'static str, &str, &str)> {
    let (label, text) = line.split_once(": ")?;
    if !label.starts_with("Static ability ") {
        return None;
    }
    let text = text.trim_end_matches('.');
    let (subject, keyword) = text.split_once(" have ")?;
    if !is_mergeable_keyword_surface(keyword) {
        return None;
    }
    let (color, base_subject) = subject.split_once(' ')?;
    let color = match color.to_ascii_lowercase().as_str() {
        "white" => "white",
        "blue" => "blue",
        "black" => "black",
        "red" => "red",
        "green" => "green",
        _ => return None,
    };
    Some((&line[..label.len() + 2], color, base_subject, keyword))
}

fn render_color_conditional_keyword_grants(
    label: &str,
    base_subject: &str,
    clauses: &[(String, &'static str)],
) -> String {
    let clauses = clauses
        .iter()
        .map(|(keyword, color)| {
            format!(
                "{} if it's {color}",
                keyword.trim_end_matches('.').to_ascii_lowercase()
            )
        })
        .collect::<Vec<_>>();
    let subject = if base_subject.eq_ignore_ascii_case("creatures you control") {
        "Each creature you control has".to_string()
    } else {
        format!("{base_subject} have")
    };
    format!("{label}{subject} {}", join_english_list(&clauses))
}

fn ability_level_range_prefix(ability: &Ability) -> Option<String> {
    let AbilityKind::Activated(activated) = &ability.kind else {
        return None;
    };
    level_range_activation_prefix(activated)
}

fn ability_has_level_tiers(ability: &Ability) -> bool {
    matches!(
        &ability.kind,
        AbilityKind::Static(static_ability)
            if static_ability.level_abilities().is_some_and(|levels| !levels.is_empty())
    )
}

fn level_tier_header_range(line: &str) -> Option<String> {
    let (_, text) = line.split_once(": ")?;
    let range = text.trim().trim_end_matches('.');
    range.starts_with("Level ").then(|| range.to_string())
}

fn interleave_level_range_activations(
    lines: Vec<String>,
    abilities: &[Ability],
    subject: &str,
    rewrite_it_deals: bool,
) -> Vec<String> {
    let mut out = Vec::new();
    let mut active_range: Option<String> = None;
    for line in lines {
        if let Some(next_range) = level_tier_header_range(&line) {
            flush_level_range_activations(
                &mut out,
                active_range.as_deref(),
                abilities,
                subject,
                rewrite_it_deals,
            );
            active_range = Some(next_range);
        }
        out.push(line);
    }
    flush_level_range_activations(
        &mut out,
        active_range.as_deref(),
        abilities,
        subject,
        rewrite_it_deals,
    );
    out
}

fn flush_level_range_activations(
    out: &mut Vec<String>,
    range: Option<&str>,
    abilities: &[Ability],
    subject: &str,
    rewrite_it_deals: bool,
) {
    let Some(range) = range else {
        return;
    };
    let range_prefix = format!("{range}. ");
    for (idx, ability) in abilities.iter().enumerate() {
        if ability_level_range_prefix(ability).as_deref() != Some(range) {
            continue;
        }
        for line in describe_ability(idx + 1, ability, subject, rewrite_it_deals) {
            out.push(
                line.strip_prefix(&range_prefix)
                    .unwrap_or(line.as_str())
                    .to_string(),
            );
        }
    }
}

fn is_mergeable_keyword_surface(keyword: &str) -> bool {
    let keyword = keyword.trim_end_matches('.');
    let lower = keyword.to_ascii_lowercase();
    // Partner-with carries a structurally modeled card-name payload. Keep it
    // on its own line so the ordinary intrinsic-keyword list renderer does
    // not lowercase that payload or absorb a following keyword.
    let carries_named_partner = lower.starts_with("partner with ");
    let is_numbered_firebending = lower
        .strip_prefix("firebending ")
        .is_some_and(|amount| amount.parse::<u32>().is_ok());
    (is_keyword_phrase(keyword) && lower != "changeling" && !carries_named_partner)
        || lower == "cascade"
        || is_numbered_firebending
        || matches!(
            lower.as_str(),
            "flying"
                | "first strike"
                | "double strike"
                | "vigilance"
                | "trample"
                | "haste"
                | "lifelink"
                | "deathtouch"
                | "menace"
                | "reach"
                | "hexproof"
                | "indestructible"
                | "shroud"
                | "fear"
                | "soulbond"
                | "ward pay 3 life"
        )
}

fn render_keyword_list(keywords: &[String], capitalize: bool) -> String {
    render_keyword_list_with_separator(keywords, capitalize, KeywordListSeparator::EnglishAnd)
}

#[derive(Clone, Copy)]
enum KeywordListSeparator {
    Comma,
    EnglishAnd,
}

fn render_intrinsic_keyword_list(keywords: &[String], capitalize: bool) -> String {
    render_keyword_list_with_separator(keywords, capitalize, KeywordListSeparator::Comma)
}

fn render_keyword_list_with_separator(
    keywords: &[String],
    capitalize: bool,
    separator: KeywordListSeparator,
) -> String {
    let mut items = Vec::new();
    let mut protections = Vec::new();
    for keyword in keywords {
        let lower = keyword.trim_end_matches('.').to_ascii_lowercase();
        if let Some(from) = lower.strip_prefix("protection from ") {
            protections.push(from.to_string());
        } else {
            items.push(lower);
        }
    }
    if !protections.is_empty() {
        let protection = if protections.len() == 1 {
            format!("protection from {}", protections[0])
        } else {
            format!(
                "protection from {}",
                join_english_list(
                    &protections
                        .iter()
                        .enumerate()
                        .map(|(idx, value)| {
                            if idx == 0 {
                                value.clone()
                            } else {
                                format!("from {value}")
                            }
                        })
                        .collect::<Vec<_>>()
                )
            )
        };
        items.push(protection);
    }
    let rendered = match separator {
        KeywordListSeparator::Comma => items.join(", "),
        KeywordListSeparator::EnglishAnd => join_english_list(&items),
    };
    if capitalize {
        capitalize_first(&rendered)
    } else {
        rendered
    }
}

fn render_bare_keyword_list(keywords: &[String]) -> String {
    let mut items = Vec::new();
    for keyword in keywords {
        let lower = keyword.trim_end_matches('.').to_ascii_lowercase();
        if !items.iter().any(|existing| existing == &lower) {
            items.push(lower);
        }
    }
    capitalize_first(&items.join(", "))
}

fn join_english_list(items: &[String]) -> String {
    match items {
        [] => String::new(),
        [one] => one.clone(),
        [first, second] => format!("{first} and {second}"),
        _ => {
            let (last, rest) = items.split_last().expect("nonempty");
            format!("{}, and {}", rest.join(", "), last)
        }
    }
}

fn modeled_source_keyword_grant(
    ability: &Ability,
) -> Option<(crate::static_abilities::StaticAbilityId, String, &Condition)> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    if let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: granted,
        condition,
    } = &model.payload
    {
        let keyword = crate::static_abilities::StaticAbility::from_model(granted.as_ref().clone());
        if !keyword.is_keyword() {
            return None;
        }
        let keyword_text = keyword
            .display()
            .trim()
            .trim_end_matches('.')
            .to_ascii_lowercase();
        if !is_mergeable_keyword_surface(&keyword_text) {
            return None;
        }
        return Some((keyword.id(), keyword_text, condition));
    }
    let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) = &model.payload
    else {
        return None;
    };
    if grant.filter != ObjectFilter::source()
        || !grant.additional_abilities.is_empty()
        || grant.set_quantifier_surface.is_some()
        || grant.ability.functional_zones.as_slice() != [Zone::Battlefield]
    {
        return None;
    }
    let ironsmith_core::AbilityKind::Static(granted) = &grant.ability.kind else {
        return None;
    };
    let keyword = crate::static_abilities::StaticAbility::from_model(granted.clone());
    if !keyword.is_keyword() {
        return None;
    }
    let keyword_text = keyword
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    if !is_mergeable_keyword_surface(&keyword_text) {
        return None;
    }
    Some((keyword.id(), keyword_text, grant.condition.as_ref()?))
}

fn modeled_object_static_grant(
    ability: &Ability,
) -> Option<(
    &ObjectFilter,
    Option<&Condition>,
    crate::static_abilities::StaticAbility,
)> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) = &model.payload
    else {
        return None;
    };
    if !grant.additional_abilities.is_empty()
        || grant.set_quantifier_surface.is_some()
        || grant.ability.functional_zones.as_slice() != [Zone::Battlefield]
    {
        return None;
    }
    let ironsmith_core::AbilityKind::Static(granted) = &grant.ability.kind else {
        return None;
    };
    Some((
        &grant.filter,
        grant.condition.as_ref(),
        crate::static_abilities::StaticAbility::from_model(granted.clone()),
    ))
}

/// Read an unquantified object grant through either runtime grant payload.
/// Parser families use `GrantObjectAbilityForFilter` for executable object
/// grants and `GrantAbility` for modeled static rules, but adjacent authored
/// grants can legitimately contain one of each.
fn modeled_unquantified_object_static_grant(
    ability: &Ability,
) -> Option<(
    &ObjectFilter,
    Option<&Condition>,
    crate::static_abilities::StaticAbility,
)> {
    if let Some(grant) = modeled_object_static_grant(ability) {
        return Some(grant);
    }
    let (filter, condition, quantifier, granted) = modeled_filter_static_grant(ability)?;
    quantifier.is_none().then_some((filter, condition, granted))
}

fn modeled_filter_static_grant(
    ability: &Ability,
) -> Option<(
    &ObjectFilter,
    Option<&Condition>,
    Option<&ironsmith_core::SetQuantifierSurface>,
    crate::static_abilities::StaticAbility,
)> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::GrantAbility(grant) = &model.payload else {
        return None;
    };
    if grant.ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let ironsmith_core::AbilityKind::Static(granted) = &grant.ability.kind else {
        return None;
    };
    Some((
        &grant.filter,
        grant.condition.as_ref(),
        grant.set_quantifier_surface.as_ref(),
        crate::static_abilities::StaticAbility::from_model(granted.clone()),
    ))
}

fn is_can_block_additional_each_combat_rule(
    ability: &crate::static_abilities::StaticAbility,
) -> bool {
    ability.compiled_model().is_some_and(|model| {
        matches!(
            model.payload,
            ironsmith_core::StaticAbilityPayload::CanBlockAdditionalCreatureEachCombat(_)
        )
    })
}

/// Rejoin the two independently executable Cascade grants produced by an
/// authored `"Cascade, cascade."` clause.  The two abilities remain separate
/// in the model so the spell cascades twice; matching the complete affected
/// spell filter keeps cast-origin and mana-value restrictions correlated.
fn describe_structural_repeated_cascade_grant_pair(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [first, second, ..] = abilities else {
        return None;
    };
    let (first_filter, first_condition, first_granted) = modeled_object_static_grant(first)?;
    let (second_filter, second_condition, second_granted) = modeled_object_static_grant(second)?;
    if first_filter != second_filter
        || first_condition.is_some()
        || second_condition.is_some()
        || first_filter.stack_kind != Some(crate::filter::StackObjectKind::Spell)
        || first_granted.id() != crate::static_abilities::StaticAbilityId::Cascade
        || second_granted.id() != crate::static_abilities::StaticAbilityId::Cascade
    {
        return None;
    }

    let AbilityKind::Static(first_static) = &first.kind else {
        return None;
    };
    let AbilityKind::Static(second_static) = &second.kind else {
        return None;
    };
    let first_display = first_static.display();
    let second_display = second_static.display();
    let (subject, first_tail, verb) =
        split_static_predicate_with_verb(&first_display, &[" has ", " have "])?;
    let (second_subject, second_tail, second_verb) =
        split_static_predicate_with_verb(&second_display, &[" has ", " have "])?;
    if subject != second_subject
        || verb != second_verb
        || !first_tail.trim().eq_ignore_ascii_case("cascade")
        || !second_tail.trim().eq_ignore_ascii_case("cascade")
    {
        return None;
    }

    Some((
        capitalize_first(&format!("{subject} {verb} \"Cascade, cascade.\"")),
        2,
    ))
}

#[cfg(test)]
mod repeated_cascade_grant_tests {
    use super::*;

    fn cascade_grant(filter: ObjectFilter) -> Ability {
        let cascade: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::cascade();
        let granted = ironsmith_core::Ability {
            kind: ironsmith_core::AbilityKind::Static(cascade),
            functional_zones: vec![Zone::Battlefield],
        };
        let model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::new(ironsmith_core::GrantObjectAbilityForFilter::new(
                filter, granted, "Cascade",
            ));
        Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
    }

    #[test]
    fn compacts_only_equal_spell_filters_with_two_cascade_grants() {
        let mut from_hand = ObjectFilter::spell()
            .cast_by(PlayerFilter::You)
            .in_zone(Zone::Hand);
        from_hand.colorless = true;
        from_hand.mana_value = Some(crate::filter::Comparison::GreaterThanOrEqual(7));
        from_hand.has_mana_cost = true;

        let equal = [
            cascade_grant(from_hand.clone()),
            cascade_grant(from_hand.clone()),
        ];
        assert_eq!(
            describe_structural_repeated_cascade_grant_pair(&equal),
            Some((
                "Colorless spells you cast from your hand with mana value 7 or greater have \"Cascade, cascade.\""
                    .to_string(),
                2,
            ))
        );

        let mut from_exile = from_hand.clone();
        from_exile.zone = Some(Zone::Exile);
        let changed_origin = [cascade_grant(from_hand), cascade_grant(from_exile)];
        assert!(describe_structural_repeated_cascade_grant_pair(&changed_origin).is_none());

        let permanents = [
            cascade_grant(ObjectFilter::creature()),
            cascade_grant(ObjectFilter::creature()),
        ];
        assert!(describe_structural_repeated_cascade_grant_pair(&permanents).is_none());
    }
}

/// Render complete nonkeyword static abilities granted to the same filtered
/// object set as quoted ability text. The executable grants stay independent;
/// this only restores the single authored `have "..." and "..."` surface.
fn describe_structural_quoted_static_grant_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let first = abilities.first()?;
    let (filter, condition, set_quantifier, first_granted) = modeled_filter_static_grant(first)?;
    if condition.is_some()
        || first_granted.is_keyword()
        || first_granted.id()
            == crate::static_abilities::StaticAbilityId::CanAttackAsThoughNoDefender
        || is_can_block_additional_each_combat_rule(&first_granted)
    {
        return None;
    }
    let AbilityKind::Static(first_static) = &first.kind else {
        return None;
    };
    let first_display = first_static.display();
    let (subject, _, verb) =
        split_static_predicate_with_verb(&first_display, &[" has ", " have "])?;
    let self_subject = granted_ability_self_subject_for_filter(filter);

    let mut quoted = Vec::new();
    let mut consumed = 0usize;
    for ability in abilities {
        let Some((next_filter, next_condition, next_quantifier, granted)) =
            modeled_filter_static_grant(ability)
        else {
            break;
        };
        if next_filter != filter
            || next_condition != condition
            || next_quantifier != set_quantifier
            || granted.is_keyword()
            || granted.id() == crate::static_abilities::StaticAbilityId::CanAttackAsThoughNoDefender
            || is_can_block_additional_each_combat_rule(&granted)
        {
            break;
        }
        let AbilityKind::Static(next_static) = &ability.kind else {
            break;
        };
        let next_display = next_static.display();
        let Some((next_subject, _, next_verb)) =
            split_static_predicate_with_verb(&next_display, &[" has ", " have "])
        else {
            break;
        };
        if next_subject != subject || next_verb != verb {
            break;
        }

        let rendered = describe_static_ability_with_subject(&granted, self_subject);
        let rendered = rendered.trim().trim_end_matches('.');
        if rendered.is_empty() {
            break;
        }
        quoted.push(rendered.to_string());
        consumed += 1;
    }
    if quoted.is_empty() {
        return None;
    }

    let last = quoted.len().saturating_sub(1);
    let quoted = quoted
        .into_iter()
        .enumerate()
        .map(|(idx, mut ability)| {
            if idx == last && !ability.ends_with(['!', '?']) {
                ability.push('.');
            }
            format!("\"{ability}\"")
        })
        .collect::<Vec<_>>();
    Some((
        format!("{subject} {verb} {}", join_english_list(&quoted)),
        consumed,
    ))
}

#[derive(Debug)]
enum AttachedGrantSurface {
    Keyword(String),
    Quoted(String),
}

fn capitalize_quoted_grant_instruction(text: &str) -> String {
    if let Some((cost, instruction)) = text.split_once(": ") {
        format!("{cost}: {}", capitalize_first(instruction))
    } else {
        capitalize_first(text)
    }
}

fn modeled_attached_grant_surface(ability: &Ability) -> Option<(String, AttachedGrantSurface)> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let (authored_tail, granted) = match &model.payload {
        ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant)
            if grant.condition.is_none()
                && grant.additional_abilities.is_empty()
                && grant.set_quantifier_surface.is_none() =>
        {
            (
                grant.display.as_str(),
                crate::static_abilities::StaticAbilityModelInterpreter::ability_from_model(
                    &grant.ability,
                ),
            )
        }
        ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(grant)
            if grant.condition.is_none() && grant.additional_abilities.is_empty() =>
        {
            let display = grant.display.trim().trim_end_matches('.');
            let (_, tail, _) = split_static_predicate_with_verb(display, &[" has ", " have "])?;
            (
                tail,
                crate::static_abilities::StaticAbilityModelInterpreter::ability_from_model(
                    &grant.ability,
                ),
            )
        }
        _ => return None,
    };

    let display = static_ability.display();
    let display = display.trim().trim_end_matches('.');
    let (subject, _, _) = split_static_predicate_with_verb(display, &[" has ", " have "])?;
    let normalized_subject = subject.to_ascii_lowercase();
    if !matches!(
        normalized_subject.as_str(),
        "enchanted creature" | "equipped creature"
    ) {
        return None;
    }

    let surface = match &granted.kind {
        AbilityKind::Static(granted_static) if granted_static.is_keyword() => {
            AttachedGrantSurface::Keyword(
                granted_static
                    .display()
                    .trim()
                    .trim_end_matches('.')
                    .to_ascii_lowercase(),
            )
        }
        AbilityKind::Triggered(triggered) if describe_annihilator_keyword(triggered).is_some() => {
            AttachedGrantSurface::Keyword(lowercase_first(&describe_annihilator_keyword(
                triggered,
            )?))
        }
        AbilityKind::Activated(_) | AbilityKind::Triggered(_) | AbilityKind::Static(_) => {
            let tail = authored_tail
                .strip_prefix('"')
                .and_then(|body| body.strip_suffix('"'))
                .unwrap_or(authored_tail)
                .trim()
                .trim_end_matches('.');
            if tail.is_empty() {
                return None;
            }
            AttachedGrantSurface::Quoted(capitalize_quoted_grant_instruction(tail))
        }
    };
    Some((normalized_subject, surface))
}

fn render_attached_grant_surfaces(surfaces: &[AttachedGrantSurface]) -> Option<String> {
    if surfaces.is_empty()
        || !surfaces
            .iter()
            .any(|surface| matches!(surface, AttachedGrantSurface::Quoted(_)))
    {
        return None;
    }

    let last = surfaces.len() - 1;
    let mut rendered = Vec::with_capacity(surfaces.len());
    for (idx, surface) in surfaces.iter().enumerate() {
        let is_last = idx == last;
        let use_serial_comma = surfaces.len() > 2 && !is_last;
        let item = match surface {
            AttachedGrantSurface::Keyword(keyword) => {
                if use_serial_comma {
                    format!("{},", keyword.trim_end_matches([',', '.']))
                } else {
                    keyword.trim_end_matches([',', '.']).to_string()
                }
            }
            AttachedGrantSurface::Quoted(text) => {
                let text = text.trim_end_matches([',', '.']);
                let punctuation = if is_last {
                    "."
                } else if use_serial_comma {
                    ","
                } else {
                    ""
                };
                format!("\"{text}{punctuation}\"")
            }
        };
        rendered.push(item);
    }
    let final_item = rendered.pop()?;
    Some(if rendered.is_empty() {
        final_item
    } else {
        format!("{} and {final_item}", rendered.join(" "))
    })
}

/// Render authored attached-object grants from their typed payloads. Lowering
/// intentionally emits one continuous effect per granted ability, while a
/// mixed authored surface is one keyword/quoted-ability list.
fn describe_structural_attached_mixed_grant_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let (subject, first_surface) = modeled_attached_grant_surface(abilities.first()?)?;
    let mut surfaces = vec![first_surface];
    let mut consumed = 1usize;

    while let Some(ability) = abilities.get(consumed) {
        let Some((next_subject, next_surface)) = modeled_attached_grant_surface(ability) else {
            break;
        };
        if next_subject != subject {
            break;
        }
        surfaces.push(next_surface);
        consumed += 1;
    }

    Some((
        format!(
            "{} has {}",
            capitalize_first(&subject),
            render_attached_grant_surfaces(&surfaces)?
        ),
        consumed,
    ))
}

#[cfg(test)]
mod quoted_static_grant_bundle_tests {
    use super::*;

    #[test]
    fn complete_static_rules_granted_to_one_filter_render_as_quoted_abilities() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Static Grant Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(
            "Commander creatures you own have \"This creature enters with an additional +1/+1 counter on it\" and \"Other creatures you control enter with an additional +1/+1 counter on them.\"",
        )
        .expect("quoted static grant bundle should compile");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![
                "Commander creatures you own have \"This creature enters with an additional +1/+1 counter on it\" and \"Other creatures you control enter with an additional +1/+1 counter on them.\""
                    .to_string()
            ],
            "{definition:#?}",
        );
    }

    #[test]
    fn quoted_cost_reduction_retains_its_commander_creature_owner() {
        let text = "Commander creatures you own have \"The first Dragon spell you cast each turn costs {2} less to cast.\"";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Quoted Cost Grant Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(text)
        .expect("quoted commander cost reduction should compile");
        let debug = format!("{definition:#?}");

        assert!(debug.contains("GrantAbility(GrantAbility"), "{debug}");
        assert!(debug.contains("is_commander: true"), "{debug}");
        assert!(debug.contains("CostReduction"), "{debug}");
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![text.to_string()]
        );
    }

    #[test]
    fn keyword_grants_remain_unquoted() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Keyword Grant Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text("Creatures you control have flying.")
        .expect("keyword grant should compile");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec!["Creatures you control have flying.".to_string()]
        );
    }

    #[test]
    fn attached_mixed_keyword_and_quoted_grants_restore_one_authored_list() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Attached Grant Probe",
        )
        .card_types(vec![CardType::Artifact])
        .parse_text(
            "Equipped creature has reach, \"{2}, {T}: Tap target artifact or creature,\" and \"{8}, {T}: Discover 10.\"",
        )
        .expect("mixed attached grants should compile");
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![
                "Equipped creature has reach, \"{2}, {T}: Tap target artifact or creature,\" and \"{8}, {T}: Discover 10.\""
                    .to_string()
            ]
        );
    }
}

/// Render a granted blocking-capacity rule as the direct rule text it
/// represents instead of treating the rule as a keyword-like noun.
///
/// The generic grant display is intentionally phrased as
/// `"<objects> have <ability>"`, which is appropriate for keywords and quoted
/// abilities. `CanBlockAdditionalCreatureEachCombat` is a rules sentence,
/// though: its authored surface is `"<objects> can block ..."`. Keeping this
/// distinction typed also prevents a separately authored vigilance line from
/// being merged into the blocking rule as one synthetic trait list.
fn describe_structural_can_block_additional_grant(ability: &Ability) -> Option<String> {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let (condition, granted) =
        if let Some((_, condition, _, granted)) = modeled_filter_static_grant(ability) {
            (condition, granted)
        } else {
            let (_, condition, granted) = modeled_object_static_grant(ability)?;
            (condition, granted)
        };
    if condition.is_some() || !is_can_block_additional_each_combat_rule(&granted) {
        return None;
    }

    let display = static_ability.display();
    let display = display.trim().trim_end_matches('.');
    let (subject, _, _) = split_static_predicate_with_verb(display, &[" has ", " have "])?;
    let rule = granted.display();
    let rule = rule.trim().trim_end_matches('.');
    Some(format!("{subject} {}", lowercase_first(rule)))
}

#[cfg(test)]
mod can_block_additional_grant_tests {
    use super::*;

    #[test]
    fn both_typed_grant_families_render_block_capacity_as_a_direct_rule() {
        let filter = ObjectFilter::creature().you_control();
        let blocking_model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility {
                id: Some(
                    crate::static_abilities::StaticAbilityId::CanBlockAdditionalCreatureEachCombat,
                ),
                label: "can block additional creature".to_string(),
                payload: ironsmith_core::StaticAbilityPayload::CanBlockAdditionalCreatureEachCombat(
                    1,
                ),
            };
        let granted = ironsmith_core::Ability {
            kind: ironsmith_core::AbilityKind::Static(blocking_model),
            functional_zones: vec![Zone::Battlefield],
        };
        let grant_ability_model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::new(ironsmith_core::GrantAbility::new(
                filter.clone(),
                granted.clone(),
            ));
        let grant_object_model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::new(ironsmith_core::GrantObjectAbilityForFilter::new(
                filter,
                granted,
                "Can block an additional creature each combat",
            ));
        let grants = [
            crate::static_abilities::StaticAbility::from_model(grant_ability_model),
            crate::static_abilities::StaticAbility::from_model(grant_object_model),
        ];

        for grant in grants {
            let definition = crate::cards::builders::CardDefinitionBuilder::new(
                crate::ids::CardId::new(),
                "Blocking Capacity Grant Probe",
            )
            .card_types(vec![CardType::Enchantment])
            .with_ability(Ability::static_ability(grant))
            .build();

            assert_eq!(
                crate::compiled_text::compiled_text_lines(&definition),
                vec![
                    "Creatures you control can block an additional creature each combat."
                        .to_string()
                ]
            );
        }
    }
}

/// Recombine adjacent, independently executable grants from an authored
/// "has [keywords] and can't be blocked by more than N creatures" clause.
/// Matching the typed affected filter and condition prevents a restriction on
/// the source from being folded into an attachment grant.
fn describe_structural_keyword_maximum_blocker_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let first = abilities.first()?;
    let (filter, condition, first_granted) = modeled_unquantified_object_static_grant(first)?;
    if !first_granted.is_keyword() {
        return None;
    }
    let AbilityKind::Static(first_static) = &first.kind else {
        return None;
    };
    let first_display = first_static.display();
    let (subject, _, verb) =
        split_static_predicate_with_verb(&first_display, &[" has ", " have "])?;
    let has_verb = if verb == "have" { "have" } else { "has" };

    let mut keywords = Vec::new();
    let mut consumed = 0usize;
    for ability in abilities {
        let (next_filter, next_condition, granted) =
            modeled_unquantified_object_static_grant(ability)?;
        if next_filter != filter || next_condition != condition {
            return None;
        }
        if granted.is_keyword() {
            let keyword = granted
                .display()
                .trim()
                .trim_end_matches('.')
                .to_ascii_lowercase();
            if !is_mergeable_keyword_surface(&keyword) {
                return None;
            }
            keywords.push(keyword);
            consumed += 1;
            continue;
        }
        let model = granted.compiled_model()?;
        let ironsmith_core::StaticAbilityPayload::CantBeBlockedByMoreThan(maximum) = &model.payload
        else {
            return None;
        };
        if keywords.is_empty() {
            return None;
        }
        consumed += 1;
        let maximum = small_number_word(*maximum as u32).unwrap_or_else(|| maximum.to_string());
        let creature = if maximum == "one" {
            "creature"
        } else {
            "creatures"
        };
        return Some((
            capitalize_first(&format!(
                "{subject} {has_verb} {} and can't be blocked by more than {maximum} {creature}",
                render_keyword_list(&keywords, false)
            )),
            consumed,
        ));
    }
    None
}

/// Render one conditional keyword granted to the source from its typed model.
/// Keeping the condition out of the grammatical subject avoids plural nouns in
/// the condition (for example, "three artifacts") selecting `have` for the
/// singular source.
fn describe_structural_conditional_source_keyword_grant(
    ability: &Ability,
    subject: &str,
) -> Option<String> {
    let (_, keyword, condition) = modeled_source_keyword_grant(ability)?;
    if matches!(
        condition,
        Condition::ActivationTiming(crate::ability::ActivationTiming::DuringYourTurn)
    ) {
        return Some(format!("During your turn, {subject} has {keyword}"));
    }
    let prefix_surface = matches!(
        condition,
        Condition::Not(inner)
            if matches!(inner.as_ref(), Condition::PlayerCastSpellsThisTurnOrMore { .. })
    ) || matches!(
        condition,
        Condition::PlayerLifeAtMostHalfStartingLifeTotal { .. }
    ) || matches!(
        condition,
        Condition::ValueComparison { left, .. }
            if matches!(left.unhinted(), Value::MaxDiceRolledThisTurn(_))
    ) || matches!(
        condition,
        Condition::TurnHistory(
            ironsmith_core::TurnHistoryCondition::PlayerVisitedAttractionThisTurn(_)
        )
    ) || matches!(
        condition,
        Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::CountersAmong(_, _),
            ..
        }
    );
    let source_match = matches!(condition, Condition::SourceMatches(_));
    let condition = describe_condition(condition);
    let condition = if source_match {
        condition
            .strip_prefix("this permanent is ")
            .or_else(|| condition.strip_prefix("this source is "))
            .map(|predicate| format!("it's {predicate}"))
            .unwrap_or(condition)
    } else {
        condition
    };
    let condition = condition
        .strip_prefix("this permanent")
        .map(|rest| format!("{}{rest}", lowercase_first(subject)))
        .unwrap_or(condition);
    if prefix_surface {
        Some(format!("As long as {condition}, {subject} has {keyword}"))
    } else {
        Some(format!("{subject} has {keyword} as long as {condition}"))
    }
}

fn modeled_conditioned_source_anthem(ability: &Ability) -> Option<(i32, i32, &Condition)> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    if static_ability.id() != crate::static_abilities::StaticAbilityId::Anthem {
        return None;
    }
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::Anthem(anthem) = &model.payload else {
        return None;
    };
    if anthem.filter.is_some()
        || anthem.set_quantifier_surface.is_some()
        || anthem.count_uses_where_x
        || anthem.additional_surface
        || anthem.replacement_surface.is_some()
    {
        return None;
    }
    let (ironsmith_core::AnthemValue::Fixed(power), ironsmith_core::AnthemValue::Fixed(toughness)) =
        (&anthem.power, &anthem.toughness)
    else {
        return None;
    };
    if (*power, *toughness) == (0, 0) {
        return None;
    }
    Some((*power, *toughness, anthem.condition.as_ref()?))
}

/// Preserve the authored combat-state negative when the typed condition says
/// that the source is neither attacking nor blocking.  The generic condition
/// display intentionally has no prose for this compound object predicate, so
/// recognize only the exact otherwise-empty filter here.
fn describe_structural_noncombat_source_anthem(ability: &Ability, subject: &str) -> Option<String> {
    let (power, toughness, condition) = modeled_conditioned_source_anthem(ability)?;
    let Condition::Not(inner) = condition else {
        return None;
    };
    let Condition::TargetMatches(filter) = inner.as_ref() else {
        return None;
    };
    if !filter.attacking || !filter.blocking {
        return None;
    }
    let mut remainder = filter.clone();
    remainder.attacking = false;
    remainder.blocking = false;
    if remainder != ObjectFilter::default() {
        return None;
    }
    Some(format!(
        "{subject} gets {} as long as it isn't attacking or blocking",
        describe_fixed_anthem_modifier(power, toughness)
    ))
}

/// Preserve the player-owned graveyard surface on a source anthem. The
/// executable value is player-relative (`CardsInGraveyard(Opponent)`); the
/// generic static display flattens that into an existential card count and
/// loses the fact that one opponent owns the qualifying graveyard.
fn describe_structural_opponent_graveyard_source_anthem(
    ability: &Ability,
    subject: &str,
) -> Option<String> {
    let (power, toughness, condition) = modeled_conditioned_source_anthem(ability)?;
    let Condition::ValueComparison {
        left,
        operator: crate::effect::ValueComparisonOperator::GreaterThanOrEqual,
        right,
    } = condition
    else {
        return None;
    };
    if !matches!(
        left.unhinted(),
        Value::CardsInGraveyard(PlayerFilter::Opponent)
    ) || !matches!(right.unhinted(), Value::Fixed(count) if *count >= 0)
    {
        return None;
    }
    Some(format!(
        "{subject} gets {} as long as {}",
        describe_fixed_anthem_modifier(power, toughness),
        describe_condition(condition)
    ))
}

/// Render a filtered anthem whose gate compares a player's life total with
/// that same player's starting life total. The runtime static display falls
/// back to value variant names for arithmetic comparisons, even though the
/// typed condition retains the complete player-relative relationship.
fn describe_structural_starting_life_threshold_anthem(ability: &Ability) -> Option<String> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    if static_ability.id() != crate::static_abilities::StaticAbilityId::Anthem {
        return None;
    }
    let model = static_ability.compiled_model()?;
    let (anthem, condition) = match &model.payload {
        ironsmith_core::StaticAbilityPayload::Anthem(anthem) => {
            (anthem, anthem.condition.as_ref()?)
        }
        ironsmith_core::StaticAbilityPayload::Conditional { ability, condition } => {
            let ironsmith_core::StaticAbilityPayload::Anthem(anthem) = &ability.payload else {
                return None;
            };
            if anthem.condition.is_some() {
                return None;
            }
            (anthem, condition)
        }
        _ => return None,
    };
    let filter = anthem.filter.as_ref()?;
    if !matches!(
        anthem.set_quantifier_surface,
        None | Some(ironsmith_core::SetQuantifierSurface::Each)
    ) || anthem.count_uses_where_x
        || anthem.additional_surface
        || anthem.replacement_surface.is_some()
    {
        return None;
    }
    let (ironsmith_core::AnthemValue::Fixed(power), ironsmith_core::AnthemValue::Fixed(toughness)) =
        (&anthem.power, &anthem.toughness)
    else {
        return None;
    };
    let Condition::ValueComparison {
        left: Value::LifeTotal(player),
        operator: crate::effect::ValueComparisonOperator::GreaterThanOrEqual,
        right,
    } = condition
    else {
        return None;
    };
    let (starting_player, offset) = match right {
        Value::StartingLifeTotal(player) => (player, 0),
        Value::Add(starting, offset) => match (starting.as_ref(), offset.as_ref()) {
            (Value::StartingLifeTotal(player), Value::Fixed(offset)) => (player, *offset),
            _ => return None,
        },
        _ => return None,
    };
    if player != starting_player || offset < 0 {
        return None;
    }

    let subject = pluralize_noun_phrase(
        filter
            .description()
            .trim_start_matches("a ")
            .trim_start_matches("an "),
    );
    Some(format!(
        "As long as {}, {subject} get {}",
        describe_condition(condition),
        describe_fixed_anthem_modifier(*power, *toughness)
    ))
}

/// Render a pregame reveal from its typed delayed trigger rather than the
/// runtime debug display. The latter intentionally knows only executable
/// runtime effects and can print `effect` placeholders for compiler-era
/// structures such as quantified mill, token creation, and counter-unless.
fn describe_structural_opening_hand_reveal(ability: &Ability) -> Option<String> {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let crate::static_abilities::PregameActionKind::RevealFromOpeningHand(spec) =
        static_ability.pregame_action_kind()?
    else {
        return None;
    };
    let [schedule_effect] = static_ability.pregame_action_effects()? else {
        return None;
    };
    let schedule =
        schedule_effect.downcast_ref::<crate::effects::ScheduleDelayedTriggerEffect>()?;
    if schedule.start_next_turn
        || schedule.duration != ironsmith_core::DelayedTriggerDuration::Forever
        || schedule.until_end_of_turn
        || schedule.until_end_of_combat
        || schedule.leading_duration_surface
        || schedule.watch_ability_source
        || schedule.watch_all_object_targets
        || schedule.either_of_watched_objects
        || schedule.while_any_tagged_object_in_zone.is_some()
        || !schedule.target_objects.is_empty()
        || schedule.target_tag.is_some()
        || schedule.target_filter.is_some()
        || schedule.controller != PlayerFilter::You
        || schedule.prepayment.is_some()
        || schedule.event_value_from_prior_prevention
    {
        return None;
    }

    let (timing, expects_one_shot, first_opponent_spell) = if let Some(upkeep) =
        schedule
            .trigger
            .downcast_ref::<crate::triggers::BeginningOfUpkeepTrigger>()
    {
        let timing = match upkeep.player {
            PlayerFilter::You => "at the beginning of your first upkeep",
            PlayerFilter::Any => "at the beginning of the first upkeep",
            _ => return None,
        };
        (timing, true, false)
    } else if let Some(main) = schedule
        .trigger
        .downcast_ref::<crate::triggers::BeginningOfMainPhaseTrigger>()
    {
        if main.player != PlayerFilter::You
            || main.phase_type != crate::triggers::phase_step::MainPhaseType::Precombat
        {
            return None;
        }
        (
            "at the beginning of your first main phase of the game",
            true,
            false,
        )
    } else if let Some(spell) = schedule
        .trigger
        .downcast_ref::<crate::triggers::SpellCastTrigger>()
    {
        if !spell.first_spell_of_game || spell.caster != PlayerFilter::Opponent {
            return None;
        }
        (
            "when each opponent casts their first spell of the game",
            false,
            true,
        )
    } else {
        return None;
    };
    if schedule.one_shot != expects_one_shot {
        return None;
    }

    let mut consequence = describe_effect_list(schedule.effects.flattened_default_effects())
        .trim()
        .trim_end_matches('.')
        .to_string();
    if consequence.is_empty() || consequence.eq_ignore_ascii_case("effect") {
        return None;
    }
    if first_opponent_spell {
        for prefix in [
            "Counter it unless that object's controller pays",
            "Counter it unless its controller pays",
            "Counter it unless that player pays",
            "Counter it unless they pay",
        ] {
            if let Some(rest) = consequence.strip_prefix(prefix) {
                consequence = format!("Counter that spell unless that player pays{rest}");
                break;
            }
        }
    }

    let prefix = "You may reveal this card from your opening hand. If you do";
    Some(if spec.effect_before_timing {
        format!("{prefix}, {} {timing}.", lowercase_first(&consequence))
    } else {
        format!("{prefix}, {timing}, {}.", lowercase_first(&consequence))
    })
}

#[cfg(test)]
mod opening_hand_reveal_surface_tests {
    use super::*;

    #[test]
    fn public_route_renders_typed_chancellor_consequences() {
        for oracle in [
            "You may reveal this card from your opening hand. If you do, at the beginning of the first upkeep, create a 1/1 red Phyrexian Goblin creature token with haste.",
            "You may reveal this card from your opening hand. If you do, at the beginning of the first upkeep, each opponent mills seven cards.",
            "You may reveal this card from your opening hand. If you do, when each opponent casts their first spell of the game, counter that spell unless that player pays {1}.",
        ] {
            let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
                crate::ids::CardId::new(),
                "Opening Hand Reveal Probe",
            )
            .card_types(vec![CardType::Creature])
            .parse_text(oracle)
            .expect("opening-hand reveal line should parse");
            assert_eq!(
                crate::compiled_text::compiled_text_lines(&definition),
                vec![oracle.to_string()]
            );
        }
    }
}

#[cfg(test)]
mod opponent_graveyard_source_anthem_tests {
    use super::*;

    #[test]
    fn public_route_keeps_one_opponents_graveyard_as_the_threshold_owner() {
        for (oracle, expected) in [
            (
                "Flying\nThis creature gets +4/+4 as long as an opponent has ten or more cards in their graveyard.",
                "This creature gets +4/+4 as long as an opponent has ten or more cards in their graveyard.",
            ),
            (
                "Menace\nThis creature gets +3/+0 as long as an opponent has eight or more cards in their graveyard.",
                "This creature gets +3/+0 as long as an opponent has eight or more cards in their graveyard.",
            ),
        ] {
            let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
                crate::ids::CardId::new(),
                "Opponent Graveyard Probe",
            )
            .card_types(vec![CardType::Creature])
            .parse_text(oracle)
            .expect("opponent graveyard source anthem should parse");
            let lines = crate::compiled_text::compiled_text_lines(&definition);
            assert_eq!(lines.last().map(String::as_str), Some(expected));
        }
    }
}

#[cfg(test)]
mod starting_life_threshold_anthem_tests {
    use super::*;

    const LINE: &str = "As long as you have at least 7 life more than your starting life total, creatures you control get +2/+2.";

    #[test]
    fn public_route_keeps_starting_life_arithmetic_as_player_relative_prose() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Starting Life Threshold Probe",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(LINE)
        .expect("starting-life threshold anthem should parse");
        let parsed_ability = definition.abilities.last().expect("parsed anthem");
        assert_eq!(
            describe_structural_starting_life_threshold_anthem(parsed_ability),
            Some(LINE.trim_end_matches('.').to_string()),
            "{parsed_ability:#?}"
        );
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![LINE.to_string()]
        );

        let mut ability = definition.abilities.last().expect("parsed anthem").clone();
        let AbilityKind::Static(static_ability) = &ability.kind else {
            panic!("expected static anthem");
        };
        let mut model = static_ability
            .compiled_model()
            .expect("compiled static model")
            .clone();
        let condition = match &mut model.payload {
            ironsmith_core::StaticAbilityPayload::Anthem(anthem) => {
                anthem.condition.as_mut().expect("anthem condition")
            }
            ironsmith_core::StaticAbilityPayload::Conditional { condition, .. } => condition,
            payload => panic!("expected conditioned anthem payload, got {payload:#?}"),
        };
        let Condition::ValueComparison {
            right: Value::Add(_, offset),
            ..
        } = condition
        else {
            panic!("expected starting-life comparison");
        };
        **offset = Value::Fixed(-1);
        ability.kind =
            AbilityKind::Static(crate::static_abilities::StaticAbility::from_model(model));
        assert_eq!(
            describe_structural_starting_life_threshold_anthem(&ability),
            None
        );
    }
}

#[cfg(test)]
mod noncombat_source_anthem_tests {
    use super::*;

    const LINE: &str = "This creature gets +2/+0 as long as it isn't attacking or blocking.";

    #[test]
    fn exact_compound_negative_renders_but_single_state_near_miss_does_not() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Intrepid Ace",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(LINE)
        .expect("compound combat-state condition should parse");
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![LINE.to_string()]
        );

        let mut changed = definition.abilities[0].clone();
        let AbilityKind::Static(static_ability) = &changed.kind else {
            panic!("expected static ability");
        };
        let mut model = static_ability
            .compiled_model()
            .expect("compiled model")
            .clone();
        let ironsmith_core::StaticAbilityPayload::Anthem(anthem) = &mut model.payload else {
            panic!("expected anthem model");
        };
        let Some(Condition::Not(inner)) = &mut anthem.condition else {
            panic!("expected negative condition");
        };
        let Condition::TargetMatches(filter) = inner.as_mut() else {
            panic!("expected object condition");
        };
        filter.blocking = false;
        changed.kind =
            AbilityKind::Static(crate::static_abilities::StaticAbility::from_model(model));
        assert!(describe_structural_noncombat_source_anthem(&changed, "this creature").is_none());
    }
}

fn modeled_direct_conditional_source_rule(
    ability: &Ability,
    expected_id: crate::static_abilities::StaticAbilityId,
) -> Option<&Condition> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: inner,
        condition,
    } = &model.payload
    else {
        return None;
    };
    (inner.id == Some(expected_id)
        && matches!(&inner.payload, ironsmith_core::StaticAbilityPayload::None))
    .then_some(condition)
}

fn describe_no_defender_condition(condition: &Condition) -> String {
    describe_condition(condition)
        .replace("this source", "this creature")
        .replace("this permanent", "this creature")
}

fn condition_is_metalcraft(condition: &Condition) -> bool {
    let Condition::CountComparison {
        count: ironsmith_core::AnthemCountExpression::MatchingFilter(filter),
        comparison: Comparison::GreaterThanOrEqual(3),
        ..
    } = condition
    else {
        return false;
    };
    let mut actual = filter.clone();
    actual.union_surface = Default::default();
    actual == ObjectFilter::artifact().you_control()
}

/// Rejoin the independently executable pieces of a conditional source line
/// that includes the defender attack override.  The pair is accepted only
/// when both members use the same typed condition and the companion is either
/// the source's fixed P/T anthem or its trample grant.
fn describe_structural_conditioned_no_defender_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [first, second, ..] = abilities else {
        return None;
    };

    let (permission, companion) = if modeled_direct_conditional_source_rule(
        first,
        crate::static_abilities::StaticAbilityId::CanAttackAsThoughNoDefender,
    )
    .is_some()
    {
        (first, second)
    } else if modeled_direct_conditional_source_rule(
        second,
        crate::static_abilities::StaticAbilityId::CanAttackAsThoughNoDefender,
    )
    .is_some()
    {
        (second, first)
    } else {
        return None;
    };
    let condition = modeled_direct_conditional_source_rule(
        permission,
        crate::static_abilities::StaticAbilityId::CanAttackAsThoughNoDefender,
    )?;
    let condition_text = describe_no_defender_condition(condition);

    let predicate = if let Some((power, toughness, anthem_condition)) =
        modeled_conditioned_source_anthem(companion)
    {
        if anthem_condition != condition {
            return None;
        }
        format!("gets {power:+}/{toughness:+} and can attack as though it didn't have defender")
    } else {
        let (filter, grant_condition, granted) = modeled_object_static_grant(companion)?;
        if filter != &ObjectFilter::source()
            || grant_condition != Some(condition)
            || granted.id() != crate::static_abilities::StaticAbilityId::Trample
        {
            return None;
        }
        "has trample and can attack as though it didn't have defender".to_string()
    };
    let label = if condition_is_metalcraft(condition) {
        "Metalcraft — "
    } else {
        ""
    };
    Some((
        format!("{label}As long as {condition_text}, this creature {predicate}"),
        2,
    ))
}

fn describe_structural_source_combat_presence_restriction(
    ability: &Ability,
    subject: &str,
) -> Option<String> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::Conditional { ability, condition } = &model.payload
    else {
        return None;
    };
    let Condition::ValueComparison {
        left: Value::Count(filter),
        operator: crate::effect::ValueComparisonOperator::GreaterThan,
        right: Value::Fixed(0),
    } = condition
    else {
        return None;
    };
    if filter.zone != Some(Zone::Battlefield) {
        return None;
    }
    let ironsmith_core::StaticAbilityPayload::RuleRestriction {
        restriction,
        additional_restrictions,
        ..
    } = &ability.payload
    else {
        return None;
    };
    if !additional_restrictions.is_empty() {
        return None;
    }
    let (affected, action) = match restriction {
        crate::effect::Restriction::Attack(filter) => (filter, "attack"),
        crate::effect::Restriction::Block(filter) => (filter, "block"),
        crate::effect::Restriction::AttackOrBlock(filter) => (filter, "attack or block"),
        _ => return None,
    };
    if affected != &ObjectFilter::source() {
        return None;
    }
    Some(format!(
        "{subject} can't {action} if {}",
        describe_condition(condition)
    ))
}

fn describe_structural_conditional_source_no_defender_permission(
    ability: &Ability,
    subject: &str,
) -> Option<String> {
    let condition = modeled_direct_conditional_source_rule(
        ability,
        crate::static_abilities::StaticAbilityId::CanAttackAsThoughNoDefender,
    )?;
    let subject = if source_status_alternatives(condition).is_some() {
        "it"
    } else {
        subject
    };
    Some(format!(
        "As long as {}, {subject} can attack as though it didn't have defender",
        describe_no_defender_condition(condition)
    ))
}

fn delirium_condition_surface(condition: &Condition) -> Option<&'static str> {
    matches!(
        condition,
        Condition::PlayerHasCardTypesInGraveyardOrMore {
            player: PlayerFilter::You,
            count: 4,
        }
    )
    .then_some("there are four or more card types among cards in your graveyard")
}

/// Render source-relative counter thresholds from their executable values.
/// Named counter predicates lower through `CountComparison`, while Oracle's
/// untyped "counters on it" form uses `ValueComparison::CountersOn`.
fn source_counter_threshold_condition_surface(
    condition: &Condition,
    source_subject: &str,
) -> Option<String> {
    let (counter_type, source_surface, comparison, object_pronoun) = match condition {
        Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::CountersOnSource(counter_type),
            comparison,
            ..
        } => (Some(*counter_type), None, *comparison, "it"),
        Condition::CountComparison {
            count:
                ironsmith_core::AnthemCountExpression::CountersOnSourceWithSurface {
                    counter_type,
                    surface,
                },
            comparison,
            ..
        } => (
            Some(*counter_type),
            Some(surface.display_text()),
            *comparison,
            "it",
        ),
        Condition::CountComparison {
            count:
                ironsmith_core::AnthemCountExpression::CountersOnSourceWithPronoun {
                    counter_type,
                    pronoun,
                },
            comparison,
            ..
        } => (
            Some(*counter_type),
            None,
            *comparison,
            pronoun.object_pronoun(),
        ),
        Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::MatchingFilter(filter),
            comparison,
            ..
        } => {
            let Some(ironsmith_core::CounterConstraint::Typed(counter_type)) = filter.with_counter
            else {
                return None;
            };
            if filter != &ObjectFilter::source().with_counter_type(counter_type) {
                return None;
            }
            (Some(counter_type), None, *comparison, "it")
        }
        Condition::ValueComparison {
            left,
            operator,
            right,
        } => {
            let Value::CountersOn(spec, counter_type) = left.unhinted() else {
                return None;
            };
            if !matches!(spec.base(), ChooseSpec::Source) {
                return None;
            }
            let Value::Fixed(threshold) = right.unhinted() else {
                return None;
            };
            let comparison = match operator {
                ironsmith_core::ValueComparisonOperator::GreaterThan => {
                    Comparison::GreaterThan(*threshold)
                }
                ironsmith_core::ValueComparisonOperator::GreaterThanOrEqual => {
                    Comparison::GreaterThanOrEqual(*threshold)
                }
                ironsmith_core::ValueComparisonOperator::Equal => Comparison::Equal(*threshold),
                ironsmith_core::ValueComparisonOperator::LessThan => {
                    Comparison::LessThan(*threshold)
                }
                ironsmith_core::ValueComparisonOperator::LessThanOrEqual => {
                    Comparison::LessThanOrEqual(*threshold)
                }
                ironsmith_core::ValueComparisonOperator::NotEqual => {
                    Comparison::NotEqual(*threshold)
                }
            };
            (
                *counter_type,
                spec.source_reference_surface()
                    .map(|surface| surface.display_text()),
                comparison,
                "it",
            )
        }
        _ => return None,
    };

    let typed_counter = counter_type.map(CounterType::description);
    let singular_counter = typed_counter
        .as_deref()
        .map(|counter| format!("{counter} counter"))
        .unwrap_or_else(|| "counter".to_string());
    let plural_counters = typed_counter
        .as_deref()
        .map(|counter| format!("{counter} counters"))
        .unwrap_or_else(|| "counters".to_string());
    let quantity = match comparison {
        Comparison::Equal(0) | Comparison::LessThanOrEqual(0) | Comparison::LessThan(1) => {
            format!("no {plural_counters}")
        }
        Comparison::GreaterThanOrEqual(1) => format!("a {singular_counter}"),
        Comparison::GreaterThanOrEqual(threshold) if threshold > 1 => {
            let threshold = u32::try_from(threshold)
                .ok()
                .and_then(small_number_word)
                .unwrap_or_else(|| threshold.to_string());
            format!("{threshold} or more {plural_counters}")
        }
        _ => return None,
    };
    Some(format!(
        "{} has {quantity} on {object_pronoun}",
        source_surface.as_deref().unwrap_or(source_subject),
    ))
}

/// Extract one counter-threshold static predicate without consulting display
/// text for its condition. This covers source P/T modifiers, source keyword
/// grants, filtered keyword grants (for example, an equipped creature), and
/// direct conditional rules such as attacking despite defender.
fn modeled_source_counter_threshold_static_predicate<'a>(
    ability: &'a Ability,
    source_subject: &str,
) -> Option<(String, String, &'a Condition)> {
    if let Some((power, toughness, condition)) = modeled_conditioned_source_anthem(ability)
        && source_counter_threshold_condition_surface(condition, source_subject).is_some()
    {
        return Some((
            source_subject.to_string(),
            format!("gets {}", describe_fixed_anthem_modifier(power, toughness)),
            condition,
        ));
    }

    if let Some((_, keyword, condition)) = modeled_source_keyword_grant(ability)
        && source_counter_threshold_condition_surface(condition, source_subject).is_some()
    {
        return Some((
            source_subject.to_string(),
            format!("has {keyword}"),
            condition,
        ));
    }

    if let Some((filter, Some(condition), granted)) = modeled_object_static_grant(ability)
        && source_counter_threshold_condition_surface(condition, source_subject).is_some()
        && granted.is_keyword()
    {
        let keyword = granted
            .display()
            .trim()
            .trim_end_matches('.')
            .to_ascii_lowercase();
        if !is_mergeable_keyword_surface(&keyword) {
            return None;
        }
        let (grant_subject, _) =
            crate::static_abilities::grant_subject_with_set_quantifier(filter, None);
        return Some((
            lowercase_first(&grant_subject),
            format!("has {keyword}"),
            condition,
        ));
    }

    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: inner,
        condition,
    } = &model.payload
    else {
        return None;
    };
    source_counter_threshold_condition_surface(condition, source_subject)?;
    if let ironsmith_core::StaticAbilityPayload::SetLandSubtypes { filter, subtypes } =
        &inner.payload
    {
        if subtypes.is_empty() {
            return None;
        }
        let mut subject_filter = filter.clone();
        subject_filter.zone = None;
        let (subject, singular) =
            crate::static_abilities::grant_subject_with_set_quantifier(&subject_filter, None);
        if singular {
            return None;
        }
        let subtype_surface =
            join_english_list(&subtypes.iter().map(ToString::to_string).collect::<Vec<_>>());
        return Some((
            lowercase_first(&subject),
            format!("are {}", pluralize_noun_phrase(&subtype_surface)),
            condition,
        ));
    }
    if !matches!(&inner.payload, ironsmith_core::StaticAbilityPayload::None) {
        return None;
    }
    let inner = crate::static_abilities::StaticAbility::from_model(inner.as_ref().clone());
    let predicate = inner.display().trim().trim_end_matches('.').to_string();
    (!predicate.is_empty()).then(|| {
        (
            source_subject.to_string(),
            lowercase_first(&predicate),
            condition,
        )
    })
}

/// Rejoin adjacent layer-correct predicates that share the exact same typed
/// source-counter threshold, then place that condition in Oracle's leading
/// `As long as ...` position.
fn describe_structural_source_counter_threshold_static_bundle(
    abilities: &[Ability],
    source_subject: &str,
) -> Option<(String, usize)> {
    let (body_subject, first_predicate, condition) =
        modeled_source_counter_threshold_static_predicate(abilities.first()?, source_subject)?;
    let condition_surface = source_counter_threshold_condition_surface(condition, source_subject)?;
    let mut predicates = vec![first_predicate];
    let mut consumed = 1usize;
    while let Some(ability) = abilities.get(consumed) {
        let Some((next_subject, predicate, next_condition)) =
            modeled_source_counter_threshold_static_predicate(ability, source_subject)
        else {
            break;
        };
        if next_subject != body_subject || next_condition != condition {
            break;
        }
        predicates.push(predicate);
        consumed += 1;
    }

    let predicate_surface = if predicates
        .iter()
        .all(|predicate| predicate.starts_with("has "))
    {
        let granted = predicates
            .iter()
            .map(|predicate| predicate.trim_start_matches("has ").to_string())
            .collect::<Vec<_>>();
        format!("has {}", join_english_list(&granted))
    } else {
        join_english_list(&predicates)
    };
    let body_subject = if body_subject == source_subject {
        "it"
    } else {
        body_subject.as_str()
    };
    Some((
        format!("As long as {condition_surface}, {body_subject} {predicate_surface}"),
        consumed,
    ))
}

/// Recombine the layer-correct static siblings emitted for a conditioned
/// source P/T modifier plus a keyword grant. The two executable payloads stay
/// independent; this only restores their single authored condition surface
/// after proving that the source, zone, and typed condition are identical.
fn describe_structural_conditioned_source_anthem_keyword_bundle(
    abilities: &[Ability],
    subject: &str,
) -> Option<(String, usize)> {
    let [anthem_ability, keyword_ability, ..] = abilities else {
        return None;
    };
    let (power, toughness, condition) = modeled_conditioned_source_anthem(anthem_ability)?;
    let (_, keyword, keyword_condition) = modeled_source_keyword_grant(keyword_ability)?;
    if keyword_condition != condition {
        return None;
    }

    let modifier = describe_fixed_anthem_modifier(power, toughness);
    let has_matching_must_attack = abilities.get(2).is_some_and(|ability| {
        modeled_direct_conditional_source_rule(
            ability,
            crate::static_abilities::StaticAbilityId::MustAttack,
        ) == Some(condition)
    });
    if has_matching_must_attack && let Some(condition) = delirium_condition_surface(condition) {
        return Some((
            format!(
                "Delirium — As long as {condition}, {subject} gets {modifier}, has {keyword}, and attacks each combat if able"
            ),
            3,
        ));
    }
    if let Some(label) = threshold_graveyard_presentation_label(condition) {
        if has_matching_must_attack {
            return Some((
                format!(
                    "{label} — As long as there are seven or more cards in your graveyard, {subject} gets {modifier}, has {keyword}, and attacks each combat if able"
                ),
                3,
            ));
        }
        return Some((
            format!(
                "{label} — As long as there are seven or more cards in your graveyard, {subject} gets {modifier} and has {keyword}"
            ),
            2,
        ));
    }
    if matches!(
        condition,
        Condition::ActivationTiming(crate::ability::ActivationTiming::DuringYourTurn)
    ) {
        return Some((
            format!("During your turn, {subject} gets {modifier} and has {keyword}"),
            2,
        ));
    }

    let prefix_surface = matches!(
        condition,
        Condition::Not(inner)
            if matches!(inner.as_ref(), Condition::PlayerCastSpellsThisTurnOrMore { .. })
    ) || matches!(
        condition,
        Condition::ValueComparison { left, .. }
            if matches!(left.unhinted(), Value::MaxDiceRolledThisTurn(_))
    );
    let condition = describe_condition(condition);
    let condition = condition
        .strip_prefix("this permanent")
        .map(|rest| format!("{}{rest}", lowercase_first(subject)))
        .unwrap_or(condition);
    let text = if prefix_surface {
        format!("As long as {condition}, {subject} gets {modifier} and has {keyword}")
    } else {
        format!("{subject} gets {modifier} and has {keyword} as long as {condition}")
    };
    Some((text, 2))
}

/// Recombine an attached creature's conditional P/T modifier and Ward grant.
///
/// Aura lowering keeps the enchanted permanent executable through the
/// `enchanted` observation tag.  That tag is implementation provenance, not
/// an Oracle noun, so only erase it after both independently executable
/// statics prove the same affected creature and the same legendary gate.
fn describe_structural_attached_legendary_anthem_ward_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [anthem_ability, ward_ability, ..] = abilities else {
        return None;
    };
    if anthem_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || ward_ability.functional_zones != anthem_ability.functional_zones
    {
        return None;
    }

    let AbilityKind::Static(anthem_static) = &anthem_ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::Anthem(anthem) =
        &anthem_static.compiled_model()?.payload
    else {
        return None;
    };
    let anthem_filter = anthem.filter.as_ref()?;
    let condition = anthem.condition.as_ref()?;
    let (grant_filter, grant_condition, granted) = modeled_object_static_grant(ward_ability)?;

    let mut semantic_anthem_filter = anthem_filter.clone();
    semantic_anthem_filter.union_surface = Default::default();
    let expected_attached = ObjectFilter::creature()
        .in_zone(Zone::Battlefield)
        .match_tagged(
            "enchanted",
            crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        );
    let mut semantic_grant_filter = grant_filter.clone();
    semantic_grant_filter.union_surface = Default::default();

    let Condition::TaggedObjectMatches(condition_tag, condition_filter) = condition else {
        return None;
    };
    let mut semantic_condition_filter = condition_filter.clone();
    semantic_condition_filter.union_surface = Default::default();
    let mut expected_legendary = ObjectFilter::creature();
    expected_legendary.zone = None;
    expected_legendary
        .supertypes
        .push(crate::types::Supertype::Legendary);

    let (ironsmith_core::AnthemValue::Fixed(power), ironsmith_core::AnthemValue::Fixed(toughness)) =
        (&anthem.power, &anthem.toughness)
    else {
        return None;
    };
    if semantic_anthem_filter != expected_attached
        || semantic_grant_filter != semantic_anthem_filter
        || grant_condition != Some(condition)
        || condition_tag.as_str() != "enchanted"
        || semantic_condition_filter != expected_legendary
        || anthem.set_quantifier_surface.is_some()
        || anthem.count_uses_where_x
        || anthem.additional_surface
        || anthem.replacement_surface.is_some()
        || granted.id() != crate::static_abilities::StaticAbilityId::Ward
    {
        return None;
    }
    let ward_cost = granted.ward_cost()?.display();
    let ward_text = granted
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    Some((
        format!(
            "As long as enchanted creature is legendary, it gets {} and has {ward_text}. {STANDARD_REMINDER_OPEN_SENTINEL}Whenever enchanted creature becomes the target of a spell or ability an opponent controls, counter it unless that player pays {ward_cost}.{STANDARD_REMINDER_CLOSE_SENTINEL}",
            describe_fixed_anthem_modifier(*power, *toughness),
        ),
        2,
    ))
}

/// A lone source keyword grant gated on the seven-card graveyard condition
/// restores its authored ability-word surface: "Threshold — As long as there
/// are seven or more cards in your graveyard, this creature has flying."
fn describe_structural_threshold_source_keyword_grant(
    ability: &Ability,
    subject: &str,
) -> Option<String> {
    let (_, keyword, condition) = modeled_source_keyword_grant(ability)?;
    let label = threshold_graveyard_presentation_label(condition)?;
    Some(format!(
        "{label} — As long as there are seven or more cards in your graveyard, {subject} has {keyword}"
    ))
}

/// Recombine a labeled conditional source animation (type add + base P/T +
/// subtype) with its sibling keyword and activated grants under the same
/// condition: "Celebration — As long as ..., Goddric is a Dragon with base
/// power and toughness 4/4, flying, and \"...\"" (Goddric, Cloaked Reveler).
fn describe_structural_labeled_animation_grant_bundle(
    abilities: &[Ability],
    subject: &str,
) -> Option<(String, usize)> {
    let [
        types_ability,
        pt_ability,
        subtypes_ability,
        keyword_ability,
        granted_ability,
        ..,
    ] = abilities
    else {
        return None;
    };
    let AbilityKind::Static(types_static) = &types_ability.kind else {
        return None;
    };
    let types_model = types_static.compiled_model()?;
    let presentation_label = types_model.label.strip_prefix(
        ironsmith_core::static_ability_model::EXPLICIT_STATIC_PRESENTATION_LABEL_PREFIX,
    );
    let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: types_inner,
        condition,
    } = &types_model.payload
    else {
        return None;
    };
    if types_inner.id != Some(crate::static_abilities::StaticAbilityId::AddCardTypes) {
        return None;
    }

    let AbilityKind::Static(pt_static) = &pt_ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: pt_inner,
        condition: pt_condition,
    } = &pt_static.compiled_model()?.payload
    else {
        return None;
    };
    if pt_condition != condition {
        return None;
    }
    let ironsmith_core::StaticAbilityPayload::SetBasePowerToughness {
        power, toughness, ..
    } = &pt_inner.payload
    else {
        return None;
    };

    let AbilityKind::Static(subtypes_static) = &subtypes_ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: subtypes_inner,
        condition: subtypes_condition,
    } = &subtypes_static.compiled_model()?.payload
    else {
        return None;
    };
    if subtypes_condition != condition {
        return None;
    }
    let ironsmith_core::StaticAbilityPayload::SetCreatureSubtypes { subtypes, .. } =
        &subtypes_inner.payload
    else {
        return None;
    };
    let [subtype] = subtypes.as_slice() else {
        return None;
    };

    let (_, keyword, keyword_condition) = modeled_source_keyword_grant(keyword_ability)?;
    if keyword_condition != condition {
        return None;
    }

    let AbilityKind::Static(granted_static) = &granted_ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) =
        &granted_static.compiled_model()?.payload
    else {
        return None;
    };
    if grant.condition.as_ref() != Some(condition)
        || grant.filter != ObjectFilter::source()
        || !matches!(
            &grant.ability.kind,
            ironsmith_core::AbilityKind::Activated(_)
        )
    {
        return None;
    }
    let granted = granted_static.source_granted_inline_abilities();
    let [granted] = granted.as_slice() else {
        return None;
    };
    let mut granted_text = describe_inline_ability_with_self_subject(granted, subject)
        .trim()
        .trim_end_matches('.')
        .to_string();
    if granted_text.is_empty() {
        return None;
    }
    if !granted_text.ends_with('!') && !granted_text.ends_with('?') {
        granted_text.push('.');
    }

    let body = format!(
        "As long as {}, {subject} is a {subtype:?} with base power and toughness {power}/{toughness}, {keyword}, and \"{granted_text}\"",
        describe_condition(condition)
    );
    Some((
        presentation_label
            .map(|label| format!("{label} — {body}"))
            .unwrap_or(body),
        5,
    ))
}

#[cfg(test)]
mod labeled_animation_grant_bundle_tests {
    use super::*;

    const CONDITION: &str =
        "two or more nonland permanents entered the battlefield under your control this turn";
    const LINE: &str = "Celebration — As long as two or more nonland permanents entered the battlefield under your control this turn, this creature is a Dragon with base power and toughness 4/4, flying, and \"{R}: Dragons you control get +1/+0 until end of turn.\"";

    #[test]
    fn source_line_group_rejoins_exact_conditioned_animation_members() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Celebration Animation Probe",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(LINE)
        .expect("conditioned animation bundle should compile");

        assert_eq!(
            source_line_static_group_count(&definition.abilities[0]),
            Some(5)
        );
        assert_eq!(
            describe_source_line_static_group(&definition.abilities[1..], 5, "this creature"),
            Some(format!(
                "As long as {CONDITION}, this creature is a Dragon with base power and toughness 4/4, flying, and \"{{R}}: Dragons you control get +1/+0 until end of turn.\""
            ))
        );
        assert!(
            describe_source_line_static_group(&definition.abilities[1..], 4, "this creature")
                .is_none(),
            "a changed source-line member count must not claim the structural bundle"
        );
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![LINE.to_string()]
        );
    }
}

/// Recombine a Threshold source P/T modifier with its sibling attack
/// requirement when both statics carry the same seven-card graveyard gate.
fn describe_structural_threshold_anthem_must_attack_bundle(
    abilities: &[Ability],
    subject: &str,
) -> Option<(String, usize)> {
    let [anthem_ability, attack_ability, ..] = abilities else {
        return None;
    };
    let (power, toughness, condition) = modeled_conditioned_source_anthem(anthem_ability)?;
    if modeled_direct_conditional_source_rule(
        attack_ability,
        crate::static_abilities::StaticAbilityId::MustAttack,
    ) != Some(condition)
    {
        return None;
    }
    let label = threshold_graveyard_presentation_label(condition)?;
    let modifier = describe_fixed_anthem_modifier(power, toughness);
    Some((
        format!(
            "{label} — As long as there are seven or more cards in your graveyard, {subject} gets {modifier} and attacks each combat if able"
        ),
        2,
    ))
}

/// Recombine a Threshold source color change with its sibling activated-ability
/// grant when both statics carry the same seven-card graveyard gate.
fn describe_structural_threshold_color_grant_bundle(
    abilities: &[Ability],
    subject: &str,
) -> Option<(String, usize)> {
    let [color_ability, grant_ability, ..] = abilities else {
        return None;
    };
    if color_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || grant_ability.functional_zones != color_ability.functional_zones
    {
        return None;
    }
    let AbilityKind::Static(color_static) = &color_ability.kind else {
        return None;
    };
    let AbilityKind::Static(grant_static) = &grant_ability.kind else {
        return None;
    };
    let color_model = color_static.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: inner_color,
        condition,
    } = &color_model.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::SetColors {
        filter: color_filter,
        colors,
    } = &inner_color.payload
    else {
        return None;
    };
    if inner_color.id != Some(crate::static_abilities::StaticAbilityId::SetColors) {
        return None;
    }
    let color_name = [
        crate::color::Color::White,
        crate::color::Color::Blue,
        crate::color::Color::Black,
        crate::color::Color::Red,
        crate::color::Color::Green,
    ]
    .into_iter()
    .find_map(|color| {
        (*colors == crate::color::ColorSet::from_color(color)).then(|| color.name())
    })?;
    let label = threshold_graveyard_presentation_label(condition)?;

    let grant_model = grant_static.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) =
        &grant_model.payload
    else {
        return None;
    };
    if color_filter != &grant.filter
        || grant.filter != ObjectFilter::source()
        || grant.condition.as_ref() != Some(condition)
        || !grant.additional_abilities.is_empty()
        || grant.set_quantifier_surface.is_some()
        || grant.ability.functional_zones.as_slice() != [Zone::Battlefield]
        || !matches!(
            &grant.ability.kind,
            ironsmith_core::AbilityKind::Activated(_)
        )
    {
        return None;
    }
    let granted = grant_static.source_granted_inline_abilities();
    let [granted] = granted.as_slice() else {
        return None;
    };
    if !matches!(&granted.kind, AbilityKind::Activated(_)) {
        return None;
    }
    let mut granted_text = describe_inline_ability_with_self_subject(granted, subject)
        .trim()
        .trim_end_matches('.')
        .to_string();
    if granted_text.is_empty() {
        return None;
    }
    if !granted_text.ends_with('!') && !granted_text.ends_with('?') {
        granted_text.push('.');
    }

    Some((
        format!(
            "{label} — As long as there are seven or more cards in your graveyard, {subject} is {color_name} and has \"{granted_text}\""
        ),
        2,
    ))
}

/// Recombine a layer-correct source animation and its granted annihilator
/// ability when all three executable static models carry the same condition.
/// The compiler intentionally keeps type addition, base P/T, and the granted
/// attack trigger independent; this restores their single authored surface
/// only after proving the source filter, condition, zone, and granted keyword.
fn describe_structural_conditioned_source_animation_annihilator_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [type_ability, pt_ability, grant_ability, ..] = abilities else {
        return None;
    };
    if type_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || pt_ability.functional_zones != type_ability.functional_zones
        || grant_ability.functional_zones != type_ability.functional_zones
    {
        return None;
    }

    let AbilityKind::Static(type_static) = &type_ability.kind else {
        return None;
    };
    let AbilityKind::Static(pt_static) = &pt_ability.kind else {
        return None;
    };
    let AbilityKind::Static(grant_static) = &grant_ability.kind else {
        return None;
    };
    if type_static.id() != crate::static_abilities::StaticAbilityId::AddCardTypes
        || pt_static.id()
            != crate::static_abilities::StaticAbilityId::SetBasePowerToughnessForFilter
        || grant_static.id()
            != crate::static_abilities::StaticAbilityId::GrantObjectAbilityForFilter
    {
        return None;
    }

    let (type_model, type_condition) = unwrapped_static_model(type_static)?;
    let (pt_model, pt_condition) = unwrapped_static_model(pt_static)?;
    let condition = type_condition?;
    if pt_condition != Some(condition) {
        return None;
    }
    let ironsmith_core::StaticAbilityPayload::AddCardTypes {
        filter: type_filter,
        card_types,
    } = &type_model.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::SetBasePowerToughness {
        filter: pt_filter,
        power,
        toughness,
    } = &pt_model.payload
    else {
        return None;
    };
    if type_filter != &ObjectFilter::source() || pt_filter != type_filter || card_types.is_empty() {
        return None;
    }

    let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) =
        &grant_static.compiled_model()?.payload
    else {
        return None;
    };
    if &grant.filter != type_filter
        || grant.condition.as_ref() != Some(condition)
        || !grant.additional_abilities.is_empty()
        || grant.set_quantifier_surface.is_some()
    {
        return None;
    }
    let granted = grant_static.source_granted_inline_abilities();
    let [granted] = granted.as_slice() else {
        return None;
    };
    let AbilityKind::Triggered(triggered) = &granted.kind else {
        return None;
    };
    let keyword = lowercase_first(&describe_annihilator_keyword(triggered)?);

    let type_phrase = card_types
        .iter()
        .map(|card_type| card_type.name().to_ascii_lowercase())
        .collect::<Vec<_>>()
        .join(" ");
    // A numeric power/toughness prefix is adjectival here, so the generic
    // noun-phrase helper intentionally leaves it articleless. Animation
    // surfaces still require the authored "a 3/3 creature" form.
    let animated = format!("a {power}/{toughness} {type_phrase}");
    Some((
        format!(
            "As long as {}, it's {animated} in addition to its other types and it has {keyword}",
            describe_condition(condition),
        ),
        3,
    ))
}

fn threshold_graveyard_presentation_label(condition: &Condition) -> Option<&'static str> {
    // Threshold is an ability word with no rules meaning. This exact typed
    // gate is therefore safe presentation provenance without consulting a
    // card name, Oracle text, or a rendered condition string.
    matches!(
        condition,
        Condition::ValueComparison {
            left: Value::CardsInGraveyard(PlayerFilter::You),
            operator: ironsmith_core::ValueComparisonOperator::GreaterThanOrEqual,
            right: Value::Fixed(7),
        }
    )
    .then_some("Threshold")
}

/// Recombine the layer-correct static siblings emitted for a Threshold source
/// modifier. Every semantic fact is proven from compiled payloads: the source
/// gets exactly +1/+1, becomes black, and gains exactly one activated ability
/// under the same seven-card graveyard gate.
fn describe_structural_threshold_source_modifier_bundle(
    abilities: &[Ability],
    subject: &str,
) -> Option<(String, usize)> {
    let [anthem_ability, color_ability, grant_ability, ..] = abilities else {
        return None;
    };
    if anthem_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || color_ability.functional_zones != anthem_ability.functional_zones
        || grant_ability.functional_zones != anthem_ability.functional_zones
    {
        return None;
    }

    let AbilityKind::Static(anthem_static) = &anthem_ability.kind else {
        return None;
    };
    let AbilityKind::Static(color_static) = &color_ability.kind else {
        return None;
    };
    let AbilityKind::Static(grant_static) = &grant_ability.kind else {
        return None;
    };
    if anthem_static.id() != crate::static_abilities::StaticAbilityId::Anthem
        || color_static.id() != crate::static_abilities::StaticAbilityId::SetColors
        || grant_static.id()
            != crate::static_abilities::StaticAbilityId::GrantObjectAbilityForFilter
    {
        return None;
    }

    let ironsmith_core::StaticAbilityPayload::Anthem(anthem) =
        &anthem_static.compiled_model()?.payload
    else {
        return None;
    };
    let (ironsmith_core::AnthemValue::Fixed(power), ironsmith_core::AnthemValue::Fixed(toughness)) =
        (&anthem.power, &anthem.toughness)
    else {
        return None;
    };
    if anthem.filter.is_some()
        || (*power, *toughness) != (1, 1)
        || anthem.set_quantifier_surface.is_some()
        || anthem.count_uses_where_x
        || anthem.additional_surface
        || anthem.replacement_surface.is_some()
    {
        return None;
    }
    let condition = anthem.condition.as_ref()?;
    let presentation_label = threshold_graveyard_presentation_label(condition)?;

    let color_model = color_static.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: inner_color,
        condition: color_condition,
    } = &color_model.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::SetColors {
        filter: color_filter,
        colors,
    } = &inner_color.payload
    else {
        return None;
    };
    if inner_color.id != Some(crate::static_abilities::StaticAbilityId::SetColors)
        || *colors != crate::color::ColorSet::BLACK
        || color_condition != condition
    {
        return None;
    }

    let grant_model = grant_static.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) =
        &grant_model.payload
    else {
        return None;
    };
    if color_filter != &grant.filter
        || grant.filter != ObjectFilter::source()
        || grant.condition.as_ref() != Some(condition)
        || !grant.additional_abilities.is_empty()
        || grant.set_quantifier_surface.is_some()
        || grant.ability.functional_zones.as_slice() != [Zone::Battlefield]
        || !matches!(
            &grant.ability.kind,
            ironsmith_core::AbilityKind::Activated(_)
        )
    {
        return None;
    }

    let granted = grant_static.source_granted_inline_abilities();
    let [granted] = granted.as_slice() else {
        return None;
    };
    if !matches!(&granted.kind, AbilityKind::Activated(_)) {
        return None;
    }
    let mut granted_text = describe_inline_ability_with_self_subject(granted, subject)
        .trim()
        .trim_end_matches('.')
        .to_string();
    if granted_text.is_empty() {
        return None;
    }
    if !granted_text.ends_with('!') && !granted_text.ends_with('?') {
        granted_text.push('.');
    }

    Some((
        format!(
            "{presentation_label} — As long as there are seven or more cards in your graveyard, {subject} gets +1/+1, is black, and has \"{granted_text}\""
        ),
        3,
    ))
}

fn modeled_keyword_count_condition(
    condition: &Condition,
    keyword: crate::static_abilities::StaticAbilityId,
) -> Option<(ObjectFilter, &str)> {
    let Condition::CountComparison {
        count: ironsmith_core::AnthemCountExpression::MatchingFilter(filter),
        comparison: Comparison::GreaterThanOrEqual(1),
        display: Some(display),
    } = condition
    else {
        return None;
    };
    if filter.static_abilities.as_slice() != [keyword]
        || !filter.excluded_static_abilities.is_empty()
        || !filter.ability_markers.is_empty()
        || !filter.excluded_ability_markers.is_empty()
    {
        return None;
    }
    let mut basis = filter.clone();
    basis.static_abilities.clear();
    Some((basis, display.trim()))
}

fn modeled_removed_draft_keyword_condition(
    condition: &Condition,
    keyword: crate::static_abilities::StaticAbilityId,
) -> Option<(PlayerFilter, ObjectFilter, &str)> {
    let Condition::PlayerRemovedDraftCardMatching {
        player,
        filter,
        with_cards_named,
    } = condition
    else {
        return None;
    };
    if filter.static_abilities.as_slice() != [keyword]
        || !filter.excluded_static_abilities.is_empty()
        || !filter.ability_markers.is_empty()
        || !filter.excluded_ability_markers.is_empty()
    {
        return None;
    }
    let mut basis = filter.clone();
    basis.static_abilities.clear();
    Some((player.clone(), basis, with_cards_named.trim()))
}

/// Render a structurally complete removed-from-draft keyword family using the
/// authored "The same is true" surface. Every grant remains independently
/// executable; this only rejoins adjacent grants proven to share provenance.
fn describe_structural_removed_draft_keyword_ladder(
    abilities: &[Ability],
    subject: &str,
) -> Option<(String, usize)> {
    let mut keywords = Vec::new();
    let mut shared_player = None;
    let mut shared_filter_basis: Option<ObjectFilter> = None;
    let mut shared_group_name: Option<String> = None;
    let mut first_condition = None;
    let mut consumed = 0usize;

    for ability in abilities {
        let Some((keyword_id, keyword, condition)) = modeled_source_keyword_grant(ability) else {
            break;
        };
        let Some((player, filter_basis, group_name)) =
            modeled_removed_draft_keyword_condition(condition, keyword_id)
        else {
            break;
        };
        if let Some(expected) = shared_player.as_ref() {
            if expected != &player {
                break;
            }
        } else {
            shared_player = Some(player.clone());
            shared_filter_basis = Some(filter_basis.clone());
            shared_group_name = Some(group_name.to_string());
            first_condition = Some(describe_condition(condition));
        }
        if shared_filter_basis.as_ref() != Some(&filter_basis)
            || shared_group_name.as_deref() != Some(group_name)
        {
            break;
        }
        keywords.push(keyword);
        consumed += 1;
    }

    if consumed < 3 {
        return None;
    }
    Some((
        format!(
            "If {}, {subject} has {}. The same is true for {}",
            first_condition?,
            keywords[0],
            render_keyword_list(&keywords[1..], false),
        ),
        consumed,
    ))
}

fn normalize_same_true_condition_statement(condition: &str, keyword: &str) -> String {
    let condition = condition.trim().trim_end_matches('.');
    if let Some(existence) = condition.strip_prefix("there is ") {
        let keyword_suffix = format!(" with {keyword}");
        if let Some(subject) = existence.strip_suffix(&keyword_suffix) {
            return format!("{subject} has {keyword}");
        }
        if let Some(subject) = existence.strip_suffix(" in a graveyard")
            && subject.ends_with(&keyword_suffix)
        {
            return format!("{subject} is in a graveyard");
        }
    }
    if let Some(subject) = condition.strip_prefix("your opponents control ") {
        return format!("an opponent controls {subject}");
    }
    condition.to_string()
}

/// Render a structurally complete family of source keyword grants whose
/// conditions differ only by requiring the same keyword on matching cards.
///
/// This is the executable shape behind "The same is true for ..." ladders.
/// Requiring one independent typed grant and one independent matching-filter
/// condition per keyword prevents a malformed, concatenated condition from
/// being made to look complete by the renderer.
fn describe_structural_keyword_same_is_true_ladder(
    abilities: &[Ability],
    subject: &str,
) -> Option<(String, usize)> {
    let mut keywords = Vec::new();
    let mut shared_condition_basis: Option<ObjectFilter> = None;
    let mut first_condition = None;
    let mut consumed = 0usize;

    for ability in abilities {
        let Some((keyword_id, keyword, condition)) = modeled_source_keyword_grant(ability) else {
            break;
        };
        let Some((condition_basis, condition_surface)) =
            modeled_keyword_count_condition(condition, keyword_id)
        else {
            break;
        };
        if let Some(shared) = &shared_condition_basis {
            if shared != &condition_basis {
                break;
            }
        } else {
            shared_condition_basis = Some(condition_basis);
            first_condition = Some(condition_surface.to_string());
        }
        keywords.push(keyword);
        consumed += 1;
    }

    if consumed < 3 {
        return None;
    }
    let first_keyword = &keywords[0];
    let condition =
        normalize_same_true_condition_statement(first_condition.as_deref()?, first_keyword);
    Some((
        format!(
            "As long as {condition}, {subject} has {first_keyword}. The same is true for {}",
            render_keyword_list(&keywords[1..], false),
        ),
        consumed,
    ))
}

fn modeled_all_subtypes_of_family_surface(
    ability: &Ability,
) -> Option<(&ObjectFilter, ironsmith_core::SubtypeFamily, String)> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::AddAllSubtypesOfFamily { filter, family } =
        &model.payload
    else {
        return None;
    };
    let display = static_ability.display();
    let (subject, predicate, _) = split_static_predicate_with_verb(&display, &[" is ", " are "])?;
    if predicate != format!("every {}", family.type_phrase()) {
        return None;
    }
    Some((filter, *family, subject.to_string()))
}

fn owned_nonbattlefield_card_union_subject(filter: &ObjectFilter) -> Option<String> {
    ironsmith_core::filter_model::describe_owned_nonbattlefield_card_union(filter)
}

/// Rejoin the typed battlefield/spell/nonbattlefield-card scopes of a shared
/// all-subtypes grant. The three filters retain distinct runtime zones while
/// the identical subtype family proves the "same is true" relationship.
fn describe_structural_all_subtypes_scope_ladder(abilities: &[Ability]) -> Option<(String, usize)> {
    // "The same is true" starts a second authored static sentence. PR-33's
    // source-line provenance marker therefore sits between the first scope
    // and the two shared-predicate scopes; bridge only that exact 1 + marker
    // + 2 layout before applying the ordinary three-member matcher.
    if let [first, marker, second, third, ..] = abilities
        && source_line_static_group_count(marker) == Some(2)
    {
        let flattened = vec![first.clone(), second.clone(), third.clone()];
        let (text, consumed) = describe_structural_all_subtypes_scope_ladder(&flattened)?;
        return (consumed == 3).then_some((text, 4));
    }
    let [battlefield, stack, nonbattlefield, ..] = abilities else {
        return None;
    };
    let (battlefield_filter, family, battlefield_subject) =
        modeled_all_subtypes_of_family_surface(battlefield)?;
    let (stack_filter, stack_family, stack_subject) =
        modeled_all_subtypes_of_family_surface(stack)?;
    let (nonbattlefield_filter, nonbattlefield_family, _) =
        modeled_all_subtypes_of_family_surface(nonbattlefield)?;
    if family != stack_family
        || family != nonbattlefield_family
        || battlefield_filter.zone != Some(Zone::Battlefield)
        || battlefield_filter.controller != Some(PlayerFilter::You)
        || stack_filter.zone != Some(Zone::Stack)
        || stack_filter.controller != Some(PlayerFilter::You)
        || stack_filter.stack_kind != Some(crate::filter::StackObjectKind::Spell)
        || battlefield_filter.card_types != stack_filter.card_types
    {
        return None;
    }
    let nonbattlefield_subject = owned_nonbattlefield_card_union_subject(nonbattlefield_filter)?;
    let first = format!("{battlefield_subject} are every {}", family.type_phrase());
    Some((
        format!(
            "{first}. The same is true for {} and {}",
            lowercase_first(&stack_subject),
            lowercase_first(&nonbattlefield_subject),
        ),
        3,
    ))
}

#[cfg(test)]
mod all_subtypes_scope_ladder_tests {
    use super::*;

    #[test]
    fn battlefield_spell_and_owned_card_scopes_rejoin_with_same_is_true() {
        let oracle = "Creatures you control are every creature type. The same is true for creature spells you control and creature cards you own that aren't on the battlefield.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "All Subtypes Scope Probe",
        )
        .card_types(vec![CardType::Artifact])
        .parse_text(oracle)
        .expect("all-subtypes scope ladder should compile");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![oracle.to_string()],
            "{:#?}",
            definition.abilities,
        );
    }
}

fn modeled_static_variant_copy(
    ability: &Ability,
) -> Option<&ironsmith_core::CopyStaticAbilityVariants> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::CopyStaticAbilityVariants(copy) = &model.payload
    else {
        return None;
    };
    Some(copy)
}

fn static_variant_selector_keyword(
    selector: ironsmith_core::StaticAbilityVariantSelector,
) -> Option<&'static str> {
    use crate::static_abilities::StaticAbilityId::*;
    use ironsmith_core::StaticAbilityVariantSelector::{Any, ProtectionFromColor};

    match selector {
        Any(Flying) => Some("flying"),
        Any(Fear) => Some("fear"),
        Any(FirstStrike) => Some("first strike"),
        Any(DoubleStrike) => Some("double strike"),
        Any(Deathtouch) => Some("deathtouch"),
        Any(Haste) => Some("haste"),
        Any(Landwalk) => Some("landwalk"),
        Any(Lifelink) => Some("lifelink"),
        Any(Protection) => Some("protection"),
        ProtectionFromColor => Some("protection from any color"),
        Any(Reach) => Some("reach"),
        Any(Trample) => Some("trample"),
        Any(Shroud) => Some("shroud"),
        Any(Vigilance) => Some("vigilance"),
        Any(Hexproof) | Any(HexproofFrom) => Some("hexproof"),
        Any(Indestructible) => Some("indestructible"),
        Any(Menace) => Some("menace"),
        Any(Shadow) => Some("shadow"),
        Any(Skulk) => Some("skulk"),
        _ => None,
    }
}

/// Rejoin a leading conditional keyword grant with the payload-preserving
/// variant-copy carrier for the remaining "The same is true" branches.
///
/// Delve-linked exile is executable provenance: the leading grant and every
/// copied variant must inspect the same source-exiled object filter, and the
/// source must actually have delve. This avoids collapsing unrelated keyword
/// grants that merely happen to be adjacent.
fn describe_structural_delve_keyword_variant_ladder(
    abilities: &[Ability],
    subject: &str,
    source_has_delve: bool,
) -> Option<(String, usize)> {
    if !source_has_delve {
        return None;
    }
    let [leading, variants, ..] = abilities else {
        return None;
    };
    let (leading_id, leading_keyword, condition) = modeled_source_keyword_grant(leading)?;
    let (condition_basis, _) = modeled_keyword_count_condition(condition, leading_id)?;
    let copy = modeled_static_variant_copy(variants)?;
    if !copy.exclude_source_id
        || copy.filter != condition_basis
        || !copy.filter.tagged_constraints.iter().any(|constraint| {
            constraint.tag.as_str() == crate::tag::SOURCE_EXILED_TAG
                && constraint.relation == crate::filter::TaggedOpbjectRelation::IsTaggedObject
        })
        || !copy
            .display
            .to_ascii_lowercase()
            .contains("the same is true for")
    {
        return None;
    }

    let mut trailing_keywords = Vec::new();
    for selector in &copy.selectors {
        let keyword = static_variant_selector_keyword(*selector)?.to_string();
        if keyword != leading_keyword && !trailing_keywords.contains(&keyword) {
            trailing_keywords.push(keyword);
        }
    }
    if trailing_keywords.len() < 2 {
        return None;
    }

    let mut noun_filter = copy.filter.clone();
    noun_filter.zone = None;
    noun_filter.tagged_constraints.retain(|constraint| {
        constraint.tag.as_str() != crate::tag::SOURCE_EXILED_TAG
            || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
    });
    let noun_description = noun_filter.description();
    let noun = strip_leading_article(&noun_description);
    Some((
        format!(
            "If {} with {leading_keyword} was exiled with {subject}'s delve ability, {subject} has {leading_keyword}. The same is true for {}",
            with_indefinite_article(noun),
            render_keyword_list(&trailing_keywords, false),
        ),
        2,
    ))
}

fn modeled_basic_land_count_condition(condition: &Condition) -> Option<(Subtype, &str)> {
    let Condition::CountComparison {
        count: ironsmith_core::AnthemCountExpression::MatchingFilter(filter),
        comparison: Comparison::GreaterThanOrEqual(1),
        display: Some(display),
    } = condition
    else {
        return None;
    };
    let [subtype] = filter.subtypes.as_slice() else {
        return None;
    };
    if filter.controller != Some(PlayerFilter::You)
        || !matches!(
            subtype,
            Subtype::Plains
                | Subtype::Island
                | Subtype::Swamp
                | Subtype::Mountain
                | Subtype::Forest
        )
    {
        return None;
    }
    let mut basis = filter.clone();
    basis.controller = None;
    basis.subtypes.clear();
    if basis != ObjectFilter::default() {
        return None;
    }
    Some((*subtype, display.trim()))
}

fn modeled_basic_land_source_modifier(ability: &Ability) -> Option<(Subtype, String)> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    if let ironsmith_core::StaticAbilityPayload::Anthem(anthem) = &model.payload {
        if anthem.filter.is_some()
            || anthem.set_quantifier_surface.is_some()
            || anthem.count_uses_where_x
            || anthem.additional_surface
            || anthem.replacement_surface.is_some()
        {
            return None;
        }
        let (
            ironsmith_core::AnthemValue::Fixed(power),
            ironsmith_core::AnthemValue::Fixed(toughness),
        ) = (&anthem.power, &anthem.toughness)
        else {
            return None;
        };
        if *power == 0 && *toughness == 0 {
            return None;
        }
        let (subtype, condition) = modeled_basic_land_count_condition(anthem.condition.as_ref()?)?;
        return Some((
            subtype,
            format!("gets {power:+}/{toughness:+} as long as {condition}"),
        ));
    }

    let (_, keyword, condition) = modeled_source_keyword_grant(ability)?;
    let (subtype, condition) = modeled_basic_land_count_condition(condition)?;
    Some((subtype, format!("has {keyword} as long as {condition}")))
}

/// Combine the full five-basic-land source-modifier ladder while retaining
/// each independent typed condition and executable modifier.
fn describe_structural_five_basic_land_modifier_ladder(
    abilities: &[Ability],
    subject: &str,
) -> Option<(String, usize)> {
    let mut items = Vec::new();
    for ability in abilities.iter().take(6) {
        let Some(item) = modeled_basic_land_source_modifier(ability) else {
            break;
        };
        items.push(item);
    }
    if items.len() != 5 {
        return None;
    }
    for required in [
        Subtype::Plains,
        Subtype::Island,
        Subtype::Swamp,
        Subtype::Mountain,
        Subtype::Forest,
    ] {
        if items
            .iter()
            .filter(|(subtype, _)| *subtype == required)
            .count()
            != 1
        {
            return None;
        }
    }
    let predicates = items
        .iter()
        .map(|(_, predicate)| predicate.clone())
        .collect::<Vec<_>>();
    Some((
        format!(
            "{} {}",
            capitalize_first(subject),
            join_english_list(&predicates)
        ),
        5,
    ))
}

#[derive(Clone, Copy)]
enum CrossSegmentResultEffect<'a> {
    If(&'a crate::effects::IfEffect),
    Reflexive(&'a crate::effects::ReflexiveTriggerEffect),
    ForPlayers(&'a crate::effects::ForPlayersEffect),
}

fn cross_segment_result_effect(effect: &Effect) -> Option<CrossSegmentResultEffect<'_>> {
    if let Some(if_effect) = effect.downcast_ref::<crate::effects::IfEffect>() {
        return Some(CrossSegmentResultEffect::If(if_effect));
    }
    if let Some(reflexive) = effect.downcast_ref::<crate::effects::ReflexiveTriggerEffect>() {
        return Some(CrossSegmentResultEffect::Reflexive(reflexive));
    }
    if let Some(for_players) = effect.downcast_ref::<crate::effects::ForPlayersEffect>() {
        return Some(CrossSegmentResultEffect::ForPlayers(for_players));
    }
    effect
        .downcast_ref::<crate::effects::WithIdEffect>()?
        .effect
        .downcast_ref::<crate::effects::IfEffect>()
        .map(CrossSegmentResultEffect::If)
}

/// Rejoin an optional source transformation with the graveyard return that
/// follows it when authored sentence preservation placed the two instructions
/// in adjacent resolution segments. The established flat specialist validates
/// every sticker, Aura, choice-tag, return, and attachment relationship.
fn describe_cross_segment_optional_sticker_aura_return_attach_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [transform_segment, return_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !transform_segment.self_replacements.is_empty()
        || !return_segment.self_replacements.is_empty()
    {
        return None;
    }

    let [tag_triggering, may_transform] = transform_segment.default_effects.as_slice() else {
        return None;
    };
    tag_triggering.downcast_ref::<crate::effects::TagTriggeringObjectEffect>()?;
    may_transform.downcast_ref::<crate::effects::MayEffect>()?;

    let [return_sequence] = return_segment.default_effects.as_slice() else {
        return None;
    };
    let return_sequence = return_sequence.downcast_ref::<crate::effects::SequenceEffect>()?;
    if !matches!(
        return_sequence.surface,
        ironsmith_core::SequenceSurface::Coordinated
    ) {
        return None;
    }

    let mut effects = vec![tag_triggering, may_transform];
    effects.extend(return_sequence.effects.iter());
    let rendered = describe_optional_sticker_aura_return_attach_sequence(&effects)?;
    Some((rendered, 2))
}

/// Rejoin a returned permanent with the one-shot ETB watcher registered by
/// the following sentence. The structural matcher retains the exact result
/// tag and rejects broader delayed triggers, plural returns, and unrelated
/// payloads.
fn describe_cross_segment_return_then_linked_enter_return_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [return_segment, delayed_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !return_segment.self_replacements.is_empty()
        || !delayed_segment.self_replacements.is_empty()
        || return_segment.starts_new_source_line
        || delayed_segment.starts_new_source_line
    {
        return None;
    }
    let [return_effect] = return_segment.default_effects.as_slice() else {
        return None;
    };
    let [delayed_effect] = delayed_segment.default_effects.as_slice() else {
        return None;
    };
    describe_return_then_linked_enter_return_pair(return_effect, delayed_effect)
        .map(|rendered| (rendered, 2))
}

#[cfg(test)]
mod linked_return_enter_window_tests {
    use super::*;

    fn permanent_card_filter(owner: PlayerFilter, enters_under_controller: bool) -> ObjectFilter {
        let mut filter = ObjectFilter::default();
        filter.zone = Some(Zone::Graveyard);
        filter.owner = Some(owner);
        filter.card_types = vec![
            CardType::Artifact,
            CardType::Creature,
            CardType::Enchantment,
            CardType::Land,
            CardType::Planeswalker,
            CardType::Battle,
        ];
        filter.set_explicit_card_noun(true);
        filter.set_enters_under_controller_surface(enters_under_controller);
        filter
    }

    fn linked_return_segments(
        watched_tag: &str,
        enters_under_controller: bool,
        payload_count: ChoiceCount,
    ) -> Vec<crate::resolution::ResolutionSegment> {
        let returned_tag = crate::TagKey::from("returned_0");
        let returned = Effect::new(crate::effects::ReturnFromGraveyardToBattlefieldEffect::new(
            ChooseSpec::target(ChooseSpec::Object(permanent_card_filter(
                PlayerFilter::Opponent,
                enters_under_controller,
            ))),
            false,
        ))
        .tag(returned_tag.clone());

        let mut payload_filter = permanent_card_filter(PlayerFilter::You, false);
        payload_filter
            .tagged_constraints
            .push(crate::filter::TaggedObjectConstraint {
                tag: returned_tag,
                relation: crate::filter::TaggedOpbjectRelation::ManaValueLteTagged,
            });
        let payload = Effect::new(crate::effects::ReturnFromGraveyardToBattlefieldEffect::new(
            ChooseSpec::target(ChooseSpec::Object(payload_filter)).with_count(payload_count),
            false,
        ))
        .tag("returned_1");
        let trigger = crate::triggers::Trigger::new(
            crate::triggers::ZoneChangeTrigger::new()
                .to(Zone::Battlefield)
                .this()
                .this_surface(crate::target::SourceReferenceSurface::ThisPermanentType(
                    "that permanent".to_string(),
                ))
                .this_subject_number(ironsmith_core::trigger_model::TriggerSubjectNumber::Singular),
        );
        let mut delayed = crate::effects::ScheduleDelayedTriggerEffect::from_tag(
            trigger,
            vec![payload],
            true,
            watched_tag,
            PlayerFilter::You,
        );
        delayed.leading_duration_surface = true;

        vec![
            crate::resolution::ResolutionSegment::from_effects(vec![returned]),
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(delayed)]),
        ]
    }

    #[test]
    fn exact_linked_return_window_restores_authored_etb_sentence() {
        let segments = linked_return_segments("returned_0", true, ChoiceCount::up_to(1));
        assert_eq!(
            describe_cross_segment_return_then_linked_enter_return_window(&segments, 0),
            Some((
                "Return target permanent card in an opponent's graveyard to the battlefield under their control. When that permanent enters, return up to one target permanent card with equal or lesser mana value from your graveyard to the battlefield".to_string(),
                2,
            ))
        );
        assert_eq!(
            describe_resolution_program(&crate::resolution::ResolutionProgram::new(segments)),
            "Return target permanent card in an opponent's graveyard to the battlefield under their control. When that permanent enters, return up to one target permanent card with equal or lesser mana value from your graveyard to the battlefield"
        );
    }

    #[test]
    fn linked_return_window_rejects_provenance_surface_and_cardinality_near_misses() {
        for (case, segments) in [
            linked_return_segments("other_return", true, ChoiceCount::up_to(1)),
            linked_return_segments("returned_0", false, ChoiceCount::up_to(1)),
            linked_return_segments("returned_0", true, ChoiceCount::exactly(1)),
        ]
        .into_iter()
        .enumerate()
        {
            assert_eq!(
                describe_cross_segment_return_then_linked_enter_return_window(&segments, 0),
                None,
                "near-miss case {case}: {segments:#?}"
            );
        }
    }
}

/// Rejoin an authored player choice and its immediately linked action when
/// lowering preserves their sentence boundary as adjacent resolution
/// segments. The structural helper still verifies that the action's actor is
/// the exact player tag produced by the choice.
fn describe_cross_segment_distinct_player_choice_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [choice_segment, action_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !choice_segment.self_replacements.is_empty() || !action_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [choice] = choice_segment.default_effects.as_slice() else {
        return None;
    };
    let [action] = action_segment.default_effects.as_slice() else {
        return None;
    };
    let effects = [choice.clone(), action.clone()];
    describe_distinct_player_choice_with_linked_action(&effects).map(|rendered| (rendered, 2))
}

/// Preserve the authored sentence boundary for a random choice from the
/// source's persistent exile pool followed by the linked owner-hand move.
///
/// The ordinary flat choice/move renderer is intentionally broader and
/// conjoins these effects. This matcher accepts only an exact random-one
/// `SOURCE_EXILED_TAG` selection and a bare move of that same choice tag to
/// its owner's hand, so unrelated two-step choices keep their canonical
/// rendering.
fn describe_cross_segment_random_source_exiled_owner_hand_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [choice_segment, move_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !choice_segment.self_replacements.is_empty() || !move_segment.self_replacements.is_empty() {
        return None;
    }
    let [choice_effect] = choice_segment.default_effects.as_slice() else {
        return None;
    };
    let [move_effect] = move_segment.default_effects.as_slice() else {
        return None;
    };
    let choose = choice_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let move_to_hand = move_effect.downcast_ref::<crate::effects::MoveToZoneEffect>()?;

    let mut expected_filter = ObjectFilter::default().in_zone(Zone::Exile);
    expected_filter.set_explicit_card_noun(true);
    expected_filter
        .tagged_constraints
        .push(crate::filter::TaggedObjectConstraint {
            tag: crate::tag::SOURCE_EXILED_TAG.into(),
            relation: crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        });
    let mut expected_choose = crate::effects::ChooseObjectsEffect::new(
        expected_filter,
        ChoiceCount::exactly(1),
        PlayerFilter::You,
        choose.tag.clone(),
    )
    .in_zone(Zone::Exile);
    expected_choose.count.random = true;
    if choose != &expected_choose {
        return None;
    }

    let expected_move = crate::effects::MoveToZoneEffect::new(
        ChooseSpec::Tagged(choose.tag.clone()),
        Zone::Hand,
        false,
    )
    .with_verb_surface(ironsmith_core::MoveToZoneVerbSurface::Put);
    if move_to_hand != &expected_move {
        return None;
    }

    Some((
        format!(
            "Choose a card at random that was {}. Put that card into its owner's hand",
            crate::compiled_text::source_exiled_with_phrase(&choose.filter),
        ),
        2,
    ))
}

#[cfg(test)]
mod random_source_exiled_owner_hand_window_tests {
    use super::*;

    fn matching_segments() -> Vec<crate::resolution::ResolutionSegment> {
        let mut filter = ObjectFilter::default().in_zone(Zone::Exile);
        filter.set_explicit_card_noun(true);
        filter
            .tagged_constraints
            .push(crate::filter::TaggedObjectConstraint {
                tag: crate::tag::SOURCE_EXILED_TAG.into(),
                relation: crate::filter::TaggedOpbjectRelation::IsTaggedObject,
            });
        let mut count = ChoiceCount::exactly(1);
        count.random = true;
        let choose = crate::effects::ChooseObjectsEffect::new(
            filter,
            count,
            PlayerFilter::You,
            "skyship_choice",
        )
        .in_zone(Zone::Exile);
        let move_to_hand = crate::effects::MoveToZoneEffect::new(
            ChooseSpec::Tagged(TagKey::from("skyship_choice")),
            Zone::Hand,
            false,
        )
        .with_verb_surface(ironsmith_core::MoveToZoneVerbSurface::Put);
        vec![
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(choose)]),
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(move_to_hand)]),
        ]
    }

    #[test]
    fn random_source_exiled_choice_keeps_owner_hand_as_a_new_sentence() {
        let segments = matching_segments();
        assert_eq!(
            describe_cross_segment_random_source_exiled_owner_hand_window(&segments, 0),
            Some((
                "Choose a card at random that was exiled with this source. Put that card into its owner's hand"
                    .to_string(),
                2,
            )),
        );

        let program = crate::resolution::ResolutionProgram::new(segments);
        assert_eq!(
            describe_resolution_program(&program),
            "Choose a card at random that was exiled with this source. Put that card into its owner's hand"
        );
    }

    #[test]
    fn nonrandom_or_mismatched_choices_do_not_use_the_specialist() {
        let mut nonrandom = matching_segments();
        let choose = nonrandom[0].default_effects[0]
            .downcast_ref::<crate::effects::ChooseObjectsEffect>()
            .expect("choice effect")
            .clone();
        let mut choose = choose;
        choose.count.random = false;
        nonrandom[0].default_effects[0] = Effect::new(choose);
        assert_eq!(
            describe_cross_segment_random_source_exiled_owner_hand_window(&nonrandom, 0),
            None,
        );

        let mut mismatched = matching_segments();
        mismatched[1].default_effects[0] = Effect::new(
            crate::effects::MoveToZoneEffect::new(
                ChooseSpec::Tagged(TagKey::from("other_choice")),
                Zone::Hand,
                false,
            )
            .with_verb_surface(ironsmith_core::MoveToZoneVerbSurface::Put),
        );
        assert_eq!(
            describe_cross_segment_random_source_exiled_owner_hand_window(&mismatched, 0),
            None,
        );
    }

    #[test]
    fn parsed_source_linked_choice_keeps_typed_program_and_exact_surface() {
        let oracle = "When Skyship Weatherlight enters, search your library for any number of artifact and/or creature cards, exile them, then shuffle.\n{4}, {T}: Choose a card at random that was exiled with Skyship Weatherlight. Put that card into its owner's hand.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Skyship Weatherlight",
        )
        .supertypes(vec![Supertype::Legendary])
        .card_types(vec![CardType::Artifact])
        .parse_text(oracle)
        .expect("source-linked random exile choice should compile");
        let activated = definition
            .abilities
            .iter()
            .find_map(|ability| match &ability.kind {
                AbilityKind::Activated(activated) => Some(activated),
                _ => None,
            })
            .expect("activated ability");
        let [choose_segment, move_segment] = activated.effects.segments.as_slice() else {
            panic!("expected the authored two-sentence program");
        };
        let [choose_effect] = choose_segment.default_effects.as_slice() else {
            panic!("expected one choice effect");
        };
        let choose = choose_effect
            .downcast_ref::<crate::effects::ChooseObjectsEffect>()
            .expect("typed choice effect");
        assert!(choose.count.random);
        assert_eq!(choose.count.min, 1);
        assert_eq!(choose.count.max, Some(1));
        assert!(choose.filter.tagged_constraints.iter().any(|constraint| {
            constraint.tag.as_str() == crate::tag::SOURCE_EXILED_TAG
                && constraint.relation == crate::filter::TaggedOpbjectRelation::IsTaggedObject
        }));
        let [move_effect] = move_segment.default_effects.as_slice() else {
            panic!("expected one owner-hand move");
        };
        let move_to_hand = move_effect
            .downcast_ref::<crate::effects::MoveToZoneEffect>()
            .expect("typed move effect");
        assert!(matches!(
            move_to_hand.target.base(),
            ChooseSpec::Tagged(tag) if tag == &choose.tag
        ));
        assert_eq!(move_to_hand.zone, Zone::Hand);

        let rendered = crate::compiled_text::compiled_text_lines(&definition);
        assert_eq!(
            rendered.last().map(String::as_str),
            Some(
                "{4}, {T}: Choose a card at random that was exiled with Skyship Weatherlight. Put that card into its owner's hand."
            )
        );
    }
}

/// Rejoin a per-opponent bottom-library exile with its following persistent
/// look/cast permission when authored sentence preservation split the two
/// clauses into adjacent resolution segments. The established flat matcher
/// remains the semantic authority for the exact player, tag, zone, duration,
/// permanent-spell filter, face-down, and mana-spend relationships.
fn describe_cross_segment_bottom_library_exile_look_cast_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [exile_segment, permission_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !exile_segment.self_replacements.is_empty()
        || !permission_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [for_players_effect] = exile_segment.default_effects.as_slice() else {
        return None;
    };
    let [look_effect, grant_effect] = permission_segment.default_effects.as_slice() else {
        return None;
    };
    let for_players = for_players_effect.downcast_ref::<crate::effects::ForPlayersEffect>()?;
    let look = look_effect.downcast_ref::<crate::effects::LookAtObjectsEffect>()?;
    let grant = grant_effect.downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
    describe_for_players_bottom_library_exile_then_look_cast(for_players, look, grant)
        .map(|rendered| (rendered, 2))
}

#[cfg(test)]
mod bottom_library_exile_look_cast_window_tests {
    use super::*;

    fn segments_with_tags(
        collection_tag: TagKey,
        permission_tag: TagKey,
    ) -> Vec<crate::resolution::ResolutionSegment> {
        let library_filter = ObjectFilter::default().in_zone(Zone::Library);
        let choose = crate::effects::ChooseObjectsEffect::new(
            library_filter,
            ChoiceCount::exactly(1),
            PlayerFilter::IteratedPlayer,
            collection_tag.clone(),
        )
        .in_zone(Zone::Library)
        .bottom_only();
        let exile = Effect::new(
            crate::effects::ExileEffect::with_spec(ChooseSpec::Tagged(collection_tag.clone()))
                .with_face_down(true),
        )
        .tag(collection_tag.clone());
        let for_players = Effect::new(crate::effects::ForPlayersEffect::new(
            PlayerFilter::Opponent,
            vec![Effect::new(choose), exile],
        ));

        let look_filter = ObjectFilter::default()
            .in_zone(Zone::Exile)
            .controlled_by(PlayerFilter::You)
            .match_tagged(
                collection_tag,
                crate::filter::TaggedOpbjectRelation::IsTaggedObject,
            );
        let look = Effect::new(crate::effects::LookAtObjectsEffect::new(
            look_filter,
            PlayerFilter::You,
            PlayerFilter::You,
        ));
        let mut permanent_spells = ObjectFilter::default();
        permanent_spells.card_types = vec![
            CardType::Artifact,
            CardType::Creature,
            CardType::Enchantment,
            CardType::Planeswalker,
            CardType::Battle,
        ];
        let grant = Effect::new(
            crate::effects::GrantPlayTaggedEffect::new(
                permission_tag,
                PlayerFilter::You,
                crate::effects::GrantPlayTaggedDuration::ForAsLongAsExiled,
                false,
                true,
            )
            .with_filter(permanent_spells),
        );

        vec![
            crate::resolution::ResolutionSegment::from_effects(vec![for_players]),
            crate::resolution::ResolutionSegment::from_effects(vec![look, grant]),
        ]
    }

    #[test]
    fn rejoins_exact_bottom_library_collection_and_plural_permission() {
        let tag = TagKey::from("exiled_collection");
        let segments = segments_with_tags(tag.clone(), tag);

        assert_eq!(
            describe_cross_segment_bottom_library_exile_look_cast_window(&segments, 0),
            Some((
                "Exile the bottom card of each opponent's library face down. For as long as those cards remain exiled, you may look at them, you may cast permanent spells from among them, and you may spend mana as though it were mana of any color to cast those spells".to_string(),
                2,
            ))
        );
        let program = crate::resolution::ResolutionProgram::new(segments);
        assert_eq!(
            describe_resolution_program(&program),
            "Exile the bottom card of each opponent's library face down. For as long as those cards remain exiled, you may look at them, you may cast permanent spells from among them, and you may spend mana as though it were mana of any color to cast those spells"
        );
    }

    #[test]
    fn rejects_mismatched_permission_tag_and_replacement_bearing_window() {
        let collection_tag = TagKey::from("exiled_collection");
        let wrong_tag = TagKey::from("unrelated_collection");
        let mismatched = segments_with_tags(collection_tag.clone(), wrong_tag);
        assert_eq!(
            describe_cross_segment_bottom_library_exile_look_cast_window(&mismatched, 0),
            None
        );

        let mut replacement_bearing = segments_with_tags(collection_tag.clone(), collection_tag);
        replacement_bearing[1].self_replacements.push(
            crate::resolution::SelfReplacementBranch::new(Condition::YourTurn, Vec::new()),
        );
        assert_eq!(
            describe_cross_segment_bottom_library_exile_look_cast_window(&replacement_bearing, 0,),
            None
        );
    }
}

fn describe_cross_segment_look_optional_payment_disposition_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let first_segment = segments.get(start)?;
    if !first_segment.self_replacements.is_empty() {
        return None;
    }
    let (look_effect, payment_effect, accepted_effect, declined_effect, consumed) = if let [
        look_effect,
        payment_effect,
        accepted_effect,
        declined_effect,
    ] =
        first_segment.default_effects.as_slice()
    {
        (
            look_effect,
            payment_effect,
            accepted_effect,
            declined_effect,
            1,
        )
    } else {
        let [
            look_segment,
            payment_segment,
            accepted_segment,
            declined_segment,
        ] = segments.get(start..start + 4)?
        else {
            return None;
        };
        if [
            look_segment,
            payment_segment,
            accepted_segment,
            declined_segment,
        ]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
        {
            return None;
        }
        let [look_effect] = look_segment.default_effects.as_slice() else {
            return None;
        };
        let [payment_effect] = payment_segment.default_effects.as_slice() else {
            return None;
        };
        let [accepted_effect] = accepted_segment.default_effects.as_slice() else {
            return None;
        };
        let [declined_effect] = declined_segment.default_effects.as_slice() else {
            return None;
        };
        (
            look_effect,
            payment_effect,
            accepted_effect,
            declined_effect,
            4,
        )
    };

    let look = unwrap_basic_render_wrapper(look_effect)
        .downcast_ref::<crate::effects::LookAtTopCardsEffect>()?;
    if look.player != PlayerFilter::You
        || look.reveal
        || !matches!(look.count.unhinted(), Value::Fixed(count) if *count >= 2)
    {
        return None;
    }

    let payment_with_id = payment_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let may = payment_with_id
        .effect
        .downcast_ref::<crate::effects::MayEffect>()?;
    if may.fallback != crate::decision::FallbackStrategy::Decline
        || !matches!(may.decider.as_ref(), None | Some(PlayerFilter::You))
    {
        return None;
    }

    fn result_if(effect: &Effect) -> Option<&crate::effects::IfEffect> {
        unwrap_basic_render_wrapper(effect).downcast_ref::<crate::effects::IfEffect>()
    }

    let accepted = result_if(accepted_effect)?;
    let declined = result_if(declined_effect)?;
    if accepted.condition != payment_with_id.id
        || declined.condition != payment_with_id.id
        || accepted.predicate != EffectPredicate::Happened
        || !matches!(
            declined.predicate,
            EffectPredicate::DidNotHappen | EffectPredicate::WasDeclined
        )
        || !accepted.else_.is_empty()
        || !declined.else_.is_empty()
    {
        return None;
    }

    fn exact_one_looked_move<'a>(
        effects: &'a [Effect],
        looked_tag: &TagKey,
        zone: Zone,
    ) -> Option<&'a crate::effects::MoveToZoneEffect> {
        let [effect] = effects else {
            return None;
        };
        let move_to_zone = unwrap_basic_render_wrapper(effect)
            .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
        let ChooseSpec::WithCount(target, count) = move_to_zone.target.unhinted() else {
            return None;
        };
        (move_to_zone.zone == zone
            && !move_to_zone.to_top
            && *count == ChoiceCount::exactly(1)
            && matches!(target.base(), ChooseSpec::Tagged(tag) if tag == looked_tag)
            && matches!(
                move_to_zone.actor_surface.as_ref(),
                None | Some(PlayerFilter::You)
            )
            && matches!(
                move_to_zone.destination_player_surface.as_ref(),
                None | Some(PlayerFilter::You)
            ))
        .then_some(move_to_zone)
    }

    exact_one_looked_move(&accepted.then, &look.tag, Zone::Hand)?;
    exact_one_looked_move(&declined.then, &look.tag, Zone::Library)?;

    let look_text = capitalize_first(describe_effect(look_effect).trim().trim_end_matches('.'));
    let payment_text =
        capitalize_first(describe_effect(payment_effect).trim().trim_end_matches('.'));
    Some((
        format!(
            "{look_text}. {payment_text}. If you do, put one of those cards into your hand. If you don't, put one of those cards on the bottom of your library"
        ),
        consumed,
    ))
}

/// Rejoin a will-of-the-council vote with its option conditionals when
/// sentence lowering placed each result sentence in its own resolution
/// segment. The named-vote helper still proves every option/condition edge.
fn describe_cross_segment_named_vote_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let first = segments.get(start)?;
    if !first.self_replacements.is_empty() {
        return None;
    }
    let [vote_effect] = first.default_effects.as_slice() else {
        return None;
    };
    vote_effect.downcast_ref::<crate::effects::VoteEffect>()?;

    let mut effects: Vec<&Effect> = vec![vote_effect];
    let mut end = start + 1;
    while let Some(segment) = segments.get(end) {
        if !segment.self_replacements.is_empty() || segment.default_effects.is_empty() {
            break;
        }
        let all_vote_conditionals = segment.default_effects.iter().all(|effect| {
            effect
                .downcast_ref::<crate::effects::ConditionalEffect>()
                .is_some_and(|conditional| {
                    matches!(
                        &conditional.condition,
                        Condition::VoteOptionGetsMoreVotes(_)
                            | Condition::VoteOptionGetsMoreVotesOrTied(_)
                    )
                })
        });
        if !all_vote_conditionals {
            break;
        }
        effects.extend(segment.default_effects.iter());
        end += 1;
    }
    if end == start + 1 {
        return None;
    }
    let rendered =
        crate::compiled_text::render_effects::describe_planeswalk_chaos_vote_sequence(&effects)
            .or_else(|| {
                crate::compiled_text::render_effects::describe_named_vote_conditional_sequence(
                    &effects,
                )
            })?;
    Some((rendered, end - start))
}

/// Rejoin an authored discard-hand/add-mana/draw sequence when sentence
/// lowering preserved its result references but split the actions into
/// adjacent resolution segments.
fn describe_cross_segment_discard_hand_add_mana_draw_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    // Sentence preservation can keep the authored mana-and-draw clause as a
    // two-child sequence in the segment after the discard. Flatten only that
    // proven shape: continuing the generic three-effect scan would otherwise
    // pull the following vote into this window without ever exposing the
    // sequence's children to the typed renderer.
    if let [discard_segment, mana_draw_segment] = segments.get(start..start + 2)?
        && discard_segment.self_replacements.is_empty()
        && mana_draw_segment.self_replacements.is_empty()
        && let [discard_effect] = discard_segment.default_effects.as_slice()
        && let [sequence_effect] = mana_draw_segment.default_effects.as_slice()
        && let Some(sequence) = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()
        && let [mana_effect, draw_effect] = sequence.effects.as_slice()
    {
        let effects = [discard_effect, mana_effect, draw_effect];
        if let Some(rendered) =
            crate::compiled_text::render_effects::describe_discard_hand_add_mana_draw_sequence(
                &effects,
            )
        {
            return Some((rendered, 2));
        }
    }

    let mut effects = Vec::with_capacity(3);
    let mut end = start;
    while effects.len() < 3 {
        let segment = segments.get(end)?;
        if !segment.self_replacements.is_empty()
            || segment.default_effects.is_empty()
            || effects.len() + segment.default_effects.len() > 3
        {
            return None;
        }
        effects.extend(segment.default_effects.iter());
        end += 1;
    }
    if end == start + 1 {
        return None;
    }
    let rendered =
        crate::compiled_text::render_effects::describe_discard_hand_add_mana_draw_sequence(
            &effects,
        )?;
    Some((rendered, end - start))
}

fn describe_cross_segment_roll_choose_destroy_create_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [roll_segment, destroy_segment, create_segment] = segments.get(start..start + 3)? else {
        return None;
    };
    if [roll_segment, destroy_segment, create_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let [roll_effect] = roll_segment.default_effects.as_slice() else {
        return None;
    };
    let [destroy_effect] = destroy_segment.default_effects.as_slice() else {
        return None;
    };
    let [create_effect] = create_segment.default_effects.as_slice() else {
        return None;
    };
    let create_effect = create_effect
        .downcast_ref::<crate::effects::SequenceEffect>()
        .and_then(|sequence| {
            let [only] = sequence.effects.as_slice() else {
                return None;
            };
            Some(only)
        })
        .unwrap_or(create_effect);
    let effects = [
        roll_effect.clone(),
        destroy_effect.clone(),
        create_effect.clone(),
    ];
    describe_roll_choose_destroy_create_structural(&effects).map(|rendered| (rendered, 3))
}

fn describe_cross_segment_secret_choice_match_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let choice_segment = segments.get(start)?;
    if !choice_segment.self_replacements.is_empty() {
        return None;
    }
    let (choice_effect, conditional_effect, consumed) =
        if let [choice_effect, conditional_effect] = choice_segment.default_effects.as_slice() {
            (choice_effect, conditional_effect, 1)
        } else {
            let conditional_segment = segments.get(start + 1)?;
            if !conditional_segment.self_replacements.is_empty() {
                return None;
            }
            let [choice_effect] = choice_segment.default_effects.as_slice() else {
                return None;
            };
            let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
                return None;
            };
            (choice_effect, conditional_effect, 2)
        };
    let effects = [choice_effect.clone(), conditional_effect.clone()];
    describe_secret_choice_match_sequence(&effects).map(|rendered| (rendered, consumed))
}

fn unwrap_singleton_sentence_boundary(effect: &Effect) -> Option<&Effect> {
    let sequence = effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::SentenceLeadingThen {
        return None;
    }
    let [effect] = sequence.effects.as_slice() else {
        return None;
    };
    Some(effect)
}

fn describe_cross_segment_player_damage_then_discard_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [damage_segment, discard_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !damage_segment.self_replacements.is_empty() || !discard_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [damage_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let [discard_effect] = discard_segment.default_effects.as_slice() else {
        return None;
    };
    let rendered =
        crate::compiled_text::render_effects::describe_player_damage_then_same_player_discards(&[
            damage_effect,
            discard_effect,
        ])?;
    Some((rendered, 2))
}

fn describe_draw_then_discard_unless_condition(condition: &Condition) -> String {
    if let Condition::ValueComparison {
        left,
        operator: crate::effect::ValueComparisonOperator::GreaterThanOrEqual,
        right,
    } = condition
        && let (Value::Count(filter), Value::Fixed(count)) = (left.unhinted(), right.unhinted())
        && *filter
            == ObjectFilter::default()
                .in_zone(Zone::Graveyard)
                .owned_by(PlayerFilter::You)
        && *count >= 0
    {
        let count = u32::try_from(*count)
            .ok()
            .and_then(small_number_word)
            .unwrap_or_else(|| count.to_string());
        return format!("there are {count} or more cards in your graveyard");
    }
    if let Condition::TriggeringSpellManaSpentToCastAtLeast {
        amount,
        symbol: None,
    } = condition
    {
        let amount = small_number_word(*amount).unwrap_or_else(|| amount.to_string());
        return format!("{amount} or more mana was spent to cast that spell");
    }
    describe_condition(condition)
}

fn describe_cross_segment_draw_then_discard_unless_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [draw_segment, discard_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !draw_segment.self_replacements.is_empty() || !discard_segment.self_replacements.is_empty() {
        return None;
    }
    let [draw_effect] = draw_segment.default_effects.as_slice() else {
        return None;
    };
    let draw = draw_effect.downcast_ref::<crate::effects::DrawCardsEffect>()?;
    let [sequence_effect] = discard_segment.default_effects.as_slice() else {
        return None;
    };
    let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::SentenceLeadingThen {
        return None;
    }
    let [conditional_effect] = sequence.effects.as_slice() else {
        return None;
    };
    let conditional = conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    if conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }
    let Condition::Not(positive_condition) = &conditional.condition else {
        return None;
    };
    let [discard_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let discard = discard_effect.downcast_ref::<crate::effects::DiscardEffect>()?;
    let draw_then_discard =
        crate::compiled_text::render_effects::describe_draw_then_discard(draw, discard)?;
    let (draw_clause, discard_clause) = draw_then_discard.split_once(", then ")?;
    let condition = describe_draw_then_discard_unless_condition(positive_condition);

    Some((
        format!("{draw_clause}. Then {discard_clause} unless {condition}"),
        2,
    ))
}

fn describe_cross_segment_return_animation_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [return_segment, animation_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !return_segment.self_replacements.is_empty()
        || !animation_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [return_effect] = return_segment.default_effects.as_slice() else {
        return None;
    };
    let [animation_effect] = animation_segment.default_effects.as_slice() else {
        return None;
    };
    crate::compiled_text::render_effects::describe_returned_battlefield_object_then_animated_pair(
        return_effect,
        animation_effect,
    )
    .map(|rendered| (rendered, 2))
}

/// Rejoin a battlefield return/move with the coordinated color-and-subtype
/// modifications authored in the following sentence. A third source-move
/// segment is retained when it is part of the same typed spell procedure.
fn describe_cross_segment_color_subtype_addition_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [producer_segment, modification_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !modification_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [producer] = producer_segment.default_effects.as_slice() else {
        return None;
    };
    let [modifications] = modification_segment.default_effects.as_slice() else {
        return None;
    };

    if let Some(followup_segment) = segments.get(start + 2)
        && followup_segment.self_replacements.is_empty()
        && let [followup] = followup_segment.default_effects.as_slice()
        && let Some(rendered) =
            crate::compiled_text::render_effects::describe_return_then_color_subtype_addition(&[
                producer,
                modifications,
                followup,
            ])
    {
        return Some((rendered, 3));
    }

    crate::compiled_text::render_effects::describe_return_then_color_subtype_addition(&[
        producer,
        modifications,
    ])
    .or_else(|| {
        crate::compiled_text::render_effects::describe_move_then_color_subtype_addition(&[
            producer,
            modifications,
        ])
    })
    .map(|rendered| (rendered, 2))
}

/// Expose a segmented same-name extraction program to its exact typed
/// renderer. Sequential wrapper effects are expanded only inside this window;
/// the matcher still verifies every chooser, zone, tag, shuffle, and token
/// count relationship before accepting it.
fn describe_cross_segment_necromentia_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let mut effects = Vec::new();
    for end in start..(start + 3).min(segments.len()) {
        let segment = &segments[end];
        if !segment.self_replacements.is_empty() || segment.default_effects.is_empty() {
            return None;
        }
        for effect in &segment.default_effects {
            if let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>() {
                effects.extend(sequence.effects.iter());
            } else {
                effects.push(effect);
            }
        }
        if end > start
            && let Some(rendered) =
                crate::compiled_text::render_effects::render_necromentia_shape(&effects)
        {
            return Some((rendered, end - start + 1));
        }
    }
    None
}

/// Rejoin a typed prior-result gate and its separately lowered `otherwise`
/// branch when both IDs prove one choose-name/mill conditional. Runtime keeps
/// the gate result addressable for the fallback; rendering can recover the
/// authored conditional once the producer tag, both IDs, and both branches
/// agree.
fn describe_cross_segment_choose_name_mill_conditional_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [producer_segment, gate_segment, fallback_segment] = segments.get(start..start + 3)? else {
        return None;
    };
    if [producer_segment, gate_segment, fallback_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty() || segment.starts_new_source_line)
    {
        return None;
    }

    let [producer_effect] = producer_segment.default_effects.as_slice() else {
        return None;
    };
    let [gate_effect] = gate_segment.default_effects.as_slice() else {
        return None;
    };
    let [fallback_effect] = fallback_segment.default_effects.as_slice() else {
        return None;
    };

    // Sentence-preserving lowering now assigns the ID to the whole authored
    // choose/target/mill sequence. Pass the three typed segment effects to the
    // flat specialist instead of rebuilding the older shape in which only the
    // mill carried the ID. That specialist validates both IDs, the chosen-name
    // tag, the target player, and both draw branches.
    let normalized = [
        producer_effect.clone(),
        gate_effect.clone(),
        fallback_effect.clone(),
    ];
    describe_choose_name_target_mills_conditional_draw(&normalized).map(|rendered| (rendered, 3))
}

#[cfg(test)]
mod choose_name_mill_conditional_window_tests {
    use super::*;

    fn predict_segments(
        fallback_condition: crate::effect::EffectId,
    ) -> Vec<crate::resolution::ResolutionSegment> {
        let chosen_name = TagKey::from("__chosen_name__");
        let producer = Effect::with_id(
            0,
            Effect::new(crate::effects::SequenceEffect::comma_then(vec![
                Effect::choose_card_name(PlayerFilter::You, None, chosen_name.clone()),
                Effect::new(crate::effects::TargetOnlyEffect::new(
                    ChooseSpec::target_player(),
                )),
                Effect::mill_player(Value::Fixed(1), PlayerFilter::target_player()).tag("milled_0"),
            ])),
        );

        let mut matching_card = ObjectFilter::default();
        matching_card.set_explicit_card_noun(true);
        matching_card
            .tagged_constraints
            .push(crate::filter::TaggedObjectConstraint {
                tag: chosen_name,
                relation: crate::filter::TaggedOpbjectRelation::SameNameAsTagged,
            });
        let matched = Effect::with_id(
            1,
            Effect::if_then(
                crate::effect::EffectId(0),
                EffectPredicate::PriorEffectResult(crate::effect::PriorEffectResultSurface::new(
                    crate::effect::PriorEffectAction::Milled,
                    matching_card,
                    crate::effect::PriorEffectResultActor::Passive,
                    crate::effect::PriorEffectResultQuantifier::One,
                )),
                vec![Effect::draw(Value::Fixed(2))],
            ),
        );
        let fallback = Effect::if_then(
            fallback_condition,
            EffectPredicate::DidNotHappen,
            vec![Effect::draw(Value::Fixed(1))],
        );

        vec![
            crate::resolution::ResolutionSegment::from_effects(vec![producer]),
            crate::resolution::ResolutionSegment::from_effects(vec![matched]),
            crate::resolution::ResolutionSegment::from_effects(vec![fallback]),
        ]
    }

    #[test]
    fn whole_sequence_producer_rejoins_its_result_and_otherwise_segments() {
        let segments = predict_segments(crate::effect::EffectId(1));

        assert_eq!(
            describe_cross_segment_choose_name_mill_conditional_window(&segments, 0),
            Some((
                "Choose a card name, then target player mills a card. If a card with the chosen name was milled this way, draw two cards. Otherwise, draw a card"
                    .to_string(),
                3,
            ))
        );
    }

    #[test]
    fn otherwise_segment_must_reference_the_matched_result_id() {
        let segments = predict_segments(crate::effect::EffectId(7));

        assert_eq!(
            describe_cross_segment_choose_name_mill_conditional_window(&segments, 0),
            None
        );
    }
}

fn describe_same_target_continuous_outcome_branches(
    accepted_effect: &Effect,
    fallback_effect: &Effect,
) -> Option<(String, String)> {
    let accepted_tagged = accepted_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let fallback_tagged = fallback_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let accepted = accepted_tagged
        .effect
        .downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let fallback = fallback_tagged
        .effect
        .downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let accepted_target = accepted.target_spec.as_ref()?;
    let fallback_target = fallback.target_spec.as_ref()?;
    if accepted_target.unhinted() != fallback_target.unhinted() {
        return None;
    }
    let ChooseSpec::Target(inner) = accepted_target.unhinted() else {
        return None;
    };
    let ChooseSpec::Object(filter) = inner.unhinted() else {
        return None;
    };
    if !filter.card_types.contains(&CardType::Creature) {
        return None;
    }

    let target_text = describe_choose_spec(accepted_target);
    let capitalized_target = capitalize_first(&target_text);
    let accepted_text = describe_effect(accepted_effect);
    let fallback_text = describe_effect(fallback_effect);
    let accepted_tail = accepted_text
        .strip_prefix(&target_text)
        .or_else(|| accepted_text.strip_prefix(&capitalized_target))?;
    let fallback_tail = fallback_text
        .strip_prefix(&target_text)
        .or_else(|| fallback_text.strip_prefix(&capitalized_target))?;
    Some((
        format!("{target_text}{accepted_tail}"),
        format!("that creature{fallback_tail}"),
    ))
}

/// Rejoin an optional action with its two exclusive same-target continuous
/// outcomes. Both branches declare the exact same target spec; the distinct
/// result tags track each branch's modification, not distinct target choices.
fn describe_cross_segment_may_same_target_continuous_outcomes_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [may_segment, accepted_segment, fallback_segment] = segments.get(start..start + 3)? else {
        return None;
    };
    if [may_segment, accepted_segment, fallback_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let [may_effect] = may_segment.default_effects.as_slice() else {
        return None;
    };
    let may_with_id = may_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let may = may_with_id
        .effect
        .downcast_ref::<crate::effects::MayEffect>()?;
    if may.fallback != crate::decision::FallbackStrategy::Decline
        || !matches!(may.decider.as_ref(), None | Some(PlayerFilter::You))
    {
        return None;
    }

    let [accepted_effect] = accepted_segment.default_effects.as_slice() else {
        return None;
    };
    let accepted_with_id = accepted_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let accepted = accepted_with_id
        .effect
        .downcast_ref::<crate::effects::IfEffect>()?;
    let [accepted_branch] = accepted.then.as_slice() else {
        return None;
    };
    let [fallback_effect] = fallback_segment.default_effects.as_slice() else {
        return None;
    };
    let fallback = fallback_effect.downcast_ref::<crate::effects::IfEffect>()?;
    let [fallback_branch] = fallback.then.as_slice() else {
        return None;
    };
    if accepted.condition != may_with_id.id
        || fallback.condition != may_with_id.id
        || accepted.predicate != EffectPredicate::Happened
        || fallback.predicate != EffectPredicate::DidNotHappen
        || !accepted.else_.is_empty()
        || !fallback.else_.is_empty()
    {
        return None;
    }
    let (accepted_text, fallback_text) =
        describe_same_target_continuous_outcome_branches(accepted_branch, fallback_branch)?;
    let may_text = describe_effect(may_effect);
    Some((
        format!(
            "{}. If you do, {accepted_text}. Otherwise, {fallback_text}",
            may_text.trim().trim_end_matches('.')
        ),
        3,
    ))
}

#[cfg(test)]
mod same_target_continuous_outcome_tests {
    use super::*;

    fn tagged_pump(target: ChooseSpec, amount: i32, tag: &str) -> Effect {
        Effect::new(crate::effects::ApplyContinuousEffect::with_spec(
            target,
            crate::continuous::Modification::ModifyPowerToughness {
                power: amount,
                toughness: amount,
            },
            Until::EndOfTurn,
        ))
        .tag(TagKey::from(tag))
    }

    #[test]
    fn outcome_branch_backref_rejects_different_declared_targets() {
        let accepted = tagged_pump(
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature())),
            4,
            "pumped_1",
        );
        let fallback = tagged_pump(
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature().you_control())),
            2,
            "pumped_2",
        );

        assert_eq!(
            describe_same_target_continuous_outcome_branches(&accepted, &fallback),
            None
        );
    }
}

fn describe_cross_segment_result_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    if let Some(rendered) =
        describe_cross_segment_may_same_target_continuous_outcomes_window(segments, start)
    {
        return Some(rendered);
    }
    let antecedent_segment = segments.get(start)?;
    if !antecedent_segment.self_replacements.is_empty() {
        return None;
    }
    let antecedent_effects = &antecedent_segment.default_effects;
    let (antecedent, sentence_leading_then) = if let Some(with_id) = antecedent_effects
        .last()?
        .downcast_ref::<crate::effects::WithIdEffect>(
    ) {
        (with_id, false)
    } else {
        let [boundary_effect] = antecedent_effects.as_slice() else {
            return None;
        };
        let sequence = boundary_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
        if sequence.surface != ironsmith_core::SequenceSurface::SentenceLeadingThen {
            return None;
        }
        let [with_id_effect] = sequence.effects.as_slice() else {
            return None;
        };
        (
            with_id_effect.downcast_ref::<crate::effects::WithIdEffect>()?,
            true,
        )
    };

    let mut end = start + 1;
    let mut saw_result = false;
    let mut saw_reflexive = false;
    while let Some(segment) = segments.get(end) {
        if !segment.self_replacements.is_empty() {
            break;
        }
        let [effect] = segment.default_effects.as_slice() else {
            break;
        };
        let Some(result) = cross_segment_result_effect(effect) else {
            break;
        };
        let matches_antecedent = match result {
            CrossSegmentResultEffect::If(if_effect) => {
                !saw_reflexive
                    && if_effect.condition == antecedent.id
                    && describe_with_id_if_clause(antecedent, if_effect).is_some()
            }
            CrossSegmentResultEffect::Reflexive(reflexive) => {
                !saw_result
                    && reflexive.condition == antecedent.id
                    && describe_with_id_then_reflexive_trigger(antecedent, reflexive).is_some()
            }
            CrossSegmentResultEffect::ForPlayers(for_players) => {
                !saw_result
                    && (describe_with_id_then_for_players_if_happened(antecedent, for_players)
                        .is_some()
                        || describe_with_id_then_for_players_if_didnt(antecedent, for_players)
                            .is_some())
            }
        };
        if !matches_antecedent {
            break;
        }
        saw_result = true;
        saw_reflexive = matches!(result, CrossSegmentResultEffect::Reflexive(_));
        end += 1;
        if saw_reflexive || matches!(result, CrossSegmentResultEffect::ForPlayers(_)) {
            break;
        }
    }
    if !saw_result {
        return None;
    }

    let mut combined = segments[start..end]
        .iter()
        .flat_map(|segment| segment.default_effects.iter().cloned())
        .collect::<Vec<_>>();
    if sentence_leading_then {
        combined[0] = Effect::new(antecedent.clone());
    }
    let rendered = describe_effect_list(&combined);
    let rendered = if sentence_leading_then {
        format!("Then {}", lowercase_first(&rendered))
    } else {
        rendered
    };
    Some((rendered, end - start))
}

#[cfg(test)]
mod sentence_leading_result_window_tests {
    use super::*;

    fn optional_energy_segments(
        result_condition: crate::effect::EffectId,
    ) -> Vec<crate::resolution::ResolutionSegment> {
        let payment = Effect::with_id(
            0,
            Effect::may_single(Effect::new(crate::effects::PayAnyEnergyEffect::new(
                ChooseSpec::Player(PlayerFilter::You),
                1,
            ))),
        );
        let leading_then =
            Effect::new(crate::effects::SequenceEffect::sentence_leading_then(vec![
                payment,
            ]));
        let result = Effect::if_then(
            result_condition,
            EffectPredicate::Happened,
            vec![Effect::draw(Value::Fixed(1))],
        );

        vec![
            crate::resolution::ResolutionSegment::from_effects(vec![leading_then]),
            crate::resolution::ResolutionSegment::from_effects(vec![result]),
        ]
    }

    #[test]
    fn sentence_leading_optional_payment_rejoins_linked_result() {
        let segments = optional_energy_segments(crate::effect::EffectId(0));

        assert_eq!(
            describe_cross_segment_result_window(&segments, 0),
            Some((
                "Then you may pay one or more {E}. If you do, you draw a card".to_string(),
                2,
            ))
        );
    }

    #[test]
    fn sentence_leading_optional_payment_rejects_mismatched_result_id() {
        let segments = optional_energy_segments(crate::effect::EffectId(4));

        assert_eq!(describe_cross_segment_result_window(&segments, 0), None);
    }
}

/// Rejoin linked graveyard choices and their optional return when sentence
/// boundaries placed the three instructions in adjacent resolution segments.
/// The effect-list matcher verifies the shared tag, reciprocal chooser, zones,
/// cardinalities, destination, and controller before compacting the surface.
fn describe_cross_segment_linked_graveyard_choices_then_may_return_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let mut effects = Vec::with_capacity(3);
    let mut consumed = 0usize;
    for segment in segments.iter().skip(start).take(3) {
        if !segment.self_replacements.is_empty() || segment.default_effects.is_empty() {
            return None;
        }
        for effect in &segment.default_effects {
            let unwrapped = structural_unwrap_render_wrappers(effect);
            if let Some(sequence) = unwrapped.downcast_ref::<crate::effects::SequenceEffect>() {
                if sequence.surface != ironsmith_core::SequenceSurface::CommaThen
                    || effects.len() + sequence.effects.len() > 3
                {
                    return None;
                }
                effects.extend(sequence.effects.iter());
            } else {
                if effects.len() == 3 {
                    return None;
                }
                effects.push(effect);
            }
        }
        consumed += 1;
        if effects.len() == 3 {
            break;
        }
    }
    if effects.len() != 3 {
        return None;
    }
    describe_linked_graveyard_choices_then_may_return_bundle(&effects)
        .map(|rendered| (rendered, consumed))
}

/// Rejoin a face-up exile choice with the following library exchange when
/// source sentence boundaries place the choice producer and its two linked
/// consumers in adjacent resolution segments. The flat matcher still proves
/// the exact zones, owner, count, tag, and top-of-library destination.
fn describe_cross_segment_choose_exiled_cards_exile_library_put_chosen_on_top_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [choice_segment, disposition_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !choice_segment.self_replacements.is_empty()
        || !disposition_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [choose_effect] = choice_segment.default_effects.as_slice() else {
        return None;
    };
    let (exile_effect, move_effect) = match disposition_segment.default_effects.as_slice() {
        [exile_effect, move_effect] => (exile_effect, move_effect),
        [sequence_effect] => {
            let sequence = structural_unwrap_render_wrappers(sequence_effect)
                .downcast_ref::<crate::effects::SequenceEffect>()?;
            if sequence.surface != ironsmith_core::SequenceSurface::CommaThen {
                return None;
            }
            let [exile_effect, move_effect] = sequence.effects.as_slice() else {
                return None;
            };
            (exile_effect, move_effect)
        }
        _ => return None,
    };
    let effects = [
        choose_effect.clone(),
        exile_effect.clone(),
        move_effect.clone(),
    ];
    super::render_effects::describe_choose_exiled_cards_exile_library_put_chosen_on_top(&effects)
        .map(|rendered| (rendered, 2))
}

/// Rejoin a source-linked exile disposition with its following source
/// sacrifice when sentence lowering preserves the authored "then" pair as a
/// sequence in one segment and the final sentence in the next. The structural
/// matcher still proves the exact source-exiled filter, owner-relative
/// graveyard destination, affected-object count, token blueprint, and source
/// sacrifice.
fn describe_cross_segment_source_exiled_graveyard_token_sacrifice_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [disposition_segment, sacrifice_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !disposition_segment.self_replacements.is_empty()
        || !sacrifice_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [sequence_effect] = disposition_segment.default_effects.as_slice() else {
        return None;
    };
    let sequence = structural_unwrap_render_wrappers(sequence_effect)
        .downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::CommaThen {
        return None;
    }
    let [move_effect, create_effect] = sequence.effects.as_slice() else {
        return None;
    };
    let [sacrifice_effect] = sacrifice_segment.default_effects.as_slice() else {
        return None;
    };

    let effects = [
        move_effect.clone(),
        create_effect.clone(),
        sacrifice_effect.clone(),
    ];
    super::render_effects::describe_source_exiled_graveyard_token_sacrifice_structural(&effects)
        .map(|rendered| (rendered, 2))
}

/// Rejoin a target declaration and its investigate action when sentence
/// lowering placed them in adjacent resolution segments. The existing effect
/// list matcher proves the target cardinality and the target-relative count;
/// this bridge only restores the context that matcher needs.
fn describe_cross_segment_target_players_investigate_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [target_segment, investigate_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !target_segment.self_replacements.is_empty()
        || !investigate_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [target_effect] = target_segment.default_effects.as_slice() else {
        return None;
    };
    let [investigate_effect] = investigate_segment.default_effects.as_slice() else {
        return None;
    };
    let rendered = super::render_effects::compile_effect_list(&[
        target_effect.clone(),
        investigate_effect.clone(),
    ]);
    (rendered
        == "Choose any number of target players. Investigate X times, where X is the total number of creatures those players control")
        .then_some((rendered, 2))
}

fn describe_cross_segment_damage_and_die_replacement_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if let [damage_segment, attachment_segment, replacement_segment] = program.segments.as_slice() {
        if [damage_segment, attachment_segment, replacement_segment]
            .iter()
            .any(|segment| {
                !segment.self_replacements.is_empty() || segment.default_effects.len() != 1
            })
        {
            return None;
        }
        let damage_effect = &damage_segment.default_effects[0];
        let damage_tag = damage_effect
            .downcast_ref::<crate::effects::TaggedEffect>()?
            .tag
            .clone();
        let damage = structural_unwrap_render_wrappers(damage_effect)
            .downcast_ref::<crate::effects::DealDamageEffect>()?;
        let mut expected_creature = ObjectFilter::creature();
        expected_creature.set_explicit_card_type_noun(Some(CardType::Creature));
        if !matches!(
            damage.target.unhinted(),
            ChooseSpec::Target(inner)
                if matches!(inner.unhinted(), ChooseSpec::Object(filter) if filter == &expected_creature)
        ) {
            return None;
        }

        let attachment_effect = &attachment_segment.default_effects[0];
        let exile = structural_unwrap_render_wrappers(attachment_effect)
            .downcast_ref::<crate::effects::ExileEffect>()?;
        let ChooseSpec::WithCount(attachment_target, count) = &exile.spec else {
            return None;
        };
        if exile.face_down
            || count.min != 0
            || count.max != Some(1)
            || count.dynamic_x
            || count.up_to_x
            || count.random
            || count.explicit_exactly
        {
            return None;
        }
        let ChooseSpec::Target(attachment_target) = attachment_target.as_ref() else {
            return None;
        };
        let ChooseSpec::Object(attachment_filter) = attachment_target.unhinted() else {
            return None;
        };
        let [attachment_constraint] = attachment_filter.tagged_constraints.as_slice() else {
            return None;
        };
        if attachment_constraint.tag != damage_tag
            || attachment_constraint.relation
                != crate::filter::TaggedOpbjectRelation::AttachedToTaggedObject
        {
            return None;
        }
        let mut unanchored_attachment_filter = attachment_filter.clone();
        unanchored_attachment_filter.tagged_constraints.clear();
        if unanchored_attachment_filter == ObjectFilter::default() {
            return None;
        }
        let unanchored_target = ChooseSpec::WithCount(
            Box::new(ChooseSpec::target(ChooseSpec::Object(
                unanchored_attachment_filter,
            ))),
            *count,
        );

        let replacement_effect = &replacement_segment.default_effects[0];
        let replacement =
            replacement_effect.downcast_ref::<crate::effects::RegisterZoneReplacementEffect>()?;
        let replacement_text =
            super::render_effects::describe_tagged_die_exile_replacement_followup(
                damage_effect,
                replacement,
            )?;
        return Some(format!(
            "{}. Exile {} attached to that creature. {replacement_text}",
            describe_effect(damage_effect).trim().trim_end_matches('.'),
            describe_choose_spec(&unanchored_target),
        ));
    }

    let [damage_segment, replacement_segment] = program.segments.as_slice() else {
        return None;
    };
    if !damage_segment.self_replacements.is_empty()
        || !replacement_segment.self_replacements.is_empty()
        || replacement_segment.default_effects.len() != 1
    {
        return None;
    }
    let mut combined = match damage_segment.default_effects.as_slice() {
        [first, second] => vec![first.clone(), second.clone()],
        [sequence_effect] => {
            let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
            if sequence.surface != ironsmith_core::SequenceSurface::Coordinated
                || sequence.effects.len() != 2
            {
                return None;
            }
            sequence.effects.clone()
        }
        _ => return None,
    };
    combined.extend(replacement_segment.default_effects.iter().cloned());
    let rendered = super::render_effects::compile_effect_list(&combined);
    rendered
        .contains("If that creature would die this turn, exile it instead")
        .then_some(rendered)
}

#[cfg(test)]
mod linked_attachment_death_replacement_surface_tests {
    use super::*;

    #[test]
    fn public_three_sentence_route_keeps_shared_creature_antecedent() {
        let oracle = "Linked Attachment Probe deals 5 damage to target creature. Exile up to one target Equipment attached to that creature. If that creature would die this turn, exile it instead.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Linked Attachment Probe",
        )
        .card_types(vec![CardType::Instant])
        .parse_text(oracle)
        .expect("linked attachment/death replacement should parse");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![oracle.to_string()]
        );

        let uncorrelated_oracle = "For each player, you choose from among the permanents that player controls an artifact, a creature, an enchantment, and a planeswalker. Then each player sacrifices all nonland permanents they control.";
        let uncorrelated = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Uncorrelated Type Slot Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(uncorrelated_oracle)
        .expect("uncorrelated type-slot program should still parse");
        let uncorrelated_lines = crate::compiled_text::compiled_text_lines(&uncorrelated);
        assert_eq!(uncorrelated_lines.len(), 1);
        assert!(
            !uncorrelated_lines.join(" ").contains("choose from among"),
            "a sacrifice without the chosen-set complement must not be folded: {uncorrelated_lines:#?}"
        );
    }
}

fn describe_cross_segment_source_exiled_copy_cast_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [producer_segment, result_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !result_segment.self_replacements.is_empty()
        || producer_segment.starts_new_source_line
    {
        return None;
    }
    let [producer] = producer_segment.default_effects.as_slice() else {
        return None;
    };
    let [result] = result_segment.default_effects.as_slice() else {
        return None;
    };
    describe_optional_source_exiled_copy_then_cast_pair(producer, result)
        .map(|rendered| (rendered, 2))
}

fn describe_cross_segment_countered_spell_exile_with_counters_gain_suspend_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let window = segments.get(start..start + 3)?;
    if window
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let effects = window
        .iter()
        .map(|segment| match segment.default_effects.as_slice() {
            [effect] => Some(effect.clone()),
            _ => None,
        })
        .collect::<Option<Vec<_>>>()?;
    let rendered = describe_separated_countered_spell_exile_with_counters_gain_suspend(&effects)?;
    Some((rendered, 3))
}

/// Rejoin a time-counter instruction with its exact tagged-object suspend
/// condition when authored sentence boundaries placed them in adjacent
/// resolution segments. The underlying helpers prove that the condition and
/// permanent grant reference the same single object that received the
/// counters; an optional preceding exile is accepted only when its tag is
/// that counter target.
fn describe_cross_segment_put_counters_then_gain_suspend_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let window = segments.get(start..start + 2)?;
    let effects = flattened_cross_segment_effects(window)?;
    let rendered = match effects.as_slice() {
        [put_effect, conditional_effect] => {
            super::render_effects::describe_put_counters_then_gain_suspend(&[
                put_effect.clone(),
                conditional_effect.clone(),
            ])
        }
        [exile_effect, put_effect, conditional_effect] => {
            super::render_effects::describe_exile_with_counters_then_gain_suspend(&[
                exile_effect.clone(),
                put_effect.clone(),
                conditional_effect.clone(),
            ])
        }
        _ => None,
    }?;
    Some((rendered, 2))
}

/// Rejoin a countered-spell result gate with the optional hand-to-battlefield
/// move it enables. Sentence lowering keeps the counter's result ID and object
/// tag executable across segments; this renderer accepts the window only when
/// the condition, decider, hand choice, and tagged move all refer to the
/// controller of that exact countered spell.
fn describe_cross_segment_countered_spell_may_put_from_hand_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [counter_segment, result_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !counter_segment.self_replacements.is_empty() || !result_segment.self_replacements.is_empty()
    {
        return None;
    }

    let [counter_effect] = counter_segment.default_effects.as_slice() else {
        return None;
    };
    let counter_with_id = counter_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let tagged_counter = counter_with_id
        .effect
        .downcast_ref::<crate::effects::TaggedEffect>()?;
    let counter = tagged_counter
        .effect
        .downcast_ref::<crate::effects::CounterEffect>()?;
    if counter.target != ChooseSpec::target(ChooseSpec::spell()) {
        return None;
    }

    let [result_effect] = result_segment.default_effects.as_slice() else {
        return None;
    };
    let result = result_effect.downcast_ref::<crate::effects::IfEffect>()?;
    let EffectPredicate::PriorEffectResult(surface) = &result.predicate else {
        return None;
    };
    if result.condition != counter_with_id.id
        || surface.negated
        || surface.action != crate::effect::PriorEffectAction::Countered
        || surface.actor != crate::effect::PriorEffectResultActor::Passive
        || surface.quantifier != crate::effect::PriorEffectResultQuantifier::One
        || !result.else_.is_empty()
    {
        return None;
    }

    let [may_effect] = result.then.as_slice() else {
        return None;
    };
    let may = may_effect.downcast_ref::<crate::effects::MayEffect>()?;
    let [choose_effect, move_effect] = may.effects.as_slice() else {
        return None;
    };
    let countered_ref = crate::filter::ObjectRef::Tagged(tagged_counter.tag.clone());
    let controller = PlayerFilter::ControllerOf(countered_ref.clone());
    let aliased_controller = PlayerFilter::AliasedControllerOf(countered_ref);
    if may.decider.as_ref() != Some(&controller) {
        return None;
    }

    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    if !choose.count.is_single()
        || choose.count_value.is_some()
        || choose.aggregate_constraint.is_some()
        || choose.chooser != aliased_controller
        || choose.zone != Some(Zone::Hand)
        || !choose.additional_zones.is_empty()
        || choose.is_search
        || choose.reveal
        || choose.top_only
        || choose.bottom_only
        || choose.filter.zone != Some(Zone::Hand)
        || choose.filter.owner.as_ref() != Some(&aliased_controller)
    {
        return None;
    }

    let move_to_zone = unwrap_basic_render_wrapper(move_effect)
        .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    if !super::render_effects::move_to_battlefield_uses_chosen_tag(
        move_to_zone,
        choose.tag.as_str(),
    ) || move_to_zone.to_top
        || move_to_zone.library_order.is_some()
        || move_to_zone.verb_surface != ironsmith_core::MoveToZoneVerbSurface::Put
        || move_to_zone.actor_surface.as_ref() != Some(&controller)
        || move_to_zone.destination_player_surface.is_some()
        || move_to_zone.destination_player_reference_surface.is_some()
        || move_to_zone.battlefield_controller != crate::effects::BattlefieldController::Preserve
        || move_to_zone.controller_surface_explicit
        || !move_to_zone.enters_with_counters.is_empty()
        || move_to_zone.enters_tapped
        || move_to_zone.enters_attacking
        || move_to_zone.attack_target_mode.is_some()
        || move_to_zone.enters_face_down
        || move_to_zone.enters_transformed
        || move_to_zone.transfer_exiled_with_source_links
    {
        return None;
    }

    let mut choice_filter = choose.filter.clone();
    choice_filter.zone = None;
    choice_filter.owner = None;
    let choice_description = choice_filter.description();
    let choice_noun = strip_leading_article(&choice_description).trim();
    if choice_noun.is_empty() {
        return None;
    }
    let choice = with_indefinite_article(choice_noun);

    Some((
        format!(
            "Counter target spell. If that spell is countered this way, its controller may put {choice} from their hand onto the battlefield"
        ),
        2,
    ))
}

/// Rejoin a counter-placement producer with an adjacent exact-set consumer
/// when sentence lowering kept the authored sentence boundary as resolution
/// segments. The typed tag proves both effects refer to the same object set;
/// the effect-list matchers choose the appropriate demonstrative surface.
fn describe_cross_segment_countered_set_followup_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [producer_segment, consumer_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !consumer_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [producer] = producer_segment.default_effects.as_slice() else {
        return None;
    };
    let rendered = match consumer_segment.default_effects.as_slice() {
        [consumer] => {
            super::render_effects::describe_put_counters_then_untap_them(producer, consumer)
                .or_else(|| {
                    super::render_effects::describe_put_counters_then_gain_life_for_each_of_them(
                        producer, consumer,
                    )
                })
        }
        [tag_matching, consumer] => {
            super::render_effects::describe_put_counters_then_tag_matching_untap_them(
                producer,
                tag_matching,
                consumer,
            )
        }
        _ => None,
    }?;
    Some((rendered, 2))
}

fn direct_or_for_players_choice_tag(effect: &Effect) -> Option<&TagKey> {
    if let Some(choose) = effect.downcast_ref::<crate::effects::ChooseObjectsEffect>() {
        return Some(&choose.tag);
    }
    let for_players = effect.downcast_ref::<crate::effects::ForPlayersEffect>()?;
    let [choose_effect] = for_players.effects.as_slice() else {
        return None;
    };
    choose_effect
        .downcast_ref::<crate::effects::ChooseObjectsEffect>()
        .map(|choose| &choose.tag)
}

fn describe_random_target_opponent_graveyard_choice_to_your_battlefield(
    choice_segment: &crate::resolution::ResolutionSegment,
    move_to_zone: &crate::effects::MoveToZoneEffect,
    moved_tag: &TagKey,
) -> Option<String> {
    let [choice_effect] = choice_segment.default_effects.as_slice() else {
        return None;
    };
    let choose = choice_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;

    let mut creature_card = ObjectFilter::default().with_type(CardType::Creature);
    creature_card.zone = Some(Zone::Graveyard);
    creature_card.owner = Some(PlayerFilter::Target(Box::new(PlayerFilter::Opponent)));
    creature_card.set_explicit_card_noun(true);
    creature_card.set_explicit_card_type_noun(Some(CardType::Creature));
    let expected_choice = crate::effects::ChooseObjectsEffect::new(
        creature_card,
        ChoiceCount::exactly(1).at_random(),
        PlayerFilter::You,
        moved_tag.clone(),
    )
    .in_zone(Zone::Graveyard);
    if choose != &expected_choice {
        return None;
    }

    let expected_move = crate::effects::MoveToZoneEffect::new(
        ChooseSpec::Tagged(moved_tag.clone()),
        Zone::Battlefield,
        false,
    )
    .with_verb_surface(ironsmith_core::MoveToZoneVerbSurface::Put)
    .under_you_control();
    if move_to_zone != &expected_move {
        return None;
    }

    Some(
        "Choose a creature card at random from target opponent's graveyard. Put that card onto the battlefield under your control"
            .to_string(),
    )
}

fn describe_target_opponent_graveyard_choice_to_your_battlefield(
    choice_segment: &crate::resolution::ResolutionSegment,
    move_to_zone: &crate::effects::MoveToZoneEffect,
    moved_tag: &TagKey,
) -> Option<String> {
    let [target_effect, choose_effect] = choice_segment.default_effects.as_slice() else {
        return None;
    };
    let target = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if !target.target.is_target()
        || !matches!(
            target.target.base(),
            ChooseSpec::Player(PlayerFilter::Opponent)
        )
    {
        return None;
    }
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    if !choose.count.is_single()
        || choose.tag != *moved_tag
        || choose.chooser != PlayerFilter::Target(Box::new(PlayerFilter::Opponent))
        || choose.filter.zone != Some(Zone::Graveyard)
        || choose.filter.owner != Some(PlayerFilter::IteratedPlayer)
        || choose.is_search
        || choose.reveal
        || move_to_zone.zone != Zone::Battlefield
        || move_to_zone.to_top
        || move_to_zone.battlefield_controller != crate::effects::BattlefieldController::You
        || !move_to_zone.enters_with_counters.is_empty()
        || move_to_zone.enters_tapped
        || move_to_zone.enters_attacking
        || move_to_zone.enters_face_down
    {
        return None;
    }

    let choice_text = capitalize_first(
        &describe_effect_list(&choice_segment.default_effects)
            .replace(" in a graveyard", " in their graveyard"),
    );
    choice_text
        .starts_with("Target opponent chooses ")
        .then(|| format!("{choice_text}. Put that card onto the battlefield under your control"))
}

/// Preserve a choice's player/zone surface when its tagged disposition was
/// lowered into the following sentence. This is deliberately limited to one
/// choice producer and one tag-linked move, so unrelated sentence boundaries
/// remain intact.
fn describe_cross_segment_choice_move_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [choice_segment, move_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !choice_segment.self_replacements.is_empty() || !move_segment.self_replacements.is_empty() {
        return None;
    }
    let [move_effect] = move_segment.default_effects.as_slice() else {
        return None;
    };
    let move_to_zone = unwrap_basic_render_wrapper(move_effect)
        .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    let ChooseSpec::Tagged(moved_tag) = move_to_zone.target.base() else {
        return None;
    };

    if let Some(rendered) = describe_random_target_opponent_graveyard_choice_to_your_battlefield(
        choice_segment,
        move_to_zone,
        moved_tag,
    ) {
        return Some((rendered, 2));
    }

    if let Some(rendered) = describe_target_opponent_graveyard_choice_to_your_battlefield(
        choice_segment,
        move_to_zone,
        moved_tag,
    ) {
        return Some((rendered, 2));
    }

    let choice_tags = choice_segment
        .default_effects
        .iter()
        .filter_map(direct_or_for_players_choice_tag)
        .collect::<Vec<_>>();
    if choice_tags.len() != 1 || choice_tags[0] != moved_tag {
        return None;
    }
    if choice_segment.default_effects.iter().any(|effect| {
        effect
            .downcast_ref::<crate::effects::TargetOnlyEffect>()
            .is_none()
            && direct_or_for_players_choice_tag(effect).is_none()
    }) {
        return None;
    }

    let combined = choice_segment
        .default_effects
        .iter()
        .chain(move_segment.default_effects.iter())
        .cloned()
        .collect::<Vec<_>>();
    Some((super::render_effects::compile_effect_list(&combined), 2))
}

#[cfg(test)]
mod random_graveyard_choice_move_tests {
    use super::*;

    fn choice_segment(tag: &TagKey) -> crate::resolution::ResolutionSegment {
        let mut creature_card = ObjectFilter::default().with_type(CardType::Creature);
        creature_card.zone = Some(Zone::Graveyard);
        creature_card.owner = Some(PlayerFilter::Target(Box::new(PlayerFilter::Opponent)));
        creature_card.set_explicit_card_noun(true);
        creature_card.set_explicit_card_type_noun(Some(CardType::Creature));
        crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(
            crate::effects::ChooseObjectsEffect::new(
                creature_card,
                ChoiceCount::exactly(1).at_random(),
                PlayerFilter::You,
                tag.clone(),
            )
            .in_zone(Zone::Graveyard),
        )])
    }

    fn battlefield_move(tag: &TagKey) -> crate::effects::MoveToZoneEffect {
        crate::effects::MoveToZoneEffect::new(
            ChooseSpec::Tagged(tag.clone()),
            Zone::Battlefield,
            false,
        )
        .with_verb_surface(ironsmith_core::MoveToZoneVerbSurface::Put)
        .under_you_control()
    }

    #[test]
    fn random_graveyard_choice_keeps_that_card_only_for_the_exact_tag() {
        let chosen = TagKey::from("chosen");
        assert_eq!(
            describe_random_target_opponent_graveyard_choice_to_your_battlefield(
                &choice_segment(&chosen),
                &battlefield_move(&chosen),
                &chosen,
            )
            .as_deref(),
            Some(
                "Choose a creature card at random from target opponent's graveyard. Put that card onto the battlefield under your control"
            )
        );

        let unrelated = TagKey::from("unrelated");
        assert!(
            describe_random_target_opponent_graveyard_choice_to_your_battlefield(
                &choice_segment(&chosen),
                &battlefield_move(&unrelated),
                &unrelated,
            )
            .is_none()
        );
    }
}

fn effect_contains_zone_replacement(effect: &Effect) -> bool {
    if effect
        .downcast_ref::<crate::effects::RegisterZoneReplacementEffect>()
        .is_some()
    {
        return true;
    }
    if let Some(tagged) = effect.downcast_ref::<crate::effects::TaggedEffect>() {
        return effect_contains_zone_replacement(&tagged.effect);
    }
    if let Some(with_id) = effect.downcast_ref::<crate::effects::WithIdEffect>() {
        return effect_contains_zone_replacement(&with_id.effect);
    }
    if let Some(conditional) = effect.downcast_ref::<crate::effects::ConditionalEffect>() {
        return conditional
            .if_true
            .iter()
            .chain(&conditional.if_false)
            .any(effect_contains_zone_replacement);
    }
    false
}

fn describe_cross_segment_death_replacement_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    if segments.len().saturating_sub(start) < 2 {
        return None;
    }

    let mut effects = Vec::new();
    for (offset, segment) in segments[start..].iter().enumerate() {
        if !segment.self_replacements.is_empty() || segment.default_effects.is_empty() {
            break;
        }
        let contains_replacement = segment
            .default_effects
            .iter()
            .any(effect_contains_zone_replacement);
        if offset == 0 && contains_replacement {
            break;
        }
        effects.extend(segment.default_effects.iter().cloned());
        if offset == 0 {
            continue;
        }
        if let Some(rendered) = describe_cross_segment_death_replacement_bundle(&effects) {
            return Some((rendered, offset + 1));
        }
        if contains_replacement {
            break;
        }
    }
    None
}

fn effect_consult_count_for_window(effect: &Effect) -> usize {
    if let Some(with_id) = effect.downcast_ref::<crate::effects::WithIdEffect>() {
        return effect_consult_count_for_window(&with_id.effect);
    }
    if let Some(tagged) = effect.downcast_ref::<crate::effects::TaggedEffect>() {
        return effect_consult_count_for_window(&tagged.effect);
    }
    if let Some(tag_all) = effect.downcast_ref::<crate::effects::TagAllEffect>() {
        return effect_consult_count_for_window(&tag_all.effect);
    }
    if effect
        .downcast_ref::<crate::effects::ConsultTopOfLibraryEffect>()
        .is_some()
    {
        return 1;
    }
    effect
        .downcast_ref::<crate::effects::ScheduleDelayedTriggerEffect>()
        .map(|schedule| {
            schedule
                .effects
                .flattened_default_effects()
                .iter()
                .map(effect_consult_count_for_window)
                .sum()
        })
        .unwrap_or(0)
}

fn describe_cross_segment_consult_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let mut effects = Vec::new();
    let mut consult_count = 0usize;
    for (offset, segment) in segments[start..].iter().take(5).enumerate() {
        if !segment.self_replacements.is_empty() || segment.default_effects.is_empty() {
            break;
        }
        consult_count += segment
            .default_effects
            .iter()
            .map(effect_consult_count_for_window)
            .sum::<usize>();
        if consult_count > 1 {
            break;
        }
        effects.extend(segment.default_effects.iter().cloned());
        if offset == 0 {
            continue;
        }
        if let Some(rendered) = describe_cross_segment_consult_bundle(&effects) {
            return Some((rendered, offset + 1));
        }
    }
    None
}

fn describe_cross_segment_turn_start_hand_conditions_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [first, second] = segments.get(start..start + 2)? else {
        return None;
    };
    if !first.self_replacements.is_empty() || !second.self_replacements.is_empty() {
        return None;
    }
    let [first_effect] = first.default_effects.as_slice() else {
        return None;
    };
    let [second_effect] = second.default_effects.as_slice() else {
        return None;
    };
    let effects = [first_effect.clone(), second_effect.clone()];
    describe_turn_start_hand_condition_effects(&effects).map(|rendered| (rendered, 2))
}

fn describe_cross_segment_choose_pay_untap_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [choice, untap] = segments.get(start..start + 2)? else {
        return None;
    };
    if !choice.self_replacements.is_empty() || !untap.self_replacements.is_empty() {
        return None;
    }
    let [choice_effect] = choice.default_effects.as_slice() else {
        return None;
    };
    let [untap_effect] = untap.default_effects.as_slice() else {
        return None;
    };
    describe_may_choose_pay_for_each_then_untap_tagged(&[choice_effect, untap_effect])
        .map(|rendered| (rendered, 2))
}

/// Rejoin a searched-card conditional whose optional battlefield move,
/// declined move, and shuffle were kept in separate source-sentence segments.
/// The existing flat structural renderer still proves the searched tag,
/// condition, effect ID, both hand fallbacks, destination, and shuffle scope.
fn describe_cross_segment_search_reveal_may_move_else_hand_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [
        search_segment,
        conditional_segment,
        fallback_segment,
        shuffle_segment,
    ] = segments.get(start..start + 4)?
    else {
        return None;
    };
    if [
        search_segment,
        conditional_segment,
        fallback_segment,
        shuffle_segment,
    ]
    .iter()
    .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }

    let [search_effect] = search_segment.default_effects.as_slice() else {
        return None;
    };
    let search_sequence = search_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    let [choose_effect, reveal_effect] = search_sequence.effects.as_slice() else {
        return None;
    };

    let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
        return None;
    };
    let with_id = conditional_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let conditional = with_id
        .effect
        .downcast_ref::<crate::effects::ConditionalEffect>()?;
    let [may_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    if !conditional.if_false.is_empty()
        || may_effect
            .downcast_ref::<crate::effects::MayEffect>()
            .is_none()
    {
        return None;
    }

    let [fallback_effect] = fallback_segment.default_effects.as_slice() else {
        return None;
    };
    let fallback = fallback_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if fallback.condition != with_id.id
        || fallback.predicate != EffectPredicate::DidNotHappen
        || !fallback.else_.is_empty()
        || fallback.then.len() != 1
    {
        return None;
    }
    let [shuffle_effect] = shuffle_segment.default_effects.as_slice() else {
        return None;
    };

    let mut merged = conditional.clone();
    merged.if_true = vec![
        Effect::with_id(with_id.id.0, may_effect.clone()),
        fallback_effect.clone(),
    ];
    merged.if_false = fallback.then.clone();
    let flat = [
        choose_effect.clone(),
        reveal_effect.clone(),
        Effect::new(merged),
        shuffle_effect.clone(),
    ];
    describe_search_reveal_conditional_battlefield_or_hand(&flat).map(|rendered| (rendered, 4))
}

/// Rejoin the dynamic kicked target declaration with its per-target counter
/// action when source-sentence preservation places them in adjacent segments.
/// The existing structural helper proves the 1 + kick-count target cardinality,
/// shared tag, iterated target, and counter amount before we cross the boundary.
fn describe_cross_segment_kicked_targets_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [target_segment, counter_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !target_segment.self_replacements.is_empty() || !counter_segment.self_replacements.is_empty()
    {
        return None;
    }
    let effects = target_segment
        .default_effects
        .iter()
        .chain(&counter_segment.default_effects)
        .collect::<Vec<_>>();
    describe_kicked_additional_targets_put_counters(&effects).map(|rendered| (rendered, 2))
}

/// Preserve the authored choose/conditional-prevention sentence when source
/// sentence boundaries place its producer and consumer in adjacent segments.
fn describe_cross_segment_color_matched_prevention_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [choose_segment, prevention_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !choose_segment.self_replacements.is_empty()
        || !prevention_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [choose_effect] = choose_segment.default_effects.as_slice() else {
        return None;
    };
    let [prevention_effect] = prevention_segment.default_effects.as_slice() else {
        return None;
    };
    let choose = structural_unwrap_render_wrappers(choose_effect)
        .downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let conditional = structural_unwrap_render_wrappers(prevention_effect)
        .downcast_ref::<crate::effects::ConditionalEffect>()?;
    describe_choose_then_color_matched_combat_prevention(choose, conditional)
        .map(|rendered| (rendered, 2))
}

/// Rejoin an authored standalone player-target declaration with the damage
/// sentence that consumes that exact target. The established helper proves
/// the player filters are identical before introducing "that player."
fn describe_cross_segment_target_only_damage_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [target_segment, damage_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !target_segment.self_replacements.is_empty() || !damage_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [target_effect] = target_segment.default_effects.as_slice() else {
        return None;
    };
    let [damage_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let target = structural_unwrap_render_wrappers(target_effect)
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let damage = structural_unwrap_render_wrappers(damage_effect)
        .downcast_ref::<crate::effects::DealDamageEffect>()?;
    describe_target_only_then_damage_that_player(target, damage).map(|rendered| (rendered, 2))
}

/// Rejoin damage to a tagged creature with the adjacent destruction of
/// attachments anchored to that exact target.
fn describe_cross_segment_target_creature_damage_then_destroy_attached_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [damage_segment, destroy_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !damage_segment.self_replacements.is_empty() || !destroy_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [damage_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let [destroy_effect] = destroy_segment.default_effects.as_slice() else {
        return None;
    };
    let effects = [damage_effect, destroy_effect];
    super::render_effects::describe_target_creature_damage_then_destroy_attached(&effects)
        .map(|rendered| (rendered, 2))
}

fn exact_creatures_blocked_by_tag_filter(filter: &ObjectFilter, tag: &TagKey) -> bool {
    let mut expected = ObjectFilter::creature();
    expected.blocked = true;
    expected.blocked_by = Some(crate::filter::ObjectRef::Tagged(tag.clone()));
    filter == &expected
}

/// Rejoin destruction of a blocking creature with the exact historical set
/// that creature blocked. The destroyed-object tag is the executable identity
/// edge; rendering it as an arbitrary plural tagged set loses the authored
/// singular antecedent.
fn describe_cross_segment_destroy_then_grant_blocked_by_that_creature_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [destroy_segment, grant_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !destroy_segment.self_replacements.is_empty()
        || !grant_segment.self_replacements.is_empty()
        || destroy_segment.starts_new_source_line
        || grant_segment.starts_new_source_line
    {
        return None;
    }

    let [destroy_effect] = destroy_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_destroy = destroy_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let destroy = tagged_destroy
        .effect
        .downcast_ref::<crate::effects::DestroyEffect>()?;
    let mut blocking_creature = ObjectFilter::creature();
    blocking_creature.blocking = true;
    if destroy.spec != ChooseSpec::target(ChooseSpec::Object(blocking_creature)) {
        return None;
    }

    let [grant_effect] = grant_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_grant = grant_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let grant = tagged_grant
        .effect
        .downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let crate::continuous::EffectTarget::Filter(granted_filter) = &grant.target else {
        return None;
    };
    let crate::continuous::Modification::AddAbility(ability) = grant.modification.as_ref()? else {
        return None;
    };
    if !exact_creatures_blocked_by_tag_filter(granted_filter, &tagged_destroy.tag)
        || ability.id() != crate::static_abilities::StaticAbilityId::Trample
        || grant.target_spec.is_some()
        || !grant.additional_modifications.is_empty()
        || !grant.runtime_modifications.is_empty()
        || grant.until != Until::EndOfTurn
        || grant.condition.is_some()
        || grant.source_type.is_some()
        || grant.source_reference_surface.is_some()
        || grant.set_quantifier_surface.is_some()
        || grant.type_retention_surface.is_some()
        || grant.animation_pt_surface.is_some()
        || grant.animation_duration_surface.is_some()
        || !grant.lock_filter_at_resolution
        || grant.resolve_set_pt_values_at_resolution
        || grant.require_creature_target
    {
        return None;
    }

    Some((
        "Destroy target blocking creature. Creatures that were blocked by that creature this combat gain trample until end of turn".to_string(),
        2,
    ))
}

/// Preserve the singular target identity through an end-of-combat delayed
/// trigger. The nested destroy filter must point to the exact standalone
/// target declaration, not merely any tagged object set.
fn describe_cross_segment_target_then_delayed_destroy_blocked_by_that_creature_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [target_segment, delayed_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !target_segment.self_replacements.is_empty()
        || !delayed_segment.self_replacements.is_empty()
        || target_segment.starts_new_source_line
        || delayed_segment.starts_new_source_line
    {
        return None;
    }

    let [target_effect] = target_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_target = target_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let target = tagged_target
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let wall = ObjectFilter::creature().with_subtype(Subtype::Wall);
    if target.target != ChooseSpec::target(ChooseSpec::Object(wall))
        || target.chooser.is_some()
        || !target.explicit_declaration
    {
        return None;
    }

    let [delayed_effect] = delayed_segment.default_effects.as_slice() else {
        return None;
    };
    let delayed = delayed_effect.downcast_ref::<crate::effects::ScheduleDelayedTriggerEffect>()?;
    if delayed
        .trigger
        .downcast_ref::<crate::triggers::EndOfCombatTrigger>()
        .is_none()
        || !delayed.one_shot
        || delayed.start_next_turn
        || delayed.duration != ironsmith_core::DelayedTriggerDuration::Forever
        || delayed.until_end_of_turn
        || delayed.until_end_of_combat
        || delayed.leading_duration_surface
        || delayed.watch_ability_source
        || delayed.watch_all_object_targets
        || delayed.either_of_watched_objects
        || delayed.while_any_tagged_object_in_zone.is_some()
        || !delayed.target_objects.is_empty()
        || delayed.target_tag.is_some()
        || delayed.target_filter.is_some()
        || delayed.controller != PlayerFilter::You
    {
        return None;
    }
    let [destroy_segment] = delayed.effects.segments.as_slice() else {
        return None;
    };
    if !destroy_segment.self_replacements.is_empty() || destroy_segment.starts_new_source_line {
        return None;
    }
    let [destroy_effect] = destroy_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_destroy = destroy_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let destroy = tagged_destroy
        .effect
        .downcast_ref::<crate::effects::DestroyEffect>()?;
    let ChooseSpec::All(destroyed_filter) = destroy.spec.base() else {
        return None;
    };
    if !exact_creatures_blocked_by_tag_filter(destroyed_filter, &tagged_target.tag) {
        return None;
    }

    Some((
        "Choose target Wall creature. At this turn's next end of combat, destroy all creatures that were blocked by that creature this turn".to_string(),
        2,
    ))
}

/// Render the complete lowest-life controller handoff only when the following
/// segment carries the executable tied-player choice. A lone control change
/// must not claim that its controller gets to choose among tied players.
fn describe_cross_segment_lowest_life_tie_control_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [lowest_segment, tie_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if lowest_segment.starts_new_source_line
        || !lowest_segment.self_replacements.is_empty()
        || !tie_segment.self_replacements.is_empty()
    {
        return None;
    }

    let [lowest_effect] = lowest_segment.default_effects.as_slice() else {
        return None;
    };
    let lowest = lowest_effect.downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let target_spec = lowest.target_spec.as_ref()?;
    if !matches!(lowest.target, crate::continuous::EffectTarget::Source)
        || !matches!(target_spec.base(), ChooseSpec::Source)
        || lowest.modification.is_some()
        || !lowest.additional_modifications.is_empty()
        || lowest.condition.is_some()
        || lowest.until != Until::Forever
        || !matches!(
            lowest.runtime_modifications.as_slice(),
            [
                crate::effects::continuous::RuntimeModification::ChangeControllerToPlayer(
                    PlayerFilter::LowestLifeTied
                )
            ]
        )
    {
        return None;
    }

    let [conditional_effect] = tie_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional = conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    if conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !conditional.if_false.is_empty()
        || !matches!(
            &conditional.condition,
            Condition::ValueComparison {
                left: Value::CountPlayers(PlayerFilter::LowestLifeTied),
                operator: crate::effect::ValueComparisonOperator::GreaterThanOrEqual,
                right: Value::Fixed(2),
            }
        )
    {
        return None;
    }

    let [choose_effect, tied_control_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let choose = choose_effect.downcast_ref::<crate::effects::ChoosePlayerEffect>()?;
    if choose.chooser != PlayerFilter::You
        || choose.filter != PlayerFilter::LowestLifeTied
        || choose.random
        || !choose.excluded_tags.is_empty()
        || choose.remember_as_chosen_player
    {
        return None;
    }
    let tied_control =
        tied_control_effect.downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let [
        crate::effects::continuous::RuntimeModification::ChangeControllerToPlayer(
            PlayerFilter::TaggedPlayer(chosen_tag),
        ),
    ] = tied_control.runtime_modifications.as_slice()
    else {
        return None;
    };
    let follows_choice = chosen_tag == &choose.tag || chosen_tag.as_str() == "__it__";
    if !follows_choice
        || !matches!(tied_control.target, crate::continuous::EffectTarget::Source)
        || !tied_control
            .target_spec
            .as_ref()
            .is_some_and(|spec| matches!(spec.base(), ChooseSpec::Source))
        || tied_control.modification.is_some()
        || !tied_control.additional_modifications.is_empty()
        || tied_control.condition.is_some()
        || tied_control.until != Until::Forever
    {
        return None;
    }

    let target = describe_choose_spec(target_spec);
    Some((
        format!(
            "The player with the lowest life total gains control of {target}. If two or more players are tied for lowest life total, you choose one of them, and that player gains control of {target}"
        ),
        2,
    ))
}

/// Rejoin a conditional triggering-spell copy with its exact did-not-copy
/// fallback. The shared effect id proves that the draw depends on this copy,
/// while the tagged predicate proves the copied spell is the trigger object.
fn describe_cross_segment_conditional_triggering_spell_copy_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [copy_segment, fallback_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if copy_segment.starts_new_source_line
        || fallback_segment.starts_new_source_line
        || !copy_segment.self_replacements.is_empty()
        || !fallback_segment.self_replacements.is_empty()
    {
        return None;
    }

    let (conditional, outer_copy_id, external_retarget) =
        match copy_segment.default_effects.as_slice() {
            [conditional_effect] => (
                conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?,
                None,
                None,
            ),
            [tag_effect, conditional_effect] => {
                let tag = tag_effect.downcast_ref::<crate::effects::TagTriggeringObjectEffect>()?;
                let with_id = conditional_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
                let (conditional, external_retarget) = if let Some(conditional) =
                    with_id
                        .effect
                        .downcast_ref::<crate::effects::ConditionalEffect>()
                {
                    (conditional, None)
                } else {
                    let result = with_id
                        .effect
                        .downcast_ref::<crate::effects::SequenceEffect>()?;
                    if result.surface != ironsmith_core::SequenceSurface::Coordinated {
                        return None;
                    }
                    let [conditional_effect, retarget_effect] = result.effects.as_slice() else {
                        return None;
                    };
                    (
                        conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?,
                        Some(retarget_effect),
                    )
                };
                if tag.tag.as_str() != "triggering" {
                    return None;
                }
                (conditional, Some(with_id.id), external_retarget)
            }
            _ => return None,
        };
    if conditional.surface != ironsmith_core::ConditionalSurface::TrailingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }
    let Condition::TaggedObjectMatches(condition_tag, condition_filter) = &conditional.condition
    else {
        return None;
    };
    let target_filter = condition_filter.targets_object.as_deref()?;
    let mut residual_target_filter = target_filter.clone();
    residual_target_filter.zone = None;
    let mut residual_condition_filter = condition_filter.clone();
    residual_condition_filter.targets_object = None;
    residual_condition_filter.targets_player = None;
    residual_condition_filter.targets_any_of = false;
    if condition_tag.as_str() != "triggering"
        || condition_filter.targets_player != Some(PlayerFilter::Any)
        || !condition_filter.targets_any_of
        || target_filter.zone != Some(Zone::Battlefield)
        || residual_target_filter != ObjectFilter::default()
        || residual_condition_filter != ObjectFilter::spell()
    {
        return None;
    }

    let (copy_effect, retarget_effect, retarget_is_external_may) =
        if let Some(retarget_effect) = external_retarget {
            let [copy_effect] = conditional.if_true.as_slice() else {
                return None;
            };
            (copy_effect, retarget_effect, true)
        } else {
            let [copy_effect, retarget_effect] = conditional.if_true.as_slice() else {
                return None;
            };
            (copy_effect, retarget_effect, false)
        };
    let copied_result = copy_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    if copied_result.tag.as_str() != "__copied_stack_object__" {
        return None;
    }
    let copy_with_id = super::render_effects::wrapped_with_id(copy_effect)?;
    let copy = copy_with_id
        .effect
        .downcast_ref::<crate::effects::CopySpellEffect>()?;
    if outer_copy_id.is_some_and(|id| id != copy_with_id.id)
        || copy.count != Value::Fixed(1)
        || copy.copier != PlayerFilter::You
        || !copy.removed_supertypes.is_empty()
        || copy.target_reference_pronoun
        || copy.target_reference_kind != Some(crate::filter::StackObjectKind::Spell)
        || !matches!(
            &copy.target,
            ChooseSpec::Tagged(copy_tag) if copy_tag == condition_tag
        )
    {
        return None;
    }
    if retarget_is_external_may {
        let may = retarget_effect.downcast_ref::<crate::effects::MayEffect>()?;
        if may.fallback != crate::decision::FallbackStrategy::Decline
            || !matches!(may.decider, Some(PlayerFilter::You))
        {
            return None;
        }
        let [retarget] = may.effects.as_slice() else {
            return None;
        };
        let tagged_retarget = retarget.downcast_ref::<crate::effects::TaggedEffect>()?;
        if !tagged_retarget.tag.as_str().starts_with("retargeted_") {
            return None;
        }
        let retarget = tagged_retarget
            .effect
            .downcast_ref::<crate::effects::RetargetStackObjectEffect>()?;
        if retarget.chooser != PlayerFilter::You
            || !matches!(retarget.mode, crate::effects::RetargetMode::All)
            || retarget.require_change
            || retarget.new_target_restriction.is_some()
            || retarget.copy_reference_plural
            || !matches!(
                &retarget.target,
                ChooseSpec::Tagged(tag) if tag == &copied_result.tag
            )
        {
            return None;
        }
    } else {
        let retarget_effect = retarget_effect
            .downcast_ref::<crate::effects::WithIdEffect>()
            .map_or(retarget_effect, |with_id| {
                if with_id.id == copy_with_id.id {
                    with_id.effect.as_ref()
                } else {
                    retarget_effect
                }
            });
        let retarget = retarget_effect.downcast_ref::<crate::effects::ChooseNewTargetsEffect>()?;
        if retarget.from_effect != copy_with_id.id
            || !retarget.may
            || retarget.single_target_surface
            || !matches!(retarget.chooser, None | Some(PlayerFilter::You))
        {
            return None;
        }
    }

    let [fallback_effect] = fallback_segment.default_effects.as_slice() else {
        return None;
    };
    let fallback = fallback_effect.downcast_ref::<crate::effects::IfEffect>()?;
    let [draw_effect] = fallback.then.as_slice() else {
        return None;
    };
    let draw = draw_effect.downcast_ref::<crate::effects::DrawCardsEffect>()?;
    if fallback.condition != copy_with_id.id
        || fallback.predicate != EffectPredicate::DidNotHappen
        || !fallback.else_.is_empty()
        || draw.player != PlayerFilter::You
        || draw.count != Value::Fixed(1)
    {
        return None;
    }

    Some((
        "Copy that spell if it targets a permanent or player, and you may choose new targets for the copy. If you don't copy a spell this way, draw a card".to_string(),
        2,
    ))
}

#[cfg(all(test, ironsmith_runtime_parser_tests))]
#[test]
fn conditional_triggering_spell_copy_window_accepts_public_result_conjunction_shape() {
    let definition = crate::cards::builders::CardDefinitionBuilder::new(
        crate::ids::CardId::new(),
        "Conditional Copy Probe",
    )
    .card_types(vec![CardType::Creature])
    .parse_text(
        "Whenever you cast your second spell each turn, copy that spell if it targets a permanent or player, and you may choose new targets for the copy. If you don't copy a spell this way, draw a card.",
    )
    .expect("conditional copy probe should parse");
    let triggered = definition
        .abilities
        .iter()
        .find_map(|ability| match &ability.kind {
            AbilityKind::Triggered(triggered) => Some(triggered),
            _ => None,
        })
        .expect("expected spell-cast trigger");

    assert_eq!(
        describe_cross_segment_conditional_triggering_spell_copy_window(
            &triggered.effects.segments,
            0,
        ),
        Some((
            "Copy that spell if it targets a permanent or player, and you may choose new targets for the copy. If you don't copy a spell this way, draw a card".to_string(),
            2,
        )),
        "full conditional-copy program:\n{:#?}",
        triggered.effects
    );
}

/// Rejoin a stored color choice and its immediately linked chosen-color mana
/// action when sentence lowering kept the two authored actions in adjacent
/// resolution segments.
fn describe_cross_segment_choose_color_mana_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [choose_segment, mana_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if choose_segment.starts_new_source_line
        || mana_segment.starts_new_source_line
        || !choose_segment.self_replacements.is_empty()
        || !mana_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [choose] = choose_segment.default_effects.as_slice() else {
        return None;
    };
    let [mana] = mana_segment.default_effects.as_slice() else {
        return None;
    };
    let rendered = describe_choose_color_then_chosen_color_mana(&[choose, mana])?;
    Some((rendered, 2))
}

#[cfg(test)]
mod cross_segment_choose_color_mana_tests {
    use super::*;

    fn program(amount: Value) -> crate::resolution::ResolutionProgram {
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment {
                default_effects: vec![Effect::choose_color(PlayerFilter::You)],
                self_replacements: vec![],
                starts_new_source_line: false,
            },
            crate::resolution::ResolutionSegment {
                default_effects: vec![Effect::new(
                    crate::effects::mana::AddManaOfChosenColorEffect::new(
                        amount,
                        PlayerFilter::You,
                    ),
                )],
                self_replacements: vec![],
                starts_new_source_line: false,
            },
        ])
    }

    #[test]
    fn chosen_color_counter_mana_keeps_that_color_and_for_each_surface() {
        let exact = program(
            Value::CountersOnSource(crate::CounterType::Charge)
                .with_surface_hint(ironsmith_core::ValueSurfaceHint::ForEach),
        );
        assert_eq!(
            describe_resolution_program(&exact),
            "Choose a color. Add one mana of that color for each charge counter on this permanent"
        );

        let missing_surface = program(Value::CountersOnSource(crate::CounterType::Charge));
        assert_ne!(
            describe_resolution_program(&missing_surface),
            describe_resolution_program(&exact)
        );
    }
}

/// Rejoin the two authored sentences of reciprocal power-based damage. The
/// structural matcher verifies the shared target tag, both damage sources,
/// both destinations, and both power references.
fn describe_cross_segment_power_damage_exchange_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    fn collect(effect: &Effect, flattened: &mut Vec<Effect>) {
        if let Some(with_id) = effect.downcast_ref::<crate::effects::WithIdEffect>() {
            collect(&with_id.effect, flattened);
        } else if let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>() {
            for member in &sequence.effects {
                collect(member, flattened);
            }
        } else {
            flattened.push(effect.clone());
        }
    }

    let [first_segment, reciprocal_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !first_segment.self_replacements.is_empty()
        || !reciprocal_segment.self_replacements.is_empty()
    {
        return None;
    }
    if let [conditional_effect] = first_segment.default_effects.as_slice()
        && let Some(conditional) =
            conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()
        && conditional.surface == ironsmith_core::ConditionalSurface::LeadingIf
        && conditional.if_false.is_empty()
        && let [first_target_effect, first_damage_effect] = conditional.if_true.as_slice()
        && let Some(first_target) =
            first_target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()
        && let [tagged_target_effect, reciprocal_effect] =
            reciprocal_segment.default_effects.as_slice()
        && let Some(tagged_target) =
            tagged_target_effect.downcast_ref::<crate::effects::TaggedEffect>()
        && let Some(reciprocal_target) = tagged_target
            .effect
            .downcast_ref::<crate::effects::TargetOnlyEffect>()
        && first_target.target.unhinted() == reciprocal_target.target.unhinted()
    {
        let normalized = [
            first_damage_effect.clone(),
            tagged_target_effect.clone(),
            reciprocal_effect.clone(),
        ];
        if let Some(rendered) = describe_power_damage_exchange_clause(&normalized)
            && let Some((_, reciprocal_clause)) = rendered.split_once(", then ")
        {
            let first_sentence = describe_effect(conditional_effect);
            return Some((
                format!(
                    "{}. {}",
                    first_sentence.trim().trim_end_matches('.'),
                    capitalize_first(reciprocal_clause)
                ),
                2,
            ));
        }
    }
    let mut flattened = Vec::new();
    for effect in first_segment
        .default_effects
        .iter()
        .chain(&reciprocal_segment.default_effects)
    {
        collect(effect, &mut flattened);
    }
    let rendered = describe_power_damage_exchange_clause(&flattened)?;
    let (first_sentence, second_sentence) = rendered.split_once(", then ")?;
    Some((
        format!("{first_sentence}. {}", capitalize_first(second_sentence)),
        2,
    ))
}

/// Restore the authored definite references in a three-sentence procedure
/// that declares two targets, conditionally modifies the first, then has the
/// first deal characteristic-based damage to the second. Runtime identity is
/// proved by the shared chosen-object collection and the two distinct target
/// tags; the renderer never guesses from equal-looking object filters.
fn describe_cross_segment_two_target_characteristic_damage_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    fn tagged_target_declaration(
        effect: &Effect,
    ) -> Option<(&TagKey, &TagKey, &crate::effects::TargetOnlyEffect)> {
        let shared = effect.downcast_ref::<crate::effects::TaggedEffect>()?;
        let individual = shared
            .effect
            .downcast_ref::<crate::effects::TaggedEffect>()?;
        let target = individual
            .effect
            .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
        Some((&shared.tag, &individual.tag, target))
    }

    fn targeted_creature_filter(
        target: &crate::effects::TargetOnlyEffect,
    ) -> Option<&ObjectFilter> {
        if !target.explicit_declaration || target.chooser.is_some() {
            return None;
        }
        let ChooseSpec::Target(inner) = &target.target else {
            return None;
        };
        let ChooseSpec::Object(filter) = inner.unhinted() else {
            return None;
        };
        (filter.zone == Some(Zone::Battlefield)
            && matches!(filter.card_types.as_slice(), [CardType::Creature]))
        .then_some(filter)
    }

    fn definite_reference(target: &crate::effects::TargetOnlyEffect) -> Option<String> {
        let choice = describe_choose_spec(&target.target);
        Some(format!("the {}", choice.strip_prefix("target ")?))
    }

    let [choice_segment, conditional_segment, damage_segment] = segments.get(start..start + 3)?
    else {
        return None;
    };
    if [choice_segment, conditional_segment, damage_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let [first_choice, second_choice] = choice_segment.default_effects.as_slice() else {
        return None;
    };
    let (first_shared, first_tag, first_target) = tagged_target_declaration(first_choice)?;
    let (second_shared, second_tag, second_target) = tagged_target_declaration(second_choice)?;
    if first_shared.as_str() != ironsmith_core::CHOSEN_OBJECTS_TAG
        || second_shared != first_shared
        || second_tag == first_tag
    {
        return None;
    }
    let first_filter = targeted_creature_filter(first_target)?;
    let second_filter = targeted_creature_filter(second_target)?;
    if first_filter.controller != Some(PlayerFilter::You)
        || !matches!(
            second_filter.controller,
            Some(PlayerFilter::Opponent | PlayerFilter::NotYou)
        )
    {
        return None;
    }

    let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional = conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    if conditional.if_false.is_empty()
        && conditional.surface == ironsmith_core::ConditionalSurface::LeadingIf
        && delirium_condition_surface(&conditional.condition).is_some()
    {
        // Continue below after proving the branch target.
    } else {
        return None;
    }
    let [counter_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let counter_effect = counter_effect
        .downcast_ref::<crate::effects::TaggedEffect>()
        .map_or(counter_effect, |tagged| tagged.effect.as_ref());
    let counters = counter_effect.downcast_ref::<crate::effects::PutCountersEffect>()?;
    if !matches!(&counters.target, ChooseSpec::Tagged(tag) if tag == first_tag) {
        return None;
    }

    let [damage_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let damage_effect = damage_effect
        .downcast_ref::<crate::effects::TaggedEffect>()?
        .effect
        .as_ref();
    let with_source = damage_effect.downcast_ref::<crate::effects::ExecuteWithSourceEffect>()?;
    let damage = with_source
        .effect
        .downcast_ref::<crate::effects::DealDamageEffect>()?;
    if !matches!(&with_source.source, ChooseSpec::Tagged(tag) if tag == first_tag)
        || !matches!(&damage.target, ChooseSpec::Tagged(tag) if tag == second_tag)
        || damage.source_is_combat
        || damage.unpreventable
    {
        return None;
    }
    let characteristic = match damage.amount.unhinted() {
        Value::PowerOf(spec) if matches!(spec.unhinted(), ChooseSpec::Source) => "power",
        Value::ToughnessOf(spec) if matches!(spec.unhinted(), ChooseSpec::Source) => "toughness",
        _ => return None,
    };

    let first_reference = definite_reference(first_target)?;
    let second_reference = definite_reference(second_target)?;
    let conditional_text = describe_effect(conditional_effect);
    let conditional_text = conditional_text
        .trim()
        .trim_end_matches('.')
        .strip_suffix(" on it")?;
    Some((
        format!(
            "Choose {} and {}.\nDelirium — {conditional_text} on {first_reference}.\n{} deals damage equal to its {characteristic} to {second_reference}",
            describe_choose_spec(&first_target.target),
            describe_choose_spec(&second_target.target),
            capitalize_first(&first_reference),
        ),
        3,
    ))
}

#[cfg(test)]
mod two_target_characteristic_damage_surface_tests {
    use super::*;

    const ORACLE: &str = "Choose target creature you control and target creature an opponent controls.\n\
Delirium — If there are four or more card types among cards in your graveyard, put two +1/+1 counters on the creature you control.\n\
The creature you control deals damage equal to its power to the creature an opponent controls.";

    fn program() -> crate::resolution::ResolutionProgram {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Two-Target Characteristic Damage Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(ORACLE)
        .expect("the correlated target procedure should parse")
        .spell_effect
        .expect("expected a spell effect")
    }

    #[test]
    fn renders_the_exact_correlated_target_surface() {
        let program = program();
        assert_eq!(
            describe_cross_segment_two_target_characteristic_damage_window(&program.segments, 0),
            Some((ORACLE.trim_end_matches('.').to_string(), 3))
        );
        assert_eq!(
            crate::compiled_text::compiled_text_lines(
                &crate::compiler_test_support::CardDefinitionBuilder::new(
                    crate::ids::CardId::new(),
                    "Two-Target Characteristic Damage Probe",
                )
                .card_types(vec![CardType::Sorcery])
                .parse_text(ORACLE)
                .expect("the public route should parse")
            )
            .join("\n"),
            ORACLE
        );
    }

    #[test]
    fn rejects_a_changed_damage_recipient() {
        let mut program = program();
        program.segments[2].default_effects[0] = Effect::deal_damage(
            Value::PowerOf(Box::new(ChooseSpec::Source)),
            ChooseSpec::AnyTarget,
        );
        assert_eq!(
            describe_cross_segment_two_target_characteristic_damage_window(&program.segments, 0),
            None
        );
    }
}

/// Render two selected creatures dealing their own power or toughness to the
/// other member of that exact selected pair. The first segment must establish
/// exactly two creature targets under one shared collection tag, while the
/// second must iterate only that collection and execute the damage with each
/// iterand as its source. The recipient must be the same tagged collection
/// filtered to the member other than that rebound source, so no third target
/// is declared for the anaphoric "the other".
fn describe_cross_segment_reciprocal_characteristic_damage_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    fn tagged_target_declaration(
        effect: &Effect,
    ) -> Option<(&TagKey, &TagKey, &crate::effects::TargetOnlyEffect)> {
        let shared = effect.downcast_ref::<crate::effects::TaggedEffect>()?;
        let individual = shared
            .effect
            .downcast_ref::<crate::effects::TaggedEffect>()?;
        let target = individual
            .effect
            .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
        Some((&shared.tag, &individual.tag, target))
    }

    fn targeted_creature_filter(
        target: &crate::effects::TargetOnlyEffect,
    ) -> Option<&ObjectFilter> {
        if !target.explicit_declaration || target.chooser.is_some() {
            return None;
        }
        let ChooseSpec::Target(inner) = &target.target else {
            return None;
        };
        let ChooseSpec::Object(filter) = inner.unhinted() else {
            return None;
        };
        (filter.zone == Some(Zone::Battlefield) && filter.card_types.contains(&CardType::Creature))
            .then_some(filter)
    }

    let (first_choice, second_choice, damage_effect, consumed) =
        if let Some([choice_segment, damage_segment]) = segments.get(start..start + 2) {
            if !choice_segment.self_replacements.is_empty()
                || !damage_segment.self_replacements.is_empty()
            {
                return None;
            }
            let (first_choice, second_choice) = match choice_segment.default_effects.as_slice() {
                [choice_effect] => {
                    let choice_sequence =
                        choice_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
                    if choice_sequence.surface != ironsmith_core::SequenceSurface::Coordinated {
                        return None;
                    }
                    let [first_choice, second_choice] = choice_sequence.effects.as_slice() else {
                        return None;
                    };
                    (first_choice, second_choice)
                }
                [first_choice, second_choice] => (first_choice, second_choice),
                _ => return None,
            };
            let [damage_effect] = damage_segment.default_effects.as_slice() else {
                return None;
            };
            (first_choice, second_choice, damage_effect, 2)
        } else {
            let choice_and_damage = segments.get(start)?;
            if !choice_and_damage.self_replacements.is_empty()
                || choice_and_damage.starts_new_source_line
            {
                return None;
            }
            let [first_choice, second_choice, damage_effect] =
                choice_and_damage.default_effects.as_slice()
            else {
                return None;
            };
            (first_choice, second_choice, damage_effect, 1)
        };
    let (first_shared_tag, first_individual_tag, first_target) =
        tagged_target_declaration(first_choice)?;
    let (second_shared_tag, second_individual_tag, second_target) =
        tagged_target_declaration(second_choice)?;
    if first_shared_tag != second_shared_tag || first_individual_tag == second_individual_tag {
        return None;
    }
    targeted_creature_filter(first_target)?;
    targeted_creature_filter(second_target)?;

    let for_each = damage_effect.downcast_ref::<crate::effects::ForEachObject>()?;
    let mut expected_filter = ObjectFilter::creature().match_tagged(
        first_shared_tag.clone(),
        crate::filter::TaggedOpbjectRelation::IsTaggedObject,
    );
    expected_filter.set_set_quantifier_surface(Some(ironsmith_core::SetQuantifierSurface::Those));
    // `Those` already proves the chosen set is plural. Public document
    // lowering may omit the redundant noun-presentation bit.
    expected_filter
        .set_plural_object_noun_surface(for_each.filter.has_plural_object_noun_surface());
    expected_filter.set_explicit_card_type_noun(Some(CardType::Creature));
    if for_each.filter != expected_filter {
        return None;
    }
    let [per_creature_effect] = for_each.effects.as_slice() else {
        return None;
    };
    let (Some(source), damage) = damage_with_source_view(per_creature_effect)? else {
        return None;
    };
    let mut expected_recipient_filter = expected_filter.clone();
    expected_recipient_filter.other = true;
    if !matches!(source.unhinted(), ChooseSpec::Iterated)
        || !matches!(
            damage.target.unhinted(),
            ChooseSpec::Object(filter) if filter == &expected_recipient_filter
        )
        || damage.source_is_combat
        || damage.unpreventable
    {
        return None;
    }
    let characteristic = match damage.amount.unhinted() {
        Value::PowerOf(spec) if matches!(spec.unhinted(), ChooseSpec::Iterated) => "power",
        Value::ToughnessOf(spec) if matches!(spec.unhinted(), ChooseSpec::Iterated) => "toughness",
        _ => return None,
    };

    Some((
        format!(
            "Choose {} and {}. Each of those creatures deals damage equal to its {characteristic} to the other",
            describe_choose_spec(&first_target.target),
            describe_choose_spec(&second_target.target),
        ),
        consumed,
    ))
}

#[cfg(test)]
mod reciprocal_characteristic_damage_surface_tests {
    use super::*;

    fn target(shared: &str, individual: &str, controller: PlayerFilter) -> Effect {
        let mut filter = ObjectFilter::creature().controlled_by(controller);
        filter.set_explicit_card_type_noun(Some(CardType::Creature));
        Effect::new(crate::effects::TargetOnlyEffect::explicit(
            ChooseSpec::target(ChooseSpec::Object(filter)),
        ))
        .tag(TagKey::from(individual))
        .tag(TagKey::from(shared))
    }

    fn segments(
        shared_damage_tag: &str,
        amount: Value,
        recipient: ChooseSpec,
    ) -> Vec<crate::resolution::ResolutionSegment> {
        let choices = Effect::new(crate::effects::SequenceEffect::coordinated(vec![
            target("chosen_pair", "targeted_0", PlayerFilter::You),
            target("chosen_pair", "targeted_1", PlayerFilter::Opponent),
        ]));
        let mut filter = ObjectFilter::creature().match_tagged(
            TagKey::from(shared_damage_tag),
            crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        );
        filter.set_set_quantifier_surface(Some(ironsmith_core::SetQuantifierSurface::Those));
        filter.set_plural_object_noun_surface(true);
        filter.set_explicit_card_type_noun(Some(CardType::Creature));
        let damage = Effect::new(crate::effects::ExecuteWithSourceEffect::new(
            ChooseSpec::Iterated,
            Effect::deal_damage(amount, recipient),
        ));
        let reciprocal = Effect::for_each(filter, vec![damage]);

        vec![
            crate::resolution::ResolutionSegment::from_effects(vec![choices]),
            crate::resolution::ResolutionSegment::from_effects(vec![reciprocal]),
        ]
    }

    #[test]
    fn renders_exact_reciprocal_toughness_damage_surface() {
        let mut recipient = ObjectFilter::creature().match_tagged(
            TagKey::from("chosen_pair"),
            crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        );
        recipient.set_set_quantifier_surface(Some(ironsmith_core::SetQuantifierSurface::Those));
        recipient.set_plural_object_noun_surface(true);
        recipient.set_explicit_card_type_noun(Some(CardType::Creature));
        let segments = segments(
            "chosen_pair",
            Value::ToughnessOf(Box::new(ChooseSpec::Iterated)),
            ChooseSpec::Object(recipient.other()),
        );

        assert_eq!(
            describe_cross_segment_reciprocal_characteristic_damage_window(&segments, 0),
            Some((
                "Choose target creature you control and target creature an opponent controls. Each of those creatures deals damage equal to its toughness to the other".to_string(),
                2,
            ))
        );
    }

    #[test]
    fn parsed_reciprocal_toughness_damage_dispatches_the_exact_two_segment_surface() {
        let oracle = "Choose target creature you control and target creature an opponent controls. Each of those creatures deals damage equal to its toughness to the other.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Reciprocal Toughness Damage Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(oracle)
        .expect("reciprocal toughness damage should parse");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![oracle.to_string()]
        );
    }

    #[test]
    fn rejects_changed_collection_tag_or_recipient() {
        let wrong_tag = segments(
            "different_pair",
            Value::ToughnessOf(Box::new(ChooseSpec::Iterated)),
            ChooseSpec::Object(
                ObjectFilter::creature()
                    .match_tagged(
                        TagKey::from("chosen_pair"),
                        crate::filter::TaggedOpbjectRelation::IsTaggedObject,
                    )
                    .other(),
            ),
        );
        assert_eq!(
            describe_cross_segment_reciprocal_characteristic_damage_window(&wrong_tag, 0),
            None
        );

        let new_target = segments(
            "chosen_pair",
            Value::ToughnessOf(Box::new(ChooseSpec::Iterated)),
            ChooseSpec::AnyTarget,
        );
        assert_eq!(
            describe_cross_segment_reciprocal_characteristic_damage_window(&new_target, 0),
            None
        );
    }
}

/// Rejoin an optional search/reveal/shuffle procedure with a trailing
/// condition and its `otherwise` disposition. Lowering gives the condition an
/// effect ID so the following `DidNotHappen` branch is executable; combine
/// those two runtime nodes only after proving they share that exact ID.
fn describe_cross_segment_may_search_conditional_disposition_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [search_segment, conditional_segment, fallback_segment] = segments.get(start..start + 3)?
    else {
        return None;
    };
    if [search_segment, conditional_segment, fallback_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }

    let [search_effect] = search_segment.default_effects.as_slice() else {
        return None;
    };
    let may = search_effect.downcast_ref::<crate::effects::MayEffect>()?;

    let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional_with_id = conditional_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let conditional = conditional_with_id
        .effect
        .downcast_ref::<crate::effects::ConditionalEffect>()?;

    let [fallback_effect] = fallback_segment.default_effects.as_slice() else {
        return None;
    };
    let fallback = fallback_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if fallback.condition != conditional_with_id.id
        || fallback.predicate != EffectPredicate::DidNotHappen
        || !fallback.else_.is_empty()
        || fallback.then.is_empty()
        || !conditional.if_false.is_empty()
    {
        return None;
    }

    let mut combined = conditional.clone();
    combined.if_false = fallback.then.clone();
    describe_may_search_reveal_shuffle_then_conditional_move(may, &combined)
        .map(|rendered| (rendered, 3))
}

/// Rejoin the named-card search branch used by effects such as Nazahn's. The
/// producer sequence, typed reveal predicate, conditional result ID,
/// `otherwise` branch, searched-object tag, and final shuffle must all agree
/// before the authored four-sentence surface is restored.
fn describe_cross_segment_named_search_conditional_disposition_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [
        search_segment,
        conditional_segment,
        fallback_segment,
        shuffle_segment,
    ] = segments.get(start..start + 4)?
    else {
        return None;
    };
    if [
        search_segment,
        conditional_segment,
        fallback_segment,
        shuffle_segment,
    ]
    .iter()
    .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }

    let [search_effect] = search_segment.default_effects.as_slice() else {
        return None;
    };
    let search_with_id = search_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let search_sequence = search_with_id
        .effect
        .downcast_ref::<crate::effects::SequenceEffect>()?;
    let [choose_effect, reveal_effect] = search_sequence.effects.as_slice() else {
        return None;
    };
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let reveal = reveal_effect.downcast_ref::<crate::effects::RevealTaggedEffect>()?;

    let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional_with_id = conditional_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let conditional = conditional_with_id
        .effect
        .downcast_ref::<crate::effects::IfEffect>()?;
    let EffectPredicate::PriorEffectResult(surface) = &conditional.predicate else {
        return None;
    };
    if conditional.condition != search_with_id.id
        || !conditional.else_.is_empty()
        || surface.negated
        || surface.action != ironsmith_core::PriorEffectAction::Revealed
        || surface.actor != ironsmith_core::PriorEffectResultActor::You
        || surface.quantifier != ironsmith_core::PriorEffectResultQuantifier::One
    {
        return None;
    }

    let [fallback_effect] = fallback_segment.default_effects.as_slice() else {
        return None;
    };
    let fallback = fallback_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if fallback.condition != conditional_with_id.id
        || fallback.predicate != EffectPredicate::DidNotHappen
        || !fallback.else_.is_empty()
    {
        return None;
    }

    let [shuffle_effect] = shuffle_segment.default_effects.as_slice() else {
        return None;
    };
    let shuffle_effect = shuffle_effect
        .downcast_ref::<crate::effects::SequenceEffect>()
        .and_then(|sequence| match sequence.effects.as_slice() {
            [shuffle] => Some(shuffle),
            _ => None,
        })
        .unwrap_or(shuffle_effect);
    let shuffle = shuffle_effect.downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;

    let combined = crate::effects::ConditionalEffect::new(
        Condition::TaggedObjectMatches(choose.tag.clone(), surface.filter.clone()),
        conditional.then.clone(),
        fallback.then.clone(),
    );
    describe_search_reveal_named_conditional_move_then_shuffle(choose, reveal, &combined, shuffle)
        .map(|rendered| (rendered, 4))
}

fn describe_cross_segment_consult_hand_remainder_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [consult_segment, disposition_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !consult_segment.self_replacements.is_empty()
        || !disposition_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [consult_effect] = consult_segment.default_effects.as_slice() else {
        return None;
    };
    let consult = structural_unwrap_render_wrappers(consult_effect)
        .downcast_ref::<crate::effects::ConsultTopOfLibraryEffect>()?;
    if consult.player != PlayerFilter::You
        || consult.mode != crate::effects::consult_helpers::LibraryConsultMode::Reveal
        || !matches!(
            consult.stop_rule,
            crate::effects::ConsultTopOfLibraryStopRule::FirstMatch
                | crate::effects::ConsultTopOfLibraryStopRule::MatchCount(Value::Fixed(1))
        )
    {
        return None;
    }

    let disposition_effects = if let [sequence_effect] =
        disposition_segment.default_effects.as_slice()
        && let Some(sequence) = structural_unwrap_render_wrappers(sequence_effect)
            .downcast_ref::<crate::effects::SequenceEffect>()
    {
        sequence.effects.as_slice()
    } else {
        disposition_segment.default_effects.as_slice()
    };
    let [move_match_effect, remainder_effect] = disposition_effects else {
        return None;
    };
    let move_match = structural_unwrap_render_wrappers(move_match_effect)
        .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    if move_match.zone != Zone::Hand
        || move_match.to_top
        || !matches!(
            move_match.target.base(),
            ChooseSpec::Tagged(tag) if tag == &consult.match_tag
        )
    {
        return None;
    }

    let remainder = structural_unwrap_render_wrappers(remainder_effect)
        .downcast_ref::<crate::effects::ForEachTaggedEffect>()?;
    if remainder.tag != consult.all_tag {
        return None;
    }
    let nested = if let [nested_sequence] = remainder.effects.as_slice()
        && let Some(nested_sequence) = structural_unwrap_render_wrappers(nested_sequence)
            .downcast_ref::<crate::effects::SequenceEffect>()
    {
        nested_sequence.effects.as_slice()
    } else {
        remainder.effects.as_slice()
    };
    let [conditional_effect] = nested else {
        return None;
    };
    let conditional = structural_unwrap_render_wrappers(conditional_effect)
        .downcast_ref::<crate::effects::ConditionalEffect>()?;
    if !condition_matches_tagged_object_membership(
        &conditional.condition,
        consult.match_tag.as_str(),
    ) || !conditional.if_true.is_empty()
    {
        return None;
    }
    let [move_remainder_effect] = conditional.if_false.as_slice() else {
        return None;
    };
    let move_remainder = structural_unwrap_render_wrappers(move_remainder_effect)
        .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    if !matches!(move_remainder.target.base(), ChooseSpec::Iterated)
        && !matches!(
            move_remainder.target.base(),
            ChooseSpec::Tagged(tag) if tag.as_str() == "__it__"
        )
    {
        return None;
    }
    let disposition = match move_remainder.zone {
        Zone::Graveyard if !move_remainder.to_top => {
            "put that card into your hand and all other cards revealed this way into your graveyard"
        }
        Zone::Exile => "put that card into your hand and exile all other cards revealed this way",
        _ => return None,
    };
    let rendered_consult = describe_effect(consult_effect);
    let rendered_consult = rendered_consult.trim().trim_end_matches('.');
    let consult_text = rendered_consult
        .strip_prefix("you ")
        .or_else(|| rendered_consult.strip_prefix("You "))
        .map(capitalize_first)
        .unwrap_or_else(|| capitalize_first(rendered_consult));
    Some((
        format!("{consult_text}. {}", capitalize_first(disposition)),
        2,
    ))
}

/// Rejoin a reveal-hand sentence, its two same-set choices, and the trailing
/// collection move when source sentence preservation splits them into three
/// resolution segments. The established structural helper verifies both
/// zones, the shared opponent, the shared result tag, and the exile consumer.
fn describe_cross_segment_reveal_hand_choose_graveyard_exile_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let window = segments.get(start..start + 3)?;
    let flattened = flattened_cross_segment_effects(window)?;
    let [look, hand_choose, graveyard_choose, exile] = flattened.as_slice() else {
        return None;
    };
    describe_reveal_hand_choose_graveyard_exile_bundle(&[
        look,
        hand_choose,
        graveyard_choose,
        exile,
    ])
    .map(|rendered| (rendered, 3))
}

fn describe_cross_segment_search_face_down_bargain_cast_else_hand_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let window = segments.get(start..start + 3)?;
    let flattened = flattened_cross_segment_effects(window)?;
    let [
        choose_effect,
        exile_effect,
        shuffle_effect,
        bargain_gate_effect,
        fallback_gate_effect,
    ] = flattened.as_slice()
    else {
        return None;
    };
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let exile = exile_effect.downcast_ref::<crate::effects::ExileEffect>()?;
    let shuffle = shuffle_effect.downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;
    let bargain_gate = bargain_gate_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let fallback_gate = fallback_gate_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    describe_search_face_down_exile_shuffle_split_bargain_cast_else_hand(
        choose,
        exile,
        shuffle,
        bargain_gate,
        fallback_gate,
    )
    .map(|rendered| (rendered, 3))
}

fn append_flattened_cross_segment_effect(effect: &Effect, flattened: &mut Vec<Effect>) {
    let unwrapped = structural_unwrap_render_wrappers(effect);
    if let Some(sequence) = unwrapped.downcast_ref::<crate::effects::SequenceEffect>() {
        for member in &sequence.effects {
            append_flattened_cross_segment_effect(member, flattened);
        }
    } else {
        flattened.push(effect.clone());
    }
}

fn flattened_cross_segment_effects(
    segments: &[crate::resolution::ResolutionSegment],
) -> Option<Vec<Effect>> {
    let mut flattened = Vec::new();
    for segment in segments {
        if !segment.self_replacements.is_empty() {
            return None;
        }
        for effect in &segment.default_effects {
            append_flattened_cross_segment_effect(effect, &mut flattened);
        }
    }
    Some(flattened)
}

/// Rejoin a delegated subset choice with the two complementary moves when
/// authored sentence boundaries lower them into adjacent resolution segments.
/// The effect-list helpers prove the chooser, exact prior collection tag,
/// generated subset tag, set difference, destinations, and optional counter;
/// this window only makes that existing structural proof visible across the
/// segment boundary.
fn describe_cross_segment_delegated_collection_partition_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let max_consumed = segments.len().saturating_sub(start).min(3);
    for consumed in (2..=max_consumed).rev() {
        let window = segments.get(start..start + consumed)?;
        if window
            .iter()
            .skip(1)
            .any(|segment| segment.starts_new_source_line)
        {
            continue;
        }
        let flattened = flattened_cross_segment_effects(window)?;
        let [player, choice, moves @ ..] = flattened.as_slice() else {
            continue;
        };
        let Some(choice_text) = describe_delegated_subset_choice(&[player.clone(), choice.clone()])
        else {
            continue;
        };
        let Some(moves_text) = describe_delegated_collection_partition_moves(moves) else {
            continue;
        };
        return Some((format!("{choice_text}. {moves_text}"), consumed));
    }
    None
}

/// Rejoin the exact shuffle/reveal and ordered permanent-group disposition
/// procedure. The existing effect-list matcher proves the shared
/// reveal count, revealed collection, non-Aura/Aura union, moved tag, and
/// random-bottom remainder. Public document lowering can retain the whole
/// procedure in one sequence, while sentence-oriented lowering may split it
/// across two adjacent segments.
fn describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    // The public document route retains the first authored clause as a plain
    // comma-then sequence, but wraps the whole disposition clause in a
    // `WithId`. It captures the first (non-Aura) partition before the union
    // used to track every moved permanent. Normalize only that exact
    // authoritative wrapper/order into the established matcher order.
    // If this wrapper shell is present but any semantic proof fails, do not
    // fall through to the more permissive generic flattener.
    if let Some(window) = segments.get(start..start + 2)
        && let [shuffle_reveal_segment, disposition_segment] = window
        && !shuffle_reveal_segment.starts_new_source_line
        && !disposition_segment.starts_new_source_line
        && shuffle_reveal_segment.self_replacements.is_empty()
        && disposition_segment.self_replacements.is_empty()
        && let [shuffle_reveal_root] = shuffle_reveal_segment.default_effects.as_slice()
        && let [disposition_root] = disposition_segment.default_effects.as_slice()
        && let Some(shuffle_reveal) =
            shuffle_reveal_root.downcast_ref::<crate::effects::SequenceEffect>()
        && shuffle_reveal.surface == ironsmith_core::SequenceSurface::CommaThen
        && shuffle_reveal.result_label.is_none()
        && let [shuffle, reveal] = shuffle_reveal.effects.as_slice()
        && let Some(disposition_with_id) =
            disposition_root.downcast_ref::<crate::effects::WithIdEffect>()
        && let Some(disposition) = disposition_with_id
            .effect
            .downcast_ref::<crate::effects::SequenceEffect>()
        && disposition.surface == ironsmith_core::SequenceSurface::CommaThen
        && disposition.result_label.is_none()
        && let [
            first_tag,
            union_tag,
            first_move,
            second_tag,
            second_move,
            bottom,
        ] = disposition.effects.as_slice()
    {
        let current_order = [
            shuffle,
            reveal,
            union_tag,
            first_tag,
            first_move,
            second_tag,
            second_move,
            bottom,
        ];
        if let Some(rendered) =
            describe_shuffle_reveal_repeated_permanent_groups_rest_bottom(&current_order)
        {
            return Some((rendered, 2));
        }

        // The public finalizer may preserve the authored first partition
        // capture before the aggregate moved-set capture. Both orders are
        // equivalent only when the strict matcher below proves every tag,
        // filter, move loop, and remainder relationship.
        let first_partition_before_union = [
            shuffle,
            reveal,
            first_tag,
            union_tag,
            first_move,
            second_tag,
            second_move,
            bottom,
        ];
        return describe_shuffle_reveal_repeated_permanent_groups_rest_bottom(
            &first_partition_before_union,
        )
        .map(|rendered| (rendered, 2));
    }

    for consumed in [2, 1] {
        let Some(window) = segments.get(start..start + consumed) else {
            continue;
        };
        if window.iter().enumerate().any(|(offset, segment)| {
            (segment.starts_new_source_line && !(consumed == 2 && offset == 1))
                || !segment.self_replacements.is_empty()
                || segment.default_effects.is_empty()
        }) {
            continue;
        }
        let flattened = flattened_cross_segment_effects(window)?;
        let refs = flattened.iter().collect::<Vec<_>>();
        if let Some(rendered) = describe_shuffle_reveal_repeated_permanent_groups_rest_bottom(&refs)
        {
            return Some((rendered, consumed));
        }
    }
    None
}

#[cfg(test)]
mod cross_segment_shuffle_reveal_repeated_permanent_groups_tests {
    use super::*;

    fn program(
        remainder_order: crate::effects::consult_helpers::LibraryBottomOrder,
        keep_tag_matches: bool,
        source_tag_matches: bool,
    ) -> Vec<crate::resolution::ResolutionSegment> {
        let revealed = TagKey::from("revealed");
        let moved = TagKey::from("moved");
        let shuffle = Effect::with_id(
            77,
            Effect::new(crate::effects::ShuffleObjectsIntoLibraryEffect::new(
                ChooseSpec::All(
                    ObjectFilter::permanent_card()
                        .in_zone(Zone::Battlefield)
                        .owned_by(PlayerFilter::You),
                ),
                PlayerFilter::You,
            )),
        );
        let look = Effect::new(crate::effects::LookAtTopCardsEffect::revealing(
            PlayerFilter::You,
            Value::EffectMetric {
                effect_id: crate::effect::EffectId(77),
                source: crate::effect::EffectMetricSource::Outcome,
                metric: crate::effect::EffectMetric::Count,
            },
            revealed.clone(),
        ));

        let tagged_revealed = |mut filter: ObjectFilter| {
            filter
                .tagged_constraints
                .push(crate::filter::TaggedObjectConstraint {
                    tag: if source_tag_matches {
                        revealed.clone()
                    } else {
                        TagKey::from("different_revealed_collection")
                    },
                    relation: crate::filter::TaggedOpbjectRelation::IsTaggedObject,
                });
            filter
        };
        let mut non_aura = tagged_revealed(ObjectFilter::permanent());
        non_aura.set_explicit_card_noun(true);
        non_aura.excluded_subtypes.push(crate::types::Subtype::Aura);
        let mut aura =
            tagged_revealed(ObjectFilter::default().with_subtype(crate::types::Subtype::Aura));
        aura.set_explicit_card_noun(true);
        let mut union = ObjectFilter::default();
        union.any_of = vec![non_aura, aura];
        let capture = Effect::new(
            crate::effects::TagMatchingObjectsEffect::new(union.clone(), moved.clone())
                .in_zone(Zone::Library),
        );
        let first_group = TagKey::from("first_group");
        let second_group = TagKey::from("second_group");
        let capture_first = Effect::new(
            crate::effects::TagMatchingObjectsEffect::new(
                union.any_of[0].clone(),
                first_group.clone(),
            )
            .in_zone(Zone::Library),
        );
        let move_first = Effect::new(crate::effects::ForEachTaggedEffect::new(
            first_group,
            vec![Effect::new(crate::effects::MoveToZoneEffect::new(
                ChooseSpec::Iterated,
                Zone::Battlefield,
                false,
            ))],
        ));
        let capture_second = Effect::new(
            crate::effects::TagMatchingObjectsEffect::new(
                union.any_of[1].clone(),
                second_group.clone(),
            )
            .in_zone(Zone::Library),
        );
        let move_second = Effect::new(crate::effects::ForEachTaggedEffect::new(
            second_group,
            vec![Effect::new(crate::effects::MoveToZoneEffect::new(
                ChooseSpec::Iterated,
                Zone::Battlefield,
                false,
            ))],
        ));
        let keep_tag = keep_tag_matches
            .then(|| moved.clone())
            .or_else(|| Some(TagKey::from("different")));
        let bottom = Effect::new(
            crate::effects::PutTaggedRemainderOnLibraryBottomEffect::new(
                revealed,
                keep_tag,
                remainder_order,
                PlayerFilter::You,
            ),
        );

        vec![
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(
                crate::effects::SequenceEffect::comma_then(vec![shuffle, look]),
            )]),
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(
                crate::effects::SequenceEffect::comma_then(vec![
                    capture,
                    capture_first,
                    move_first,
                    capture_second,
                    move_second,
                    bottom,
                ]),
            )]),
        ]
    }

    fn authoritative_wrapped_program() -> Vec<crate::resolution::ResolutionSegment> {
        let mut segments = program(
            crate::effects::consult_helpers::LibraryBottomOrder::Random,
            true,
            true,
        );
        let [disposition_root] = segments[1].default_effects.as_slice() else {
            panic!("fixture should have one disposition root: {segments:#?}");
        };
        let disposition = disposition_root
            .downcast_ref::<crate::effects::SequenceEffect>()
            .expect("fixture disposition should be a sequence");
        let [
            union_tag,
            first_tag,
            first_move,
            second_tag,
            second_move,
            bottom,
        ] = disposition.effects.as_slice()
        else {
            panic!("fixture should have six disposition members: {disposition:#?}");
        };
        segments[1].default_effects = vec![Effect::with_id(
            91,
            Effect::new(crate::effects::SequenceEffect::comma_then(vec![
                first_tag.clone(),
                union_tag.clone(),
                first_move.clone(),
                second_tag.clone(),
                second_move.clone(),
                bottom.clone(),
            ])),
        )];
        segments
    }

    fn aggregate_capture_first_wrapped_program() -> Vec<crate::resolution::ResolutionSegment> {
        let mut segments = program(
            crate::effects::consult_helpers::LibraryBottomOrder::Random,
            true,
            true,
        );
        let [disposition_root] = segments[1].default_effects.as_slice() else {
            panic!("fixture should have one disposition root: {segments:#?}");
        };
        segments[1].default_effects = vec![Effect::with_id(91, disposition_root.clone())];
        segments
    }

    #[test]
    fn rejoins_exact_shuffle_reveal_and_repeated_groups() {
        let segments = program(
            crate::effects::consult_helpers::LibraryBottomOrder::Random,
            true,
            true,
        );
        assert_eq!(
            describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(&segments, 0),
            Some((
                "Shuffle all permanents you own into your library, then reveal that many cards from the top of your library. Put all non-Aura permanent cards revealed this way onto the battlefield, then do the same for Aura cards, then put the rest on the bottom of your library in a random order".to_string(),
                2,
            ))
        );
    }

    #[test]
    fn rejoins_exact_public_with_id_disposition_wrapper() {
        let segments = authoritative_wrapped_program();
        assert_eq!(
            describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(&segments, 0),
            Some((
                "Shuffle all permanents you own into your library, then reveal that many cards from the top of your library. Put all non-Aura permanent cards revealed this way onto the battlefield, then do the same for Aura cards, then put the rest on the bottom of your library in a random order".to_string(),
                2,
            ))
        );
    }

    #[test]
    fn rejoins_public_wrapper_with_aggregate_capture_before_partitions() {
        let segments = aggregate_capture_first_wrapped_program();
        assert_eq!(
            describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(&segments, 0),
            Some((
                "Shuffle all permanents you own into your library, then reveal that many cards from the top of your library. Put all non-Aura permanent cards revealed this way onto the battlefield, then do the same for Aura cards, then put the rest on the bottom of your library in a random order".to_string(),
                2,
            ))
        );
    }

    #[test]
    fn public_with_id_disposition_wrapper_rejects_altered_member_order() {
        let mut segments = authoritative_wrapped_program();
        let [disposition_root] = segments[1].default_effects.as_slice() else {
            panic!("fixture should have one disposition root: {segments:#?}");
        };
        let with_id = disposition_root
            .downcast_ref::<crate::effects::WithIdEffect>()
            .expect("fixture disposition should retain its id wrapper");
        let sequence = with_id
            .effect
            .downcast_ref::<crate::effects::SequenceEffect>()
            .expect("fixture disposition should be a sequence");
        let mut altered = sequence.effects.clone();
        altered.swap(1, 2);
        segments[1].default_effects = vec![Effect::with_id(
            91,
            Effect::new(crate::effects::SequenceEffect::comma_then(altered)),
        )];

        assert!(
            describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(&segments, 0,)
                .is_none()
        );
    }

    #[test]
    fn rejoins_exact_single_segment_public_sequence() {
        let split = program(
            crate::effects::consult_helpers::LibraryBottomOrder::Random,
            true,
            true,
        );
        let effects = split
            .into_iter()
            .flat_map(|segment| segment.default_effects)
            .collect();
        let segments = vec![crate::resolution::ResolutionSegment::from_effects(effects)];

        assert_eq!(
            describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(&segments, 0),
            Some((
                "Shuffle all permanents you own into your library, then reveal that many cards from the top of your library. Put all non-Aura permanent cards revealed this way onto the battlefield, then do the same for Aura cards, then put the rest on the bottom of your library in a random order".to_string(),
                1,
            ))
        );
    }

    #[test]
    fn public_no_mana_cost_suspend_spell_keeps_authored_order_and_grouping() {
        let oracle = "Suspend 3—{R}{R}\nShuffle all permanents you own into your library, then reveal that many cards from the top of your library. Put all non-Aura permanent cards revealed this way onto the battlefield, then do the same for Aura cards, then put the rest on the bottom of your library in a random order.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Shuffle Reveal Groups Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(oracle)
        .expect("no-mana-cost suspend shuffle/reveal spell should parse");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            oracle.lines().map(str::to_string).collect::<Vec<_>>()
        );
    }

    #[test]
    fn rejects_altered_remainder_tag_and_order() {
        let wrong_tag = program(
            crate::effects::consult_helpers::LibraryBottomOrder::Random,
            false,
            true,
        );
        assert!(
            describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(&wrong_tag, 0)
                .is_none()
        );

        let wrong_order = program(
            crate::effects::consult_helpers::LibraryBottomOrder::ChooserChooses,
            true,
            true,
        );
        assert!(
            describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(
                &wrong_order,
                0,
            )
            .is_none()
        );

        let mut authored_second_sentence = program(
            crate::effects::consult_helpers::LibraryBottomOrder::Random,
            true,
            true,
        );
        authored_second_sentence[1].starts_new_source_line = true;
        assert_eq!(
            describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(
                &authored_second_sentence,
                0,
            ),
            Some((
                "Shuffle all permanents you own into your library, then reveal that many cards from the top of your library. Put all non-Aura permanent cards revealed this way onto the battlefield, then do the same for Aura cards, then put the rest on the bottom of your library in a random order".to_string(),
                2,
            ))
        );

        let wrong_revealed_collection = program(
            crate::effects::consult_helpers::LibraryBottomOrder::Random,
            true,
            false,
        );
        assert!(
            describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(
                &wrong_revealed_collection,
                0,
            )
            .is_none()
        );
    }
}

/// Rejoin a sacrifice selection and its reflexive result when the authored
/// sentence boundary puts the result trigger in the next resolution segment.
/// The shared tags and exact EffectId are validated by the ordinary structural
/// bundle before any prose is combined.
fn describe_choose_sacrifice_reflexive_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if program.segments.len() != 2 {
        return None;
    }
    let flattened = flattened_cross_segment_effects(&program.segments)?;
    let refs = flattened.iter().collect::<Vec<_>>();
    describe_choose_sacrifice_then_reflexive_trigger_refs(&refs)
}

fn describe_damaged_player_gain_control_then_rewards_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if program.segments.len() != 2 {
        return None;
    }
    let flattened = flattened_cross_segment_effects(&program.segments)?;
    let [control_effect, reward_effect] = flattened.as_slice() else {
        return None;
    };
    describe_damaged_player_gain_control_then_rewards(control_effect, reward_effect)
}

fn describe_explicit_target_then_coin_flip_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if program.segments.len() != 3 {
        return None;
    }
    let flattened = flattened_cross_segment_effects(&program.segments)?;
    let (rendered, consumed) = describe_explicit_target_then_coin_flip(&flattened)?;
    (consumed == flattened.len()).then_some(rendered)
}

/// Rejoin a target-plus-fanout collection and a later action over the exact
/// captured union when an authored sentence boundary split the effects into
/// adjacent resolution segments (for example, Radiance followed by "Those
/// creatures ..."). The underlying matcher validates every producer,
/// relation, collection tag, and consumer before any prose is combined.
fn describe_cross_segment_linked_target_set_followup_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let max_consumed = segments.len().saturating_sub(start).min(4);
    for consumed in 2..=max_consumed {
        let window = segments.get(start..start + consumed)?;
        let flattened = flattened_cross_segment_effects(window)?;
        if let Some((rendered, effect_count)) =
            describe_linked_target_set_followup_prefix(&flattened)
                .or_else(|| describe_same_name_exile_then_investigate_prefix(&flattened))
            && effect_count == flattened.len()
        {
            return Some((rendered, consumed));
        }
    }
    None
}

/// Preserve the producer's typed object noun for a later per-object consumer
/// even when source sentence boundaries converted `ForEachTagged` into an
/// equivalent exact-tag `ForEachObject` in the next segment.
fn describe_cross_segment_result_producer_for_each_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let window = segments.get(start..start + 2)?;
    let flattened = flattened_cross_segment_effects(window)?;
    let [producer, for_each] = flattened.as_slice() else {
        return None;
    };
    describe_result_producer_then_for_each_tagged(producer, for_each).map(|rendered| (rendered, 2))
}

/// Rejoin a searched-and-revealed collection, an opponent's division of that
/// collection, and the two destinations when authored sentence boundaries put
/// the linked effects in separate resolution segments. The underlying bundle
/// matcher validates every tag, count, zone, chooser, and optional source-exile
/// effect before it produces text.
fn describe_cross_segment_search_reveal_opponent_choose_rest_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let max_consumed = segments.len().saturating_sub(start).min(8);
    for consumed in (1..=max_consumed).rev() {
        let window = segments.get(start..start + consumed)?;
        let flattened = flattened_cross_segment_effects(window)?;
        let refs = flattened.iter().collect::<Vec<_>>();
        if let Some(rendered) = render_search_reveal_opponent_choose_rest_bundle(&refs) {
            return Some((rendered, consumed));
        }
    }
    None
}

/// Rejoin an optional action with its exact declined branch when authored
/// sentence boundaries place the `WithId` producer and `If` consumer in
/// adjacent resolution segments.
fn describe_cross_segment_declined_may_mill_damage_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [producer_segment, branch_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !branch_segment.self_replacements.is_empty()
    {
        return None;
    }
    let with_ids = producer_segment
        .default_effects
        .iter()
        .filter_map(|effect| effect.downcast_ref::<crate::effects::WithIdEffect>())
        .collect::<Vec<_>>();
    let [with_id] = with_ids.as_slice() else {
        return None;
    };
    let [branch_effect] = branch_segment.default_effects.as_slice() else {
        return None;
    };
    let if_effect = branch_effect.downcast_ref::<crate::effects::IfEffect>()?;
    let declined = describe_declined_may_mill_then_damage(with_id, if_effect)?;
    let setup = describe_effect_list(&producer_segment.default_effects);
    (!setup.trim().is_empty()).then(|| {
        (
            format!(
                "{}. {}",
                setup.trim().trim_end_matches('.'),
                declined.trim().trim_end_matches('.')
            ),
            2,
        )
    })
}

/// Rejoin a hand reveal with a graveyard-or-hand choice when source sentence
/// boundaries place the reveal and the tag-linked choice/move in adjacent
/// resolution segments.
fn describe_cross_segment_reveal_hand_choose_graveyard_or_hand_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    for consumed in (2..=4).rev() {
        let Some(window) = segments.get(start..start + consumed) else {
            continue;
        };
        let Some(flattened) = flattened_cross_segment_effects(window) else {
            continue;
        };
        let refs = flattened.iter().collect::<Vec<_>>();
        if let Some(rendered) = describe_reveal_hand_choose_graveyard_or_hand_exile(&refs) {
            return Some((rendered, consumed));
        }
    }
    None
}

/// Rejoin a fixed counter instruction and its immediately following typed
/// counter-kind choice when document sentence boundaries keep them in two
/// segments. The effect-list helper proves the exact shared target and all
/// mode payloads.
fn describe_cross_segment_fixed_and_chosen_counter_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let window = segments.get(start..start + 2)?;
    if window.iter().any(|segment| segment.starts_new_source_line) {
        return None;
    }
    let flattened = flattened_cross_segment_effects(window)?;
    crate::compiled_text::render_effects::describe_fixed_counter_and_counter_choice_same_target(
        &flattened,
    )
    .map(|rendered| (rendered, 2))
}

/// Rejoin a standalone color choice with the following reveal-and-discard
/// sentence when both use the same chosen-color state.
fn describe_cross_segment_choose_color_reveal_discard_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let window = segments.get(start..start + 2)?;
    let flattened = flattened_cross_segment_effects(window)?;
    let refs = flattened.iter().collect::<Vec<_>>();
    describe_choose_color_reveal_hand_discard_that_color(&refs).map(|rendered| (rendered, 2))
}

/// Rejoin a revealed hand with a tag-linked choose/discard-or-exile action
/// when the authored sentence boundary leaves the reveal in its own segment.
fn describe_cross_segment_look_hand_choose_action_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let window = segments.get(start..start + 2)?;
    let flattened = flattened_cross_segment_effects(window)?;
    let refs = flattened.iter().collect::<Vec<_>>();
    describe_look_hand_choose_then_discard_or_exile(&refs).map(|rendered| (rendered, 2))
}

/// Rejoin an optional, causative hand reveal with the exact successful
/// reveal-result choice in the following `if you do` sentence.
///
/// Lowering keeps the optional reveal under `WithId(May(..))` so the branch
/// can test whether it happened. The ordinary hand-reveal sequence renderer
/// still proves that the choice is constrained to the revealed collection and
/// that the final action consumes that singular choice.
fn describe_cross_segment_may_reveal_hand_choose_action_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [producer_segment, branch_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !branch_segment.self_replacements.is_empty()
    {
        return None;
    }

    let [producer_effect] = producer_segment.default_effects.as_slice() else {
        return None;
    };
    let with_id = producer_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let may = with_id.effect.downcast_ref::<crate::effects::MayEffect>()?;
    if may.fallback != crate::decision::FallbackStrategy::Decline
        || may
            .decider
            .as_ref()
            .is_some_and(|decider| decider != &PlayerFilter::You)
    {
        return None;
    }
    let [target_effect, look_effect] = may.effects.as_slice() else {
        return None;
    };
    let target = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let look = look_effect.downcast_ref::<crate::effects::LookAtHandEffect>()?;
    if !look.reveal
        || target.target != look.target
        || target
            .chooser
            .as_ref()
            .is_some_and(|chooser| chooser != &PlayerFilter::You)
    {
        return None;
    }

    let [branch_effect] = branch_segment.default_effects.as_slice() else {
        return None;
    };
    let branch = branch_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if branch.condition != with_id.id
        || branch.predicate != EffectPredicate::Happened
        || !branch.else_.is_empty()
    {
        return None;
    }
    let [choose_effect, action_effect] = branch.then.as_slice() else {
        return None;
    };
    let compact = describe_look_hand_choose_then_discard_or_exile(&[
        look_effect,
        choose_effect,
        action_effect,
    ])?;
    let (_, successful_branch) = compact.split_once(". You choose ")?;
    let setup = describe_effect_list(&producer_segment.default_effects);
    let setup = setup.trim().trim_end_matches('.');
    (!setup.is_empty()).then(|| (format!("{setup}. If you do, choose {successful_branch}"), 2))
}

/// Preserve the coordinated reveal/move surface when the following life-loss
/// sentence consumes the revealed card's mana value.
fn describe_cross_segment_reveal_to_hand_lose_mana_value_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    for consumed in 2..=3 {
        let Some(window) = segments.get(start..start + consumed) else {
            break;
        };
        let flattened = flattened_cross_segment_effects(window)?;
        if let Some(rendered) = describe_reveal_top_to_hand_then_lose_mana_value_effects(&flattened)
        {
            return Some((rendered, consumed));
        }
    }
    None
}

/// Rejoin a hand reveal, its tag-linked choice/discard, and a trailing optional
/// move from exile when source sentence preservation split the authored
/// sequence into adjacent resolution segments.
fn describe_cross_segment_reveal_hand_choose_discard_adventure_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    for consumed in 2..=5 {
        let Some(window) = segments.get(start..start + consumed) else {
            break;
        };
        let flattened = flattened_cross_segment_effects(window)?;
        let refs = flattened.iter().collect::<Vec<_>>();
        if let Some(rendered) = describe_reveal_hand_choose_discard_then_adventure_move(&refs) {
            return Some((rendered, consumed));
        }
    }
    None
}

/// The while-exiled cast permission and its free-cast modifier are separate
/// runtime effects, but jointly represent one authored permission sentence.
fn describe_cross_segment_exile_then_free_cast_while_exiled_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let window = segments.get(start..start + 2)?;
    let flattened = flattened_cross_segment_effects(window)?;
    describe_exile_then_free_cast_while_exiled_structural(&flattened).map(|rendered| (rendered, 2))
}

/// Rejoin a graveyard-card exile/copy clause with its following optional
/// cast-the-copy permission when source-sentence preservation placed them in
/// adjacent resolution segments. The effect-list renderer requires the exact
/// shared exile tag, so unrelated permissions cannot be folded together.
fn graveyard_exile_copy_surface_effect(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<(Effect, &[Effect])> {
    let (last, preceding) = segment.default_effects.split_last()?;
    if structural_unwrap_render_wrappers(last)
        .downcast_ref::<crate::effects::CopySpellEffect>()
        .is_some()
    {
        let (exile, provenance) = preceding.split_last()?;
        return Some((
            Effect::new(crate::effects::SequenceEffect::coordinated(vec![
                exile.clone(),
                last.clone(),
            ])),
            provenance,
        ));
    }
    Some((last.clone(), preceding))
}

fn describe_cross_segment_graveyard_exile_copy_cast_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [exile_segment, may_cast_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !exile_segment.self_replacements.is_empty() || !may_cast_segment.self_replacements.is_empty()
    {
        return None;
    }

    let (exile_effect, provenance_effects) = graveyard_exile_copy_surface_effect(exile_segment)?;
    if provenance_effects.iter().any(|effect| {
        let inner = structural_unwrap_render_wrappers(effect);
        inner
            .downcast_ref::<crate::effects::TagTriggeringObjectEffect>()
            .is_none()
            && inner
                .downcast_ref::<crate::effects::TagTriggeringSourceEffect>()
                .is_none()
    }) {
        return None;
    }
    let [may_cast_effect] = may_cast_segment.default_effects.as_slice() else {
        return None;
    };

    super::render_effects::render_graveyard_exile_copy_cast_pair(&exile_effect, may_cast_effect)
        .or_else(|| {
            super::render_effects::render_conditional_graveyard_exile_copy_cast_pair(
                &exile_effect,
                may_cast_effect,
            )
        })
        .map(|rendered| (rendered, 2))
}

/// Rejoin the three authored instructions used by graveyard card-copy
/// abilities whose optional cast result controls a trailing `if you do`.
///
/// The executable program intentionally stores the copied-card cast as a
/// `WithId(May(CastTagged(as_copy)))` and the result consumer in a later
/// segment.  Prove the exact shared exile tag and result ID before restoring
/// the authored surface; unrelated result gates must remain separate.
fn describe_cross_segment_graveyard_exile_copy_cast_result_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [exile_segment, cast_segment, result_segment] = segments.get(start..start + 3)? else {
        return None;
    };
    if [exile_segment, cast_segment, result_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }

    let (exile_effect, provenance_effects) = graveyard_exile_copy_surface_effect(exile_segment)?;
    if provenance_effects.iter().any(|effect| {
        let inner = structural_unwrap_render_wrappers(effect);
        inner
            .downcast_ref::<crate::effects::TagTriggeringObjectEffect>()
            .is_none()
            && inner
                .downcast_ref::<crate::effects::TagTriggeringSourceEffect>()
                .is_none()
    }) {
        return None;
    }
    let [with_id_effect] = cast_segment.default_effects.as_slice() else {
        return None;
    };
    let with_id = with_id_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let [result_effect] = result_segment.default_effects.as_slice() else {
        return None;
    };
    let result = structural_unwrap_render_wrappers(result_effect)
        .downcast_ref::<crate::effects::IfEffect>()?;
    if result.condition != with_id.id
        || result.predicate != crate::effect::EffectPredicate::Happened
        || !result.else_.is_empty()
        || result.per_player_result
        || result.prior_result_replacement_surface
    {
        return None;
    }

    let copy_cast = super::render_effects::render_graveyard_exile_copy_cast_pair(
        &exile_effect,
        &with_id.effect,
    )?;
    let result_text = describe_effect_list(&result.then);
    if result_text.is_empty() || result_text.contains(". ") {
        return None;
    }
    Some((format!("{copy_cast}. If you do, {result_text}"), 3))
}

/// Preserve a tagged copy followed by its authored plural retarget sentence
/// when sentence lowering placed the two actions in adjacent segments.
fn describe_cross_segment_tagged_copy_retarget_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [copy_segment, retarget_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !copy_segment.self_replacements.is_empty()
        || !retarget_segment.self_replacements.is_empty()
        || retarget_segment.starts_new_source_line
    {
        return None;
    }
    let [copy_effect] = copy_segment.default_effects.as_slice() else {
        return None;
    };
    let [retarget_effect] = retarget_segment.default_effects.as_slice() else {
        return None;
    };
    let rendered = describe_tagged_copy_then_plural_retarget_pair(copy_effect, retarget_effect)?;
    Some((rendered, 2))
}

/// Rejoin a targeted hand reveal with a following count over that exact
/// revealed hand. The typed renderer proves both the aliased player and the
/// subtype-or-color union before recovering the authored `in it` surface.
fn describe_cross_segment_revealed_hand_union_count_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [look_segment, draw_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !look_segment.self_replacements.is_empty()
        || !draw_segment.self_replacements.is_empty()
        || draw_segment.starts_new_source_line
    {
        return None;
    }
    let [look_effect] = look_segment.default_effects.as_slice() else {
        return None;
    };
    let [draw_effect] = draw_segment.default_effects.as_slice() else {
        return None;
    };
    let draw_effect = structural_unwrap_render_wrappers(draw_effect);
    describe_revealed_hand_then_draw_shared_terminal_union(look_effect, draw_effect)
        .map(|rendered| (rendered, 2))
}

fn describe_cross_segment_revealed_hand_optional_free_cast_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let first = segments.get(start)?;
    if first.self_replacements.is_empty()
        && let Some(rendered) =
            crate::compiled_text::describe_chosen_hand_optional_free_cast(&first.default_effects)
    {
        return Some((rendered, 1));
    }
    if first.self_replacements.is_empty()
        && let [look, may_cast] = first.default_effects.as_slice()
        && let Some(rendered) = describe_revealed_hand_then_optional_free_cast(look, may_cast)
    {
        return Some((rendered, 1));
    }
    let [look_segment, cast_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !look_segment.self_replacements.is_empty()
        || !cast_segment.self_replacements.is_empty()
        || cast_segment.starts_new_source_line
    {
        return None;
    }
    let [look_effect] = look_segment.default_effects.as_slice() else {
        return None;
    };
    let [may_effect] = cast_segment.default_effects.as_slice() else {
        return None;
    };
    describe_revealed_hand_then_optional_free_cast(look_effect, may_effect)
        .map(|rendered| (rendered, 2))
}

#[cfg(test)]
mod graveyard_exile_copy_cast_window_tests {
    use super::*;

    fn segments(
        exile_tag: &TagKey,
        cast_tag: &TagKey,
    ) -> Vec<crate::resolution::ResolutionSegment> {
        let mut filter = ObjectFilter::default().in_zone(Zone::Graveyard);
        filter.card_types = vec![CardType::Instant, CardType::Sorcery];
        let exile = Effect::new(crate::effects::ExileEffect::target(ChooseSpec::Object(
            filter,
        )))
        .tag(exile_tag.clone());
        let cast = Effect::new(
            crate::effects::CastTaggedEffect::new(cast_tag.clone(), PlayerFilter::You)
                .without_paying_mana_cost()
                .as_copy(),
        );
        let may_cast = Effect::new(crate::effects::MayEffect::new(vec![cast]));
        vec![
            crate::resolution::ResolutionSegment::from_effects(vec![
                Effect::new(crate::effects::TagTriggeringObjectEffect::new("triggering")),
                exile,
            ]),
            crate::resolution::ResolutionSegment::from_effects(vec![may_cast]),
        ]
    }

    #[test]
    fn graveyard_exile_copy_cast_window_requires_shared_exile_provenance() {
        let exile_tag = TagKey::from("__sentence_helper_exiled_l0_s0_e40");
        let matching = segments(&exile_tag, &exile_tag);
        assert_eq!(
            describe_cross_segment_graveyard_exile_copy_cast_window(&matching, 0),
            Some((
                "Exile target instant or sorcery card from a graveyard and copy it. You may cast the copy without paying its mana cost".to_string(),
                2,
            ))
        );

        let unrelated = TagKey::from("__sentence_helper_exiled_l0_s41_e80");
        assert_eq!(
            describe_cross_segment_graveyard_exile_copy_cast_window(
                &segments(&exile_tag, &unrelated),
                0,
            ),
            None,
            "an unrelated copy-cast permission must not fold into the graveyard exile"
        );
    }

    #[test]
    fn public_attack_route_keeps_prowess_and_one_copy_clause() {
        let oracle = "Creatures you control have prowess.\nWhenever Narset attacks, exile target noncreature, nonland card with mana value less than Narset's power from a graveyard and copy it. You may cast the copy without paying its mana cost.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Narset, Enlightened Exile",
        )
        .supertypes(vec![Supertype::Legendary])
        .card_types(vec![CardType::Creature])
        .power_toughness(crate::card::PowerToughness::fixed(3, 4))
        .parse_text(oracle)
        .expect("prowess grant and graveyard copy/cast trigger should parse");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            oracle.lines().map(str::to_string).collect::<Vec<_>>()
        );
    }

    #[test]
    fn public_crime_trigger_keeps_copy_cast_and_if_you_do_in_one_semantic_program() {
        let oracle = "Whenever you commit a crime, exile up to one target black card from your graveyard and copy it. You may cast the copy. If you do, you lose 2 life.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Crime Copy-Cast Probe",
        )
        .card_types(vec![CardType::Creature])
        .power_toughness(crate::card::PowerToughness::fixed(3, 3))
        .parse_text(oracle)
        .expect("crime-triggered graveyard copy/cast sequence should parse");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            [oracle]
        );
        let debug = format!("{:#?}", definition.abilities);
        assert_eq!(debug.matches("CopySpellEffect").count(), 0, "{debug}");
        assert!(
            debug.contains("CastTaggedEffect") && debug.contains("as_copy: true"),
            "{debug}"
        );
        assert!(
            debug.contains("IfEffect") && debug.contains("LoseLifeEffect"),
            "{debug}"
        );
    }

    #[test]
    fn result_window_requires_the_optional_cast_result_id() {
        let exile_tag = TagKey::from("__sentence_helper_exiled_l0_s0_e40");
        let mut fixture = segments(&exile_tag, &exile_tag);
        let may = fixture[1].default_effects[0].clone();
        fixture[1].default_effects[0] = Effect::with_id(7, may);
        fixture.push(crate::resolution::ResolutionSegment::from_effects(vec![
            Effect::if_then(
                crate::effect::EffectId(7),
                crate::effect::EffectPredicate::Happened,
                vec![Effect::lose_life(2)],
            ),
        ]));
        assert_eq!(
            describe_cross_segment_graveyard_exile_copy_cast_result_window(&fixture, 0),
            Some((
                "Exile target instant or sorcery card from a graveyard and copy it. You may cast the copy without paying its mana cost. If you do, you lose 2 life"
                    .to_string(),
                3,
            ))
        );

        let wrong = Effect::if_then(
            crate::effect::EffectId(8),
            crate::effect::EffectPredicate::Happened,
            vec![Effect::lose_life(2)],
        );
        fixture[2].default_effects[0] = wrong;
        assert_eq!(
            describe_cross_segment_graveyard_exile_copy_cast_result_window(&fixture, 0),
            None
        );
    }
}

fn describe_cross_segment_observation_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let mut effects = Vec::new();
    for segment in segments[start..].iter().take(5) {
        if !segment.self_replacements.is_empty() {
            break;
        }
        let [effect] = segment.default_effects.as_slice() else {
            break;
        };
        effects.push(effect);
    }
    if effects.len() < 2 {
        return None;
    }
    let (rendered, consumed) = describe_immediate_observation_conditionals(&effects)?;
    (consumed >= 2 && consumed <= effects.len()).then_some((rendered, consumed))
}

fn describe_cross_segment_wrapped_search_two_split_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [partition_segment, suffix_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !partition_segment.self_replacements.is_empty()
        || !suffix_segment.self_replacements.is_empty()
    {
        return None;
    }

    let [sequence_effect] = partition_segment.default_effects.as_slice() else {
        return None;
    };
    let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    let suffix_effects = match suffix_segment.default_effects.as_slice() {
        [suffix_effect]
            if suffix_effect
                .downcast_ref::<crate::effects::SequenceEffect>()
                .is_some_and(|suffix| {
                    suffix.surface == ironsmith_core::SequenceSurface::CommaThen
                }) =>
        {
            &suffix_effect
                .downcast_ref::<crate::effects::SequenceEffect>()?
                .effects
        }
        effects => effects,
    };
    if sequence.surface != ironsmith_core::SequenceSurface::Coordinated
        || sequence.effects.len() != 5
        || !matches!(suffix_effects.len(), 1 | 2)
    {
        return None;
    }

    // Lowering keeps the coordinated search/reveal/split sentence inside one
    // sequence, then starts a new resolution segment for shuffle and scry;
    // that suffix may itself retain its authored comma-then sequence.
    // Rejoin only that exact boundary; the established matcher still proves
    // every tag, count, destination, player, and optional-search invariant.
    let mut flattened = Vec::with_capacity(sequence.effects.len() + suffix_effects.len());
    flattened.extend(sequence.effects.iter());
    flattened.extend(suffix_effects.iter());
    let rendered = describe_search_two_split_hand_graveyard_sequence(&flattened)?;
    Some((rendered, 2))
}

fn describe_cross_segment_search_move_shuffle_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    if let Some([search_segment, followup_segment]) = segments.get(start..start + 2)
        && search_segment.self_replacements.is_empty()
        && followup_segment.self_replacements.is_empty()
        && let Some(choose) = cross_segment_search_choose(search_segment)
        && let Some(followup_effects) =
            flattened_cross_segment_effects(std::slice::from_ref(followup_segment))
        && let [reveal_effect, move_effect, shuffle_effect] = followup_effects.as_slice()
        && let Some(reveal) = reveal_effect.downcast_ref::<crate::effects::RevealTaggedEffect>()
        && let Some(move_to_zone) = move_effect.downcast_ref::<crate::effects::MoveToZoneEffect>()
        && let Some(shuffle) = shuffle_effect.downcast_ref::<crate::effects::ShuffleLibraryEffect>()
        && let Some(rendered) =
            describe_search_choose_then_move(choose, Some(reveal), move_to_zone, Some(shuffle))
    {
        return Some((rendered, 2));
    }

    let [search_segment, move_segment, shuffle_segment] = segments.get(start..start + 3)? else {
        return None;
    };
    if [search_segment, move_segment, shuffle_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }

    let choose = cross_segment_search_choose(search_segment)?;

    let [move_effect] = move_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_move = move_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let move_to_zone = tagged_move
        .effect
        .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    if move_to_zone.zone != Zone::Battlefield
        || !matches!(
            move_to_zone.target.base(),
            ChooseSpec::Tagged(tag) if tag.as_str() == choose.tag.as_str()
        )
    {
        return None;
    }

    let [shuffle_effect] = shuffle_segment.default_effects.as_slice() else {
        return None;
    };
    // Sentence lowering may preserve the authored `Then` boundary by wrapping
    // its sole executable effect in a one-item sequence. That wrapper carries
    // no extra semantics, so accept either representation here.
    let shuffle_effect = shuffle_effect
        .downcast_ref::<crate::effects::SequenceEffect>()
        .and_then(|sequence| match sequence.effects.as_slice() {
            [shuffle] => Some(shuffle),
            _ => None,
        })
        .unwrap_or(shuffle_effect);
    let shuffle = shuffle_effect.downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;
    let rendered = describe_search_choose_then_move(choose, None, move_to_zone, Some(shuffle))?;
    let rendered = if choose.count.max == Some(1) {
        rendered
            .replacen(", put it ", ". Put that card ", 1)
            .replacen(", then ", ". Then ", 1)
    } else {
        rendered
            .replacen(", put ", ". Put ", 1)
            .replacen(", then ", ". Then ", 1)
    };
    Some((rendered, 3))
}

fn cross_segment_search_choose(
    search_segment: &crate::resolution::ResolutionSegment,
) -> Option<&crate::effects::ChooseObjectsEffect> {
    match search_segment.default_effects.as_slice() {
        [search_effect] => {
            Some(search_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?)
        }
        [target_effect, search_effect] => {
            let target_only = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
            let choose = search_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
            let ChooseSpec::Player(target_player) = target_only.target.base() else {
                return None;
            };
            let search_owner = choose.filter.owner.as_ref().unwrap_or(&choose.chooser);
            if !same_search_player_filter(target_player, search_owner) {
                return None;
            }
            Some(choose)
        }
        _ => None,
    }
}

fn count_damage_controller_references_to_tag(effect: &Effect, tag: &TagKey) -> usize {
    let mut count = effect
        .downcast_ref::<crate::effects::DealDamageEffect>()
        .is_some_and(|damage| {
            matches!(
                damage.target.base(),
                ChooseSpec::Player(PlayerFilter::ControllerOf(
                    crate::target::ObjectRef::Tagged(reference),
                )) if reference == tag
            )
        }) as usize;
    effect.visit_child_effects(&mut |child| {
        count += count_damage_controller_references_to_tag(child, tag);
    });
    count
}

fn tagged_object_filter(effect: &Effect) -> Option<(&TagKey, &ObjectFilter)> {
    let tagged = effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let filter = if let Some(destroy) = tagged
        .effect
        .downcast_ref::<crate::effects::DestroyEffect>()
    {
        exact_single_target_object_filter(&destroy.spec)?
    } else if let Some(target_only) = tagged
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()
    {
        exact_single_target_object_filter(&target_only.target)?
    } else if let Some(counter) = tagged
        .effect
        .downcast_ref::<crate::effects::CounterEffect>()
    {
        exact_single_target_object_filter(&counter.target)?
    } else if let Some(unless_pays) = tagged
        .effect
        .downcast_ref::<crate::effects::UnlessPaysEffect>()
    {
        let [counter_effect] = unless_pays.effects.as_slice() else {
            return None;
        };
        let counter = counter_effect.downcast_ref::<crate::effects::CounterEffect>()?;
        exact_single_target_object_filter(&counter.target)?
    } else {
        return None;
    };
    Some((&tagged.tag, filter))
}

fn collect_tagged_object_filters(effect: &Effect, filters: &mut Vec<(TagKey, ObjectFilter)>) {
    if let Some((tag, filter)) = tagged_object_filter(effect) {
        filters.push((tag.clone(), filter.clone()));
    }
    effect.visit_child_effects(&mut |child| {
        collect_tagged_object_filters(child, filters);
    });
}

fn effect_tree_has_tagged_destroy(effect: &Effect, tag: &TagKey) -> bool {
    if let Some(tagged) = effect.downcast_ref::<crate::effects::TaggedEffect>()
        && tagged.tag == *tag
        && tagged
            .effect
            .downcast_ref::<crate::effects::DestroyEffect>()
            .is_some()
    {
        return true;
    }
    let mut found = false;
    effect.visit_child_effects(&mut |child| {
        if !found && effect_tree_has_tagged_destroy(child, tag) {
            found = true;
        }
    });
    found
}

/// Rejoin an authored mass tap with the locked continuous untap restriction
/// on that exact set. The plural pronoun is proven by `All`, while `other`
/// plus the explicit single card-type noun supplies the source noun without
/// relying on a card name or ambient definition context.
fn describe_cross_segment_locked_set_tap_untap_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [tap_segment, restriction_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !tap_segment.self_replacements.is_empty()
        || !restriction_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [tap_effect] = tap_segment.default_effects.as_slice() else {
        return None;
    };
    let tap = tap_effect.downcast_ref::<crate::effects::TapEffect>()?;
    let ChooseSpec::All(tapped_filter) = tap.target.base() else {
        return None;
    };

    let [card_type] = tapped_filter.card_types.as_slice() else {
        return None;
    };
    if tapped_filter.explicit_card_type_noun() != Some(*card_type) {
        return None;
    }
    let mut exact_filter = ObjectFilter::default()
        .with_type(*card_type)
        .in_zone(Zone::Battlefield);
    exact_filter.other = true;
    exact_filter.set_explicit_card_type_noun(Some(*card_type));
    if tapped_filter != &exact_filter {
        return None;
    }

    let [restriction_effect] = restriction_segment.default_effects.as_slice() else {
        return None;
    };
    let apply = restriction_effect.downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let crate::continuous::EffectTarget::Filter(restricted_filter) = &apply.target else {
        return None;
    };
    if restricted_filter != tapped_filter
        || apply.target_spec.is_some()
        || !matches!(
            apply.modification.as_ref(),
            Some(crate::continuous::Modification::DoesntUntap)
        )
        || !apply.additional_modifications.is_empty()
        || !apply.runtime_modifications.is_empty()
        || apply.until != Until::SourceUntaps
        || apply.condition != Some(Condition::SourceIsTapped)
        || apply.source_type.is_some()
        || apply.set_quantifier_surface.is_some()
        || apply.type_retention_surface.is_some()
        || apply.animation_pt_surface.is_some()
        || apply.animation_duration_surface.is_some()
        || !apply.lock_filter_at_resolution
        || apply.resolve_set_pt_values_at_resolution
        || apply.require_creature_target
    {
        return None;
    }

    let mut noun_filter = tapped_filter.clone();
    noun_filter.zone = None;
    noun_filter.other = false;
    let noun_description = noun_filter.description();
    let noun = strip_indefinite_article(&noun_description);
    let typed_source = format!("this {noun}");
    let source = match apply.source_reference_surface.as_ref() {
        None => typed_source,
        Some(crate::target::SourceReferenceSurface::ThisPermanentType(surface))
            if surface.eq_ignore_ascii_case(&typed_source) =>
        {
            surface.clone()
        }
        Some(
            crate::target::SourceReferenceSurface::FullName(surface)
            | crate::target::SourceReferenceSurface::ShortName(surface),
        ) => surface.clone(),
        Some(crate::target::SourceReferenceSurface::ThisPermanentType(_)) => return None,
    };
    let tap_text = describe_effect(tap_effect)
        .trim()
        .trim_end_matches('.')
        .to_string();
    Some((
        format!(
            "{tap_text}. They don't untap during their controllers' untap steps for as long as {source} remains tapped"
        ),
        2,
    ))
}

#[cfg(test)]
mod locked_set_tap_untap_window_tests {
    use super::*;

    fn other_artifacts() -> ObjectFilter {
        let mut filter = ObjectFilter::default()
            .with_type(CardType::Artifact)
            .in_zone(Zone::Battlefield);
        filter.other = true;
        filter.set_explicit_card_type_noun(Some(CardType::Artifact));
        filter
    }

    fn program_with_apply(
        apply: crate::effects::ApplyContinuousEffect,
    ) -> crate::resolution::ResolutionProgram {
        let filter = other_artifacts();
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::tap_all(filter)]),
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(apply)]),
        ])
    }

    fn exact_apply(filter: ObjectFilter) -> crate::effects::ApplyContinuousEffect {
        crate::effects::ApplyContinuousEffect::new(
            crate::continuous::EffectTarget::Filter(filter),
            crate::continuous::Modification::DoesntUntap,
            Until::SourceUntaps,
        )
        .with_condition(Condition::SourceIsTapped)
        .lock_filter_at_resolution()
    }

    #[test]
    fn direct_mass_tap_locked_set_keeps_its_plural_pronoun() {
        let program = program_with_apply(exact_apply(other_artifacts()));
        assert_eq!(
            describe_resolution_program(&program),
            "Tap all other artifacts. They don't untap during their controllers' untap steps for as long as this artifact remains tapped"
        );
    }

    #[test]
    fn direct_mass_tap_compactor_requires_locked_equal_filter_and_typed_source_noun() {
        let expected =
            describe_resolution_program(&program_with_apply(exact_apply(other_artifacts())));

        let unlocked = crate::effects::ApplyContinuousEffect::new(
            crate::continuous::EffectTarget::Filter(other_artifacts()),
            crate::continuous::Modification::DoesntUntap,
            Until::SourceUntaps,
        )
        .with_condition(Condition::SourceIsTapped);
        assert_ne!(
            describe_resolution_program(&program_with_apply(unlocked)),
            expected
        );

        let mut different_filter = other_artifacts();
        different_filter.tapped = true;
        assert_ne!(
            describe_resolution_program(&program_with_apply(exact_apply(different_filter))),
            expected
        );

        let mut no_typed_noun = other_artifacts();
        no_typed_noun.set_explicit_card_type_noun(None);
        let no_typed_noun_program = crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::tap_all(
                no_typed_noun.clone(),
            )]),
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(exact_apply(
                no_typed_noun,
            ))]),
        ]);
        assert_ne!(
            describe_resolution_program(&no_typed_noun_program),
            expected
        );
    }
}

fn describe_cross_segment_tagged_tap_untap_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [tap_segment, restriction_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !tap_segment.self_replacements.is_empty()
        || !restriction_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [tagged_effect] = tap_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged = tagged_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let tap = tagged.effect.downcast_ref::<crate::effects::TapEffect>()?;
    let ChooseSpec::Object(tapped_filter) = tap.target.base() else {
        return None;
    };
    if tap.target.count().is_single() {
        return None;
    }

    let [restriction_effect] = restriction_segment.default_effects.as_slice() else {
        return None;
    };
    let cant = restriction_effect.downcast_ref::<crate::effects::CantEffect>()?;
    let crate::effect::Restriction::Untap(restricted_filter) = &cant.restriction else {
        return None;
    };
    let [constraint] = restricted_filter.tagged_constraints.as_slice() else {
        return None;
    };
    if constraint.tag != tagged.tag
        || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
    {
        return None;
    }
    let mut untagged_filter = restricted_filter.clone();
    untagged_filter.tagged_constraints.clear();
    let mut selected_filter = tapped_filter.clone();
    if untagged_filter.controller.is_none() {
        selected_filter.controller = None;
    }
    if untagged_filter.owner.is_none() {
        selected_filter.owner = None;
    }
    if untagged_filter != ObjectFilter::default() && untagged_filter != selected_filter {
        return None;
    }

    let tapped_text = describe_effect(tagged_effect)
        .trim()
        .trim_end_matches('.')
        .to_string();
    let mut referent_filter = tapped_filter.clone();
    referent_filter.controller = None;
    referent_filter.owner = None;
    let subject = pluralize_noun_phrase(
        referent_filter
            .description()
            .trim_start_matches("a ")
            .trim_start_matches("an "),
    );
    let restriction_text = match cant.duration {
        Until::ControllersNextUntapStep => {
            format!("Those {subject} don't untap during their controller's next untap step")
        }
        Until::YouStopControllingThis => format!(
            "Those {subject} don't untap during their controllers' untap steps {}",
            describe_until(&cant.duration)
        ),
        _ => return None,
    };
    Some((format!("{tapped_text}. {restriction_text}"), 2))
}

#[cfg(test)]
mod tagged_tap_untap_window_tests {
    use super::*;

    fn shipbreaker_shape(restriction_tag: &str) -> crate::resolution::ResolutionProgram {
        let target = ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature()))
            .with_count(crate::effect::ChoiceCount::up_to(4));
        let tapped_tag = TagKey::from("tapped_group");
        let tap = Effect::tap(target).tag(tapped_tag);
        let restricted = ObjectFilter::default().match_tagged(
            restriction_tag,
            crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        );
        let cant = Effect::cant_until(
            crate::effect::Restriction::Untap(restricted),
            Until::YouStopControllingThis,
        );
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![tap]),
            crate::resolution::ResolutionSegment::from_effects(vec![cant]),
        ])
    }

    #[test]
    fn tag_only_restriction_keeps_plural_tapped_set_surface() {
        let rendered = describe_resolution_program(&shipbreaker_shape("tapped_group"));
        assert!(
            rendered.starts_with(
                "Tap up to four target creatures. Those creatures don't untap during their controllers' untap steps"
            ),
            "{rendered}"
        );
    }

    #[test]
    fn different_restriction_tag_does_not_claim_plural_tapped_set_surface() {
        let rendered = describe_resolution_program(&shipbreaker_shape("different_group"));
        assert!(
            !rendered.contains("Those creatures don't untap"),
            "{rendered}"
        );
    }
}

fn describe_cross_segment_conditional_tagged_tap_untap_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [conditional_segment, restriction_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !conditional_segment.self_replacements.is_empty()
        || !restriction_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional = conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let [tag_effect, tap_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    if !conditional.if_false.is_empty()
        || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
    {
        return None;
    }
    let tag = tag_effect.downcast_ref::<crate::effects::TagMatchingObjectsEffect>()?;
    let tap = tap_effect.downcast_ref::<crate::effects::TapEffect>()?;
    let ChooseSpec::All(tapped_filter) = tap.target.base() else {
        return None;
    };
    if tag.filter != *tapped_filter || !tag.additional_zones.is_empty() || tag.zone.is_some() {
        return None;
    }

    let [restriction_effect] = restriction_segment.default_effects.as_slice() else {
        return None;
    };
    let cant = restriction_effect.downcast_ref::<crate::effects::CantEffect>()?;
    if cant.duration != Until::ControllersNextUntapStep {
        return None;
    }
    let crate::effect::Restriction::Untap(restricted_filter) = &cant.restriction else {
        return None;
    };
    let [constraint] = restricted_filter.tagged_constraints.as_slice() else {
        return None;
    };
    if constraint.tag != tag.tag
        || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
    {
        return None;
    }
    let mut untagged_filter = restricted_filter.clone();
    untagged_filter.tagged_constraints.clear();
    let mut selected_filter = tapped_filter.clone();
    if !untagged_filter.attacking {
        selected_filter.attacking = false;
    }
    if untagged_filter != selected_filter {
        return None;
    }

    let condition_text = describe_condition(&conditional.condition);
    let label = matches!(
        &conditional.condition,
        Condition::ValueComparison {
            left: Value::LifeTotal(PlayerFilter::You),
            operator: crate::effect::ValueComparisonOperator::LessThanOrEqual,
            right: Value::Fixed(5),
        }
    )
    .then_some("Fateful hour — ")
    .unwrap_or_default();
    let tap_text = lowercase_first(describe_effect(tap_effect).trim().trim_end_matches('.'));
    let subject = pluralize_noun_phrase(
        untagged_filter
            .description()
            .trim_start_matches("a ")
            .trim_start_matches("an "),
    );
    Some((
        format!(
            "{label}If {condition_text}, {tap_text}. Those {subject} don't untap during their controller's next untap step"
        ),
        2,
    ))
}

fn describe_cross_segment_typed_controller_damage_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [producer_segment, damage_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !damage_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [damage_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let mut tagged_filters = Vec::new();
    for effect in &producer_segment.default_effects {
        collect_tagged_object_filters(effect, &mut tagged_filters);
    }
    let candidates = tagged_filters
        .iter()
        .filter(|(tag, _)| count_damage_controller_references_to_tag(damage_effect, tag) == 1)
        .collect::<Vec<_>>();
    let [(tag, filter)] = candidates.as_slice() else {
        return None;
    };
    let noun = if filter.stack_kind == Some(crate::filter::StackObjectKind::Spell) {
        "spell"
    } else if !filter.subtypes.is_empty()
        && filter.subtypes.iter().all(crate::Subtype::is_land_subtype)
    {
        "land"
    } else {
        match filter.card_types.as_slice() {
            [CardType::Land] => "land",
            [CardType::Artifact] => "artifact",
            [CardType::Creature] => "creature",
            [CardType::Enchantment] => "enchantment",
            [CardType::Planeswalker] => "planeswalker",
            [CardType::Battle] => "battle",
            [CardType::Artifact, CardType::Enchantment]
            | [CardType::Enchantment, CardType::Artifact] => "permanent",
            _ => return None,
        }
    };
    if count_damage_controller_references_to_tag(damage_effect, tag) != 1 {
        return None;
    }
    let producer_text = describe_effect_list(&producer_segment.default_effects)
        .trim()
        .trim_end_matches('.')
        .to_string();
    let mut damage_text = describe_effect(damage_effect)
        .trim()
        .trim_end_matches('.')
        .to_string();
    let existing_controller_reference = [
        "that object's controller",
        "that permanent's controller",
        "that artifact's controller",
        "that creature's controller",
        "that enchantment's controller",
        "that land's controller",
        "that planeswalker's controller",
        "that battle's controller",
        "that spell's controller",
    ]
    .into_iter()
    .find(|candidate| damage_text.matches(candidate).count() == 1)?;
    damage_text = damage_text.replace(
        existing_controller_reference,
        &format!("that {noun}'s controller"),
    );
    if producer_segment
        .default_effects
        .iter()
        .any(|effect| effect_tree_has_tagged_destroy(effect, tag))
        && let Some(conditional) = damage_effect.downcast_ref::<crate::effects::ConditionalEffect>()
        && matches!(conditional.condition, Condition::TaggedObjectMatches(_, _))
    {
        let Condition::TaggedObjectMatches(condition_tag, condition_filter) =
            &conditional.condition
        else {
            return None;
        };
        let mut semantic_condition_filter = condition_filter.clone();
        semantic_condition_filter.set_demonstrative_antecedent_surface(None);
        let [true_effect] = conditional.if_true.as_slice() else {
            return None;
        };
        if condition_tag != tag
            || semantic_condition_filter != ObjectFilter::default()
            || !conditional.if_false.is_empty()
            || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        {
            return None;
        }
        let true_text = describe_effect(true_effect)
            .trim()
            .trim_end_matches('.')
            .to_string();
        let controller = "that object's controller";
        let (source, amount) = true_text
            .strip_suffix(controller)
            .and_then(|prefix| prefix.rsplit_once(" deals damage "))?;
        damage_text = format!(
            "If that {noun} is put into a graveyard this way, {source} deals damage to that {noun}'s controller {}",
            amount.trim_end_matches(" to ")
        );
    }
    if noun == "land" {
        damage_text = damage_text.replace(
            "If it was a nonbasic permanent, ",
            "If that land was nonbasic, ",
        );
    } else if noun == "creature" {
        damage_text =
            damage_text.replace("If it was attacking, ", "If that creature was attacking, ");
        if damage_effect
            .downcast_ref::<crate::effects::ConditionalEffect>()
            .is_some_and(|conditional| {
                matches!(
                    &conditional.condition,
                    Condition::TaggedObjectMatches(condition_tag, condition_filter)
                        if condition_tag == tag && condition_filter.attacking
                )
            })
        {
            damage_text = damage_text.replace(
                "If that creature was attacking, ",
                "If that creature is attacking, ",
            );
        }
    }
    Some((
        format!("{producer_text}. {}", capitalize_first(&damage_text)),
        2,
    ))
}

#[cfg(test)]
mod typed_controller_damage_source_line_tests {
    use super::*;

    fn program(controller_tag: &str) -> crate::resolution::ResolutionProgram {
        let destroyed_tag = TagKey::from("destroyed_0");
        let mut land = ObjectFilter::land().in_zone(Zone::Battlefield);
        land.excluded_subtypes.push(Subtype::Swamp);
        let destroy = Effect::new(crate::effects::DestroyEffect::with_spec(
            ChooseSpec::target(ChooseSpec::Object(land)),
        ))
        .tag(destroyed_tag.clone());

        let mut nonbasic = ObjectFilter::default();
        nonbasic.excluded_supertypes.push(Supertype::Basic);
        nonbasic.set_demonstrative_antecedent_surface(Some(
            ironsmith_core::DemonstrativeAntecedentSurface::Land,
        ));
        let damage = Effect::deal_damage(
            Value::Fixed(2),
            ChooseSpec::Player(PlayerFilter::ControllerOf(
                crate::filter::ObjectRef::tagged(controller_tag),
            )),
        );
        let conditional = Effect::conditional_only(
            Condition::TaggedObjectMatchedLastKnown(destroyed_tag, nonbasic),
            vec![damage],
        );
        let mut followup = crate::resolution::ResolutionSegment::from_effects(vec![conditional]);
        followup.starts_new_source_line = true;

        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![destroy]),
            followup,
        ])
    }

    #[test]
    fn source_line_preservation_keeps_the_destroyed_land_controller_relation() {
        let rendered = describe_resolution_program_preserving_source_lines(&program("destroyed_0"));
        assert!(rendered.contains("If that land was nonbasic"), "{rendered}");
        assert!(rendered.contains("that land's controller"), "{rendered}");

        let changed_tag =
            describe_resolution_program_preserving_source_lines(&program("unrelated"));
        assert!(
            !changed_tag.contains("that land's controller"),
            "{changed_tag}"
        );
    }
}

/// Preserve the shared target set across the authored sentence boundary in
/// "Choose two ... . Exile one ... and put counters on the other." The
/// target-set tag and `AnyOtherTarget` are the executable proof that the two
/// consumers refer to complementary members of the original pair.
fn describe_cross_segment_choose_two_move_one_put_counters_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [target_segment, action_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if target_segment.starts_new_source_line
        || !action_segment.starts_new_source_line
        || !target_segment.self_replacements.is_empty()
        || !action_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [target_effect] = target_segment.default_effects.as_slice() else {
        return None;
    };
    let [action_effect] = action_segment.default_effects.as_slice() else {
        return None;
    };
    let sequence = action_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::Coordinated
        || sequence.result_label.is_some()
    {
        return None;
    }
    let [move_effect, put_effect] = sequence.effects.as_slice() else {
        return None;
    };
    let effects = [
        target_effect.clone(),
        move_effect.clone(),
        put_effect.clone(),
    ];
    Some((
        describe_choose_two_move_one_put_counters_on_other(&effects)?,
        2,
    ))
}

#[cfg(test)]
mod choose_two_move_one_put_counters_source_line_tests {
    use super::*;

    fn program(move_tag: &str) -> crate::resolution::ResolutionProgram {
        let target_tag = TagKey::from("targeted_0");
        let mut target_filter = ObjectFilter::creature().in_zone(Zone::Battlefield);
        target_filter.target_set_same_controller = true;
        let target = ChooseSpec::target(ChooseSpec::Object(target_filter))
            .with_count(ChoiceCount::exactly(2));
        let target_effect =
            Effect::new(crate::effects::TargetOnlyEffect::new(target)).tag(target_tag);

        let exile_one = Effect::new(crate::effects::MoveToZoneEffect::new(
            ChooseSpec::Object(ObjectFilter::tagged(move_tag)).with_count(ChoiceCount::exactly(1)),
            Zone::Exile,
            true,
        ));
        let counters = Effect::new(crate::effects::PutCountersEffect::new(
            crate::object::CounterType::PlusOnePlusOne,
            2,
            ChooseSpec::AnyOtherTarget,
        ));
        let coordinated = Effect::new(crate::effects::SequenceEffect::coordinated(vec![
            exile_one, counters,
        ]));
        let mut action_segment =
            crate::resolution::ResolutionSegment::from_effects(vec![coordinated]);
        action_segment.starts_new_source_line = true;

        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![target_effect]),
            action_segment,
        ])
    }

    #[test]
    fn source_line_preservation_keeps_the_complementary_target_pair() {
        assert_eq!(
            describe_resolution_program_preserving_source_lines(&program("targeted_0")),
            "Choose two target creatures controlled by the same player. Exile one of those creatures and put two +1/+1 counters on the other"
        );

        assert_ne!(
            describe_resolution_program_preserving_source_lines(&program("unrelated")),
            "Choose two target creatures controlled by the same player. Exile one of those creatures and put two +1/+1 counters on the other"
        );
    }
}

fn describe_cross_segment_chain_copy_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [antecedent_segment, enabling_segment, conditional_segment] =
        segments.get(start..start + 3)?
    else {
        return None;
    };
    if [antecedent_segment, enabling_segment, conditional_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
        || antecedent_segment.default_effects.is_empty()
    {
        return None;
    }

    let [enabling_effect] = enabling_segment.default_effects.as_slice() else {
        return None;
    };
    let enabling_effect = enabling_effect
        .downcast_ref::<crate::effects::SequenceEffect>()
        .and_then(|sequence| {
            let [only] = sequence.effects.as_slice() else {
                return None;
            };
            Some(only)
        })
        .unwrap_or(enabling_effect);
    let enabling_with_id = enabling_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    enabling_with_id
        .effect
        .downcast_ref::<crate::effects::MayEffect>()?;

    let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
        return None;
    };
    conditional_effect.downcast_ref::<crate::effects::IfEffect>()?;

    let mut combined = antecedent_segment.default_effects.clone();
    combined.push(enabling_effect.clone());
    combined.push(conditional_effect.clone());
    let rendered = describe_chain_copy_effect_list(&combined)?;
    Some((rendered, 3))
}

fn describe_cross_segment_chooser_target_exchange_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [target_segment, exchange_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !target_segment.self_replacements.is_empty()
        || !exchange_segment.self_replacements.is_empty()
    {
        return None;
    }
    let effects = target_segment
        .default_effects
        .iter()
        .chain(&exchange_segment.default_effects)
        .collect::<Vec<_>>();
    describe_target_only_then_exchange_control(&effects).map(|rendered| (rendered, 2))
}

fn describe_cross_segment_source_owner_consult_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [shuffle_segment, consult_segment, partition_segment] = segments.get(start..start + 3)?
    else {
        return None;
    };
    if shuffle_segment.starts_new_source_line
        || consult_segment.starts_new_source_line
        || partition_segment.starts_new_source_line
        || !shuffle_segment.self_replacements.is_empty()
        || !consult_segment.self_replacements.is_empty()
        || !partition_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [shuffle_effect] = shuffle_segment.default_effects.as_slice() else {
        return None;
    };
    let shuffle_with_id = shuffle_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let [conditional_effect] = consult_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional = conditional_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if conditional.condition != shuffle_with_id.id
        || conditional.predicate != EffectPredicate::Happened
        || !conditional.else_.is_empty()
        || conditional.prior_result_replacement_surface
    {
        return None;
    }
    let [consult_effect] = conditional.then.as_slice() else {
        return None;
    };
    let [move_effect, remainder_effect] = partition_segment.default_effects.as_slice() else {
        return None;
    };
    describe_source_owner_shuffle_then_reveal_named_to_battlefield(&[
        shuffle_effect,
        consult_effect,
        move_effect,
        remainder_effect,
    ])
    .map(|rendered| (rendered, 3))
}

fn describe_cross_segment_created_token_followup_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [create_segment, followup_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !create_segment.self_replacements.is_empty()
        || !followup_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [create_effect] = create_segment.default_effects.as_slice() else {
        return None;
    };
    let (created_tag, create) = tagged_create_token_effect_for_keyword(create_effect)?;
    if matches!(create.count.unhinted(), Value::Fixed(1)) {
        return None;
    }
    let [followup_effect] = followup_segment.default_effects.as_slice() else {
        return None;
    };

    let followup = if let Some(exile) =
        followup_effect.downcast_ref::<crate::effects::ExileTaggedWhenSourceLeavesEffect>()
        && &exile.tag == created_tag
    {
        "Exile those tokens when this permanent leaves the battlefield".to_string()
    } else if let Some(schedule) =
        followup_effect.downcast_ref::<crate::effects::ScheduleDelayedTriggerEffect>()
        && schedule.one_shot
        && schedule.start_next_turn
        && !schedule.until_end_of_turn
        && schedule.controller == PlayerFilter::You
        && schedule
            .trigger
            .downcast_ref::<crate::triggers::BeginningOfUpkeepTrigger>()
            .is_some()
    {
        let [delayed_effect] = schedule.effects.flattened_default_effects() else {
            return None;
        };
        let sacrifice = delayed_effect.downcast_ref::<crate::effects::SacrificeTargetEffect>()?;
        if !matches!(&sacrifice.target, ChooseSpec::Tagged(tag) if tag == created_tag) {
            return None;
        }
        "Sacrifice those tokens at the beginning of your next upkeep".to_string()
    } else {
        return None;
    };

    let creation = describe_effect(create_effect)
        .trim()
        .trim_end_matches('.')
        .to_string();
    Some((format!("{creation}. {followup}"), 2))
}

fn describe_cross_segment_created_token_reciprocal_leave_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [create_segment, exile_segment, sacrifice_segment] = segments.get(start..start + 3)? else {
        return None;
    };
    if [create_segment, exile_segment, sacrifice_segment]
        .iter()
        .any(|segment| {
            segment.starts_new_source_line
                || !segment.self_replacements.is_empty()
                || segment.default_effects.len() != 1
        })
    {
        return None;
    }
    let create_effect = &create_segment.default_effects[0];
    let (created_tag, create) = tagged_create_token_effect_for_keyword(create_effect)?;
    let exile = exile_segment.default_effects[0]
        .downcast_ref::<crate::effects::ExileTaggedWhenSourceLeavesEffect>()?;
    let reciprocal = sacrifice_segment.default_effects[0]
        .downcast_ref::<crate::effects::ScheduleEffectsWhenTaggedLeavesEffect>()?;
    let [sacrifice_effect] = reciprocal.effects.as_slice() else {
        return None;
    };
    let sacrifice = sacrifice_effect.downcast_ref::<crate::effects::SacrificeTargetEffect>()?;
    if create.count.unhinted() != &Value::Fixed(1)
        || create.controller != PlayerFilter::You
        || exile.tag != *created_tag
        || exile.controller != PlayerFilter::You
        || reciprocal.tag != *created_tag
        || reciprocal.controller != PlayerFilter::You
        || reciprocal.ability_source != crate::effects::TaggedLeavesAbilitySource::CurrentSource
        || !matches!(sacrifice.target.unhinted(), ChooseSpec::Source)
    {
        return None;
    }
    let creation = describe_effect(create_effect)
        .trim()
        .trim_end_matches('.')
        .to_string();
    Some((
        format!(
            "{creation}. Exile that token when this permanent leaves the battlefield. Sacrifice this permanent when that token leaves the battlefield"
        ),
        3,
    ))
}

fn describe_cross_segment_each_player_choose_creature_destroy_complement_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [choice_segment, destroy_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if choice_segment.starts_new_source_line
        || !choice_segment.self_replacements.is_empty()
        || !destroy_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [producer] = choice_segment.default_effects.as_slice() else {
        return None;
    };
    let [consumer] = destroy_segment.default_effects.as_slice() else {
        return None;
    };
    let consumer = if let Some(sequence) = consumer.downcast_ref::<crate::effects::SequenceEffect>()
    {
        if sequence.surface != ironsmith_core::SequenceSurface::SentenceLeadingThen
            || sequence.result_label.is_some()
        {
            return None;
        }
        let [consumer] = sequence.effects.as_slice() else {
            return None;
        };
        consumer
    } else {
        consumer
    };
    super::render_effects::describe_each_player_choose_creature_then_destroy_others_pair(
        producer, consumer,
    )
    .map(|rendered| (rendered, 2))
}

#[cfg(test)]
mod each_player_choose_creature_destroy_complement_tests {
    use super::*;

    const ORACLE: &str = "When this artifact enters, if you cast it, for each player, choose a creature with power 2 or less that player controls. Then destroy all creatures except creatures chosen this way.";

    fn definition() -> crate::CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Unstable Glyphbridge",
        )
        .card_types(vec![CardType::Artifact])
        .parse_text(ORACLE)
        .expect("quantified chosen-complement trigger should compile")
    }

    fn program(definition: &crate::CardDefinition) -> &crate::resolution::ResolutionProgram {
        let AbilityKind::Triggered(triggered) = &definition.abilities[0].kind else {
            panic!("expected triggered ability");
        };
        &triggered.effects
    }

    #[test]
    fn public_surface_rejoins_the_cross_sentence_chosen_complement() {
        let definition = definition();
        assert_eq!(
            describe_cross_segment_each_player_choose_creature_destroy_complement_window(
                &program(&definition).segments,
                0,
            )
            .map(|(rendered, _)| rendered),
            Some(
                ORACLE
                    .strip_prefix("When this artifact enters, if you cast it, ")
                    .unwrap()
                    .trim_end_matches('.')
                    .to_string()
            )
        );
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            [ORACLE]
        );
    }

    #[test]
    fn changed_choice_tag_is_not_rejoined_with_the_destroy_complement() {
        let mut effects = program(&definition()).clone();
        let mut for_players = effects.segments[0].default_effects[0]
            .downcast_ref::<crate::effects::ForPlayersEffect>()
            .expect("quantified choice")
            .clone();
        let mut choose = for_players.effects[0]
            .downcast_ref::<crate::effects::ChooseObjectsEffect>()
            .expect("object choice")
            .clone();
        choose.tag = TagKey::from("different_choice");
        for_players.effects[0] = Effect::new(choose);
        effects.segments[0].default_effects[0] = Effect::new(for_players);

        assert!(
            describe_cross_segment_each_player_choose_creature_destroy_complement_window(
                &effects.segments,
                0,
            )
            .is_none()
        );
    }
}

fn describe_cross_segment_each_player_token_characteristic_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [create_segment, characteristic_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if create_segment.starts_new_source_line
        || !create_segment.self_replacements.is_empty()
        || !characteristic_segment.self_replacements.is_empty()
    {
        return None;
    }

    let [for_players_effect] = create_segment.default_effects.as_slice() else {
        return None;
    };
    let for_players = for_players_effect.downcast_ref::<crate::effects::ForPlayersEffect>()?;
    if for_players.filter != PlayerFilter::Any
        || for_players.starting_with_controller
        || for_players.stop_after_first_happened
    {
        return None;
    }
    let [create_effect] = for_players.effects.as_slice() else {
        return None;
    };
    let (created_tag, create) = tagged_create_token_effect_for_keyword(create_effect)?;
    if create.count.unhinted() != &Value::Fixed(1)
        || create.controller != PlayerFilter::IteratedPlayer
        || create.controller_target.is_some()
        || create.use_source_chosen_color
        || create.use_source_chosen_creature_type
        || create.actor_surface_explicit
        || create.suppress_aura_attachment_choice
        || create.ability_presentation.is_some()
        || create.enters_tapped
        || create.enters_attacking
        || create.attack_target_mode.is_some()
        || create.exile_at_end_of_combat
        || create.sacrifice_at_end_of_combat
        || create.sacrifice_at_next_end_step
        || create.exile_at_next_end_step
        || !create.token.abilities.is_empty()
    {
        return None;
    }
    let token_blueprint = super::normalize_common::describe_token_blueprint(&create.token);
    let token_blueprint = token_blueprint.strip_prefix("0/0 ")?;

    let [characteristic_effect] = characteristic_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged = unwrap_effect_for_keyword(characteristic_effect)
        .downcast_ref::<crate::effects::TaggedEffect>()?;
    let continuous = unwrap_effect_for_keyword(&tagged.effect)
        .downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    if continuous.target != crate::continuous::EffectTarget::Source
        || !matches!(
            &continuous.target_spec,
            Some(ChooseSpec::Tagged(tag)) if tag == created_tag
        )
        || !continuous.additional_modifications.is_empty()
        || !continuous.runtime_modifications.is_empty()
        || continuous.until != Until::Forever
        || continuous.condition.is_some()
        || continuous.source_type.is_some()
        || continuous.source_reference_surface.is_some()
        || continuous.set_quantifier_surface != Some(ironsmith_core::SetQuantifierSurface::Those)
        || continuous.type_retention_surface.is_some()
        || continuous.animation_pt_surface.is_some()
        || continuous.animation_duration_surface.is_some()
        || continuous.lock_filter_at_resolution
        || continuous.resolve_set_pt_values_at_resolution
        || continuous.require_creature_target
    {
        return None;
    }
    let Some(crate::continuous::Modification::AddAbility(characteristic)) =
        continuous.modification.as_ref()
    else {
        return None;
    };
    let characteristic = if characteristic.id()
        == crate::static_abilities::StaticAbilityId::CharacteristicDefiningPT
    {
        characteristic.clone()
    } else {
        let model = characteristic.compiled_model()?;
        let ironsmith_core::StaticAbilityPayload::GrantAbility(grant) = &model.payload else {
            return None;
        };
        if grant.condition.is_some()
            || grant.set_quantifier_surface.is_some()
            || grant.ability.functional_zones.as_slice() != [Zone::Battlefield]
        {
            return None;
        }
        let [constraint] = grant.filter.tagged_constraints.as_slice() else {
            return None;
        };
        if constraint.tag.as_str() != "__it__"
            || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
        {
            return None;
        }
        let mut subject_filter = grant.filter.clone();
        subject_filter.tagged_constraints.clear();
        let mut expected_filter = ObjectFilter::creature();
        expected_filter.set_plural_object_noun_surface(true);
        expected_filter.set_explicit_card_type_noun(Some(CardType::Creature));
        if subject_filter != expected_filter {
            return None;
        }
        let ironsmith_core::AbilityKind::Static(granted) = &grant.ability.kind else {
            return None;
        };
        crate::static_abilities::StaticAbility::from_model(granted.clone())
    };
    if characteristic.id() != crate::static_abilities::StaticAbilityId::CharacteristicDefiningPT {
        return None;
    }
    let characteristic_text =
        describe_static_ability_with_subject(&characteristic, "this creature");
    if !characteristic_text.starts_with("This creature's power and toughness are each equal to ") {
        return None;
    }
    let quoted = super::normalize_common::quote_token_granted_ability_text(&characteristic_text);
    Some((
        format!("Each player creates a {token_blueprint}. Those creatures have {quoted}"),
        2,
    ))
}

#[cfg(test)]
mod each_player_token_characteristic_tests {
    use super::*;

    const ORACLE: &str = "Each player creates a green Elephant creature token. Those creatures have \"This token's power and toughness are each equal to the number of creature cards in its controller's graveyard.\"";

    fn definition() -> crate::CardDefinition {
        crate::CardDefinitionBuilder::new(crate::ids::CardId::new(), "Elephant Resurgence")
            .card_types(vec![CardType::Sorcery])
            .parse_text(ORACLE)
            .expect("linked per-player token characteristic should compile")
    }

    #[test]
    fn linked_created_set_characteristic_rejoins_across_the_sentence_boundary() {
        let definition = definition();
        let program = definition.spell_effect.as_ref().expect("spell program");
        assert_eq!(
            describe_cross_segment_each_player_token_characteristic_window(&program.segments, 0,)
                .map(|(rendered, _)| rendered),
            Some(ORACLE.to_string()),
            "{program:#?}"
        );
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition).join("\n"),
            ORACLE
        );
    }

    #[test]
    fn changed_created_set_tag_is_not_rejoined() {
        let mut program = definition().spell_effect.expect("spell program");
        let tagged = program.segments[1].default_effects[0]
            .downcast_ref::<crate::effects::TaggedEffect>()
            .expect("tagged characteristic grant")
            .clone();
        let mut grant = tagged
            .effect
            .downcast_ref::<crate::effects::ApplyContinuousEffect>()
            .expect("continuous characteristic grant")
            .clone();
        grant.target_spec = Some(ChooseSpec::Tagged(TagKey::from("different_created_set")));
        program.segments[1].default_effects[0] = Effect::new(grant).tag(tagged.tag);

        assert_eq!(
            describe_cross_segment_each_player_token_characteristic_window(&program.segments, 0,),
            None
        );
    }
}

fn describe_cross_segment_quantified_choice_exile_until_source_leaves_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let first_segment = segments.get(start)?;
    let (for_players_effect, exile_effect, consumed) =
        if let [for_players_effect, exile_effect] = first_segment.default_effects.as_slice() {
            if first_segment.starts_new_source_line || !first_segment.self_replacements.is_empty() {
                return None;
            }
            (for_players_effect, exile_effect, 1)
        } else {
            let exile_segment = segments.get(start + 1)?;
            if [first_segment, exile_segment].iter().any(|segment| {
                segment.starts_new_source_line || !segment.self_replacements.is_empty()
            }) {
                return None;
            }
            let [for_players_effect] = first_segment.default_effects.as_slice() else {
                return None;
            };
            let [exile_effect] = exile_segment.default_effects.as_slice() else {
                return None;
            };
            (for_players_effect, exile_effect, 2)
        };
    let for_players = for_players_effect.downcast_ref::<crate::effects::ForPlayersEffect>()?;
    let [choose_effect] = for_players.effects.as_slice() else {
        return None;
    };
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    if for_players.filter != PlayerFilter::Any
        || for_players.starting_with_controller
        || for_players.stop_after_first_happened
        || choose.chooser != PlayerFilter::IteratedPlayer
        || choose.count != crate::effect::ChoiceCount::exactly(2)
        || choose.count_value.is_some()
        || choose.aggregate_constraint.is_some()
        || choose.zone != Some(Zone::Battlefield)
        || !choose.additional_zones.is_empty()
        || choose.filter.zone != Some(Zone::Battlefield)
        || choose.filter.controller != Some(PlayerFilter::IteratedPlayer)
        || choose.is_search
        || choose.reveal
        || choose.top_only
        || choose.bottom_only
        || choose.replace_tagged_objects
    {
        return None;
    }
    let exile = exile_effect.downcast_ref::<crate::effects::ExileUntilEffect>()?;
    if exile.spec != ChooseSpec::Tagged(choose.tag.clone())
        || exile.duration != crate::effects::ExileUntilDuration::SourceLeavesBattlefield
        || exile.leave_watcher.is_some()
        || exile.return_zone != Zone::Battlefield
        || exile.face_down
        || exile.explicit_return_surface
    {
        return None;
    }
    let mut choice_filter = choose.filter.clone();
    choice_filter.zone = None;
    choice_filter.controller = None;
    choice_filter.card_types.clear();
    choice_filter.excluded_subtypes.clear();
    choice_filter.nontoken = false;
    choice_filter.union_surface = Default::default();
    let exact_nontoken_nonvehicle_creatures = choose.filter.card_types.as_slice()
        == [CardType::Creature]
        && choose.filter.excluded_subtypes.as_slice() == [Subtype::Vehicle]
        && choose.filter.nontoken
        && choice_filter == ObjectFilter::default();
    let choice = if exact_nontoken_nonvehicle_creatures {
        "Each player chooses two nontoken, non-Vehicle creatures they control".to_string()
    } else {
        describe_effect(for_players_effect)
            .trim()
            .trim_end_matches('.')
            .to_string()
    };
    Some((
        format!("{choice}. Exile them until this permanent leaves the battlefield"),
        consumed,
    ))
}

#[cfg(test)]
mod quantified_choice_exile_until_tests {
    use super::*;

    fn program(exile_tag: &str) -> crate::resolution::ResolutionProgram {
        let choice_tag = TagKey::from("participant_choice");
        let choose = crate::effects::ChooseObjectsEffect::new(
            ObjectFilter::creature()
                .controlled_by(PlayerFilter::IteratedPlayer)
                .in_zone(Zone::Battlefield),
            crate::effect::ChoiceCount::exactly(2),
            PlayerFilter::IteratedPlayer,
            choice_tag,
        )
        .in_zone(Zone::Battlefield);
        let players = Effect::new(crate::effects::ForPlayersEffect::new(
            PlayerFilter::Any,
            vec![Effect::new(choose)],
        ));
        let exile = Effect::new(crate::effects::ExileUntilEffect::new(
            ChooseSpec::Tagged(TagKey::from(exile_tag)),
            crate::effects::ExileUntilDuration::SourceLeavesBattlefield,
        ));
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![players]),
            crate::resolution::ResolutionSegment::from_effects(vec![exile]),
        ])
    }

    #[test]
    fn rejoins_the_exact_quantified_choice_collection() {
        let program = program("participant_choice");
        assert_eq!(
            describe_cross_segment_quantified_choice_exile_until_source_leaves_window(
                &program.segments,
                0,
            )
            .map(|(_, consumed)| consumed),
            Some(2)
        );
    }

    #[test]
    fn rejects_a_different_collection_tag() {
        let program = program("other_choice");
        assert_eq!(
            describe_cross_segment_quantified_choice_exile_until_source_leaves_window(
                &program.segments,
                0,
            ),
            None
        );
    }

    #[test]
    fn rejoins_the_equivalent_single_segment_program() {
        let mut program = program("participant_choice");
        let exile = program.segments.remove(1);
        program.segments[0]
            .default_effects
            .extend(exile.default_effects);

        assert_eq!(
            describe_cross_segment_quantified_choice_exile_until_source_leaves_window(
                &program.segments,
                0,
            )
            .map(|(_, consumed)| consumed),
            Some(1)
        );
    }
}

fn describe_cross_segment_created_token_copy_retarget_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [create_copy_segment, retarget_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !create_copy_segment.self_replacements.is_empty()
        || !retarget_segment.self_replacements.is_empty()
    {
        return None;
    }

    let sequence_effect = match create_copy_segment.default_effects.as_slice() {
        [sequence_effect] => sequence_effect,
        [trigger_tag, sequence_effect]
            if trigger_tag
                .downcast_ref::<crate::effects::TagTriggeringObjectEffect>()
                .is_some() =>
        {
            sequence_effect
        }
        _ => return None,
    };
    let sequence = structural_unwrap_render_wrappers(sequence_effect)
        .downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::CommaThen {
        return None;
    }
    let [create_effect, copy_effect] = sequence.effects.as_slice() else {
        return None;
    };
    let [retarget_effect] = retarget_segment.default_effects.as_slice() else {
        return None;
    };

    super::render_effects::describe_create_token_then_copy_retarget_to_created_token(&[
        create_effect,
        copy_effect,
        retarget_effect,
    ])
    .map(|rendered| (rendered, 2))
}

fn describe_cross_segment_shuffle_exile_top_cast_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [producer_segment, permission_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !permission_segment.self_replacements.is_empty()
    {
        return None;
    }
    let (shuffle_effect, exile_effect) = match producer_segment.default_effects.as_slice() {
        [shuffle_effect, exile_effect] => (shuffle_effect, exile_effect),
        [sequence_effect] => {
            let sequence = structural_unwrap_render_wrappers(sequence_effect)
                .downcast_ref::<crate::effects::SequenceEffect>()?;
            let [shuffle_effect, exile_effect] = sequence.effects.as_slice() else {
                return None;
            };
            (shuffle_effect, exile_effect)
        }
        _ => return None,
    };
    let [permission_effect] = permission_segment.default_effects.as_slice() else {
        return None;
    };
    render_shuffle_exile_top_then_cast_any_number_with_mana_value_cap(&[
        shuffle_effect,
        exile_effect,
        permission_effect,
    ])
    .map(|rendered| (rendered, 2))
}

/// Rejoin a coordinated producer sentence with the following permission
/// sentence when lowering placed them in adjacent resolution segments. The
/// shared exile tag proves the permission consumes the cards produced by the
/// first segment, so the typed effect-list renderer can recover the authored
/// duration surface and sentence boundary.
fn describe_cross_segment_linked_exile_top_play_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    fn flattened_segment_effects(
        segment: &crate::resolution::ResolutionSegment,
    ) -> Option<Vec<Effect>> {
        if !segment.self_replacements.is_empty() || segment.default_effects.is_empty() {
            return None;
        }
        if let [effect] = segment.default_effects.as_slice()
            && let Some(sequence) = structural_unwrap_render_wrappers(effect)
                .downcast_ref::<crate::effects::SequenceEffect>()
        {
            return Some(sequence.effects.clone());
        }
        Some(segment.default_effects.clone())
    }

    let mut effects = flattened_segment_effects(segments.get(start)?)?;
    let mut linked_tags = effects
        .iter()
        .filter_map(|effect| {
            structural_unwrap_render_wrappers(effect)
                .downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()
        })
        .flat_map(|exile| exile.moved_tags.iter().cloned())
        .collect::<Vec<_>>();
    let mut consumed_following_producer_segment = false;
    if linked_tags.is_empty() {
        // A preceding cost-reduction sentence can define the same authored X
        // used by an exile-top producer in the next source sentence. Start
        // the linked window at that typed reduction so the effect-list
        // renderer can emit the shared `where X is ...` basis only once.
        let [reduction_effect] = effects.as_slice() else {
            return None;
        };
        let reduction = structural_unwrap_render_wrappers(reduction_effect)
            .downcast_ref::<crate::effects::GrantNextSpellCostReductionEffect>()?;
        let reduction_value = reduction.generic_reduction.as_ref()?;
        let producer_effects = flattened_segment_effects(segments.get(start + 1)?)?;
        linked_tags = producer_effects
            .iter()
            .filter_map(|effect| {
                structural_unwrap_render_wrappers(effect)
                    .downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()
            })
            .filter(|exile| &exile.count == reduction_value)
            .flat_map(|exile| exile.moved_tags.iter().cloned())
            .collect();
        if linked_tags.is_empty() {
            return None;
        }
        effects.extend(producer_effects);
        consumed_following_producer_segment = true;
    }

    let next_segment = if consumed_following_producer_segment {
        start + 2
    } else {
        start + 1
    };
    let mut derived_tag_sources = std::collections::HashMap::new();
    for end in next_segment..(start + 5).min(segments.len()) {
        let segment_effects = flattened_segment_effects(&segments[end])?;
        for effect in &segment_effects {
            let Some(matching) = effect.downcast_ref::<crate::effects::TagMatchingObjectsEffect>()
            else {
                continue;
            };
            let source_tag = linked_tags.iter().find(|source_tag| {
                matching.filter
                    == ObjectFilter::nonland().in_zone(Zone::Exile).match_tagged(
                        (*source_tag).clone(),
                        crate::filter::TaggedOpbjectRelation::IsTaggedObject,
                    )
            });
            if let Some(source_tag) = source_tag {
                derived_tag_sources.insert(matching.tag.clone(), source_tag.clone());
            }
        }
        linked_tags.extend(derived_tag_sources.keys().cloned());

        let mut normalized_segment_effects = Vec::new();
        let mut has_linked_grant = false;
        for effect in segment_effects {
            if effect
                .downcast_ref::<crate::effects::TagMatchingObjectsEffect>()
                .is_some_and(|matching| derived_tag_sources.contains_key(&matching.tag))
            {
                continue;
            }
            if let Some(grant) = effect.downcast_ref::<crate::effects::GrantPlayTaggedEffect>()
                && let Some(source_tag) = derived_tag_sources.get(&grant.tag)
            {
                let mut normalized = grant.clone();
                normalized.tag = source_tag.clone();
                has_linked_grant = true;
                normalized_segment_effects.push(Effect::new(normalized));
                continue;
            }
            has_linked_grant |= effect
                .downcast_ref::<crate::effects::GrantPlayTaggedEffect>()
                .is_some_and(|grant| linked_tags.contains(&grant.tag));
            normalized_segment_effects.push(effect);
        }
        effects.extend(normalized_segment_effects);
        if has_linked_grant {
            return describe_linked_exile_top_play_clause(&effects)
                .map(|rendered| (rendered, end - start + 1));
        }
    }
    None
}

/// Render a narrowed temporary cast permission whose successful use grants an
/// ability to that exact card.  The helper tag used for the narrowed subset is
/// executable provenance; the authored surface is the spell filter plus the
/// prior exiled collection.
fn describe_cross_segment_filtered_exile_cast_then_has_ability_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [exile_segment, permission_segment, result_segment] = segments.get(start..start + 3)?
    else {
        return None;
    };
    if [exile_segment, permission_segment, result_segment]
        .iter()
        .any(|segment| segment.starts_new_source_line || !segment.self_replacements.is_empty())
    {
        return None;
    }

    let [exile_effect] = exile_segment.default_effects.as_slice() else {
        return None;
    };
    let exile = exile_effect.downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()?;
    let [exiled_tag] = exile.moved_tags.as_slice() else {
        return None;
    };
    if exile.player != PlayerFilter::You || exile.face_down || !exile.accumulated_tags.is_empty() {
        return None;
    }

    let [matching_effect, permission_effect] = permission_segment.default_effects.as_slice() else {
        return None;
    };
    let matching = matching_effect.downcast_ref::<crate::effects::TagMatchingObjectsEffect>()?;
    if matching.zone != Some(Zone::Exile) || !matching.additional_zones.is_empty() {
        return None;
    }
    let [constraint] = matching.filter.tagged_constraints.as_slice() else {
        return None;
    };
    if constraint.tag != *exiled_tag
        || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
        || matching.filter.zone != Some(Zone::Exile)
    {
        return None;
    }
    let mut spell_filter = matching.filter.clone();
    spell_filter.zone = None;
    spell_filter.tagged_constraints.clear();
    if spell_filter == ObjectFilter::default() {
        return None;
    }

    let with_id = permission_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let grant = with_id
        .effect
        .downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
    if grant.tag != matching.tag
        || grant.player != PlayerFilter::You
        || grant.duration != crate::effects::GrantPlayTaggedDuration::UntilEndOfTurn
        || grant.surface.is_some()
        || grant.allow_land
        || grant.mana_spend_mode != ironsmith_core::value_model::ManaSpendMode::Normal
        || grant.allow_any_color_for_cast
        || grant.while_on_top_of_library
        || grant.filter.is_some()
        || grant.during_turns_counter_put_on_source.is_some()
        || grant.spell_cost_increase.is_some()
        || grant.lands_enter_tapped
        || grant.cast_pool_is_plural
        || grant.max_plays.is_some()
    {
        return None;
    }

    let [result_effect] = result_segment.default_effects.as_slice() else {
        return None;
    };
    let result = result_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if result.condition != with_id.id
        || result.predicate != EffectPredicate::Happened
        || !result.else_.is_empty()
        || result.per_player_result
        || result.prior_result_replacement_surface
    {
        return None;
    }
    let [apply_effect] = result.then.as_slice() else {
        return None;
    };
    let apply = apply_effect.downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let Some(crate::continuous::Modification::AddAbility(ability)) = apply.modification.as_ref()
    else {
        return None;
    };
    if apply.target != crate::continuous::EffectTarget::Source
        || !matches!(&apply.target_spec, Some(ChooseSpec::Tagged(tag)) if tag == &matching.tag)
        || !apply.additional_modifications.is_empty()
        || !apply.runtime_modifications.is_empty()
        || apply.until != Until::Forever
        || apply.condition.is_some()
        || apply.source_type.is_some()
        || apply.source_reference_surface.is_some()
        || apply.set_quantifier_surface.is_some()
        || apply.type_retention_surface.is_some()
        || apply.animation_pt_surface.is_some()
        || apply.animation_duration_surface.is_some()
        || apply.lock_filter_at_resolution
        || apply.resolve_set_pt_values_at_resolution
        || apply.require_creature_target
    {
        return None;
    }

    let exile_text = describe_effect(exile_effect)
        .trim()
        .trim_end_matches('.')
        .to_string();
    let spell = super::normalize_common::describe_cast_spell_filter(
        &spell_filter,
        super::normalize_common::CastSpellFilterContext::EnclosingPermission,
    );
    let ability = lowercase_first(ability.display().trim().trim_end_matches('.'));
    Some((
        format!(
            "{exile_text}. You may cast {} from among them this turn. If you do, it has {ability}",
            with_indefinite_article(&spell)
        ),
        3,
    ))
}

fn describe_cross_segment_filtered_exile_cast_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [exile_segment, permission_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if [exile_segment, permission_segment]
        .iter()
        .any(|segment| segment.starts_new_source_line || !segment.self_replacements.is_empty())
    {
        return None;
    }
    let [exile_effect] = exile_segment.default_effects.as_slice() else {
        return None;
    };
    let exile = exile_effect.downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()?;
    let [exiled_tag] = exile.moved_tags.as_slice() else {
        return None;
    };
    if exile.player != PlayerFilter::You || exile.face_down || !exile.accumulated_tags.is_empty() {
        return None;
    }
    // The free-cast rider is a separate effect, so the paid window has two
    // effects in the permission segment and the free window has three.
    let (matching_effect, permission_effect, free_cast_effect) =
        match permission_segment.default_effects.as_slice() {
            [matching_effect, permission_effect] => (matching_effect, permission_effect, None),
            [matching_effect, permission_effect, free_cast_effect] => {
                (matching_effect, permission_effect, Some(free_cast_effect))
            }
            _ => return None,
        };
    let matching = matching_effect.downcast_ref::<crate::effects::TagMatchingObjectsEffect>()?;
    let [constraint] = matching.filter.tagged_constraints.as_slice() else {
        return None;
    };
    if matching.zone != Some(Zone::Exile)
        || !matching.additional_zones.is_empty()
        || matching.filter.zone != Some(Zone::Exile)
        || constraint.tag != *exiled_tag
        || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
    {
        return None;
    }
    let mut spell_filter = matching.filter.clone();
    spell_filter.zone = None;
    spell_filter.tagged_constraints.clear();
    if spell_filter == ObjectFilter::default() {
        return None;
    }
    let grant = permission_effect.downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
    if grant.tag != matching.tag
        || grant.player != PlayerFilter::You
        || grant.duration != crate::effects::GrantPlayTaggedDuration::UntilEndOfTurn
        || grant.surface.is_some()
        || grant.allow_land
        || grant.mana_spend_mode != ironsmith_core::value_model::ManaSpendMode::Normal
        || grant.allow_any_color_for_cast
        || grant.while_on_top_of_library
        || grant.filter.is_some()
        || grant.during_turns_counter_put_on_source.is_some()
        || grant.spell_cost_increase.is_some()
        || grant.lands_enter_tapped
        || grant.cast_pool_is_plural
        || grant.max_plays.is_some()
    {
        return None;
    }
    if let Some(free_cast_effect) = free_cast_effect {
        let free_cast = free_cast_effect
            .downcast_ref::<crate::effects::GrantTaggedSpellFreeCastUntilEndOfTurnEffect>(
        )?;
        if free_cast.tag != matching.tag
            || free_cast.player != PlayerFilter::You
            || free_cast.duration != crate::effects::GrantPlayTaggedDuration::UntilEndOfTurn
            || free_cast.while_on_top_of_library
            || free_cast.zone != Some(Zone::Exile)
        {
            return None;
        }
    }

    let exile_text = describe_effect(exile_effect)
        .trim()
        .trim_end_matches('.')
        .to_string();
    let spells = describe_plural_cast_spell_pool(&spell_filter);
    let payment = if free_cast_effect.is_some() {
        " without paying their mana costs"
    } else {
        ""
    };
    Some((
        format!("{exile_text}. You may cast {spells} from among them this turn{payment}"),
        2,
    ))
}

/// Oracle spells a plural card-type pool with "and" ("instant and sorcery
/// spells") where the singular reading of the same filter is "an instant or
/// sorcery spell".
fn describe_plural_cast_spell_pool(spell_filter: &ObjectFilter) -> String {
    let spell = super::normalize_common::describe_cast_spell_filter(
        spell_filter,
        super::normalize_common::CastSpellFilterContext::EnclosingPermission,
    );
    let spells = super::normalize_common::pluralize_cast_spell_description(&spell);
    if !is_plain_card_type_union(spell_filter) {
        return spells;
    }
    match spells.split_once(" or ") {
        Some((head, tail)) => format!("{head} and {tail}"),
        None => spells,
    }
}

/// True when the pool is only a union of bare card types, the one shape whose
/// plural wording swaps the branch connective.
fn is_plain_card_type_union(filter: &ObjectFilter) -> bool {
    if filter.any_of.len() != 2 || !filter.card_types.is_empty() {
        return false;
    }
    filter.any_of.iter().all(|branch| {
        if branch.card_types.len() != 1 {
            return false;
        }
        let mut residual = branch.clone();
        residual.card_types.clear();
        residual.set_explicit_card_type_noun(None);
        residual == ObjectFilter::default()
    })
}

#[cfg(test)]
mod filtered_exile_cast_then_has_ability_tests {
    use super::*;

    fn program(result_id: u32, result_tag: &str) -> crate::resolution::ResolutionProgram {
        let exiled_tag = TagKey::from("exiled_pool");
        let castable_tag = TagKey::from("castable_pool");
        let exile = Effect::new(
            crate::effects::ExileTopOfLibraryEffect::new(Value::Fixed(5), PlayerFilter::You)
                .tag_moved(exiled_tag.clone()),
        );
        let matching_filter = ObjectFilter::artifact().in_zone(Zone::Exile).match_tagged(
            exiled_tag,
            crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        );
        let matching = Effect::new(
            crate::effects::TagMatchingObjectsEffect::new(matching_filter, castable_tag.clone())
                .in_zone(Zone::Exile),
        );
        let grant = Effect::new(crate::effects::WithIdEffect::new(
            crate::effect::EffectId(7),
            Effect::new(crate::effects::GrantPlayTaggedEffect::new(
                castable_tag,
                PlayerFilter::You,
                crate::effects::GrantPlayTaggedDuration::UntilEndOfTurn,
                false,
                ironsmith_core::value_model::ManaSpendMode::Normal,
            )),
        ));
        let mut apply = crate::effects::ApplyContinuousEffect::new(
            crate::continuous::EffectTarget::Source,
            crate::continuous::Modification::AddAbility(
                crate::static_abilities::StaticAbility::affinity_for_artifacts(),
            ),
            Until::Forever,
        );
        apply.target_spec = Some(ChooseSpec::Tagged(TagKey::from(result_tag)));
        let apply = Effect::new(apply);
        let result = Effect::if_then(
            crate::effect::EffectId(result_id),
            EffectPredicate::Happened,
            vec![apply],
        );
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![exile]),
            crate::resolution::ResolutionSegment::from_effects(vec![matching, grant]),
            crate::resolution::ResolutionSegment::from_effects(vec![result]),
        ])
    }

    #[test]
    fn renders_exact_filtered_exiled_subset_and_result_link() {
        let program = program(7, "castable_pool");
        assert_eq!(
            describe_cross_segment_filtered_exile_cast_then_has_ability_window(
                &program.segments,
                0,
            ),
            Some((
                "Exile the top five cards of your library. You may cast an artifact spell from among them this turn. If you do, it has affinity for artifacts"
                    .to_string(),
                3,
            ))
        );
    }

    #[test]
    fn rejects_changed_result_id_or_subset_tag() {
        for program in [program(8, "castable_pool"), program(7, "other_pool")] {
            assert_eq!(
                describe_cross_segment_filtered_exile_cast_then_has_ability_window(
                    &program.segments,
                    0,
                ),
                None
            );
        }
    }

    #[test]
    fn direct_filtered_permission_uses_the_plural_spell_collection() {
        let mut program = program(7, "castable_pool");
        let with_id = program.segments[1].default_effects[1]
            .downcast_ref::<crate::effects::WithIdEffect>()
            .expect("permission result")
            .clone();
        program.segments[1].default_effects[1] = *with_id.effect;
        program.segments.pop();
        assert_eq!(
            describe_cross_segment_filtered_exile_cast_window(&program.segments, 0),
            Some((
                "Exile the top five cards of your library. You may cast artifact spells from among them this turn"
                    .to_string(),
                2,
            ))
        );
    }

    /// Ral, Leyline Prodigy's "-8: Exile the top eight cards of your library.
    /// You may cast instant and sorcery spells from among them this turn
    /// without paying their mana costs."
    fn free_cast_union_program() -> crate::resolution::ResolutionProgram {
        let exiled_tag = TagKey::from("exiled_pool");
        let castable_tag = TagKey::from("castable_pool");
        let exile = Effect::new(
            crate::effects::ExileTopOfLibraryEffect::new(Value::Fixed(8), PlayerFilter::You)
                .tag_moved(exiled_tag.clone()),
        );
        let mut instant = ObjectFilter::default();
        instant.card_types = vec![CardType::Instant];
        let mut sorcery = ObjectFilter::default();
        sorcery.card_types = vec![CardType::Sorcery];
        let mut matching_filter = ObjectFilter::default().in_zone(Zone::Exile).match_tagged(
            exiled_tag,
            crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        );
        matching_filter.any_of = vec![instant, sorcery];
        let matching = Effect::new(
            crate::effects::TagMatchingObjectsEffect::new(matching_filter, castable_tag.clone())
                .in_zone(Zone::Exile),
        );
        let grant = Effect::new(crate::effects::GrantPlayTaggedEffect::new(
            castable_tag.clone(),
            PlayerFilter::You,
            crate::effects::GrantPlayTaggedDuration::UntilEndOfTurn,
            false,
            ironsmith_core::value_model::ManaSpendMode::Normal,
        ));
        let free_cast = Effect::new(
            crate::effects::GrantTaggedSpellFreeCastUntilEndOfTurnEffect::new(
                castable_tag,
                PlayerFilter::You,
            ),
        );
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![exile]),
            crate::resolution::ResolutionSegment::from_effects(vec![matching, grant, free_cast]),
        ])
    }

    #[test]
    fn free_cast_rider_keeps_the_filtered_pool_and_plural_connective() {
        let program = free_cast_union_program();
        assert_eq!(
            describe_cross_segment_filtered_exile_cast_window(&program.segments, 0),
            Some((
                "Exile the top eight cards of your library. You may cast instant and sorcery spells from among them this turn without paying their mana costs"
                    .to_string(),
                2,
            ))
        );
    }

    #[test]
    fn rejects_a_free_cast_rider_for_another_pool() {
        let mut program = free_cast_union_program();
        let free_cast = program.segments[1].default_effects[2]
            .downcast_ref::<crate::effects::GrantTaggedSpellFreeCastUntilEndOfTurnEffect>()
            .expect("free cast rider")
            .clone();
        program.segments[1].default_effects[2] = Effect::new(
            crate::effects::GrantTaggedSpellFreeCastUntilEndOfTurnEffect {
                tag: TagKey::from("other_pool"),
                ..free_cast
            },
        );
        assert_eq!(
            describe_cross_segment_filtered_exile_cast_window(&program.segments, 0),
            None
        );
    }
}

/// Rejoin an exact singleton top-card observation with its following
/// while-exiled permission. The parser keeps the semantically useful
/// look/choose/exile provenance even when Oracle's singular wording does not
/// expose a choice, and the permission uses the shared source-exiled alias.
/// Match that full typed bridge so broader sequence rendering does not leak
/// the internal `choose one of those cards` step.
fn describe_cross_segment_treasure_look_exile_permission_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [producer_segment, permission_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !permission_segment.self_replacements.is_empty()
        || producer_segment.starts_new_source_line
    {
        return None;
    }
    let [sequence_effect] = producer_segment.default_effects.as_slice() else {
        return None;
    };
    let sequence = structural_unwrap_render_wrappers(sequence_effect)
        .downcast_ref::<crate::effects::SequenceEffect>()?;
    if !matches!(
        sequence.surface,
        ironsmith_core::SequenceSurface::CommaThen
            | ironsmith_core::SequenceSurface::RepeatedCommaThen
    ) || sequence.result_label.is_some()
    {
        return None;
    }
    let [create_effect, look_effect, choose_effect, exile_effect] = sequence.effects.as_slice()
    else {
        return None;
    };
    let (_, create) = tagged_create_token_effect_for_keyword(create_effect)?;
    if create.count.unhinted() != &Value::Fixed(1)
        || create.controller != PlayerFilter::You
        || create.controller_target.is_some()
        || create.use_source_chosen_color
        || create.use_source_chosen_creature_type
        || create.actor_surface_explicit
        || create.suppress_aura_attachment_choice
        || create.ability_presentation.is_some()
        || create.enters_tapped
        || create.enters_attacking
        || create.attack_target_mode.is_some()
        || create.exile_at_end_of_combat
        || create.sacrifice_at_end_of_combat
        || create.sacrifice_at_next_end_step
        || create.exile_at_next_end_step
        || create.token.card.name != "Treasure"
        || !create.token.card.is_token
        || create.token.card.card_types.as_slice() != [CardType::Artifact]
        || create.token.card.subtypes.as_slice() != [crate::types::Subtype::Treasure]
    {
        return None;
    }

    let look = structural_unwrap_render_wrappers(look_effect)
        .downcast_ref::<crate::effects::LookAtTopCardsEffect>()?;
    if look.player != PlayerFilter::IteratedPlayer
        || look.count.unhinted() != &Value::Fixed(1)
        || look.reveal
    {
        return None;
    }
    let choose = structural_unwrap_render_wrappers(choose_effect)
        .downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let expected_choice_filter = ObjectFilter::default().in_zone(Zone::Library).match_tagged(
        look.tag.clone(),
        crate::filter::TaggedOpbjectRelation::IsTaggedObject,
    );
    if choose.filter != expected_choice_filter
        || !choose.count.is_single()
        || choose.count_value.is_some()
        || choose.aggregate_constraint.is_some()
        || choose.chooser != PlayerFilter::You
        || choose.zone != Some(Zone::Library)
        || !choose.additional_zones.is_empty()
        || choose.description != "Choose"
        || choose.is_search
        || choose.reveal
        || choose.search_mode != crate::effect::SearchSelectionMode::Exact
        || choose.search_reveal_reference_surface.is_some()
        || choose.search_result_reference_surface.is_some()
        || choose.search_top_in_any_order_surface.is_some()
        || choose.top_only
        || choose.bottom_only
        || choose.replace_tagged_objects
        || choose.remember_as_chosen_object
    {
        return None;
    }
    let exile = structural_unwrap_render_wrappers(exile_effect)
        .downcast_ref::<crate::effects::ExileEffect>()?;
    if !exile.face_down
        || exile.turn_face_up
        || !matches!(exile.spec.base(), ChooseSpec::Tagged(tag) if tag == &choose.tag)
    {
        return None;
    }

    let [permission_effect] = permission_segment.default_effects.as_slice() else {
        return None;
    };
    let grant = structural_unwrap_render_wrappers(permission_effect)
        .downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
    if grant.tag.as_str() != crate::tag::SOURCE_EXILED_TAG
        || grant.player != PlayerFilter::You
        || grant.duration != crate::effects::GrantPlayTaggedDuration::ForAsLongAsExiled
        || grant.surface.is_some()
        || grant.allow_land
        || grant.mana_spend_mode != ironsmith_core::value_model::ManaSpendMode::Normal
        || grant.allow_any_color_for_cast
        || grant.while_on_top_of_library
        || grant.filter.is_some()
        || grant.during_turns_counter_put_on_source.is_some()
        || grant.spell_cost_increase.is_some()
        || grant.lands_enter_tapped
        || grant.cast_pool_is_plural
        || grant.max_plays.is_some()
    {
        return None;
    }

    Some((
        "Create a Treasure token, then look at the top card of that player's library and exile it face down. You may cast that card for as long as it remains exiled"
            .to_string(),
        2,
    ))
}

#[cfg(test)]
mod cross_segment_treasure_look_exile_permission_tests {
    use super::*;

    const LINE: &str = "Whenever one or more creatures you control deal combat damage to a player, create a Treasure token, then look at the top card of that player's library and exile it face down. You may cast that card for as long as it remains exiled.";

    fn program() -> crate::resolution::ResolutionProgram {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Treasure Observation Probe",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(LINE)
        .expect("public triggered line should parse");
        definition
            .abilities
            .into_iter()
            .find_map(|ability| match ability.kind {
                AbilityKind::Triggered(triggered) => Some(triggered.effects),
                _ => None,
            })
            .expect("line should lower to a triggered program")
    }

    #[test]
    fn singleton_look_choice_bridge_renders_atomically_and_tag_near_miss_does_not() {
        let exact = program();
        assert_eq!(
            describe_cross_segment_treasure_look_exile_permission_window(&exact.segments, 0),
            Some((
                "Create a Treasure token, then look at the top card of that player's library and exile it face down. You may cast that card for as long as it remains exiled"
                    .to_string(),
                2,
            )),
            "{exact:#?}"
        );
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Treasure Observation Probe",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(LINE)
        .expect("public triggered line should parse");
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition).join("\n"),
            LINE
        );

        let mut near_miss_segments = exact.segments.clone();
        let permission = near_miss_segments[1].default_effects[0]
            .downcast_ref::<crate::effects::GrantPlayTaggedEffect>()
            .expect("second segment should retain the typed permission");
        let mut wrong_permission = permission.clone();
        wrong_permission.tag = "unrelated_exiled_set".into();
        near_miss_segments[1].default_effects = vec![Effect::new(wrong_permission)];
        assert_eq!(
            describe_cross_segment_treasure_look_exile_permission_window(&near_miss_segments, 0,),
            None
        );
    }
}

fn describe_cross_segment_exile_top_put_from_among_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [producer_segment, disposition_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !disposition_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [exile_effect] = producer_segment.default_effects.as_slice() else {
        return None;
    };
    let [selection_effect, put_effect] = disposition_segment.default_effects.as_slice() else {
        return None;
    };
    render_exile_top_then_put_from_among_onto_battlefield(&[
        exile_effect,
        selection_effect,
        put_effect,
    ])
    .map(|rendered| (rendered, 2))
}

fn describe_cross_segment_exile_top_choose_play_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [selection_segment, permission_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !selection_segment.self_replacements.is_empty()
        || !permission_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [exile_effect, choose_effect] = selection_segment.default_effects.as_slice() else {
        return None;
    };
    let [grant_effect] = permission_segment.default_effects.as_slice() else {
        return None;
    };
    let exile_top = exile_effect.downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()?;
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let grant_play = grant_effect.downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
    describe_exile_top_choose_one_then_play(exile_top, choose, grant_play)
        .map(|rendered| (rendered, 2))
}

fn describe_cross_segment_reciprocal_library_inspection_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [first_segment, second_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !first_segment.self_replacements.is_empty() || !second_segment.self_replacements.is_empty() {
        return None;
    }
    let [first_effect] = first_segment.default_effects.as_slice() else {
        return None;
    };
    let [second_effect] = second_segment.default_effects.as_slice() else {
        return None;
    };
    let first = first_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    let second = second_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if first.surface != ironsmith_core::SequenceSurface::RepeatedCommaThen
        || !matches!(
            second.surface,
            ironsmith_core::SequenceSurface::CommaThen
                | ironsmith_core::SequenceSurface::Coordinated
        )
    {
        return None;
    }
    let [target_effect, first_look_effect, first_reorder_effect] = first.effects.as_slice() else {
        return None;
    };
    let [second_look_effect, second_reorder_effect] = second.effects.as_slice() else {
        return None;
    };
    let target = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let first_look = first_look_effect.downcast_ref::<crate::effects::LookAtTopCardsEffect>()?;
    let first_reorder =
        first_reorder_effect.downcast_ref::<crate::effects::ReorderLibraryTopEffect>()?;
    let second_look = second_look_effect.downcast_ref::<crate::effects::LookAtTopCardsEffect>()?;
    let second_reorder =
        second_reorder_effect.downcast_ref::<crate::effects::ReorderLibraryTopEffect>()?;
    let target_opponent = PlayerFilter::Target(Box::new(PlayerFilter::Opponent));
    if target.target != ChooseSpec::target(ChooseSpec::Player(PlayerFilter::Opponent))
        || first_look.player != target_opponent
        || first_look.viewer != PlayerFilter::You
        || first_look.reveal
        || first_reorder.tag != first_look.tag
        || first_reorder.chooser != PlayerFilter::You
        || second_look.player != PlayerFilter::You
        || second_look.viewer != target_opponent
        || second_look.reveal
        || second_reorder.tag != second_look.tag
        || second_reorder.chooser != target_opponent
        || first_look.count != Value::Fixed(3)
        || second_look.count != first_look.count
    {
        return None;
    }
    Some((
        "Look at the top three cards of target opponent's library, then put them back in any order. That player looks at the top three cards of your library, then puts them back in any order"
            .to_string(),
        2,
    ))
}

fn describe_cross_segment_target_player_exile_hand_delayed_return_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [exile_segment, delayed_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !exile_segment.self_replacements.is_empty() || !delayed_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [target, exile] = exile_segment.default_effects.as_slice() else {
        return None;
    };
    let [schedule] = delayed_segment.default_effects.as_slice() else {
        return None;
    };
    describe_target_player_exile_hand_delayed_return(&[
        target.clone(),
        exile.clone(),
        schedule.clone(),
    ])
    .map(|rendered| (rendered, 2))
}

fn describe_cross_segment_shuffle_exile_top_free_play_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let producer_segment = segments.get(start)?;
    if !producer_segment.self_replacements.is_empty() {
        return None;
    }
    let (shuffle_effect, exile_effect, grant_play_effect, grant_free_cast_effect, consumed) =
        match producer_segment.default_effects.as_slice() {
            [
                shuffle_effect,
                exile_effect,
                grant_play_effect,
                grant_free_cast_effect,
            ] => (
                shuffle_effect,
                exile_effect,
                grant_play_effect,
                grant_free_cast_effect,
                1,
            ),
            producer_effects => {
                let permission_segment = segments.get(start + 1)?;
                if !permission_segment.self_replacements.is_empty() {
                    return None;
                }
                let (shuffle_effect, exile_effect) = match producer_effects {
                    [shuffle_effect, exile_effect] => (shuffle_effect, exile_effect),
                    [sequence_effect] => {
                        let sequence = structural_unwrap_render_wrappers(sequence_effect)
                            .downcast_ref::<crate::effects::SequenceEffect>()?;
                        if sequence.surface != ironsmith_core::SequenceSurface::CommaThen {
                            return None;
                        }
                        let [shuffle_effect, exile_effect] = sequence.effects.as_slice() else {
                            return None;
                        };
                        (shuffle_effect, exile_effect)
                    }
                    _ => return None,
                };
                let [grant_play_effect, grant_free_cast_effect] =
                    permission_segment.default_effects.as_slice()
                else {
                    return None;
                };
                (
                    shuffle_effect,
                    exile_effect,
                    grant_play_effect,
                    grant_free_cast_effect,
                    2,
                )
            }
        };
    let shuffle = shuffle_effect.downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;
    if shuffle.player != PlayerFilter::You || shuffle.target_spec.is_some() {
        return None;
    }
    let exile_top = exile_effect.downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()?;
    let grant_play = grant_play_effect.downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
    let grant_free_cast = grant_free_cast_effect
        .downcast_ref::<crate::effects::GrantTaggedSpellFreeCastUntilEndOfTurnEffect>(
    )?;
    let tail =
        describe_exile_top_then_play_without_paying_mana(exile_top, grant_play, grant_free_cast)?;
    let tail = tail.replacen(" of your library.", ".", 1);
    Some((
        format!("Shuffle your library, then {}", lowercase_first(&tail)),
        consumed,
    ))
}

#[cfg(test)]
mod shuffle_exile_top_free_play_rendering_tests {
    use super::*;

    fn segments(
        moved_tag: &TagKey,
        grant_tag: &TagKey,
    ) -> Vec<crate::resolution::ResolutionSegment> {
        let shuffle = Effect::new(crate::effects::ShuffleLibraryEffect::you());
        let exile = Effect::new(
            crate::effects::ExileTopOfLibraryEffect::new(Value::Fixed(1), PlayerFilter::You)
                .tag_moved(moved_tag.clone()),
        );
        let grant_play = Effect::new(crate::effects::GrantPlayTaggedEffect::new(
            grant_tag.clone(),
            PlayerFilter::You,
            crate::effects::GrantPlayTaggedDuration::UntilEndOfTurn,
            true,
            false,
        ));
        let grant_free = Effect::new(
            crate::effects::GrantTaggedSpellFreeCastUntilEndOfTurnEffect::new(
                grant_tag.clone(),
                PlayerFilter::You,
            ),
        );
        vec![crate::resolution::ResolutionSegment {
            default_effects: vec![shuffle, exile, grant_play, grant_free],
            self_replacements: Vec::new(),
            starts_new_source_line: false,
        }]
    }

    #[test]
    fn typed_adjacent_shuffle_exile_free_play_requires_the_shared_moved_tag() {
        let moved_tag = TagKey::from("exiled_top");
        let matching = segments(&moved_tag, &moved_tag);
        assert_eq!(
            describe_cross_segment_shuffle_exile_top_free_play_window(&matching, 0),
            Some((
                "Shuffle your library, then exile the top card. Until end of turn, you may play that card without paying its mana cost".to_string(),
                1,
            ))
        );

        let unrelated_tag = TagKey::from("unrelated");
        assert_eq!(
            describe_cross_segment_shuffle_exile_top_free_play_window(
                &segments(&moved_tag, &unrelated_tag),
                0,
            ),
            None,
            "a free-play grant for an unrelated object must not fold into the exile clause"
        );
    }
}

fn describe_cross_segment_shuffle_reveal_top_free_play_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let max_consumed = segments.len().saturating_sub(start).min(5);
    for consumed in 1..=max_consumed {
        let window = segments.get(start..start + consumed)?;
        let effects = flattened_cross_segment_effects(window)?;
        if effects.len() > 5 {
            return None;
        }
        if effects.len() < 5 {
            continue;
        }

        let [
            shuffle_effect,
            reveal_effect,
            reveal_permission_effect,
            grant_play_effect,
            grant_free_effect,
        ] = effects.as_slice()
        else {
            return None;
        };
        let shuffle = structural_unwrap_render_wrappers(shuffle_effect)
            .downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;
        let reveal_top = structural_unwrap_render_wrappers(reveal_effect)
            .downcast_ref::<crate::effects::RevealTopEffect>()?;
        let reveal_permission = structural_unwrap_render_wrappers(reveal_permission_effect)
            .downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
        let grant_play = structural_unwrap_render_wrappers(grant_play_effect)
            .downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
        let grant_free_cast = structural_unwrap_render_wrappers(grant_free_effect)
            .downcast_ref::<crate::effects::GrantTaggedSpellFreeCastUntilEndOfTurnEffect>(
        )?;
        let rendered = describe_shuffle_then_reveal_top_then_temporarily_play_revealed_top_card(
            shuffle,
            reveal_top,
            reveal_permission,
            grant_play,
            grant_free_cast,
        )?;
        return Some((rendered, consumed));
    }
    None
}

/// Rejoin an authored choice sentence with its adjacent tagged play/free-cast
/// permission sentence. The effect-list matcher proves the exact shared tag,
/// exile/owner/counter filter, duration, and payment semantics; this window
/// only exposes those typed effects across the resolution-segment boundary.
fn describe_cross_segment_choose_exiled_card_free_play_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [choice_segment, permission_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !choice_segment.self_replacements.is_empty()
        || !permission_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [choose_effect] = choice_segment.default_effects.as_slice() else {
        return None;
    };
    let [grant_play_effect, grant_free_effect] = permission_segment.default_effects.as_slice()
    else {
        return None;
    };
    let effects = vec![
        choose_effect.clone(),
        grant_play_effect.clone(),
        grant_free_effect.clone(),
    ];
    describe_choose_exiled_card_then_play_without_paying(&effects).map(|rendered| (rendered, 2))
}

/// Preserve the typed producer target across an authored sentence boundary.
/// The effect-list matcher still proves the implicit tag edge, matching
/// counter type, duration, and single granted ability; this window only makes
/// the two adjacent default-only segments visible to that matcher together.
fn describe_cross_segment_counter_linked_grant_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [put_segment, grant_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !put_segment.self_replacements.is_empty() || !grant_segment.self_replacements.is_empty() {
        return None;
    }
    let [put_effect] = put_segment.default_effects.as_slice() else {
        return None;
    };
    let [grant_effect] = grant_segment.default_effects.as_slice() else {
        return None;
    };
    describe_counter_linked_grant_after_put(put_effect, grant_effect).map(|rendered| (rendered, 2))
}

/// Rejoin a tagged target modification with a conditional that inspects the
/// same affected object when authored sentence boundaries split the pair.
fn describe_cross_segment_tagged_continuous_then_counter_conditional_draw_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [leading_segment, conditional_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !leading_segment.self_replacements.is_empty()
        || !conditional_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [leading_effect] = leading_segment.default_effects.as_slice() else {
        return None;
    };
    let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
        return None;
    };
    let effects = [leading_effect, conditional_effect];
    super::render_effects::describe_tagged_continuous_then_counter_conditional_draw(&effects)
        .map(|rendered| (rendered, 2))
}

/// Rejoin a tagged temporary pump with the adjacent conditional keyword grant
/// that inspects the same affected object.
fn describe_cross_segment_tagged_pump_then_conditional_keyword_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [pump_segment, conditional_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !pump_segment.self_replacements.is_empty()
        || !conditional_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [pump_effect] = pump_segment.default_effects.as_slice() else {
        return None;
    };
    let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
        return None;
    };
    let effects = [pump_effect, conditional_effect];
    super::render_effects::describe_tagged_pump_then_conditional_keyword(&effects)
        .map(|rendered| (rendered, 2))
}

/// Rejoin a counted draw with the adjacent grant that consumes the exact same
/// filter. Sentence lowering keeps these effects in separate segments, while
/// the typed effect-list matcher proves both set identity and cardinality.
fn describe_cross_segment_draw_count_grant_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [draw_segment, grant_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !draw_segment.self_replacements.is_empty() || !grant_segment.self_replacements.is_empty() {
        return None;
    }
    let mut effects = draw_segment.default_effects.clone();
    effects.extend(grant_segment.default_effects.iter().cloned());
    describe_draw_count_then_grant_same_filter(&effects).map(|rendered| (rendered, 2))
}

/// Rejoin a hidden-pile producer with its adjacent manifest/cloak consumer.
/// The effect-list matcher proves the shared accumulating tag, face-down
/// exiles, controller, and final operation; this window only exposes those
/// typed effects across the authored sentence boundary.
fn describe_cross_segment_face_down_pile_manifest_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [pile_segment, manifest_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !pile_segment.self_replacements.is_empty()
        || !manifest_segment.self_replacements.is_empty()
        || pile_segment.default_effects.is_empty()
        || manifest_segment.default_effects.is_empty()
    {
        return None;
    }
    let effects = pile_segment
        .default_effects
        .iter()
        .chain(&manifest_segment.default_effects)
        .cloned()
        .collect::<Vec<_>>();
    describe_face_down_pile_then_manifest(&effects).map(|rendered| (rendered, 2))
}

/// Rejoin quantified choices with later consumers only when an existing
/// structural renderer proves the shared tag edge. Parser-authored sentence
/// boundaries are useful for execution, but they must not hide an aggregate
/// chosen set from renderers that already understand its typed producer and
/// consumers.
fn describe_cross_segment_quantified_choice_collection_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let collect_default_effects = |count: usize| {
        let window = segments.get(start..start + count)?;
        if window.iter().any(|segment| {
            !segment.self_replacements.is_empty() || segment.default_effects.is_empty()
        }) {
            return None;
        }
        Some(
            window
                .iter()
                .flat_map(|segment| segment.default_effects.iter().cloned())
                .collect::<Vec<_>>(),
        )
    };

    if let Some(mut effects) = collect_default_effects(3)
        && effects
            .first()
            .and_then(|effect| {
                structural_unwrap_render_wrappers(effect)
                    .downcast_ref::<crate::effects::ForPlayersEffect>()
            })
            .is_some()
    {
        if let Some(last) = effects.last_mut()
            && let Some(inner) = unwrap_singleton_sentence_boundary(last)
        {
            *last = inner.clone();
        }
        if let Some(rendered) = describe_structural_multisentence_effect_list(&effects) {
            return Some((rendered, 3));
        }
    }

    let effects = collect_default_effects(2)?;
    // A direct choice joined to the participant choices with authored
    // ", then" remains one two-child sequence in the first segment. Expose
    // exactly those two children to the existing shared-tag renderer while
    // keeping the following destroy instruction as the second segment.
    if let [sequence_effect, destroy_effect] = effects.as_slice()
        && let Some(sequence) = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()
        && sequence.surface == ironsmith_core::SequenceSurface::CommaThen
        && let [direct_effect, participant_effect] = sequence.effects.as_slice()
    {
        let flattened = [
            direct_effect.clone(),
            participant_effect.clone(),
            destroy_effect.clone(),
        ];
        if let Some(rendered) = describe_direct_then_players_choose_destroy_complement(&flattened) {
            return Some((rendered, 2));
        }
    }
    if let Some(rendered) =
        describe_for_players_choose_then_destroy_chosen_collection_pair(&effects[0], &effects[1])
    {
        return Some((rendered, 2));
    }
    if let Some(for_players) = structural_unwrap_render_wrappers(&effects[0])
        .downcast_ref::<crate::effects::ForPlayersEffect>()
        && let Some(destroy) =
            crate::compiled_text::render_effects::destroy_effect_for_choose_compaction(&effects[1])
        && let Some(rendered) =
            crate::compiled_text::render_effects::describe_for_players_may_choose_then_destroy_chosen(
                for_players,
                destroy,
            )
    {
        return Some((rendered, 2));
    }
    if let Some(rendered) = describe_direct_then_players_choose_destroy_complement(&effects) {
        return Some((rendered, 2));
    }
    if let Some(rendered) = describe_distinct_power_choice_destroy_complement(&effects) {
        return Some((rendered, 2));
    }
    // A source sentence beginning with "Then" is retained as a one-effect
    // SequenceEffect. The correlated choice/complement renderer already owns
    // that sentence boundary, so expose only that exact singleton wrapper
    // before asking it to prove the shared chosen-set tag.
    let split_choice_effects = effects
        .iter()
        .map(|effect| {
            let sentence_effect = effect
                .downcast_ref::<crate::effects::SequenceEffect>()
                .filter(|sequence| {
                    sequence.surface == ironsmith_core::SequenceSurface::SentenceLeadingThen
                        && sequence.effects.len() == 1
                })
                .map(|sequence| sequence.effects[0].clone())
                .unwrap_or_else(|| effect.clone());
            structural_unwrap_render_wrappers(&sentence_effect).clone()
        })
        .collect::<Vec<_>>();
    describe_split_for_players_choose_then_sacrifice(&split_choice_effects)
        .map(|rendered| (rendered, 2))
}

#[cfg(test)]
mod split_for_players_cross_segment_surface_tests {
    use super::*;

    #[test]
    fn sentence_leading_then_keeps_correlated_type_slot_choices_together() {
        let oracle = "For each player, you choose from among the permanents that player controls an artifact, a creature, an enchantment, and a planeswalker. Then each player sacrifices all other nonland permanents they control.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Correlated Type Slot Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(oracle)
        .expect("typed type-slot choice/complement program should parse");

        let program = definition
            .spell_effect
            .as_ref()
            .expect("sorcery should retain its resolution program");
        let [choice_segment, sacrifice_segment] = program.segments.as_slice() else {
            panic!("expected the authored two-sentence resolution program");
        };
        let [choice_effect] = choice_segment.default_effects.as_slice() else {
            panic!("expected one quantified choice effect");
        };
        let [sacrifice_sequence_effect] = sacrifice_segment.default_effects.as_slice() else {
            panic!("expected one sentence-leading sacrifice sequence");
        };
        let sacrifice_sequence = sacrifice_sequence_effect
            .downcast_ref::<crate::effects::SequenceEffect>()
            .expect("second sentence should retain its sequence surface");
        let [sacrifice_effect] = sacrifice_sequence.effects.as_slice() else {
            panic!("expected one quantified sacrifice effect");
        };
        let sacrifice_effect = structural_unwrap_render_wrappers(sacrifice_effect).clone();
        let paired_effects = [choice_effect.clone(), sacrifice_effect];
        assert_eq!(
            describe_split_for_players_choose_then_sacrifice(&paired_effects).as_deref(),
            Some(oracle.trim_end_matches('.'))
        );

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![oracle.to_string()]
        );
    }
}

/// Rejoin a looked-card procedure split across source-sentence resolution
/// segments. The effect-list helper still has to prove the shared looked tag,
/// selected tag, destination, grant target, and exact remainder, and it must
/// consume every flattened effect before this window crosses a boundary.
fn describe_cross_segment_looked_cards_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let remaining = segments.len().saturating_sub(start).min(6);
    for consumed in (2..=remaining).rev() {
        let window = segments.get(start..start + consumed)?;
        if window.iter().any(|segment| {
            !segment.self_replacements.is_empty() || segment.default_effects.is_empty()
        }) {
            continue;
        }
        let effects = window
            .iter()
            .flat_map(|segment| segment.default_effects.iter().cloned())
            .collect::<Vec<_>>();
        if let Some(rendered) = describe_complete_looked_cards_clause(&effects) {
            return Some((rendered, consumed));
        }
    }
    None
}

/// Rejoin an optional looked-card producer with the exact `if you do`
/// singleton top/rest-bottom partition in the following source sentence.
fn describe_cross_segment_may_look_top_rest_bottom_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [producer_segment, branch_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !branch_segment.self_replacements.is_empty()
    {
        return None;
    }
    let with_ids = producer_segment
        .default_effects
        .iter()
        .filter_map(|effect| effect.downcast_ref::<crate::effects::WithIdEffect>())
        .collect::<Vec<_>>();
    let [with_id] = with_ids.as_slice() else {
        return None;
    };
    let may = with_id.effect.downcast_ref::<crate::effects::MayEffect>()?;
    if may.fallback != crate::decision::FallbackStrategy::Decline {
        return None;
    }
    let [look_effect] = may.effects.as_slice() else {
        return None;
    };
    let look = look_effect.downcast_ref::<crate::effects::LookAtTopCardsEffect>()?;
    if may
        .decider
        .as_ref()
        .is_some_and(|decider| decider != &look.player)
    {
        return None;
    }

    let [branch_effect] = branch_segment.default_effects.as_slice() else {
        return None;
    };
    let branch = branch_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if branch.condition != with_id.id
        || branch.predicate != EffectPredicate::Happened
        || !branch.else_.is_empty()
    {
        return None;
    }
    let [choose_effect, move_effect, remainder_effect] = branch.then.as_slice() else {
        return None;
    };
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let remainder = remainder_effect
        .downcast_ref::<crate::effects::PutTaggedRemainderOnLibraryBottomEffect>()?;
    describe_may_look_then_put_one_top_rest_bottom(look, choose, move_effect, remainder)
        .map(|rendered| (rendered, 2))
}

/// Rejoin a declared pair of targets with the following tap-and-unattach
/// sentence when authored sentence boundaries split the shared tagged set.
fn describe_cross_segment_choose_two_tap_unattach_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let window = segments.get(start..start + 2)?;
    let effects = flattened_cross_segment_effects(window)?;
    describe_choose_two_tap_then_unattach_equipment_sequence(&effects).map(|rendered| (rendered, 2))
}

/// Rejoin a bulk battlefield return with the permanent decayed grant applied
/// to that exact returned collection in the next authored sentence.
fn describe_cross_segment_bulk_return_decayed_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let window = segments.get(start..start + 2)?;
    let effects = flattened_cross_segment_effects(window)?;
    describe_bulk_battlefield_move_then_grant_decayed(&effects).map(|rendered| (rendered, 2))
}

/// Rejoin coordinated player choices with the following count of the exact
/// affected collection. The structural matcher proves both player actions,
/// the shared tag, and the tagged graveyard count before crossing the authored
/// sentence boundary.
fn describe_cross_segment_joint_discard_or_sacrifice_then_draw_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [choice_segment, draw_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !choice_segment.self_replacements.is_empty() || !draw_segment.self_replacements.is_empty() {
        return None;
    }
    let [choice_effect] = choice_segment.default_effects.as_slice() else {
        return None;
    };
    let [draw_effect] = draw_segment.default_effects.as_slice() else {
        return None;
    };
    let effects = [choice_effect.clone(), draw_effect.clone()];
    describe_joint_discard_or_sacrifice_then_draw(&effects).map(|rendered| (rendered, 2))
}

/// Rejoin a standalone opponent choice with the following two correlated
/// graveyard choices and returns. The structural bundle matcher validates the
/// chosen-player tag and both return actors before producing Offering-style
/// coordinated text.
fn describe_cross_segment_opponent_choice_returns_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [choice_segment, returns_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !choice_segment.self_replacements.is_empty()
        || !returns_segment.self_replacements.is_empty()
        || returns_segment.starts_new_source_line
        || !matches!(choice_segment.default_effects.as_slice(), [effect] if effect.downcast_ref::<crate::effects::ChoosePlayerEffect>().is_some())
    {
        return None;
    }
    let returns = match returns_segment.default_effects.as_slice() {
        [effect] => {
            let sequence = effect.downcast_ref::<crate::effects::SequenceEffect>()?;
            if sequence.surface != ironsmith_core::SequenceSurface::CommaThen
                || sequence.result_label.is_some()
            {
                return None;
            }
            sequence.effects.as_slice()
        }
        effects => effects,
    };
    if returns.len() != 4 {
        return None;
    }
    let mut effects = choice_segment.default_effects.clone();
    effects.extend_from_slice(returns);
    describe_structural_multisentence_effect_list(&effects).map(|rendered| (rendered, 2))
}

/// Restore a relationship-aware fight surface when sentence lowering keeps
/// the two targets, the conditional action, and the fight in separate
/// resolution segments. The exact helpers prove every tag edge and require
/// the complete four-effect shape before any boundaries are crossed.
fn describe_cross_segment_conditional_pump_fight_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    fn exact_tagged_creature_reference(spec: &ChooseSpec, expected_tag: &TagKey) -> bool {
        let ChooseSpec::Object(filter) = spec.base() else {
            return false;
        };
        let [constraint] = filter.tagged_constraints.as_slice() else {
            return false;
        };
        if &constraint.tag != expected_tag
            || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
        {
            return false;
        }

        let mut residual = filter.clone();
        residual.tagged_constraints.clear();
        residual.source_surface = None;
        residual.union_surface = Default::default();
        residual == ObjectFilter::creature().in_zone(Zone::Battlefield)
    }

    let [setup_segment, fight_segment] = program.segments.as_slice() else {
        return None;
    };
    if setup_segment.starts_new_source_line
        || !setup_segment.self_replacements.is_empty()
        || !fight_segment.self_replacements.is_empty()
    {
        return None;
    }

    let [setup_target_root, conditional_root] = setup_segment.default_effects.as_slice() else {
        return None;
    };
    let setup_target_tagged = setup_target_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let setup_target = setup_target_tagged
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let conditional = conditional_root.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let [counter_root] = conditional.if_true.as_slice() else {
        return None;
    };
    let counter_tagged = counter_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let counter = counter_tagged
        .effect
        .downcast_ref::<crate::effects::PutCountersEffect>()?;
    let condition_target = match &conditional.condition {
        Condition::ValueComparison {
            left,
            operator: crate::effect::ValueComparisonOperator::GreaterThanOrEqual,
            ..
        } => match left.unhinted() {
            Value::PowerOf(target) => target.as_ref(),
            _ => return None,
        },
        _ => return None,
    };
    if setup_target.chooser.is_some()
        || setup_target.explicit_declaration
        || conditional.surface != ironsmith_core::ConditionalSurface::TrailingIf
        || !conditional.if_false.is_empty()
        || setup_target_tagged.tag != counter_tagged.tag
        || !matches!(condition_target.base(), ChooseSpec::Tagged(tag) if tag == &setup_target_tagged.tag)
        || !target_specs_select_same_objects(&counter.target, &setup_target.target)
    {
        return None;
    }

    let [then_root] = fight_segment.default_effects.as_slice() else {
        return None;
    };
    let then_sequence = then_root.downcast_ref::<crate::effects::SequenceEffect>()?;
    if then_sequence.surface != ironsmith_core::SequenceSurface::SentenceLeadingThen
        || then_sequence.result_label.is_some()
    {
        return None;
    }
    // Coordination normalization may preserve the pump/fight conjunction as
    // an inner coordinated sequence or flatten its three executable members
    // directly into the authored `Then` sequence. Both shapes retain the same
    // typed target, pump, and fight edges.
    let coordinated_effects = match then_sequence.effects.as_slice() {
        [coordinated_root]
            if coordinated_root
                .downcast_ref::<crate::effects::SequenceEffect>()
                .is_some_and(|coordinated| {
                    coordinated.surface == ironsmith_core::SequenceSurface::Coordinated
                        && coordinated.result_label.is_none()
                }) =>
        {
            &coordinated_root
                .downcast_ref::<crate::effects::SequenceEffect>()?
                .effects
        }
        effects => effects,
    };
    let [fight_target_root, pump_root, fight_root] = coordinated_effects else {
        return None;
    };

    let fight_target = fight_target_root.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let pump_tagged = pump_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let pump = pump_tagged
        .effect
        .downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let fight = fight_root.downcast_ref::<crate::effects::FightEffect>()?;
    if fight_target.chooser.is_some()
        || fight_target.explicit_declaration
        || !matches!(
            pump.runtime_modifications.as_slice(),
            [crate::effects::continuous::RuntimeModification::ModifyPowerToughness { .. }]
        )
        || pump.until != Until::EndOfTurn
        || pump.modification.is_some()
        || !pump.additional_modifications.is_empty()
        || pump.condition.is_some()
        || pump.source_type.is_some()
        || pump.set_quantifier_surface.is_some()
        || pump.type_retention_surface.is_some()
        || pump.animation_pt_surface.is_some()
        || pump.animation_duration_surface.is_some()
        || pump.lock_filter_at_resolution
        || pump.resolve_set_pt_values_at_resolution
        || !pump.require_creature_target
        || !pump
            .target_spec
            .as_ref()
            .is_some_and(|target| exact_tagged_creature_reference(target, &setup_target_tagged.tag))
        || !exact_tagged_creature_reference(&fight.creature1, &pump_tagged.tag)
        || !target_specs_select_same_objects(&fight.creature2, &fight_target.target)
        || fight.mutual_surface
    {
        return None;
    }

    let first_sentence = describe_effect_list(&setup_segment.default_effects);
    let pump_text = describe_effect(pump_root);
    if !pump_text.starts_with("that creature gets ") {
        return None;
    }
    Some(format!(
        "{}. Then {} and fights {}",
        first_sentence.trim().trim_end_matches('.'),
        pump_text.trim().trim_end_matches('.'),
        describe_choose_spec(&fight.creature2)
    ))
}

fn describe_cross_segment_conditional_fight_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if program.segments.is_empty()
        || program
            .segments
            .iter()
            .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    if let Some(rendered) = describe_cross_segment_conditional_pump_fight_program(program) {
        return Some(rendered);
    }
    let effects = program
        .flattened_default_effects()
        .iter()
        .collect::<Vec<_>>();
    describe_conditional_bonus_before_fight(&effects)
        .or_else(|| describe_two_distinct_targets_conditional_then_fight(&effects))
        .or_else(|| describe_targeted_conditional_action_then_fight(&effects))
        .or_else(|| describe_two_distinct_targets_counter_then_fight(&effects))
}

#[cfg(test)]
mod explicit_two_target_counter_fight_tests {
    use super::*;

    fn target_declaration(tag: &TagKey, controller: PlayerFilter) -> Effect {
        let mut filter = ObjectFilter::creature().in_zone(Zone::Battlefield);
        filter.controller = Some(controller);
        Effect::new(crate::effects::TargetOnlyEffect::explicit(
            ChooseSpec::target(ChooseSpec::Object(filter)),
        ))
        .tag(tag.clone())
        .tag("__chosen_objects__")
    }

    fn program(fight_first_tag: &TagKey) -> crate::resolution::ResolutionProgram {
        let first_tag = TagKey::from("targeted_0");
        let second_tag = TagKey::from("targeted_1");
        let mut threshold = ObjectFilter::default();
        threshold.power = Some(ironsmith_core::FilterComparison::GreaterThanOrEqual(4));
        let counter = Effect::new(crate::effects::PutCountersEffect::new(
            CounterType::PlusOnePlusOne,
            Value::Fixed(1),
            ChooseSpec::Tagged(first_tag.clone()),
        ))
        .tag("counters_2");
        let conditional = Effect::new(crate::effects::ConditionalEffect::new(
            Condition::TaggedObjectMatches(first_tag.clone(), threshold),
            vec![counter],
            vec![],
        ));
        let fight = Effect::new(crate::effects::FightEffect::new(
            ChooseSpec::Tagged(fight_first_tag.clone()),
            ChooseSpec::Tagged(second_tag.clone()),
        ));

        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![
                target_declaration(&first_tag, PlayerFilter::You),
                target_declaration(&second_tag, PlayerFilter::Opponent),
                conditional,
                fight,
            ]),
        ])
    }

    #[test]
    fn same_segment_target_declarations_remain_visible_to_the_fight_renderer() {
        let first_tag = TagKey::from("targeted_0");
        let rendered = describe_cross_segment_conditional_fight_program(&program(&first_tag))
            .expect("exact shared target tags should render");
        assert_eq!(
            rendered,
            "Put a +1/+1 counter on target creature you control if its power is 4 or greater. Then it fights target creature an opponent controls"
        );

        assert!(
            describe_cross_segment_conditional_fight_program(&program(&TagKey::from("unrelated")))
                .is_none()
        );
    }
}

/// Rejoin a counter-unless action with the exact result branch that applies
/// to the nonpaying spell controller. The shared branch renderer proves the
/// effect id, payer identity, captured land set, tap target, and mana-pool
/// owner before this crosses an authored sentence boundary.
fn describe_cross_segment_unless_payer_followup_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [setup_segment, followup_segment] = program.segments.as_slice() else {
        return None;
    };
    if !setup_segment.self_replacements.is_empty() || !followup_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [setup_effect] = setup_segment.default_effects.as_slice() else {
        return None;
    };
    let [followup_effect] = followup_segment.default_effects.as_slice() else {
        return None;
    };
    let setup = setup_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let followup = followup_effect.downcast_ref::<crate::effects::IfEffect>()?;
    let followup_text = describe_unless_payer_tap_controlled_set_and_empty_mana(setup, followup)?;
    let setup_text = describe_effect(&setup.effect);
    Some(format!(
        "{}. {followup_text}",
        setup_text.trim().trim_end_matches('.')
    ))
}

fn describe_cross_segment_damage_unless_paid_followup_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    fn tagged_damage_view(
        effect: &Effect,
    ) -> Option<(&crate::TagKey, &crate::effects::DealDamageEffect)> {
        let tagged = effect.downcast_ref::<crate::effects::TaggedEffect>()?;
        let outer_tag = &tagged.tag;
        let mut inner = &tagged.effect;
        while let Some(nested) = inner.downcast_ref::<crate::effects::TaggedEffect>() {
            inner = &nested.effect;
        }
        Some((outer_tag, inner.downcast_ref()?))
    }

    fn damage_view(effect: &Effect) -> Option<&crate::effects::DealDamageEffect> {
        let mut inner = effect;
        while let Some(tagged) = inner.downcast_ref::<crate::effects::TaggedEffect>() {
            inner = &tagged.effect;
        }
        inner.downcast_ref()
    }

    let [setup_segment, followup_segment] = program.segments.as_slice() else {
        return None;
    };
    if !setup_segment.self_replacements.is_empty() || !followup_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [setup_effect] = setup_segment.default_effects.as_slice() else {
        return None;
    };
    let setup = setup_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let unless = setup
        .effect
        .downcast_ref::<crate::effects::UnlessPaysEffect>()?;
    let [setup_damage_effect] = unless.effects.as_slice() else {
        return None;
    };
    let (setup_tag, setup_damage) = tagged_damage_view(setup_damage_effect)?;

    let [followup_effect] = followup_segment.default_effects.as_slice() else {
        return None;
    };
    let followup = followup_effect.downcast_ref::<crate::effects::IfEffect>()?;
    let [branch_effect] = followup.then.as_slice() else {
        return None;
    };
    let branch_damage = damage_view(branch_effect)?;
    if followup.condition != setup.id
        || followup.predicate != EffectPredicate::DidNotHappen
        || !followup.else_.is_empty()
        || unless.player != PlayerFilter::TargetPlayerOrControllerOfTarget
        || setup_damage.target != ChooseSpec::AnyTarget
        || !matches!(&branch_damage.target, ChooseSpec::Tagged(tag) if tag == setup_tag)
        || setup_damage.source_is_combat
        || branch_damage.source_is_combat
        || setup_damage.unpreventable
        || branch_damage.unpreventable
    {
        return None;
    }

    let payment = describe_total_cost(&unless.cost);
    if payment.is_empty() {
        return None;
    }
    Some(format!(
        "Deal {} damage to any target unless that permanent's controller or that player pays {payment}. If they do, deal {} damage to the permanent or player",
        describe_value(&setup_damage.amount),
        describe_value(&branch_damage.amount),
    ))
}

#[cfg(test)]
mod cross_segment_damage_unless_paid_tests {
    use super::*;

    fn program(branch_tag: &str) -> crate::resolution::ResolutionProgram {
        let damaged = TagKey::from("damaged_0");
        let setup = Effect::with_id(
            7,
            Effect::unless_pays(
                vec![Effect::deal_damage(Value::Fixed(4), ChooseSpec::AnyTarget).tag(damaged)],
                PlayerFilter::TargetPlayerOrControllerOfTarget,
                vec![ManaSymbol::Generic(2)],
            ),
        );
        let followup = Effect::if_then(
            crate::effect::EffectId(7),
            EffectPredicate::DidNotHappen,
            vec![Effect::deal_damage(
                Value::Fixed(2),
                ChooseSpec::Tagged(TagKey::from(branch_tag)),
            )],
        );
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![setup]),
            crate::resolution::ResolutionSegment::from_effects(vec![followup]),
        ])
    }

    #[test]
    fn linked_paid_branch_reuses_the_exact_damaged_recipient() {
        assert_eq!(
            describe_cross_segment_damage_unless_paid_followup_program(&program("damaged_0")),
            Some("Deal 4 damage to any target unless that permanent's controller or that player pays {2}. If they do, deal 2 damage to the permanent or player".to_string())
        );
        assert_eq!(
            describe_cross_segment_damage_unless_paid_followup_program(&program("other")),
            None
        );
    }
}

/// Rejoin a revealed hand, its singular choice, and the equivalent
/// move-then-shuffle implementation of “shuffles that card into their
/// library” before source-sentence preservation separates their shared tag.
fn describe_cross_segment_reveal_hand_choose_shuffle_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if program.segments.len() != 3
        || program
            .segments
            .iter()
            .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let flattened = program.flattened_default_effects();
    let effects = flattened.iter().collect::<Vec<_>>();
    describe_reveal_hand_choose_shuffle_into_library_bundle(&effects)
}

/// Preserve the exact three-part protection procedure while keeping the
/// first two authored sentences on one line and the final self-exile on its
/// own line. Every clause is proven from typed restrictions, phase-out
/// selection, shared duration, and source move rather than the card name.
fn describe_life_lock_protection_phase_out_exile_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [protection_segment, phase_segment, exile_segment] = program.segments.as_slice() else {
        return None;
    };
    if program
        .segments
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let [protection_effect] = protection_segment.default_effects.as_slice() else {
        return None;
    };
    let protection_sequence = protection_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    let protection_refs = protection_sequence.effects.iter().collect::<Vec<_>>();
    let protection = describe_life_lock_and_protection_from_everything(&protection_refs)?;

    let [phase_effect] = phase_segment.default_effects.as_slice() else {
        return None;
    };
    let mut phase_and_protection = vec![phase_effect];
    phase_and_protection.extend(protection_sequence.effects.iter());
    describe_phase_out_then_life_lock_and_protection(&phase_and_protection)?;

    let [exile_effect] = exile_segment.default_effects.as_slice() else {
        return None;
    };
    let exile = move_to_zone_surface_view(exile_effect)?;
    if !matches!(exile.target.base(), ChooseSpec::Source)
        || exile.zone != Zone::Exile
        || exile.enters_tapped
        || exile.enters_attacking
        || exile.enters_face_down
        || exile.enters_transformed
        || exile.transfer_exiled_with_source_links
    {
        return None;
    }
    Some(format!(
        "{protection}. {}.\n{}",
        describe_effect(phase_effect).trim().trim_end_matches('.'),
        capitalize_first(describe_effect(exile_effect).trim().trim_end_matches('.'))
    ))
}

/// Rejoin the exact two-target declaration and fight payload when the source
/// sentence boundary keeps them in separate resolution segments. The shared
/// typed helper proves the exact-two cardinality, creature domain, distinct
/// creature-type constraint, and both references to the announced target set.
fn describe_cross_segment_two_target_creature_fight_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [target_segment, fight_segment] = program.segments.as_slice() else {
        return None;
    };
    if !target_segment.self_replacements.is_empty() || !fight_segment.self_replacements.is_empty() {
        return None;
    }
    let [target] = target_segment.default_effects.as_slice() else {
        return None;
    };
    let [fight] = fight_segment.default_effects.as_slice() else {
        return None;
    };
    describe_two_target_creature_exchange_or_fight(&[target, fight])
}

#[cfg(test)]
mod cross_segment_two_target_creature_fight_tests {
    use super::*;

    const LINE: &str = "Choose two target creatures that share no creature types. Those creatures fight each other.";

    fn parsed_program() -> crate::resolution::ResolutionProgram {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Cross-Segment Fight Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(LINE)
        .expect("correlated creature fight should parse")
        .spell_effect
        .expect("spell program")
    }

    #[test]
    fn exact_two_segment_fight_rejoins_but_changed_arity_does_not() {
        let program = parsed_program();
        assert_eq!(
            describe_cross_segment_two_target_creature_fight_program(&program).as_deref(),
            Some(LINE.trim_end_matches('.')),
            "{program:#?}"
        );

        let mut changed_arity = program;
        changed_arity.segments[1]
            .default_effects
            .push(crate::effect::Effect::new(
                crate::effects::DrawCardsEffect::you(1),
            ));
        assert!(describe_cross_segment_two_target_creature_fight_program(&changed_arity).is_none());
    }
}

/// Rejoin the chosen-creature untap, counters, keyword grant, additional
/// combat, and attack restriction when each authored sentence occupies its
/// own resolution segment.
fn describe_cross_segment_chosen_creatures_blessing_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if program.segments.len() < 2
        || program
            .segments
            .iter()
            .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let effects = flattened_cross_segment_effects(&program.segments)?;
    describe_chosen_creatures_blessing_additional_combat_clause(&effects)
}

/// Target declarations and optional actions can occupy one or more resolution
/// segments even when Oracle presents them as one multi-sentence procedure.
/// Flatten only the default branches, then require the complete typed
/// target/chooser/consult shape before choosing that authored surface.
fn describe_cross_segment_relative_player_target_consult_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if program.segments.is_empty()
        || program
            .segments
            .iter()
            .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    describe_relative_player_target_then_optional_consult(program.flattened_default_effects())
        .or_else(|| {
            describe_relative_player_target_then_optional_search(
                program.flattened_default_effects(),
            )
        })
}

/// A mixed player/planeswalker target collection is represented by separate
/// typed player and object iterators in the second source-sentence segment.
/// Flatten only replacement-free default branches, then let the structural
/// matcher prove the shared declaration and complete equivalent procedures.
fn describe_cross_segment_mixed_target_consult_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if program.segments.is_empty()
        || program
            .segments
            .iter()
            .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    describe_mixed_target_collection_consult_damage(program.flattened_default_effects())
}

#[cfg(test)]
mod relative_player_target_consult_program_tests {
    use super::*;

    #[test]
    fn rejoins_active_player_target_and_optional_consult_across_segments() {
        const EXPECTED: &str = "That player chooses target player who controls more creatures than they do and is their opponent. The first player may reveal cards from the top of their library until they reveal a creature card. If the first player does, that player puts that card onto the battlefield and all other cards revealed this way into their graveyard";

        let all_tag = TagKey::from("revealed");
        let match_tag = TagKey::from("matched");
        let target = Effect::new(
            crate::effects::TargetOnlyEffect::explicit(ChooseSpec::target(ChooseSpec::Player(
                PlayerFilter::OpponentWithMoreControlledObjectsThan {
                    player: Box::new(PlayerFilter::Active),
                    filter: Box::new(ObjectFilter::creature()),
                },
            )))
            .with_chooser(PlayerFilter::Active),
        );

        let mut creature_card = ObjectFilter::creature();
        creature_card.zone = None;
        let consult = Effect::new(crate::effects::ConsultTopOfLibraryEffect::new(
            PlayerFilter::Active,
            crate::effects::consult_helpers::LibraryConsultMode::Reveal,
            creature_card,
            crate::effects::ConsultTopOfLibraryStopRule::FirstMatch,
            all_tag.clone(),
            match_tag.clone(),
        ));
        let move_match = Effect::new(crate::effects::MoveToZoneEffect::new(
            ChooseSpec::Tagged(match_tag.clone()),
            Zone::Battlefield,
            false,
        ));
        let mut iterated_is_match = ObjectFilter::default();
        iterated_is_match
            .tagged_constraints
            .push(crate::filter::TaggedObjectConstraint {
                tag: match_tag,
                relation: crate::filter::TaggedOpbjectRelation::IsTaggedObject,
            });
        let move_remainder = Effect::new(crate::effects::ForEachTaggedEffect::new(
            all_tag,
            vec![Effect::conditional(
                Condition::TaggedObjectMatches(TagKey::from("__it__"), iterated_is_match),
                vec![],
                vec![Effect::new(crate::effects::MoveToZoneEffect::new(
                    ChooseSpec::Iterated,
                    Zone::Graveyard,
                    false,
                ))],
            )],
        ));
        let may = Effect::new(crate::effects::MayEffect::new_for_player(
            vec![consult, move_match, move_remainder],
            PlayerFilter::Active,
        ));

        let single_segment =
            crate::resolution::ResolutionProgram::from_effects(vec![target.clone(), may.clone()]);
        assert_eq!(describe_resolution_program(&single_segment), EXPECTED);

        let program = crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![target]),
            crate::resolution::ResolutionSegment::from_effects(vec![may]),
        ]);

        assert_eq!(describe_resolution_program(&program), EXPECTED);
    }
}

/// Rejoin the same-name extraction procedure when source sentence boundaries
/// placed its typed effects in adjacent resolution segments. Sequence wrappers
/// only preserve punctuation/coordination here; the shared renderer still has
/// to prove the chosen-name or targeted-object tag, the three-zone search, the
/// matching exile consumer, and the searched player's shuffle (and draw, when
/// present) before this window can succeed.
fn describe_cross_segment_same_name_extraction_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    fn collect_sequence_members(effect: &Effect, flattened: &mut Vec<Effect>) {
        let unwrapped = structural_unwrap_render_wrappers(effect);
        if let Some(sequence) = unwrapped.downcast_ref::<crate::effects::SequenceEffect>() {
            for member in &sequence.effects {
                collect_sequence_members(member, flattened);
            }
        } else if unwrapped
            .downcast_ref::<crate::effects::TagTriggeringObjectEffect>()
            .is_some()
            || unwrapped
                .downcast_ref::<crate::effects::TagTriggeringSourceEffect>()
                .is_some()
            || unwrapped
                .downcast_ref::<crate::effects::TagTriggeringBlockersEffect>()
                .is_some()
        {
            // Trigger referent setup has no authored surface. The typed
            // player/object filters on the remaining effects retain the
            // relationship that the extraction matcher must prove.
        } else {
            flattened.push(effect.clone());
        }
    }

    let remaining = segments.len().saturating_sub(start).min(6);
    for consumed in (1..=remaining).rev() {
        let window = segments.get(start..start + consumed)?;
        if window.iter().any(|segment| {
            !segment.self_replacements.is_empty() || segment.default_effects.is_empty()
        }) {
            continue;
        }
        let mut flattened = Vec::new();
        for effect in window
            .iter()
            .flat_map(|segment| segment.default_effects.iter())
        {
            collect_sequence_members(effect, &mut flattened);
        }
        if let Some(rendered) = describe_same_name_three_zone_extraction(&flattened) {
            return Some((rendered, consumed));
        }
    }
    None
}

/// Expose same-name search pipelines across both sentence segments and
/// punctuation-only `SequenceEffect` wrappers. The downstream matchers still
/// require the complete typed producer/tag/search/reveal/move/shuffle graph,
/// so flattening here cannot turn an unrelated sequence into authored prose.
fn describe_cross_segment_same_name_search_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    fn collect_sequence_members<'a>(effect: &'a Effect, flattened: &mut Vec<&'a Effect>) {
        let unwrapped = structural_unwrap_render_wrappers(effect);
        if let Some(sequence) = unwrapped.downcast_ref::<crate::effects::SequenceEffect>() {
            for member in &sequence.effects {
                collect_sequence_members(member, flattened);
            }
        } else {
            flattened.push(effect);
        }
    }

    let remaining = segments.len().saturating_sub(start).min(4);
    for consumed in (1..=remaining).rev() {
        let window = segments.get(start..start + consumed)?;
        if window.iter().any(|segment| {
            !segment.self_replacements.is_empty() || segment.default_effects.is_empty()
        }) {
            continue;
        }
        let mut flattened = Vec::new();
        for effect in window
            .iter()
            .flat_map(|segment| segment.default_effects.iter())
        {
            collect_sequence_members(effect, &mut flattened);
        }
        if let Some(rendered) = describe_single_hand_reveal_same_name_search(&flattened)
            .or_else(|| describe_same_name_reference_search_bundle(&flattened))
            .or_else(|| describe_target_player_search_exile_shuffle_bundle(&flattened))
            .or_else(|| describe_single_hand_reveal_setup(&flattened))
        {
            return Some((rendered, consumed));
        }
    }
    None
}

fn describe_cross_segment_target_player_draw_exile_copy_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [setup_segment, result_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !setup_segment.self_replacements.is_empty() || !result_segment.self_replacements.is_empty() {
        return None;
    }
    let [setup_effect] = setup_segment.default_effects.as_slice() else {
        return None;
    };
    let [result_effect] = result_segment.default_effects.as_slice() else {
        return None;
    };
    let rendered = describe_target_player_draw_exile_then_copy_result(&[
        setup_effect.clone(),
        result_effect.clone(),
    ])?;
    Some((rendered, 2))
}

/// Strip the compiler's last-known-information guard when an outer
/// `ForEachTaggedEffect` already proves that the current object is a card.
fn unwrap_tautological_iterated_card_lki_guard(effect: &Effect) -> Option<&Effect> {
    if effect
        .downcast_ref::<crate::effects::SequenceEffect>()
        .is_some()
    {
        return Some(effect);
    }

    let conditional = effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::TaggedObjectMatchedLastKnown(tag, filter) = &conditional.condition else {
        return None;
    };
    if tag.as_str() != "__it__"
        || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }

    // `card` is presentation-only here: every member of the outer exiled-card
    // tag is already a card. Do not erase a real per-card filter such as
    // creature, nonland, or a characteristic constraint.
    if !filter.has_explicit_card_noun() {
        return None;
    }
    let mut bare_card = filter.clone();
    bare_card.set_explicit_card_noun(false);
    if bare_card != ObjectFilter::default() {
        return None;
    }

    let [guarded_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    guarded_effect
        .downcast_ref::<crate::effects::SequenceEffect>()
        .map(|_| guarded_effect)
}

/// Compact the general "other than basic land cards" graveyard-exile
/// procedure. The parser represents that exception as the De Morgan union
/// `nonland OR nonbasic`, then iterates the exact exiled tag to search and
/// exile same-name cards before the targeted player shuffles.
fn describe_graveyard_exception_same_name_exile_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [exile_segment, search_segment, shuffle_segment] = program.segments.as_slice() else {
        return None;
    };
    if [exile_segment, search_segment, shuffle_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let [exile_effect] = exile_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_exile = exile_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let exile = tagged_exile
        .effect
        .downcast_ref::<crate::effects::ExileEffect>()?;
    let ChooseSpec::All(exile_filter) = &exile.spec else {
        return None;
    };
    let [first_exception, second_exception] = exile_filter.any_of.as_slice() else {
        return None;
    };

    let target_graveyard = |filter: &ObjectFilter| {
        filter.zone == Some(Zone::Graveyard)
            && matches!(
                filter.owner.as_ref(),
                Some(PlayerFilter::Target(player)) if **player == PlayerFilter::Any
            )
    };
    if !target_graveyard(first_exception) || !target_graveyard(second_exception) {
        return None;
    }
    let only_exception =
        |filter: &ObjectFilter, card_type: Option<CardType>, supertype: Option<Supertype>| {
            if card_type.is_some_and(|kind| filter.excluded_card_types.as_slice() != [kind])
                || card_type.is_none() && !filter.excluded_card_types.is_empty()
                || supertype.is_some_and(|kind| filter.excluded_supertypes.as_slice() != [kind])
                || supertype.is_none() && !filter.excluded_supertypes.is_empty()
            {
                return false;
            }
            let mut bare = filter.clone();
            bare.zone = None;
            bare.owner = None;
            bare.excluded_card_types.clear();
            bare.excluded_supertypes.clear();
            bare.set_explicit_card_noun(false);
            bare == ObjectFilter::default()
        };
    let is_nonland = |filter: &ObjectFilter| only_exception(filter, Some(CardType::Land), None);
    let is_nonbasic = |filter: &ObjectFilter| only_exception(filter, None, Some(Supertype::Basic));
    if !((is_nonland(first_exception) && is_nonbasic(second_exception))
        || (is_nonbasic(first_exception) && is_nonland(second_exception)))
    {
        return None;
    }
    let mut exile_base = exile_filter.clone();
    exile_base.any_of.clear();
    if exile_base != ObjectFilter::default() {
        return None;
    }

    let [search_effect] = search_segment.default_effects.as_slice() else {
        return None;
    };
    let for_each = search_effect.downcast_ref::<crate::effects::ForEachTaggedEffect>()?;
    if for_each.tag != tagged_exile.tag {
        return None;
    }
    let [search_sequence_effect] = for_each.effects.as_slice() else {
        return None;
    };
    let search_sequence = unwrap_tautological_iterated_card_lki_guard(search_sequence_effect)?
        .downcast_ref::<crate::effects::SequenceEffect>()?;
    let [choose_effect, move_each_effect] = search_sequence.effects.as_slice() else {
        return None;
    };
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let [same_name_constraint] = choose.filter.tagged_constraints.as_slice() else {
        return None;
    };
    if !choose.is_search
        || choose.count.min != 0
        || choose.count.max.is_some()
        || choose.chooser != PlayerFilter::You
        || choose.search_mode != crate::effect::SearchSelectionMode::AllMatching
        || choose.zone != Some(Zone::Library)
        || !choose.additional_zones.is_empty()
        || choose.filter.zone != Some(Zone::Library)
        || !matches!(
            choose.filter.owner.as_ref(),
            Some(PlayerFilter::AliasedTarget(player)) if **player == PlayerFilter::Any
        )
        || same_name_constraint.tag.as_str() != "__it__"
        || same_name_constraint.relation != crate::filter::TaggedOpbjectRelation::SameNameAsTagged
    {
        return None;
    }
    let mut search_base = choose.filter.clone();
    search_base.zone = None;
    search_base.owner = None;
    search_base.tagged_constraints.clear();
    search_base.set_explicit_card_noun(false);
    if search_base != ObjectFilter::default() {
        return None;
    }
    let move_each = move_each_effect.downcast_ref::<crate::effects::ForEachTaggedEffect>()?;
    let [move_effect] = move_each.effects.as_slice() else {
        return None;
    };
    let move_to_zone = move_effect.downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    if move_each.tag != choose.tag
        || move_to_zone.zone != Zone::Exile
        || !matches!(move_to_zone.target.base(), ChooseSpec::Iterated)
    {
        return None;
    }

    let [shuffle_effect] = shuffle_segment.default_effects.as_slice() else {
        return None;
    };
    let shuffle_effect = shuffle_effect
        .downcast_ref::<crate::effects::SequenceEffect>()
        .and_then(|sequence| match sequence.effects.as_slice() {
            [shuffle] => Some(shuffle),
            _ => None,
        })
        .unwrap_or(shuffle_effect);
    let shuffle = shuffle_effect.downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;
    if !matches!(
        shuffle.player,
        PlayerFilter::Target(ref player) | PlayerFilter::AliasedTarget(ref player)
            if **player == PlayerFilter::Any
    ) {
        return None;
    }

    Some(
        "Exile all cards from target player's graveyard other than basic land cards. For each card exiled this way, search that player's library for all cards with the same name as that card and exile them. Then that player shuffles"
            .to_string(),
    )
}

#[cfg(test)]
mod graveyard_exception_same_name_exile_program_tests {
    use super::*;

    fn guarded_search_sequence(tag: &str, filter: ObjectFilter) -> Effect {
        let sequence = Effect::new(crate::effects::SequenceEffect::new(vec![
            Effect::gain_life(1),
        ]));
        Effect::conditional_only(
            Condition::TaggedObjectMatchedLastKnown(TagKey::from(tag), filter),
            vec![sequence],
        )
    }

    #[test]
    fn iterated_card_lki_guard_unwraps_the_tautological_search_sequence() {
        let mut card_filter = ObjectFilter::default();
        card_filter.set_explicit_card_noun(true);
        let guarded = guarded_search_sequence("__it__", card_filter);

        assert!(
            unwrap_tautological_iterated_card_lki_guard(&guarded)
                .and_then(|effect| effect.downcast_ref::<crate::effects::SequenceEffect>())
                .is_some(),
            "the current iterated card is necessarily a card"
        );
    }

    #[test]
    fn iterated_card_lki_guard_rejects_filtered_or_unrelated_conditions() {
        let filtered = guarded_search_sequence("__it__", ObjectFilter::creature());
        assert!(unwrap_tautological_iterated_card_lki_guard(&filtered).is_none());

        let nounless = guarded_search_sequence("__it__", ObjectFilter::default());
        assert!(unwrap_tautological_iterated_card_lki_guard(&nounless).is_none());

        let mut card_filter = ObjectFilter::default();
        card_filter.set_explicit_card_noun(true);
        let unrelated = guarded_search_sequence("some_other_card", card_filter);
        assert!(unwrap_tautological_iterated_card_lki_guard(&unrelated).is_none());
    }
}

fn describe_cross_segment_may_search_conditional_else_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let first = segments.get(start)?;
    if !first.self_replacements.is_empty() {
        return None;
    }
    let (may_effect, conditional_effect, otherwise_segment, consumed) =
        if let [may_effect, conditional_effect] = first.default_effects.as_slice() {
            (may_effect, conditional_effect, segments.get(start + 1)?, 2)
        } else {
            let [may_effect] = first.default_effects.as_slice() else {
                return None;
            };
            let conditional_segment = segments.get(start + 1)?;
            let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
                return None;
            };
            if !conditional_segment.self_replacements.is_empty() {
                return None;
            }
            (may_effect, conditional_effect, segments.get(start + 2)?, 3)
        };
    if !otherwise_segment.self_replacements.is_empty() {
        return None;
    }
    let may = may_effect.downcast_ref::<crate::effects::MayEffect>()?;
    let with_id = conditional_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let conditional = with_id
        .effect
        .downcast_ref::<crate::effects::ConditionalEffect>()?;
    if !conditional.if_false.is_empty() {
        return None;
    }

    let [otherwise_effect] = otherwise_segment.default_effects.as_slice() else {
        return None;
    };
    let otherwise = otherwise_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if otherwise.condition != with_id.id
        || otherwise.predicate != crate::effect::EffectPredicate::DidNotHappen
        || !otherwise.else_.is_empty()
        || otherwise.then.len() != 1
    {
        return None;
    }

    let mut merged = conditional.clone();
    merged.if_false = otherwise.then.clone();
    describe_may_search_reveal_shuffle_then_conditional_move(may, &merged)
        .map(|rendered| (rendered, consumed))
}

/// Rejoin an all-creatures goad result with a restriction on the exact
/// generated `goaded_*` set. Source-sentence lowering keeps these as adjacent
/// executable segments; the result tag proves that "those creatures" is a
/// back-reference rather than an unrelated global blocking restriction.
fn describe_cross_segment_goaded_set_block_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [goad_effect] = segments.get(start)?.default_effects.as_slice() else {
        return None;
    };
    let first = segments.get(start)?;
    let second = segments.get(start + 1)?;
    if !first.self_replacements.is_empty() || !second.self_replacements.is_empty() {
        return None;
    }
    let [cant_effect] = second.default_effects.as_slice() else {
        return None;
    };
    let (goad, produced_tag) =
        if let Some(tagged) = goad_effect.downcast_ref::<crate::effects::TaggedEffect>() {
            (
                tagged.effect.downcast_ref::<crate::effects::GoadEffect>()?,
                Some(&tagged.tag),
            )
        } else {
            (
                goad_effect.downcast_ref::<crate::effects::GoadEffect>()?,
                None,
            )
        };
    let ChooseSpec::All(goaded_filter) = &goad.target else {
        return None;
    };
    if goad.duration != Until::YourNextTurn
        || !goaded_filter.card_types.contains(&CardType::Creature)
    {
        return None;
    }

    let cant = cant_effect.downcast_ref::<crate::effects::CantEffect>()?;
    let crate::effect::Restriction::Block(block_filter) = &cant.restriction else {
        return None;
    };
    if cant.duration != Until::YourNextTurn
        || !matches!(cant.start, crate::effect::RestrictionStart::Immediate)
    {
        return None;
    }
    let [reference] = block_filter.tagged_constraints.as_slice() else {
        return None;
    };
    if reference.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
        || produced_tag.is_some_and(|tag| tag != &reference.tag)
        || (produced_tag.is_none()
            && prior_effect_action_for_filter(block_filter)
                != Some(crate::effect::PriorEffectAction::Goaded))
    {
        return None;
    }
    let mut reference_only = block_filter.clone();
    reference_only.set_prior_effect_action_surface(None);
    reference_only.set_plural_object_noun_surface(false);
    reference_only.tagged_constraints.retain(|constraint| {
        constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
    });
    if reference_only != ObjectFilter::default() {
        return None;
    }

    let goad_text = describe_effect(goad_effect);
    Some((
        format!(
            "{}. Until your next turn, those creatures can't block",
            goad_text.trim().trim_end_matches('.')
        ),
        2,
    ))
}

/// Rejoin an Aura's attached-object setup, tap action, and Equipment-only
/// unattach rider. The attachment tag proves that the conditional's "it" and
/// both actions refer to the same object across the two source sentences.
fn describe_cross_segment_attached_tap_equipment_unattach_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let first = segments.get(start)?;
    let second = segments.get(start + 1)?;
    if !first.self_replacements.is_empty() || !second.self_replacements.is_empty() {
        return None;
    }

    let [tag_effect, tap_effect] = first.default_effects.as_slice() else {
        return None;
    };
    let tag_attached = tag_effect.downcast_ref::<crate::effects::TagAttachedToSourceEffect>()?;
    if tag_attached.tag.as_str() != "enchanted" {
        return None;
    }
    let tap =
        unwrap_basic_render_wrapper(tap_effect).downcast_ref::<crate::effects::TapEffect>()?;
    if tap.target != ChooseSpec::Tagged(tag_attached.tag.clone()) {
        return None;
    }

    let [conditional_effect] = second.default_effects.as_slice() else {
        return None;
    };
    let conditional = conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let crate::effect::Condition::TaggedObjectMatches(condition_tag, equipment_filter) =
        &conditional.condition
    else {
        return None;
    };
    let mut expected_equipment = ObjectFilter::default();
    expected_equipment.subtypes.push(Subtype::Equipment);
    if condition_tag != &tag_attached.tag
        || equipment_filter != &expected_equipment
        || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }

    let [unattach_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let unattach = unwrap_basic_render_wrapper(unattach_effect)
        .downcast_ref::<crate::effects::UnattachObjectsEffect>()?;
    if unattach.objects != ChooseSpec::Tagged(tag_attached.tag.clone()) {
        return None;
    }

    Some((
        "Tap enchanted creature. If it's an Equipment, unattach it".to_string(),
        2,
    ))
}

/// Rejoin an authored target-player declaration with the following
/// choose-half/return bundle. Reference resolution aliases the target inside
/// the second segment, while the explicit target-only effect remains the
/// source of the opening `Choose target player` sentence.
fn describe_cross_segment_target_player_choose_half_return_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [target_segment, return_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !target_segment.self_replacements.is_empty() || !return_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [target_effect] = target_segment.default_effects.as_slice() else {
        return None;
    };
    let target = structural_unwrap_render_wrappers(target_effect)
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if !target.explicit_declaration
        || target.chooser.is_some()
        || target.target != ChooseSpec::target_player()
    {
        return None;
    }

    let [choose_effect, return_effect] = return_segment.default_effects.as_slice() else {
        return None;
    };
    let choose = structural_unwrap_render_wrappers(choose_effect)
        .downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let return_to_hand = structural_unwrap_render_wrappers(return_effect)
        .downcast_ref::<crate::effects::ReturnToHandEffect>()?;
    describe_target_player_choose_half_then_return_to_hand(choose, return_to_hand)
        .map(|rendered| (rendered, 2))
}

fn describe_participant_choice_complement_phase_out_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [choice_segment, phase_segment] = program.segments.as_slice() else {
        return None;
    };
    if !choice_segment.self_replacements.is_empty() || !phase_segment.self_replacements.is_empty() {
        return None;
    }
    let [player_effect] = choice_segment.default_effects.as_slice() else {
        return None;
    };
    let for_players = structural_unwrap_render_wrappers(player_effect)
        .downcast_ref::<crate::effects::ForPlayersEffect>()?;
    if for_players.filter != PlayerFilter::Any
        || !for_players.starting_with_controller
        || for_players.stop_after_first_happened
    {
        return None;
    }
    let [choose_effect] = for_players.effects.as_slice() else {
        return None;
    };
    let choose = structural_unwrap_render_wrappers(choose_effect)
        .downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let expected_choice = crate::effects::ChooseObjectsEffect::new(
        ObjectFilter::permanent_card()
            .in_zone(crate::zone::Zone::Battlefield)
            .controlled_by(PlayerFilter::IteratedPlayer),
        ChoiceCount::up_to(5),
        PlayerFilter::IteratedPlayer,
        choose.tag.clone(),
    );
    let mut semantic_choice = choose.clone();
    if semantic_choice.zone == Some(crate::zone::Zone::Battlefield) {
        // Lowering also records the filter's zone on the choice envelope.
        // That redundant execution hint does not change the selected set.
        semantic_choice.zone = None;
    }
    if semantic_choice != expected_choice {
        return None;
    }

    let [phase_effect] = phase_segment.default_effects.as_slice() else {
        return None;
    };
    let phase_out = structural_unwrap_render_wrappers(phase_effect)
        .downcast_ref::<crate::effects::PhaseOutEffect>()?;
    let ChooseSpec::All(phase_filter) = &phase_out.spec else {
        return None;
    };
    let [constraint] = phase_filter.tagged_constraints.as_slice() else {
        return None;
    };
    if constraint.tag != choose.tag
        || constraint.relation != crate::filter::TaggedOpbjectRelation::IsNotTaggedObject
        || !phase_filter.other
        || phase_filter.prior_effect_action_surface()
            != Some(ironsmith_core::PriorEffectAction::Chosen)
        || !matches!(
            phase_filter.source_surface.as_ref(),
            Some(crate::target::SourceReferenceSurface::ThisPermanentType(surface))
                if surface.eq_ignore_ascii_case("this creature")
        )
        || phase_out.duration != crate::effects::PhaseOutDuration::UntilNextUntap
        || phase_out.source_surface.is_some()
    {
        return None;
    }
    let mut semantic_phase_filter = phase_filter.clone();
    semantic_phase_filter.other = false;
    semantic_phase_filter.source_surface = None;
    semantic_phase_filter.tagged_constraints.clear();
    semantic_phase_filter.set_prior_effect_action_surface(None);
    if semantic_phase_filter
        != ObjectFilter::permanent_card().in_zone(crate::zone::Zone::Battlefield)
    {
        return None;
    }

    Some(
        "Starting with you, each player chooses up to five permanents they control. All permanents other than this creature that weren't chosen this way phase out"
            .to_string(),
    )
}

/// Restore the authored sentence boundary in a target-player life-loss spell
/// whose second sentence coordinates life gain with token creation. The
/// parser keeps that second sentence as a `SequenceEffect`; matching its typed
/// actors and the token's independently rendered ability sentence prevents
/// the generic list renderer from dropping "you" and folding all three
/// sentences into one comma chain.
fn describe_target_player_life_loss_then_gain_and_create_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let [target_effect, lose_effect, sequence_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let target = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let lose = lose_effect.downcast_ref::<crate::effects::LoseLifeEffect>()?;
    let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if target.explicit_declaration
        || target.chooser.is_some()
        || target.target != ChooseSpec::target_player()
        || lose.player != ChooseSpec::Player(PlayerFilter::target_player())
        || !matches!(
            sequence.surface,
            ironsmith_core::SequenceSurface::Coordinated
        )
    {
        return None;
    }
    let [gain_effect, create_effect] = sequence.effects.as_slice() else {
        return None;
    };
    let gain = gain_effect.downcast_ref::<crate::effects::GainLifeEffect>()?;
    let create = structural_unwrap_render_wrappers(create_effect)
        .downcast_ref::<crate::effects::CreateTokenEffect>()?;
    if gain.player != ChooseSpec::Player(PlayerFilter::You)
        || gain.amount != lose.amount
        || create.controller != PlayerFilter::You
    {
        return None;
    }

    let create_text = describe_effect(create_effect);
    let (create_main, token_ability) = create_text.split_once(". They have ")?;
    let create_main = create_main
        .strip_prefix("You create ")
        .or_else(|| create_main.strip_prefix("Create "))?;
    let create_main = if let Value::Fixed(count) = create.count {
        let numeric_prefix = format!("{count} ");
        create_main
            .strip_prefix(&numeric_prefix)
            .and_then(|rest| number_word(count).map(|word| format!("{word} {rest}")))
            .unwrap_or_else(|| create_main.to_string())
    } else {
        create_main.to_string()
    };
    Some(format!(
        "Target player loses {}. You gain {} and create {create_main}. They have {token_ability}",
        describe_life_amount_phrase(&lose.amount),
        describe_life_amount_phrase(&gain.amount),
    ))
}

/// Reconstruct a prior-result self-replacement nested under an earlier
/// successful-action gate. The nesting is executable control flow: declining
/// the optional producer skips both alternatives, while an accepted producer
/// executes exactly the default or replacement arm. The explicit surface bit
/// distinguishes authored `instead` text from an ordinary if/otherwise pair.
fn describe_prior_result_self_replacement_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [producer_segment, result_segment] = program.segments.as_slice() else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !result_segment.self_replacements.is_empty()
        || producer_segment.default_effects.is_empty()
    {
        return None;
    }
    let [success_effect] = result_segment.default_effects.as_slice() else {
        return None;
    };
    let success_gate = success_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if success_gate.predicate != crate::effect::EffectPredicate::Happened
        || !success_gate.else_.is_empty()
    {
        return None;
    }
    let [result_effect] = success_gate.then.as_slice() else {
        return None;
    };
    let result_gate = result_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if result_gate.condition != success_gate.condition
        || !result_gate.prior_result_replacement_surface
        || !matches!(
            result_gate.predicate,
            crate::effect::EffectPredicate::PriorEffectResult(_)
        )
        || result_gate.then.is_empty()
        || result_gate.else_.is_empty()
    {
        return None;
    }

    let producer = describe_effect_list(&producer_segment.default_effects)
        .trim()
        .trim_end_matches('.')
        .to_string();
    let default_effect = lowercase_first(
        describe_effect_list(&result_gate.else_)
            .trim()
            .trim_end_matches('.'),
    );
    let replacement_effect = lowercase_first(
        describe_effect_list(&result_gate.then)
            .trim()
            .trim_end_matches('.'),
    );
    let condition = describe_effect_predicate(&result_gate.predicate);
    Some(format!(
        "{producer}. If you do, {default_effect}. If {condition}, {replacement_effect} instead"
    ))
}

/// Render a searched-card partition whose self-replacement changes only the
/// search limit. Resolution branches must materialize the whole tagged
/// pipeline, but Oracle states the shared reveal and one-versus-rest
/// disposition only once (Nissa's Pilgrimage and structurally identical
/// effects). The second segment proves that shuffle is common to both paths.
fn exact_search_one_battlefield_rest_hand_tail<'a>(
    effects: &'a [Effect],
    search: &crate::effects::ChooseObjectsEffect,
) -> Option<&'a crate::effects::ChooseObjectsEffect> {
    let [
        reveal_effect,
        choose_effect,
        battlefield_effect,
        remainder_effect,
    ] = effects
    else {
        return None;
    };
    let reveal = reveal_effect.downcast_ref::<crate::effects::RevealTaggedEffect>()?;
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let expected_choice = crate::effects::ChooseObjectsEffect::new(
        ObjectFilter::tagged(search.tag.clone()).in_zone(Zone::Library),
        ChoiceCount::exactly(1),
        PlayerFilter::You,
        choose.tag.clone(),
    )
    .in_zone(Zone::Library);
    if reveal.tag != search.tag || choose != &expected_choice {
        return None;
    }

    let battlefield = battlefield_effect.downcast_ref::<crate::effects::ForEachTaggedEffect>()?;
    let [put_effect] = battlefield.effects.as_slice() else {
        return None;
    };
    let put = put_effect.downcast_ref::<crate::effects::PutOntoBattlefieldEffect>()?;
    if battlefield.tag != choose.tag
        || put != &crate::effects::PutOntoBattlefieldEffect::you_control(ChooseSpec::Iterated, true)
    {
        return None;
    }

    let remainder = remainder_effect.downcast_ref::<crate::effects::ForEachTaggedEffect>()?;
    let [conditional_effect] = remainder.effects.as_slice() else {
        return None;
    };
    let conditional = conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::TaggedObjectMatches(iterated_tag, chosen_filter) = &conditional.condition else {
        return None;
    };
    let expected_chosen_filter =
        ObjectFilter::default().same_stable_id_as_tagged(choose.tag.clone());
    let [hand_effect] = conditional.if_false.as_slice() else {
        return None;
    };
    let hand = hand_effect.downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    let expected_hand =
        crate::effects::MoveToZoneEffect::new(ChooseSpec::Iterated, Zone::Hand, false)
            .with_remainder_surface(ironsmith_core::LibraryRemainderSurface::Rest);
    if remainder.tag != search.tag
        || iterated_tag.as_str() != "__it__"
        || chosen_filter != &expected_chosen_filter
        || !conditional.if_true.is_empty()
        || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || hand != &expected_hand
    {
        return None;
    }

    Some(choose)
}

fn describe_search_partition_count_self_replacement_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [partition_segment, shuffle_segment] = program.segments.as_slice() else {
        return None;
    };
    let [branch] = partition_segment.self_replacements.as_slice() else {
        return None;
    };
    if branch.condition_after_replacement
        || branch.leading_instead_surface
        || branch.presentation_label.is_none()
        || !shuffle_segment.self_replacements.is_empty()
    {
        return None;
    }

    let [default_sequence_effect] = partition_segment.default_effects.as_slice() else {
        return None;
    };
    let [replacement_sequence_effect] = branch.replacement_effects.as_slice() else {
        return None;
    };
    let default_sequence =
        default_sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    let replacement_sequence =
        replacement_sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if default_sequence.surface != ironsmith_core::SequenceSurface::Coordinated
        || replacement_sequence.surface != default_sequence.surface
    {
        return None;
    }
    let [default_search_effect, default_tail @ ..] = default_sequence.effects.as_slice() else {
        return None;
    };
    let [replacement_search_effect, replacement_tail @ ..] =
        replacement_sequence.effects.as_slice()
    else {
        return None;
    };

    let default_search =
        default_search_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let replacement_search =
        replacement_search_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let (Some(default_max), Some(replacement_max)) =
        (default_search.count.max, replacement_search.count.max)
    else {
        return None;
    };
    if default_search.count.min != 0
        || replacement_search.count.min != 0
        || default_max < 2
        || replacement_max <= default_max
        || default_search.count.dynamic_x
        || default_search.count.up_to_x
        || default_search.count.random
        || default_search.count.explicit_exactly
        || replacement_search.count.dynamic_x
        || replacement_search.count.up_to_x
        || replacement_search.count.random
        || replacement_search.count.explicit_exactly
        || !default_search.is_search
        || default_search.reveal
        || default_search.search_mode != crate::effect::SearchSelectionMode::Optional
        || default_search.zone != Some(Zone::Library)
        || !default_search.additional_zones.is_empty()
        || default_search.filter.zone != Some(Zone::Library)
        || default_search.filter.owner != Some(PlayerFilter::You)
        || default_search.chooser != PlayerFilter::You
    {
        return None;
    }
    let mut replacement_search_at_default_count = replacement_search.clone();
    replacement_search_at_default_count.count = default_search.count;
    if replacement_search_at_default_count != *default_search {
        return None;
    }

    let default_choice = exact_search_one_battlefield_rest_hand_tail(default_tail, default_search)?;
    let replacement_choice =
        exact_search_one_battlefield_rest_hand_tail(replacement_tail, replacement_search)?;
    if default_choice != replacement_choice {
        return None;
    }

    let [shuffle_wrapper] = shuffle_segment.default_effects.as_slice() else {
        return None;
    };
    let shuffle_sequence = shuffle_wrapper.downcast_ref::<crate::effects::SequenceEffect>()?;
    let [shuffle_effect] = shuffle_sequence.effects.as_slice() else {
        return None;
    };
    let shuffle = shuffle_effect.downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;
    if shuffle_sequence.surface != ironsmith_core::SequenceSurface::SentenceLeadingThen
        || shuffle.player != PlayerFilter::You
        || shuffle.target_spec.is_some()
    {
        return None;
    }

    let default_search_rendered = describe_effect(default_search_effect);
    let default_search_clause = default_search_rendered.trim().trim_end_matches('.');
    let default_search_text = default_search_clause
        .strip_prefix("You ")
        .unwrap_or(default_search_clause);
    let replacement_search_rendered = describe_effect(replacement_search_effect);
    let replacement_search_clause = replacement_search_rendered.trim().trim_end_matches('.');
    let replacement_search_text = lowercase_first(
        replacement_search_clause
            .strip_prefix("You ")
            .unwrap_or(replacement_search_clause),
    );
    let default_count =
        small_number_word(default_max as u32).unwrap_or_else(|| default_max.to_string());
    let condition = describe_condition(&branch.condition);
    let rendered = format!(
        "{}, reveal those cards, and put one onto the battlefield tapped and the rest into your hand. Then shuffle. If {condition}, {replacement_search_text} instead of {default_count}",
        capitalize_first(default_search_text)
    );
    Some(apply_self_replacement_presentation_label(branch, rendered))
}

fn describe_put_counter_then_counter_removal_damage_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [put_segment, conditional_segment] = program.segments.as_slice() else {
        return None;
    };
    if !put_segment.self_replacements.is_empty()
        || !conditional_segment.self_replacements.is_empty()
        || put_segment.starts_new_source_line
        || conditional_segment.starts_new_source_line
    {
        return None;
    }
    let [put_effect] = put_segment.default_effects.as_slice() else {
        return None;
    };
    let put = structural_unwrap_render_wrappers(put_effect)
        .downcast_ref::<crate::effects::PutCountersEffect>()?;
    if put.counter_type != crate::object::CounterType::PlusOnePlusOne
        || !matches!(put.amount.unhinted(), Value::Fixed(1))
        || !matches!(put.target.base(), ChooseSpec::Source)
        || put.target_count.is_some()
        || put.distributed
    {
        return None;
    }
    let source_surface = match put.target.source_reference_surface()? {
        crate::target::SourceReferenceSurface::FullName(text)
        | crate::target::SourceReferenceSurface::ShortName(text) => text,
        crate::target::SourceReferenceSurface::ThisPermanentType(_) => return None,
    };
    let conditional = describe_activated_counter_removal_damage_with_source_surface(
        &conditional_segment.default_effects,
        source_surface,
    )?;
    Some(format!(
        "{}. {conditional}",
        describe_effect(put_effect).trim().trim_end_matches('.')
    ))
}

/// Render a complete each-player physical coin-face correlation. The first
/// effect records one face-only result per player; the second consumes that
/// exact result ID and retains an iterated-player choose/sacrifice action.
/// Depending on sentence-boundary lowering, that exact pair can occupy one
/// segment or two adjacent one-effect segments.
fn describe_each_player_coin_face_result_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let (flip_effect, followup_effect) = match program.segments.as_slice() {
        [segment] if segment.self_replacements.is_empty() && !segment.starts_new_source_line => {
            let [flip_effect, followup_effect] = segment.default_effects.as_slice() else {
                return None;
            };
            (flip_effect, followup_effect)
        }
        [flip_segment, followup_segment]
            if flip_segment.self_replacements.is_empty()
                && followup_segment.self_replacements.is_empty()
                && !flip_segment.starts_new_source_line
                && !followup_segment.starts_new_source_line =>
        {
            let [flip_effect] = flip_segment.default_effects.as_slice() else {
                return None;
            };
            let [followup_effect] = followup_segment.default_effects.as_slice() else {
                return None;
            };
            (flip_effect, followup_effect)
        }
        _ => return None,
    };
    let flip_result = flip_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let flip_players = flip_result
        .effect
        .downcast_ref::<crate::effects::ForPlayersEffect>()?;
    if flip_players.filter != PlayerFilter::Any
        || flip_players.starting_with_controller
        || flip_players.stop_after_first_happened
    {
        return None;
    }
    let [flip_effect] = flip_players.effects.as_slice() else {
        return None;
    };
    let flip = flip_effect.downcast_ref::<crate::effects::FlipCoinEffect>()?;
    if flip.player != PlayerFilter::IteratedPlayer
        || flip.kind != ironsmith_core::CoinFlipKind::FaceOnly
        || flip.forced_face.is_some()
        || flip.forced_winner.is_some()
        || flip.forced_loser.is_some()
    {
        return None;
    }

    let followup = followup_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if followup.condition != flip_result.id || !followup.else_.is_empty() {
        return None;
    }
    let face = match &followup.predicate {
        EffectPredicate::Happened => "heads",
        EffectPredicate::DidNotHappen => "tails",
        _ => return None,
    };
    let [choose_effect, sacrifice_effect] = followup.then.as_slice() else {
        return None;
    };
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let sacrifice = sacrifice_view(sacrifice_effect)?;
    if choose.chooser != PlayerFilter::IteratedPlayer
        || choose.filter.controller != Some(PlayerFilter::IteratedPlayer)
        || choose_primary_zone(choose) != Some(Zone::Battlefield)
    {
        return None;
    }
    let action = describe_choose_then_sacrifice(choose, sacrifice)?;
    let action = action
        .strip_prefix("that player ")
        .or_else(|| action.strip_prefix("they "))?;
    Some(format!(
        "Each player flips a coin. Each player whose coin comes up {face} {action}"
    ))
}

#[cfg(test)]
mod each_player_coin_face_result_tests {
    use super::*;

    fn program(predicate: EffectPredicate) -> crate::resolution::ResolutionProgram {
        let result_id = ironsmith_core::EffectId(73);
        let flip_players = Effect::new(crate::effects::ForPlayersEffect::new(
            PlayerFilter::Any,
            vec![Effect::flip_coin_for_face(PlayerFilter::IteratedPlayer)],
        ));
        let flip = Effect::with_id(result_id.0, flip_players);

        let tag = TagKey::from("sacrificed");
        let choose = Effect::new(
            crate::effects::ChooseObjectsEffect::new(
                ObjectFilter::creature()
                    .controlled_by(PlayerFilter::IteratedPlayer)
                    .in_zone(Zone::Battlefield),
                ChoiceCount::exactly(1),
                PlayerFilter::IteratedPlayer,
                tag.clone(),
            )
            .in_zone(Zone::Battlefield),
        );
        let sacrifice = Effect::new(crate::effects::zones::SacrificePlayerEffect::new(
            ObjectFilter::tagged(tag),
            Value::Fixed(1),
            PlayerFilter::IteratedPlayer,
        ));
        let followup = Effect::if_then(result_id, predicate, vec![choose, sacrifice]);

        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![flip]),
            crate::resolution::ResolutionSegment::from_effects(vec![followup]),
        ])
    }

    #[test]
    fn linked_player_counts_render_heads_and_tails_without_exposing_result_ids() {
        assert_eq!(
            describe_each_player_coin_face_result_program(&program(EffectPredicate::DidNotHappen))
                .as_deref(),
            Some(
                "Each player flips a coin. Each player whose coin comes up tails sacrifices a creature of their choice"
            )
        );
        assert_eq!(
            describe_each_player_coin_face_result_program(&program(EffectPredicate::Happened))
                .as_deref(),
            Some(
                "Each player flips a coin. Each player whose coin comes up heads sacrifices a creature of their choice"
            )
        );
    }

    #[test]
    fn same_segment_linked_player_counts_render_the_same_authored_pair() {
        let two_segment = program(EffectPredicate::DidNotHappen);
        let [flip_segment, followup_segment] = two_segment.segments.as_slice() else {
            panic!("test fixture should contain the linked two-segment pair");
        };
        let mut single_segment = crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![
                flip_segment.default_effects[0].clone(),
                followup_segment.default_effects[0].clone(),
            ]),
        ]);

        assert_eq!(
            describe_each_player_coin_face_result_program(&single_segment).as_deref(),
            Some(
                "Each player flips a coin. Each player whose coin comes up tails sacrifices a creature of their choice"
            )
        );

        single_segment.segments[0]
            .default_effects
            .push(Effect::draw(1));
        assert_eq!(
            describe_each_player_coin_face_result_program(&single_segment),
            None,
            "an unrelated third effect must not be hidden by the compact surface"
        );
    }

    #[test]
    fn called_coin_flip_is_not_a_physical_face_result() {
        let mut program = program(EffectPredicate::DidNotHappen);
        let flip_result = program.segments[0].default_effects[0]
            .downcast_ref::<crate::effects::WithIdEffect>()
            .expect("flip result")
            .clone();
        let mut flip_players = flip_result
            .effect
            .downcast_ref::<crate::effects::ForPlayersEffect>()
            .expect("player flip loop")
            .clone();
        flip_players.effects = vec![Effect::flip_coin(PlayerFilter::IteratedPlayer)];
        program.segments[0].default_effects =
            vec![Effect::with_id(flip_result.id.0, Effect::new(flip_players))];

        assert_eq!(
            describe_each_player_coin_face_result_program(&program),
            None
        );
    }
}

/// Render an aggregate participant choice followed by a copy whose copiable
/// characteristics are calculated from that exact chosen set.
fn describe_per_opponent_choose_aggregate_copy_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [choice_segment, copy_segment] = program.segments.as_slice() else {
        return None;
    };
    if !choice_segment.self_replacements.is_empty()
        || !copy_segment.self_replacements.is_empty()
        || choice_segment.starts_new_source_line
        || copy_segment.starts_new_source_line
    {
        return None;
    }
    let [for_players_effect] = choice_segment.default_effects.as_slice() else {
        return None;
    };
    let [copy_effect] = copy_segment.default_effects.as_slice() else {
        return None;
    };
    let for_players = unwrap_basic_render_wrapper(for_players_effect)
        .downcast_ref::<crate::effects::ForPlayersEffect>()?;
    if for_players.filter != PlayerFilter::Opponent
        || for_players.starting_with_controller
        || for_players.stop_after_first_happened
    {
        return None;
    }
    let [choose_effect] = for_players.effects.as_slice() else {
        return None;
    };
    let choose = unwrap_basic_render_wrapper(choose_effect)
        .downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    if choose.chooser != PlayerFilter::You
        || choose.filter.zone != Some(Zone::Battlefield)
        || choose.filter.controller != Some(PlayerFilter::IteratedPlayer)
        || choose.filter.card_types.as_slice() != [CardType::Creature]
        || !choose.filter.tagged_constraints.is_empty()
        || !choose.count.is_single()
        || choose.count.min != 1
        || choose.count_value.is_some()
        || choose.aggregate_constraint.is_some()
        || choose.zone != Some(Zone::Battlefield)
        || !choose.additional_zones.is_empty()
        || choose.is_search
        || choose.reveal
        || choose.top_only
        || choose.bottom_only
        || choose.replace_tagged_objects
        || choose.remember_as_chosen_object
        || !choose.tag.as_str().starts_with("participant_choice_l")
    {
        return None;
    }

    let copy = unwrap_basic_render_wrapper(copy_effect)
        .downcast_ref::<crate::effects::CreateTokenCopyEffect>()?;
    let ChooseSpec::Object(copy_source_filter) = copy.target.base() else {
        return None;
    };
    let (power, toughness) = copy.set_base_power_toughness_value.as_ref()?;
    let (Value::TotalPower(power_filter), Value::TotalToughness(toughness_filter)) =
        (power.unhinted(), toughness.unhinted())
    else {
        return None;
    };
    let references_choice = |filter: &ObjectFilter| {
        filter.tagged_constraints.iter().any(|constraint| {
            constraint.tag == choose.tag
                && constraint.relation == crate::filter::TaggedOpbjectRelation::IsTaggedObject
        })
    };
    if power_filter != toughness_filter
        || copy_source_filter != power_filter
        || !references_choice(copy_source_filter)
        || !copy_source_filter.has_one_of_tagged_set_surface()
        || copy.count != Value::Fixed(1)
        || copy.controller != PlayerFilter::You
        || copy.enters_tapped
        || copy.has_haste
        || copy.enters_attacking
        || copy.attack_target_mode.is_some()
        || copy.exile_at_end_of_combat
        || copy.loses_soulbond
        || copy.sacrifice_at_next_end_step
        || copy.exile_at_next_end_step
        || copy.pt_adjustment.is_some()
        || copy.clear_mana_cost
        || !copy.added_card_types.is_empty()
        || !copy.added_subtypes.is_empty()
        || !copy.removed_supertypes.is_empty()
        || copy.set_base_power_toughness.is_some()
        || copy.set_colors != Some(crate::color::ColorSet::new())
        || copy.set_card_types.as_deref() != Some(&[CardType::Creature])
        || copy.set_subtypes.as_deref() != Some(&[Subtype::Eldrazi])
        || !copy.granted_static_abilities.is_empty()
    {
        return None;
    }

    Some(
        "For each opponent, choose a creature that player controls. Create a token that's a copy of one of those creatures, except its power is equal to the total power of those creatures, its toughness is equal to the total toughness of those creatures, and it's a colorless Eldrazi creature"
            .to_string(),
    )
}

fn describe_cross_segment_iterated_exile_power_damage_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [exile_segment, damage_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if !exile_segment.self_replacements.is_empty()
        || !damage_segment.self_replacements.is_empty()
        || !damage_segment.starts_new_source_line
    {
        return None;
    }
    let [producer] = exile_segment.default_effects.as_slice() else {
        return None;
    };
    let with_id = producer.downcast_ref::<crate::effects::WithIdEffect>()?;
    let exile_for_players = with_id
        .effect
        .downcast_ref::<crate::effects::ForPlayersEffect>()?;
    if exile_for_players.filter != PlayerFilter::Opponent
        || exile_for_players.starting_with_controller
        || exile_for_players.stop_after_first_happened
    {
        return None;
    }
    let [move_effect] = exile_for_players.effects.as_slice() else {
        return None;
    };
    let move_effect = move_effect.downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    if move_effect.zone != Zone::Exile
        || move_effect.actor_surface != Some(PlayerFilter::IteratedPlayer)
        || move_effect.enters_tapped
        || move_effect.enters_attacking
        || move_effect.enters_face_down
        || move_effect.enters_transformed
    {
        return None;
    }
    let ChooseSpec::WithCount(target, count) = move_effect.target.unhinted() else {
        return None;
    };
    if !count.is_single() {
        return None;
    }
    let ChooseSpec::Object(target_filter) = target.unhinted() else {
        return None;
    };
    let mut target_base = target_filter.clone();
    let power = target_base.power.take()?;
    let expected_base = ObjectFilter::creature().controlled_by(PlayerFilter::IteratedPlayer);
    if target_base != expected_base {
        return None;
    }
    let crate::filter::Comparison::EqualExpr(greatest) = power else {
        return None;
    };
    let Value::GreatestPower(greatest_filter) = greatest.as_ref().unhinted() else {
        return None;
    };
    if greatest_filter != &expected_base {
        return None;
    }

    let [conditional] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional = conditional.downcast_ref::<crate::effects::ConditionalEffect>()?;
    if conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }
    let Condition::ValueComparison {
        left: Value::Count(spell_mastery_filter),
        operator: crate::effect::ValueComparisonOperator::GreaterThanOrEqual,
        right: Value::Fixed(2),
    } = &conditional.condition
    else {
        return None;
    };
    let mut expected_spell_mastery = ObjectFilter::default().in_zone(Zone::Graveyard);
    expected_spell_mastery.owner = Some(PlayerFilter::You);
    expected_spell_mastery.card_types = vec![CardType::Instant, CardType::Sorcery];
    if spell_mastery_filter != &expected_spell_mastery {
        return None;
    }
    let [damage_for_players] = conditional.if_true.as_slice() else {
        return None;
    };
    let damage_for_players =
        damage_for_players.downcast_ref::<crate::effects::ForPlayersEffect>()?;
    if damage_for_players.filter != PlayerFilter::Opponent
        || damage_for_players.starting_with_controller
        || damage_for_players.stop_after_first_happened
    {
        return None;
    }
    let [damage] = damage_for_players.effects.as_slice() else {
        return None;
    };
    let damage = damage.downcast_ref::<crate::effects::DealDamageEffect>()?;
    if damage.source_is_combat
        || damage.unpreventable
        || damage.target != ChooseSpec::Player(PlayerFilter::IteratedPlayer)
    {
        return None;
    }
    let Value::PriorEffectMetric { effect_id, query } = damage.amount.unhinted() else {
        return None;
    };
    if *effect_id != with_id.id
        || query.source != ironsmith_core::EffectMetricSource::AffectedObjects
        || query.metric != ironsmith_core::EffectMetric::FirstPower
        || query.player != Some(PlayerFilter::IteratedPlayer)
        || query.action != Some(ironsmith_core::PriorEffectAction::Exiled)
        || query.filter.as_ref() != Some(&ObjectFilter::creature())
    {
        return None;
    }

    Some((
        "Each opponent exiles a creature with the greatest power among creatures that player controls.\nSpell mastery — If there are two or more instant and/or sorcery cards in your graveyard, this spell deals damage to each opponent equal to the power of the creature they exiled"
            .to_string(),
        2,
    ))
}

#[cfg(test)]
mod iterated_exile_power_damage_window_tests {
    use super::*;

    fn program(damage_effect_id: u32) -> crate::resolution::ResolutionProgram {
        let expected_base = ObjectFilter::creature().controlled_by(PlayerFilter::IteratedPlayer);
        let mut greatest = expected_base.clone();
        greatest.power = Some(crate::filter::Comparison::EqualExpr(Box::new(
            Value::GreatestPower(expected_base),
        )));
        let target = ChooseSpec::Object(greatest).with_count(ChoiceCount::exactly(1));
        let mut exile = crate::effects::MoveToZoneEffect::new(target, Zone::Exile, true);
        exile.actor_surface = Some(PlayerFilter::IteratedPlayer);
        let producer = Effect::with_id(
            12,
            Effect::for_players(PlayerFilter::Opponent, vec![Effect::new(exile)]),
        );

        let mut spell_mastery = ObjectFilter::default().in_zone(Zone::Graveyard);
        spell_mastery.owner = Some(PlayerFilter::You);
        spell_mastery.card_types = vec![CardType::Instant, CardType::Sorcery];
        let condition = Condition::ValueComparison {
            left: Value::Count(spell_mastery),
            operator: crate::effect::ValueComparisonOperator::GreaterThanOrEqual,
            right: Value::Fixed(2),
        };
        let amount = Value::PriorEffectMetric {
            effect_id: crate::effect::EffectId(damage_effect_id),
            query: ironsmith_core::PriorEffectMetricQuery::new(
                ironsmith_core::EffectMetricSource::AffectedObjects,
                ironsmith_core::EffectMetric::FirstPower,
            )
            .with_filter(ObjectFilter::creature())
            .with_player(PlayerFilter::IteratedPlayer)
            .with_action(ironsmith_core::PriorEffectAction::Exiled),
        };
        let damage = Effect::deal_damage(amount, ChooseSpec::Player(PlayerFilter::IteratedPlayer));
        let conditional = Effect::conditional_only(
            condition,
            vec![Effect::for_players(PlayerFilter::Opponent, vec![damage])],
        );

        let first = crate::resolution::ResolutionSegment::from_effects(vec![producer]);
        let mut second = crate::resolution::ResolutionSegment::from_effects(vec![conditional]);
        second.starts_new_source_line = true;
        crate::resolution::ResolutionProgram::new(vec![first, second])
    }

    #[test]
    fn renders_each_players_exact_exiled_creature_power() {
        let program = program(12);
        assert_eq!(
            describe_resolution_program_preserving_source_lines(&program),
            "Each opponent exiles a creature with the greatest power among creatures that player controls.\nSpell mastery — If there are two or more instant and/or sorcery cards in your graveyard, this spell deals damage to each opponent equal to the power of the creature they exiled"
        );
    }

    #[test]
    fn rejects_damage_bound_to_a_different_exile_producer() {
        let program = program(13);
        assert_eq!(
            describe_cross_segment_iterated_exile_power_damage_window(&program.segments, 0),
            None
        );
    }
}

/// A library search, its linked exile-and-shuffle sentence, and the optional
/// cast permission can retain separate resolution segments even though they
/// form one authored procedure. Rejoin only that exact segment structure and
/// delegate the tag, zone, player, count, and cast-permission proof to the
/// ordinary effect-list compactor.
fn describe_cross_segment_search_exile_shuffle_may_cast_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [search_segment, exile_segment, cast_segment] = program.segments.as_slice() else {
        return None;
    };
    if [search_segment, exile_segment, cast_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty() || segment.starts_new_source_line)
    {
        return None;
    }

    let [search_effect] = search_segment.default_effects.as_slice() else {
        return None;
    };
    let choose = search_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;

    let [exile_effect] = exile_segment.default_effects.as_slice() else {
        return None;
    };
    let sequence = exile_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if !matches!(
        sequence.surface,
        ironsmith_core::SequenceSurface::CommaThen | ironsmith_core::SequenceSurface::Coordinated
    ) || sequence.result_label.is_some()
    {
        return None;
    }
    let [move_effect, shuffle_effect] = sequence.effects.as_slice() else {
        return None;
    };
    let shuffle = shuffle_effect.downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;

    let [cast_effect] = cast_segment.default_effects.as_slice() else {
        return None;
    };
    describe_search_choose_then_exile_and_cast(choose, move_effect, shuffle, cast_effect)
}

/// Preserve the identity of one card exiled from an opponent's library across
/// a conditional free-cast attempt and its result-linked permission fallback.
///
/// Lowering intentionally keeps the producer, the trailing-if cast, and the
/// `if you don't cast it this way` branch in separate segments. This matcher
/// claims only the exact typed program: one opponent target, one face-up
/// exiled card, one Treasure, a matching spell/mana-value predicate, and one
/// shared exile tag on both cast permissions.
fn describe_exile_top_treasure_conditional_cast_fallback_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [producer_segment, cast_segment, fallback_segment] = program.segments.as_slice() else {
        return None;
    };
    if [producer_segment, cast_segment, fallback_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty() || segment.starts_new_source_line)
    {
        return None;
    }

    let [producer_effect] = producer_segment.default_effects.as_slice() else {
        return None;
    };
    let producer = producer_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if producer.surface != ironsmith_core::SequenceSurface::Coordinated
        || producer.result_label.is_some()
    {
        return None;
    }
    let [target_effect, exile_effect, create_effect] = producer.effects.as_slice() else {
        return None;
    };
    let target = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if target.target != ChooseSpec::target(ChooseSpec::Player(PlayerFilter::Opponent))
        || target.chooser.is_some()
        || target.explicit_declaration
    {
        return None;
    }
    let exile = exile_effect.downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()?;
    let [exile_tag] = exile.moved_tags.as_slice() else {
        return None;
    };
    if exile.count.unhinted() != &Value::Fixed(1)
        || exile.player != PlayerFilter::Target(Box::new(PlayerFilter::Opponent))
        || exile.surface.is_some()
        || !exile.accumulated_tags.is_empty()
        || exile.face_down
    {
        return None;
    }
    let created = create_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    if &created.tag == exile_tag {
        return None;
    }
    let create = created
        .effect
        .downcast_ref::<crate::effects::CreateTokenEffect>()?;
    if create.count.unhinted() != &Value::Fixed(1)
        || create.controller != PlayerFilter::You
        || create.token.card.card_types != [CardType::Artifact]
        || create.token.card.subtypes != [crate::types::Subtype::Treasure]
        || create.controller_target.is_some()
        || create.use_source_chosen_color
        || create.use_source_chosen_creature_type
        || create.actor_surface_explicit
        || create.suppress_aura_attachment_choice
        || create.ability_presentation.is_some()
        || create.enters_tapped
        || create.enters_attacking
        || create.attack_target_mode.is_some()
        || create.exile_at_end_of_combat
        || create.sacrifice_at_end_of_combat
        || create.sacrifice_at_next_end_step
        || create.exile_at_next_end_step
        || create.next_end_step_player != PlayerFilter::Any
    {
        return None;
    }

    let [cast_effect] = cast_segment.default_effects.as_slice() else {
        return None;
    };
    let cast_sequence = cast_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if cast_sequence.surface != ironsmith_core::SequenceSurface::SentenceLeadingThen
        || cast_sequence.result_label.is_some()
    {
        return None;
    }
    let [with_id_effect] = cast_sequence.effects.as_slice() else {
        return None;
    };
    let with_id = with_id_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let conditional = with_id
        .effect
        .downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::TaggedObjectMatches(condition_tag, predicate) = &conditional.condition else {
        return None;
    };
    if condition_tag != exile_tag
        || conditional.surface != ironsmith_core::ConditionalSurface::TrailingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }
    let mut artifact_count_filter = ObjectFilter::default()
        .in_zone(Zone::Battlefield)
        .controlled_by(PlayerFilter::You);
    artifact_count_filter.card_types.push(CardType::Artifact);
    let mut expected_predicate = ObjectFilter::default().in_zone(Zone::Stack);
    expected_predicate.stack_kind = Some(crate::filter::StackObjectKind::Spell);
    expected_predicate.has_mana_cost = true;
    expected_predicate.mana_value = Some(crate::filter::Comparison::LessThanExpr(Box::new(
        Value::Count(artifact_count_filter),
    )));
    if predicate != &expected_predicate {
        return None;
    }
    let [may_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let may = may_effect.downcast_ref::<crate::effects::MayEffect>()?;
    if may.fallback != crate::decision::FallbackStrategy::Decline
        || may
            .decider
            .as_ref()
            .is_some_and(|decider| decider != &PlayerFilter::You)
    {
        return None;
    }
    let [cast_effect] = may.effects.as_slice() else {
        return None;
    };
    let cast = cast_effect.downcast_ref::<crate::effects::CastTaggedEffect>()?;
    if &cast.tag != exile_tag
        || cast.player != PlayerFilter::You
        || cast.allow_land
        || cast.as_copy
        || cast.copy_cast_reminder_surface
        || !cast.without_paying_mana_cost
        || cast.additional_mana_cost.is_some()
        || cast.cost_reduction.is_some()
        || cast.mana_spend_mode != ironsmith_core::value_model::ManaSpendMode::Normal
    {
        return None;
    }

    let [fallback_effect] = fallback_segment.default_effects.as_slice() else {
        return None;
    };
    let fallback = fallback_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if fallback.condition != with_id.id
        || fallback.predicate != EffectPredicate::DidNotHappen
        || !fallback.else_.is_empty()
        || fallback.per_player_result
        || fallback.prior_result_replacement_surface
    {
        return None;
    }
    let [grant_effect] = fallback.then.as_slice() else {
        return None;
    };
    let grant = grant_effect.downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
    if &grant.tag != exile_tag
        || grant.player != PlayerFilter::You
        || grant.duration != crate::effects::GrantPlayTaggedDuration::UntilEndOfTurn
        || grant.allow_land
        || grant.mana_spend_mode != ironsmith_core::value_model::ManaSpendMode::Normal
        || grant.allow_any_color_for_cast
        || grant.while_on_top_of_library
        || grant.filter.is_some()
        || grant.during_turns_counter_put_on_source.is_some()
        || grant.spell_cost_increase.is_some()
        || grant.lands_enter_tapped
        || grant.cast_pool_is_plural
        || grant.max_plays.is_some()
        || !matches!(
            &grant.surface,
            Some(ironsmith_core::GrantPlayTaggedSurface {
                leading_duration: false,
                object: Some(ironsmith_core::GrantPlayTaggedObjectSurface::It),
                mana_reference: None,
                control_source: None,
                until_source_exiles_another: None,
            })
        )
    {
        return None;
    }

    Some(
        "Exile the top card of target opponent's library and create a Treasure token. Then you may cast the exiled card without paying its mana cost if it's a spell with mana value less than the number of artifacts you control. If you don't cast it this way, you may cast it this turn"
            .to_string(),
    )
}

#[cfg(test)]
mod exile_top_treasure_conditional_cast_fallback_tests {
    use super::*;

    fn program(fallback_tag: &str) -> crate::resolution::ResolutionProgram {
        let exile_tag = TagKey::from("__sentence_helper_exiled_probe");
        let target = Effect::new(crate::effects::TargetOnlyEffect::new(ChooseSpec::target(
            ChooseSpec::Player(PlayerFilter::Opponent),
        )));
        let exile = Effect::new(
            crate::effects::ExileTopOfLibraryEffect::new(
                Value::Fixed(1),
                PlayerFilter::Target(Box::new(PlayerFilter::Opponent)),
            )
            .tag_moved(exile_tag.clone()),
        );
        let create = Effect::new(crate::effects::TaggedEffect::new(
            "created_0",
            Effect::new(crate::effects::CreateTokenEffect::one(
                crate::cards::tokens::treasure_token_definition(),
            )),
        ));
        let producer = Effect::new(crate::effects::SequenceEffect::coordinated(vec![
            target, exile, create,
        ]));

        let mut artifact_count_filter = ObjectFilter::default()
            .in_zone(Zone::Battlefield)
            .controlled_by(PlayerFilter::You);
        artifact_count_filter.card_types.push(CardType::Artifact);
        let mut predicate = ObjectFilter::default().in_zone(Zone::Stack);
        predicate.stack_kind = Some(crate::filter::StackObjectKind::Spell);
        predicate.has_mana_cost = true;
        predicate.mana_value = Some(crate::filter::Comparison::LessThanExpr(Box::new(
            Value::Count(artifact_count_filter),
        )));
        let cast = Effect::new(
            crate::effects::CastTaggedEffect::new(exile_tag.clone(), PlayerFilter::You)
                .without_paying_mana_cost(),
        );
        let conditional = Effect::new(crate::effects::ConditionalEffect::trailing_if(
            Condition::TaggedObjectMatches(exile_tag, predicate),
            vec![Effect::new(crate::effects::MayEffect::new(vec![cast]))],
        ));
        let cast_attempt =
            Effect::new(crate::effects::SequenceEffect::sentence_leading_then(vec![
                Effect::new(crate::effects::WithIdEffect::new(
                    crate::effect::EffectId(7),
                    conditional,
                )),
            ]));

        let grant = crate::effects::GrantPlayTaggedEffect::new(
            fallback_tag,
            PlayerFilter::You,
            crate::effects::GrantPlayTaggedDuration::UntilEndOfTurn,
            false,
            ironsmith_core::value_model::ManaSpendMode::Normal,
        )
        .with_surface(ironsmith_core::GrantPlayTaggedSurface {
            leading_duration: false,
            object: Some(ironsmith_core::GrantPlayTaggedObjectSurface::It),
            mana_reference: None,
            control_source: None,
            until_source_exiles_another: None,
        });
        let fallback = Effect::if_then(
            crate::effect::EffectId(7),
            EffectPredicate::DidNotHappen,
            vec![Effect::new(grant)],
        );

        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![producer]),
            crate::resolution::ResolutionSegment::from_effects(vec![cast_attempt]),
            crate::resolution::ResolutionSegment::from_effects(vec![fallback]),
        ])
    }

    #[test]
    fn rejoins_the_exact_exiled_card_cast_attempt_and_fallback() {
        let program = program("__sentence_helper_exiled_probe");
        assert_eq!(
            describe_exile_top_treasure_conditional_cast_fallback_program(&program),
            Some(
                "Exile the top card of target opponent's library and create a Treasure token. Then you may cast the exiled card without paying its mana cost if it's a spell with mana value less than the number of artifacts you control. If you don't cast it this way, you may cast it this turn"
                    .to_string()
            )
        );
    }

    #[test]
    fn rejects_a_fallback_permission_for_a_different_exiled_card() {
        let program = program("__sentence_helper_exiled_unrelated");
        assert_eq!(
            describe_exile_top_treasure_conditional_cast_fallback_program(&program),
            None
        );
    }

    #[test]
    fn rejects_replacement_bearing_segments() {
        let mut program = program("__sentence_helper_exiled_probe");
        program.segments[1]
            .self_replacements
            .push(crate::resolution::SelfReplacementBranch::new(
                Condition::YouControl(ObjectFilter::artifact()),
                vec![],
            ));
        assert_eq!(
            describe_exile_top_treasure_conditional_cast_fallback_program(&program),
            None
        );
    }
}

/// Restore an authored failed-result sentence after the executable draw gate
/// and its `DidNotHappen` branch were split at the sentence boundary.
fn describe_conditional_draw_then_failed_draw_followup_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [draw_segment, fallback_segment] = program.segments.as_slice() else {
        return None;
    };
    if !draw_segment.self_replacements.is_empty()
        || !fallback_segment.self_replacements.is_empty()
        || draw_segment.starts_new_source_line
        || fallback_segment.starts_new_source_line
    {
        return None;
    }
    let [draw_effect] = draw_segment.default_effects.as_slice() else {
        return None;
    };
    let with_id = draw_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let conditional = with_id
        .effect
        .downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::PlayerControls { player, filter } = &conditional.condition else {
        return None;
    };
    let mut expected_filter = ObjectFilter::creature()
        .controlled_by(PlayerFilter::You)
        .with_any_counter();
    expected_filter.set_explicit_card_type_noun(Some(CardType::Creature));
    if player != &PlayerFilter::You
        || filter != &expected_filter
        || conditional.surface != ironsmith_core::ConditionalSurface::TrailingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }
    let [draw] = conditional.if_true.as_slice() else {
        return None;
    };
    let draw = draw.downcast_ref::<crate::effects::DrawCardsEffect>()?;
    if draw.player != PlayerFilter::You || !matches!(draw.count.unhinted(), Value::Fixed(1)) {
        return None;
    }

    let [fallback] = fallback_segment.default_effects.as_slice() else {
        return None;
    };
    let fallback = fallback.downcast_ref::<crate::effects::IfEffect>()?;
    if fallback.condition != with_id.id
        || fallback.predicate != crate::effect::EffectPredicate::DidNotHappen
        || fallback.per_player_result
        || fallback.prior_result_replacement_surface
        || !fallback.else_.is_empty()
    {
        return None;
    }
    let [put] = fallback.then.as_slice() else {
        return None;
    };
    let put = put.downcast_ref::<crate::effects::PutCountersEffect>()?;
    if put.counter_type != CounterType::PlusOnePlusOne
        || !matches!(put.amount.unhinted(), Value::Fixed(1))
        || !matches!(put.target.unhinted(), ChooseSpec::Source)
        || !put.amount.surface_hints().iter().any(|hint| {
            matches!(
                hint,
                ironsmith_core::ValueSurfaceHint::CounterFollowupSeparateSentence
            )
        })
        || !put.target.surface_hints().iter().any(|hint| {
            matches!(
                hint,
                ironsmith_core::ChooseSpecSurfaceHint::SourceReference(
                    crate::target::SourceReferenceSurface::ThisPermanentType(kind)
                ) if kind == "this creature"
            )
        })
        || put.target_count.is_some()
        || put.distributed
    {
        return None;
    }

    Some(
        "Draw a card if you control a creature with a counter on it. If you don't draw a card this way, put a +1/+1 counter on this creature"
            .to_string(),
    )
}

/// Rejoin an amass instruction with the exact cards milled by the following
/// instruction and the optional free cast chosen from that set.  The public
/// lowering intentionally keeps the three authored sentences as separate
/// segments; the tags are the executable proof that `them` means only the
/// cards just milled.
fn describe_amass_mill_then_optional_capped_cast_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let (amass_effect, mill_effect, choose_effect, cast_effect) = match program.segments.as_slice()
    {
        [segment] if segment.self_replacements.is_empty() && !segment.starts_new_source_line => {
            let [amass, mill, choose, cast] = segment.default_effects.as_slice() else {
                return None;
            };
            (amass, mill, choose, cast)
        }
        [amass_segment, mill_segment, cast_segment]
            if [amass_segment, mill_segment, cast_segment]
                .iter()
                .all(|segment| {
                    segment.self_replacements.is_empty() && !segment.starts_new_source_line
                }) =>
        {
            let [amass] = amass_segment.default_effects.as_slice() else {
                return None;
            };
            let [mill] = mill_segment.default_effects.as_slice() else {
                return None;
            };
            let [choose, cast] = cast_segment.default_effects.as_slice() else {
                return None;
            };
            (amass, mill, choose, cast)
        }
        _ => return None,
    };
    let amass_tagged = amass_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let amass = amass_tagged
        .effect
        .downcast_ref::<crate::effects::AmassEffect>()?;
    if amass.subtype != Some(crate::types::Subtype::Orc) || amass.amount.unhinted() != &Value::X {
        return None;
    }

    let mill_tagged = mill_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let mill = mill_tagged
        .effect
        .downcast_ref::<crate::effects::MillEffect>()?;
    if mill.player != PlayerFilter::You || mill.count.unhinted() != &Value::X {
        return None;
    }

    let choose = structural_unwrap_render_wrappers(choose_effect)
        .downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let mut expected_instant = ObjectFilter::default();
    expected_instant.card_types = vec![CardType::Instant];
    expected_instant.mana_value = Some(crate::filter::Comparison::LessThanOrEqualExpr(Box::new(
        Value::X,
    )));
    let mut expected_sorcery = ObjectFilter::default();
    expected_sorcery.card_types = vec![CardType::Sorcery];
    expected_sorcery.mana_value = Some(crate::filter::Comparison::LessThanOrEqualExpr(Box::new(
        Value::X,
    )));
    let mut expected_filter = ObjectFilter::default().in_zone(Zone::Graveyard);
    expected_filter.any_of = vec![expected_instant, expected_sorcery];
    expected_filter
        .tagged_constraints
        .push(crate::filter::TaggedObjectConstraint {
            tag: mill_tagged.tag.clone(),
            relation: crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        });
    if choose.filter != expected_filter
        || choose.count != crate::effect::ChoiceCount::up_to(1)
        || choose.count_value.is_some()
        || choose.aggregate_constraint.is_some()
        || choose.chooser != PlayerFilter::You
        || choose.zone != Some(Zone::Graveyard)
        || !choose.additional_zones.is_empty()
        || choose.is_search
        || choose.reveal
    {
        return None;
    }
    let cast = structural_unwrap_render_wrappers(cast_effect)
        .downcast_ref::<crate::effects::CastTaggedEffect>()?;
    if cast.tag != choose.tag
        || cast.player != PlayerFilter::You
        || cast.allow_land
        || cast.as_copy
        || !cast.without_paying_mana_cost
        || cast.additional_mana_cost.is_some()
        || cast.cost_reduction.is_some()
        || cast.mana_spend_mode != ironsmith_core::value_model::ManaSpendMode::Normal
    {
        return None;
    }

    Some(
        "Amass Orcs X. Mill X cards. You may cast an instant or sorcery spell with mana value X or less from among them without paying its mana cost"
            .to_string(),
    )
}

fn describe_cross_segment_choose_mount_vehicle_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let mut effects = Vec::with_capacity(3);
    for consumed in 1..=3 {
        let segment = segments.get(start + consumed - 1)?;
        if !segment.self_replacements.is_empty() {
            return None;
        }
        for effect in &segment.default_effects {
            let unwrapped = structural_unwrap_render_wrappers(effect);
            if let Some(sequence) = unwrapped.downcast_ref::<crate::effects::SequenceEffect>()
                && sequence.surface == ironsmith_core::SequenceSurface::Coordinated
            {
                effects.extend(sequence.effects.iter());
            } else {
                // The target declaration's tag is executable provenance for
                // both following conditions. Only inspect through transparent
                // wrappers when detecting a coordinated sequence; retain the
                // original effect for the strict three-effect matcher.
                effects.push(effect);
            }
        }
        if effects.len() == 3
            && let Some(rendered) = describe_choose_then_mount_vehicle_become(&effects)
        {
            return Some((rendered, consumed));
        }
        if effects.len() >= 3 {
            return None;
        }
    }
    None
}

/// Preserve a declared target through a one-shot graveyard watcher. The
/// target tag is the executable identity edge: the delayed trigger watches
/// only that permanent and the nested return consumes the same snapshot.
fn describe_choose_another_creature_or_artifact_then_delayed_return_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let (target_effect, delayed_effect) = match program.segments.as_slice() {
        [segment] if segment.self_replacements.is_empty() && !segment.starts_new_source_line => {
            let [target, delayed] = segment.default_effects.as_slice() else {
                return None;
            };
            (target, delayed)
        }
        [target_segment, delayed_segment]
            if [target_segment, delayed_segment].iter().all(|segment| {
                segment.self_replacements.is_empty() && !segment.starts_new_source_line
            }) =>
        {
            let [target] = target_segment.default_effects.as_slice() else {
                return None;
            };
            let [delayed] = delayed_segment.default_effects.as_slice() else {
                return None;
            };
            (target, delayed)
        }
        _ => return None,
    };
    let tagged_target = target_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let target_only = tagged_target
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if target_only.chooser.is_some() || !target_only.explicit_declaration {
        return None;
    }
    let ChooseSpec::Target(inner) = target_only.target.unhinted() else {
        return None;
    };
    let ChooseSpec::Object(target_filter) = inner.unhinted() else {
        return None;
    };
    let mut creature = ObjectFilter::creature();
    creature.other = true;
    let mut expected_target = ObjectFilter::default().in_zone(Zone::Battlefield);
    expected_target.any_of = vec![creature, ObjectFilter::artifact()];
    if target_filter != &expected_target {
        return None;
    }

    let delayed = delayed_effect.downcast_ref::<crate::effects::ScheduleDelayedTriggerEffect>()?;
    if !delayed.one_shot
        || delayed.start_next_turn
        || delayed.duration != ironsmith_core::DelayedTriggerDuration::EndOfTurn
        || !delayed.until_end_of_turn
        || delayed.until_end_of_combat
        || delayed.leading_duration_surface
        || delayed.watch_ability_source
        || delayed.watch_all_object_targets
        || delayed.either_of_watched_objects
        || delayed.while_any_tagged_object_in_zone.is_some()
        || !delayed.target_objects.is_empty()
        || delayed.target_tag.as_ref() != Some(&tagged_target.tag)
        || delayed.controller != PlayerFilter::You
        || delayed.prepayment.is_some()
        || delayed.event_value_from_prior_prevention
    {
        return None;
    }
    let expected_watched_filter = ObjectFilter::tagged(tagged_target.tag.clone());
    if delayed.target_filter.as_ref() != Some(&expected_watched_filter) {
        return None;
    }
    let dies = delayed
        .trigger
        .downcast_ref::<crate::triggers::ZoneChangeTrigger>()?;
    if dies.from != crate::triggers::zone_changes::ZonePattern::Specific(Zone::Battlefield)
        || dies.to != crate::triggers::zone_changes::ZonePattern::Specific(Zone::Graveyard)
        || dies.object_filter != ObjectFilter::default().in_zone(Zone::Battlefield)
        || dies.player != crate::triggers::zone_changes::PlayerRelation::Any
        || dies.cause_filter.is_some()
        || dies.during_turn.is_some()
        || dies.timing.is_some()
        || dies.origin_condition.is_some()
        || dies.count_mode != crate::triggers::zone_changes::CountMode::Each
        || !dies.this_object
        || dies.this_object_surface.is_some()
        || dies.this_object_subject_number
            != ironsmith_core::trigger_model::TriggerSubjectNumber::Singular
        || dies.graveyard_surface.is_some()
    {
        return None;
    }

    let [return_segment] = delayed.effects.segments.as_slice() else {
        return None;
    };
    if !return_segment.self_replacements.is_empty() || return_segment.starts_new_source_line {
        return None;
    }
    let [return_effect] = return_segment.default_effects.as_slice() else {
        return None;
    };
    let returned = return_effect.downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    if !matches!(returned.target.unhinted(), ChooseSpec::Tagged(tag) if tag == &tagged_target.tag)
        || returned.target.source_reference_surface()
            != Some(&crate::target::SourceReferenceSurface::ThisPermanentType(
                "that card".to_string(),
            ))
        || returned.zone != Zone::Battlefield
        || returned.to_top
        || returned.library_order.is_some()
        || returned.verb_surface != ironsmith_core::MoveToZoneVerbSurface::Return
        || returned.target_plural_surface
        || returned.remainder_surface.is_some()
        || returned.actor_surface.is_some()
        || returned.destination_player_surface.is_some()
        || returned.destination_player_reference_surface.is_some()
        || returned.exiled_with_source_surface.is_some()
        || returned.battlefield_controller != ironsmith_core::BattlefieldController::Owner
        || !returned.controller_surface_explicit
        || !returned.enters_with_counters.is_empty()
        || returned.enters_tapped
        || returned.enters_attacking
        || returned.attack_target_mode.is_some()
        || returned.enters_face_down
        || returned.enters_transformed
        || returned.transfer_exiled_with_source_links
    {
        return None;
    }

    Some(
        "Choose another target creature or artifact. When it's put into a graveyard this turn, return that card to the battlefield under its owner's control"
            .to_string(),
    )
}

fn describe_target_creature_delayed_owner_graveyard_return_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [target_segment, delayed_segment] = program.segments.as_slice() else {
        return None;
    };
    if [target_segment, delayed_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty() || segment.starts_new_source_line)
    {
        return None;
    }
    let [target_root] = target_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_target = target_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let target = tagged_target
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let ChooseSpec::Target(target_inner) = target.target.unhinted() else {
        return None;
    };
    let ChooseSpec::Object(target_filter) = target_inner.unhinted() else {
        return None;
    };
    let mut semantic_target_filter = target_filter.clone();
    semantic_target_filter.union_surface = Default::default();
    if semantic_target_filter != ObjectFilter::creature().in_zone(Zone::Battlefield)
        || target.chooser.is_some()
        || !target.explicit_declaration
    {
        return None;
    }

    let [delayed_root] = delayed_segment.default_effects.as_slice() else {
        return None;
    };
    let schedule = delayed_root.downcast_ref::<crate::effects::ScheduleDelayedTriggerEffect>()?;
    if schedule.target_tag.as_ref() != Some(&tagged_target.tag)
        || !schedule.one_shot
        || schedule.duration != ironsmith_core::DelayedTriggerDuration::EndOfTurn
        || !schedule.until_end_of_turn
        || schedule.start_next_turn
        || schedule.controller != PlayerFilter::You
    {
        return None;
    }
    let dies = schedule
        .trigger
        .downcast_ref::<crate::triggers::ZoneChangeTrigger>()?;
    if dies.from != crate::triggers::ZonePattern::Specific(Zone::Battlefield)
        || dies.to != crate::triggers::ZonePattern::Specific(Zone::Graveyard)
        || !dies.this_object
    {
        return None;
    }
    let [payload] = schedule.effects.segments.as_slice() else {
        return None;
    };
    let [tag_triggering, choose_root, move_root] = payload.default_effects.as_slice() else {
        return None;
    };
    let triggering = tag_triggering.downcast_ref::<crate::effects::TagTriggeringObjectEffect>()?;
    let choose = choose_root.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let tagged_move = move_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let moved = tagged_move
        .effect
        .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    let expected_owner =
        PlayerFilter::AliasedOwnerOf(crate::filter::ObjectRef::tagged(triggering.tag.clone()));
    let mut expected_filter = ObjectFilter::creature().in_zone(Zone::Graveyard);
    expected_filter.owner = Some(expected_owner);
    let mut semantic_choice_filter = choose.filter.clone();
    semantic_choice_filter.union_surface = Default::default();
    if !payload.self_replacements.is_empty()
        || payload.starts_new_source_line
        || semantic_choice_filter != expected_filter
        || choose.count != crate::ChoiceCount::exactly(1)
        || choose.chooser != PlayerFilter::You
        || !matches!(moved.target.unhinted(), ChooseSpec::Tagged(tag) if tag == &choose.tag)
        || moved.zone != Zone::Battlefield
        || moved.battlefield_controller != crate::effects::BattlefieldController::Owner
        || !moved.controller_surface_explicit
    {
        return None;
    }

    Some("Choose target creature. When that creature dies this turn, return a creature card from its owner's graveyard to the battlefield under the control of that creature's owner".to_string())
}

/// Preserve an explicit target collection across an intervening sacrifice
/// cost and its result branch. The chosen-object accumulator is the runtime
/// identity edge for the original target declaration; the effect id proves
/// that the return happens only when that exact sacrifice succeeds.
fn describe_chosen_creatures_sacrifice_then_return_tapped_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [target_segment, sacrifice_segment, return_segment] = program.segments.as_slice() else {
        return None;
    };
    if [target_segment, sacrifice_segment, return_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }

    let [target_root] = target_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_target = target_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let target_only = structural_unwrap_render_wrappers(target_root)
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let ChooseSpec::WithCount(target, target_count) = target_only.target.unhinted() else {
        return None;
    };
    let ChooseSpec::Target(target) = target.unhinted() else {
        return None;
    };
    let ChooseSpec::Object(target_filter) = target.unhinted() else {
        return None;
    };
    let mut semantic_target = target_filter.clone();
    semantic_target.union_surface = Default::default();
    let expected_target = ObjectFilter::creature()
        .in_zone(Zone::Graveyard)
        .owned_by(PlayerFilter::You);
    if semantic_target != expected_target
        || target_count != &ChoiceCount::exactly(2)
        || target_only.chooser.is_some()
        || !target_only.explicit_declaration
    {
        return None;
    }

    let [choose_root, sacrifice_root] = sacrifice_segment.default_effects.as_slice() else {
        return None;
    };
    let choose = choose_root.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let with_id = sacrifice_root.downcast_ref::<crate::effects::WithIdEffect>()?;
    let sacrifice = with_id
        .effect
        .downcast_ref::<crate::effects::zones::SacrificePlayerEffect>()?;
    let expected_sacrifice_filter = ObjectFilter::creature()
        .in_zone(Zone::Battlefield)
        .controlled_by(PlayerFilter::You);
    let mut semantic_sacrifice_choice = choose.filter.clone();
    semantic_sacrifice_choice.union_surface = Default::default();
    if semantic_sacrifice_choice != expected_sacrifice_filter
        || choose.count != ChoiceCount::exactly(1)
        || choose.count_value.is_some()
        || choose.aggregate_constraint.is_some()
        || choose.chooser != PlayerFilter::You
        || choose.zone.is_some()
        || !choose.additional_zones.is_empty()
        || choose.is_search
        || choose.reveal
        || choose.replace_tagged_objects
        || choose.remember_as_chosen_object
        || sacrifice
            != &crate::effects::zones::SacrificePlayerEffect::new(
                ObjectFilter::tagged(choose.tag.clone()),
                Value::Fixed(1),
                PlayerFilter::You,
            )
    {
        return None;
    }

    let [if_root] = return_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional = if_root.downcast_ref::<crate::effects::IfEffect>()?;
    if conditional.condition != with_id.id
        || conditional.predicate != crate::effect::EffectPredicate::Happened
        || !conditional.else_.is_empty()
        || conditional.per_player_result
        || conditional.prior_result_replacement_surface
    {
        return None;
    }
    let [return_root] = conditional.then.as_slice() else {
        return None;
    };
    let returned = structural_unwrap_render_wrappers(return_root)
        .downcast_ref::<crate::effects::ReturnFromGraveyardToBattlefieldEffect>()?;
    if !matches!(returned.target.unhinted(), ChooseSpec::Tagged(tag) if tag == &tagged_target.tag)
        || !returned.tapped
        || returned.as_aura.is_some()
        || !returned.enters_with_counters.is_empty()
    {
        return None;
    }

    Some(
        "Choose two target creature cards in your graveyard. Sacrifice a creature. If you do, return the chosen cards to the battlefield tapped"
            .to_string(),
    )
}

#[cfg(test)]
mod chosen_creatures_sacrifice_return_tests {
    use super::*;

    fn exact_program() -> crate::resolution::ResolutionProgram {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Chosen Creature Return Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text("Choose two target creature cards in your graveyard. Sacrifice a creature. If you do, return the chosen cards to the battlefield tapped.")
        .expect("typed sacrifice and return program should compile")
        .spell_effect
        .expect("spell program")
    }

    #[test]
    fn renders_the_exact_chosen_set_and_sacrifice_result() {
        let program = exact_program();
        assert_eq!(
            describe_chosen_creatures_sacrifice_then_return_tapped_program(&program).as_deref(),
            Some(
                "Choose two target creature cards in your graveyard. Sacrifice a creature. If you do, return the chosen cards to the battlefield tapped"
            ),
            "{program:#?}",
        );
    }

    #[test]
    fn rejects_a_return_bound_to_a_different_result_id() {
        let mut program = exact_program();
        let conditional = program.segments[2].default_effects[0]
            .downcast_ref::<crate::effects::IfEffect>()
            .expect("conditional return")
            .clone();
        let mut changed = conditional;
        changed.condition = crate::effect::EffectId(changed.condition.0 + 1);
        program.segments[2].default_effects[0] = Effect::new(changed);
        assert!(describe_chosen_creatures_sacrifice_then_return_tapped_program(&program).is_none());
    }
}

fn describe_each_creature_except_controlled_flying_damage_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() || segment.starts_new_source_line {
        return None;
    }
    let [root] = segment.default_effects.as_slice() else {
        return None;
    };
    let for_each = root
        .downcast_ref::<crate::effects::WithIdEffect>()
        .map(|with_id| with_id.effect.as_ref())
        .unwrap_or(root)
        .downcast_ref::<crate::effects::ForEachObject>()?;
    let mut expected = ObjectFilter::creature().in_zone(Zone::Battlefield);
    expected.any_of = vec![
        ObjectFilter::default().controlled_by(PlayerFilter::NotYou),
        ObjectFilter::default()
            .without_static_ability(crate::static_abilities::StaticAbilityId::Flying),
    ];
    let [damage_root] = for_each.effects.as_slice() else {
        return None;
    };
    let tagged_damage = damage_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let damage = tagged_damage
        .effect
        .downcast_ref::<crate::effects::DealDamageEffect>()?;
    if for_each.filter != expected
        || !matches!(damage.target.unhinted(), ChooseSpec::Iterated)
        || damage.source_is_combat
        || damage.unpreventable
    {
        return None;
    }
    Some(format!(
        "This spell deals {} damage to each creature except for creatures you control with flying",
        super::normalize_common::describe_value(&damage.amount)
    ))
}

fn describe_looked_hand_optional_free_cast_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() || segment.starts_new_source_line {
        return None;
    }
    let [look_effect, cast_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let look = look_effect.downcast_ref::<crate::effects::LookAtHandEffect>()?;
    let cast = cast_effect
        .downcast_ref::<crate::effects::MayCastMatchingSpellWithoutPayingManaCostEffect>()?;
    if look.target != ChooseSpec::Player(PlayerFilter::DamagedPlayer)
        || look.reveal
        || cast.player != PlayerFilter::You
        || cast.zone_owner != PlayerFilter::DamagedPlayer
        || cast.zone != Zone::Hand
        || cast.filter != ObjectFilter::nonland().in_zone(Zone::Hand)
        || cast.payment != ironsmith_core::MayCastMatchingSpellPayment::WithoutPayingManaCost
    {
        return None;
    }
    Some(
        "Look at that player's hand. You may cast a spell from among those cards without paying its mana cost"
            .to_string(),
    )
}

/// Render a prevention shield whose chosen target is conditionally scheduled
/// for counters equal to the amount that shield actually prevented. The
/// scheduler's prevention-shield flag is the executable numeric provenance;
/// the `targeted_0` snapshot is the exact original target, not the spell.
fn describe_prevent_next_damage_then_delayed_target_counters_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let (prevention_effect, conditional_effect) = match program.segments.as_slice() {
        [segment] if segment.self_replacements.is_empty() => {
            let [prevention, conditional] = segment.default_effects.as_slice() else {
                return None;
            };
            (prevention, conditional)
        }
        [prevention_segment, conditional_segment]
            if [prevention_segment, conditional_segment]
                .iter()
                .all(|segment| segment.self_replacements.is_empty()) =>
        {
            let [prevention] = prevention_segment.default_effects.as_slice() else {
                return None;
            };
            let [conditional] = conditional_segment.default_effects.as_slice() else {
                return None;
            };
            (prevention, conditional)
        }
        _ => return None,
    };
    let with_id = prevention_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let prevention = with_id
        .effect
        .downcast_ref::<crate::effects::PreventDamageEffect>()?;
    if !matches!(prevention.target.unhinted(), ChooseSpec::AnyTarget)
        || prevention.duration != Until::EndOfTurn
        || prevention.damage_filter != crate::prevention::DamageFilter::default()
        || !prevention.follow_up_effects.is_empty()
        || prevention.source_of_your_choice
        || prevention.protect_you_and_permanents_you_control
    {
        return None;
    }

    let conditional = conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::TargetMatches(condition_filter) = &conditional.condition else {
        return None;
    };
    let mut semantic_condition_filter = condition_filter.clone();
    semantic_condition_filter.union_surface = Default::default();
    let mut creature_characteristic = ObjectFilter::default();
    creature_characteristic.card_types.push(CardType::Creature);
    if semantic_condition_filter != creature_characteristic
        || !conditional.if_false.is_empty()
        || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
    {
        return None;
    }
    let [schedule_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let schedule =
        schedule_effect.downcast_ref::<crate::effects::ScheduleDelayedTriggerEffect>()?;
    if !schedule.one_shot
        || schedule.start_next_turn
        || schedule.duration != ironsmith_core::DelayedTriggerDuration::Forever
        || schedule.until_end_of_turn
        || schedule.until_end_of_combat
        || schedule.leading_duration_surface
        || schedule.watch_ability_source
        || schedule.watch_all_object_targets
        || schedule.either_of_watched_objects
        || schedule.while_any_tagged_object_in_zone.is_some()
        || !schedule.target_objects.is_empty()
        || schedule.target_tag.is_some()
        || schedule.target_filter.is_some()
        || schedule.controller != PlayerFilter::You
        || schedule.prepayment.is_some()
        || !schedule.event_value_from_prior_prevention
    {
        return None;
    }
    let end_step = schedule
        .trigger
        .downcast_ref::<crate::triggers::BeginningOfEndStepTrigger>()?;
    if end_step.player != PlayerFilter::Any {
        return None;
    }
    let [counter_segment] = schedule.effects.segments.as_slice() else {
        return None;
    };
    if !counter_segment.self_replacements.is_empty() {
        return None;
    }
    let [counter_effect] = counter_segment.default_effects.as_slice() else {
        return None;
    };
    let counter_effect = structural_unwrap_render_wrappers(counter_effect);
    let counters = counter_effect.downcast_ref::<crate::effects::PutCountersEffect>()?;
    if counters.amount.unhinted() != &Value::EventValue(EventValueSpec::Amount)
        || !matches!(counters.target.unhinted(), ChooseSpec::Tagged(tag) if tag.as_str() == "targeted_0")
        || counters.target_count.is_some()
        || counters.distributed
    {
        return None;
    }

    let prevention_text = describe_effect(prevention_effect)
        .trim()
        .trim_end_matches('.')
        .to_string();
    if !prevention_text.starts_with("Prevent the next ")
        || !prevention_text.ends_with("damage that would be dealt to any target this turn")
    {
        return None;
    }
    let counter = super::normalize_common::describe_counter_type(counters.counter_type);
    Some(format!(
        "{prevention_text}. If it's a creature, put {} on it for each 1 damage prevented this way at the beginning of the next end step",
        with_indefinite_article(&format!("{counter} counter")),
    ))
}

#[cfg(test)]
mod chosen_target_delayed_graveyard_return_tests {
    use super::*;

    fn program(watched_tag: &str, returned_tag: &str) -> crate::resolution::ResolutionProgram {
        let mut creature = ObjectFilter::creature();
        creature.other = true;
        let mut target_filter = ObjectFilter::default().in_zone(Zone::Battlefield);
        target_filter.any_of = vec![creature, ObjectFilter::artifact()];
        let target = Effect::new(crate::effects::TargetOnlyEffect::explicit(
            ChooseSpec::target(ChooseSpec::Object(target_filter.clone())),
        ))
        .tag(watched_tag);

        let returned_target = ChooseSpec::Tagged(TagKey::from(returned_tag)).with_surface_hint(
            ironsmith_core::ChooseSpecSurfaceHint::SourceReference(
                crate::target::SourceReferenceSurface::ThisPermanentType("that card".to_string()),
            ),
        );
        let mut returned =
            crate::effects::MoveToZoneEffect::new(returned_target, Zone::Battlefield, false);
        returned.verb_surface = ironsmith_core::MoveToZoneVerbSurface::Return;
        returned.battlefield_controller = ironsmith_core::BattlefieldController::Owner;
        returned.controller_surface_explicit = true;

        let trigger = crate::triggers::Trigger::new(
            crate::triggers::ZoneChangeTrigger::new()
                .from(Zone::Battlefield)
                .to(Zone::Graveyard)
                .filter(ObjectFilter::default().in_zone(Zone::Battlefield))
                .this(),
        );
        let delayed = crate::effects::ScheduleDelayedTriggerEffect::from_tag(
            trigger,
            vec![Effect::new(returned)],
            true,
            watched_tag,
            PlayerFilter::You,
        )
        .with_target_filter(ObjectFilter::tagged(watched_tag))
        .until_end_of_turn();

        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![target]),
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(delayed)]),
        ])
    }

    #[test]
    fn exact_chosen_target_identity_restores_pronoun_and_zone_change_surface() {
        assert_eq!(
            describe_choose_another_creature_or_artifact_then_delayed_return_program(&program(
                "targeted_0",
                "targeted_0",
            )),
            Some(
                "Choose another target creature or artifact. When it's put into a graveyard this turn, return that card to the battlefield under its owner's control"
                    .to_string(),
            )
        );
    }

    #[test]
    fn different_delayed_return_identity_does_not_claim_the_surface() {
        assert_eq!(
            describe_choose_another_creature_or_artifact_then_delayed_return_program(&program(
                "targeted_0",
                "other_target",
            )),
            None
        );
    }
}

fn describe_optional_target_cant_block_while_control_source_then_ring(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() || segment.starts_new_source_line {
        return None;
    }
    let [tag_triggering, target_effect, cant_effect, ring_effect] =
        segment.default_effects.as_slice()
    else {
        return None;
    };
    tag_triggering.downcast_ref::<crate::effects::TagTriggeringObjectEffect>()?;

    let targeted = target_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let target = targeted
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let expected_target = ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature()))
        .with_count(crate::effect::ChoiceCount::up_to(1));
    if target.chooser.is_some() || target.explicit_declaration || target.target != expected_target {
        return None;
    }

    let cant = cant_effect.downcast_ref::<crate::effects::CantEffect>()?;
    let mut restricted = ObjectFilter::creature();
    restricted
        .tagged_constraints
        .push(crate::filter::TaggedObjectConstraint {
            tag: targeted.tag.clone(),
            relation: crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        });
    if cant.restriction != crate::effect::Restriction::Block(restricted)
        || cant.duration != Until::YouStopControllingThis
        || cant.start != ironsmith_core::RestrictionStart::Immediate
        || cant.duration_surface != ironsmith_core::RestrictionDurationSurface::Default
    {
        return None;
    }
    let ring = ring_effect.downcast_ref::<crate::effects::RingTemptsYouEffect>()?;
    if ring.player != PlayerFilter::You {
        return None;
    }

    Some(
        "Up to one target creature can't block for as long as you control this Saga. The Ring tempts you"
            .to_string(),
    )
}

fn describe_return_chosen_set_then_draw_for_opponents_controlling_it(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let (target_effect, return_effect, draw_effect) = match program.segments.as_slice() {
        [segment] if segment.self_replacements.is_empty() && !segment.starts_new_source_line => {
            let [target, returned, draw] = segment.default_effects.as_slice() else {
                return None;
            };
            (target, returned, draw)
        }
        [target_segment, return_segment, draw_segment]
            if [target_segment, return_segment, draw_segment]
                .iter()
                .all(|segment| {
                    segment.self_replacements.is_empty() && !segment.starts_new_source_line
                }) =>
        {
            let [target] = target_segment.default_effects.as_slice() else {
                return None;
            };
            let [returned] = return_segment.default_effects.as_slice() else {
                return None;
            };
            let [draw] = draw_segment.default_effects.as_slice() else {
                return None;
            };
            (target, returned, draw)
        }
        _ => return None,
    };
    let targeted = target_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let target = targeted
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let ChooseSpec::Object(target_filter) = target.target.base() else {
        return None;
    };
    let mut semantic_target_filter = target_filter.clone();
    semantic_target_filter.union_surface = Default::default();
    let mut graveyard_permanents = ObjectFilter::permanent_card().in_zone(Zone::Graveyard);
    graveyard_permanents.entered_graveyard_this_turn = true;
    graveyard_permanents.entered_graveyard_from_battlefield_this_turn = true;
    if target.chooser.is_some()
        || !target.explicit_declaration
        || !target.target.is_target()
        || target.target.count() != crate::effect::ChoiceCount::up_to(3)
        || semantic_target_filter != graveyard_permanents
    {
        return None;
    }
    let returned = return_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let move_effect = returned
        .effect
        .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    let expected_move = crate::effects::MoveToZoneEffect::new(
        ChooseSpec::Tagged(targeted.tag.clone()),
        Zone::Battlefield,
        false,
    )
    .with_verb_surface(ironsmith_core::MoveToZoneVerbSurface::Return)
    .under_owner_control()
    .tapped();
    if move_effect != &expected_move {
        return None;
    }
    let players = draw_effect.downcast_ref::<crate::effects::ForPlayersEffect>()?;
    if players.filter != PlayerFilter::Opponent
        || players.starting_with_controller
        || players.stop_after_first_happened
    {
        return None;
    }
    let [conditional_effect] = players.effects.as_slice() else {
        return None;
    };
    let conditional = conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::PlayerControls { player, filter } = &conditional.condition else {
        return None;
    };
    let mut controlled = ObjectFilter::permanent_card().in_zone(Zone::Battlefield);
    controlled
        .tagged_constraints
        .push(crate::filter::TaggedObjectConstraint {
            tag: returned.tag.clone(),
            relation: crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        });
    let mut semantic_controlled_filter = filter.clone();
    semantic_controlled_filter.union_surface = Default::default();
    if player != &PlayerFilter::IteratedPlayer
        || semantic_controlled_filter != controlled
        || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }
    let [draw] = conditional.if_true.as_slice() else {
        return None;
    };
    let draw = draw.downcast_ref::<crate::effects::DrawCardsEffect>()?;
    if draw.count != Value::Fixed(1) || draw.player != PlayerFilter::You {
        return None;
    }

    Some(
        "Choose up to three target permanent cards in graveyards that were put there from the battlefield this turn. Return them to the battlefield tapped under their owners' control. You draw a card for each opponent who controls one or more of those permanents"
            .to_string(),
    )
}

/// Recombine one shared player target with two resolution-time permanent
/// choices and a later damage sentence. The synthetic target declaration is
/// executable targeting scaffolding; both choices and the damage recipient
/// must resolve through that same `Target(Any)` player filter.
fn describe_shared_target_player_two_sacrifices_then_damage_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    fn exact_target_player() -> PlayerFilter {
        PlayerFilter::Target(Box::new(PlayerFilter::Any))
    }

    fn exact_choice_and_sacrifice(
        choose_effect: &Effect,
        sacrifice_effect: &Effect,
        card_type: CardType,
    ) -> Option<()> {
        let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
        if choose.count != ChoiceCount::exactly(1)
            || choose.count_value.is_some()
            || choose.aggregate_constraint.is_some()
            || choose.chooser != exact_target_player()
            || choose.is_search
            || choose.reveal
            || choose.top_only
            || choose.bottom_only
            || choose.replace_tagged_objects
            || !choose.additional_zones.is_empty()
        {
            return None;
        }
        let mut filter = choose.filter.clone();
        filter.union_surface = Default::default();
        filter.source_surface = None;
        let expected = match card_type {
            CardType::Artifact => ObjectFilter::artifact(),
            CardType::Land => ObjectFilter::land(),
            _ => return None,
        }
        .controlled_by(exact_target_player());
        if filter != expected {
            return None;
        }

        let sacrifice = sacrifice_view(sacrifice_effect)?;
        if sacrifice.count.unhinted() != &Value::Fixed(1)
            || sacrifice.player != &exact_target_player()
            || sacrifice.filter != &ObjectFilter::tagged(choose.tag.clone())
        {
            return None;
        }
        Some(())
    }

    let [sacrifice_segment, damage_segment] = program.segments.as_slice() else {
        return None;
    };
    if sacrifice_segment.starts_new_source_line
        || damage_segment.starts_new_source_line
        || !sacrifice_segment.self_replacements.is_empty()
        || !damage_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [sequence_effect] = sacrifice_segment.default_effects.as_slice() else {
        return None;
    };
    let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::Coordinated {
        return None;
    }
    let [
        target_effect,
        artifact_choose,
        artifact_sacrifice,
        land_choose,
        land_sacrifice,
    ] = sequence.effects.as_slice()
    else {
        return None;
    };
    let target = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if target.explicit_declaration
        || target.chooser.is_some()
        || target.target != ChooseSpec::target_player()
    {
        return None;
    }
    exact_choice_and_sacrifice(artifact_choose, artifact_sacrifice, CardType::Artifact)?;
    exact_choice_and_sacrifice(land_choose, land_sacrifice, CardType::Land)?;

    let [damage_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let damage = unwrap_basic_tag_wrappers(damage_effect)
        .downcast_ref::<crate::effects::DealDamageEffect>()?;
    if damage.amount.unhinted() != &Value::Fixed(2)
        || damage.target != ChooseSpec::Player(exact_target_player())
        || damage.source_is_combat
        || damage.unpreventable
    {
        return None;
    }

    Some(
        "Target player sacrifices an artifact and a land of their choice. Deal 2 damage to that player"
            .to_string(),
    )
}

/// Preserve two damage recipients that are declared before a coordinated
/// same-source damage pair. The second `EffectValue` is the typed "that much"
/// correlation; the two declarations prove which recipient belongs to each
/// damage instruction.
fn describe_declared_linked_same_source_damage_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let [sequence_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::Coordinated {
        return None;
    }
    let [
        first_declaration,
        second_declaration,
        first_effect,
        second_effect,
    ] = sequence.effects.as_slice()
    else {
        return None;
    };
    let first_declaration = first_declaration.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let second_declaration =
        second_declaration.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if first_declaration.explicit_declaration
        || second_declaration.explicit_declaration
        || first_declaration.chooser.is_some()
        || second_declaration.chooser.is_some()
    {
        return None;
    }
    let (first_source, first_damage) = damage_with_source_view(first_effect)?;
    let (second_source, second_damage) = damage_with_source_view(second_effect)?;
    let (Some(first_source), Some(second_source)) = (first_source, second_source) else {
        return None;
    };
    if first_source.unhinted() != second_source.unhinted()
        || first_declaration.target.unhinted() != first_damage.target.unhinted()
        || second_declaration.target.unhinted() != second_damage.target.unhinted()
        || !matches!(second_damage.amount.unhinted(), Value::EffectValue(_))
        || first_damage.source_is_combat
        || second_damage.source_is_combat
        || first_damage.unpreventable
        || second_damage.unpreventable
        || !first_damage
            .amount
            .has_surface_hint(ironsmith_core::ValueSurfaceHint::EqualTo)
    {
        return None;
    }
    let source = first_source
        .source_reference_surface()
        .map(crate::target::SourceReferenceSurface::display_text)
        .unwrap_or_else(|| describe_choose_spec(first_source));
    let first_clause = describe_effect(first_effect);
    let (_, first_predicate) = first_clause.split_once(" deals ")?;
    let first_predicate = if source.trim().eq_ignore_ascii_case("it") {
        [
            "this artifact",
            "this creature",
            "this enchantment",
            "this permanent",
            "this source",
        ]
        .into_iter()
        .fold(first_predicate.to_string(), |text, source_noun| {
            text.replace(&format!("on {source_noun}"), "on it")
        })
    } else {
        first_predicate.to_string()
    };
    Some(format!(
        "{} deals {first_predicate} and that much damage to {}",
        capitalize_first_ascii(&source),
        describe_damage_target(&second_damage.target),
    ))
}

fn plain_creature_target_filter(spec: &ChooseSpec) -> Option<&ObjectFilter> {
    let filter = match spec.unhinted() {
        ChooseSpec::Target(inner) => match inner.unhinted() {
            ChooseSpec::Object(filter) => filter,
            _ => return None,
        },
        _ => return None,
    };
    let mut residual = filter.clone();
    if residual.zone != Some(Zone::Battlefield)
        || residual.card_types.as_slice() != [CardType::Creature]
    {
        return None;
    }
    residual.zone = None;
    residual.card_types.clear();
    residual.set_explicit_card_type_noun(None);
    (residual == ObjectFilter::default()).then_some(filter)
}

/// Rejoin a creature's self-power damage with a following condition on that
/// exact creature and damage to its controller. All three references must use
/// the same exported target tag.
fn describe_cross_segment_self_power_then_controller_damage_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [self_segment, conditional_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if self_segment.starts_new_source_line
        || conditional_segment.starts_new_source_line
        || !self_segment.self_replacements.is_empty()
        || !conditional_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [target_effect, self_damage_effect] = self_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_target = target_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let target = tagged_target
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    plain_creature_target_filter(&target.target)?;
    if target.explicit_declaration || target.chooser.is_some() {
        return None;
    }
    let (_, self_damage) = damage_with_source_view(self_damage_effect)?;
    let self_damage_root = self_damage_effect
        .downcast_ref::<crate::effects::TaggedEffect>()
        .map_or(self_damage_effect, |tagged| tagged.effect.as_ref());
    let with_source = self_damage_root.downcast_ref::<crate::effects::ExecuteWithSourceEffect>()?;
    if with_source.source.unhinted() != &ChooseSpec::Tagged(tagged_target.tag.clone())
        || self_damage.target.unhinted() != &ChooseSpec::Source
        || self_damage.source_is_combat
        || self_damage.unpreventable
        || !matches!(
            self_damage.amount.unhinted(),
            Value::PowerOf(source) if source.unhinted() == &ChooseSpec::Tagged(tagged_target.tag.clone())
        )
    {
        return None;
    }

    let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional = conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::TaggedObjectMatches(condition_tag, condition_filter) = &conditional.condition
    else {
        return None;
    };
    let mut plain_attacking = condition_filter.clone();
    if !plain_attacking.attacking {
        return None;
    }
    plain_attacking.attacking = false;
    plain_attacking.set_demonstrative_antecedent_surface(None);
    if condition_tag != &tagged_target.tag
        || plain_attacking != ObjectFilter::default()
        || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }
    let [controller_damage_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let controller_damage =
        controller_damage_effect.downcast_ref::<crate::effects::DealDamageEffect>()?;
    if controller_damage.source_is_combat || controller_damage.unpreventable {
        return None;
    }
    let ChooseSpec::Player(PlayerFilter::ControllerOf(crate::filter::ObjectRef::Tagged(
        controller_tag,
    ))) = controller_damage.target.base()
    else {
        return None;
    };
    if controller_tag != &tagged_target.tag {
        return None;
    }
    if !matches!(controller_damage.amount.unhinted(), Value::Fixed(_)) {
        return None;
    }
    let controller_amount = format!("{} damage", describe_value(&controller_damage.amount));
    Some((
        format!(
            "{} deals damage equal to its power to itself. If that creature is attacking, this deals {controller_amount} to that creature's controller",
            capitalize_first_ascii(&describe_choose_spec(&target.target)),
        ),
        2,
    ))
}

fn exact_controlled_battlefield_count(value: &Value) -> Option<&ObjectFilter> {
    if !value.has_surface_hint(ironsmith_core::ValueSurfaceHint::EqualTo) {
        return None;
    }
    let Value::Count(filter) = value.unhinted() else {
        return None;
    };
    (filter.zone == Some(Zone::Battlefield) && filter.controller == Some(PlayerFilter::You))
        .then_some(filter)
}

/// Preserve independent typed counts when source damage and life gain occupy
/// adjacent source sentences. The counts deliberately need not match: their
/// distinct filters are the Dogs/Cats-style correlation this window protects.
fn describe_cross_segment_count_damage_then_count_life_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [damage_segment, life_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if damage_segment.starts_new_source_line
        || life_segment.starts_new_source_line
        || !damage_segment.self_replacements.is_empty()
        || !life_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [damage_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let (Some(source), damage) = damage_with_source_view(damage_effect)? else {
        return None;
    };
    let [life_effect] = life_segment.default_effects.as_slice() else {
        return None;
    };
    let life = life_effect.downcast_ref::<crate::effects::GainLifeEffect>()?;
    let damage_filter = exact_controlled_battlefield_count(&damage.amount)?;
    let life_filter = exact_controlled_battlefield_count(&life.amount)?;
    if damage_filter == life_filter
        || damage.target.unhinted() != &ChooseSpec::AnyTarget
        || life.player.unhinted() != &ChooseSpec::Player(PlayerFilter::You)
        || damage.source_is_combat
        || damage.unpreventable
    {
        return None;
    }
    Some((
        format!(
            "{} deals damage to any target equal to {}. You gain life equal to {}",
            capitalize_first_ascii(&describe_choose_spec(source)),
            describe_value(&damage.amount),
            describe_value(&life.amount),
        ),
        2,
    ))
}

/// Preserve the result identity between a source-linked damage instruction
/// based on a sacrificed creature and the following life gain. The shared
/// effect id is both executable correlation and the typed "damage dealt this
/// way" surface.
fn describe_cross_segment_sacrificed_power_damage_life_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [damage_segment, life_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if damage_segment.starts_new_source_line
        || life_segment.starts_new_source_line
        || !damage_segment.self_replacements.is_empty()
        || !life_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [with_id_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let with_id = with_id_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let with_source = with_id
        .effect
        .downcast_ref::<crate::effects::ExecuteWithSourceEffect>()?;
    let damage = with_source
        .effect
        .downcast_ref::<crate::effects::DealDamageEffect>()?;
    let Value::PowerOf(power_source) = damage.amount.unhinted() else {
        return None;
    };
    let ChooseSpec::Tagged(sacrificed_tag) = power_source.unhinted() else {
        return None;
    };
    if !sacrificed_tag.as_str().starts_with("sacrificed_")
        || !damage
            .amount
            .has_surface_hint(ironsmith_core::ValueSurfaceHint::SacrificedObject(
                ironsmith_core::SacrificedObjectKind::Creature,
            ))
        || damage.target.unhinted() != &ChooseSpec::PlayerOrPlaneswalker(PlayerFilter::Any)
        || damage.source_is_combat
        || damage.unpreventable
    {
        return None;
    }
    let [life_effect] = life_segment.default_effects.as_slice() else {
        return None;
    };
    let life = life_effect.downcast_ref::<crate::effects::GainLifeEffect>()?;
    if life.player.unhinted() != &ChooseSpec::Player(PlayerFilter::You)
        || life.amount.unhinted() != &Value::EffectValue(with_id.id)
        || !life
            .amount
            .has_surface_hint(ironsmith_core::ValueSurfaceHint::DamageDealt)
        || !life
            .amount
            .has_surface_hint(ironsmith_core::ValueSurfaceHint::EqualTo)
    {
        return None;
    }
    let source = with_source
        .source
        .source_reference_surface()
        .map(crate::target::SourceReferenceSurface::display_text)
        .unwrap_or_else(|| describe_choose_spec(&with_source.source));
    Some((
        format!(
            "{} deals damage equal to the sacrificed creature's power to target player or planeswalker. You gain life equal to the damage dealt this way",
            capitalize_first_ascii(&source),
        ),
        2,
    ))
}

#[cfg(test)]
mod sacrificed_power_damage_life_window_tests {
    use super::*;

    fn program(life_result_id: u32) -> crate::resolution::ResolutionProgram {
        let sacrificed_tag = TagKey::from("sacrificed_0");
        let amount = Value::PowerOf(Box::new(ChooseSpec::Tagged(sacrificed_tag)))
            .with_surface_hint(ironsmith_core::ValueSurfaceHint::SacrificedObject(
                ironsmith_core::SacrificedObjectKind::Creature,
            ));
        let damage = Effect::new(crate::effects::ExecuteWithSourceEffect::new(
            ChooseSpec::Source,
            Effect::deal_damage(amount, ChooseSpec::PlayerOrPlaneswalker(PlayerFilter::Any)),
        ));
        let life_amount = Value::EffectValue(crate::effect::EffectId(life_result_id))
            .with_surface_hints([
                ironsmith_core::ValueSurfaceHint::DamageDealt,
                ironsmith_core::ValueSurfaceHint::EqualTo,
            ]);
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::with_id(7, damage)]),
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::gain_life(
                life_amount,
            )]),
        ])
    }

    #[test]
    fn sacrificed_power_damage_life_requires_the_same_result_id() {
        assert!(
            describe_cross_segment_sacrificed_power_damage_life_window(&program(7).segments, 0)
                .is_some()
        );
        assert!(
            describe_cross_segment_sacrificed_power_damage_life_window(&program(8).segments, 0)
                .is_none()
        );
    }
}

fn exact_single_tagged_creature_target(effect: &Effect) -> Option<(&TagKey, &ChooseSpec)> {
    let tagged = effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let target = tagged
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let filter = match target.target.unhinted() {
        ChooseSpec::Target(inner) => match inner.unhinted() {
            ChooseSpec::Object(filter) => filter,
            _ => return None,
        },
        _ => return None,
    };
    let mut residual = filter.clone();
    if residual.zone != Some(Zone::Battlefield)
        || residual.controller != Some(PlayerFilter::You)
        || residual.card_types.as_slice() != [CardType::Creature]
    {
        return None;
    }
    residual.zone = None;
    residual.controller = None;
    residual.card_types.clear();
    residual.set_explicit_card_type_noun(None);
    if residual != ObjectFilter::default() {
        return None;
    }
    (!target.explicit_declaration && target.chooser.is_none())
        .then_some((&tagged.tag, &target.target))
}

/// Rejoin an attachment result with the reflexive trigger it enables. The
/// WithId/If pair proves "this way", while the destination tag proves that
/// the creature receiving the Equipment is also the damage source.
fn describe_cross_segment_attach_result_power_damage_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [attach_segment, result_segment] = segments.get(start..start + 2)? else {
        return None;
    };
    if attach_segment.starts_new_source_line
        || result_segment.starts_new_source_line
        || !attach_segment.self_replacements.is_empty()
        || !result_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [attached_choice, destination_choice, with_id_effect] =
        attach_segment.default_effects.as_slice()
    else {
        return None;
    };
    let attached_choice = attached_choice.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if attached_choice.explicit_declaration || attached_choice.chooser.is_some() {
        return None;
    }
    let attached_filter = match attached_choice.target.unhinted() {
        ChooseSpec::WithCount(inner, count) if count.is_any_number() => match inner.unhinted() {
            ChooseSpec::Target(target) => match target.unhinted() {
                ChooseSpec::Object(filter) => filter,
                _ => return None,
            },
            _ => return None,
        },
        _ => return None,
    };
    let mut plain_equipment = attached_filter.clone();
    if plain_equipment.zone != Some(Zone::Battlefield)
        || plain_equipment.controller != Some(PlayerFilter::You)
        || plain_equipment.subtypes.as_slice() != [Subtype::Equipment]
    {
        return None;
    }
    plain_equipment.zone = None;
    plain_equipment.controller = None;
    plain_equipment.subtypes.clear();
    if plain_equipment != ObjectFilter::default() {
        return None;
    }
    let (destination_tag, destination_target) =
        exact_single_tagged_creature_target(destination_choice)?;
    let with_id = with_id_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let attach = with_id
        .effect
        .downcast_ref::<crate::effects::AttachObjectsEffect>()?;
    if attach.individual_targets
        || attach.objects.unhinted() != attached_choice.target.unhinted()
        || attach.target.unhinted() != &ChooseSpec::Tagged(destination_tag.clone())
    {
        return None;
    }

    let [result_effect] = result_segment.default_effects.as_slice() else {
        return None;
    };
    let result = result_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if result.condition != with_id.id
        || result.predicate != EffectPredicate::Happened
        || !result.else_.is_empty()
        || result.per_player_result
        || result.prior_result_replacement_surface
    {
        return None;
    }
    let [damage_target, damage_effect] = result.then.as_slice() else {
        return None;
    };
    let damage_target = damage_target.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    plain_creature_target_filter(match damage_target.target.unhinted() {
        ChooseSpec::WithCount(inner, count) if count.min == 0 && count.max == Some(1) => inner,
        _ => return None,
    })?;
    let (Some(damage_source), damage) = damage_with_source_view(damage_effect)? else {
        return None;
    };
    if damage_source.unhinted() != &ChooseSpec::Tagged(destination_tag.clone())
        || !matches!(
            damage.amount.unhinted(),
            Value::PowerOf(source) if source.unhinted() == &ChooseSpec::Tagged(destination_tag.clone())
        )
        || damage.target.unhinted() != damage_target.target.unhinted()
        || damage.source_is_combat
        || damage.unpreventable
    {
        return None;
    }
    let setup = format!(
        "Attach {} to {}",
        describe_choose_spec(&attached_choice.target),
        describe_choose_spec(destination_target),
    );
    Some((
        format!(
            "{setup}. When one or more Equipment become attached to that creature this way, that creature deals damage equal to its power to {}",
            describe_damage_target(&damage.target),
        ),
        2,
    ))
}

/// Rejoin a revealed top card, its optional characteristic-gated free cast,
/// and the exact failed-cast bottom placement. The shared tag and result ID
/// prove that both later `it` references name the revealed card.
fn describe_cross_segment_reveal_optional_cast_decline_bottom_window(
    segments: &[crate::resolution::ResolutionSegment],
    start: usize,
) -> Option<(String, usize)> {
    let [reveal_segment, cast_segment, decline_segment] = segments.get(start..start + 3)? else {
        return None;
    };
    if [reveal_segment, cast_segment, decline_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let [reveal_effect] = reveal_segment.default_effects.as_slice() else {
        return None;
    };
    let reveal = reveal_effect.downcast_ref::<crate::effects::RevealTopEffect>()?;
    let revealed_tag = reveal.tag.as_ref()?;
    if reveal.player != PlayerFilter::You {
        return None;
    }

    let [cast_effect] = cast_segment.default_effects.as_slice() else {
        return None;
    };
    let with_id = cast_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let conditional = with_id
        .effect
        .downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::TaggedObjectMatches(condition_tag, filter) = &conditional.condition else {
        return None;
    };
    let [relation] = filter.characteristic_relations.as_slice() else {
        return None;
    };
    if condition_tag != revealed_tag
        || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !conditional.if_false.is_empty()
        || relation.kind != ironsmith_core::ObjectCharacteristicRelationKind::SharesAny
        || relation.characteristics.as_slice()
            != [ironsmith_core::ObjectCharacteristic::Subtype(
                ironsmith_core::SubtypeFamily::Creature,
            )]
    {
        return None;
    }
    let mut candidate = filter.clone();
    candidate.characteristic_relations.clear();
    if candidate.card_types.as_slice() != [CardType::Creature] {
        return None;
    }
    candidate.card_types.clear();
    candidate.set_explicit_card_noun(false);
    candidate.set_explicit_card_type_noun(None);
    if candidate != ObjectFilter::default() {
        return None;
    }
    let mut comparison = relation.comparison.clone();
    if comparison.zone != Some(Zone::Battlefield)
        || comparison.controller != Some(PlayerFilter::You)
        || comparison.card_types.as_slice() != [CardType::Creature]
    {
        return None;
    }
    comparison.zone = None;
    comparison.controller = None;
    comparison.card_types.clear();
    comparison.set_explicit_card_type_noun(None);
    if comparison != ObjectFilter::default() {
        return None;
    }
    let [may_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let may = may_effect.downcast_ref::<crate::effects::MayEffect>()?;
    let [cast_effect] = may.effects.as_slice() else {
        return None;
    };
    let cast = cast_effect.downcast_ref::<crate::effects::CastTaggedEffect>()?;
    if !matches!(may.decider.as_ref(), None | Some(PlayerFilter::You))
        || may.fallback != crate::decision::FallbackStrategy::Decline
        || cast.tag != *revealed_tag
        || cast.player != PlayerFilter::You
        || cast.allow_land
        || cast.as_copy
        || !cast.without_paying_mana_cost
        || cast.additional_mana_cost.is_some()
        || cast.cost_reduction.is_some()
    {
        return None;
    }

    let [decline_effect] = decline_segment.default_effects.as_slice() else {
        return None;
    };
    let decline = decline_effect.downcast_ref::<crate::effects::IfEffect>()?;
    let [bottom_effect] = decline.then.as_slice() else {
        return None;
    };
    let bottom = bottom_effect.downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    if decline.condition != with_id.id
        || decline.predicate != EffectPredicate::DidNotHappen
        || !decline.else_.is_empty()
        || decline.per_player_result
        || bottom.zone != Zone::Library
        || bottom.to_top
        || bottom.library_order.is_some()
        || !matches!(bottom.target.base(), ChooseSpec::Tagged(tag) if tag == revealed_tag)
    {
        return None;
    }

    Some((
        "Reveal the top card of your library. If it's a creature card that shares a creature type with a creature you control, you may cast it without paying its mana cost. If you don't cast it, put it on the bottom of your library".to_string(),
        3,
    ))
}

#[cfg(test)]
mod reveal_optional_cast_decline_bottom_tests {
    use super::*;

    fn program() -> crate::resolution::ResolutionProgram {
        let oracle = "At the beginning of your upkeep, reveal the top card of your library. If it's a creature card that shares a creature type with a creature you control, you may cast it without paying its mana cost. If you don't cast it, put it on the bottom of your library.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Characteristic Consult Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(oracle)
        .expect("characteristic consult should parse");
        let crate::ability::AbilityKind::Triggered(triggered) = &definition.abilities[0].kind
        else {
            panic!("expected triggered ability");
        };
        triggered.effects.clone()
    }

    #[test]
    fn exact_shared_tag_and_result_id_rejoin_the_three_sentences() {
        let program = program();
        assert!(
            describe_cross_segment_reveal_optional_cast_decline_bottom_window(
                &program.segments,
                0,
            )
            .is_some()
        );
    }

    #[test]
    fn changed_result_id_is_not_claimed() {
        let mut program = program();
        let fallback = program.segments[2].default_effects[0]
            .downcast_ref::<crate::effects::IfEffect>()
            .expect("expected decline fallback")
            .clone();
        let mut changed = fallback;
        changed.condition = crate::effect::EffectId(99);
        program.segments[2].default_effects[0] = Effect::new(changed);
        assert!(
            describe_cross_segment_reveal_optional_cast_decline_bottom_window(
                &program.segments,
                0,
            )
            .is_none()
        );
    }
}

#[cfg(test)]
mod wave5_structural_and_winter_surface_tests {
    use super::*;

    fn choice_and_sacrifice(card_type: CardType, tag: &str) -> [Effect; 2] {
        let player = PlayerFilter::Target(Box::new(PlayerFilter::Any));
        let filter = match card_type {
            CardType::Artifact => ObjectFilter::artifact(),
            CardType::Land => ObjectFilter::land(),
            _ => panic!("fixture supports artifact or land"),
        }
        .controlled_by(player.clone());
        let tag = TagKey::from(tag);
        let choose = Effect::new(crate::effects::ChooseObjectsEffect::new(
            filter,
            ChoiceCount::exactly(1),
            player.clone(),
            tag.clone(),
        ));
        let sacrifice = Effect::new(crate::effects::zones::SacrificePlayerEffect::new(
            ObjectFilter::tagged(tag),
            Value::Fixed(1),
            player,
        ));
        [choose, sacrifice]
    }

    fn structural_program(damage: i32) -> crate::resolution::ResolutionProgram {
        let [artifact_choose, artifact_sacrifice] =
            choice_and_sacrifice(CardType::Artifact, "artifact");
        let [land_choose, land_sacrifice] = choice_and_sacrifice(CardType::Land, "land");
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(
                crate::effects::SequenceEffect::coordinated(vec![
                    Effect::new(crate::effects::TargetOnlyEffect::new(
                        ChooseSpec::target_player(),
                    )),
                    artifact_choose,
                    artifact_sacrifice,
                    land_choose,
                    land_sacrifice,
                ]),
            )]),
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::deal_damage(
                Value::Fixed(damage),
                ChooseSpec::Player(PlayerFilter::Target(Box::new(PlayerFilter::Any))),
            )]),
        ])
    }

    #[test]
    fn shared_target_two_sacrifices_and_damage_require_the_exact_linked_shape() {
        assert_eq!(
            describe_shared_target_player_two_sacrifices_then_damage_program(&structural_program(
                2
            ))
            .as_deref(),
            Some(
                "Target player sacrifices an artifact and a land of their choice. Deal 2 damage to that player"
            )
        );
        assert!(
            describe_shared_target_player_two_sacrifices_then_damage_program(&structural_program(
                3
            ))
            .is_none()
        );
    }

    fn winter_ability(count: u32) -> Ability {
        let model = crate::static_abilities::CompiledStaticAbility::max_hand_size_seven_minus_your_graveyard_card_types(
            PlayerFilter::Opponent,
            0,
        )
        .with_condition(Condition::PlayerHasCardTypesInGraveyardOrMore {
            player: PlayerFilter::You,
            count,
        });
        Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
    }

    #[test]
    fn delirium_maximum_hand_size_surface_requires_the_exact_four_type_gate() {
        assert_eq!(
            describe_structural_delirium_maximum_hand_size(&winter_ability(4)).as_deref(),
            Some(
                "Delirium — As long as there are four or more card types among cards in your graveyard, each opponent's maximum hand size is equal to seven minus the number of those card types"
            )
        );
        assert!(describe_structural_delirium_maximum_hand_size(&winter_ability(5)).is_none());
    }
}

/// Rejoin a revealed-card selection whose move, granted ability, and bottom
/// disposition are split across source-sentence segments. The shared reveal
/// and choice tags prove that the permission moves exactly one selected card,
/// that the grant applies to that same card, and that only the remainder is
/// put on the bottom.
fn describe_reveal_may_put_grant_then_bottom_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let (
        reveal_effect,
        choose_effect,
        move_each_effect,
        grant_effect,
        remainder_effect,
        optional_choice_count,
    ) = match program.segments.as_slice() {
        [segment] if segment.self_replacements.is_empty() && !segment.starts_new_source_line => {
            let [reveal, choose, move_each, grant, remainder] = segment.default_effects.as_slice()
            else {
                return None;
            };
            (reveal, choose, move_each, grant, remainder, true)
        }
        [reveal_segment, choice_segment, followup_segment]
            if [reveal_segment, choice_segment, followup_segment]
                .iter()
                .all(|segment| segment.self_replacements.is_empty()) =>
        {
            let [reveal] = reveal_segment.default_effects.as_slice() else {
                return None;
            };
            let [may_effect] = choice_segment.default_effects.as_slice() else {
                return None;
            };
            let may = may_effect.downcast_ref::<crate::effects::MayEffect>()?;
            if may.decider.as_ref() != Some(&PlayerFilter::You) {
                return None;
            }
            let [choose, move_each] = may.effects.as_slice() else {
                return None;
            };
            let [sequence_effect] = followup_segment.default_effects.as_slice() else {
                return None;
            };
            let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
            if sequence.surface != ironsmith_core::SequenceSurface::Coordinated {
                return None;
            }
            let [grant, remainder] = sequence.effects.as_slice() else {
                return None;
            };
            (reveal, choose, move_each, grant, remainder, false)
        }
        _ => return None,
    };

    let reveal = reveal_effect.downcast_ref::<crate::effects::LookAtTopCardsEffect>()?;
    if !reveal.reveal || reveal.player != PlayerFilter::You || reveal.viewer != PlayerFilter::You {
        return None;
    }

    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let exact_optional_choice_count = choose.count.min == 0
        && choose.count.max == Some(1)
        && !choose.count.dynamic_x
        && !choose.count.up_to_x
        && !choose.count.random
        && !choose.count.explicit_exactly;
    if choose.chooser != PlayerFilter::You
        || if optional_choice_count {
            !exact_optional_choice_count
        } else {
            choose.count != ChoiceCount::exactly(1)
        }
        || choose.count_value.is_some()
        || choose.is_search
        || choose.reveal
        || choose.zone != Some(Zone::Library)
        || !choose.filter.tagged_constraints.iter().any(|constraint| {
            constraint.tag == reveal.tag
                && constraint.relation == crate::filter::TaggedOpbjectRelation::IsTaggedObject
        })
    {
        return None;
    }
    let move_each = move_each_effect.downcast_ref::<crate::effects::ForEachTaggedEffect>()?;
    let [move_effect] = move_each.effects.as_slice() else {
        return None;
    };
    let moved = move_effect.downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    let mut expected_move =
        crate::effects::MoveToZoneEffect::new(ChooseSpec::Iterated, Zone::Battlefield, false);
    expected_move.verb_surface = ironsmith_core::MoveToZoneVerbSurface::Put;
    if move_each.tag != choose.tag || moved != &expected_move {
        return None;
    }

    let grant = structural_unwrap_render_wrappers(grant_effect)
        .downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    if grant.target != crate::continuous::EffectTarget::Source
        || grant.until != Until::Forever
        || grant.condition.is_some()
        || !grant.additional_modifications.is_empty()
        || !grant.runtime_modifications.is_empty()
        || !matches!(
            grant.target_spec.as_ref().map(ChooseSpec::unhinted),
            Some(ChooseSpec::Tagged(tag)) if tag == &choose.tag
        )
        || !matches!(
            grant.modification,
            Some(crate::continuous::Modification::AddAbilityGeneric(_))
        )
    {
        return None;
    }
    let remainder = remainder_effect
        .downcast_ref::<crate::effects::PutTaggedRemainderOnLibraryBottomEffect>()?;
    if remainder.tag != reveal.tag
        || remainder.keep_tagged.as_ref() != Some(&choose.tag)
        || remainder.order != crate::effects::consult_helpers::LibraryBottomOrder::ChooserChooses
        || remainder.player != PlayerFilter::You
        || remainder.surface != ironsmith_core::LibraryRemainderSurface::RestOfCardsRevealedThisWay
    {
        return None;
    }

    let mut selection_filter = choose.filter.clone();
    selection_filter.zone = None;
    selection_filter.tagged_constraints.clear();
    let selection =
        with_indefinite_article(strip_indefinite_article(&selection_filter.description()));
    let reveal_text = describe_effect(reveal_effect);
    let grant_text = capitalize_first(describe_effect(grant_effect).trim().trim_end_matches('.'));
    let grant_sentence = if grant_text.ends_with(".\"")
        || grant_text.ends_with("!\"")
        || grant_text.ends_with("?\"")
    {
        grant_text
    } else {
        format!("{grant_text}.")
    };
    let remainder_text = describe_effect(remainder_effect);
    Some(format!(
        "{}. You may put {selection} from among them onto the battlefield. {grant_sentence} Then {}",
        reveal_text.trim().trim_end_matches('.'),
        lowercase_first(remainder_text.trim().trim_end_matches('.'))
    ))
}

#[cfg(test)]
mod reveal_may_put_grant_then_bottom_tests {
    use super::*;

    const TEXT: &str = "Reveal the top four cards of your library. You may put a creature card from among them onto the battlefield. It gains \"At the beginning of your end step, return this creature to its owner's hand.\" Then put the rest of the cards revealed this way on the bottom of your library in any order.";

    fn program() -> crate::resolution::ResolutionProgram {
        crate::CardDefinitionBuilder::new(crate::ids::CardId::new(), "Aethermage's Touch")
            .card_types(vec![CardType::Instant])
            .parse_text(TEXT)
            .expect("linked revealed-card deployment should compile")
            .spell_effect
            .expect("instant spell program")
    }

    #[test]
    fn selected_card_move_grant_and_remainder_rejoin_exactly() {
        let program = program();
        assert_eq!(
            describe_reveal_may_put_grant_then_bottom_program(&program).as_deref(),
            Some(TEXT.trim_end_matches('.')),
            "{program:#?}",
        );
        let definition =
            crate::CardDefinitionBuilder::new(crate::ids::CardId::new(), "Aethermage's Touch")
                .card_types(vec![CardType::Instant])
                .parse_text(TEXT)
                .expect("linked revealed-card deployment should compile");
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition).join("\n"),
            TEXT
        );
    }

    #[test]
    fn changed_remainder_selection_tag_is_not_claimed() {
        let mut program = program();
        let mut remainder = program.segments[0].default_effects[4]
            .downcast_ref::<crate::effects::PutTaggedRemainderOnLibraryBottomEffect>()
            .expect("typed remainder")
            .clone();
        remainder.keep_tagged = Some(TagKey::from("different_selection"));
        program.segments[0].default_effects[4] = Effect::new(remainder);

        assert!(describe_reveal_may_put_grant_then_bottom_program(&program).is_none());
    }
}

fn describe_exile_creatures_then_per_player_fractal_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [exile_segment, player_segment] = program.segments.as_slice() else {
        return None;
    };
    if !exile_segment.self_replacements.is_empty()
        || !player_segment.self_replacements.is_empty()
        || exile_segment.starts_new_source_line
    {
        return None;
    }
    let [exile_effect] = exile_segment.default_effects.as_slice() else {
        return None;
    };
    let [player_effect] = player_segment.default_effects.as_slice() else {
        return None;
    };
    super::render_effects::describe_exile_all_creatures_each_player_fractal_power_counters(&[
        exile_effect.clone(),
        player_effect.clone(),
    ])
}

/// Rejoin a death-trigger return and its exact permanent-type reset when the
/// authored sentence boundary caused lowering to place them in adjacent
/// resolution segments. The existing effect-list matcher proves the
/// triggering-object and returned-object tags; this adapter only makes that
/// proof visible across the segment boundary.
fn describe_cross_segment_returned_object_set_to_enchantment(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [return_segment, type_segment] = program.segments.as_slice() else {
        return None;
    };
    if !return_segment.self_replacements.is_empty()
        || !type_segment.self_replacements.is_empty()
        || return_segment.starts_new_source_line
    {
        return None;
    }
    let effects = program
        .segments
        .iter()
        .flat_map(|segment| segment.default_effects.iter().cloned())
        .collect::<Vec<_>>();
    describe_returned_object_set_to_enchantment(&effects)
}

/// Preserve the authored action-first surface for a mass exile followed by a
/// one-shot return at the next end step. Lowering keeps the exiled-set tag and
/// delayed trigger semantics intact but places the two source sentences in
/// separate resolution segments; rendering those segments independently
/// changes both the order and the set reference.
fn describe_cross_segment_exile_then_delayed_return(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [exile_segment, delayed_segment] = program.segments.as_slice() else {
        return None;
    };
    if !exile_segment.self_replacements.is_empty()
        || !delayed_segment.self_replacements.is_empty()
        || exile_segment.starts_new_source_line
    {
        return None;
    }
    let exile_effect = match exile_segment.default_effects.as_slice() {
        [exile_effect] => exile_effect,
        [target_effect, exile_effect] => {
            let target = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
            if target.chooser.is_some()
                || target.explicit_declaration
                || !matches!(
                    target.target.unhinted(),
                    ChooseSpec::Target(inner)
                        if matches!(inner.unhinted(), ChooseSpec::Player(PlayerFilter::Any))
                )
            {
                return None;
            }
            exile_effect
        }
        _ => return None,
    };
    let tagged_exile = exile_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    if !crate::cards::is_sentence_helper_tag(tagged_exile.tag.as_str(), "exiled") {
        return None;
    }
    let exile = tagged_exile
        .effect
        .downcast_ref::<crate::effects::ExileEffect>()?;
    if exile.face_down || exile.turn_face_up {
        return None;
    }
    let plural_owners = match exile.spec.unhinted() {
        ChooseSpec::WithCount(inner, count)
            if !count.is_single() && matches!(inner.unhinted(), ChooseSpec::Target(_)) =>
        {
            true
        }
        ChooseSpec::All(filter)
            if filter.controller == Some(PlayerFilter::Target(Box::new(PlayerFilter::Any))) =>
        {
            false
        }
        _ => return None,
    };
    let [delayed_effect] = delayed_segment.default_effects.as_slice() else {
        return None;
    };
    let delayed = delayed_effect.downcast_ref::<crate::effects::ScheduleDelayedTriggerEffect>()?;
    if !delayed.one_shot
        || delayed.start_next_turn
        || delayed.duration != ironsmith_core::DelayedTriggerDuration::Forever
        || delayed.until_end_of_turn
        || delayed.until_end_of_combat
        || delayed.leading_duration_surface
        || delayed.watch_ability_source
        || delayed.watch_all_object_targets
        || delayed.either_of_watched_objects
        || delayed.while_any_tagged_object_in_zone.is_some()
        || !delayed.target_objects.is_empty()
        || delayed.target_tag.is_some()
        || delayed.target_filter.is_some()
        || delayed.controller != PlayerFilter::You
        || delayed.prepayment.is_some()
        || delayed.event_value_from_prior_prevention
    {
        return None;
    }
    let end_step = delayed
        .trigger
        .downcast_ref::<crate::triggers::BeginningOfEndStepTrigger>()?;
    if end_step.player != PlayerFilter::Any {
        return None;
    }
    let [return_segment] = delayed.effects.segments.as_slice() else {
        return None;
    };
    if !return_segment.self_replacements.is_empty() || return_segment.starts_new_source_line {
        return None;
    }
    let [return_effect] = return_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_return = return_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let returned = tagged_return
        .effect
        .downcast_ref::<crate::effects::ReturnAllToBattlefieldEffect>()?;
    if returned.tapped
        || returned.face_down
        || returned.battlefield_controller != ironsmith_core::BattlefieldController::Owner
        || !returned.controller_surface_explicit
        || returned.verb_surface != ironsmith_core::MoveToZoneVerbSurface::Return
    {
        return None;
    }
    let [constraint] = returned.filter.tagged_constraints.as_slice() else {
        return None;
    };
    if constraint.tag != tagged_exile.tag
        || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
    {
        return None;
    }
    let mut return_remainder = returned.filter.clone();
    return_remainder.tagged_constraints.clear();
    if return_remainder
        .zone
        .take()
        .is_some_and(|zone| zone != Zone::Exile)
        || return_remainder != ObjectFilter::default()
    {
        return None;
    }

    let exile_text = describe_effect_list(&exile_segment.default_effects)
        .trim()
        .trim_end_matches('.')
        .to_string();
    let owner = if plural_owners { "owners'" } else { "owner's" };
    Some(format!(
        "{exile_text}. Return the exiled cards to the battlefield under their {owner} control at the beginning of the next end step"
    ))
}

#[cfg(test)]
mod cross_segment_exile_then_delayed_return_tests {
    use super::*;

    #[test]
    fn public_route_preserves_action_first_delayed_return_and_owner_surface() {
        for oracle in [
            "Exile X target artifacts and/or creatures. Return the exiled cards to the battlefield under their owners' control at the beginning of the next end step.",
            "Exile all nonland permanents target player controls. Return the exiled cards to the battlefield under their owner's control at the beginning of the next end step.",
        ] {
            let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
                crate::ids::CardId::new(),
                "Delayed Return Probe",
            )
            .card_types(vec![CardType::Sorcery])
            .parse_text(oracle)
            .expect("mass exile with delayed return should parse");

            assert_eq!(
                crate::compiled_text::compiled_text_lines(&definition),
                vec![oracle.to_string()]
            );
        }
    }
}

fn removed_counter_type_in_effect_scope(
    effect: &Effect,
    effect_id: crate::effect::EffectId,
    matching_scope: bool,
) -> Option<CounterType> {
    if let Some(tagged) = effect.downcast_ref::<crate::effects::TaggedEffect>() {
        return removed_counter_type_in_effect_scope(&tagged.effect, effect_id, matching_scope);
    }
    if let Some(with_id) = effect.downcast_ref::<crate::effects::WithIdEffect>() {
        return removed_counter_type_in_effect_scope(
            &with_id.effect,
            effect_id,
            matching_scope || with_id.id == effect_id,
        );
    }
    if let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>() {
        let mut counter_type = None;
        for member in &sequence.effects {
            if let Some(next) =
                removed_counter_type_in_effect_scope(member, effect_id, matching_scope)
                && counter_type.replace(next).is_some()
            {
                return None;
            }
        }
        return counter_type;
    }
    let remove = effect.downcast_ref::<crate::effects::RemoveCountersEffect>()?;
    matching_scope.then_some(remove.counter_type)
}

/// Reconnect an effect-ID-counted counter removal with a scaled-mana consumer
/// across an authored sentence boundary. Rendering each source-line segment
/// independently hides the producer's counter kind and degrades the second
/// sentence to "Add that much" even though the typed metric retains both.
fn describe_cross_segment_removed_counter_scaled_mana(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [producer_segment, consumer_segment] = program.segments.as_slice() else {
        return None;
    };
    if !producer_segment.self_replacements.is_empty()
        || !consumer_segment.self_replacements.is_empty()
        || producer_segment.starts_new_source_line
    {
        return None;
    }
    let [producer] = producer_segment.default_effects.as_slice() else {
        return None;
    };
    let [consumer] = consumer_segment.default_effects.as_slice() else {
        return None;
    };
    let add = consumer.downcast_ref::<crate::effects::AddScaledManaEffect>()?;
    let Value::PriorEffectMetric { effect_id, query } = add.amount.unhinted() else {
        return None;
    };
    if add.mana.is_empty()
        || !add
            .amount
            .has_surface_hint(ironsmith_core::ValueSurfaceHint::CountersRemovedThisWay)
        || query.source != ironsmith_core::EffectMetricSource::Outcome
        || query.metric != ironsmith_core::EffectMetric::Count
        || query.action != Some(ironsmith_core::PriorEffectAction::Removed)
        || query.filter.is_some()
        || query.player.is_some()
    {
        return None;
    }
    let counter_type = removed_counter_type_in_effect_scope(producer, *effect_id, false)?;
    if query
        .counter_type
        .is_some_and(|typed| typed != counter_type)
    {
        return None;
    }

    let rendered = describe_effect_list(&[producer.clone(), consumer.clone()]);
    rendered
        .contains(&format!(
            "for each {} counter removed this way",
            counter_type.description()
        ))
        .then_some(rendered)
}

/// Rejoin a source-linked phase-out program split at its authored sentence
/// boundary. The first sentence's untap and phase-out share the same complete
/// object set; the second sentence counts exactly the objects affected by the
/// phase-out effect ID before putting counters on the source.
fn describe_cross_segment_untap_phase_out_counter_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [untap_segment, counter_segment] = program.segments.as_slice() else {
        return None;
    };
    if !untap_segment.self_replacements.is_empty()
        || !counter_segment.self_replacements.is_empty()
        || untap_segment.starts_new_source_line
    {
        return None;
    }
    let [untap_effect] = untap_segment.default_effects.as_slice() else {
        return None;
    };
    let [phase_effect, counter_effect] = counter_segment.default_effects.as_slice() else {
        return None;
    };
    let phase_with_id = phase_effect.downcast_ref::<crate::effects::WithIdEffect>()?;
    let phase_out = phase_with_id
        .effect
        .downcast_ref::<crate::effects::PhaseOutEffect>()?;
    let put_counters = counter_effect.downcast_ref::<crate::effects::PutCountersEffect>()?;
    let Value::PriorEffectMetric { effect_id, query } = put_counters.amount.unhinted() else {
        return None;
    };
    let ChooseSpec::All(phased_filter) = phase_out.spec.base() else {
        return None;
    };
    if *effect_id != phase_with_id.id
        || query.source != ironsmith_core::EffectMetricSource::AffectedObjects
        || query.metric != ironsmith_core::EffectMetric::Count
        || query.action != Some(ironsmith_core::PriorEffectAction::PhasedOut)
        || query.filter.as_ref() != Some(phased_filter)
        || query.player.is_some()
        || query.counter_type.is_some()
        || !matches!(put_counters.target.base(), ChooseSpec::Source)
        || put_counters.target_count.is_some()
        || put_counters.distributed
    {
        return None;
    }
    let source = phase_out
        .source_surface
        .as_ref()
        .map(crate::target::SourceReferenceSurface::display_text)?;
    let (phase_text, consumed) = describe_untap_then_phase_out_until_source_leaves(&[
        untap_effect.clone(),
        phase_effect.clone(),
    ])?;
    if consumed != 2 {
        return None;
    }
    let phased_objects = describe_choose_spec(&phase_out.spec);
    let phased_objects = phased_objects
        .strip_prefix("all ")
        .unwrap_or(phased_objects.as_str());
    let phased_object = singularize_simple_noun_phrase(phased_objects);
    let counter = with_indefinite_article(&format!(
        "{} counter",
        describe_counter_type(put_counters.counter_type)
    ));
    Some(format!(
        "{phase_text}. Put {counter} on {source} for each {phased_object} that phased out this way"
    ))
}

#[cfg(test)]
mod cross_segment_removed_counter_scaled_mana_tests {
    use super::*;

    fn removed_counter_mana_program(
        consumer_id: crate::effect::EffectId,
    ) -> crate::resolution::ResolutionProgram {
        let producer_id = crate::effect::EffectId(41);
        let producer = Effect::with_id(
            producer_id.0,
            Effect::new(crate::effects::RemoveCountersEffect::new(
                CounterType::Charge,
                Value::CountersOnSource(CounterType::Charge),
                ChooseSpec::Source,
            )),
        );
        let amount = Value::PriorEffectMetric {
            effect_id: consumer_id,
            query: ironsmith_core::PriorEffectMetricQuery::new(
                ironsmith_core::EffectMetricSource::Outcome,
                ironsmith_core::EffectMetric::Count,
            )
            .with_action(ironsmith_core::PriorEffectAction::Removed),
        }
        .with_surface_hint(ironsmith_core::ValueSurfaceHint::CountersRemovedThisWay);
        let consumer = Effect::new(crate::effects::AddScaledManaEffect::new(
            vec![crate::mana::ManaSymbol::Colorless],
            amount,
            PlayerFilter::You,
        ));
        let producer_segment = crate::resolution::ResolutionSegment::from_effects(vec![producer]);
        let mut consumer_segment =
            crate::resolution::ResolutionSegment::from_effects(vec![consumer]);
        consumer_segment.starts_new_source_line = true;
        crate::resolution::ResolutionProgram::new(vec![producer_segment, consumer_segment])
    }

    #[test]
    fn public_route_preserves_singular_gate_and_counter_kind_correlation() {
        let oracle = "At the beginning of your first main phase, if this artifact has a charge counter on it, tap it and remove all charge counters from it. Add {C} for each charge counter removed this way.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Counter Removal Mana Probe",
        )
        .card_types(vec![CardType::Artifact])
        .parse_text(oracle)
        .expect("counter-removal mana trigger should parse");
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![oracle.to_string()]
        );
    }

    #[test]
    fn scaled_mana_requires_the_exact_counter_removal_effect_id() {
        let matching = removed_counter_mana_program(crate::effect::EffectId(41));
        assert_eq!(
            describe_cross_segment_removed_counter_scaled_mana(&matching).as_deref(),
            Some(
                "Remove all charge counters from it. Add {C} for each charge counter removed this way"
            )
        );

        let mismatched = removed_counter_mana_program(crate::effect::EffectId(42));
        assert_eq!(
            describe_cross_segment_removed_counter_scaled_mana(&mismatched),
            None
        );
    }
}

fn describe_cross_segment_remove_abilities_then_destroy_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [ability_loss_segment, destroy_segment] = program.segments.as_slice() else {
        return None;
    };
    if ability_loss_segment.starts_new_source_line
        || !ability_loss_segment.self_replacements.is_empty()
        || !destroy_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [ability_loss_effect] = ability_loss_segment.default_effects.as_slice() else {
        return None;
    };
    let [destroy_effect] = destroy_segment.default_effects.as_slice() else {
        return None;
    };
    render_remove_abilities_then_destroy_matching_creatures(
        apply_continuous_for_compaction(ability_loss_effect)?,
        downcast_destroy(destroy_effect)?,
    )
}

fn describe_you_life_change_exile_then_play_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [action_segment, permission_segment] = program.segments.as_slice() else {
        return None;
    };
    if action_segment.starts_new_source_line
        || !action_segment.self_replacements.is_empty()
        || !permission_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [sequence_effect] = action_segment.default_effects.as_slice() else {
        return None;
    };
    let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::Coordinated {
        return None;
    }
    let action = describe_you_life_change_and_exile_top(&sequence.effects)?;
    let [_, exile_effect] = sequence.effects.as_slice() else {
        return None;
    };
    let exile = structural_unwrap_render_wrappers(exile_effect)
        .downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()?;

    let [permission_effect] = permission_segment.default_effects.as_slice() else {
        return None;
    };
    let permission = structural_unwrap_render_wrappers(permission_effect)
        .downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
    if !exile.moved_tags.contains(&permission.tag)
        || permission.player != PlayerFilter::You
        || permission.duration != crate::effects::GrantPlayTaggedDuration::ForAsLongAsExiled
        || permission.surface.is_some()
        || !permission.allow_land
        || permission.mana_spend_mode != ironsmith_core::value_model::ManaSpendMode::Normal
        || permission.allow_any_color_for_cast
        || permission.while_on_top_of_library
        || permission.filter.is_some()
        || permission.during_turns_counter_put_on_source.is_some()
        || permission.spell_cost_increase.is_some()
        || permission.lands_enter_tapped
        || permission.cast_pool_is_plural
        || permission.max_plays.is_some()
    {
        return None;
    }

    Some(format!(
        "{action}. You may play that card for as long as it remains exiled"
    ))
}

#[cfg(test)]
mod you_life_change_exile_then_play_program_tests {
    use super::*;

    fn program(permission_tag: &str) -> crate::resolution::ResolutionProgram {
        let moved_tag = TagKey::from("exiled_this_way");
        let lose = Effect::new(crate::effects::LoseLifeEffect::you(2));
        let exile = Effect::new(
            crate::effects::ExileTopOfLibraryEffect::new(1, PlayerFilter::You)
                .with_surface(ironsmith_core::ExileTopLibrarySurface::LibraryOwnerAsActor)
                .tag_moved(moved_tag),
        );
        let action = Effect::new(crate::effects::SequenceEffect::coordinated(vec![
            lose, exile,
        ]));
        let mut permission_segment =
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(
                crate::effects::GrantPlayTaggedEffect::new(
                    TagKey::from(permission_tag),
                    PlayerFilter::You,
                    crate::effects::GrantPlayTaggedDuration::ForAsLongAsExiled,
                    true,
                    ironsmith_core::value_model::ManaSpendMode::Normal,
                ),
            )]);
        permission_segment.starts_new_source_line = true;
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![action]),
            permission_segment,
        ])
    }

    #[test]
    fn linked_permission_rejoins_shared_you_subject_and_rejects_changed_tag() {
        assert_eq!(
            describe_you_life_change_exile_then_play_program(&program("exiled_this_way")),
            Some(
                "You lose 2 life and exile the top card of your library. You may play that card for as long as it remains exiled"
                    .to_string()
            )
        );
        assert!(
            describe_you_life_change_exile_then_play_program(&program("different_tag")).is_none()
        );
    }
}

fn exact_everybody_lives_apply_metadata(
    apply: &crate::effects::ApplyContinuousEffect,
    until: Until,
) -> bool {
    apply.until == until
        && apply.condition.is_none()
        && apply.source_type.is_none()
        && apply.source_reference_surface.is_none()
        && apply.type_retention_surface.is_none()
        && apply.animation_pt_surface.is_none()
        && apply.animation_duration_surface.is_none()
        && !apply.resolve_set_pt_values_at_resolution
        && !apply.require_creature_target
        && apply.additional_modifications.is_empty()
        && apply.runtime_modifications.is_empty()
}

fn exact_everybody_lives_cant(cant: &crate::effects::CantEffect) -> bool {
    cant.duration == Until::EndOfTurn
        && cant.start == crate::effect::RestrictionStart::Immediate
        && cant.duration_surface == Default::default()
}

/// Reconstruct a three-sentence protection/rules program from its typed
/// grants and restrictions. Source-line migration deliberately separates the
/// player grant from the coordinated rule restrictions; matching the complete
/// graph keeps those boundaries without relying on the final rendered text.
fn describe_everybody_lives_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [creature_segment, player_segment, rules_segment] = program.segments.as_slice() else {
        return None;
    };
    if !creature_segment.self_replacements.is_empty()
        || !player_segment.self_replacements.is_empty()
        || !rules_segment.self_replacements.is_empty()
        || creature_segment.starts_new_source_line
        || !player_segment.starts_new_source_line
        || !rules_segment.starts_new_source_line
    {
        return None;
    }

    let [creature_sequence_effect] = creature_segment.default_effects.as_slice() else {
        return None;
    };
    let creature_sequence =
        creature_sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    let [hexproof_effect, indestructible_effect] = creature_sequence.effects.as_slice() else {
        return None;
    };
    if creature_sequence.surface != ironsmith_core::SequenceSurface::Coordinated {
        return None;
    }
    let hexproof_tagged = hexproof_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let indestructible_tagged =
        indestructible_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let hexproof = hexproof_tagged
        .effect
        .downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let indestructible = indestructible_tagged
        .effect
        .downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let Some(crate::continuous::Modification::AddAbility(hexproof_ability)) =
        hexproof.modification.as_ref()
    else {
        return None;
    };
    let Some(crate::continuous::Modification::AddAbility(indestructible_ability)) =
        indestructible.modification.as_ref()
    else {
        return None;
    };
    if hexproof.target != crate::continuous::EffectTarget::Filter(ObjectFilter::creature())
        || hexproof.target_spec.is_some()
        || hexproof_ability.id() != crate::static_abilities::StaticAbilityId::Hexproof
        || hexproof.set_quantifier_surface != Some(ironsmith_core::SetQuantifierSurface::All)
        || !hexproof.lock_filter_at_resolution
        || !exact_everybody_lives_apply_metadata(hexproof, Until::EndOfTurn)
        || indestructible.target != crate::continuous::EffectTarget::Source
        || !matches!(
            &indestructible.target_spec,
            Some(ChooseSpec::Tagged(tag)) if tag == &hexproof_tagged.tag
        )
        || indestructible_ability.id() != crate::static_abilities::StaticAbilityId::Indestructible
        || indestructible.set_quantifier_surface.is_some()
        || indestructible.lock_filter_at_resolution
        || !exact_everybody_lives_apply_metadata(indestructible, Until::EndOfTurn)
        || indestructible_tagged.tag == hexproof_tagged.tag
    {
        return None;
    }

    let [player_effect] = player_segment.default_effects.as_slice() else {
        return None;
    };
    let player_cant = player_effect.downcast_ref::<crate::effects::CantEffect>()?;
    let crate::effect::Restriction::BeTargetedPlayerFrom(player, source_filter) =
        &player_cant.restriction
    else {
        return None;
    };
    if *player != PlayerFilter::Any
        || source_filter != &ObjectFilter::default().controlled_by(PlayerFilter::Opponent)
        || !exact_everybody_lives_cant(player_cant)
    {
        return None;
    }

    let [rules_sequence_effect] = rules_segment.default_effects.as_slice() else {
        return None;
    };
    let rules_sequence = rules_sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    let [lose_life_effect, lose_game_effect, win_game_effect] = rules_sequence.effects.as_slice()
    else {
        return None;
    };
    if rules_sequence.surface != ironsmith_core::SequenceSurface::Coordinated {
        return None;
    }
    let lose_life = lose_life_effect.downcast_ref::<crate::effects::CantEffect>()?;
    let lose_game = lose_game_effect.downcast_ref::<crate::effects::CantEffect>()?;
    let win_game = win_game_effect.downcast_ref::<crate::effects::CantEffect>()?;
    if lose_life.restriction != crate::effect::Restriction::LoseLife(PlayerFilter::Any)
        || lose_game.restriction != crate::effect::Restriction::LoseGame(PlayerFilter::Any)
        || win_game.restriction != crate::effect::Restriction::WinGame(PlayerFilter::Any)
        || !exact_everybody_lives_cant(lose_life)
        || !exact_everybody_lives_cant(lose_game)
        || !exact_everybody_lives_cant(win_game)
    {
        return None;
    }

    Some(
        "All creatures gain hexproof and indestructible until end of turn. Players gain hexproof until end of turn. Players can't lose life this turn and players can't lose the game or win the game this turn"
            .to_string(),
    )
}

#[cfg(test)]
mod everybody_lives_program_tests {
    use super::*;

    fn build_program(lose_life_duration: Until) -> crate::resolution::ResolutionProgram {
        let creature_tag = TagKey::from("creature_hexproof_group");
        let hexproof = crate::effects::ApplyContinuousEffect::new(
            crate::continuous::EffectTarget::Filter(ObjectFilter::creature()),
            crate::continuous::Modification::AddAbility(
                crate::static_abilities::StaticAbility::hexproof(),
            ),
            Until::EndOfTurn,
        )
        .with_set_quantifier_surface(Some(ironsmith_core::SetQuantifierSurface::All))
        .lock_filter_at_resolution();
        let mut indestructible = crate::effects::ApplyContinuousEffect::new(
            crate::continuous::EffectTarget::Source,
            crate::continuous::Modification::AddAbility(
                crate::static_abilities::StaticAbility::indestructible(),
            ),
            Until::EndOfTurn,
        );
        indestructible.target_spec = Some(ChooseSpec::Tagged(creature_tag.clone()));
        let creature_sequence = Effect::new(crate::effects::SequenceEffect::coordinated(vec![
            Effect::new(crate::effects::TaggedEffect::new(
                creature_tag,
                Effect::new(hexproof),
            )),
            Effect::new(crate::effects::TaggedEffect::new(
                "creature_indestructible_group",
                Effect::new(indestructible),
            )),
        ]));

        let player_hexproof = Effect::new(crate::effects::CantEffect::until_end_of_turn(
            crate::effect::Restriction::BeTargetedPlayerFrom(
                PlayerFilter::Any,
                ObjectFilter::default().controlled_by(PlayerFilter::Opponent),
            ),
        ));
        let rules_sequence = Effect::new(crate::effects::SequenceEffect::coordinated(vec![
            Effect::new(crate::effects::CantEffect::new(
                crate::effect::Restriction::LoseLife(PlayerFilter::Any),
                lose_life_duration,
            )),
            Effect::new(crate::effects::CantEffect::until_end_of_turn(
                crate::effect::Restriction::LoseGame(PlayerFilter::Any),
            )),
            Effect::new(crate::effects::CantEffect::until_end_of_turn(
                crate::effect::Restriction::WinGame(PlayerFilter::Any),
            )),
        ]));

        let creature_segment =
            crate::resolution::ResolutionSegment::from_effects(vec![creature_sequence]);
        let mut player_segment =
            crate::resolution::ResolutionSegment::from_effects(vec![player_hexproof]);
        player_segment.starts_new_source_line = true;
        let mut rules_segment =
            crate::resolution::ResolutionSegment::from_effects(vec![rules_sequence]);
        rules_segment.starts_new_source_line = true;
        crate::resolution::ResolutionProgram::new(vec![
            creature_segment,
            player_segment,
            rules_segment,
        ])
    }

    #[test]
    fn exact_protection_and_player_rule_program_preserves_three_sentences() {
        let expected = "All creatures gain hexproof and indestructible until end of turn. Players gain hexproof until end of turn. Players can't lose life this turn and players can't lose the game or win the game this turn";
        let program = build_program(Until::EndOfTurn);
        assert_eq!(
            describe_everybody_lives_program(&program).as_deref(),
            Some(expected)
        );
        assert_eq!(
            describe_resolution_program_preserving_source_lines(&program),
            expected
        );

        let changed_duration = build_program(Until::Forever);
        assert!(describe_everybody_lives_program(&changed_duration).is_none());
    }
}

/// A target restated in a later reciprocal-damage sentence is an antecedent,
/// even when the first damage instruction is guarded by a condition.
fn describe_conditional_reciprocal_damage_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [first, second] = program.segments.as_slice() else {
        return None;
    };
    if !first.self_replacements.is_empty() || !second.self_replacements.is_empty() {
        return None;
    }
    let [conditional] = first.default_effects.as_slice() else {
        return None;
    };
    let conditional = conditional.downcast_ref::<crate::effects::ConditionalEffect>()?;
    if !conditional.if_false.is_empty() {
        return None;
    }
    let [first_declaration, first_damage] = conditional.if_true.as_slice() else {
        return None;
    };
    let first_declaration = first_declaration.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    let [capture, declaration, second_damage] = second.default_effects.as_slice() else {
        return None;
    };
    let tagged = declaration.downcast_ref::<crate::effects::TaggedEffect>()?;
    let second_declaration = tagged
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if first_declaration != second_declaration {
        return None;
    }
    // The common damage helper proves source/recipient identity, power and
    // capture ordering. It is used here as a structural validation only.
    describe_reciprocal_power_damage(&[
        first_damage.clone(),
        capture.clone(),
        declaration.clone(),
        second_damage.clone(),
    ])?;
    let subject = describe_choose_spec(&first_declaration.target);
    let second_text = describe_effect_list(&second.default_effects);
    let tail = second_text
        .strip_prefix(&capitalize_first(&subject))
        .or_else(|| second_text.strip_prefix(&subject))?;
    Some(format!(
        "{}. That creature{tail}",
        describe_effect_list(&first.default_effects).trim_end_matches('.')
    ))
}

/// A target-capturing damage tag names the selected object even if damage
/// is prevented. Keep a subsequent type test distinct from an outcome test.
fn describe_damage_target_type_restriction_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [damage_segment, condition_segment, tail @ ..] = program.segments.as_slice() else {
        return None;
    };
    if !damage_segment.self_replacements.is_empty()
        || !condition_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [damage_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged = damage_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    if tagged.outcome_only {
        return None;
    }
    let damage = tagged
        .effect
        .downcast_ref::<crate::effects::DealDamageEffect>()?;
    if damage.target != ChooseSpec::AnyTarget {
        return None;
    }
    let [condition_effect] = condition_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional = condition_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let crate::effect::Condition::TaggedObjectMatches(tag, kind) = &conditional.condition else {
        return None;
    };
    if tag != &tagged.tag
        || !conditional.if_false.is_empty()
        || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
    {
        return None;
    }
    // Only a pure card-type predicate can be phrased as “it's a …”.
    let mut remainder = kind.clone();
    if remainder.card_types.len() != 1 {
        return None;
    }
    remainder.card_types.clear();
    remainder.set_explicit_card_type_noun(None);
    if remainder != ObjectFilter::default() {
        return None;
    }
    let [restriction_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let restriction = restriction_effect.downcast_ref::<crate::effects::CantEffect>()?;
    let crate::effect::Restriction::Block(subject) = &restriction.restriction else {
        return None;
    };
    if subject != &ObjectFilter::tagged(tag.clone()) {
        return None;
    }
    let rendered_restriction = describe_effect(restriction_effect);
    let predicate = rendered_restriction.strip_prefix("permanent ")?;
    let mut rendered = format!(
        "{}. If it's {}, it {}",
        describe_effect(damage_effect).trim_end_matches('.'),
        with_indefinite_article(&kind.description()),
        predicate.trim_end_matches('.')
    );
    if !tail.is_empty() {
        let mut rest = program.clone();
        rest.segments = tail.to_vec();
        rendered.push_str(". ");
        rendered.push_str(&describe_resolution_program(&rest));
    }
    Some(rendered)
}

#[cfg(test)]
mod damage_target_type_restriction_tests {
    use super::*;

    #[test]
    fn damage_target_type_condition_requires_capture_and_shared_identity() {
        let definition = crate::CardDefinitionBuilder::new(crate::ids::CardId::new(), "Damage fixture")
            .card_types(vec![CardType::Sorcery])
            .parse_text("Damage fixture deals 3 damage to any target. If it's a creature, it can't block this turn.").unwrap();
        let program = definition.spell_effect.unwrap();
        assert!(describe_damage_target_type_restriction_program(&program).is_some());
        let mut outcome_program = program.clone();
        let mut tagged = outcome_program.segments[0].default_effects[0]
            .downcast_ref::<crate::effects::TaggedEffect>()
            .unwrap()
            .clone();
        tagged.outcome_only = true;
        outcome_program.segments[0].default_effects[0] = Effect::new(tagged);
        assert!(describe_damage_target_type_restriction_program(&outcome_program).is_none());
        let mut unrelated_program = program.clone();
        let mut conditional = unrelated_program.segments[1].default_effects[0]
            .downcast_ref::<crate::effects::ConditionalEffect>()
            .unwrap()
            .clone();
        let crate::effect::Condition::TaggedObjectMatches(tag, _) = &mut conditional.condition
        else {
            panic!("type condition");
        };
        *tag = TagKey::from("unrelated");
        unrelated_program.segments[1].default_effects[0] = Effect::new(conditional);
        assert!(describe_damage_target_type_restriction_program(&unrelated_program).is_none());
    }
}

pub(super) fn describe_resolution_program(
    program: &crate::resolution::ResolutionProgram,
) -> String {
    if let Some(rendered) = describe_damage_target_type_restriction_program(program) {
        return rendered;
    }
    // Preserve the shared group across source-sentence resolution segments.
    if program
        .segments
        .iter()
        .all(|segment| segment.self_replacements.is_empty())
        && let Some(rendered) =
            super::render_effects::describe_group_pump_then_conditional_extra_bonus(
                program.flattened_default_effects(),
            )
    {
        return rendered;
    }
    if let [segment] = program.segments.as_slice()
        && segment.self_replacements.is_empty()
        && let Some(rendered) =
            crate::compiled_text::render_effects::describe_player_damage_then_same_player_discards(
                &segment.default_effects.iter().collect::<Vec<_>>(),
            )
        && rendered.contains(", then sacrifices ")
    {
        return rendered;
    }
    if let Some(rendered) = describe_conditional_reciprocal_damage_program(program) {
        return rendered;
    }
    if let [segment] = program.segments.as_slice()
        && segment.self_replacements.is_empty()
        && let Some(rendered) =
            describe_quantified_tap_goad_then_watch_set(&segment.default_effects)
    {
        // The clause-list renderer normally owns a coordinated single
        // segment before the generic effect-list renderer. Preserve this
        // exact tagged three-sentence program at the resolution boundary so
        // the selected per-opponent set and its delayed watcher stay linked.
        return rendered;
    }
    if program
        .segments
        .iter()
        .all(|segment| segment.self_replacements.is_empty())
        && let Some(rendered) =
            describe_linked_resolution_program(program.flattened_default_effects())
    {
        return rendered;
    }

    if program
        .segments
        .iter()
        .all(|segment| segment.self_replacements.is_empty())
    {
        let refs = program
            .flattened_default_effects()
            .iter()
            .collect::<Vec<_>>();
        if let Some(rendered) = describe_choose_copy_spell_and_retarget_copy_to_chosen(&refs) {
            return rendered;
        }
    }

    if program
        .segments
        .iter()
        .all(|segment| segment.self_replacements.is_empty())
        && let Some(rendered) =
            describe_opponent_top_exile_and_play(program.flattened_default_effects())
    {
        return rendered;
    }
    if program
        .segments
        .iter()
        .all(|segment| segment.self_replacements.is_empty())
        && let Some(rendered) =
            describe_hand_choice_exile_permission(program.flattened_default_effects())
    {
        return rendered;
    }

    if let Some(rendered) =
        describe_player_or_planeswalker_damage_then_controller_discard_program(program)
    {
        return rendered;
    }
    if let Some(rendered) = describe_everybody_lives_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_remove_abilities_then_destroy_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_you_life_change_exile_then_play_program(program) {
        return rendered;
    }
    if let [segment] = program.segments.as_slice()
        && segment.self_replacements.is_empty()
        && let [effect] = segment.default_effects.as_slice()
        && let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>()
        && sequence.surface == ironsmith_core::SequenceSurface::Coordinated
        && let Some(rendered) = describe_you_life_change_and_exile_top(&sequence.effects)
    {
        return rendered;
    }
    if let [segment] = program.segments.as_slice()
        && segment.self_replacements.is_empty()
        && let Some(rendered) =
            describe_optional_looked_entry_with_counter_and_remainder(&segment.default_effects)
    {
        // The broad looked-partition program renderer deliberately ignores
        // battlefield-entry modifiers. Claim this stricter typed shape first
        // so its selected-card condition and entry-time counters remain
        // visible instead of collapsing to a bare "put up to one" action.
        return rendered;
    }
    if let Some(rendered) = describe_single_quantified_unless_payment_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_exile_creatures_then_per_player_fractal_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_returned_object_set_to_enchantment(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_exile_then_delayed_return(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_removed_counter_scaled_mana(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_untap_phase_out_counter_program(program) {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_each_player_choose_creature_destroy_complement_window(
            &program.segments,
            0,
        )
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let [gain_segment, shuffle_segment] = program.segments.as_slice()
        && gain_segment.self_replacements.is_empty()
        && shuffle_segment.self_replacements.is_empty()
        && !gain_segment.starts_new_source_line
        && shuffle_segment.starts_new_source_line
        && gain_segment.default_effects.len() == 1
        && shuffle_segment.default_effects.len() == 3
    {
        let effects = program
            .segments
            .iter()
            .flat_map(|segment| segment.default_effects.iter().cloned())
            .collect::<Vec<_>>();
        if let Some(rendered) = describe_gain_life_shuffle_source_and_graveyard(&effects) {
            return rendered;
        }
    }
    if let [return_segment, aura_segment] = program.segments.as_slice()
        && return_segment.self_replacements.is_empty()
        && aura_segment.self_replacements.is_empty()
        && !return_segment.starts_new_source_line
    {
        let effects = program
            .segments
            .iter()
            .flat_map(|segment| segment.default_effects.iter().cloned())
            .collect::<Vec<_>>();
        if let Some(rendered) = describe_return_as_aura_with_granted_abilities(&effects) {
            // The return and the permanent-type/Aura conversion execute in
            // separate segments after migration. Rejoin only the strict
            // triggering-object/tagged-return bundle so the executable
            // attachment filter and granted abilities retain their authored
            // one-sentence Aura surface.
            return rendered;
        }
    }
    if let Some(rendered) = describe_reveal_may_put_grant_then_bottom_program(program) {
        return rendered;
    }
    if let [segment] = program.segments.as_slice()
        && segment.self_replacements.is_empty()
        && let [effect] = segment.default_effects.as_slice()
        && let Some(for_players) = effect.downcast_ref::<crate::effects::ForPlayersEffect>()
        && let Some(rendered) = describe_for_players_choose_graveyard_then_exile_rest(for_players)
    {
        // Pre-clause rendering owns activated resolution programs before the
        // generic effect-list fallback. Preserve this exact quantified
        // choice/complement pair at the program boundary.
        return rendered;
    }
    if let [segment] = program.segments.as_slice()
        && segment.self_replacements.is_empty()
        && let [triggering_effect, for_players_effect] = segment.default_effects.as_slice()
        && triggering_effect
            .downcast_ref::<crate::effects::TagTriggeringObjectEffect>()
            .is_some()
        && let Some(for_players) =
            for_players_effect.downcast_ref::<crate::effects::ForPlayersEffect>()
        && let Some(rendered) = describe_for_players_choose_then_exile(for_players)
    {
        // The trigger tag is a hidden reference prelude. Claim this exact
        // quantified choose/exile pair before clause-list rendering, whose
        // actor-aware fallback otherwise rewrites the authored conjunction as
        // comma-then.
        return rendered;
    }
    if let Some(rendered) = describe_fervent_alternative_cost_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_chosen_type_additional_cost_search_destination(program) {
        return rendered;
    }
    if let Some(rendered) = describe_each_opponent_sacrifice_discard_then_return_draw(program) {
        return rendered;
    }
    if let Some(rendered) = describe_alternative_cost_chosen_opponent_treasure_scry(program) {
        return rendered;
    }
    if let Some(rendered) = describe_prior_exile_until_next_turn_permission_program(program) {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_created_token_reciprocal_leave_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_delegated_search_partition_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_guided_library_category_choice_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_player_choice_characteristics_program(program) {
        return rendered;
    }
    if let Some(rendered) =
        describe_cross_segment_consult_battlefield_remainder_graveyard_program(program)
    {
        return rendered;
    }
    if let Some(rendered) = describe_mixed_target_exile_top_damage_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_damage_result_tap_and_player_cast_lock_program(program) {
        return rendered;
    }
    // Target declarations can share one authored sentence while the random
    // destruction that consumes their collection starts a later resolution
    // segment. Rejoin only the exact collection-tag relationship.
    if program
        .segments
        .iter()
        .all(|segment| segment.self_replacements.is_empty())
    {
        let effects = program
            .flattened_default_effects()
            .iter()
            .collect::<Vec<_>>();
        if let Some(rendered) = describe_target_groups_then_random_destroy(&effects) {
            return rendered;
        }
    }
    if let [target_segment, apply_segment] = program.segments.as_slice()
        && target_segment.self_replacements.is_empty()
        && apply_segment.self_replacements.is_empty()
        && let [target_effect] = target_segment.default_effects.as_slice()
        && let Some(tagged_target) = target_effect.downcast_ref::<crate::effects::TaggedEffect>()
        && let Some(target) = tagged_target
            .effect
            .downcast_ref::<crate::effects::TargetOnlyEffect>()
        && target.target.is_target()
        && let [apply_effect] = apply_segment.default_effects.as_slice()
        && let Some(apply) = apply_effect.downcast_ref::<crate::effects::ApplyContinuousEffect>()
        && matches!(&apply.target_spec, Some(ChooseSpec::Tagged(tag)) if tag == &tagged_target.tag)
        && apply.until == Until::EndOfTurn
        && apply.condition.is_none()
        && apply.additional_modifications.is_empty()
        && apply.runtime_modifications.is_empty()
        && let Some(crate::continuous::Modification::SetPowerToughness {
            power: Value::PowerOf(power_spec),
            toughness: Value::ToughnessOf(toughness_spec),
            sublayer: crate::continuous::PtSublayer::Setting,
        }) = apply.modification.as_ref()
        && matches!(power_spec.base(), ChooseSpec::Source)
        && matches!(toughness_spec.base(), ChooseSpec::Source)
    {
        return format!(
            "Choose {}. Its base power and toughness become equal to this source's power and toughness until end of turn",
            describe_choose_spec(&target.target)
        );
    }
    if let Some(rendered) = describe_declared_linked_same_source_damage_program(program) {
        return rendered;
    }
    if let Some(rendered) =
        describe_shared_target_player_two_sacrifices_then_damage_program(program)
    {
        return rendered;
    }
    if let Some(rendered) =
        describe_optional_target_cant_block_while_control_source_then_ring(program)
    {
        return rendered;
    }
    if let Some(rendered) =
        describe_return_chosen_set_then_draw_for_opponents_controlling_it(program)
    {
        return rendered;
    }
    if let Some(rendered) = describe_shared_target_conditional_counter_arms_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_counter_removal_empty_result_otherwise_mana_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_amass_mill_then_optional_capped_cast_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_conditional_draw_then_failed_draw_followup_program(program) {
        return rendered;
    }
    if let Some(rendered) =
        describe_choose_another_creature_or_artifact_then_delayed_return_program(program)
    {
        return rendered;
    }
    if let Some(rendered) = describe_target_creature_delayed_owner_graveyard_return_program(program)
    {
        return rendered;
    }
    if let Some(rendered) = describe_chosen_creatures_sacrifice_then_return_tapped_program(program)
    {
        return rendered;
    }
    if let Some(rendered) = describe_each_creature_except_controlled_flying_damage_program(program)
    {
        return rendered;
    }
    if let Some(rendered) = describe_looked_hand_optional_free_cast_program(program) {
        return rendered;
    }
    if let Some(rendered) =
        describe_prevent_next_damage_then_delayed_target_counters_program(program)
    {
        return rendered;
    }
    // The revealed-hand pair can remain one flat segment when the registered
    // sequence rule consumes both authored sentences before source-boundary
    // reconstruction. Claim that exact typed pair here as well as in the
    // cross-segment window below; otherwise the generic list renderer loses
    // the shared hand/aliased-player antecedent and renders the union arms as
    // unrelated battlefield permanents.
    if let [segment] = program.segments.as_slice()
        && segment.self_replacements.is_empty()
        && let [look_effect, draw_effect] = segment.default_effects.as_slice()
        && let draw_effect = structural_unwrap_render_wrappers(draw_effect)
        && let Some(rendered) =
            describe_revealed_hand_then_draw_shared_terminal_union(look_effect, draw_effect)
    {
        return rendered;
    }
    // This exact search procedure is split at authored sentence boundaries.
    // Claim it before broad per-segment rendering loses the sacrificed-object
    // value provenance and the shared searched/exiled-card identity.
    if let Some(rendered) = describe_cross_segment_search_exile_shuffle_may_cast_program(program) {
        return rendered;
    }
    // The exiled card, conditional free-cast attempt, and failed-attempt
    // fallback span three source sentences. Claim their exact typed identity
    // chain before broad per-segment rendering can substitute the intervening
    // Treasure or reduce the result branch to a generic `otherwise` clause.
    if let Some(rendered) = describe_exile_top_treasure_conditional_cast_fallback_program(program) {
        return rendered;
    }
    // This exact two-segment copy/fallback program is also recognizable as a
    // broad conditional plus a generic result branch. Claim it before those
    // program-level compactors so the shared triggering-object tag and result
    // id retain the authored "copy that spell ... If you don't copy" surface.
    if let Some((rendered, consumed)) =
        describe_cross_segment_conditional_triggering_spell_copy_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    // A single correlated damage sequence may be claimed by the broad
    // program-level clause renderer before `describe_effect` reaches the
    // sequence compactor. Preserve the shared target tag and equal amount at
    // this boundary so the second recipient set does not repeat that amount.
    if let [segment] = program.segments.as_slice()
        && segment.self_replacements.is_empty()
        && let [effect] = segment.default_effects.as_slice()
        && let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>()
        && sequence.surface == ironsmith_core::SequenceSurface::Coordinated
        && let [first, second] = sequence.effects.as_slice()
        && let Some(rendered) = describe_target_creature_damage_fanout_pair(first, second)
    {
        return rendered;
    }
    // Historical block references are one correlated executable program. Run
    // its exact structural renderer before the broad single-segment clause
    // compactors can independently consume the target, destroy set, and
    // controller-at-last-block follow-up and thereby lose their authored
    // relationship surface.
    if let [segment] = program.segments.as_slice()
        && segment.self_replacements.is_empty()
        && let Some(rendered) = describe_historical_block_reanimation(&segment.default_effects)
    {
        return rendered;
    }
    if let Some(rendered) = describe_put_counter_then_counter_removal_damage_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_each_player_coin_face_result_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_per_opponent_choose_aggregate_copy_program(program) {
        return rendered;
    }
    if let [segment] = program.segments.as_slice()
        && segment.self_replacements.is_empty()
    {
        let effects = segment.default_effects.iter().collect::<Vec<_>>();
        if let Some(rendered) = describe_energy_then_pay_any_then_destroy(&effects) {
            return rendered;
        }
    }
    if let Some(rendered) = describe_target_player_life_loss_then_gain_and_create_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_prior_result_self_replacement_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_damaged_player_gain_control_then_rewards_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_explicit_target_then_coin_flip_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_choose_sacrifice_reflexive_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_participant_choice_complement_phase_out_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_graveyard_exception_same_name_exile_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_spell_mastery_reanimation_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_search_partition_count_self_replacement_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_group_pump_then_conditional_untap_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_madness_target_threshold_replacement(program) {
        return rendered;
    }
    if let Some(rendered) = describe_repeated_die_parity_result_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_roll_die_then_draw_equal_result_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_sequenced_d20_numeric_result_table_program(program) {
        return rendered;
    }
    if let Some(rendered) =
        describe_roll_result_damage_then_random_source_attachment_program(program)
    {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_conditional_fight_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_two_target_creature_fight_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_chosen_creatures_blessing_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_relative_player_target_consult_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_mixed_target_consult_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_damage_and_die_replacement_program(program) {
        return rendered;
    }
    if let [segment] = program.segments.as_slice()
        && segment.self_replacements.is_empty()
        && let Some(rendered) =
            describe_nested_search_for_each_conditional_shuffle(&segment.default_effects)
    {
        return rendered;
    }

    let mut rendered_segments = Vec::new();
    let mut skipped_segments = 0usize;
    for (segment_index, segment) in program.segments.iter().enumerate() {
        if skipped_segments > 0 {
            skipped_segments -= 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_failed_sequence_draw_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_counter_then_keyword_grants_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_gain_control_aura_legal_attach_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_delegated_collection_partition_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_reveal_optional_cast_decline_bottom_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_self_power_then_controller_damage_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_count_damage_then_count_life_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_sacrificed_power_damage_life_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) = describe_cross_segment_attach_result_power_damage_window(
            &program.segments,
            segment_index,
        ) {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_choose_mount_vehicle_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_return_then_linked_enter_return_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_lowest_life_tie_control_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_conditional_triggering_spell_copy_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_distinct_player_choice_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_bottom_library_exile_look_cast_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_optional_sticker_aura_return_attach_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_target_player_choose_half_return_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_attached_tap_equipment_unattach_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_goaded_set_block_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        // Preserve the free-cast payment rider before the broader linked-play
        // window consumes the same exile/grant pair independently.
        if let Some((rendered, consumed)) =
            describe_cross_segment_shuffle_exile_top_free_play_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_treasure_look_exile_permission_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_filtered_exile_cast_then_has_ability_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_filtered_exile_cast_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_linked_exile_top_play_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_may_search_conditional_else_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_countered_spell_may_put_from_hand_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_countered_set_followup_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_same_name_extraction_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            super::render_effects::describe_single_counter_target_followup_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            super::render_effects::describe_single_damage_target_followup_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) = describe_cross_segment_linked_target_set_followup_window(
            &program.segments,
            segment_index,
        ) {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_result_producer_for_each_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_countered_spell_exile_with_counters_gain_suspend_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_put_counters_then_gain_suspend_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        // Exact linked-choice bundles must run before broader two-segment
        // windows consume their opening choices independently.
        if let Some((rendered, consumed)) =
            describe_cross_segment_linked_graveyard_choices_then_may_return_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_choose_exiled_cards_exile_library_put_chosen_on_top_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_source_exiled_graveyard_token_sacrifice_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_search_reveal_opponent_choose_rest_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) = describe_cross_segment_turn_start_hand_conditions_window(
            &program.segments,
            segment_index,
        ) {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_choose_pay_untap_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_search_reveal_may_move_else_hand_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_declined_may_mill_damage_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) = describe_cross_segment_target_players_investigate_window(
            &program.segments,
            segment_index,
        ) {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) = describe_cross_segment_revealed_hand_union_count_window(
            &program.segments,
            segment_index,
        ) {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_revealed_hand_optional_free_cast_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_reveal_hand_choose_graveyard_or_hand_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_fixed_and_chosen_counter_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_choose_color_reveal_discard_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_may_reveal_hand_choose_action_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_look_hand_choose_action_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_reveal_to_hand_lose_mana_value_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_reveal_hand_choose_discard_adventure_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_exile_then_free_cast_while_exiled_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_graveyard_exile_copy_cast_result_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) = describe_cross_segment_graveyard_exile_copy_cast_window(
            &program.segments,
            segment_index,
        ) {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_tagged_copy_retarget_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_random_source_exiled_owner_hand_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_choice_move_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) = describe_cross_segment_discard_hand_add_mana_draw_window(
            &program.segments,
            segment_index,
        ) {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) = describe_cross_segment_roll_choose_destroy_create_window(
            &program.segments,
            segment_index,
        ) {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_secret_choice_match_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) = describe_cross_segment_player_damage_then_discard_window(
            &program.segments,
            segment_index,
        ) {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_draw_then_discard_unless_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_return_animation_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_color_subtype_addition_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_necromentia_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_named_vote_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_named_search_conditional_disposition_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_may_search_conditional_disposition_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_same_name_search_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_target_player_draw_exile_copy_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_may_look_top_rest_bottom_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_choose_color_mana_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_looked_cards_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_choose_two_tap_unattach_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_bulk_return_decayed_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_joint_discard_or_sacrifice_then_draw_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_opponent_choice_returns_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if segment.self_replacements.is_empty()
            && let Some(rendered) = describe_face_down_pile_then_manifest(&segment.default_effects)
        {
            rendered_segments.push(rendered);
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_choose_exiled_card_free_play_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_tagged_continuous_then_counter_conditional_draw_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_tagged_pump_then_conditional_keyword_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_counter_linked_grant_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_draw_count_grant_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_face_down_pile_manifest_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_kicked_targets_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_color_matched_prevention_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_target_creature_damage_then_destroy_attached_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_target_only_damage_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_destroy_then_grant_blocked_by_that_creature_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_target_then_delayed_destroy_blocked_by_that_creature_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_two_target_characteristic_damage_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_reciprocal_characteristic_damage_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_power_damage_exchange_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_quantified_choice_collection_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_chooser_target_exchange_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_source_owner_consult_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_created_token_copy_retarget_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_each_player_token_characteristic_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_quantified_choice_exile_until_source_leaves_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_created_token_reciprocal_leave_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_created_token_followup_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_exile_top_put_from_among_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_exile_top_choose_play_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_reciprocal_library_inspection_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_target_player_exile_hand_delayed_return_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_shuffle_reveal_top_free_play_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_shuffle_exile_top_cast_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_observation_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_search_face_down_bargain_cast_else_hand_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_reveal_hand_choose_graveyard_exile_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_consult_hand_remainder_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_consult_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_wrapped_search_two_split_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_search_move_shuffle_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_locked_set_tap_untap_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_tagged_tap_untap_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_conditional_tagged_tap_untap_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_typed_controller_damage_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_chain_copy_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_source_exiled_copy_cast_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_look_optional_payment_disposition_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_choose_name_mill_conditional_window(
                &program.segments,
                segment_index,
            )
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_result_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if let Some((rendered, consumed)) =
            describe_cross_segment_death_replacement_window(&program.segments, segment_index)
        {
            rendered_segments.push(rendered);
            skipped_segments = consumed - 1;
            continue;
        }
        if segment.self_replacements.len() == 1 {
            let rendered = describe_single_self_replacement_segment(segment).unwrap_or_else(|| {
                let branch = &segment.self_replacements[0];
                describe_effect_list(&[Effect::conditional(
                    branch.condition.clone(),
                    branch.replacement_effects.clone(),
                    segment.default_effects.clone(),
                )])
            });
            rendered_segments.push(apply_self_replacement_presentation_label(
                &segment.self_replacements[0],
                rendered,
            ));
            continue;
        }

        if !segment.default_effects.is_empty() {
            if let [look_effect, conditional_effect] = segment.default_effects.as_slice()
                && let Some(look) =
                    look_effect.downcast_ref::<crate::effects::LookAtTopCardsEffect>()
                && let Some(conditional) =
                    conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()
                && let Some(rendered) =
                    describe_nested_look_top_card_matching_hand_else_bottom(look, conditional)
            {
                rendered_segments.push(rendered);
                continue;
            }
            if let [choose_effect, cant_effect] = segment.default_effects.as_slice()
                && let Some(rendered) = describe_split_piles_then_choose_attack_or_block_restriction(
                    choose_effect,
                    cant_effect,
                )
            {
                rendered_segments.push(rendered);
                continue;
            }
            if let Some(rendered) = describe_conjoined_same_source_damage(&segment.default_effects)
            {
                rendered_segments.push(rendered);
                continue;
            }
            if let Some(rendered) =
                describe_target_player_exile_hand_delayed_return(&segment.default_effects)
            {
                rendered_segments.push(rendered);
                continue;
            }
            if let [effect] = segment.default_effects.as_slice()
                && effect
                    .downcast_ref::<crate::effects::ScheduleDelayedTriggerEffect>()
                    .is_some_and(|schedule| schedule.target_tag.is_some())
            {
                rendered_segments.push(describe_effect(effect));
                continue;
            }
            if let [exile_top_effect, grant_play_effect, grant_free_cast_effect] =
                segment.default_effects.as_slice()
                && let Some(exile_top) =
                    exile_top_effect.downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()
                && let Some(grant_play) =
                    grant_play_effect.downcast_ref::<crate::effects::GrantPlayTaggedEffect>()
                && let Some(grant_free_cast) = grant_free_cast_effect
                    .downcast_ref::<crate::effects::GrantTaggedSpellFreeCastUntilEndOfTurnEffect>()
                && let Some(rendered) =
                    describe_exile_top_then_play_without_paying_mana(exile_top, grant_play, grant_free_cast)
            {
                rendered_segments.push(rendered);
                continue;
            }
            if let [create_effect, manifest_effect] = segment.default_effects.as_slice()
                && let Some(create) =
                    create_effect.downcast_ref::<crate::effects::CreateTokenEffect>()
                && let Some(manifest) =
                    manifest_effect.downcast_ref::<crate::effects::ManifestTopCardOfLibraryEffect>()
                && let Some(rendered) =
                    describe_create_token_and_manifest_top_card(create, manifest)
            {
                rendered_segments.push(rendered);
                continue;
            }
            if let Some(rendered) =
                describe_chosen_name_consult_after_top_exile_effects(&segment.default_effects)
            {
                rendered_segments.push(rendered);
                continue;
            }
            if let Some(flattened) = flattened_cross_segment_effects(std::slice::from_ref(segment))
                && let Some(rendered) =
                    describe_reveal_hand_choose_discard_then_random_effects(&flattened)
            {
                rendered_segments.push(rendered);
                continue;
            }
            if let Some(rendered) =
                describe_choose_sacrifice_then_source_damage_effects(&segment.default_effects)
            {
                rendered_segments.push(rendered);
                continue;
            }
            rendered_segments.push(
                describe_pre_clause_structural_effect_list(&segment.default_effects)
                    .or_else(|| {
                        describe_structural_multisentence_effect_list(&segment.default_effects)
                    })
                    .or_else(|| {
                        render_consult_reveal_move_matches_then_bottom(&segment.default_effects)
                    })
                    .or_else(|| {
                        describe_tagged_target_then_conditional_action(&segment.default_effects)
                    })
                    .or_else(|| {
                        describe_effect_clause_list(&segment.default_effects)
                            .map(|text| capitalize_first(&text))
                    })
                    .unwrap_or_else(|| describe_effect_list(&segment.default_effects)),
            );
        }
        for branch in &segment.self_replacements {
            rendered_segments.push(describe_effect_list(&branch.replacement_effects));
        }
    }
    rendered_segments
        .into_iter()
        .map(|segment| {
            segment
                .trim()
                .trim_start_matches(". ")
                .trim_end_matches('.')
                .to_string()
        })
        .filter(|segment| !segment.is_empty())
        .collect::<Vec<_>>()
        .join(". ")
}

#[cfg(test)]
mod cross_segment_source_and_graveyard_shuffle_tests {
    use super::*;

    const LINE: &str = "{2}, {T}: You gain 5 life. Shuffle this artifact and your graveyard into their owner's library.";

    #[test]
    fn exact_authored_boundary_rejoins_life_gain_and_both_shuffle_sets() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Elixir Probe",
        )
        .card_types(vec![CardType::Artifact])
        .parse_text(LINE)
        .expect("source and graveyard shuffle ability should compile");
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![LINE.to_string()]
        );

        let AbilityKind::Activated(activated) = &definition.abilities[0].kind else {
            panic!("expected activated ability");
        };
        let shuffle =
            structural_unwrap_render_wrappers(&activated.effects.segments[1].default_effects[0])
                .downcast_ref::<crate::effects::ShuffleObjectsIntoLibraryEffect>()
                .expect("the combined set must be one shuffle operation");
        assert!(shuffle.owner_library_destination && shuffle.shuffle_subject_library);
        let mut changed = shuffle.clone();
        changed.player = PlayerFilter::Opponent;
        assert_ne!(
            describe_effect(&Effect::new(changed)),
            "Shuffle this permanent and your graveyard into their owner's library",
            "a different shuffle participant must not acquire the controller's surface"
        );
    }
}

fn describe_alternative_cost_chosen_opponent_treasure_scry(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [paid_segment, unpaid_segment] = program.segments.as_slice() else {
        return None;
    };
    if paid_segment.starts_new_source_line
        || !paid_segment.self_replacements.is_empty()
        || !unpaid_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [paid_effect] = paid_segment.default_effects.as_slice() else {
        return None;
    };
    let paid = paid_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::ThisSpellPaidLabel(paid_label) = &paid.condition else {
        return None;
    };
    let crate::cost::OptionalCostKind::AlternativeCast(paid_reference) = &paid_label.kind else {
        return None;
    };
    let mana_cost = paid_reference.mana_cost_text()?;
    if paid_reference.surface() != ironsmith_core::AlternativeCostReferenceSurface::ManaCost
        || paid_label.discriminator.is_some()
        || paid.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !paid.if_false.is_empty()
    {
        return None;
    }
    let [sequence_effect] = paid.if_true.as_slice() else {
        return None;
    };
    let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if !matches!(
        sequence.surface,
        ironsmith_core::SequenceSurface::CommaThen | ironsmith_core::SequenceSurface::Coordinated
    ) || sequence.result_label.is_some()
    {
        return None;
    }
    let [draw_effect, choose_effect, create_effect, scry_effect] = sequence.effects.as_slice()
    else {
        return None;
    };
    let draw = draw_effect.downcast_ref::<crate::effects::DrawCardsEffect>()?;
    let choose = choose_effect.downcast_ref::<crate::effects::ChoosePlayerEffect>()?;
    let (_, create) = tagged_create_token_effect_for_keyword(create_effect)?;
    let scry = scry_effect.downcast_ref::<crate::effects::ScryEffect>()?;
    let chosen_player = PlayerFilter::TaggedPlayer(choose.tag.clone());
    if draw.player != PlayerFilter::You
        || draw.count.unhinted() != &Value::Fixed(3)
        || choose.chooser != PlayerFilter::You
        || choose.filter != PlayerFilter::Opponent
        || !choose.excluded_tags.is_empty()
        || choose.random
        || create.count.unhinted() != &Value::Fixed(2)
        || create.controller != chosen_player
        || create.controller_target.is_some()
        || create.token.card.name != "Treasure"
        || !create.token.card.is_token
        || create.token.card.card_types.as_slice() != [CardType::Artifact]
        || create.token.card.subtypes.as_slice() != [crate::types::Subtype::Treasure]
        || create.enters_tapped
        || create.enters_attacking
        || scry.player != PlayerFilter::TaggedPlayer(choose.tag.clone())
        || scry.count.unhinted() != &Value::Fixed(2)
    {
        return None;
    }

    let [unpaid_effect] = unpaid_segment.default_effects.as_slice() else {
        return None;
    };
    let unpaid = unpaid_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::Not(inner) = &unpaid.condition else {
        return None;
    };
    let Condition::ThisSpellPaidLabel(unpaid_label) = inner.as_ref() else {
        return None;
    };
    let crate::cost::OptionalCostKind::AlternativeCast(unpaid_reference) = &unpaid_label.kind
    else {
        return None;
    };
    let [unpaid_draw_effect] = unpaid.if_true.as_slice() else {
        return None;
    };
    let unpaid_draw = unpaid_draw_effect.downcast_ref::<crate::effects::DrawCardsEffect>()?;
    if unpaid_reference.method_name() != paid_reference.method_name()
        || unpaid_reference.mana_cost_text() != Some(mana_cost)
        || unpaid_reference.surface() != ironsmith_core::AlternativeCostReferenceSurface::ThatCost
        || unpaid_label.discriminator.is_some()
        || unpaid.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !unpaid.if_false.is_empty()
        || unpaid_draw.player != PlayerFilter::You
        || unpaid_draw.count.unhinted() != &Value::X
    {
        return None;
    }

    Some(format!(
        "If the {mana_cost} cost was paid, you draw three cards, then an opponent creates two Treasure tokens and they scry 2. If that cost wasn't paid, you draw X cards"
    ))
}

fn describe_fervent_alternative_cost_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [paid_segment, search_segment] = program.segments.as_slice() else {
        return None;
    };
    if !paid_segment.self_replacements.is_empty() || !search_segment.self_replacements.is_empty() {
        return None;
    }
    let [paid_root] = paid_segment.default_effects.as_slice() else {
        return None;
    };
    let paid = paid_root.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::ThisSpellPaidLabel(label) = &paid.condition else {
        return None;
    };
    let crate::cost::OptionalCostKind::AlternativeCast(reference) = &label.kind else {
        return None;
    };
    if reference.mana_cost_text() != Some("{2}{R}{R}")
        || reference.surface() != ironsmith_core::AlternativeCostReferenceSurface::ManaCost
        || label.discriminator.is_some()
        || paid.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !paid.if_false.is_empty()
    {
        return None;
    }
    let [sequence_root] = paid.if_true.as_slice() else {
        return None;
    };
    let sequence = sequence_root.downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::Coordinated
        || sequence.result_label.is_some()
    {
        return None;
    }
    let [choose_root, discard_root, draw_root] = sequence.effects.as_slice() else {
        return None;
    };
    let choose = choose_root.downcast_ref::<crate::effects::ChoosePlayerEffect>()?;
    let discard_with_id = discard_root.downcast_ref::<crate::effects::WithIdEffect>()?;
    let discard = discard_with_id
        .effect
        .downcast_ref::<crate::effects::DiscardEffect>()?;
    let draw = draw_root.downcast_ref::<crate::effects::DrawCardsEffect>()?;
    let chosen = PlayerFilter::TaggedPlayer(choose.tag.clone());
    if choose.chooser != PlayerFilter::You
        || choose.filter != PlayerFilter::Opponent
        || !choose.excluded_tags.is_empty()
        || choose.random
        || discard.player != chosen
        || discard.count.unhinted() != &Value::Fixed(0)
        || !discard.any_number
        || discard.random
        || discard.card_filter.is_some()
        || draw.player != PlayerFilter::TaggedPlayer(choose.tag.clone())
        || !matches!(
            draw.count.unhinted(),
            Value::EffectMetric { effect_id, .. } if *effect_id == discard_with_id.id
        )
    {
        return None;
    }
    let [search_root] = search_segment.default_effects.as_slice() else {
        return None;
    };
    let search_sequence = search_root.downcast_ref::<crate::effects::SequenceEffect>()?;
    let [search_and_move_root, final_discard_root] = search_sequence.effects.as_slice() else {
        return None;
    };
    let search_and_move = search_and_move_root.downcast_ref::<crate::effects::SequenceEffect>()?;
    let [choose_root, move_loop_root, shuffle_root] = search_and_move.effects.as_slice() else {
        return None;
    };
    let choose_cards = choose_root.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let move_loop = move_loop_root.downcast_ref::<crate::effects::ForEachTaggedEffect>()?;
    let [move_root] = move_loop.effects.as_slice() else {
        return None;
    };
    let move_to_hand = move_root.downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    let shuffle = shuffle_root.downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;
    let final_discard = final_discard_root.downcast_ref::<crate::effects::DiscardEffect>()?;
    if search_sequence.surface != ironsmith_core::SequenceSurface::CommaThen
        || search_and_move.surface != ironsmith_core::SequenceSurface::Sequential
        || choose_cards.chooser != PlayerFilter::You
        || choose_cards.zone != Some(Zone::Library)
        || !choose_cards.additional_zones.is_empty()
        || choose_cards.count.min != 0
        || choose_cards.count.max != Some(3)
        || !choose_cards.is_search
        || choose_cards.reveal
        || move_loop.tag != choose_cards.tag
        || move_loop.controller_at_last_blocked_by.is_some()
        || move_to_hand.zone != Zone::Hand
        || shuffle.player != PlayerFilter::You
        || shuffle.target_spec.is_some()
        || final_discard.player != PlayerFilter::You
        || final_discard.count != Value::Fixed(3)
        || !final_discard.random
        || final_discard.any_number
        || final_discard.card_filter.is_some()
    {
        return None;
    }
    Some(
        "If the {2}{R}{R} cost was paid, an opponent discards any number of cards, then draws that many cards. Search your library for up to three cards, put them into your hand, shuffle, then discard three cards at random"
            .to_string(),
    )
}

fn describe_chosen_type_additional_cost_search_destination(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    if segment.self_replacements.len() != 1 {
        return None;
    }
    let [search_root] = segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_search = search_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let search = tagged_search
        .effect
        .downcast_ref::<crate::effects::SearchLibraryEffect>()?;
    let branch = &segment.self_replacements[0];
    let Condition::ThisSpellPaidLabel(label) = &branch.condition else {
        return None;
    };
    let [rewrite_root] = branch.replacement_effects.as_slice() else {
        return None;
    };
    let rewrite = rewrite_root.downcast_ref::<crate::effects::LocalRewriteEffect>()?;
    let rewritten_search = rewrite
        .effect
        .downcast_ref::<crate::effects::TaggedEffect>()?;
    let [replacement] = rewrite.zone_replacements.as_slice() else {
        return None;
    };
    let ChooseSpec::Object(replacement_filter) = replacement.target.unhinted() else {
        return None;
    };
    if label.kind != crate::cost::OptionalCostKind::Additional
        || label.discriminator.is_some()
        || rewritten_search.tag != tagged_search.tag
        || search.chooser != PlayerFilter::You
        || search.player != PlayerFilter::You
        || search.destination != Zone::Hand
        || !search.reveal
        || search.filter.card_types.as_slice() != [CardType::Creature]
        || !matches!(
            search.filter.mana_value.as_ref(),
            Some(crate::target::Comparison::LessThanOrEqualExpr(value))
                if value.as_ref() == &Value::X
        )
        || replacement.from_zone != Some(Zone::Library)
        || replacement.to_zone != Some(Zone::Hand)
        || replacement.replacement_zone != Zone::Battlefield
        || replacement.mode != crate::effects::ReplacementApplyMode::OneShot
        || replacement.optional
        || !filter_is_chosen_creature_type_search_destination(replacement_filter)
    {
        return None;
    }
    Some(
        "Search your library for a creature card with mana value X or less, reveal it, put it into your hand, then shuffle. If this spell's additional cost was paid and the revealed card is the chosen type, put that card onto the battlefield instead of putting it into your hand"
            .to_string(),
    )
}

fn filter_is_chosen_creature_type_search_destination(filter: &ObjectFilter) -> bool {
    if filter.card_types.as_slice() == [CardType::Creature] && filter.chosen_creature_type {
        return true;
    }
    if filter.any_of.len() != 2 {
        return false;
    }
    let mut zones = filter
        .any_of
        .iter()
        .filter_map(|arm| {
            (arm.card_types.as_slice() == [CardType::Creature] && arm.chosen_creature_type)
                .then_some(arm.zone?)
        })
        .collect::<Vec<_>>();
    zones.sort_by_key(|zone| match zone {
        Zone::Library => 0,
        Zone::Hand => 1,
        _ => 2,
    });
    zones == [Zone::Library, Zone::Hand]
}

fn describe_each_opponent_sacrifice_discard_then_return_draw(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [opponents_segment, controller_segment] = program.segments.as_slice() else {
        return None;
    };
    if !opponents_segment.self_replacements.is_empty()
        || !controller_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [for_players_effect] = opponents_segment.default_effects.as_slice() else {
        return None;
    };
    let for_players = for_players_effect.downcast_ref::<crate::effects::ForPlayersEffect>()?;
    if for_players.filter != PlayerFilter::Opponent
        || for_players.starting_with_controller
        || for_players.stop_after_first_happened
    {
        return None;
    }
    let [sequence_effect] = for_players.effects.as_slice() else {
        return None;
    };
    let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if !matches!(
        sequence.surface,
        ironsmith_core::SequenceSurface::RepeatedCommaThen
            | ironsmith_core::SequenceSurface::CommaThen
    ) {
        return None;
    }
    let [choose_effect, sacrifice_effect, discard_effect] = sequence.effects.as_slice() else {
        return None;
    };
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let sacrifice = sacrifice_view(sacrifice_effect)?;
    let mut semantic_choice_shell = choose.filter.clone();
    let choice_arms = std::mem::take(&mut semantic_choice_shell.any_of);
    let shared_iterated_controller =
        semantic_choice_shell.controller == Some(PlayerFilter::IteratedPlayer);
    semantic_choice_shell.controller = None;
    semantic_choice_shell.union_surface = Default::default();
    let expected_arms = [
        ObjectFilter::creature()
            .controlled_by(PlayerFilter::IteratedPlayer)
            .in_zone(Zone::Battlefield),
        ObjectFilter::planeswalker()
            .controlled_by(PlayerFilter::IteratedPlayer)
            .in_zone(Zone::Battlefield),
    ];
    let choice_domain_matches = choice_arms.len() == expected_arms.len()
        && expected_arms.iter().all(|expected| {
            choice_arms.iter().any(|arm| {
                let mut semantic_arm = arm.clone();
                semantic_arm.union_surface = Default::default();
                semantic_arm == *expected
            })
        });
    let mut semantic_sacrifice = sacrifice.filter.clone();
    let sacrifice_tags = std::mem::take(&mut semantic_sacrifice.tagged_constraints);
    semantic_sacrifice.union_surface = Default::default();
    if choose.chooser != PlayerFilter::IteratedPlayer
        || choose.count != crate::effect::ChoiceCount::exactly(1)
        || choose.zone.is_some()
        || !choose.additional_zones.is_empty()
        || choose.is_search
        || !shared_iterated_controller
        || !choice_domain_matches
        || sacrifice.player != &PlayerFilter::IteratedPlayer
        || sacrifice.count != &Value::Fixed(1)
        || semantic_sacrifice != ObjectFilter::default()
        || !matches!(
            sacrifice_tags.as_slice(),
            [constraint]
                if constraint.tag == choose.tag
                    && constraint.relation
                        == crate::filter::TaggedOpbjectRelation::IsTaggedObject
        )
    {
        return None;
    }
    let discard = discard_effect.downcast_ref::<crate::effects::DiscardEffect>()?;
    if discard.player != PlayerFilter::IteratedPlayer
        || discard.count != Value::Fixed(1)
        || discard.random
        || discard.any_number
        || discard.card_filter.is_some()
    {
        return None;
    }
    let [controller_effect] = controller_segment.default_effects.as_slice() else {
        return None;
    };
    let controller_sequence = controller_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    let [return_root, draw_root] = controller_sequence.effects.as_slice() else {
        return None;
    };
    let return_root = return_root
        .downcast_ref::<crate::effects::TaggedEffect>()
        .map(|tagged| tagged.effect.as_ref())
        .unwrap_or(return_root);
    let (return_spec, return_actor_surface) = if let Some(return_to_hand) =
        return_root.downcast_ref::<crate::effects::ReturnToHandEffect>()
    {
        (&return_to_hand.spec, return_to_hand.actor_surface.as_ref())
    } else {
        let return_to_hand =
            return_root.downcast_ref::<crate::effects::ReturnFromGraveyardToHandEffect>()?;
        (
            &return_to_hand.target,
            return_to_hand.actor_surface.as_ref(),
        )
    };
    let ChooseSpec::Object(return_filter) = return_spec.unhinted() else {
        return None;
    };
    let draw = draw_root.downcast_ref::<crate::effects::DrawCardsEffect>()?;
    let mut semantic_return = return_filter.clone();
    semantic_return.union_surface = Default::default();
    let mut expected_return = ObjectFilter::default().in_zone(Zone::Graveyard);
    expected_return.owner = Some(PlayerFilter::You);
    expected_return.card_types = vec![CardType::Creature, CardType::Planeswalker];
    if controller_sequence.surface != ironsmith_core::SequenceSurface::CommaThen
        || semantic_return != expected_return
        || return_actor_surface.is_some()
        || draw.player != PlayerFilter::You
        || draw.count != Value::Fixed(1)
    {
        return None;
    }
    Some(
        "Each opponent sacrifices a creature or planeswalker of their choice, then discards a card. You return a creature or planeswalker card from your graveyard to your hand, then draw a card"
            .to_string(),
    )
}

fn describe_graveyard_cast_entry_counter_trigger(ability: &Ability) -> Option<String> {
    if ability.functional_zones.as_slice() != [Zone::Graveyard] {
        return None;
    }
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || triggered
            .effects
            .segments
            .iter()
            .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let cast = triggered
        .trigger
        .downcast_ref::<crate::triggers::SpellCastTrigger>()?;
    let filter = cast.filter.as_ref()?;
    let [tag_effect, move_effect, register_effect] = triggered.effects.flattened_default_effects()
    else {
        return None;
    };
    let tag = tag_effect.downcast_ref::<crate::effects::TagTriggeringObjectEffect>()?;
    let movement = move_to_zone_surface_view(move_effect)?;
    let register = register_effect
        .downcast_ref::<crate::effects::RegisterNextBatchEnterWithCountersEffect>()?;
    let mut expected_filter = filter.clone();
    expected_filter.zone = Some(Zone::Battlefield);
    expected_filter.stack_kind = None;
    expected_filter.has_mana_cost = false;
    if movement.target != ChooseSpec::Source
        || movement.zone != Zone::Exile
        || register.same_stable_id_tag.as_ref() != Some(&tag.tag)
        || register.filter != expected_filter
        || !register
            .count
            .has_surface_hint(ironsmith_core::ValueSurfaceHint::InlineBattlefieldEntryCounter)
        || !register
            .count
            .has_surface_hint(ironsmith_core::ValueSurfaceHint::AdditionalEntryCounter)
    {
        return None;
    }
    let noun = if let [kind] = filter.card_types.as_slice() {
        kind.to_string().to_ascii_lowercase()
    } else {
        "permanent".into()
    };
    let counter = register.counter_type.description();
    let amount = if register.count.unhinted() == &Value::Fixed(1) {
        format!("an additional {counter} counter")
    } else {
        describe_put_counter_phrase(&register.count, register.counter_type).replacen(
            &format!("{counter} counter"),
            &format!("additional {counter} counter"),
            1,
        )
    };
    Some(format!(
        "{}, exile this card from your graveyard. That {noun} enters with {amount} on it",
        triggered.trigger.display()
    ))
}

/// Render two complementary counter-placement predicates against one declared
/// target as the authored creature/planeswalker conditional sentence. The
/// shared tag is executable target identity; accepting a result tag from the
/// first arm here would hide the exact reference-carry bug this surface is
/// meant to expose.
fn describe_shared_target_conditional_counter_arms_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    fn exact_filter(
        filter: &ObjectFilter,
        card_type: CardType,
        target_tag: &crate::tag::TagKey,
    ) -> bool {
        if filter.zone != Some(Zone::Battlefield)
            || filter.card_types.as_slice() != [card_type]
            || !matches!(
                filter.tagged_constraints.as_slice(),
                [constraint]
                    if constraint.tag == *target_tag
                        && constraint.relation
                            == crate::filter::TaggedOpbjectRelation::IsTaggedObject
            )
        {
            return false;
        }
        let mut remainder = filter.clone();
        remainder.zone = None;
        remainder.card_types.clear();
        remainder.tagged_constraints.clear();
        remainder.set_explicit_card_type_noun(None);
        remainder == ObjectFilter::default()
    }

    let [target_segment, counters_segment] = program.segments.as_slice() else {
        return None;
    };
    if !target_segment.self_replacements.is_empty()
        || !counters_segment.self_replacements.is_empty()
        || target_segment.starts_new_source_line
        || counters_segment.starts_new_source_line
    {
        return None;
    }
    let [target_root] = target_segment.default_effects.as_slice() else {
        return None;
    };
    let target_tagged = target_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let target_only = target_tagged
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if target_only.chooser.is_some() || !target_only.explicit_declaration {
        return None;
    }
    let ChooseSpec::Target(target_spec) = target_only.target.unhinted() else {
        return None;
    };
    let ChooseSpec::Object(target_filter) = target_spec.unhinted() else {
        return None;
    };
    let mut target_remainder = target_filter.clone();
    target_remainder.zone = None;
    target_remainder.is_commander = false;
    target_remainder.entered_battlefield_this_turn = false;
    if target_filter.zone != Some(Zone::Battlefield)
        || !target_filter.is_commander
        || !target_filter.entered_battlefield_this_turn
        || target_remainder != ObjectFilter::default()
    {
        return None;
    }

    let [sequence_root] = counters_segment.default_effects.as_slice() else {
        return None;
    };
    let sequence = sequence_root.downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::Coordinated
        || sequence.result_label.is_some()
    {
        return None;
    }
    let [creature_root, planeswalker_root] = sequence.effects.as_slice() else {
        return None;
    };
    let creature_tagged = creature_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let planeswalker_tagged = planeswalker_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let creature_counters = creature_tagged
        .effect
        .downcast_ref::<crate::effects::PutCountersEffect>()?;
    let planeswalker_counters = planeswalker_tagged
        .effect
        .downcast_ref::<crate::effects::PutCountersEffect>()?;
    if creature_counters.counter_type != crate::CounterType::PlusOnePlusOne
        || planeswalker_counters.counter_type != crate::CounterType::Loyalty
        || creature_counters.amount.unhinted() != &Value::Fixed(1)
        || planeswalker_counters.amount.unhinted() != &Value::Fixed(1)
        || creature_counters.target_count.is_some()
        || planeswalker_counters.target_count.is_some()
        || creature_counters.distributed
        || planeswalker_counters.distributed
        || !matches!(
            creature_counters.target.unhinted(),
            ChooseSpec::Object(filter)
                if exact_filter(filter, CardType::Creature, &target_tagged.tag)
        )
        || !matches!(
            planeswalker_counters.target.unhinted(),
            ChooseSpec::Object(filter)
                if exact_filter(filter, CardType::Planeswalker, &target_tagged.tag)
        )
    {
        return None;
    }

    Some(
        "Choose target commander that entered this turn. Put a +1/+1 counter on it if it's a creature and a loyalty counter on it if it's a planeswalker"
            .to_string(),
    )
}

#[cfg(test)]
mod shared_target_conditional_counter_arms_tests {
    use super::*;

    fn program(second_target_tag: &str) -> crate::resolution::ResolutionProgram {
        let target_tag = crate::tag::TagKey::from("declared_target");
        let mut target_filter = ObjectFilter::default()
            .in_zone(Zone::Battlefield)
            .commander();
        target_filter.entered_battlefield_this_turn = true;
        let target = Effect::new(crate::effects::TargetOnlyEffect::explicit(
            ChooseSpec::target(ChooseSpec::Object(target_filter)),
        ))
        .tag(target_tag.clone());

        let counter = |counter_type, card_type, tag: crate::tag::TagKey| {
            let mut filter = ObjectFilter::default().in_zone(Zone::Battlefield);
            filter.card_types = vec![card_type];
            filter.set_explicit_card_type_noun(Some(card_type));
            filter = filter.match_tagged(tag, crate::filter::TaggedOpbjectRelation::IsTaggedObject);
            Effect::new(crate::effects::PutCountersEffect::new(
                counter_type,
                Value::Fixed(1),
                ChooseSpec::Object(filter),
            ))
        };
        let creature = counter(
            crate::CounterType::PlusOnePlusOne,
            CardType::Creature,
            target_tag,
        )
        .tag("creature_result");
        let planeswalker = counter(
            crate::CounterType::Loyalty,
            CardType::Planeswalker,
            crate::tag::TagKey::from(second_target_tag),
        )
        .tag("planeswalker_result");
        let sequence = Effect::new(crate::effects::SequenceEffect {
            effects: vec![creature, planeswalker],
            surface: ironsmith_core::SequenceSurface::Coordinated,
            result_label: None,
        });
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![target]),
            crate::resolution::ResolutionSegment::from_effects(vec![sequence]),
        ])
    }

    #[test]
    fn shared_target_keeps_complementary_creature_planeswalker_conditions() {
        let exact = program("declared_target");
        assert_eq!(
            describe_shared_target_conditional_counter_arms_program(&exact),
            Some("Choose target commander that entered this turn. Put a +1/+1 counter on it if it's a creature and a loyalty counter on it if it's a planeswalker".to_string())
        );

        assert_eq!(
            describe_shared_target_conditional_counter_arms_program(&program("creature_result")),
            None,
            "the second arm must not silently follow the first counter result"
        );
    }
}

/// Rejoin a counter-removal result branch and its ID-linked `Otherwise`
/// branch. The parser represents `no counters were removed this way` as the
/// executable `DidNotHappen` predicate; the following inverse result ID proves
/// that the mana instruction is the authored alternative, not an unrelated
/// conditional.
fn describe_counter_removal_empty_result_otherwise_mana_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [remove_segment, empty_segment, otherwise_segment] = program.segments.as_slice() else {
        return None;
    };
    if [remove_segment, empty_segment, otherwise_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }

    let [remove_root] = remove_segment.default_effects.as_slice() else {
        return None;
    };
    let remove_with_id = remove_root.downcast_ref::<crate::effects::WithIdEffect>()?;
    let mut remove_leaf = remove_with_id.effect.as_ref();
    while let Some(nested) = remove_leaf.downcast_ref::<crate::effects::WithIdEffect>() {
        if nested.id != remove_with_id.id {
            return None;
        }
        remove_leaf = nested.effect.as_ref();
    }
    let remove = remove_leaf.downcast_ref::<crate::effects::RemoveCountersEffect>()?;
    if !matches!(remove.target.unhinted(), ChooseSpec::Source)
        || !matches!(
            remove.count.unhinted(),
            Value::CountersOnSource(counter_type) if *counter_type == remove.counter_type
        )
    {
        return None;
    }

    let [empty_root] = empty_segment.default_effects.as_slice() else {
        return None;
    };
    let empty_with_id = empty_root.downcast_ref::<crate::effects::WithIdEffect>()?;
    let empty = empty_with_id
        .effect
        .downcast_ref::<crate::effects::IfEffect>()?;
    if empty.condition != remove_with_id.id
        || empty.predicate != EffectPredicate::DidNotHappen
        || !empty.else_.is_empty()
    {
        return None;
    }
    let [empty_sequence_root] = empty.then.as_slice() else {
        return None;
    };
    let empty_sequence = empty_sequence_root.downcast_ref::<crate::effects::SequenceEffect>()?;
    if !matches!(
        empty_sequence.surface,
        ironsmith_core::SequenceSurface::ResultConjunction {
            leading_duration: false
        }
    ) || empty_sequence.result_label.is_some()
    {
        return None;
    }
    let [put_root, draw_root] = empty_sequence.effects.as_slice() else {
        return None;
    };
    let put = unwrap_basic_render_wrapper(put_root)
        .downcast_ref::<crate::effects::PutCountersEffect>()?;
    let draw =
        unwrap_basic_render_wrapper(draw_root).downcast_ref::<crate::effects::DrawCardsEffect>()?;
    if put.counter_type != remove.counter_type
        || !matches!(put.amount.unhinted(), Value::Fixed(1))
        || !matches!(put.target.unhinted(), ChooseSpec::Source)
        || put.target_count.is_some()
        || put.distributed
        || draw.player != PlayerFilter::You
        || !matches!(draw.count.unhinted(), Value::Fixed(1))
    {
        return None;
    }
    let source_surface = put.target.source_reference_surface()?.display_text();
    if !source_surface.starts_with("this ") {
        return None;
    }

    let [otherwise_root] = otherwise_segment.default_effects.as_slice() else {
        return None;
    };
    let otherwise = otherwise_root.downcast_ref::<crate::effects::IfEffect>()?;
    if otherwise.condition != empty_with_id.id
        || otherwise.predicate != EffectPredicate::DidNotHappen
        || !otherwise.else_.is_empty()
    {
        return None;
    }
    let [mana_root] = otherwise.then.as_slice() else {
        return None;
    };
    let mana =
        unwrap_basic_render_wrapper(mana_root).downcast_ref::<crate::effects::AddManaEffect>()?;
    if mana.player != PlayerFilter::You || mana.mana.is_empty() {
        return None;
    }
    let add_mana = describe_effect(mana_root)
        .trim()
        .trim_end_matches('.')
        .to_string();
    let counter_type = super::normalize_common::describe_counter_type(remove.counter_type);
    Some(format!(
        "Remove all {counter_type} counters from {source_surface}. If no counters were removed this way, put {} on {source_surface} and draw a card. Otherwise, {}",
        with_indefinite_article(&format!("{counter_type} counter")),
        super::normalize_common::lowercase_first(&add_mana),
    ))
}

fn describe_group_pump_then_conditional_untap_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [pump_segment, untap_segment] = program.segments.as_slice() else {
        return None;
    };
    if !pump_segment.self_replacements.is_empty()
        || !untap_segment.self_replacements.is_empty()
        || pump_segment.default_effects.len() != 1
        || untap_segment.default_effects.len() != 1
    {
        return None;
    }

    let tagged_pump =
        pump_segment.default_effects[0].downcast_ref::<crate::effects::TaggedEffect>()?;
    let pump = tagged_pump
        .effect
        .downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let crate::continuous::EffectTarget::Filter(pump_filter) = &pump.target else {
        return None;
    };
    if pump_filter.card_types != [CardType::Creature] {
        return None;
    }

    let conditional =
        untap_segment.default_effects[0].downcast_ref::<crate::effects::ConditionalEffect>()?;
    if !matches!(
        &conditional.condition,
        Condition::Not(inner) if matches!(inner.as_ref(), Condition::YourTurn)
    ) || !conditional.if_false.is_empty()
        || conditional.if_true.len() != 1
    {
        return None;
    }
    let tagged_untap = conditional.if_true[0].downcast_ref::<crate::effects::TaggedEffect>()?;
    let untap = tagged_untap
        .effect
        .downcast_ref::<crate::effects::UntapEffect>()?;
    let ChooseSpec::Object(untap_filter) = untap.target.base() else {
        return None;
    };
    if !untap_filter.tagged_constraints.iter().any(|constraint| {
        constraint.tag == tagged_pump.tag
            && constraint.relation == crate::filter::TaggedOpbjectRelation::IsTaggedObject
    }) {
        return None;
    }

    Some(format!(
        "{}. If it's not your turn, untap those creatures",
        describe_effect_list(&pump_segment.default_effects).trim_end_matches('.')
    ))
}

pub(super) fn describe_mana_ability_resolution_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if program.segments.len() != 1 {
        return None;
    }
    let segment = &program.segments[0];
    if segment.self_replacements.len() != 1 || segment.default_effects.is_empty() {
        return None;
    }
    let branch = &segment.self_replacements[0];
    let default_text = describe_effect_list(&segment.default_effects);
    let replacement_text = describe_effect_list(&branch.replacement_effects);
    let condition_text = super::normalize_common::describe_condition(&branch.condition);
    Some(format!(
        "{default_text}. If {condition_text}, {} instead",
        super::normalize_common::lowercase_first(&replacement_text)
    ))
}

fn is_standard_gift_render_payload(lower: &str) -> bool {
    lower.contains("chosen player draws a card")
        || lower.contains("chosen player creates a treasure token")
        || lower.contains("create a treasure token under the chosen player's control")
        || lower.contains("chosen player creates a food token")
        || lower.contains("create a food token under the chosen player's control")
        || lower.contains("chosen player creates a tapped 1/1 blue fish creature token")
        || lower.contains("chosen player creates a 1/1 blue fish creature token, tapped")
        || lower.contains("create a 1/1 blue fish creature token under the chosen player's control")
        || lower.contains("chosen player takes an extra turn after this one")
        || lower.contains("chosen player creates an 8/8 blue octopus creature token")
        || lower
            .contains("create an 8/8 blue octopus creature token under the chosen player's control")
        || lower
            .contains("create a 8/8 blue octopus creature token under the chosen player's control")
}

fn is_hidden_gift_resolution_segment(segment: &crate::resolution::ResolutionSegment) -> bool {
    if !segment.self_replacements.is_empty() || segment.default_effects.is_empty() {
        return false;
    }

    let lower = describe_effect_list(&segment.default_effects).to_ascii_lowercase();
    lower.starts_with("if the gift was promised") && is_standard_gift_render_payload(&lower)
}

fn is_hidden_gift_etb_ability(ability: &Ability) -> bool {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return false;
    };
    if !matches!(
        triggered.intervening_if.as_ref(),
        Some(Condition::ThisSpellPaidLabel(label))
            if label.display_label().eq_ignore_ascii_case("Gift")
    ) || !triggered.choices.is_empty()
        || !trigger_is_this_enters_battlefield(&triggered.trigger)
    {
        return false;
    }
    let rendered = describe_resolution_program(&triggered.effects).to_ascii_lowercase();
    rendered.starts_with("if the gift was promised") || is_standard_gift_render_payload(&rendered)
}

fn apply_self_replacement_presentation_label(
    branch: &crate::resolution::SelfReplacementBranch,
    line: String,
) -> String {
    let Some(label) = branch
        .presentation_label
        .as_ref()
        .and_then(crate::ability::PresentationLabel::display_prefix)
    else {
        return line;
    };
    let label = label.trim();
    if label.is_empty() || label.starts_with("__ironsmith_") || line.contains(&format!("{label} —"))
    {
        return line;
    }
    if branch.condition_after_replacement {
        if let Some((default, conditional)) = line.split_once(". If ")
            && let Some((condition, replacement)) = conditional.split_once(", ")
            && let Some(replacement) = replacement.strip_suffix(" instead")
        {
            return format!(
                "{default}. {label} — {} instead if {condition}",
                capitalize_first(replacement)
            );
        }
        if let Some((default, replacement)) = line.split_once(". ")
            && replacement.contains(" instead if ")
        {
            return format!("{default}. {label} — {replacement}");
        }
    }
    if let Some((default, replacement)) = line.split_once(". If ") {
        return format!("{default}. {label} — If {replacement}");
    }
    format!("{label} — {line}")
}

fn describe_conjoined_same_source_damage(effects: &[Effect]) -> Option<String> {
    fn collect_implicit_source_damage<'a>(
        effect: &'a Effect,
        collected: &mut Vec<&'a crate::effects::DealDamageEffect>,
    ) -> Option<()> {
        let effect = unwrap_basic_render_wrapper(effect);
        if let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>() {
            if sequence.surface != ironsmith_core::SequenceSurface::Coordinated {
                return None;
            }
            for child in &sequence.effects {
                collect_implicit_source_damage(child, collected)?;
            }
            return Some(());
        }
        if let Some(for_each) = effect.downcast_ref::<crate::effects::ForEachObject>() {
            let [inner] = for_each.effects.as_slice() else {
                return None;
            };
            let (source, damage) = damage_with_source_view(inner)?;
            if source.is_some() || !matches!(damage.target.base(), ChooseSpec::Iterated) {
                return None;
            }
            collected.push(damage);
            return Some(());
        }
        if let Some(for_players) = effect.downcast_ref::<crate::effects::ForPlayersEffect>() {
            let [inner] = for_players.effects.as_slice() else {
                return None;
            };
            let (source, damage) = damage_with_source_view(inner)?;
            if source.is_some()
                || !matches!(
                    damage.target.base(),
                    ChooseSpec::Player(PlayerFilter::IteratedPlayer)
                )
            {
                return None;
            }
            collected.push(damage);
            return Some(());
        }
        let (source, damage) = damage_with_source_view(effect)?;
        if source.is_some() {
            return None;
        }
        collected.push(damage);
        Some(())
    }

    let effects = match effects {
        [only] => {
            let sequence = unwrap_basic_render_wrapper(only)
                .downcast_ref::<crate::effects::SequenceEffect>()?;
            if matches!(
                sequence.surface,
                ironsmith_core::SequenceSurface::Sequential
            ) {
                return None;
            }
            sequence.effects.as_slice()
        }
        _ => effects,
    };
    let [first, second] = effects else {
        return None;
    };
    if let Some(compact) =
        describe_player_or_planeswalker_damage_then_controlled_creature_damage(first, second)
    {
        return Some(compact);
    }
    if let Some(compact) = describe_joint_subject_pair(first, second) {
        if compact.starts_with("Deal ") {
            return Some(compact);
        }
        if let Some(rest) = compact.strip_prefix("this deals ") {
            return Some(format!("Deal {rest}"));
        }
    }

    let mut damage = Vec::new();
    for effect in effects {
        collect_implicit_source_damage(effect, &mut damage)?;
    }
    let [first_damage, rest @ ..] = damage.as_slice() else {
        return None;
    };
    if rest.is_empty()
        || rest.iter().any(|candidate| {
            candidate.source_is_combat != first_damage.source_is_combat
                || candidate.unpreventable != first_damage.unpreventable
        })
    {
        return None;
    }
    let parts = effects
        .iter()
        .map(describe_effect)
        .map(|rendered| rendered.trim().trim_end_matches('.').to_string())
        .map(|rendered| {
            rendered
                .strip_prefix("this deals ")
                .or_else(|| rendered.strip_prefix("Deal "))
                .map(|damage| {
                    let Some((amount, _)) = damage.split_once(" damage to ") else {
                        return damage.to_string();
                    };
                    damage.replace(&format!(" and deal {amount} damage to "), " and ")
                })
        })
        .collect::<Option<Vec<_>>>()?;
    let joined = match parts.as_slice() {
        [] => return None,
        [only] => only.clone(),
        [first, second] => format!("{first} and {second}"),
        _ => {
            let (last, leading) = parts.split_last()?;
            format!("{}, and {last}", leading.join(", "))
        }
    };
    Some(format!("Deal {joined}"))
}

fn format_self_replacement_fallback(
    default_text: &str,
    condition_text: &str,
    replacement: &str,
    leading_instead_surface: bool,
) -> String {
    if let Some((shuffle, continuation)) = replacement.split_once(". ")
        && shuffle.to_ascii_lowercase().starts_with("shuffle ")
        && shuffle.ends_with("library")
        && continuation.starts_with("Draw ")
    {
        // Both actions belong to the same replacement branch. Keep the
        // shuffle/draw boundary inside that conditional clause.
        return format!(
            "{default_text}. If {condition_text}, instead {shuffle}, {}",
            lowercase_first(continuation)
        );
    }
    if leading_instead_surface || replacement.contains(". ") {
        format!("{default_text}. If {condition_text}, instead {replacement}")
    } else {
        format!("{default_text}. If {condition_text}, {replacement} instead")
    }
}

/// Render the typed replacement that prepends a scry to the same draw action.
/// Requiring the replacement draw to match the default draw exactly prevents
/// this presentation rule from hiding a changed player, count, or draw
/// surface. The leading `instead` is significant for the authored
/// replacement clause, as is the imperative surface of a spell's default
/// draw instruction.
fn describe_scry_then_same_draw_self_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if branch.condition_after_replacement {
        return None;
    }
    let [default_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let [scry_effect, replacement_draw_effect] = branch.replacement_effects.as_slice() else {
        return None;
    };
    let default_draw = unwrap_basic_render_wrapper(default_effect)
        .downcast_ref::<crate::effects::DrawCardsEffect>()?;
    let scry =
        unwrap_basic_render_wrapper(scry_effect).downcast_ref::<crate::effects::ScryEffect>()?;
    let replacement_draw = unwrap_basic_render_wrapper(replacement_draw_effect)
        .downcast_ref::<crate::effects::DrawCardsEffect>()?;
    if default_draw.player != replacement_draw.player
        || default_draw.count != replacement_draw.count
    {
        return None;
    }

    let rendered_default = describe_effect(default_effect);
    let rendered_default = rendered_default.trim().trim_end_matches('.');
    let default_text = if default_draw.player == PlayerFilter::You {
        rendered_default
            .strip_prefix("You ")
            .or_else(|| rendered_default.strip_prefix("you "))
            .unwrap_or(rendered_default)
    } else {
        rendered_default
    };
    let replacement_text = describe_scry_then_draw(scry, replacement_draw)?;
    let raw_condition_text = super::normalize_common::describe_condition(&branch.condition);
    let condition_text = normalize_target_quality_condition(default_text, &raw_condition_text);
    Some(format!(
        "{}. If {condition_text}, instead {}",
        capitalize_first(default_text),
        super::normalize_common::lowercase_first(replacement_text.trim().trim_end_matches('.'))
    ))
}

fn collect_self_replacement_action_targets<'a>(
    effect: &'a Effect,
    targets: &mut Vec<&'a ChooseSpec>,
) {
    let effect = unwrap_basic_render_wrapper(effect);
    if let Some(may) = effect.downcast_ref::<crate::effects::MayEffect>() {
        for nested in &may.effects {
            collect_self_replacement_action_targets(nested, targets);
        }
        return;
    }
    if let Some(unless_pays) = effect.downcast_ref::<crate::effects::UnlessPaysEffect>() {
        for nested in &unless_pays.effects {
            collect_self_replacement_action_targets(nested, targets);
        }
        return;
    }
    if let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>() {
        for nested in &sequence.effects {
            collect_self_replacement_action_targets(nested, targets);
        }
        return;
    }

    let target = rendered_action_target(effect)
        .or_else(|| {
            effect
                .downcast_ref::<crate::effects::DealDamageEffect>()
                .map(|damage| &damage.target)
        })
        .or_else(|| {
            effect
                .downcast_ref::<crate::effects::CounterEffect>()
                .map(|counter| &counter.target)
        })
        .or_else(|| {
            effect
                .downcast_ref::<crate::effects::ExileEffect>()
                .map(|exile| &exile.spec)
        })
        .or_else(|| {
            effect
                .downcast_ref::<crate::effects::ReturnToHandEffect>()
                .map(|return_to_hand| &return_to_hand.spec)
        })
        .or_else(|| {
            effect
                .downcast_ref::<crate::effects::MoveToZoneEffect>()
                .map(|move_to_zone| &move_to_zone.target)
        })
        .or_else(|| {
            effect
                .downcast_ref::<crate::effects::ReturnFromGraveyardToHandEffect>()
                .map(|return_to_hand| &return_to_hand.target)
        })
        .or_else(|| {
            effect
                .downcast_ref::<crate::effects::ReturnFromGraveyardToBattlefieldEffect>()
                .map(|return_to_battlefield| &return_to_battlefield.target)
        })
        .or_else(|| {
            effect
                .downcast_ref::<crate::effects::TargetOnlyEffect>()
                .map(|target_only| &target_only.target)
        });
    if let Some(target) = target {
        targets.push(target);
    }
}

/// Return the one target selection shared by every target-bearing action in
/// this branch. Follow-up actions without targets (for example, scrying) are
/// ignored, while a second, different target makes the branch ineligible for
/// implicit-reference rendering.
fn shared_self_replacement_action_target(effects: &[Effect]) -> Option<&ChooseSpec> {
    let mut targets = Vec::new();
    for effect in effects {
        collect_self_replacement_action_targets(effect, &mut targets);
    }
    let first = *targets.first()?;
    targets
        .iter()
        .all(|target| target_specs_select_same_objects(first, target))
        .then_some(first)
}

fn has_matching_implicit_target_declaration(effects: &[Effect], target: &ChooseSpec) -> bool {
    fn matches_effect(effect: &Effect, target: &ChooseSpec) -> bool {
        let effect = unwrap_basic_render_wrapper(effect);
        if let Some(target_only) = effect.downcast_ref::<crate::effects::TargetOnlyEffect>() {
            return !target_only.explicit_declaration
                && target_specs_select_same_objects(&target_only.target, target);
        }
        if let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>() {
            return sequence
                .effects
                .iter()
                .any(|nested| matches_effect(nested, target));
        }
        if let Some(unless_pays) = effect.downcast_ref::<crate::effects::UnlessPaysEffect>() {
            return unless_pays
                .effects
                .iter()
                .any(|nested| matches_effect(nested, target));
        }
        false
    }

    effects.iter().any(|effect| matches_effect(effect, target))
}

fn source_match_is_only_ability_markers(filter: &ObjectFilter) -> bool {
    if filter.ability_markers.is_empty() {
        return false;
    }
    let mut residual = filter.clone();
    residual.ability_markers.clear();
    residual == ObjectFilter::default()
}

/// Prove that `tag` was produced by applying a tagged wrapper to an action
/// selecting exactly `target`. This is intentionally narrower than a general
/// result-tag lookup: self-replacement pronouns are only safe when the
/// condition's observed object and both branch actions share one target.
fn default_effect_tags_exact_target(effect: &Effect, tag: &TagKey, target: &ChooseSpec) -> bool {
    if let Some(with_id) = effect.downcast_ref::<crate::effects::WithIdEffect>() {
        return default_effect_tags_exact_target(&with_id.effect, tag, target);
    }
    if let Some(tagged) = effect.downcast_ref::<crate::effects::TaggedEffect>() {
        if &tagged.tag == tag
            && shared_self_replacement_action_target(std::slice::from_ref(tagged.effect.as_ref()))
                .is_some_and(|tagged_target| {
                    target_specs_select_same_objects(tagged_target, target)
                })
        {
            return true;
        }
        return default_effect_tags_exact_target(&tagged.effect, tag, target);
    }
    if let Some(tag_all) = effect.downcast_ref::<crate::effects::TagAllEffect>() {
        if &tag_all.tag == tag
            && shared_self_replacement_action_target(std::slice::from_ref(tag_all.effect.as_ref()))
                .is_some_and(|tagged_target| {
                    target_specs_select_same_objects(tagged_target, target)
                })
        {
            return true;
        }
        return default_effect_tags_exact_target(&tag_all.effect, tag, target);
    }
    if let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>() {
        return sequence
            .effects
            .iter()
            .any(|nested| default_effect_tags_exact_target(nested, tag, target));
    }
    false
}

fn self_replacement_target_referent(target: &ChooseSpec) -> Option<&'static str> {
    match target.base() {
        ChooseSpec::AnyTarget | ChooseSpec::AnyOtherTarget => Some("that permanent or player"),
        ChooseSpec::Player(_) | ChooseSpec::SpecificPlayer(_) => Some("that player"),
        ChooseSpec::PlayerOrPlaneswalker(_) | ChooseSpec::ObjectOrPlayer(_, _) => {
            Some("that permanent or player")
        }
        ChooseSpec::Object(filter) => {
            if let Some(stack_kind) = filter.stack_kind {
                return Some(match stack_kind {
                    crate::filter::StackObjectKind::Spell => "that spell",
                    crate::filter::StackObjectKind::Ability
                    | crate::filter::StackObjectKind::ActivatedAbility
                    | crate::filter::StackObjectKind::TriggeredAbility => "that ability",
                    crate::filter::StackObjectKind::SpellOrAbility => "that spell or ability",
                });
            }
            if filter.zone.is_some_and(|zone| zone != Zone::Battlefield) {
                return Some("that card");
            }
            let is_creature = filter.card_types.as_slice() == [CardType::Creature]
                || (!filter.any_of.is_empty()
                    && filter
                        .any_of
                        .iter()
                        .all(|branch| branch.card_types.as_slice() == [CardType::Creature]));
            if is_creature {
                return Some("that creature");
            }
            Some("that permanent")
        }
        _ => None,
    }
}

fn condition_refers_to_self_replacement_target(condition_text: &str, referent: &str) -> bool {
    let lower = condition_text.to_ascii_lowercase();
    lower.contains(referent)
        || lower.starts_with("it's ")
        || lower.starts_with("it is ")
        || lower.starts_with("it was ")
        || lower.starts_with("it has ")
}

fn first_self_replacement_action_moves_target_to_card_zone(effects: &[Effect]) -> bool {
    fn first_action(effect: &Effect) -> Option<&Effect> {
        let effect = unwrap_basic_render_wrapper(effect);
        if let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>() {
            return sequence.effects.iter().find_map(first_action);
        }
        rendered_action_target(effect)
            .or_else(|| {
                effect
                    .downcast_ref::<crate::effects::ExileEffect>()
                    .map(|exile| &exile.spec)
            })
            .or_else(|| {
                effect
                    .downcast_ref::<crate::effects::ReturnToHandEffect>()
                    .map(|return_to_hand| &return_to_hand.spec)
            })
            .or_else(|| {
                effect
                    .downcast_ref::<crate::effects::MoveToZoneEffect>()
                    .map(|move_to_zone| &move_to_zone.target)
            })
            .map(|_| effect)
    }

    let effect = effects.iter().find_map(first_action);
    effect.is_some_and(|effect| {
        effect
            .downcast_ref::<crate::effects::ExileEffect>()
            .is_some()
            || effect
                .downcast_ref::<crate::effects::ReturnToHandEffect>()
                .is_some()
            || effect
                .downcast_ref::<crate::effects::MoveToZoneEffect>()
                .is_some_and(|move_to_zone| move_to_zone.zone != Zone::Battlefield)
            || effect
                .downcast_ref::<crate::effects::DestroyEffect>()
                .is_some()
    })
}

/// Replace a repeated rendered target only after typed target identity proves
/// that the replacement action reuses the default action's target. The text
/// operation is deliberately limited to target surfaces produced from that
/// same `ChooseSpec`. A sequential zone move can mention the same target in
/// more than one action; after the first move, later references use the card
/// referent rather than repeating a fresh target selection.
fn describe_shared_target_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    default_text: &str,
    replacement_text: &str,
    condition: &Condition,
    condition_text: &str,
    leading_instead_surface: bool,
) -> Option<String> {
    let default_target = shared_self_replacement_action_target(default_effects)?;
    let replacement_target = shared_self_replacement_action_target(replacement_effects)?;
    if !default_target.is_target()
        || !default_target.is_single()
        || !replacement_target.is_target()
        || !replacement_target.is_single()
        || !target_specs_select_same_objects(default_target, replacement_target)
    {
        return None;
    }

    let condition_text =
        if has_matching_implicit_target_declaration(replacement_effects, replacement_target)
            && let Condition::SourceMatches(filter) = condition
            && source_match_is_only_ability_markers(filter)
        {
            super::normalize_common::describe_condition(&Condition::TargetMatches(filter.clone()))
        } else if let Condition::TaggedObjectMatches(tag, filter) = condition
            && source_match_is_only_ability_markers(filter)
            && default_effects
                .iter()
                .any(|effect| default_effect_tags_exact_target(effect, tag, default_target))
        {
            super::normalize_common::describe_condition(&Condition::TargetMatches(filter.clone()))
        } else {
            condition_text.to_string()
        };
    let target_surface = super::normalize_common::describe_choose_spec(replacement_target);
    let mut replacement = super::normalize_common::lowercase_first(replacement_text)
        .trim_end_matches('.')
        .to_string();
    let target_starts = replacement
        .match_indices(target_surface.as_str())
        .map(|(start, _)| start)
        .collect::<Vec<_>>();
    let target_start = *target_starts.first()?;
    let repeated_target = target_starts.len() > 1;
    let moves_target_to_card_zone =
        first_self_replacement_action_moves_target_to_card_zone(replacement_effects);
    let referent = self_replacement_target_referent(default_target)?;
    let before_target = &replacement[..target_start];
    let first_referent = if before_target.ends_with(" on ")
        || condition_refers_to_self_replacement_target(&condition_text, referent)
    {
        "it"
    } else {
        referent
    };
    if target_starts.len() == 1 {
        replacement.replace_range(
            target_start..target_start + target_surface.len(),
            first_referent,
        );
    } else {
        let later_referent = if moves_target_to_card_zone {
            "that card"
        } else {
            "it"
        };
        let mut compact = String::with_capacity(replacement.len());
        let mut cursor = 0usize;
        for (index, start) in target_starts.into_iter().enumerate() {
            compact.push_str(&replacement[cursor..start]);
            compact.push_str(if index == 0 {
                first_referent
            } else {
                later_referent
            });
            cursor = start + target_surface.len();
        }
        compact.push_str(&replacement[cursor..]);
        replacement = compact;
    }

    Some(format_self_replacement_fallback(
        default_text,
        &condition_text,
        &replacement,
        leading_instead_surface || (repeated_target && moves_target_to_card_zone),
    ))
}

/// Render the common "same target, different damage result" self-replacement
/// directly from its damage nodes. This preserves an attached scry action or
/// the unpreventable-damage rider, neither of which survives the generic
/// one-damage-effect conditional compactor.
fn describe_shared_target_damage_self_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if branch.condition_after_replacement {
        return None;
    }
    let [default_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let default_damage = self_replacement_damage_view(default_effect)?;
    let (replacement_effect, scry) = match branch.replacement_effects.as_slice() {
        [replacement_effect] => (replacement_effect, None),
        [replacement_effect, scry_effect] => (
            replacement_effect,
            Some(
                unwrap_basic_render_wrapper(scry_effect)
                    .downcast_ref::<crate::effects::ScryEffect>()?,
            ),
        ),
        _ => return None,
    };
    let replacement_damage = self_replacement_damage_view(replacement_effect)?;
    if default_damage.source_is_combat
        || default_damage.unpreventable
        || replacement_damage.source_is_combat
        || !default_damage.target.is_target()
        || !default_damage.target.is_single()
        || !replacement_damage.target.is_target()
        || !replacement_damage.target.is_single()
        || !target_specs_select_same_objects(&default_damage.target, &replacement_damage.target)
    {
        return None;
    }
    if let Some(scry) = scry
        && scry.player != PlayerFilter::You
    {
        return None;
    }

    let referent = self_replacement_target_referent(&default_damage.target)?;
    let plain_amount_override = scry.is_none() && !replacement_damage.unpreventable;
    let target_suffix = if plain_amount_override {
        String::new()
    } else {
        format!(" to {referent}")
    };
    let mut replacement = format!(
        "it deals {} damage{}",
        super::normalize_common::describe_value(&replacement_damage.amount),
        target_suffix
    );
    if let Some(scry) = scry {
        replacement.push_str(&format!(
            " and you scry {}",
            super::normalize_common::describe_value(&scry.count)
        ));
    }
    if replacement_damage.unpreventable {
        replacement.push_str(" and the damage can't be prevented");
    }
    let default_text = describe_effect_list(&segment.default_effects)
        .trim()
        .trim_end_matches('.')
        .to_string();
    let raw_condition_text = super::normalize_common::describe_condition(&branch.condition);
    let condition_text = normalize_target_quality_condition(&default_text, &raw_condition_text);
    if plain_amount_override {
        return Some(format!(
            "{default_text}. If {condition_text}, {replacement} instead"
        ));
    }
    Some(format!(
        "{}. If {condition_text}, instead {replacement}",
        default_text
    ))
}

/// View damage through the bookkeeping wrappers used by self-replacement
/// lowering. `ExecuteWithSourceEffect` preserves the authored damage source;
/// it does not change the target, amount, or prevention semantics that this
/// renderer compares.
fn self_replacement_damage_view(effect: &Effect) -> Option<&crate::effects::DealDamageEffect> {
    let effect = unwrap_basic_render_wrapper(effect);
    let effect = effect
        .downcast_ref::<crate::effects::ExecuteWithSourceEffect>()
        .map(|execute| unwrap_basic_render_wrapper(&execute.effect))
        .unwrap_or(effect);
    effect.downcast_ref::<crate::effects::DealDamageEffect>()
}

/// Render a trailing-condition damage replacement only when the typed damage
/// nodes prove that both instructions use the same source and target. A bare
/// damage effect implicitly uses the resolving spell or ability as its source,
/// so an explicit `ChooseSpec::Source` wrapper is equivalent to that implicit
/// source; no other implicit/explicit source pairing is safe to collapse to
/// the pronoun "It."
fn describe_trailing_same_source_damage_self_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if !branch.condition_after_replacement {
        return None;
    }
    let [default_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let [replacement_effect] = branch.replacement_effects.as_slice() else {
        return None;
    };
    let (default_source, default_damage) = damage_with_source_view(default_effect)?;
    let (replacement_source, replacement_damage) = damage_with_source_view(replacement_effect)?;
    let sources_match = match (default_source, replacement_source) {
        (None, None) => true,
        (None, Some(source)) | (Some(source), None) => {
            matches!(source.unhinted(), ChooseSpec::Source)
        }
        (Some(default_source), Some(replacement_source)) => {
            default_source.unhinted() == replacement_source.unhinted()
        }
    };
    if !sources_match
        || default_damage.source_is_combat
        || replacement_damage.source_is_combat
        || default_damage.unpreventable
        || replacement_damage.unpreventable
        || !default_damage.target.is_target()
        || !default_damage.target.is_single()
        || !replacement_damage.target.is_target()
        || !replacement_damage.target.is_single()
        || !target_specs_select_same_objects(&default_damage.target, &replacement_damage.target)
    {
        return None;
    }

    let referent = self_replacement_target_referent(&default_damage.target)?;
    let default_text = describe_effect_list(&segment.default_effects);
    let default_text = default_text.trim().trim_end_matches('.');
    let raw_condition_text = super::normalize_common::describe_condition(&branch.condition);
    let condition_text = normalize_target_quality_condition(default_text, &raw_condition_text);
    Some(format!(
        "{default_text}. It deals {} damage to {referent} instead if {condition_text}",
        super::normalize_common::describe_value(&replacement_damage.amount)
    ))
}

/// Render an amount-only replacement over one announced plural AnyTarget set.
///
/// The replacement grammar's "each of those permanents and/or players" is a
/// reference to the targets already chosen for the spell, not a second target
/// declaration or a battlefield-only iteration. Requiring identical typed
/// target specs and compatible damage sources keeps that correlation explicit.
fn describe_trailing_plural_any_target_damage_self_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if !branch.condition_after_replacement || branch.leading_instead_surface {
        return None;
    }
    let [default_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let [replacement_effect] = branch.replacement_effects.as_slice() else {
        return None;
    };
    let (default_source, default_damage) = damage_with_source_view(default_effect)?;
    let (replacement_source, replacement_damage) = damage_with_source_view(replacement_effect)?;
    let sources_match = match (default_source, replacement_source) {
        (None, None) => true,
        (None, Some(source)) | (Some(source), None) => {
            matches!(source.unhinted(), ChooseSpec::Source)
        }
        (Some(default_source), Some(replacement_source)) => {
            default_source.unhinted() == replacement_source.unhinted()
        }
    };
    let ChooseSpec::WithCount(inner, count) = default_damage.target.unhinted() else {
        return None;
    };
    if !sources_match
        || default_damage.source_is_combat
        || replacement_damage.source_is_combat
        || default_damage.unpreventable
        || replacement_damage.unpreventable
        || !matches!(inner.unhinted(), ChooseSpec::AnyTarget)
        || count.min != 0
        || count.max.is_none_or(|maximum| maximum <= 1)
        || count.dynamic_x
        || count.up_to_x
        || count.random
        || count.explicit_exactly
        || default_damage.target != replacement_damage.target
    {
        return None;
    }

    let default_text = describe_effect_list(&segment.default_effects);
    let default_text = default_text.trim().trim_end_matches('.');
    let condition_text = super::normalize_common::describe_condition(&branch.condition);
    Some(format!(
        "{default_text}. It deals {} damage to each of those permanents and/or players instead if {condition_text}",
        super::normalize_common::describe_value(&replacement_damage.amount)
    ))
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum TargetThresholdCharacteristic {
    Toughness,
    ManaValue,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum TargetThresholdAction {
    Destroy,
    GainControl,
}

struct TargetThresholdConditional<'a> {
    characteristic: TargetThresholdCharacteristic,
    limit: Value,
    action: TargetThresholdAction,
    action_effect: &'a Effect,
    action_target: &'a ChooseSpec,
    characteristic_target: ChooseSpec,
}

fn target_threshold_action(effect: &Effect) -> Option<(TargetThresholdAction, &ChooseSpec)> {
    let action = unwrap_basic_render_wrapper(effect);
    if let Some(destroy) = action.downcast_ref::<crate::effects::DestroyEffect>() {
        return Some((TargetThresholdAction::Destroy, &destroy.spec));
    }
    let apply = action.downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    if apply.until != Until::Forever
        || apply.modification.is_some()
        || !apply.additional_modifications.is_empty()
        || apply.condition.is_some()
        || !matches!(
            apply.runtime_modifications.as_slice(),
            [crate::effects::continuous::RuntimeModification::ChangeControllerToEffectController]
        )
    {
        return None;
    }
    Some((
        TargetThresholdAction::GainControl,
        apply.target_spec.as_ref()?,
    ))
}

fn effect_has_result_tag(effect: &Effect, expected: &TagKey) -> bool {
    if let Some(tagged) = effect.downcast_ref::<crate::effects::TaggedEffect>() {
        return &tagged.tag == expected || effect_has_result_tag(&tagged.effect, expected);
    }
    if let Some(with_id) = effect.downcast_ref::<crate::effects::WithIdEffect>() {
        return effect_has_result_tag(&with_id.effect, expected);
    }
    if let Some(tag_all) = effect.downcast_ref::<crate::effects::TagAllEffect>() {
        return &tag_all.tag == expected || effect_has_result_tag(&tag_all.effect, expected);
    }
    false
}

fn characteristic_reference_matches_action(
    characteristic_target: &ChooseSpec,
    action_target: &ChooseSpec,
    action_effect: &Effect,
) -> bool {
    if target_specs_select_same_objects(characteristic_target, action_target) {
        return true;
    }
    matches!(
        characteristic_target.base(),
        ChooseSpec::Tagged(tag)
            if matches!(action_target.base(), ChooseSpec::Tagged(action_tag) if action_tag == tag)
                || matches!(
                    action_target.base(),
                    ChooseSpec::Object(filter)
                        if matches!(
                            filter.tagged_constraints.as_slice(),
                            [constraint]
                                if &constraint.tag == tag
                                    && constraint.relation
                                        == crate::target::TaggedOpbjectRelation::IsTaggedObject
                        )
                )
                || effect_has_result_tag(action_effect, tag)
    )
}

fn action_target_reuses_announced_target(
    action_target: &ChooseSpec,
    announced_target: &ChooseSpec,
    announced_effect: &Effect,
) -> bool {
    if target_specs_select_same_objects(action_target, announced_target) {
        return true;
    }
    let ChooseSpec::Object(filter) = action_target.base() else {
        return false;
    };
    let [constraint] = filter.tagged_constraints.as_slice() else {
        return false;
    };
    if constraint.relation != crate::target::TaggedOpbjectRelation::IsTaggedObject
        || !effect_has_result_tag(announced_effect, &constraint.tag)
    {
        return false;
    }
    let mut residual = filter.clone();
    residual.tagged_constraints.clear();
    target_specs_select_same_objects(&ChooseSpec::Object(residual), announced_target.base())
}

fn target_threshold_conditional(effect: &Effect) -> Option<TargetThresholdConditional<'_>> {
    let conditional =
        unwrap_basic_render_wrapper(effect).downcast_ref::<crate::effects::ConditionalEffect>()?;
    if conditional.surface != ironsmith_core::ConditionalSurface::TrailingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }
    let [action_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let (action, action_target) = target_threshold_action(action_effect)?;
    let (characteristic, characteristic_target, limit) = match &conditional.condition {
        Condition::ValueComparison {
            left,
            operator: crate::effect::ValueComparisonOperator::LessThanOrEqual,
            right,
        } if matches!(right.unhinted(), Value::Fixed(_) | Value::X) => {
            let (characteristic, target) = match left.unhinted() {
                Value::ToughnessOf(target) => {
                    (TargetThresholdCharacteristic::Toughness, target.as_ref())
                }
                Value::ManaValueOf(target) => {
                    (TargetThresholdCharacteristic::ManaValue, target.as_ref())
                }
                _ => return None,
            };
            (characteristic, target.clone(), right.clone())
        }
        Condition::TaggedObjectMatches(tag, filter) => {
            let mut residual = filter.clone();
            let (characteristic, limit) = match (&filter.toughness, &filter.mana_value) {
                (Some(crate::filter::Comparison::LessThanOrEqual(limit)), None) => {
                    residual.toughness = None;
                    (TargetThresholdCharacteristic::Toughness, *limit)
                }
                (None, Some(crate::filter::Comparison::LessThanOrEqual(limit))) => {
                    residual.mana_value = None;
                    (TargetThresholdCharacteristic::ManaValue, *limit)
                }
                _ => return None,
            };
            if residual != ObjectFilter::default() {
                return None;
            }
            (
                characteristic,
                ChooseSpec::Tagged(tag.clone()),
                Value::Fixed(limit),
            )
        }
        _ => return None,
    };
    if !characteristic_reference_matches_action(
        &characteristic_target,
        action_target,
        action_effect,
    ) {
        return None;
    }
    Some(TargetThresholdConditional {
        characteristic,
        limit,
        action,
        action_effect,
        action_target,
        characteristic_target,
    })
}

/// Fold a synthesized target declaration back into two threshold-guarded uses
/// of that same target. Both conditional actions and both characteristic
/// references must be linked to the one typed target before this emits the
/// authored pronouns.
fn describe_target_threshold_paid_cost_self_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [target_effect, default_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let target_only = unwrap_basic_render_wrapper(target_effect)
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if target_only.explicit_declaration {
        return None;
    }
    exact_single_target_object_filter(&target_only.target)?;
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if branch.condition_after_replacement || branch.presentation_label.is_some() {
        return None;
    }
    let replacement_effect = match branch.replacement_effects.as_slice() {
        [effect] => effect,
        [replacement_target_effect, effect] => {
            let replacement_target = unwrap_basic_render_wrapper(replacement_target_effect)
                .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
            if replacement_target.explicit_declaration
                || !target_specs_select_same_objects(
                    &replacement_target.target,
                    &target_only.target,
                )
            {
                return None;
            }
            effect
        }
        _ => return None,
    };
    let default = target_threshold_conditional(default_effect)?;
    let replacement = target_threshold_conditional(replacement_effect)?;
    if default.characteristic != replacement.characteristic
        || default.action != replacement.action
        || !action_target_reuses_announced_target(
            default.action_target,
            &target_only.target,
            target_effect,
        )
        || !action_target_reuses_announced_target(
            replacement.action_target,
            &target_only.target,
            target_effect,
        )
        || !characteristic_reference_matches_action(
            &default.characteristic_target,
            default.action_target,
            default.action_effect,
        )
        || !characteristic_reference_matches_action(
            &replacement.characteristic_target,
            replacement.action_target,
            replacement.action_effect,
        )
    {
        return None;
    }
    if !matches!(
        &branch.condition,
        Condition::ThisSpellWasKicked
            | Condition::ThisSpellPaidLabel(_)
            | Condition::TurnHistory(ironsmith_core::TurnHistoryCondition::SourceWasKicked { .. })
    ) {
        return None;
    }

    let target_phrase = describe_choose_spec(&target_only.target);
    let target_noun = target_phrase.strip_prefix("target ")?;
    if target_noun.is_empty() || target_noun.split_whitespace().count() != 1 {
        return None;
    }
    let characteristic = match default.characteristic {
        TargetThresholdCharacteristic::Toughness => "toughness",
        TargetThresholdCharacteristic::ManaValue => "mana value",
    };
    let (default_action, replacement_action) = match default.action {
        TargetThresholdAction::Destroy => (
            format!("Destroy target {target_noun}"),
            format!("destroy that {target_noun}"),
        ),
        TargetThresholdAction::GainControl => (
            format!("Gain control of target {target_noun}"),
            format!("gain control of that {target_noun}"),
        ),
    };
    let default_limit = super::normalize_common::describe_value(&default.limit);
    let replacement_limit = super::normalize_common::describe_value(&replacement.limit);
    let paid_condition = super::normalize_common::describe_condition(&branch.condition);
    let default_clause =
        format!("{default_action} if its {characteristic} is {default_limit} or less");
    let replacement_clause =
        format!("{replacement_action} if its {characteristic} is {replacement_limit} or less");
    if branch.leading_instead_surface {
        Some(format!(
            "{default_clause}. If {paid_condition}, instead {replacement_clause}"
        ))
    } else {
        Some(format!(
            "{default_clause}. If {paid_condition}, {replacement_clause} instead"
        ))
    }
}

/// The document lowering route may preserve an authored threshold
/// replacement as two source-sentence segments: the ordinary target/action
/// followed by a leading-if Madness branch over that same tagged target.
/// Rebuild only that exact typed replacement view for rendering; the existing
/// threshold compactor still proves action kind, characteristic, target
/// identity, and both characteristic references before emitting `instead`.
fn describe_cross_segment_madness_target_threshold_replacement(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [default_segment, replacement_segment] = program.segments.as_slice() else {
        return None;
    };
    if default_segment.starts_new_source_line
        || replacement_segment.starts_new_source_line
        || !default_segment.self_replacements.is_empty()
        || !replacement_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [replacement_effect] = replacement_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional = replacement_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let Condition::ThisSpellPaidLabel(label) = &conditional.condition else {
        return None;
    };
    if label.kind != crate::cost::OptionalCostKind::Madness
        || label.discriminator.is_some()
        || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || conditional.if_true.len() != 1
        || !conditional.if_false.is_empty()
    {
        return None;
    }

    let mut normalized = default_segment.clone();
    let mut branch = crate::resolution::SelfReplacementBranch::new(
        conditional.condition.clone(),
        conditional.if_true.clone(),
    );
    branch.leading_instead_surface = true;
    normalized.self_replacements.push(branch);
    describe_target_threshold_paid_cost_self_replacement(&normalized)
}

fn targeted_search_exile_shuffle_branch<'a>(
    effects: &'a [Effect],
    inherited_target: Option<&'a Effect>,
) -> Option<(
    &'a Effect,
    [&'a Effect; 3],
    &'a crate::effects::ChooseObjectsEffect,
)> {
    let (target_effect, tail_sequence_effect) = match effects {
        [outer_effect] => {
            let outer = structural_unwrap_render_wrappers(outer_effect)
                .downcast_ref::<crate::effects::SequenceEffect>()?;
            match (outer.surface, outer.effects.as_slice(), inherited_target) {
                (
                    ironsmith_core::SequenceSurface::CommaThen,
                    [target_effect, tail_sequence_effect],
                    None,
                ) => (target_effect, tail_sequence_effect),
                (ironsmith_core::SequenceSurface::Sequential, _, Some(target_effect)) => {
                    (target_effect, outer_effect)
                }
                _ => return None,
            }
        }
        _ => return None,
    };
    let target = structural_unwrap_render_wrappers(target_effect)
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if target.explicit_declaration {
        return None;
    }
    let ChooseSpec::Player(target_player) = target.target.base() else {
        return None;
    };
    if !target.target.is_target()
        || !target.target.count().is_single()
        || !matches!(target_player, PlayerFilter::Target(inner) if inner.as_ref() == &PlayerFilter::Any)
    {
        return None;
    }

    let tail = structural_unwrap_render_wrappers(tail_sequence_effect)
        .downcast_ref::<crate::effects::SequenceEffect>()?;
    if tail.surface != ironsmith_core::SequenceSurface::Sequential {
        return None;
    }
    let [search_effect, exile_effect, shuffle_effect] = tail.effects.as_slice() else {
        return None;
    };
    let search = structural_unwrap_render_wrappers(search_effect)
        .downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    Some((
        target_effect,
        [search_effect, exile_effect, shuffle_effect],
        search,
    ))
}

fn compact_search_exile_shuffle_punctuation(rendered: &str) -> Option<String> {
    let stem = rendered.strip_suffix(". Then that player shuffles")?;
    let (search, pronoun) = stem
        .strip_suffix(" and exile them")
        .map(|search| (search, "them"))
        .or_else(|| {
            stem.strip_suffix(" and exile it")
                .map(|search| (search, "it"))
        })?;
    Some(format!(
        "{search}, exile {pronoun}, then that player shuffles"
    ))
}

/// Preserve a target-owned search/exile/shuffle procedure across a kicked
/// self replacement. The target declaration and both searched-set tags are
/// executable scaffolding; the complete typed graph proves the authored
/// pronouns and comma-then surface.
fn describe_targeted_search_count_self_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if branch.condition_after_replacement
        || !branch.leading_instead_surface
        || branch.presentation_label.is_some()
        || !matches!(
            &branch.condition,
            Condition::ThisSpellWasKicked
                | Condition::TurnHistory(
                    ironsmith_core::TurnHistoryCondition::SourceWasKicked { .. }
                )
        )
    {
        return None;
    }

    let (target_effect, default_tail, default_search) =
        targeted_search_exile_shuffle_branch(&segment.default_effects, None)?;
    let (_, replacement_tail, replacement_search) =
        targeted_search_exile_shuffle_branch(&branch.replacement_effects, Some(target_effect))?;
    if default_search.count.min != 0
        || replacement_search.count.min != 0
        || default_search.count.dynamic_x
        || default_search.count.up_to_x
        || default_search.count.random
        || default_search.count.explicit_exactly
        || replacement_search.count.dynamic_x
        || replacement_search.count.up_to_x
        || replacement_search.count.random
        || replacement_search.count.explicit_exactly
        || !matches!(default_search.count.max, Some(max) if max > 1)
        || !matches!(
            (default_search.count.max, replacement_search.count.max),
            (Some(default_max), Some(replacement_max)) if replacement_max > default_max
        )
    {
        return None;
    }
    let mut replacement_at_default_count = replacement_search.clone();
    replacement_at_default_count.count = default_search.count;
    if replacement_at_default_count != *default_search {
        return None;
    }

    let default_refs = [
        target_effect,
        default_tail[0],
        default_tail[1],
        default_tail[2],
    ];
    let replacement_refs = [
        target_effect,
        replacement_tail[0],
        replacement_tail[1],
        replacement_tail[2],
    ];
    let default = compact_search_exile_shuffle_punctuation(
        &describe_target_player_search_exile_shuffle_bundle(&default_refs)?,
    )?;
    let replacement = compact_search_exile_shuffle_punctuation(
        &describe_target_player_search_exile_shuffle_bundle(&replacement_refs)?,
    )?;
    let replacement = replacement
        .strip_prefix("Search target player's library ")?
        .to_string();
    let condition = super::normalize_common::describe_condition(&branch.condition);
    Some(format!(
        "{default}. If {condition}, instead search that player's library {replacement}"
    ))
}

fn two_single_mana_symbols_spent_to_cast(condition: &Condition) -> Option<String> {
    let Condition::And(left, right) = condition else {
        return None;
    };
    let symbol = |condition: &Condition| match condition {
        Condition::ManaSpentToCastThisSpellAtLeast {
            amount: 1,
            symbol: Some(symbol),
        } => Some(*symbol),
        _ => None,
    };
    Some(format!(
        "{}{}",
        super::normalize_common::describe_mana_symbol(symbol(left)?),
        super::normalize_common::describe_mana_symbol(symbol(right)?),
    ))
}

/// Rejoin a copy-token self replacement whose only semantic addition is one
/// quoted object ability. The two copy effects must otherwise be identical,
/// including their result tag, while the replacement target must retain the
/// authored `that ...` source-reference surface.
fn describe_copy_token_quoted_ability_self_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    fn same_copy_source(left: &ChooseSpec, right: &ChooseSpec) -> bool {
        match (left.base(), right.base()) {
            (ChooseSpec::Object(left), ChooseSpec::Object(right)) => {
                let mut left = left.clone();
                let mut right = right.clone();
                // The demonstrative is retained both as a target hint and on
                // the filter by the migrated reference resolver. It is a
                // presentation distinction, not a different copy source.
                left.source_surface = None;
                right.source_surface = None;
                left == right
            }
            (left, right) => left == right,
        }
    }

    let [default_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    let [replacement_effect] = branch.replacement_effects.as_slice() else {
        return None;
    };
    if segment.starts_new_source_line
        || branch.starts_new_source_line
        || branch.condition_after_replacement
        || !branch.leading_instead_surface
        || branch.presentation_label.is_some()
    {
        return None;
    }
    let mana_symbols = two_single_mana_symbols_spent_to_cast(&branch.condition)?;
    let default_tagged = default_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let replacement_tagged = replacement_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    if default_tagged.tag != replacement_tagged.tag {
        return None;
    }
    let default = default_tagged
        .effect
        .downcast_ref::<crate::effects::CreateTokenCopyEffect>()?;
    let replacement = replacement_tagged
        .effect
        .downcast_ref::<crate::effects::CreateTokenCopyEffect>()?;
    if !default.granted_static_abilities.is_empty()
        || replacement.granted_static_abilities.len() != 1
        || !default.target.is_target()
        || !default.target.is_single()
        || replacement.target.is_target()
        || replacement.target.source_reference_surface()
            != Some(&crate::target::SourceReferenceSurface::ThisPermanentType(
                "that permanent".to_string(),
            ))
        || !same_copy_source(&default.target, &replacement.target)
    {
        return None;
    }

    let granted = &replacement.granted_static_abilities[0];
    let model = granted.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) = &model.payload
    else {
        return None;
    };
    if grant.filter != ObjectFilter::source()
        || !grant.additional_abilities.is_empty()
        || grant.condition.is_some()
        || grant.set_quantifier_surface.is_some()
        || !matches!(
            grant.ability.kind,
            ironsmith_core::AbilityKind::Triggered(_)
        )
    {
        return None;
    }
    let structural_rule;
    let authored_rule = grant.display.trim().trim_matches('"');
    let rule_source = if authored_rule.is_empty() {
        let ability = crate::static_abilities::StaticAbilityModelInterpreter::ability_from_model(
            &grant.ability,
        );
        structural_rule = super::render_effects::describe_inline_ability_with_self_subject(
            &ability,
            "this token",
        )
        .replace("if that object is a creature", "if it's a creature");
        structural_rule.as_str()
    } else {
        authored_rule
    };
    let rule = capitalize_first(rule_source);
    if rule.is_empty() || rule.contains('"') || rule.contains('\n') {
        return None;
    }
    let rule = if rule.ends_with('.') {
        rule
    } else {
        format!("{rule}.")
    };

    let mut normalized_default = default.clone();
    let mut normalized_replacement = replacement.clone();
    normalized_default.target = ChooseSpec::Source;
    normalized_replacement.target = ChooseSpec::Source;
    normalized_replacement.granted_static_abilities.clear();
    if normalized_default != normalized_replacement {
        return None;
    }

    let default_text = describe_effect_list(&segment.default_effects);
    let mut replacement_without_grant = replacement.clone();
    replacement_without_grant.granted_static_abilities.clear();
    let replacement_text = super::normalize_common::lowercase_first(&describe_effect(
        &Effect::new(replacement_without_grant),
    ));
    Some(format!(
        "{default_text}. If {mana_symbols} was spent to cast this spell, instead {replacement_text}, except the token has \"{rule}\""
    ))
}

fn choice_complement_players(effects: &[Effect]) -> Option<&crate::effects::ForPlayersEffect> {
    let [effect] = effects else {
        return None;
    };
    let players =
        unwrap_basic_render_wrapper(effect).downcast_ref::<crate::effects::ForPlayersEffect>()?;
    if players.filter != PlayerFilter::Any
        || players.starting_with_controller
        || players.stop_after_first_happened
    {
        return None;
    }
    Some(players)
}

fn describe_each_player_choice_complement_chooser_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if branch.condition_after_replacement || branch.leading_instead_surface {
        return None;
    }
    let default = choice_complement_players(&segment.default_effects)?;
    let replacement = choice_complement_players(&branch.replacement_effects)?;
    fn members(players: &crate::effects::ForPlayersEffect) -> &[Effect] {
        if let [effect] = players.effects.as_slice()
            && let Some(sequence) = effect.downcast_ref::<crate::effects::SequenceEffect>()
        {
            &sequence.effects
        } else {
            &players.effects
        }
    }
    let (default_tail, default_choices) = members(default).split_last()?;
    let (replacement_tail, replacement_choices) = members(replacement).split_last()?;
    let default_sacrifice =
        default_tail.downcast_ref::<crate::effects::zones::SacrificePlayerEffect>()?;
    let replacement_sacrifice =
        replacement_tail.downcast_ref::<crate::effects::zones::SacrificePlayerEffect>()?;
    if default_sacrifice != replacement_sacrifice
        || default_choices.is_empty()
        || default_choices.len() != replacement_choices.len()
    {
        return None;
    }
    for (original, changed) in default_choices.iter().zip(replacement_choices) {
        let original = original.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
        let mut changed = changed
            .downcast_ref::<crate::effects::ChooseObjectsEffect>()?
            .clone();
        if changed.chooser != PlayerFilter::You {
            return None;
        }
        changed.chooser = PlayerFilter::IteratedPlayer;
        if changed != *original {
            return None;
        }
    }
    let rendered =
        super::render_effects::describe_for_players_choose_types_then_sacrifice_rest(default)?;
    let condition = if let Some(mana) = two_single_mana_symbols_spent_to_cast(&branch.condition) {
        format!("{mana} was spent to cast this spell")
    } else {
        describe_condition(&branch.condition)
    };
    Some(format!(
        "{rendered}. If {condition}, you choose the permanents for each player instead"
    ))
}

fn describe_trailing_same_target_fixed_pt_self_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    struct FixedPtView<'a> {
        target: &'a ChooseSpec,
        power: &'a Value,
        toughness: &'a Value,
        duration: &'a Until,
    }

    fn fixed_pt_view(effects: &[Effect]) -> Option<FixedPtView<'_>> {
        let (target_declaration, effect) = match effects {
            [effect] => (None, effect),
            [target_effect, effect] => (
                Some(
                    unwrap_basic_render_wrapper(target_effect)
                        .downcast_ref::<crate::effects::TargetOnlyEffect>()?,
                ),
                effect,
            ),
            _ => return None,
        };
        let effect = unwrap_basic_render_wrapper(effect);
        let view = if let Some(pump) =
            effect.downcast_ref::<crate::effects::ModifyPowerToughnessEffect>()
        {
            FixedPtView {
                target: &pump.target,
                power: &pump.power,
                toughness: &pump.toughness,
                duration: &pump.duration,
            }
        } else {
            let continuous = effect.downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
            let [
                crate::effects::continuous::RuntimeModification::ModifyPowerToughness {
                    power,
                    toughness,
                },
            ] = continuous.runtime_modifications.as_slice()
            else {
                return None;
            };
            if continuous.modification.is_some()
                || !continuous.additional_modifications.is_empty()
                || continuous.condition.is_some()
                || continuous.lock_filter_at_resolution
                || continuous.resolve_set_pt_values_at_resolution
            {
                return None;
            }
            FixedPtView {
                target: continuous.target_spec.as_ref()?,
                power,
                toughness,
                duration: &continuous.until,
            }
        };
        if let Some(target) = target_declaration
            && (target.explicit_declaration
                || !target_specs_select_same_objects(&target.target, view.target))
        {
            return None;
        }
        matches!(
            (view.power.unhinted(), view.toughness.unhinted()),
            (Value::Fixed(_), Value::Fixed(_))
        )
        .then_some(view)
    }

    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if !branch.condition_after_replacement
        || branch.leading_instead_surface
        || branch.presentation_label.is_some()
    {
        return None;
    }
    let default = fixed_pt_view(&segment.default_effects)?;
    let replacement = fixed_pt_view(&branch.replacement_effects)?;
    if *default.duration != Until::EndOfTurn
        || replacement.duration != default.duration
        || !default.target.is_target()
        || !default.target.is_single()
        || !replacement.target.is_target()
        || !replacement.target.is_single()
        || !target_specs_select_same_objects(default.target, replacement.target)
        || (default.power == replacement.power && default.toughness == replacement.toughness)
        || self_replacement_target_referent(default.target) != Some("that creature")
    {
        return None;
    }

    let default_text = describe_effect_list(&segment.default_effects);
    let replacement_text = describe_effect_list(&branch.replacement_effects);
    let target_text = super::normalize_common::describe_choose_spec(replacement.target);
    let replacement_text = replacement_text.trim().trim_end_matches('.');
    let replacement_tail = replacement_text
        .strip_prefix(&target_text)
        .or_else(|| replacement_text.strip_prefix(&capitalize_first(&target_text)))?
        .strip_suffix(" until end of turn")?;
    let mut condition = super::normalize_common::describe_condition(&branch.condition);
    if let Condition::PlayerControls { filter, .. } = &branch.condition
        && let Some(name) = filter.name.as_deref()
    {
        condition = condition.replace(
            name,
            &super::normalize_common::title_case_card_name_fragment(name),
        );
    }
    Some(format!(
        "{}. That creature{replacement_tail} instead if {condition}",
        capitalize_first(default_text.trim().trim_end_matches('.'))
    ))
}

#[cfg(test)]
mod trailing_same_target_fixed_pt_self_replacement_tests {
    use super::*;

    fn segment(
        replacement_target: ChooseSpec,
        replacement_duration: Until,
        condition_after_replacement: bool,
    ) -> crate::resolution::ResolutionSegment {
        let target = ChooseSpec::target(ChooseSpec::Object(
            ObjectFilter::creature().controlled_by(PlayerFilter::Opponent),
        ));
        let mut branch = crate::resolution::SelfReplacementBranch::new(
            Condition::YourTurn,
            vec![Effect::new(
                crate::effects::ModifyPowerToughnessEffect::new(
                    replacement_target,
                    -4,
                    -4,
                    replacement_duration,
                ),
            )],
        );
        branch.condition_after_replacement = condition_after_replacement;
        crate::resolution::ResolutionSegment {
            default_effects: vec![Effect::new(
                crate::effects::ModifyPowerToughnessEffect::new(target, -1, -1, Until::EndOfTurn),
            )],
            self_replacements: vec![branch],
            starts_new_source_line: false,
        }
    }

    fn creature_target(controller: PlayerFilter) -> ChooseSpec {
        ChooseSpec::target(ChooseSpec::Object(
            ObjectFilter::creature().controlled_by(controller),
        ))
    }

    #[test]
    fn renders_trailing_condition_for_same_target_pt_override() {
        let segment = segment(
            creature_target(PlayerFilter::Opponent),
            Until::EndOfTurn,
            true,
        );
        assert_eq!(
            describe_trailing_same_target_fixed_pt_self_replacement(&segment),
            Some(
                "Target creature an opponent controls gets -1/-1 until end of turn. That creature gets -4/-4 instead if it's your turn"
                    .to_string()
            )
        );
    }

    #[test]
    fn rejects_changed_target_duration_or_condition_order() {
        assert_eq!(
            describe_trailing_same_target_fixed_pt_self_replacement(&segment(
                creature_target(PlayerFilter::You),
                Until::EndOfTurn,
                true,
            )),
            None
        );
        assert_eq!(
            describe_trailing_same_target_fixed_pt_self_replacement(&segment(
                creature_target(PlayerFilter::Opponent),
                Until::Forever,
                true,
            )),
            None
        );
        assert_eq!(
            describe_trailing_same_target_fixed_pt_self_replacement(&segment(
                creature_target(PlayerFilter::Opponent),
                Until::EndOfTurn,
                false,
            )),
            None
        );
    }
}

fn named_artifact_control_condition(condition: &Condition) -> Option<[String; 2]> {
    let Condition::And(left, right) = condition else {
        return None;
    };
    let named_artifact = |condition: &Condition| -> Option<String> {
        let Condition::PlayerControls { player, filter } = condition else {
            return None;
        };
        if player != &PlayerFilter::You {
            return None;
        }
        let mut plain = filter.clone();
        let name = plain.name.take()?;
        plain.set_explicit_card_type_noun(None);
        (plain == ObjectFilter::artifact().controlled_by(PlayerFilter::You)).then_some(name)
    };
    Some([named_artifact(left)?, named_artifact(right)?])
}

fn describe_named_artifacts_tap_gain_control_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [default_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if segment.starts_new_source_line
        || branch.starts_new_source_line
        || branch.leading_instead_surface
        || !branch.condition_after_replacement
        || branch.presentation_label.is_some()
    {
        return None;
    }
    let default_effect = unwrap_basic_render_wrapper(default_effect);
    let tap = default_effect.downcast_ref::<crate::effects::TapEffect>()?;
    if !tap.target.is_target() || !tap.target.is_single() {
        return None;
    }
    let [replacement] = branch.replacement_effects.as_slice() else {
        return None;
    };
    let replacement = unwrap_basic_render_wrapper(replacement);
    let apply = replacement.downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let replacement_target = apply.target_spec.as_ref()?;
    if apply.target != crate::continuous::EffectTarget::Source
        || apply.until != Until::Forever
        || apply.modification.is_some()
        || !apply.additional_modifications.is_empty()
        || apply.condition.is_some()
        || apply.runtime_modifications.as_slice()
            != [crate::effects::continuous::RuntimeModification::ChangeControllerToEffectController]
        || apply.lock_filter_at_resolution
        || apply.resolve_set_pt_values_at_resolution
        || apply.require_creature_target
        || !target_specs_select_same_objects(&tap.target, replacement_target)
    {
        return None;
    }
    let [left_name, right_name] = named_artifact_control_condition(&branch.condition)?;
    Some(format!(
        "Tap target creature. Gain control of that creature instead if you control artifacts named {} and {}",
        super::normalize_common::title_case_card_name_fragment(&left_name),
        super::normalize_common::title_case_card_name_fragment(&right_name),
    ))
}

fn describe_named_artifacts_damage_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [default_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if segment.starts_new_source_line
        || branch.starts_new_source_line
        || branch.leading_instead_surface
        || !branch.condition_after_replacement
        || branch.presentation_label.is_some()
    {
        return None;
    }
    let [replacement_effect] = branch.replacement_effects.as_slice() else {
        return None;
    };
    let (Some(default_source), default_damage) = damage_with_source_view(default_effect)? else {
        return None;
    };
    let (Some(replacement_source), replacement_damage) =
        damage_with_source_view(replacement_effect)?
    else {
        return None;
    };
    let exact_artifact_source = |source: &ChooseSpec| {
        matches!(source.unhinted(), ChooseSpec::Source)
            && source.source_reference_surface()
                == Some(&crate::target::SourceReferenceSurface::ThisPermanentType(
                    "this artifact".to_string(),
                ))
    };
    if !exact_artifact_source(default_source)
        || !exact_artifact_source(replacement_source)
        || default_damage.amount.unhinted() != &Value::Fixed(1)
        || replacement_damage.amount.unhinted() != &Value::Fixed(3)
        || default_damage.target != ChooseSpec::PlayerOrPlaneswalker(PlayerFilter::Any)
        || replacement_damage.target != default_damage.target
        || default_damage.source_is_combat
        || replacement_damage.source_is_combat
        || default_damage.unpreventable
        || replacement_damage.unpreventable
    {
        return None;
    }
    let [left_name, right_name] = named_artifact_control_condition(&branch.condition)?;
    Some(format!(
        "This artifact deals 1 damage to target player or planeswalker. It deals 3 damage instead if you control artifacts named {} and {}",
        super::normalize_common::title_case_card_name_fragment(&left_name),
        super::normalize_common::title_case_card_name_fragment(&right_name),
    ))
}

pub(super) fn resolution_uses_named_artifacts_damage_replacement(
    program: &crate::resolution::ResolutionProgram,
) -> bool {
    matches!(
        program.segments.as_slice(),
        [segment] if describe_named_artifacts_damage_replacement(segment).is_some()
    )
}

#[cfg(test)]
mod named_artifacts_damage_replacement_tests {
    use super::*;

    fn named_artifact(name: &str) -> Condition {
        Condition::PlayerControls {
            player: PlayerFilter::You,
            filter: ObjectFilter::artifact()
                .controlled_by(PlayerFilter::You)
                .named(name),
        }
    }

    fn segment(replacement_target: ChooseSpec) -> crate::resolution::ResolutionSegment {
        let source = ChooseSpec::Source.with_surface_hint(
            crate::target::ChooseSpecSurfaceHint::SourceReference(
                crate::target::SourceReferenceSurface::ThisPermanentType(
                    "this artifact".to_string(),
                ),
            ),
        );
        let target = ChooseSpec::PlayerOrPlaneswalker(PlayerFilter::Any);
        let mut branch = crate::resolution::SelfReplacementBranch::new(
            Condition::And(
                Box::new(named_artifact("crown of empires")),
                Box::new(named_artifact("throne of empires")),
            ),
            vec![Effect::new(crate::effects::ExecuteWithSourceEffect::new(
                source.clone(),
                Effect::deal_damage(Value::Fixed(3), replacement_target),
            ))],
        );
        branch.condition_after_replacement = true;
        crate::resolution::ResolutionSegment {
            default_effects: vec![Effect::new(crate::effects::ExecuteWithSourceEffect::new(
                source,
                Effect::deal_damage(Value::Fixed(1), target),
            ))],
            self_replacements: vec![branch],
            starts_new_source_line: false,
        }
    }

    #[test]
    fn exact_named_artifact_damage_upgrade_uses_shared_source_and_target() {
        assert_eq!(
            describe_named_artifacts_damage_replacement(&segment(
                ChooseSpec::PlayerOrPlaneswalker(PlayerFilter::Any)
            ))
            .as_deref(),
            Some(
                "This artifact deals 1 damage to target player or planeswalker. It deals 3 damage instead if you control artifacts named Crown of Empires and Throne of Empires"
            )
        );
    }

    #[test]
    fn named_artifact_damage_upgrade_rejects_a_changed_target() {
        assert!(
            describe_named_artifacts_damage_replacement(&segment(ChooseSpec::AnyTarget)).is_none()
        );
    }
}

fn describe_single_self_replacement_segment(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    if segment.self_replacements.len() != 1 || segment.default_effects.is_empty() {
        return None;
    }
    let branch = &segment.self_replacements[0];
    if let Some(rendered) =
        super::render_effects::describe_single_counter_target_self_replacement(segment)
    {
        return Some(rendered);
    }
    if let Some(rendered) = describe_each_player_choice_complement_chooser_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) =
        describe_player_or_planeswalker_and_controlled_creature_damage_upgrade(segment)
    {
        return Some(rendered);
    }
    if let Some(rendered) = describe_trailing_same_target_fixed_pt_self_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_repeated_search_count_self_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_additive_search_slots_shared_disposition_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_named_artifacts_tap_gain_control_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_named_artifacts_damage_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_copy_token_quoted_ability_self_replacement(segment) {
        return Some(rendered);
    }
    let raw_condition_text = super::normalize_common::describe_condition(&branch.condition);
    if let Some(rendered) = describe_copy_count_replacement_with_shared_plural_retarget(
        &segment.default_effects,
        &branch.replacement_effects,
        &raw_condition_text,
        branch.leading_instead_surface,
    ) {
        return Some(rendered);
    }
    let compact_default = describe_conjoined_same_source_damage(&segment.default_effects);
    let compact_replacement = describe_conjoined_same_source_damage(&branch.replacement_effects);
    if let (Some(default_text), Some(replacement_text)) = (&compact_default, &compact_replacement) {
        let condition_text = normalize_target_quality_condition(default_text, &raw_condition_text);
        return Some(format_self_replacement_fallback(
            default_text,
            &condition_text,
            &super::normalize_common::lowercase_first(replacement_text),
            branch.leading_instead_surface,
        ));
    }
    if let Some(rendered) = describe_scry_then_same_draw_self_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_looked_and_or_destination_self_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_looked_count_self_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_trailing_same_source_damage_self_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_trailing_plural_any_target_damage_self_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_shared_target_damage_self_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_target_threshold_paid_cost_self_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_targeted_search_count_self_replacement(segment) {
        return Some(rendered);
    }
    if let Some(rendered) = describe_reveal_hand_choose_discard_self_replacement(segment) {
        return Some(rendered);
    }
    let conditional = Effect::conditional(
        branch.condition.clone(),
        branch.replacement_effects.clone(),
        segment.default_effects.clone(),
    );
    let conditional_text = describe_effect_list(&[conditional]);
    let loses_repeated_set_target =
        conditional_text
            .split_once(". ")
            .is_some_and(|(default, replacement)| {
                default.contains(" damage to each ")
                    && replacement.contains(" damage instead if ")
                    && !replacement.contains(" damage to each ")
            });
    if conditional_text.contains(" damage instead if ") && !loses_repeated_set_target {
        return Some(conditional_text);
    }
    let default_text =
        compact_default.unwrap_or_else(|| describe_effect_list(&segment.default_effects));
    let replacement_text =
        compact_replacement.unwrap_or_else(|| describe_effect_list(&branch.replacement_effects));
    let condition_text = normalize_target_quality_condition(&default_text, &raw_condition_text);
    if let Some(return_text) = describe_same_target_hand_to_battlefield_replacement(
        &segment.default_effects,
        &branch.replacement_effects,
        &default_text,
        &condition_text,
        branch.condition_after_replacement,
    ) {
        return Some(return_text);
    }
    if let Some(search_destination_text) =
        describe_shared_search_hand_to_battlefield_self_replacement(
            &segment.default_effects,
            &branch.replacement_effects,
            &default_text,
            &condition_text,
        )
    {
        return Some(search_destination_text);
    }
    if let Some(looked_cards_text) = describe_looked_cards_non_hand_self_replacement(
        &segment.default_effects,
        &branch.replacement_effects,
        &condition_text,
    ) {
        return Some(looked_cards_text);
    }
    if let Some(local_rewrite_text) = describe_rendered_optional_zone_rewrite_self_replacement(
        &segment.default_effects,
        &branch.replacement_effects,
        &default_text,
        &replacement_text,
        &condition_text,
        branch.condition_after_replacement,
    ) {
        return Some(local_rewrite_text);
    }
    if let Some(count_override_text) = describe_rendered_count_override_self_replacement(
        &default_text,
        &replacement_text,
        &condition_text,
    ) {
        return Some(count_override_text);
    }
    if let Some(mill_override_text) = describe_mill_count_override_self_replacement(
        &segment.default_effects,
        &branch.replacement_effects,
        &default_text,
        &replacement_text,
        &condition_text,
    ) {
        return Some(mill_override_text);
    }
    if let Some(draw_discard_text) = describe_target_player_draw_discard_self_replacement(
        &segment.default_effects,
        &branch.replacement_effects,
        &condition_text,
    ) {
        return Some(draw_discard_text);
    }
    if let Some(phase_out_text) =
        describe_phase_out_exile_self_replacement(&default_text, &replacement_text, &condition_text)
    {
        return Some(phase_out_text);
    }
    if let Some(gets_text) =
        describe_rendered_gets_self_replacement(&default_text, &replacement_text, &condition_text)
    {
        return Some(gets_text);
    }
    if let Some(destroy_text) = describe_tagged_target_set_destroy_self_replacement(
        &segment.default_effects,
        &branch.replacement_effects,
        &default_text,
        &condition_text,
    ) {
        return Some(destroy_text);
    }
    if let Some(damage_text) =
        describe_rendered_damage_self_replacement(&default_text, &replacement_text, &condition_text)
    {
        return Some(damage_text);
    }
    if let Some(counter_unless_text) = describe_counter_unless_self_replacement(
        &segment.default_effects,
        &branch.replacement_effects,
        &default_text,
        &condition_text,
    ) {
        return Some(counter_unless_text);
    }
    if let Some(counter_bonus_text) = describe_counter_bonus_self_replacement(
        &segment.default_effects,
        &branch.replacement_effects,
        &default_text,
        &condition_text,
    ) {
        return Some(counter_bonus_text);
    }
    if let Some(token_count_text) = describe_same_token_count_self_replacement(
        &segment.default_effects,
        &branch.replacement_effects,
        &default_text,
        &condition_text,
    ) {
        return Some(token_count_text);
    }
    if let Some(token_life_text) = describe_token_life_self_replacement(
        &segment.default_effects,
        &branch.replacement_effects,
        &condition_text,
    ) {
        return Some(token_life_text);
    }
    if let Some(void_text) = describe_void_self_replacement(
        &segment.default_effects,
        &branch.replacement_effects,
        &branch.condition,
        &condition_text,
    ) {
        return Some(void_text);
    }
    if let Some(shared_shuffle_text) = describe_shared_terminal_shuffle_self_replacement(
        &default_text,
        &replacement_text,
        &condition_text,
    ) {
        return Some(shared_shuffle_text);
    }
    if !branch.condition_after_replacement
        && let Some(shared_target_text) = describe_shared_target_self_replacement(
            &segment.default_effects,
            &branch.replacement_effects,
            &default_text,
            &replacement_text,
            &branch.condition,
            &condition_text,
            branch.leading_instead_surface,
        )
    {
        return Some(shared_target_text);
    }
    let mut replacement =
        rewrite_self_replacement_referent_phrase(&default_text, &replacement_text);
    if condition_text.starts_with("that creature is ")
        && replacement.starts_with("that creature gets ")
    {
        replacement = replacement.replacen("that creature", "it", 1);
    }
    if condition_text.starts_with("that creature has ") {
        if replacement.starts_with("that creature gets ") {
            replacement = replacement.replacen("that creature", "it", 1);
        } else if replacement.starts_with("that creature you control gets ") {
            replacement = replacement.replacen("that creature you control", "it", 1);
        }
    }
    Some(format_self_replacement_fallback(
        &default_text,
        &condition_text,
        &replacement,
        branch.leading_instead_surface,
    ))
}

/// Compact a kicked one-card library search whose replacement searches for a
/// repeated number of the same filter. The outer shared tag proves the slots
/// are the replacement for the original searched card rather than additional
/// independent searches.
fn describe_repeated_search_count_self_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [default_root] = segment.default_effects.as_slice() else {
        return None;
    };
    let default_tagged = default_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let default = default_tagged
        .effect
        .downcast_ref::<crate::effects::SearchLibraryEffect>()?;
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    let [replacement_root] = branch.replacement_effects.as_slice() else {
        return None;
    };
    let replacement_tagged = replacement_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let replacement = replacement_tagged
        .effect
        .downcast_ref::<crate::effects::SearchLibrarySlotsEffect>()?;
    if !matches!(
        branch.condition,
        Condition::ThisSpellWasKicked
            | Condition::TurnHistory(ironsmith_core::TurnHistoryCondition::SourceWasKicked { .. })
    ) || branch.presentation_label.is_some()
        || branch.condition_after_replacement
        || branch.leading_instead_surface
        || branch.starts_new_source_line
        || default_tagged.tag != replacement_tagged.tag
        || replacement.slots.len() < 2
        || replacement
            .slots
            .iter()
            .any(|slot| slot.filter != default.filter)
        || default.destination != replacement.destination
        || default.chooser != replacement.chooser
        || default.player != replacement.player
        || default.reveal != replacement.reveal
        || default.destination != Zone::Hand
        || default.chooser != PlayerFilter::You
        || default.player != PlayerFilter::You
        || !default.reveal
        || default.search_mode != crate::effect::SearchSelectionMode::Exact
        || default.library_position_from_top.is_some()
        || default.result_reference_surface != ironsmith_core::SearchResultReferenceSurface::It
    {
        return None;
    }

    let default_text = describe_effect_list(&segment.default_effects);
    // The search action already supplies "your library".  Keep the zone in
    // the executable filter, but omit the redundant location when the same
    // filter is reused as the replacement's noun phrase.
    let mut described_filter = default.filter.clone();
    described_filter.zone = None;
    let description = described_filter.description();
    let noun = strip_leading_article(&description);
    let count = number_word(replacement.slots.len() as i32)
        .unwrap_or_else(|| replacement.slots.len().to_string());
    Some(format!(
        "{}. If this spell was kicked, search your library for {count} {} instead of one",
        default_text.trim_end_matches('.'),
        pluralize_noun_phrase(noun)
    ))
}

/// Share one reveal/move/shuffle disposition across an additive library-search
/// self replacement.
///
/// The replacement must search for every default slot plus exactly one new
/// slot, using the same player, chooser, progress tag, destination, and reveal
/// semantics. This proves that only the searched collection changes and lets
/// the common disposition remain after both search sentences.
fn describe_additive_search_slots_shared_disposition_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [default_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let default = default_effect.downcast_ref::<crate::effects::SearchLibrarySlotsEffect>()?;
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    let [replacement_effect] = branch.replacement_effects.as_slice() else {
        return None;
    };
    let replacement =
        replacement_effect.downcast_ref::<crate::effects::SearchLibrarySlotsEffect>()?;
    if branch.condition != Condition::ThisSpellWasKicked
        || branch.presentation_label.is_some()
        || branch.condition_after_replacement
        || !branch.leading_instead_surface
        || branch.starts_new_source_line
        || default.slots.is_empty()
        || replacement.slots.len() != default.slots.len() + 1
        || !replacement.slots.starts_with(&default.slots)
        || replacement.destination != default.destination
        || replacement.chooser != default.chooser
        || replacement.player != default.player
        || replacement.reveal != default.reveal
        || replacement.progress_tag != default.progress_tag
        || default.destination != Zone::Hand
        || default.chooser != PlayerFilter::You
        || default.player != PlayerFilter::You
        || !default.reveal
    {
        return None;
    }

    const SHARED_DISPOSITION: &str = ", reveal those cards, put them into your hand, then shuffle";
    let default_text = describe_effect_list(&segment.default_effects);
    let replacement_text = describe_effect_list(&branch.replacement_effects);
    let default_search = default_text.strip_suffix(SHARED_DISPOSITION)?;
    let replacement_search = replacement_text.strip_suffix(SHARED_DISPOSITION)?;
    Some(format!(
        "{default_search}. If this spell was kicked, instead {}. Reveal those cards, put them into your hand, then shuffle",
        super::normalize_common::lowercase_first(replacement_search)
    ))
}

#[cfg(test)]
mod additive_search_slots_shared_disposition_tests {
    use super::*;

    fn slot(filter: ObjectFilter) -> crate::effects::SearchLibrarySlot {
        crate::effects::SearchLibrarySlot::optional(
            filter.in_zone(Zone::Library).owned_by(PlayerFilter::You),
        )
    }

    fn segment(
        reveal: bool,
        replacement_destination: Zone,
    ) -> crate::resolution::ResolutionSegment {
        let mut basic_land_filter =
            ObjectFilter::land().with_supertype(crate::types::Supertype::Basic);
        basic_land_filter.set_explicit_card_noun(true);
        let basic_land = slot(basic_land_filter);
        let shrine = slot(ObjectFilter::default().with_subtype(Subtype::Shrine));
        let progress = TagKey::from("search_library_slots_progress");
        let default = crate::effects::SearchLibrarySlotsEffect::new(
            vec![basic_land.clone()],
            Zone::Hand,
            PlayerFilter::You,
            PlayerFilter::You,
            reveal,
            progress.clone(),
        );
        let replacement = crate::effects::SearchLibrarySlotsEffect::new(
            vec![basic_land, shrine],
            replacement_destination,
            PlayerFilter::You,
            PlayerFilter::You,
            reveal,
            progress,
        );
        let mut branch = crate::resolution::SelfReplacementBranch::new(
            Condition::ThisSpellWasKicked,
            vec![Effect::new(replacement)],
        );
        branch.leading_instead_surface = true;
        crate::resolution::ResolutionSegment {
            default_effects: vec![Effect::new(default)],
            self_replacements: vec![branch],
            starts_new_source_line: false,
        }
    }

    #[test]
    fn shares_the_exact_reveal_hand_and_shuffle_disposition() {
        assert_eq!(
            describe_additive_search_slots_shared_disposition_replacement(&segment(
                true,
                Zone::Hand,
            )),
            Some(
                "Search your library for a basic land card. If this spell was kicked, instead search your library for a basic land card and a Shrine card. Reveal those cards, put them into your hand, then shuffle"
                    .to_string()
            )
        );
    }

    #[test]
    fn parsed_additive_kicked_search_uses_the_shared_disposition_surface() {
        let oracle = "Kicker {2}\nSearch your library for a basic land card. If this spell was kicked, instead search your library for a basic land card and a Shrine card. Reveal those cards, put them into your hand, then shuffle.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Additive Search Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(oracle)
        .expect("the public additive kicked search should parse");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition).join("\n"),
            oracle
        );
    }

    #[test]
    fn rejects_a_changed_reveal_or_destination() {
        assert_eq!(
            describe_additive_search_slots_shared_disposition_replacement(&segment(
                false,
                Zone::Hand,
            )),
            None
        );
        assert_eq!(
            describe_additive_search_slots_shared_disposition_replacement(&segment(
                true,
                Zone::Battlefield,
            )),
            None
        );
    }
}

/// Render an amount-only upgrade of the same two announced recipients: a
/// player-or-planeswalker target and a creature controlled by that recipient.
/// The guard requires both replacement damage arms to reuse the exact default
/// target specs; this prevents a merely similar second clause from acquiring
/// cross-target wording.
fn describe_player_or_planeswalker_and_controlled_creature_damage_upgrade(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    fn sequence_damage_pair(
        effects: &[Effect],
    ) -> Option<(
        &crate::effects::DealDamageEffect,
        &TagKey,
        &crate::effects::DealDamageEffect,
    )> {
        let [sequence_effect] = effects else {
            return None;
        };
        let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
        if sequence.surface != ironsmith_core::SequenceSurface::Coordinated {
            return None;
        }
        let [first_effect, second_effect] = sequence.effects.as_slice() else {
            return None;
        };
        let (first_source, first) = damage_with_source_view(first_effect)?;
        let tagged = second_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
        let (second_source, second) = damage_with_source_view(&tagged.effect)?;
        if first_source.is_some()
            || second_source.is_some()
            || first.source_is_combat
            || first.unpreventable
            || second.source_is_combat
            || second.unpreventable
        {
            return None;
        }
        Some((first, &tagged.tag, second))
    }

    fn exact_controlled_creature_target(spec: &ChooseSpec) -> bool {
        if !spec.is_target() || !spec.is_single() {
            return false;
        }
        let ChooseSpec::Object(filter) = spec.base() else {
            return false;
        };
        let mut normalized = filter.clone();
        normalized.source_surface = None;
        normalized.union_surface = Default::default();
        let mut expected = ObjectFilter::creature();
        expected.controller = Some(PlayerFilter::TargetPlayerOrControllerOfTarget);
        normalized == expected
    }

    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if branch.condition_after_replacement
        || branch.leading_instead_surface
        || branch
            .presentation_label
            .as_ref()
            .and_then(crate::ability::PresentationLabel::display_prefix)
            .as_deref()
            != Some("Landfall")
        || !matches!(
            branch.condition,
            Condition::PlayerHadLandEnterBattlefieldThisTurn {
                player: PlayerFilter::You
            }
        )
    {
        return None;
    }
    let (default_first, default_tag, default_second) =
        sequence_damage_pair(&segment.default_effects)?;
    let (replacement_first, replacement_tag, replacement_second) =
        sequence_damage_pair(&branch.replacement_effects)?;
    if default_tag != replacement_tag
        || default_first.amount.unhinted() != &Value::Fixed(1)
        || default_second.amount.unhinted() != &Value::Fixed(1)
        || replacement_first.amount.unhinted() != &Value::Fixed(3)
        || replacement_second.amount.unhinted() != &Value::Fixed(3)
        || default_first.target != ChooseSpec::PlayerOrPlaneswalker(PlayerFilter::Any)
        || replacement_first.target != default_first.target
        || replacement_second.target != default_second.target
        || !exact_controlled_creature_target(&default_second.target)
    {
        return None;
    }

    Some(
        "Deal 1 damage to target player or planeswalker and 1 damage to target creature that player or that planeswalker's controller controls. If you had a land enter the battlefield under your control this turn, deal 3 damage to that player or planeswalker and 3 damage to that creature instead"
            .to_string(),
    )
}

/// Preserve an authored reveal/choose/discard replacement as one comma-then
/// clause. Sentence lowering may keep the replacement inside a typed
/// `CommaThen` sequence and synthesize an implicit target declaration in both
/// branches; neither scaffold is an authored action. The existing inline
/// matcher still proves the revealed hand, chooser, selected-card tag,
/// discard player, and count before this bridge changes presentation.
fn describe_reveal_hand_choose_discard_self_replacement(
    segment: &crate::resolution::ResolutionSegment,
) -> Option<String> {
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if branch.condition_after_replacement {
        return None;
    }

    let default_visible = segment
        .default_effects
        .iter()
        .map(structural_unwrap_render_wrappers)
        .filter(|effect| {
            effect
                .downcast_ref::<crate::effects::TargetOnlyEffect>()
                .is_none_or(|target| target.explicit_declaration)
        })
        .collect::<Vec<_>>();
    let [default_effect] = default_visible.as_slice() else {
        return None;
    };
    default_effect.downcast_ref::<crate::effects::DiscardEffect>()?;

    let replacement_effects = match branch.replacement_effects.as_slice() {
        [sequence_effect] => {
            if let Some(sequence) = structural_unwrap_render_wrappers(sequence_effect)
                .downcast_ref::<crate::effects::SequenceEffect>()
            {
                if sequence.surface != ironsmith_core::SequenceSurface::CommaThen {
                    return None;
                }
                sequence.effects.as_slice()
            } else {
                branch.replacement_effects.as_slice()
            }
        }
        effects => effects,
    };
    let replacement_visible = replacement_effects
        .iter()
        .map(structural_unwrap_render_wrappers)
        .filter(|effect| {
            effect
                .downcast_ref::<crate::effects::TargetOnlyEffect>()
                .is_none_or(|target| target.explicit_declaration)
        })
        .collect::<Vec<_>>();
    let inline = describe_reveal_hand_choose_discard_inline(&replacement_visible)?;
    let default_text = describe_effect_list(&segment.default_effects);
    let condition_text = super::normalize_common::describe_condition(&branch.condition);
    let rendered = format!(
        "{default_text}. If {condition_text}, instead {}",
        super::normalize_common::lowercase_first(&inline)
    );
    Some(super::normalize_common::capitalize_first(&rendered))
}

/// Recognize a complete search/reveal/move/shuffle replacement pipeline whose
/// only semantic change is the searched collection's destination.  The full
/// structural comparison is intentional: it lets compiled text express the
/// local "instead of putting them into your hand" replacement without hiding
/// a changed selector, count, reveal, tag, or shuffle instruction.
fn describe_shared_search_hand_to_battlefield_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    default_text: &str,
    condition_text: &str,
) -> Option<String> {
    struct Pipeline<'a> {
        choose: &'a crate::effects::ChooseObjectsEffect,
        reveal: &'a crate::effects::RevealTaggedEffect,
        for_each_tag: &'a TagKey,
        move_wrapper_tag: Option<&'a TagKey>,
        move_to_zone: &'a crate::effects::MoveToZoneEffect,
        shuffle: &'a crate::effects::ShuffleLibraryEffect,
    }

    fn move_with_wrapper_tag(
        effect: &Effect,
    ) -> Option<(Option<&TagKey>, &crate::effects::MoveToZoneEffect)> {
        if let Some(tagged) = effect.downcast_ref::<crate::effects::TaggedEffect>() {
            return Some((
                Some(&tagged.tag),
                tagged
                    .effect
                    .downcast_ref::<crate::effects::MoveToZoneEffect>()?,
            ));
        }
        Some((
            None,
            effect.downcast_ref::<crate::effects::MoveToZoneEffect>()?,
        ))
    }

    fn pipeline(effects: &[Effect]) -> Option<Pipeline<'_>> {
        let [
            choose_effect,
            reveal_effect,
            for_each_effect,
            shuffle_effect,
        ] = effects
        else {
            return None;
        };
        let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
        let reveal = reveal_effect.downcast_ref::<crate::effects::RevealTaggedEffect>()?;
        let for_each = for_each_effect.downcast_ref::<crate::effects::ForEachTaggedEffect>()?;
        let [move_effect] = for_each.effects.as_slice() else {
            return None;
        };
        let (move_wrapper_tag, move_to_zone) = move_with_wrapper_tag(move_effect)?;
        let shuffle = shuffle_effect.downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;
        if !choose.is_search
            || reveal.tag != choose.tag
            || for_each.tag != choose.tag
            || !matches!(move_to_zone.target.base(), ChooseSpec::Tagged(tag) if tag == &choose.tag)
        {
            return None;
        }
        Some(Pipeline {
            choose,
            reveal,
            for_each_tag: &for_each.tag,
            move_wrapper_tag,
            move_to_zone,
            shuffle,
        })
    }

    let default = pipeline(default_effects)?;
    let replacement = pipeline(replacement_effects)?;
    let mut replacement_move_at_default_destination = replacement.move_to_zone.clone();
    replacement_move_at_default_destination.zone = default.move_to_zone.zone;
    if default.choose != replacement.choose
        || default.reveal != replacement.reveal
        || default.for_each_tag != replacement.for_each_tag
        || default.move_wrapper_tag != replacement.move_wrapper_tag
        || default.shuffle != replacement.shuffle
        || default.move_to_zone != &replacement_move_at_default_destination
        || default.move_to_zone.zone != Zone::Hand
        || replacement.move_to_zone.zone != Zone::Battlefield
    {
        return None;
    }

    Some(format!(
        "{}. If {condition_text}, put those cards onto the battlefield instead of putting them into your hand",
        default_text.trim().trim_end_matches('.')
    ))
}

fn describe_same_target_hand_to_battlefield_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    default_text: &str,
    condition_text: &str,
    condition_after_replacement: bool,
) -> Option<String> {
    let [default_effect] = default_effects else {
        return None;
    };
    let [replacement_effect] = replacement_effects else {
        return None;
    };
    let return_to_hand = unwrap_basic_render_wrapper(default_effect)
        .downcast_ref::<crate::effects::ReturnFromGraveyardToHandEffect>()?;
    let replacement_effect = unwrap_basic_render_wrapper(replacement_effect);
    let (replacement_target, tapped, verb) = if let Some(return_to_battlefield) =
        replacement_effect.downcast_ref::<crate::effects::ReturnFromGraveyardToBattlefieldEffect>()
    {
        if return_to_battlefield.as_aura.is_some()
            || !return_to_battlefield.enters_with_counters.is_empty()
        {
            return None;
        }
        (
            &return_to_battlefield.target,
            return_to_battlefield.tapped,
            "return",
        )
    } else if let Some(move_to_zone) =
        replacement_effect.downcast_ref::<crate::effects::MoveToZoneEffect>()
    {
        if move_to_zone.zone != Zone::Battlefield
            || move_to_zone.to_top
            || move_to_zone.library_order.is_some()
            || move_to_zone.target_plural_surface
            || move_to_zone.actor_surface.is_some()
            || move_to_zone.destination_player_surface.is_some()
            || move_to_zone.destination_player_reference_surface.is_some()
            || move_to_zone.exiled_with_source_surface.is_some()
            || move_to_zone.battlefield_controller
                != crate::effects::BattlefieldController::Preserve
            || move_to_zone.controller_surface_explicit
            || !move_to_zone.enters_with_counters.is_empty()
            || move_to_zone.enters_attacking
            || move_to_zone.attack_target_mode.is_some()
            || move_to_zone.enters_face_down
            || move_to_zone.enters_transformed
            || move_to_zone.transfer_exiled_with_source_links
        {
            return None;
        }
        let verb = match move_to_zone.verb_surface {
            ironsmith_core::MoveToZoneVerbSurface::Put => "put",
            ironsmith_core::MoveToZoneVerbSurface::Return => "return",
            _ => return None,
        };
        (&move_to_zone.target, move_to_zone.enters_tapped, verb)
    } else {
        return None;
    };
    if !return_to_hand.target.is_target()
        || !return_to_hand.target.is_single()
        || !replacement_target.is_target()
        || !replacement_target.is_single()
        || !target_specs_select_same_objects(&return_to_hand.target, replacement_target)
    {
        return None;
    }
    let destination = if verb == "put" {
        "onto the battlefield"
    } else {
        "to the battlefield"
    };
    let replacement = format!(
        "{verb} that card {destination}{} instead",
        if tapped { " tapped" } else { "" }
    );
    if condition_after_replacement {
        Some(format!(
            "{default_text}. {} if {condition_text}",
            capitalize_first(&replacement)
        ))
    } else {
        Some(format!(
            "{default_text}. If {condition_text}, {replacement}"
        ))
    }
}

fn describe_shared_terminal_shuffle_self_replacement(
    default_text: &str,
    replacement_text: &str,
    condition_text: &str,
) -> Option<String> {
    fn ends_with_shuffle(text: &str) -> bool {
        text.trim_end_matches('.')
            .to_ascii_lowercase()
            .ends_with("then shuffle")
    }

    if !ends_with_shuffle(default_text) || !ends_with_shuffle(replacement_text) {
        return None;
    }

    Some(format!(
        "{default_text}. If {condition_text}, instead {}",
        super::normalize_common::lowercase_first(replacement_text)
    ))
}

fn describe_phase_out_exile_self_replacement(
    default_text: &str,
    replacement_text: &str,
    condition_text: &str,
) -> Option<String> {
    let target = default_text.strip_prefix("Phase out ")?;
    let replacement_target = replacement_text.strip_prefix("Exile ")?;
    if !target.eq_ignore_ascii_case(replacement_target) {
        return None;
    }
    let condition = condition_text
        .strip_prefix("it's a ")
        .and_then(|quality| quality.strip_suffix(" permanent"))
        .map(|quality| format!("that permanent is {quality}"))
        .unwrap_or_else(|| condition_text.to_string());
    Some(format!(
        "{} phases out. If {condition}, exile it instead",
        capitalize_first(target)
    ))
}

fn normalize_target_quality_condition(default_text: &str, condition_text: &str) -> String {
    let default_lower = default_text.to_ascii_lowercase();
    // `Condition::TargetMatches` deliberately renders without assuming which
    // instruction introduced the target ("the target is ..."). Inside a
    // self-replacement segment the default and replacement actions have
    // already proved their shared target identity, so normalize that generic
    // surface through the same path as the older implicit-object spelling.
    let target_condition = condition_text
        .strip_prefix("the target is a ")
        .map(|quality| format!("it's a {quality}"))
        .or_else(|| {
            condition_text
                .strip_prefix("the target is an ")
                .map(|quality| format!("it's an {quality}"))
        });
    let condition_text = target_condition.as_deref().unwrap_or(condition_text);
    if default_lower.contains("target creature") && condition_text == "it's a blocking permanent" {
        return "it's blocking".to_string();
    }
    if default_lower.contains("target creature") && condition_text.starts_with("it's an ") {
        return condition_text.to_string();
    }
    if default_lower.contains("target creature")
        && let Some(quality) = condition_text.strip_prefix("that creature is a ")
    {
        if let Some(property) = quality
            .strip_prefix("creature with ")
            .or_else(|| quality.strip_prefix("permanent with "))
        {
            return format!("that creature has {property}");
        }
        if let Some(colors) = quality.strip_suffix(" permanent")
            && colors
                .split_whitespace()
                .all(|word| word == "or" || is_color_quality_word(word))
        {
            return format!("that creature is {colors}");
        }
        if quality.ends_with(" creature") {
            return format!("it's {}", with_indefinite_article(quality));
        }
    }
    if let Some(quality) = condition_text
        .strip_suffix(" was dealt damage this way")
        .and_then(|subject| {
            subject
                .strip_prefix("a ")
                .or_else(|| subject.strip_prefix("an "))
        })
    {
        let target_subject = if default_lower.contains("target creature or planeswalker")
            || default_lower.contains("target red creature or planeswalker")
            || default_lower.contains("target permanent")
        {
            Some("that permanent")
        } else if default_lower.contains("target creature") {
            Some("that creature")
        } else if default_lower.contains("any target") {
            Some("it")
        } else {
            None
        };
        if let Some(subject) = target_subject {
            if let Some(property) = quality
                .strip_prefix("creature with ")
                .or_else(|| quality.strip_prefix("permanent with "))
            {
                return format!("{subject} has {property}");
            }
            if let Some(colors) = quality.strip_suffix(" permanent")
                && colors
                    .split_whitespace()
                    .all(|word| word == "or" || is_color_quality_word(word))
            {
                return format!("{subject} is {colors}");
            }
            if subject == "that creature" && quality.ends_with(" creature") {
                return format!("it's {}", with_indefinite_article(quality));
            }
            return format!("{subject} is {}", with_indefinite_article(quality));
        }
    }

    let mut creature_quality = None;
    if let Some(quality) = condition_text.strip_prefix("it's a ") {
        creature_quality = Some(quality);
    }
    let Some(quality) = condition_text
        .strip_prefix("it's a ")
        .and_then(|rest| rest.strip_suffix(" permanent"))
    else {
        if default_lower.contains("target")
            && default_lower.contains("creature")
            && let Some(quality) = creature_quality
        {
            if let Some(rest) = quality.strip_prefix("creature with ") {
                return format!("that creature has {rest}");
            }
            if let Some(rest) = quality.strip_prefix("permanent with ") {
                return format!("that creature has {rest}");
            }
            if quality == "blocking permanent" {
                return "it's blocking".to_string();
            }
            if quality.ends_with(" creature") {
                return condition_text.to_string();
            }
            if default_lower.starts_with("target creature gets")
                || default_lower.starts_with("target creature you control gets")
            {
                return condition_text.to_string();
            }
            return format!("that creature is a {quality}");
        }
        return condition_text.to_string();
    };
    if !quality.split(" or ").all(is_color_quality_word) {
        return condition_text.to_string();
    }
    let subject = if default_lower.contains("target creature or planeswalker")
        || default_lower.contains("target permanent")
    {
        "that permanent"
    } else if default_lower.contains("target creature") {
        "that creature"
    } else {
        return condition_text.to_string();
    };
    format!("{subject} is {quality}")
}

fn is_color_quality_word(word: &str) -> bool {
    matches!(
        word.trim(),
        "white" | "blue" | "black" | "red" | "green" | "colorless" | "multicolored"
    )
}

fn describe_void_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    condition: &Condition,
    condition_text: &str,
) -> Option<String> {
    if !is_void_condition(condition) {
        return None;
    }
    let replacement_clause = describe_void_replacement_clause(replacement_effects)?;
    Some(format!(
        "{}. Void — If {condition_text}, instead {replacement_clause}",
        describe_effect_list(default_effects)
    ))
}

fn is_void_condition(condition: &Condition) -> bool {
    let Condition::Or(left, right) = condition else {
        return false;
    };
    (matches!(
        left.as_ref(),
        Condition::NonlandPermanentLeftBattlefieldThisTurn
    ) && matches!(right.as_ref(), Condition::SpellWasWarpedThisTurn))
        || (matches!(left.as_ref(), Condition::SpellWasWarpedThisTurn)
            && matches!(
                right.as_ref(),
                Condition::NonlandPermanentLeftBattlefieldThisTurn
            ))
}

fn describe_void_replacement_clause(effects: &[Effect]) -> Option<String> {
    if let [draw_effect, for_players_effect] = effects {
        let draw = unwrap_basic_render_wrapper(draw_effect)
            .downcast_ref::<crate::effects::DrawCardsEffect>()?;
        let for_players = unwrap_basic_render_wrapper(for_players_effect)
            .downcast_ref::<crate::effects::ForPlayersEffect>()?;
        let [inner_effect] = for_players.effects.as_slice() else {
            return None;
        };
        let lose = unwrap_basic_render_wrapper(inner_effect)
            .downcast_ref::<crate::effects::LoseLifeEffect>()?;
        if matches!(
            draw.player,
            PlayerFilter::You | PlayerFilter::EffectController
        ) && for_players.filter == PlayerFilter::Opponent
            && matches!(
                lose.player,
                ChooseSpec::Player(PlayerFilter::IteratedPlayer)
            )
        {
            return Some(format!(
                "you draw {} and each opponent loses {} life",
                describe_card_count(&draw.count),
                describe_value(&lose.amount)
            ));
        }
    }

    let mut rendered = super::normalize_common::lowercase_first(
        describe_effect_list(effects).trim_end_matches('.'),
    )
    .replace(". Each ", " and each ")
    .replace(". each ", " and each ");
    if rendered.starts_with("draw ") {
        rendered = format!("you {rendered}");
    }
    Some(rendered)
}

fn describe_looked_cards_non_hand_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    condition_text: &str,
) -> Option<String> {
    if condition_text != "this spell was cast from anywhere other than your hand" {
        return None;
    }

    let [default_look, choose, move_chosen, remainder] = default_effects else {
        return None;
    };
    let [replacement_look, move_all] = replacement_effects else {
        return None;
    };

    let default_look = default_look.downcast_ref::<crate::effects::LookAtTopCardsEffect>()?;
    let replacement_look =
        replacement_look.downcast_ref::<crate::effects::LookAtTopCardsEffect>()?;
    if default_look != replacement_look
        || default_look.reveal
        || default_look.player != PlayerFilter::You
    {
        return None;
    }

    let choose = choose.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    if choose.count.min != 1
        || choose.count.max != Some(1)
        || choose.chooser != PlayerFilter::You
        || choose.filter.zone != Some(Zone::Library)
        || !choose
            .filter
            .tagged_constraints
            .iter()
            .any(|constraint| constraint.tag == default_look.tag)
    {
        return None;
    }

    let move_chosen =
        unwrap_tagged_effect(move_chosen).downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    if move_chosen.zone != Zone::Hand
        || !matches!(&move_chosen.target, ChooseSpec::Tagged(tag) if tag == &choose.tag)
    {
        return None;
    }

    let remainder =
        remainder.downcast_ref::<crate::effects::PutTaggedRemainderOnLibraryBottomEffect>()?;
    if remainder.tag != default_look.tag
        || remainder.keep_tagged.as_ref() != Some(&choose.tag)
        || remainder.player != PlayerFilter::You
    {
        return None;
    }

    let move_all =
        unwrap_tagged_effect(move_all).downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    if move_all.zone != Zone::Hand
        || !matches!(&move_all.target, ChooseSpec::Tagged(tag) if tag == &default_look.tag)
    {
        return None;
    }

    let count_text = describe_value(&default_look.count);
    let order_suffix = match remainder.order {
        crate::effects::consult_helpers::LibraryBottomOrder::ChooserChooses => " in any order",
        crate::effects::consult_helpers::LibraryBottomOrder::Random => " in a random order",
    };
    Some(format!(
        "Look at the top {count_text} cards of your library. Put one of those cards into your hand and the rest on the bottom of your library{order_suffix}. If {condition_text}, put each of those cards into your hand instead"
    ))
}

fn exact_tagged_create_token(
    effects: &[Effect],
) -> Option<(&TagKey, &crate::effects::CreateTokenEffect)> {
    let [effect] = effects else {
        return None;
    };
    let tagged = effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let create = tagged
        .effect
        .downcast_ref::<crate::effects::CreateTokenEffect>()?;
    Some((&tagged.tag, create))
}

fn exact_tagged_create_token_copy(
    effects: &[Effect],
) -> Option<(&TagKey, &crate::effects::CreateTokenCopyEffect)> {
    let [effect] = effects else {
        return None;
    };
    let tagged = effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let create = tagged
        .effect
        .downcast_ref::<crate::effects::CreateTokenCopyEffect>()?;
    Some((&tagged.tag, create))
}

fn same_token_creation_except_count(
    default: &crate::effects::CreateTokenEffect,
    replacement: &crate::effects::CreateTokenEffect,
) -> bool {
    describe_create_token_blueprint(default) == describe_create_token_blueprint(replacement)
        && default.controller == replacement.controller
        && default.controller_target == replacement.controller_target
        && default.use_source_chosen_color == replacement.use_source_chosen_color
        && default.use_source_chosen_creature_type == replacement.use_source_chosen_creature_type
        && default.actor_surface_explicit == replacement.actor_surface_explicit
        && default.suppress_aura_attachment_choice == replacement.suppress_aura_attachment_choice
        && default.ability_presentation == replacement.ability_presentation
        && default.enters_tapped == replacement.enters_tapped
        && default.enters_attacking == replacement.enters_attacking
        && default.attack_target_mode == replacement.attack_target_mode
        && default.exile_at_end_of_combat == replacement.exile_at_end_of_combat
        && default.sacrifice_at_end_of_combat == replacement.sacrifice_at_end_of_combat
        && default.sacrifice_at_next_end_step == replacement.sacrifice_at_next_end_step
        && default.exile_at_next_end_step == replacement.exile_at_next_end_step
        && default.next_end_step_player == replacement.next_end_step_player
}

/// A self-replacement that only increases the count of the exact same tagged
/// token payload refers back to the authored token blueprint instead of
/// repeating it. The shared result tag is also the executable provenance edge
/// used by later `those tokens` consumers.
fn describe_same_token_count_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    default_text: &str,
    condition_text: &str,
) -> Option<String> {
    let replacement_count = if let (
        Some((default_tag, default_create)),
        Some((replacement_tag, replacement_create)),
    ) = (
        exact_tagged_create_token(default_effects),
        exact_tagged_create_token(replacement_effects),
    ) {
        let Value::Fixed(1) = default_create.count.unhinted() else {
            return None;
        };
        let Value::Fixed(replacement_count) = replacement_create.count.unhinted() else {
            return None;
        };
        if *replacement_count <= 1
            || default_tag != replacement_tag
            || !same_token_creation_except_count(default_create, replacement_create)
        {
            return None;
        }
        *replacement_count
    } else {
        let (default_tag, default_create) = exact_tagged_create_token_copy(default_effects)?;
        let (replacement_tag, replacement_create) =
            exact_tagged_create_token_copy(replacement_effects)?;
        let Value::Fixed(1) = default_create.count.unhinted() else {
            return None;
        };
        let Value::Fixed(replacement_count) = replacement_create.count.unhinted() else {
            return None;
        };
        let mut normalized_default = default_create.clone();
        normalized_default.count = Value::Fixed(0);
        let mut normalized_replacement = replacement_create.clone();
        normalized_replacement.count = Value::Fixed(0);
        if *replacement_count <= 1
            || default_tag != replacement_tag
            || normalized_default != normalized_replacement
        {
            return None;
        }
        *replacement_count
    };
    let count = number_word(replacement_count).unwrap_or_else(|| replacement_count.to_string());
    Some(format!(
        "{default_text}. If {condition_text}, create {count} of those tokens instead"
    ))
}

fn describe_token_life_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    condition_text: &str,
) -> Option<String> {
    let default_create = token_life_create_effect(default_effects)?;
    let replacement_create = token_life_create_effect(replacement_effects)?;
    if !same_token_life_replacement_token(default_create, replacement_create) {
        return None;
    }

    let default_clause = describe_token_life_clause(default_effects, false)?;
    let replacement_clause = describe_token_life_clause(replacement_effects, true)?;
    Some(format!(
        "{default_clause}. If {condition_text}, instead {replacement_clause}"
    ))
}

fn token_life_create_effect(effects: &[Effect]) -> Option<&crate::effects::CreateTokenEffect> {
    let [create_effect, gain_effect] = effects else {
        return None;
    };
    let create =
        unwrap_tagged_effect(create_effect).downcast_ref::<crate::effects::CreateTokenEffect>()?;
    let gain =
        unwrap_tagged_effect(gain_effect).downcast_ref::<crate::effects::GainLifeEffect>()?;
    if create.controller != PlayerFilter::You
        || create.controller_target.is_some()
        || create.enters_attacking
        || create.exile_at_end_of_combat
        || create.sacrifice_at_end_of_combat
        || create.sacrifice_at_next_end_step
        || create.exile_at_next_end_step
        || gain.player != ChooseSpec::Player(PlayerFilter::You)
    {
        return None;
    }
    Some(create)
}

fn same_token_life_replacement_token(
    default_create: &crate::effects::CreateTokenEffect,
    replacement_create: &crate::effects::CreateTokenEffect,
) -> bool {
    describe_create_token_blueprint(default_create)
        == describe_create_token_blueprint(replacement_create)
        && default_create.enters_tapped == replacement_create.enters_tapped
}

fn describe_token_life_clause(effects: &[Effect], refer_to_prior_token: bool) -> Option<String> {
    let [create_effect, gain_effect] = effects else {
        return None;
    };
    let create =
        unwrap_tagged_effect(create_effect).downcast_ref::<crate::effects::CreateTokenEffect>()?;
    let gain =
        unwrap_tagged_effect(gain_effect).downcast_ref::<crate::effects::GainLifeEffect>()?;
    let created = if refer_to_prior_token {
        format!("{} of those tokens", describe_value(&create.count))
    } else {
        describe_create_token_amount(create)
    };
    Some(format!(
        "create {created} and you gain {} life",
        describe_value(&gain.amount)
    ))
}

fn describe_create_token_amount(create: &crate::effects::CreateTokenEffect) -> String {
    let mut token = describe_create_token_blueprint(create);
    if create.enters_tapped {
        token = format!("tapped {token}");
    }
    match create.count.unhinted() {
        Value::Fixed(1) => format!("a {}", singular_token_phrase(&token)),
        Value::Fixed(n) => {
            let count = number_word(*n).unwrap_or_else(|| n.to_string());
            format!("{count} {}", plural_token_phrase(&token))
        }
        _ => format!(
            "{} {}",
            describe_value(&create.count),
            plural_token_phrase(&token)
        ),
    }
}

fn singular_token_phrase(token: &str) -> String {
    if token.ends_with(" token") {
        token.to_string()
    } else {
        format!("{token} token")
    }
}

fn plural_token_phrase(token: &str) -> String {
    if let Some(stem) = token.strip_suffix(" token") {
        format!("{stem} tokens")
    } else {
        format!("{token} tokens")
    }
}

fn unwrap_tagged_effect(mut effect: &Effect) -> &Effect {
    while let Some(tagged) = effect.downcast_ref::<crate::effects::TaggedEffect>() {
        effect = &tagged.effect;
    }
    effect
}

fn counter_effect_target(effect: &Effect) -> Option<&ChooseSpec> {
    unwrap_tagged_effect(effect)
        .downcast_ref::<crate::effects::CounterEffect>()
        .map(|counter| &counter.target)
}

fn unless_pays_counter_target(effect: &Effect) -> Option<&ChooseSpec> {
    let unless_pays =
        unwrap_tagged_effect(effect).downcast_ref::<crate::effects::UnlessPaysEffect>()?;
    let [counter_effect] = unless_pays.effects.as_slice() else {
        return None;
    };
    counter_effect_target(counter_effect)
}

fn counter_replacement_referent(target: &ChooseSpec) -> Option<&'static str> {
    let target_text = super::normalize_common::describe_choose_spec(target).to_ascii_lowercase();
    if target_text.contains(" spell or ability") || target_text.contains(" ability or spell") {
        Some("that spell or ability")
    } else if target_text.contains(" ability") && !target_text.contains(" spell") {
        Some("that ability")
    } else if target_text.contains(" spell") {
        Some("that spell")
    } else {
        None
    }
}

fn describe_counter_unless_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    default_text: &str,
    condition_text: &str,
) -> Option<String> {
    let [default_effect] = default_effects else {
        return None;
    };
    let replacement_counter_target = counter_effect_target(replacement_effects.first()?)?;
    let default_counter_target = unless_pays_counter_target(default_effect)?;
    if replacement_counter_target != default_counter_target {
        return None;
    }

    let referent = counter_replacement_referent(default_counter_target)?;
    let mut replacement_text = format!("instead counter {referent}");
    if replacement_effects.len() > 1 {
        let followup_text = describe_effect_list(&replacement_effects[1..]);
        replacement_text.push_str(", then ");
        replacement_text.push_str(&super::normalize_common::lowercase_first(&followup_text));
    }

    Some(format!(
        "{default_text}. If {condition_text}, {replacement_text}"
    ))
}

fn normalize_counter_bonus_condition(condition_text: &str) -> String {
    for prefix in [
        "it's a permanent that targets ",
        "it's a spell that targets ",
        "it targets ",
    ] {
        if let Some(rest) = condition_text.strip_prefix(prefix) {
            return format!(
                "that spell targets {}",
                rest.replace("commander permanent", "commander")
            );
        }
    }
    condition_text.to_string()
}

fn describe_counter_bonus_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    default_text: &str,
    condition_text: &str,
) -> Option<String> {
    let [default_effect] = default_effects else {
        return None;
    };
    let default_counter_target = counter_effect_target(default_effect)?;
    let replacement_counter_target = counter_effect_target(replacement_effects.first()?)?;
    if replacement_counter_target != default_counter_target {
        return None;
    }

    let referent = counter_replacement_referent(default_counter_target)?;
    let condition = normalize_counter_bonus_condition(condition_text);
    let mut replacement_text = format!("instead counter {referent}");
    if replacement_effects.len() > 1 {
        let followup_text = describe_effect_list(&replacement_effects[1..]);
        replacement_text.push_str(", ");
        replacement_text.push_str(&super::normalize_common::lowercase_first(&followup_text));
    }

    Some(format!(
        "{default_text}. If {condition}, {replacement_text}"
    ))
}

fn rewrite_self_replacement_referent_phrase(default_text: &str, replacement_text: &str) -> String {
    let mut replacement = super::normalize_common::lowercase_first(replacement_text);
    if default_text
        .to_ascii_lowercase()
        .contains("target creature")
    {
        for prefix in ["put ", "you may put "] {
            let needle = format!("{prefix}target creature on");
            if replacement.starts_with(&needle) {
                replacement = replacement.replacen("target creature", "it", 1);
                break;
            }
        }
    }
    if default_text
        .to_ascii_lowercase()
        .contains("target permanent")
    {
        for prefix in ["put ", "you may put "] {
            let needle = format!("{prefix}target permanent on");
            if replacement.starts_with(&needle) {
                replacement = replacement.replacen("target permanent", "it", 1);
                break;
            }
        }
    }
    if default_text
        .to_ascii_lowercase()
        .contains("target creature")
        && replacement.starts_with("target creature ")
    {
        replacement = replacement.replacen("target creature", "that creature", 1);
    }
    if default_text.to_ascii_lowercase().contains("target player")
        && replacement.starts_with("target player ")
    {
        replacement = replacement.replacen("target player", "that player", 1);
    }
    if default_text
        .to_ascii_lowercase()
        .contains("target opponent")
        && replacement.starts_with("target opponent ")
    {
        replacement = replacement.replacen("target opponent", "that opponent", 1);
    }
    let default_lower = default_text.to_ascii_lowercase();
    if default_lower.contains("counter target")
        && default_lower.contains("spell unless")
        && replacement == "counter those spells"
    {
        replacement = "counter that spell".to_string();
    }
    replacement
}

fn describe_rendered_optional_zone_rewrite_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    default_text: &str,
    replacement_text: &str,
    condition_text: &str,
    condition_after_replacement: bool,
) -> Option<String> {
    let typed_single_search_rewrite = (|| {
        let [default_effect] = default_effects else {
            return None;
        };
        let searched = default_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
        let search = searched
            .effect
            .downcast_ref::<crate::effects::SearchLibraryEffect>()?;
        let [replacement_effect] = replacement_effects else {
            return None;
        };
        let may = replacement_effect.downcast_ref::<crate::effects::MayEffect>()?;
        if may.decider.as_ref() != Some(&PlayerFilter::You) {
            return None;
        }
        let [move_effect] = may.effects.as_slice() else {
            return None;
        };
        let moved = move_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
        let move_to_zone = moved
            .effect
            .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
        if search.destination != Zone::Hand
            || search.chooser != PlayerFilter::You
            || search.player != PlayerFilter::You
            || !search.reveal
            || move_to_zone.zone != Zone::Battlefield
            || !matches!(
                move_to_zone.target.unhinted(),
                ChooseSpec::Tagged(tag) if tag == &searched.tag
            )
        {
            return None;
        }
        Some(())
    })()
    .is_some();
    let returned_subset_rewrite = (|| {
        let [default_effect] = default_effects else {
            return None;
        };
        let returned = default_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
        let return_effect = unwrap_tagged_effect(&returned.effect)
            .downcast_ref::<crate::effects::ReturnFromGraveyardToHandEffect>()?;
        if return_effect.target.count().max.is_none_or(|max| max <= 1) {
            return None;
        }

        let [replacement_effect] = replacement_effects else {
            return None;
        };
        let may =
            unwrap_tagged_effect(replacement_effect).downcast_ref::<crate::effects::MayEffect>()?;
        if may.decider.as_ref() != Some(&PlayerFilter::You) {
            return None;
        }
        let [move_effect] = may.effects.as_slice() else {
            return None;
        };
        let move_to_zone =
            unwrap_tagged_effect(move_effect).downcast_ref::<crate::effects::MoveToZoneEffect>()?;
        if move_to_zone.zone != Zone::Battlefield || !move_to_zone.target.count().is_single() {
            return None;
        }
        let ChooseSpec::Object(filter) = move_to_zone.target.base() else {
            return None;
        };
        let [constraint] = filter.tagged_constraints.as_slice() else {
            return None;
        };
        if constraint.tag != returned.tag
            || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
        {
            return None;
        }

        let normalized_replacement =
            super::normalize_common::normalize_common_semantic_phrasing(replacement_text);
        let replacement = normalized_replacement.trim().trim_end_matches('.');
        let qualifier = [
            "You may put a card",
            "you may put a card",
            "You may put that card",
            "you may put that card",
        ]
        .iter()
        .find_map(|prefix| replacement.strip_prefix(prefix))?
        .strip_suffix(" onto the battlefield")?;
        Some(format!(
            "You may put one of those cards{qualifier} onto the battlefield instead of putting it into your hand"
        ))
    })();
    let replacement_lower = replacement_text
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    let appended_local_rewrite = replacement_text
        .strip_prefix(default_text)
        .is_some_and(|suffix| suffix.contains("you may put it into battlefield instead"));
    let standalone_local_rewrite = matches!(
        replacement_lower.as_str(),
        "you may put it onto the battlefield"
            | "you may put it into battlefield"
            | "you may put it onto battlefield"
    );
    if !typed_single_search_rewrite
        && !appended_local_rewrite
        && !standalone_local_rewrite
        && returned_subset_rewrite.is_none()
    {
        return None;
    }
    let lower_default = default_text.to_ascii_lowercase();
    let replacement_clause =
        if lower_default.starts_with("search your library for a basic land card") {
            "You may put that card onto the battlefield instead of putting it into your hand"
                .to_string()
        } else {
            returned_subset_rewrite?
        };
    Some(if condition_after_replacement {
        format!("{default_text}. {replacement_clause} if {condition_text}")
    } else {
        format!(
            "{default_text}. If {condition_text}, {}",
            super::normalize_common::lowercase_first(&replacement_clause)
        )
    })
}

fn describe_rendered_count_override_self_replacement(
    default_text: &str,
    replacement_text: &str,
    condition_text: &str,
) -> Option<String> {
    let default_sentences: Vec<_> = default_text.trim_end_matches('.').split(". ").collect();
    let replacement_sentences: Vec<_> =
        replacement_text.trim_end_matches('.').split(". ").collect();
    // Optional single-card selections are commonly rendered as "You may put
    // a ..." while the larger replacement count is rendered as "Put up to
    // two ...".  They are the same count override even though neither branch
    // uses the literal "Put one" / "Put two" surface handled below.
    if let [default_intro, default_choice, default_rest] = default_sentences.as_slice()
        && let [replacement_intro, replacement_choice_and_rest] = replacement_sentences.as_slice()
        && default_intro == replacement_intro
        && let Some(default_selection) = default_choice.strip_prefix("You may put ")
        && let Some((_, default_destination)) = default_selection.split_once(" from among them")
        && let Some(replacement_choice) = replacement_choice_and_rest
            .strip_prefix("Put up to ")
            .map(|_| *replacement_choice_and_rest)
        && let Some((replacement_choice, replacement_rest)) =
            replacement_choice.split_once(" and the rest ")
        && let Some((_, replacement_destination)) =
            replacement_choice.split_once(" from among them")
        && default_destination == replacement_destination
        && default_rest.strip_prefix("Put the rest ") == Some(replacement_rest)
    {
        return Some(format!(
            "{default_intro}. {default_choice}. If {condition_text}, {} instead of one. {default_rest}",
            super::normalize_common::lowercase_first(replacement_choice)
        ));
    }
    if let (
        [default_intro, default_choice, default_rest],
        [replacement_intro, replacement_choice, replacement_rest],
    ) = (
        default_sentences.as_slice(),
        replacement_sentences.as_slice(),
    ) && default_intro == replacement_intro
        && default_rest == replacement_rest
        && let Some(default_selection) = default_choice.strip_prefix("You may put ")
        && let Some((_, default_destination)) = default_selection.split_once(" from among them")
        && replacement_choice.starts_with("Put up to ")
        && let Some((_, replacement_destination)) =
            replacement_choice.split_once(" from among them")
        && default_destination == replacement_destination
    {
        return Some(format!(
            "{default_intro}. {default_choice}. If {condition_text}, {} instead of one. {default_rest}",
            super::normalize_common::lowercase_first(replacement_choice)
        ));
    }
    if let ([default_intro, default_choice], [replacement_intro, replacement_choice]) = (
        default_sentences.as_slice(),
        replacement_sentences.as_slice(),
    ) && default_intro == replacement_intro
        && default_choice.starts_with("Put one ")
        && replacement_choice.starts_with("Put two ")
        && let Some((default_choice_hand, default_rest)) =
            default_choice.split_once(" and the rest ")
        && let Some((replacement_choice_hand, replacement_rest)) =
            replacement_choice.split_once(" and the rest ")
        && default_rest == replacement_rest
    {
        let default_choice_hand = default_choice_hand.replace("of them", "of those cards");
        let replacement_choice_hand = replacement_choice_hand.replace("of them", "of those cards");
        return Some(format!(
            "{default_intro}. {default_choice_hand}. If {condition_text}, {} instead. Put the rest {default_rest}",
            super::normalize_common::lowercase_first(&replacement_choice_hand)
        ));
    }
    let [default_intro, default_choice, default_rest] = default_sentences.as_slice() else {
        return None;
    };
    let [replacement_intro, replacement_choice, replacement_rest] =
        replacement_sentences.as_slice()
    else {
        return None;
    };
    if default_intro != replacement_intro
        || default_rest != replacement_rest
        || !default_choice.starts_with("Put one ")
        || !replacement_choice.starts_with("Put two ")
    {
        return None;
    }
    Some(format!(
        "{default_intro}. {default_choice}. If {condition_text}, {} instead. {default_rest}",
        super::normalize_common::lowercase_first(replacement_choice)
    ))
}

fn describe_mill_count_override_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    default_text: &str,
    replacement_text: &str,
    condition_text: &str,
) -> Option<String> {
    let default_mill = single_mill_effect(default_effects)?;
    let replacement_mill = single_mill_effect(replacement_effects)?;
    if default_mill.player != replacement_mill.player {
        return None;
    }
    if !mill_count_is_twice_default(&default_mill.count, &replacement_mill.count) {
        return None;
    }

    let replacement_count = super::normalize_common::describe_value(&replacement_mill.count);
    let mut replacement = rewrite_self_replacement_referent_phrase(default_text, replacement_text);
    replacement = replacement.trim_end_matches('.').replace(
        &format!("{replacement_count} cards"),
        "twice that many cards",
    );
    Some(format!(
        "{default_text}. If {condition_text}, {replacement} instead"
    ))
}

fn mill_count_is_twice_default(default_count: &Value, replacement_count: &Value) -> bool {
    matches!(replacement_count, Value::Scaled(inner, 2) if inner.as_ref() == default_count)
        || matches!(
            (default_count, replacement_count),
            (Value::X, Value::XTimes(2))
        )
}

fn single_mill_effect(effects: &[Effect]) -> Option<&crate::effects::MillEffect> {
    let mut found = None;
    for effect in effects {
        let Some(mill) =
            unwrap_basic_render_wrapper(effect).downcast_ref::<crate::effects::MillEffect>()
        else {
            continue;
        };
        if found.is_some() {
            return None;
        }
        found = Some(mill);
    }
    found
}

fn unwrap_basic_render_wrapper(effect: &Effect) -> &Effect {
    if let Some(with_id) = effect.downcast_ref::<crate::effects::WithIdEffect>() {
        return unwrap_basic_render_wrapper(&with_id.effect);
    }
    if let Some(tag_all) = effect.downcast_ref::<crate::effects::TagAllEffect>() {
        return unwrap_basic_render_wrapper(&tag_all.effect);
    }
    if let Some(tagged) = effect.downcast_ref::<crate::effects::TaggedEffect>() {
        return unwrap_basic_render_wrapper(&tagged.effect);
    }
    effect
}

/// Preserve a quantified participant's alternative unless payment before the
/// clause renderer rewrites the iterated player into a singular pronoun and
/// independently conjugates each cost branch.
fn describe_single_quantified_unless_payment_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let [effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let effect = structural_unwrap_render_wrappers(effect);
    let for_players = effect.downcast_ref::<crate::effects::ForPlayersEffect>()?;
    describe_for_players_unless_pays(for_players)
}

fn target_player_draw_discard_counts(effects: &[Effect]) -> Option<(&Value, &Value)> {
    let [target_effect, draw_effect, discard_effect] = effects else {
        return None;
    };
    let target = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if target.target != target_any_player_spec() {
        return None;
    }

    let draw = draw_effect.downcast_ref::<crate::effects::DrawCardsEffect>()?;
    let discard = discard_effect.downcast_ref::<crate::effects::DiscardEffect>()?;
    if draw.player != target_any_player_filter()
        || discard.player != target_any_player_filter()
        || discard.random
        || discard.any_number
        || discard.card_filter.is_some()
    {
        return None;
    }
    Some((&draw.count, &discard.count))
}

fn card_count_noun(count: &Value) -> &'static str {
    if matches!(count.unhinted(), Value::Fixed(1)) {
        "card"
    } else {
        "cards"
    }
}

fn card_count_text(count: &Value) -> String {
    match count.unhinted() {
        Value::Fixed(n) => number_word(*n).unwrap_or_else(|| n.to_string()),
        _ => describe_value(count),
    }
}

fn describe_target_player_draw_discard_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    condition_text: &str,
) -> Option<String> {
    let (default_draw, default_discard) = target_player_draw_discard_counts(default_effects)?;
    let (replacement_draw, replacement_discard) =
        target_player_draw_discard_counts(replacement_effects)?;
    if default_draw != replacement_draw {
        return None;
    }

    let draw_count = card_count_text(default_draw);
    let default_discard_count = card_count_text(default_discard);
    let replacement_discard_count = card_count_text(replacement_discard);
    Some(format!(
        "Target player draws {draw_count} {}, then discards {default_discard_count} {}. If {condition_text}, instead that player draws {draw_count} {}, then discards {replacement_discard_count} {}",
        card_count_noun(default_draw),
        card_count_noun(default_discard),
        card_count_noun(replacement_draw),
        card_count_noun(replacement_discard),
    ))
}

fn describe_rendered_gets_self_replacement(
    default_text: &str,
    replacement_text: &str,
    condition_text: &str,
) -> Option<String> {
    let default_text = default_text.trim().trim_end_matches('.');
    if !default_text.contains(" gets ") {
        return None;
    }
    let replacement_text = replacement_text.trim().trim_end_matches('.');
    let replacement = replacement_text
        .strip_prefix("It gets ")
        .or_else(|| replacement_text.strip_prefix("it gets "))?;
    Some(format!(
        "{default_text}. It gets {replacement} instead if {condition_text}"
    ))
}

fn describe_tagged_target_set_destroy_self_replacement(
    default_effects: &[Effect],
    replacement_effects: &[Effect],
    default_text: &str,
    condition_text: &str,
) -> Option<String> {
    let [default] = default_effects else {
        return None;
    };
    let tagged_damage = default.downcast_ref::<crate::effects::TaggedEffect>()?;
    let damage = tagged_damage
        .effect
        .downcast_ref::<crate::effects::DealDamageEffect>()?;
    let ChooseSpec::WithCount(inner, count) = damage.target.unhinted() else {
        return None;
    };
    if count.max.is_some_and(|max| max <= 1) {
        return None;
    }
    let ChooseSpec::Target(target) = inner.unhinted() else {
        return None;
    };
    let ChooseSpec::Object(target_filter) = target.unhinted() else {
        return None;
    };
    let noun = match target_filter.card_types.as_slice() {
        [CardType::Artifact] => "artifact",
        [CardType::Battle] => "battle",
        [CardType::Creature] => "creature",
        [CardType::Enchantment] => "enchantment",
        [CardType::Land] => "land",
        [CardType::Planeswalker] => "planeswalker",
        _ => "permanent",
    };

    let [replacement] = replacement_effects else {
        return None;
    };
    let destroy =
        unwrap_basic_render_wrapper(replacement).downcast_ref::<crate::effects::DestroyEffect>()?;
    let ChooseSpec::Object(destroy_filter) = destroy.spec.base() else {
        return None;
    };
    if !destroy_filter.tagged_constraints.iter().any(|constraint| {
        constraint.tag == tagged_damage.tag
            && constraint.relation == crate::filter::TaggedOpbjectRelation::IsTaggedObject
    }) {
        return None;
    }

    Some(format!(
        "{default_text}. Destroy each {noun} instead if {condition_text}"
    ))
}

fn describe_rendered_damage_self_replacement(
    default_text: &str,
    replacement_text: &str,
    condition_text: &str,
) -> Option<String> {
    let (base_amount, base_target) = split_rendered_damage(default_text)?;
    let (replacement_amount, replacement_target) = split_rendered_damage(replacement_text)?;
    if base_target != replacement_target {
        return None;
    }
    let repeated_target = if base_target.starts_with("each ") || base_target.starts_with("all ") {
        format!(" to {base_target}")
    } else {
        String::new()
    };
    Some(format!(
        "Deal {base_amount} damage to {base_target}. It deals {replacement_amount} damage{repeated_target} instead if {condition_text}"
    ))
}

fn split_rendered_damage(text: &str) -> Option<(&str, &str)> {
    let text = text.trim().trim_end_matches('.');
    let rest = text.strip_prefix("Deal ")?;
    let (amount, target) = rest.split_once(" damage to ")?;
    if amount.trim().is_empty() || target.trim().is_empty() {
        return None;
    }
    Some((amount.trim(), target.trim()))
}

fn describe_sentence_leading_then_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [setup_segment, then_segment] = program.segments.as_slice() else {
        return None;
    };
    if !setup_segment.self_replacements.is_empty()
        || !then_segment.self_replacements.is_empty()
        || setup_segment.starts_new_source_line
        || !then_segment.starts_new_source_line
    {
        return None;
    }
    let [then_effect] = then_segment.default_effects.as_slice() else {
        return None;
    };
    let sequence = then_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::SentenceLeadingThen
        || sequence.result_label.is_some()
        || sequence.effects.is_empty()
        || !sequence.effects.iter().any(|effect| {
            structural_unwrap_render_wrappers(effect)
                .downcast_ref::<crate::effects::VillainousChoiceEffect>()
                .is_some()
        })
    {
        return None;
    }
    let setup = describe_effect_list(&setup_segment.default_effects);
    let then = describe_effect(then_effect);
    if setup.trim().is_empty() || !then.trim_start().starts_with("Then ") {
        return None;
    }
    Some(format!(
        "{}. {}",
        setup.trim().trim_end_matches('.'),
        then.trim().trim_end_matches('.')
    ))
}

fn describe_cross_segment_search_conditional_destination_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let (search_segment, destination_segment, fallback_segment) = match program.segments.as_slice()
    {
        [search, destination] => (search, destination, None),
        [search, destination, fallback] => (search, destination, Some(fallback)),
        _ => return None,
    };
    if !search_segment.self_replacements.is_empty()
        || !destination_segment.self_replacements.is_empty()
        || fallback_segment.is_some_and(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let [search_effect] = search_segment.default_effects.as_slice() else {
        return None;
    };
    let [destination_effect] = destination_segment.default_effects.as_slice() else {
        return None;
    };
    let may = structural_unwrap_render_wrappers(search_effect)
        .downcast_ref::<crate::effects::MayEffect>()?;
    let mut conditional = structural_unwrap_render_wrappers(destination_effect)
        .downcast_ref::<crate::effects::ConditionalEffect>()?
        .clone();
    if let Some(fallback_segment) = fallback_segment {
        let destination_id = destination_effect
            .downcast_ref::<crate::effects::WithIdEffect>()?
            .id;
        let [fallback_effect] = fallback_segment.default_effects.as_slice() else {
            return None;
        };
        let fallback = fallback_effect.downcast_ref::<crate::effects::IfEffect>()?;
        if fallback.condition != destination_id
            || fallback.predicate != EffectPredicate::DidNotHappen
            || !fallback.else_.is_empty()
            || !conditional.if_false.is_empty()
        {
            return None;
        }
        conditional.if_false = fallback.then.clone();
    }
    super::render_effects::describe_may_search_reveal_shuffle_then_conditional_move(
        may,
        &conditional,
    )
}

/// A few migrated procedures already have complete tag/ID-aware structural
/// rendering in `describe_resolution_program`; only the source-sentence
/// splitter obscures that proof. Rejoin those narrowly identifiable surfaces
/// before splitting. Each phrase below can be emitted only after the existing
/// effect-list renderer has validated the correlated result tag or prior
/// effect metric.
fn describe_correlated_authored_sentence_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if program.segments.len() < 2
        || program
            .segments
            .iter()
            .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    compact_correlated_authored_sentence_surface(&describe_resolution_program(program))
}

fn compact_correlated_authored_sentence_surface(rendered: &str) -> Option<String> {
    let rendered = rendered
        .lines()
        .map(|line| line.trim().trim_end_matches('.'))
        .filter(|line| !line.is_empty())
        .collect::<Vec<_>>()
        .join(". ");
    let reveal_offer_remainder = rendered.starts_with("Reveal the top ")
        && rendered.contains(". For each of those cards, ")
        && rendered.contains(". Then exile the rest");
    let scaled_discard_payment = rendered.starts_with("You discard any number of cards. ")
        && rendered.contains("unless its controller pays ")
        && rendered.contains(" for each card discarded this way");
    let delegated_graveyard_partition = rendered
        .starts_with("Choose up to two target creature cards in your graveyard. ")
        && rendered.contains("An opponent chooses one of them. Return that card to your hand. ")
        && rendered.contains("Return the other to the battlefield under your control. ")
        && rendered.contains("It gains haste. Exile it at the beginning of the next end step");
    (reveal_offer_remainder || scaled_discard_payment || delegated_graveyard_partition)
        .then_some(rendered)
}

fn describe_resolution_program_preserving_source_lines(
    program: &crate::resolution::ResolutionProgram,
) -> String {
    // Recognize the complete extraction graph before generic linked-target
    // compaction splits the counter instruction from its search follow-up.
    if let Some((rendered, consumed)) =
        describe_cross_segment_same_name_extraction_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if program
        .segments
        .iter()
        .all(|segment| segment.self_replacements.is_empty())
        && let Some(rendered) =
            describe_linked_resolution_program(program.flattened_default_effects())
    {
        return rendered;
    }
    if let Some(rendered) =
        describe_player_or_planeswalker_damage_then_controller_discard_program(program)
    {
        return rendered;
    }
    if let Some(rendered) = describe_everybody_lives_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_remove_abilities_then_destroy_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_delegated_search_partition_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_single_quantified_unless_payment_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_exile_creatures_then_per_player_fractal_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_returned_object_set_to_enchantment(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_exile_then_delayed_return(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_removed_counter_scaled_mana(program) {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_each_player_token_characteristic_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_each_player_choose_creature_destroy_complement_window(
            &program.segments,
            0,
        )
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_quantified_choice_collection_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        // The chosen-set producer and its complement sacrifice intentionally
        // occupy separate authored sentences. Prove their shared tag before
        // source-line preservation renders each half in isolation.
        return rendered;
    }
    if let Some(rendered) = describe_target_power_fanout_then_graveyard_refill_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_fervent_alternative_cost_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_alternative_cost_chosen_opponent_treasure_scry(program) {
        return rendered;
    }
    if let Some(rendered) = describe_chosen_type_additional_cost_search_destination(program) {
        return rendered;
    }
    if let Some(rendered) = describe_each_opponent_sacrifice_discard_then_return_draw(program) {
        return rendered;
    }
    // Conditional fight cards announce both targets across an authored
    // sentence boundary, then consume those exact slots in the condition,
    // action, and fight. Match the complete typed program before source-line
    // preservation splits the declarations from their consumers and exposes
    // the internal opposing-target tag.
    if let Some(rendered) = describe_cross_segment_conditional_fight_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_damage_unless_paid_followup_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_unless_payer_followup_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_reveal_hand_choose_shuffle_program(program) {
        return rendered;
    }
    // Haunting Echoes' typed procedure intentionally spans three authored
    // lines. Prove the exiled-card and target-player links across the whole
    // program before source-line preservation hides them from the strict
    // structural renderer.
    if let Some(rendered) = describe_graveyard_exception_same_name_exile_program(program) {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_may_search_conditional_else_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_may_search_conditional_disposition_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some(rendered) =
        describe_cross_segment_consult_battlefield_remainder_graveyard_program(program)
    {
        return rendered;
    }
    if let Some(rendered) = describe_life_lock_protection_phase_out_exile_program(program) {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_iterated_exile_power_damage_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_two_target_characteristic_damage_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_source_exiled_copy_cast_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_shuffle_reveal_repeated_permanent_groups_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_failed_sequence_draw_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_counter_then_keyword_grants_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_gain_control_aura_legal_attach_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_typed_controller_damage_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_choose_two_move_one_put_counters_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some(rendered) = describe_sentence_leading_then_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_search_conditional_destination_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_two_target_creature_fight_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_correlated_authored_sentence_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_chosen_creatures_sacrifice_then_return_tapped_program(program)
    {
        return rendered;
    }
    if let Some(rendered) = describe_reveal_may_put_grant_then_bottom_program(program) {
        return rendered;
    }
    if let Some(rendered) = describe_cross_segment_damage_and_die_replacement_program(program) {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_reveal_optional_cast_decline_bottom_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_treasure_look_exile_permission_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    if let Some((rendered, consumed)) =
        describe_cross_segment_reciprocal_characteristic_damage_window(&program.segments, 0)
        && consumed == program.segments.len()
    {
        return rendered;
    }
    let has_typed_boundary = program.segments.iter().any(|segment| {
        segment.starts_new_source_line
            || segment
                .self_replacements
                .iter()
                .any(|branch| branch.starts_new_source_line)
    });
    if !has_typed_boundary {
        return describe_resolution_program(program);
    }

    fn render_segment_group_with_replacement_boundaries(
        segments: Vec<crate::resolution::ResolutionSegment>,
    ) -> String {
        let program = crate::resolution::ResolutionProgram::new(segments);
        let mut rendered = describe_resolution_program(&program);
        for (segment, branch) in program.segments.iter().flat_map(|segment| {
            segment
                .self_replacements
                .iter()
                .filter(|branch| branch.starts_new_source_line)
                .map(move |branch| (segment, branch))
        }) {
            let marker = if branch.condition_after_replacement {
                format!(
                    "{}. ",
                    describe_effect_list(&segment.default_effects)
                        .trim()
                        .trim_end_matches('.')
                )
            } else {
                branch
                    .presentation_label
                    .as_ref()
                    .and_then(crate::ability::PresentationLabel::display_prefix)
                    .map(|label| format!(". {} — ", label.trim()))
                    .unwrap_or_else(|| ". If ".to_string())
            };
            if branch.condition_after_replacement {
                if let Some(after) = rendered.strip_prefix(&marker) {
                    rendered = format!("{}\n{after}", marker.trim_end().trim_end_matches('.'));
                }
                continue;
            }
            if let Some((before, after)) = rendered.split_once(&marker) {
                let next_line = format!("{}{after}", marker.trim_start_matches(". "));
                rendered = format!("{before}\n{next_line}");
            }
        }
        rendered
    }

    let mut source_lines = Vec::new();
    let mut line_segments = Vec::new();
    for segment in &program.segments {
        if segment.starts_new_source_line && !line_segments.is_empty() {
            source_lines.push(render_segment_group_with_replacement_boundaries(
                std::mem::take(&mut line_segments),
            ));
        }
        let mut segment = segment.clone();
        segment.starts_new_source_line = false;
        line_segments.push(segment);
    }
    if !line_segments.is_empty() {
        source_lines.push(render_segment_group_with_replacement_boundaries(
            line_segments,
        ));
    }
    let rendered = source_lines
        .into_iter()
        .filter(|line| !line.trim().is_empty())
        .map(|line| capitalize_first(&line))
        .collect::<Vec<_>>()
        .join("\n");
    compact_correlated_authored_sentence_surface(&rendered).unwrap_or(rendered)
}

/// Rejoin a chosen creature's power fanout with the adjacent cast-origin
/// refill clause. These are three executable sentences on one authored source
/// line; the typed target tag, damage source/value, recipient exclusion, and
/// graveyard condition make the compaction unambiguous.
fn describe_target_power_fanout_then_graveyard_refill_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [target_segment, damage_segment, conditional_segment] = program.segments.as_slice() else {
        return None;
    };
    if [target_segment, damage_segment, conditional_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }

    let [target_effect] = target_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged_target = target_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let target_only = tagged_target
        .effect
        .downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if !target_only.explicit_declaration
        || target_only.chooser.is_some()
        || describe_choose_spec(&target_only.target) != "target creature you control"
    {
        return None;
    }

    let [damage_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let execute = structural_unwrap_render_wrappers(damage_effect)
        .downcast_ref::<crate::effects::ExecuteWithSourceEffect>()?;
    let damage = execute
        .effect
        .downcast_ref::<crate::effects::DealDamageEffect>()?;
    let exact_other_creature_fanout = match damage.target.unhinted() {
        ChooseSpec::Object(filter)
            if filter.other
                && filter.set_quantifier_surface()
                    == Some(ironsmith_core::SetQuantifierSurface::Each)
                && matches!(filter.tagged_constraints.as_slice(), [constraint]
                    if constraint.tag == tagged_target.tag
                        && constraint.relation
                            == crate::filter::TaggedOpbjectRelation::IsNotTaggedObject) =>
        {
            let mut normalized = filter.clone();
            normalized.other = false;
            describe_damage_target(&ChooseSpec::Object(normalized)) == "each other creature"
        }
        _ => false,
    };
    if damage.source_is_combat
        || damage.unpreventable
        || !matches!(&execute.source, ChooseSpec::Tagged(tag) if tag == &tagged_target.tag)
        || !matches!(damage.amount.unhinted(), Value::PowerOf(spec)
            if matches!(spec.base(), ChooseSpec::Tagged(tag) if tag == &tagged_target.tag))
        || !exact_other_creature_fanout
    {
        return None;
    }

    let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
        return None;
    };
    let conditional = conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    if conditional.condition != Condition::ThisSpellWasCastFromZone(Zone::Graveyard)
        || conditional.surface != ironsmith_core::ConditionalSurface::LeadingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }
    let [sequence_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    let [discard_effect, draw_effect] = sequence.effects.as_slice() else {
        return None;
    };
    let discard = discard_effect.downcast_ref::<crate::effects::DiscardHandEffect>()?;
    let draw = draw_effect.downcast_ref::<crate::effects::DrawCardsEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::Coordinated
        || discard.player != PlayerFilter::You
        || draw.player != PlayerFilter::You
    {
        return None;
    }

    Some(format!(
        "{}. {}. {}",
        describe_effect(target_effect).trim().trim_end_matches('.'),
        capitalize_first(describe_effect(damage_effect).trim().trim_end_matches('.'),),
        capitalize_first(
            describe_effect(conditional_effect)
                .trim()
                .trim_end_matches('.'),
        ),
    ))
}

#[cfg(test)]
mod target_power_fanout_graveyard_refill_tests {
    use super::*;

    #[test]
    fn rejoins_one_authored_line_and_rejects_a_different_cast_origin() {
        let oracle = "Choose target creature you control. It deals damage equal to its power to each other creature. If this spell was cast from a graveyard, discard your hand and draw four cards.\nFlashback {5}{R}{R}";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Tagged Power Fanout Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(oracle)
        .expect("typed target/fanout/refill program should parse");
        let program = definition
            .spell_effect
            .as_ref()
            .expect("sorcery should retain its resolution program");
        let [target_segment, damage_segment, conditional_segment] = program.segments.as_slice()
        else {
            panic!("expected three resolution segments: {program:#?}");
        };
        let [target_effect] = target_segment.default_effects.as_slice() else {
            panic!("expected a single target declaration");
        };
        let tagged_target = target_effect
            .downcast_ref::<crate::effects::TaggedEffect>()
            .expect("target declaration should retain its result tag");
        let [damage_effect] = damage_segment.default_effects.as_slice() else {
            panic!("expected a single source-linked damage effect");
        };
        let execute = structural_unwrap_render_wrappers(damage_effect)
            .downcast_ref::<crate::effects::ExecuteWithSourceEffect>()
            .expect("damage should execute with the chosen creature as source");
        let damage = execute
            .effect
            .downcast_ref::<crate::effects::DealDamageEffect>()
            .expect("source-linked effect should deal damage");
        assert!(matches!(&execute.source, ChooseSpec::Tagged(tag) if tag == &tagged_target.tag));
        assert!(matches!(damage.amount.unhinted(), Value::PowerOf(spec)
            if matches!(spec.base(), ChooseSpec::Tagged(tag) if tag == &tagged_target.tag)));
        assert_eq!(
            describe_effect(damage_effect),
            "that creature deals damage equal to its power to each other creature"
        );
        let [conditional_effect] = conditional_segment.default_effects.as_slice() else {
            panic!("expected a single cast-origin conditional");
        };
        let conditional = conditional_effect
            .downcast_ref::<crate::effects::ConditionalEffect>()
            .expect("third segment should be conditional");
        assert_eq!(
            conditional.condition,
            Condition::ThisSpellWasCastFromZone(Zone::Graveyard)
        );
        assert_eq!(
            describe_target_power_fanout_then_graveyard_refill_program(program).as_deref(),
            Some(
                "Choose target creature you control. That creature deals damage equal to its power to each other creature. If this spell was cast from a graveyard, discard your hand and draw four cards"
            )
        );
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![
                "Choose target creature you control. That creature deals damage equal to its power to each other creature. If this spell was cast from a graveyard, discard your hand and draw four cards.".to_string(),
                "Flashback—{5}{R}{R}.".to_string(),
            ]
        );

        let near_miss = "Choose target creature you control. It deals damage equal to its power to each other creature. If you control an artifact, discard your hand and draw four cards.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Different Cast Origin Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(near_miss)
        .expect("different cast-origin program should still parse");
        let near_miss_program = definition.spell_effect.as_ref().expect("spell program");
        assert!(
            describe_target_power_fanout_then_graveyard_refill_program(near_miss_program).is_none(),
            "a different condition must not acquire the graveyard compaction"
        );
        let rendered = crate::compiled_text::compiled_text_lines(&definition).join(" ");
        assert!(
            rendered.contains("If you control an artifact"),
            "{rendered}"
        );
        assert!(!rendered.contains("cast from a graveyard"), "{rendered}");
    }
}

fn bracket_removed_cleave_words(default_text: &str, cleaved_text: &str) -> Option<String> {
    // This word-level reconstruction is deliberately scoped to one authored
    // instruction. Collapsing whitespace across distinct source lines would
    // discard the line provenance preserved by the card-level renderer.
    if default_text.contains('\n') || cleaved_text.contains('\n') {
        return None;
    }
    let default_words = default_text.split_whitespace().collect::<Vec<_>>();
    let cleaved_words = cleaved_text.split_whitespace().collect::<Vec<_>>();
    if default_words.is_empty()
        || cleaved_words.is_empty()
        || default_words.len() <= cleaved_words.len()
    {
        return None;
    }

    let mut lcs = vec![vec![0usize; cleaved_words.len() + 1]; default_words.len() + 1];
    for default_index in (0..default_words.len()).rev() {
        for cleaved_index in (0..cleaved_words.len()).rev() {
            lcs[default_index][cleaved_index] =
                if cleave_words_match(default_words[default_index], cleaved_words[cleaved_index]) {
                    1 + lcs[default_index + 1][cleaved_index + 1]
                } else {
                    lcs[default_index + 1][cleaved_index].max(lcs[default_index][cleaved_index + 1])
                };
        }
    }
    if lcs[0][0] != cleaved_words.len() {
        return None;
    }

    let mut retained = vec![false; default_words.len()];
    let (mut default_index, mut cleaved_index) = (0usize, 0usize);
    while default_index < default_words.len() && cleaved_index < cleaved_words.len() {
        if cleave_words_match(default_words[default_index], cleaved_words[cleaved_index])
            && lcs[default_index][cleaved_index] == 1 + lcs[default_index + 1][cleaved_index + 1]
        {
            retained[default_index] = true;
            default_index += 1;
            cleaved_index += 1;
        } else if lcs[default_index + 1][cleaved_index] >= lcs[default_index][cleaved_index + 1] {
            default_index += 1;
        } else {
            cleaved_index += 1;
        }
    }

    let mut rendered = Vec::new();
    let mut index = 0usize;
    while index < default_words.len() {
        if retained[index] {
            rendered.push(default_words[index].to_string());
            index += 1;
            continue;
        }
        let start = index;
        while index < default_words.len() && !retained[index] {
            index += 1;
        }
        rendered.push(format!("[{}]", default_words[start..index].join(" ")));
    }
    let mut rendered = rendered.join(" ");
    for punctuation in ['.', '!', '?'] {
        let bracketed_punctuation = format!("{punctuation}]");
        rendered = rendered.replace(&bracketed_punctuation, &format!("]{punctuation}"));
    }
    Some(rendered)
}

fn cleave_words_match(default_word: &str, cleaved_word: &str) -> bool {
    default_word.eq_ignore_ascii_case(cleaved_word)
        || default_word
            .trim_end_matches(['.', '!', '?'])
            .eq_ignore_ascii_case(cleaved_word.trim_end_matches(['.', '!', '?']))
}

fn preserve_cleave_bracket_surface(def: &CardDefinition, default_text: &str) -> Option<String> {
    let cleaved_effects = def
        .alternative_casts
        .iter()
        .find_map(AlternativeCastingMethod::cleave_effects)?;
    let cleaved_text = describe_effect_list(cleaved_effects);
    bracket_removed_cleave_words(default_text, &cleaved_text).map(|text| {
        text.replace('[', CLEAVE_BRACKET_OPEN_SENTINEL)
            .replace(']', CLEAVE_BRACKET_CLOSE_SENTINEL)
    })
}

fn describe_named_spell_shared_target_damage_self_replacement(
    def: &CardDefinition,
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if !(def.card.is_instant() || def.card.is_sorcery())
        || def.card.name.contains(" // ")
        || !def.optional_costs.is_empty()
    {
        return None;
    }
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    let [branch] = segment.self_replacements.as_slice() else {
        return None;
    };
    if branch.condition_after_replacement
        || branch.leading_instead_surface
        || branch.presentation_label.is_some()
    {
        return None;
    }
    let [default_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let [replacement_effect] = branch.replacement_effects.as_slice() else {
        return None;
    };
    let (default_source, default_damage) = damage_with_source_view(default_effect)?;
    let (replacement_source, replacement_damage) = damage_with_source_view(replacement_effect)?;
    let source_is_resolving_spell = |source: Option<&ChooseSpec>| {
        source.is_none()
            || source.is_some_and(|source| matches!(source.unhinted(), ChooseSpec::Source))
    };
    if !source_is_resolving_spell(default_source)
        || !source_is_resolving_spell(replacement_source)
        || default_damage.source_is_combat
        || default_damage.unpreventable
        || replacement_damage.source_is_combat
        || replacement_damage.unpreventable
        || !default_damage.target.is_target()
        || !default_damage.target.is_single()
        || !replacement_damage.target.is_target()
        || !replacement_damage.target.is_single()
        || !target_specs_select_same_objects(&default_damage.target, &replacement_damage.target)
    {
        return None;
    }
    let target_referent = self_replacement_target_referent(&default_damage.target)?;
    if !matches!(target_referent, "that creature" | "that permanent") {
        return None;
    }

    let default_text = rewrite_spell_resolution_damage_source(
        def,
        describe_effect_list(&segment.default_effects)
            .trim()
            .trim_end_matches('.'),
    );
    let raw_condition = super::normalize_common::describe_condition(&branch.condition);
    let condition = normalize_target_quality_condition(&default_text, &raw_condition);
    let trailing_as_cast_condition = matches!(
        &branch.condition,
        Condition::PlayerControls {
            player: PlayerFilter::You,
            filter,
        } if filter.has_as_you_cast_this_turn_surface()
    );
    if trailing_as_cast_condition {
        return Some(format!(
            "{default_text}. {} deals {} damage to {target_referent} instead if {condition}",
            def.card.name,
            super::normalize_common::describe_value(&replacement_damage.amount)
        ));
    }
    let replacement_target = if target_referent == "that creature" {
        " to it"
    } else {
        ""
    };
    Some(format!(
        "{default_text}. If {condition}, {} deals {} damage{replacement_target} instead",
        def.card.name,
        super::normalize_common::describe_value(&replacement_damage.amount)
    ))
}

fn describe_player_or_planeswalker_damage_then_controller_discard_program(
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    let [damage_segment, discard_segment] = program.segments.as_slice() else {
        return None;
    };
    if damage_segment.starts_new_source_line
        || !discard_segment.starts_new_source_line
        || !damage_segment.self_replacements.is_empty()
        || !discard_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [damage_effect] = damage_segment.default_effects.as_slice() else {
        return None;
    };
    let [discard_effect] = discard_segment.default_effects.as_slice() else {
        return None;
    };
    let damage = structural_unwrap_render_wrappers(damage_effect)
        .downcast_ref::<crate::effects::DealDamageEffect>()?;
    let discard = structural_unwrap_render_wrappers(discard_effect)
        .downcast_ref::<crate::effects::DiscardEffect>()?;
    if damage.target != ChooseSpec::PlayerOrPlaneswalker(PlayerFilter::Any)
        || damage.source_is_combat
        || damage.unpreventable
        || discard.player != PlayerFilter::TargetPlayerOrControllerOfTarget
        || discard.random
        || discard.any_number
        || discard.card_filter.is_some()
    {
        return None;
    }

    Some(format!(
        "Deal {} damage to target player or planeswalker\nThat player or that planeswalker's controller discards {} {}",
        super::normalize_common::describe_value(&damage.amount),
        card_count_text(&discard.count),
        card_count_noun(&discard.count),
    ))
}

#[cfg(test)]
mod source_line_program_tests {
    use super::*;

    #[test]
    fn card_level_program_renderer_preserves_authored_line_boundaries() {
        let first = crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(
            crate::effects::DrawCardsEffect::new(1, PlayerFilter::You),
        )]);
        let mut second =
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::gain_life(1)]);
        second.starts_new_source_line = true;
        let program = crate::resolution::ResolutionProgram::new(vec![first, second]);

        assert_eq!(
            describe_resolution_program_preserving_source_lines(&program),
            "You draw a card\nYou gain 1 life"
        );
        assert_eq!(
            typed_resolution_source_lines(
                &program,
                &describe_resolution_program_preserving_source_lines(&program),
            ),
            Some(vec![
                "You draw a card".to_string(),
                "You gain 1 life".to_string(),
            ]),
            "typed program boundaries must become distinct card-level entries"
        );

        let unmarked = crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(
                crate::effects::DrawCardsEffect::new(1, PlayerFilter::You),
            )]),
        ]);
        assert_eq!(
            typed_resolution_source_lines(&unmarked, "You draw a card\nYou gain 1 life"),
            None,
            "an arbitrary embedded newline without typed provenance must not split a card line"
        );
    }

    #[test]
    fn returned_card_mana_value_damage_requires_linked_tag_and_optional_target() {
        let returned_tag = TagKey::from("returned_probe");
        let definition = crate::cards::builders::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Rebirth Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .build();
        let mut returned_filter = ObjectFilter::default()
            .in_zone(Zone::Graveyard)
            .owned_by(PlayerFilter::You);
        returned_filter.card_types = vec![CardType::Artifact, CardType::Creature];
        let returned = Effect::return_from_graveyard_to_hand(ChooseSpec::target(
            ChooseSpec::Object(returned_filter),
        ))
        .tag(returned_tag.clone());
        let program_for = |value_tag: TagKey, count: ChoiceCount| {
            let mut damage_filter = ObjectFilter::default().in_zone(Zone::Battlefield);
            damage_filter.card_types = vec![CardType::Creature, CardType::Planeswalker];
            damage_filter.set_explicit_card_type_noun(Some(CardType::Planeswalker));
            let damage = Effect::deal_damage(
                Value::ManaValueOf(Box::new(ChooseSpec::Tagged(value_tag)))
                    .with_surface_hint(ironsmith_core::ValueSurfaceHint::EqualTo),
                ChooseSpec::target(ChooseSpec::Object(damage_filter)).with_count(count),
            )
            .tag("damaged_probe");
            crate::resolution::ResolutionProgram::new(vec![
                crate::resolution::ResolutionSegment::from_effects(vec![returned.clone()]),
                crate::resolution::ResolutionSegment::from_effects(vec![damage]),
            ])
        };

        let linked = program_for(returned_tag.clone(), ChoiceCount::up_to(1));
        assert_eq!(
            describe_return_then_linked_mana_value_damage(&definition, &linked).as_deref(),
            Some(
                "Return target artifact or creature card from your graveyard to your hand. Rebirth Probe deals damage equal to that card's mana value to up to one target creature or planeswalker"
            )
        );
        assert_eq!(
            describe_return_then_linked_mana_value_damage(
                &definition,
                &program_for(TagKey::from("unrelated"), ChoiceCount::up_to(1)),
            ),
            None,
            "an unrelated value tag must not acquire the returned-card surface"
        );
        assert_eq!(
            describe_return_then_linked_mana_value_damage(
                &definition,
                &program_for(returned_tag, ChoiceCount::exactly(1)),
            ),
            None,
            "a mandatory target must not acquire optional targeting"
        );
    }

    #[test]
    fn cleave_word_diff_brackets_each_removed_source_span() {
        assert_eq!(
            bracket_removed_cleave_words(
                "Search your library for a basic land card, reveal it, put it into your hand, then shuffle",
                "Search your library for a card, put it into your hand, then shuffle",
            )
            .as_deref(),
            Some(
                "Search your library for a [basic land] card, [reveal it,] put it into your hand, then shuffle"
            )
        );
        assert_eq!(
            bracket_removed_cleave_words(
                "Return target nonland permanent you control to its owner's hand",
                "Return target nonland permanent to its owner's hand",
            )
            .as_deref(),
            Some("Return target nonland permanent [you control] to its owner's hand")
        );
        assert_eq!(
            bracket_removed_cleave_words("Draw a card\nGain 2 life", "Draw a card"),
            None,
            "cleave reconstruction must not erase authored source lines"
        );
        assert_eq!(
            bracket_removed_cleave_words(
                "You choose a nonland card from it with mana value 2 or less.",
                "You choose a nonland card from it.",
            )
            .as_deref(),
            Some("You choose a nonland card from it [with mana value 2 or less].")
        );
    }

    #[test]
    fn cleave_card_renderer_preserves_cast_origin_restriction_brackets() {
        let oracle =
            "Cleave {1}{U}{U}\nCounter target spell [that wasn't cast from its owner's hand].";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Wash Away",
        )
        .card_types(vec![CardType::Instant])
        .parse_text(oracle)
        .expect("Wash Away should compile");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition).join("\n"),
            oracle
        );
    }

    #[test]
    fn cleave_card_renderer_preserves_mana_value_restriction_brackets() {
        let oracle = "Cleave {4}{W}{B}\nDestroy all creatures [with mana value 2 or less].";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Path of Peril",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(oracle)
        .expect("Path of Peril should compile");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition).join("\n"),
            oracle
        );
    }

    #[test]
    fn cleave_card_renderer_preserves_other_frozen_restriction_brackets() {
        for (name, card_type, oracle) in [
            (
                "Dig Up",
                CardType::Sorcery,
                "Cleave {1}{B}{B}{G}\nSearch your library for a [basic land] card, [reveal it,] put it into your hand, then shuffle.",
            ),
            (
                "Dread Fugue",
                CardType::Sorcery,
                "Cleave {2}{B}\nTarget player reveals their hand. You choose a nonland card from it [with mana value 2 or less]. That player discards that card.",
            ),
            (
                "Fierce Retribution",
                CardType::Instant,
                "Cleave {5}{W}\nDestroy target [attacking] creature.",
            ),
        ] {
            let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
                crate::ids::CardId::new(),
                name,
            )
            .card_types(vec![card_type])
            .parse_text(oracle)
            .expect("frozen Cleave card should compile");

            assert_eq!(
                crate::compiled_text::compiled_text_lines(&definition).join("\n"),
                oracle,
                "{name}"
            );
        }
    }
}

fn describe_resolution_program_for_card(
    def: &CardDefinition,
    program: &crate::resolution::ResolutionProgram,
) -> String {
    fn restore_standard_mill_reminder(rendered: String) -> String {
        let lower = rendered.to_ascii_lowercase();
        if lower.contains(" mills ")
            && lower.contains("for each creature card put into a graveyard this way")
            && !lower.contains("(to mill a card,")
        {
            format!(
                "{rendered}. {STANDARD_REMINDER_OPEN_SENTINEL}To mill a card, a player puts the top card of their library into their graveyard.{STANDARD_REMINDER_CLOSE_SENTINEL}"
            )
        } else {
            rendered
        }
    }

    if let Some(rendered) = describe_inline_spell_attack_or_block_damage_choice(def, program) {
        return rendered;
    }
    if program.segments.len() == 2
        && let Some(flattened) = flattened_cross_segment_effects(&program.segments)
        && let Some(rendered) = describe_return_then_conditional_animation(&flattened)
    {
        return rendered;
    }
    if let Some(rendered) = describe_return_then_linked_mana_value_damage(def, program) {
        return rendered;
    }
    if let Some(rendered) = describe_named_spell_shared_target_damage_self_replacement(def, program)
    {
        return rendered;
    }
    let has_visible_gift_line = def
        .optional_costs
        .iter()
        .any(|cost| matches!(cost.kind, crate::cost::OptionalCostKind::Gift));
    if !has_visible_gift_line {
        let rendered = describe_resolution_program_preserving_source_lines(program);
        return restore_standard_mill_reminder(rewrite_spell_resolution_damage_source(
            def, &rendered,
        ));
    }

    // Gift setup is represented as a runtime-only resolution segment. Remove
    // only that recognized payload, then render the remaining program as a
    // whole so cross-sentence relationships (target pairs, replacements,
    // consult pipelines, and similar structures) remain visible to the same
    // structural matchers used by non-Gift cards.
    let visible_segments = program
        .segments
        .iter()
        .filter(|segment| !is_hidden_gift_resolution_segment(segment))
        .cloned()
        .collect();
    let visible_program = crate::resolution::ResolutionProgram::new(visible_segments);
    let rendered = describe_resolution_program_preserving_source_lines(&visible_program);
    restore_standard_mill_reminder(rewrite_spell_resolution_damage_source(def, &rendered))
}

/// Preserve the sentence-level resolving choice used by effects such as
/// “this spell deals damage to each attacking creature or ... each blocking
/// creature.” A printed modal spell has no resolving chooser and therefore
/// cannot enter this renderer.
fn describe_inline_spell_attack_or_block_damage_choice(
    def: &CardDefinition,
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if !(def.card.is_instant() || def.card.is_sorcery()) {
        return None;
    }
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let [effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let modal = structural_unwrap_render_wrappers(effect)
        .downcast_ref::<crate::effects::ChooseModeEffect>()?;
    if modal.chooser != Some(PlayerFilter::You)
        || modal.min != Value::Fixed(1)
        || modal.max != Value::Fixed(1)
        || modal.choose_count != Value::Fixed(1)
        || modal.min_choose_count != Value::Fixed(1)
        || modal.allow_repeat
        || modal.random
        || modal.allow_repeated_modes
        || modal.spree
        || modal.tiered
        || !modal.common_prefix_effects.is_empty()
        || modal.common_suffix_effect_count != 0
        || !modal.mode_additional_mana_costs.is_empty()
        || modal.mode_point_costs != [1, 1]
        || modal.disallow_previously_chosen_modes
        || modal.disallow_previously_chosen_modes_this_turn
        || modal.distinct_player_targets_per_mode
        || modal.conditional_mode_range.is_some()
        || modal.presentation_label.is_some()
        || modal.modes.len() != 2
    {
        return None;
    }

    struct Arm<'a> {
        filter: &'a ObjectFilter,
        amount: &'a Value,
        tail: String,
    }

    fn arm(mode: &crate::effect::EffectMode) -> Option<Arm<'_>> {
        if !mode.source_text.trim().is_empty() {
            return None;
        }
        let [effect] = mode.effects.as_slice() else {
            return None;
        };
        let for_each = structural_unwrap_render_wrappers(effect)
            .downcast_ref::<crate::effects::ForEachObject>()?;
        let [damage_effect] = for_each.effects.as_slice() else {
            return None;
        };
        let (source, damage) = damage_with_source_view(damage_effect)?;
        if source.is_some_and(|source| !matches!(source.unhinted(), ChooseSpec::Source))
            || damage.source_is_combat
            || damage.unpreventable
            || !matches!(damage.target.unhinted(), ChooseSpec::Iterated)
        {
            return None;
        }
        let rendered = describe_effect_list(&mode.effects);
        let tail = rendered
            .trim()
            .trim_end_matches('.')
            .strip_prefix("Deal ")?
            .to_string();
        Some(Arm {
            filter: &for_each.filter,
            amount: &damage.amount,
            tail,
        })
    }

    let first = arm(&modal.modes[0])?;
    let second = arm(&modal.modes[1])?;
    if first.amount != second.amount {
        return None;
    }
    let complementary_roles = (first.filter.attacking
        && !first.filter.blocking
        && !second.filter.attacking
        && second.filter.blocking)
        || (!first.filter.attacking
            && first.filter.blocking
            && second.filter.attacking
            && !second.filter.blocking);
    if !complementary_roles {
        return None;
    }
    let mut first_filter = first.filter.clone();
    let mut second_filter = second.filter.clone();
    first_filter.attacking = false;
    first_filter.blocking = false;
    second_filter.attacking = false;
    second_filter.blocking = false;
    if first_filter != second_filter {
        return None;
    }

    let source_name = def
        .card
        .name
        .split_once(" // ")
        .map_or(def.card.name.as_str(), |(front, _)| front);
    Some(format!(
        "{source_name} deals {} or {source_name} deals {}",
        first.tail, second.tail
    ))
}

#[cfg(test)]
mod inline_spell_attack_or_block_damage_choice_tests {
    use super::*;

    fn definition() -> CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Lava Storm",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(
            "Lava Storm deals 2 damage to each attacking creature or Lava Storm deals 2 damage to each blocking creature.",
        )
        .expect("inline attack-or-block damage choice should compile")
    }

    #[test]
    fn resolving_attack_or_block_damage_choice_keeps_inline_spell_surface() {
        let definition = definition();
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition).join("\n"),
            "Lava Storm deals 2 damage to each attacking creature or Lava Storm deals 2 damage to each blocking creature."
        );

        let program = definition
            .spell_effect
            .as_ref()
            .expect("sorcery should have a resolution program");
        let [segment] = program.segments.as_slice() else {
            panic!("expected one resolution segment");
        };
        let [effect] = segment.default_effects.as_slice() else {
            panic!("expected one resolving choice");
        };
        let mut printed_modal = effect
            .downcast_ref::<crate::effects::ChooseModeEffect>()
            .expect("expected typed resolving choice")
            .clone();
        printed_modal.chooser = None;
        let printed_program =
            crate::resolution::ResolutionProgram::from_effects(vec![Effect::new(printed_modal)]);
        assert_eq!(
            describe_inline_spell_attack_or_block_damage_choice(&definition, &printed_program),
            None,
            "a printed modal choice must retain its bullet presentation"
        );
    }
}

/// Compact an exact return-then-damage program whose damage amount is linked
/// to the returned object's tag. The tag is the executable provenance for
/// “that card”; the optional-single target remains part of targeting legality.
fn describe_return_then_linked_mana_value_damage(
    def: &CardDefinition,
    program: &crate::resolution::ResolutionProgram,
) -> Option<String> {
    if !(def.card.is_instant() || def.card.is_sorcery()) || def.card.name.contains(" // ") {
        return None;
    }
    let [return_segment, damage_segment] = program.segments.as_slice() else {
        return None;
    };
    if !return_segment.self_replacements.is_empty() || !damage_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [return_root] = return_segment.default_effects.as_slice() else {
        return None;
    };
    let [damage_root] = damage_segment.default_effects.as_slice() else {
        return None;
    };

    let returned = return_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let return_effect = unwrap_basic_render_wrapper(&returned.effect)
        .downcast_ref::<crate::effects::ReturnFromGraveyardToHandEffect>()?;
    if return_effect.random
        || !return_effect.target.is_target()
        || !return_effect.target.count().is_single()
        || !matches!(
            return_effect.target.base(),
            ChooseSpec::Object(filter) if filter.zone == Some(Zone::Graveyard)
        )
    {
        return None;
    }

    let damage = unwrap_basic_render_wrapper(damage_root)
        .downcast_ref::<crate::effects::DealDamageEffect>()?;
    let Value::ManaValueOf(value_target) = damage.amount.unhinted() else {
        return None;
    };
    if !matches!(
        value_target.base(),
        ChooseSpec::Tagged(tag) if tag == &returned.tag
    ) || damage.source_is_combat
        || damage.unpreventable
        || !damage.target.is_target()
        || damage.target.count() != ChoiceCount::up_to(1)
    {
        return None;
    }
    let ChooseSpec::Object(target_filter) = damage.target.base() else {
        return None;
    };
    let mut semantic_target = target_filter.clone();
    semantic_target.set_explicit_card_type_noun(None);
    let mut expected_target = ObjectFilter::default().in_zone(Zone::Battlefield);
    expected_target.card_types = vec![CardType::Creature, CardType::Planeswalker];
    if semantic_target != expected_target {
        return None;
    }

    let return_text = describe_effect_list(std::slice::from_ref(return_root));
    Some(format!(
        "{}. {} deals damage equal to that card's mana value to {}",
        return_text.trim().trim_end_matches('.'),
        def.card.name,
        describe_damage_target(&damage.target)
    ))
}

/// A direct costed-modal program is one authored source block: its header and
/// every cost-labeled mode are distinct source lines even though they execute
/// through one typed `ChooseModeEffect`.
fn direct_spree_mode_count(program: &crate::resolution::ResolutionProgram) -> Option<usize> {
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let [effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let modal = effect.downcast_ref::<crate::effects::ChooseModeEffect>()?;
    (modal.spree
        && !modal.modes.is_empty()
        && modal.mode_additional_mana_costs.len() == modal.modes.len())
    .then_some(modal.modes.len())
}

fn typed_spree_source_lines(
    program: &crate::resolution::ResolutionProgram,
    rendered: &str,
) -> Option<Vec<String>> {
    let mode_count = direct_spree_mode_count(program)?;
    let lines = rendered
        .lines()
        .map(str::trim)
        .filter(|line| !line.is_empty())
        .map(str::to_string)
        .collect::<Vec<_>>();
    (lines.len() == mode_count + 1).then_some(lines)
}

/// Split a rendered spell program only when its typed resolution segments
/// prove authored source-line boundaries. The program renderer deliberately
/// carries those boundaries as embedded newlines so ability renderers can
/// keep one `String`; the card-level spell collector, however, returns one
/// entry per source line and must lift those proven boundaries back into the
/// outer vector.
fn typed_resolution_source_lines(
    program: &crate::resolution::ResolutionProgram,
    rendered: &str,
) -> Option<Vec<String>> {
    let boundary_count = program
        .segments
        .iter()
        .skip(1)
        .filter(|segment| segment.starts_new_source_line)
        .count()
        + program
            .segments
            .iter()
            .flat_map(|segment| &segment.self_replacements)
            .filter(|branch| branch.starts_new_source_line)
            .count();
    if boundary_count == 0 {
        return None;
    }
    let lines = rendered
        .lines()
        .map(str::trim)
        .filter(|line| !line.is_empty())
        .map(str::to_string)
        .collect::<Vec<_>>();
    (lines.len() == boundary_count + 1).then_some(lines)
}

fn rewrite_spell_resolution_damage_source(def: &CardDefinition, rendered: &str) -> String {
    if !(def.card.is_instant() || def.card.is_sorcery()) {
        return rendered.to_string();
    }
    let mut rewritten = String::with_capacity(rendered.len());
    let mut segment_start = 0usize;
    let mut quoted = false;
    for (index, character) in rendered.char_indices() {
        if character != '"' {
            continue;
        }
        let segment = &rendered[segment_start..index];
        if quoted {
            rewritten.push_str(segment);
        } else {
            rewritten.push_str(&rewrite_unquoted_spell_resolution_damage_source(
                def, segment,
            ));
        }
        rewritten.push('"');
        quoted = !quoted;
        segment_start = index + character.len_utf8();
    }
    let segment = &rendered[segment_start..];
    if quoted {
        rewritten.push_str(segment);
    } else {
        rewritten.push_str(&rewrite_unquoted_spell_resolution_damage_source(
            def, segment,
        ));
    }
    rewritten
}

fn rewrite_unquoted_spell_resolution_damage_source(def: &CardDefinition, rendered: &str) -> String {
    // Regression fixtures may compile the first face's oracle text under a
    // combined split-card name. The resolving spell is that face, not the
    // combined card, so use the front-face name as the authored source.
    let source_name = def
        .card
        .name
        .split_once(" // ")
        .map_or(def.card.name.as_str(), |(front, _)| front);
    let rendered = if let Some(rest) = rendered.strip_prefix("This creature deals ") {
        format!("{source_name} deals {rest}")
    } else if let Some(rest) = rendered.strip_prefix("This deals ") {
        format!("{source_name} deals {rest}")
    } else {
        rendered.to_string()
    };
    let rendered = rewrite_damage_phrases_for_permanent_abilities(&rendered, source_name, false)
        .replace("This creature deals ", "It deals ")
        .replace("this creature deals ", "it deals ")
        .replace("This spell deals ", &format!("{source_name} deals "))
        .replace("this spell deals ", &format!("{source_name} deals "))
        .replace(", This deals ", &format!(", {source_name} deals "))
        .replace(", this deals ", &format!(", {source_name} deals "))
        .replace("This source deal ", &format!("{source_name} deal "))
        .replace("this source deal ", &format!("{source_name} deal "))
        .replace("Exile this source", &format!("Exile {source_name}"));
    let rendered = if let Some(rest) = rendered.strip_prefix("Deal ") {
        format!("{source_name} deals {rest}")
    } else {
        rendered
    };
    let rendered = rendered
        .replace("\nDeal ", &format!("\n{source_name} deals "))
        .replace(" — Deal ", &format!(" — {source_name} deals "))
        .replace(" — It deals ", &format!(" — {source_name} deals "))
        .replace(", deal ", &format!(", {source_name} deals "));
    let rendered = rewrite_standalone_spell_self_exile(&rendered, source_name);
    rewrite_inline_spell_self_exile(&rendered, source_name)
}

#[cfg(test)]
mod spell_resolution_damage_source_line_tests {
    use super::*;

    fn definition(name: &str, card_type: CardType) -> CardDefinition {
        crate::cards::builders::CardDefinitionBuilder::new(crate::ids::CardId::new(), name)
            .card_types(vec![card_type])
            .build()
    }

    #[test]
    fn later_spell_source_line_keeps_the_resolving_spell_as_damage_source() {
        let definition = definition("Structural Offering", CardType::Sorcery);

        assert_eq!(
            rewrite_spell_resolution_damage_source(
                &definition,
                "Destroy target land.\nDeal 7 damage to target creature."
            ),
            "Destroy target land.\nStructural Offering deals 7 damage to target creature."
        );
    }

    #[test]
    fn permanent_text_does_not_inherit_the_spell_source_line_rewrite() {
        let definition = definition("Structural Permanent", CardType::Creature);
        let rendered = "Destroy target land.\nDeal 7 damage to target creature.";

        assert_eq!(
            rewrite_spell_resolution_damage_source(&definition, rendered),
            rendered
        );
    }
}

fn rewrite_standalone_spell_self_exile(rendered: &str, card_name: &str) -> String {
    let needle = "Exile this";
    let replacement = format!("Exile {card_name}");
    let mut out = String::with_capacity(rendered.len() + card_name.len());
    let mut rest = rendered;

    while let Some(idx) = rest.find(needle) {
        out.push_str(&rest[..idx]);
        let after = &rest[idx + needle.len()..];
        if matches!(
            after.chars().next(),
            None | Some('.') | Some(',') | Some(';')
        ) {
            out.push_str(&replacement);
        } else {
            out.push_str(needle);
        }
        rest = after;
    }

    out.push_str(rest);
    out
}

fn rewrite_inline_spell_self_exile(rendered: &str, card_name: &str) -> String {
    let needle = "exile this";
    let replacement = format!("you exile {card_name}");
    let mut out = String::with_capacity(rendered.len() + card_name.len() + 4);
    let mut rest = rendered;

    while let Some(idx) = rest.find(needle) {
        out.push_str(&rest[..idx]);
        let after = &rest[idx + needle.len()..];
        if out.ends_with("then ")
            && matches!(
                after.chars().next(),
                None | Some('.') | Some(',') | Some(';')
            )
        {
            out.push_str(&replacement);
        } else {
            out.push_str(needle);
        }
        rest = after;
    }

    out.push_str(rest);
    out
}

fn ability_has_begin_on_battlefield_pregame(ability: &Ability) -> bool {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return false;
    };
    matches!(
        static_ability.pregame_action_kind(),
        Some(crate::static_abilities::PregameActionKind::BeginOnBattlefield(_))
    )
}

fn substitute_pregame_self_reference(line: &str, card_name: &str) -> String {
    line.replace(
        "begin the game with this on the battlefield",
        &format!("begin the game with {card_name} on the battlefield"),
    )
}

fn substitute_pregame_card_self_reference(line: &str, subject: &str, card_name: &str) -> String {
    if subject.is_empty() || subject.eq_ignore_ascii_case("this source") {
        return line.to_string();
    }
    let lower = line.to_ascii_lowercase();
    if !lower.contains("begin the game") {
        return line.to_string();
    }
    let capitalized = capitalize_first_ascii(subject);
    line.replace(subject, card_name)
        .replace(&capitalized, card_name)
}

pub(super) fn substitute_legendary_source_reference(
    line: &str,
    card: &crate::card::Card,
    subject: &str,
    oracle_short_name: Option<&str>,
) -> String {
    let line = collapse_duplicate_source_type_subject(line);
    let canonical_name_lower = card.name.to_ascii_lowercase();
    let line = if canonical_name_lower != card.name {
        replace_outside_quotes(&line, &canonical_name_lower, &card.name)
    } else {
        line
    };
    let line = line.as_str();
    // Prefer the name the oracle text actually used to refer to the source (a
    // captured ShortName surface) over a naive comma split, so a card whose
    // oracle shortens itself ("Loran of the Third Path" -> "Loran") renders that
    // way, while one that doesn't ("Kodama of the Center Tree") stays full.
    let comma_short = card.name.split(',').next().unwrap_or(&card.name).trim();
    let validated_oracle_short = oracle_short_name.map(str::trim).filter(|candidate| {
        !candidate.is_empty()
            && card.name.strip_prefix(candidate).is_some_and(|tail| {
                tail.is_empty()
                    || tail.starts_with(',')
                    || tail.starts_with(' ')
                    || tail.starts_with('-')
            })
    });
    // A combined split-card name is not itself an Oracle source subject. Only
    // proceed when parsing retained a typed short-name surface for the face
    // whose rules are being rendered.
    if card.name.contains(" // ") && validated_oracle_short.is_none() {
        return line.to_string();
    }
    let self_name = validated_oracle_short.unwrap_or(comma_short);

    // Named source filters describe a proper-noun subject, while the generic
    // object-filter renderer necessarily supplies an indefinite article.
    // Remove that article only for the validated name captured from Oracle.
    let line = if validated_oracle_short.is_some() {
        ["When", "Whenever"]
            .into_iter()
            .flat_map(|frequency| {
                ["a", "an"]
                    .into_iter()
                    .map(move |article| (frequency, article))
            })
            .fold(line.to_string(), |line, (frequency, article)| {
                replace_outside_quotes(
                    &line,
                    &format!("{frequency} {article} {self_name} "),
                    &format!("{frequency} {self_name} "),
                )
            })
    } else {
        line.to_string()
    };
    let line =
        ["power", "toughness", "mana value"]
            .into_iter()
            .fold(line, |line, characteristic| {
                replace_outside_quotes(
                    &line,
                    &format!("this's {characteristic}"),
                    &format!("{self_name}'s {characteristic}"),
                )
            });
    let line = line.as_str();

    let source_subject = if subject.is_empty() {
        subject_for_card(card)
    } else {
        subject
    };

    // A captured short-name surface belongs to the source card, not only to
    // the trigger that happened to expose it. Static characteristic abilities
    // can already render the canonical card name directly, so normalize their
    // possessive self-reference before the type-relative substitutions below.
    // Legendary comma titles use their established pre-comma short name even
    // when the static payload retained only the typed SourceNameSubject hint;
    // a non-comma legendary name still needs an explicit captured ShortName
    // surface. Nonlegendary named characteristic subjects remain named.
    let has_oracle_short_surface = validated_oracle_short.is_some() || card.name.contains(',');
    let canonical_possessive = format!("{}'s", card.name);
    let line = if let Some(rest) = line.strip_prefix(&canonical_possessive)
        && (rest.starts_with(" power") || rest.starts_with(" toughness"))
    {
        if card.supertypes.contains(&Supertype::Legendary)
            && has_oracle_short_surface
            && self_name != card.name
        {
            format!("{self_name}'s{rest}")
        } else {
            line.to_string()
        }
    } else {
        line.to_string()
    };

    // A named-source characteristic subject is lowered through the same typed
    // object path as an ordinary filter. That path can retain both the card
    // name and its source noun, yielding surfaces such as
    // "Card Name creature's power". Collapse the redundant noun before the
    // normal legendary substitution. Ordinary permanents use the type-relative
    // self reference; legendary permanents use their valid oracle short name.
    let source_noun = source_subject
        .strip_prefix("this ")
        .or_else(|| source_subject.strip_prefix("This "));
    let line = if let Some(source_noun) = source_noun {
        let malformed_prefix = format!("{} {source_noun}'s", card.name);
        if let Some(rest) = line.strip_prefix(&malformed_prefix) {
            let replacement = if card.supertypes.contains(&Supertype::Legendary) {
                self_name
            } else {
                source_subject
            };
            format!("{replacement}'s{rest}")
        } else {
            line.to_string()
        }
    } else {
        line.to_string()
    };

    let lower = line.to_ascii_lowercase();
    let named_attack_characteristic_reference = lower
        .strip_prefix("whenever ")
        .and_then(|rest| rest.strip_prefix(&self_name.to_ascii_lowercase()))
        .is_some_and(|rest| rest.starts_with(" attacks"))
        && ["this creature's power", "this creature's toughness"]
            .iter()
            .any(|reference| lower.contains(reference));
    let authored_body_lower = lower
        .split_once('—')
        .map(|(_, body)| body.trim_start())
        .unwrap_or(lower.as_str());
    let conditional_static_self_surface = ((authored_body_lower.starts_with("as long as ")
        || lower.contains(": as long as "))
        && (lower.contains(", this creature has ")
            || lower.contains(" this creature has ")
            || lower.contains(", this creature is ")
            || lower.contains(" this creature is ")
            || lower.contains(", this planeswalker is ")
            || lower.contains(" this planeswalker is ")))
        || (authored_body_lower.starts_with("this creature has ")
            && lower.contains(" as long as "))
        || ((lower.contains(": as long as ") || lower.contains("— as long as "))
            && lower.contains(", this creature gets "));
    let uses_named_source_surface = authored_body_lower.starts_with("this creature gets ")
        || authored_body_lower.starts_with("this creature's power and toughness ")
        || authored_body_lower.starts_with("this creature gains ")
        || authored_body_lower.starts_with("as long as this planeswalker ")
        || authored_body_lower.starts_with("as this enters")
        || conditional_static_self_surface
        || lower.contains("if this land has ")
        || lower.contains("if this creature has one or more ")
        || lower.contains(" counters on this artifact")
        || authored_body_lower.starts_with("whenever this creature enters or attacks")
        || authored_body_lower.starts_with("whenever this creature attacks")
        || authored_body_lower.starts_with("whenever this creature is dealt damage")
        || authored_body_lower
            .starts_with("whenever this creature deals combat damage to a player")
        || authored_body_lower.starts_with("whenever this creature or another ")
        || authored_body_lower.starts_with("whenever this or another ")
        || authored_body_lower.starts_with("this creature can't block")
        || authored_body_lower.starts_with("this creature can't be the target ")
        || authored_body_lower.starts_with("exile this creature:")
        || authored_body_lower.starts_with("exile this:")
        || lower.contains("choose one and this creature gets ")
        || lower.contains(" this creature deals ")
        || lower.contains(", this creature deals ")
        || lower.contains(": target creature blocks this creature")
        || lower.contains(": exile target creature blocking or blocked by this creature")
        || (lower.contains(": put ") && lower.contains(" exiled with this creature"))
        || (lower.contains("at random that was exiled with this artifact")
            && lower.contains("put that card into its owner's hand"))
        || lower.contains(": this creature gets ")
        || lower.contains(": this creature gains ")
        || lower.contains(": this creature deals ")
        || (lower.contains(": put ")
            && lower.contains(", then ")
            && lower.matches(" counters on this creature").count() >= 2)
        || lower.contains(": until end of turn, this creature can be the target ")
        || lower.contains("you may have this creature gain ")
        || lower.contains("sacrifice this permanent when that token leaves the battlefield")
        || lower.contains("sacrifice this creature when that token leaves the battlefield")
        || lower.contains("if this creature is untapped")
        || lower.contains(": whenever this creature deals combat damage to a player")
        || authored_body_lower.starts_with("this planeswalker has ")
        || authored_body_lower.starts_with("this planeswalker deals ")
        || lower.contains(": this planeswalker deals ");
    let uses_named_source_surface =
        uses_named_source_surface || named_attack_characteristic_reference;
    if card.supertypes.contains(&Supertype::Legendary)
        && lower.starts_with("soulshift ")
        && !self_name.is_empty()
    {
        return format!("{self_name} has {}", lowercase_first(&line));
    }
    if !card.supertypes.contains(&Supertype::Legendary) || !uses_named_source_surface {
        // A legendary source can already have its authored name at this
        // stage.  In that case there is no self-reference left to substitute,
        // but a combined subject still takes the plural attack verb.
        if card.supertypes.contains(&Supertype::Legendary) && self_name.contains(" & ") {
            let singular = format!("Whenever {self_name} attacks");
            if let Some(rest) = line.strip_prefix(&singular) {
                return format!("Whenever {self_name} attack{rest}");
            }
        }
        return line.to_string();
    }

    let source_name = self_name;
    if source_name.is_empty() {
        return line.to_string();
    }

    let line = line
        .strip_prefix("Whenever this or another ")
        .map(|rest| format!("Whenever {source_name} or another {rest}"))
        .unwrap_or_else(|| line.to_string());

    let line = if let Some(rest) = line.strip_prefix("As this enters") {
        format!("As {source_name} enters{rest}")
    } else if let Some(rest) = line.strip_prefix("as this enters") {
        format!("As {source_name} enters{rest}")
    } else {
        line
    };

    let line = line
        .strip_prefix("Exile this:")
        .map(|rest| format!("Exile {source_name}:{rest}"))
        .unwrap_or(line);
    let substituted = [
        ("This creature", source_name),
        ("this creature", source_name),
        ("This land", source_name),
        ("this land", source_name),
        ("This artifact", source_name),
        ("this artifact", source_name),
        ("This planeswalker", source_name),
        ("this planeswalker", source_name),
        ("This permanent", source_name),
        ("this permanent", source_name),
    ]
    .into_iter()
    .fold(line, |line, (from, to)| {
        replace_outside_quotes(&line, from, to)
    });
    let quoted_cant_attack = format!("{source_name} gain \"this creature can't attack\"");
    let substituted = if substituted.contains(&quoted_cant_attack) {
        substituted.replace(
            &quoted_cant_attack,
            &format!("{source_name} gain \"{source_name} can't attack\""),
        )
    } else {
        substituted
    };
    let substituted = if source_name.contains(" & ") {
        let singular = format!("Whenever {source_name} attacks");
        let plural = format!("Whenever {source_name} attack");
        if let Some(rest) = substituted.strip_prefix(&singular) {
            format!("{plural}{rest}")
        } else if let Some((rendered_subject, rest)) = substituted
            .strip_prefix("Whenever ")
            .and_then(|trigger_tail| trigger_tail.split_once(" attacks"))
            .filter(|(rendered_subject, _)| rendered_subject.contains(" & "))
        {
            format!("Whenever {rendered_subject} attack{rest}")
        } else {
            substituted
        }
    } else {
        substituted
    };
    let lower_source_name = source_name.to_ascii_lowercase();
    if lower_source_name != source_name {
        replace_outside_quotes(&substituted, &lower_source_name, source_name)
    } else {
        substituted
    }
}

fn collapse_duplicate_source_type_subject(line: &str) -> String {
    let mut collapsed = line.to_string();
    for noun in [
        "artifact",
        "battle",
        "card",
        "creature",
        "enchantment",
        "land",
        "permanent",
        "planeswalker",
        "source",
        "spell",
    ] {
        let upper_from = format!("This {noun} {noun} ");
        let upper_to = format!("This {noun} ");
        collapsed = collapsed.replace(&upper_from, &upper_to);

        let lower_from = format!("this {noun} {noun} ");
        let lower_to = format!("this {noun} ");
        collapsed = collapsed.replace(&lower_from, &lower_to);
    }
    collapsed
}

/// The shortened self-name the card's oracle text uses for itself, if any —
/// captured as a `ShortName` source-reference surface on one of its triggers
/// (e.g. "When Loran enters" on "Loran of the Third Path"). Returns `None` when
/// the oracle only ever names the card in full ("Kodama of the Center Tree"),
/// so render-side substitution stays faithful to the printed wording.
pub(super) fn oracle_short_self_name(def: &CardDefinition) -> Option<String> {
    for ability in &def.abilities {
        let crate::ability::AbilityKind::Triggered(triggered) = &ability.kind else {
            continue;
        };
        if matches!(
            triggered.presentation_label.as_ref(),
            Some(crate::ability::PresentationLabel::AbilityWord(label))
                if label.eq_ignore_ascii_case("eminence")
        ) {
            return eminence_short_self_name(&def.card.name);
        }
        if let Some(zone_change) = triggered
            .trigger
            .downcast_ref::<crate::triggers::zone_changes::ZoneChangeTrigger>()
            && let Some(crate::target::SourceReferenceSurface::ShortName(name)) = zone_change
                .this_object_surface
                .as_ref()
                .or(zone_change.object_filter.source_surface.as_ref())
        {
            return Some(name.clone());
        }
    }
    None
}

#[cfg(test)]
mod split_card_named_source_surface_tests {
    use super::*;
    use crate::triggers::Trigger;

    fn optimus_definition(short_name: Option<&str>) -> CardDefinition {
        let mut filter = ObjectFilter::source();
        filter.source_surface = short_name
            .map(|name| crate::target::SourceReferenceSurface::ShortName(name.to_string()));
        crate::cards::builders::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Optimus Prime, Hero // Optimus Prime, Autobot Leader",
        )
        .supertypes(vec![Supertype::Legendary])
        .card_types(vec![CardType::Artifact, CardType::Creature])
        .with_ability(Ability::triggered(
            Trigger::dies(filter),
            Vec::<Effect>::new(),
        ))
        .build()
    }

    #[test]
    fn split_card_dies_filter_exposes_its_typed_short_self_name() {
        let definition = optimus_definition(Some("Optimus Prime"));
        assert_eq!(
            oracle_short_self_name(&definition).as_deref(),
            Some("Optimus Prime")
        );
        assert_eq!(
            substitute_legendary_source_reference(
                "When an Optimus Prime dies, return it",
                &definition.card,
                "this creature",
                oracle_short_self_name(&definition).as_deref(),
            ),
            "When Optimus Prime dies, return it"
        );
    }

    #[test]
    fn split_card_name_is_untouched_without_a_matching_typed_short_name() {
        let definition = optimus_definition(None);
        let line = "When an Optimus Prime dies, return it";
        assert_eq!(
            substitute_legendary_source_reference(line, &definition.card, "this creature", None),
            line
        );
        assert_eq!(
            substitute_legendary_source_reference(
                line,
                &definition.card,
                "this creature",
                Some("Megatron"),
            ),
            line
        );
    }
}

#[cfg(test)]
mod source_possessive_tests {
    use super::*;

    fn card(name: &str, legendary: bool) -> crate::card::Card {
        crate::cards::builders::CardDefinitionBuilder::new(crate::ids::CardId::new(), name)
            .supertypes(if legendary {
                vec![Supertype::Legendary]
            } else {
                Vec::new()
            })
            .card_types(vec![CardType::Creature])
            .build()
            .card
    }

    #[test]
    fn malformed_source_possessive_uses_the_card_name_only_outside_quotes() {
        let vivi = card("Vivi Ornitier", true);
        assert_eq!(
            substitute_legendary_source_reference(
                "Add X mana, where X is this's power",
                &vivi,
                "this creature",
                None,
            ),
            "Add X mana, where X is Vivi Ornitier's power"
        );
        assert_eq!(
            substitute_legendary_source_reference(
                "This creature has \"where X is this's power\"",
                &vivi,
                "this creature",
                None,
            ),
            "This creature has \"where X is this's power\""
        );

        let ordinary = card("Mana Adept", false);
        let line = "Add X mana, where X is this's power";
        assert_eq!(
            substitute_legendary_source_reference(line, &ordinary, "this creature", None),
            "Add X mana, where X is Mana Adept's power"
        );
    }

    #[test]
    fn paired_source_counter_procedure_uses_the_legendary_short_name() {
        let jhoira = card("Jhoira, Ageless Innovator", true);
        let line = "{T}: Put two ingenuity counters on this creature, then you may put an artifact card with mana value X or less from your hand onto the battlefield, where X is the number of ingenuity counters on this creature.";
        assert_eq!(
            substitute_legendary_source_reference(line, &jhoira, "this creature", None),
            "{T}: Put two ingenuity counters on Jhoira, then you may put an artifact card with mana value X or less from your hand onto the battlefield, where X is the number of ingenuity counters on Jhoira."
        );

        let single_reference = "{T}: Put two ingenuity counters on this creature.";
        assert_eq!(
            substitute_legendary_source_reference(single_reference, &jhoira, "this creature", None),
            single_reference,
            "one ordinary source-counter reference must not imply an authored proper-name surface"
        );
    }
}

fn replace_outside_quotes(input: &str, from: &str, to: &str) -> String {
    if from.is_empty() {
        return input.to_string();
    }

    let mut output = String::with_capacity(input.len());
    let mut in_quote = false;
    let mut index = 0;
    while index < input.len() {
        let ch = input[index..]
            .chars()
            .next()
            .expect("index should be on a char boundary");
        if ch == '"' {
            in_quote = !in_quote;
            output.push(ch);
            index += ch.len_utf8();
        } else if !in_quote && input[index..].starts_with(from) {
            output.push_str(to);
            index += from.len();
        } else {
            output.push(ch);
            index += ch.len_utf8();
        }
    }
    output
}

fn capitalize_first_ascii(s: &str) -> String {
    let mut chars = s.chars();
    match chars.next() {
        Some(c) => {
            let mut out = c.to_ascii_uppercase().to_string();
            out.push_str(chars.as_str());
            out
        }
        None => String::new(),
    }
}

fn title_case_card_name_surface(name: &str) -> String {
    let small_words = [
        "a", "an", "and", "as", "at", "but", "by", "for", "from", "in", "of", "or", "the", "to",
        "with",
    ];
    name.split_whitespace()
        .enumerate()
        .map(|(idx, word)| {
            if idx > 0 && small_words.contains(&word) {
                word.to_string()
            } else {
                capitalize_first_ascii(word)
            }
        })
        .collect::<Vec<_>>()
        .join(" ")
}

fn target_any_player_spec() -> ChooseSpec {
    ChooseSpec::Target(Box::new(ChooseSpec::Player(PlayerFilter::Any)))
}

fn target_any_player_filter() -> PlayerFilter {
    PlayerFilter::Target(Box::new(PlayerFilter::Any))
}

fn describe_structural_partner_with_pair(first: &Ability, second: &Ability) -> Option<String> {
    let AbilityKind::Static(static_ability) = &first.kind else {
        return None;
    };
    if static_ability.id() != crate::static_abilities::StaticAbilityId::PartnerWith {
        return None;
    }

    let AbilityKind::Triggered(triggered) = &second.kind else {
        return None;
    };
    if triggered.choices.as_slice() != [target_any_player_spec()].as_slice() {
        return None;
    }

    let zone_change = triggered
        .trigger
        .downcast_ref::<crate::triggers::ZoneChangeTrigger>()?;
    if zone_change.from != crate::triggers::ZonePattern::Any
        || zone_change.to != crate::triggers::ZonePattern::Specific(Zone::Battlefield)
        || zone_change.player != crate::triggers::PlayerRelation::Any
        || zone_change.count_mode != crate::triggers::CountMode::Each
        || !zone_change.this_object
        || zone_change.object_filter != ObjectFilter::default()
    {
        return None;
    }

    fn partner_name_from_search_library(
        search: &crate::effects::SearchLibraryEffect,
    ) -> Option<&str> {
        let partner_name = search.filter.name.as_deref()?;
        if search.filter.zone != Some(Zone::Library)
            || search.filter.owner != Some(target_any_player_filter())
            || search.destination != Zone::Hand
            || search.chooser != target_any_player_filter()
            || search.player != target_any_player_filter()
            || !search.reveal
            || search.search_mode != crate::effect::SearchSelectionMode::Exact
            || search.library_position_from_top.is_some()
        {
            return None;
        }
        Some(partner_name)
    }

    let [segment] = triggered.effects.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let (outer_target_effect, may_effect) = match segment.default_effects.as_slice() {
        [may_effect] => (None, may_effect),
        [target_effect, may_effect] => (Some(target_effect), may_effect),
        _ => return None,
    };
    if let Some(target_effect) = outer_target_effect {
        let target_only = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
        if target_only.target != target_any_player_spec() {
            return None;
        }
    }
    let may = may_effect.downcast_ref::<crate::effects::MayEffect>()?;
    if may.decider != Some(target_any_player_filter()) {
        return None;
    }
    let [target_effect, payload_effect] = may.effects.as_slice() else {
        return None;
    };
    let target_only = target_effect.downcast_ref::<crate::effects::TargetOnlyEffect>()?;
    if target_only.target != target_any_player_spec() {
        return None;
    }

    if let Some(search) = payload_effect.downcast_ref::<crate::effects::SearchLibraryEffect>() {
        let partner_name = partner_name_from_search_library(search)?;
        return Some(format!(
            "Partner with {}",
            title_case_card_name_surface(partner_name)
        ));
    }

    let sequence = payload_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    let [choose_effect, for_each_effect, shuffle_effect] = sequence.effects.as_slice() else {
        return None;
    };
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let partner_name = choose.filter.name.as_deref()?;
    if choose.filter.zone != Some(Zone::Library)
        || choose.filter.owner != Some(target_any_player_filter())
        || choose.count != ChoiceCount::exactly(1)
        || choose.count_value.is_some()
        || choose.chooser != target_any_player_filter()
        || choose.zone != Some(Zone::Library)
        || !choose.additional_zones.is_empty()
        || !choose.is_search
        || !choose.reveal
        || choose.search_mode != crate::effect::SearchSelectionMode::Exact
        || choose.top_only
        || choose.replace_tagged_objects
    {
        return None;
    }

    let for_each = for_each_effect.downcast_ref::<crate::effects::ForEachTaggedEffect>()?;
    if for_each.tag != choose.tag {
        return None;
    }
    let [move_effect] = for_each.effects.as_slice() else {
        return None;
    };
    let move_to_zone = move_effect.downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    if move_to_zone.target != ChooseSpec::Iterated
        || move_to_zone.zone != Zone::Hand
        || move_to_zone.to_top
        || move_to_zone.battlefield_controller != crate::effects::BattlefieldController::Preserve
        || move_to_zone.enters_tapped
    {
        return None;
    }

    let shuffle = shuffle_effect.downcast_ref::<crate::effects::ShuffleLibraryEffect>()?;
    if shuffle.player != target_any_player_filter()
        || shuffle.target_spec != Some(target_any_player_spec())
    {
        return None;
    }

    Some(format!(
        "Partner with {}",
        title_case_card_name_surface(partner_name)
    ))
}

fn describe_structural_echo_pair(first: &Ability, second: &Ability) -> Option<String> {
    let AbilityKind::Static(static_ability) = &first.kind else {
        return None;
    };
    let static_display = static_ability
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    if static_ability.id() != crate::static_abilities::StaticAbilityId::EnterWithCounters
        || !static_display.contains("echo")
        || !static_display.contains("counter")
    {
        return None;
    }
    let AbilityKind::Triggered(triggered) = &second.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || triggered
            .trigger
            .downcast_ref::<crate::triggers::BeginningOfUpkeepTrigger>()
            .is_none_or(|trigger| trigger.player != PlayerFilter::You)
    {
        return None;
    }
    let [segment] = triggered.effects.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let echo_effects = match segment.default_effects.as_slice() {
        [conditional_effect] => {
            let conditional =
                conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
            if conditional.condition != crate::effect::Condition::SourceIsInZone(Zone::Battlefield)
                || !conditional.if_false.is_empty()
            {
                return None;
            }
            conditional.if_true.as_slice()
        }
        effects => effects,
    };
    let [remove_effect, if_effect] = echo_effects else {
        return None;
    };
    let Some(remove_with_id) = remove_effect.downcast_ref::<crate::effects::WithIdEffect>() else {
        return None;
    };
    let remove = remove_with_id
        .effect
        .downcast_ref::<crate::effects::RemoveCountersEffect>()?;
    if remove.counter_type != CounterType::Echo
        || remove.count != Value::Fixed(1)
        || !matches!(remove.target, ChooseSpec::Source)
    {
        return None;
    }

    let if_effect = if_effect.downcast_ref::<crate::effects::IfEffect>()?;
    if if_effect.condition != remove_with_id.id
        || if_effect.predicate != crate::effect::EffectPredicate::Happened
        || !if_effect.else_.is_empty()
    {
        return None;
    }
    let [unless] = if_effect.then.as_slice() else {
        return None;
    };
    let unless = unless.downcast_ref::<crate::effects::UnlessActionEffect>()?;
    if unless.player != PlayerFilter::You {
        return None;
    }
    let [sacrifice] = unless.effects.as_slice() else {
        return None;
    };
    let sacrifice = sacrifice.downcast_ref::<crate::effects::SacrificeTargetEffect>()?;
    if !matches!(sacrifice.target, ChooseSpec::Source) {
        return None;
    }
    let cost = describe_echo_alternative_cost(&unless.alternative)?;
    Some(format!("Echo{cost}"))
}

fn describe_as_enters_source_exile_counter_pair(
    first: &Ability,
    second: &Ability,
) -> Option<String> {
    if first.functional_zones.as_slice() != [Zone::Battlefield]
        || second.functional_zones != first.functional_zones
    {
        return None;
    }
    let AbilityKind::Static(as_enters) = &first.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::AsEntersEffectProgram {
        program,
        uses_enters_with_counter_surface: true,
        ..
    } = &as_enters.compiled_model()?.payload
    else {
        return None;
    };
    let [segment] = program.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let [effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let exile = effect.downcast_ref::<crate::effects::ExileEffect>()?;
    let ChooseSpec::WithCount(inner, count) = exile.spec.unhinted() else {
        return None;
    };
    let ChooseSpec::Object(filter) = inner.unhinted() else {
        return None;
    };
    if *count != ChoiceCount::up_to_dynamic_x()
        || filter.zone != Some(Zone::Graveyard)
        || filter.owner != Some(PlayerFilter::You)
        || filter.card_types.as_slice() != [CardType::Creature]
    {
        return None;
    }
    let mut exile_residual = filter.clone();
    exile_residual.zone = None;
    exile_residual.owner = None;
    exile_residual.card_types.clear();
    exile_residual.set_explicit_card_noun(false);
    exile_residual.set_explicit_card_type_noun(None);
    if exile_residual != ObjectFilter::default() {
        return None;
    }
    let AbilityKind::Static(counter_static) = &second.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::EntersWithCountersValue { counter, count } =
        &counter_static.compiled_model()?.payload
    else {
        return None;
    };
    let Value::CountScaled(filter, multiplier) = count.unhinted() else {
        return None;
    };
    if *counter != CounterType::PlusOnePlusOne || *multiplier <= 0 {
        return None;
    }
    let mut residual = filter.clone();
    if residual.zone != Some(Zone::Exile)
        || residual.card_types.as_slice() != [CardType::Creature]
        || !residual.tagged_constraints.iter().any(|constraint| {
            constraint.relation == crate::target::TaggedOpbjectRelation::IsTaggedObject
                && constraint.tag.as_str() == crate::tag::SOURCE_EXILED_TAG
        })
    {
        return None;
    }
    residual.zone = None;
    residual.card_types.clear();
    residual.tagged_constraints.retain(|constraint| {
        !(constraint.relation == crate::target::TaggedOpbjectRelation::IsTaggedObject
            && constraint.tag.as_str() == crate::tag::SOURCE_EXILED_TAG)
    });
    residual.set_explicit_card_noun(false);
    residual.set_explicit_card_type_noun(None);
    if residual != ObjectFilter::default() {
        return None;
    }
    let amount = u32::try_from(*multiplier)
        .ok()
        .and_then(ironsmith_core::cardinal_word)
        .unwrap_or_else(|| multiplier.to_string());
    let first = describe_static_ability_with_subject(as_enters, "this permanent");
    Some(format!(
        "{}. It enters with {amount} +1/+1 counters on it for each creature card exiled this way",
        first.trim().trim_end_matches('.')
    ))
}

fn describe_echo_alternative_cost(alternative: &[Effect]) -> Option<String> {
    if let [pay] = alternative {
        let pay = unwrap_basic_render_wrapper(pay);
        if let Some(pay) = pay.downcast_ref::<crate::effects::PayManaEffect>() {
            if !matches!(pay.player, ChooseSpec::SourceController) {
                return None;
            }
            return Some(format!(" {}", pay.cost.to_oracle()));
        }

        if let Some(discard) = pay.downcast_ref::<crate::effects::DiscardEffect>() {
            if discard.random
                || discard.player != PlayerFilter::You
                || discard.tag.is_some()
                || discard.card_filter.is_some()
            {
                return None;
            }
            let Value::Fixed(count) = discard.count else {
                return None;
            };
            if count == 1 {
                return Some("—Discard a card".to_string());
            }
            return Some(format!("—Discard {count} cards"));
        }

        if let Some(pay_life) = pay.downcast_ref::<crate::effects::PayLifeEffect>() {
            if !matches!(pay_life.player, ChooseSpec::Player(PlayerFilter::You)) {
                return None;
            }
            return Some(format!("—Pay {} life", describe_value(&pay_life.amount)));
        }

        if let Some(lose_life) = pay.downcast_ref::<crate::effects::LoseLifeEffect>() {
            if !matches!(lose_life.player, ChooseSpec::Player(PlayerFilter::You)) {
                return None;
            }
            return Some(format!("—Pay {} life", describe_value(&lose_life.amount)));
        }

        if let Some(sacrifice) = pay.downcast_ref::<crate::effects::SacrificeEffect>() {
            let Value::Fixed(count) = sacrifice.count else {
                return None;
            };
            if count <= 0
                || sacrifice.player != PlayerFilter::You
                || sacrifice.filter.zone != Some(Zone::Battlefield)
                || sacrifice.filter.controller != Some(PlayerFilter::You)
                || !sacrifice.event_object_tags.is_empty()
                || !sacrifice.event_source_tags.is_empty()
                || !sacrifice.filter.tagged_constraints.is_empty()
            {
                return None;
            }
            let mut noun_filter = sacrifice.filter.clone();
            noun_filter.zone = None;
            noun_filter.controller = None;
            let noun_description = noun_filter.description();
            let noun = strip_leading_article(&noun_description);
            if count == 1 {
                return Some(format!("—Sacrifice {}", with_indefinite_article(noun)));
            }
            let count_text = number_word(count).unwrap_or_else(|| count.to_string());
            return Some(format!(
                "—Sacrifice {count_text} {}",
                pluralize_noun_phrase(noun)
            ));
        }
    }

    if let [choose, sacrifice] = alternative {
        let choose = choose.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
        let sacrifice = sacrifice.downcast_ref::<crate::effects::SacrificeEffect>()?;
        let exact = choose.count.max.filter(|max| *max == choose.count.min)?;
        if exact == 0
            || choose.is_search
            || choose.chooser != PlayerFilter::You
            || choose.filter.controller != Some(PlayerFilter::You)
            || sacrifice.player != PlayerFilter::You
            || sacrifice.count != Value::Fixed(exact as i32)
            || !sacrifice
                .filter
                .tagged_constraints
                .iter()
                .any(|constraint| {
                    constraint.tag == choose.tag
                        && matches!(
                            constraint.relation,
                            crate::filter::TaggedOpbjectRelation::IsTaggedObject
                        )
                })
        {
            return None;
        }
        let description = choose.filter.description();
        let noun = strip_leading_article(&description);
        if exact == 1 {
            return Some(format!("—Sacrifice {}", with_indefinite_article(noun)));
        }
        let count = number_word(exact as i32).unwrap_or_else(|| exact.to_string());
        return Some(format!(
            "—Sacrifice {count} {}",
            pluralize_noun_phrase(noun)
        ));
    }

    None
}

fn describe_structural_equipment_token_keyword(ability: &Ability) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || !trigger_is_this_enters_battlefield(&triggered.trigger)
    {
        return None;
    }
    let [segment] = triggered.effects.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let [create_effect, attach_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let (tag, create) = tagged_create_token_effect_for_keyword(create_effect)?;
    if create.count != Value::Fixed(1)
        || create.controller != PlayerFilter::You
        || create.controller_target.is_some()
        || create.suppress_aura_attachment_choice
        || create.enters_tapped
        || create.enters_attacking
        || create.exile_at_end_of_combat
        || create.sacrifice_at_end_of_combat
        || create.sacrifice_at_next_end_step
        || create.exile_at_next_end_step
    {
        return None;
    }
    let attach = unwrap_effect_for_keyword(attach_effect)
        .downcast_ref::<crate::effects::AttachToEffect>()?;
    if !matches!(&attach.target, ChooseSpec::Tagged(found) if found == tag) {
        return None;
    }
    if is_living_weapon_germ_token(&create.token) {
        return Some("Living weapon".to_string());
    }
    if is_for_mirrodin_rebel_token(&create.token) {
        return Some("For Mirrodin!".to_string());
    }
    None
}

fn unwrap_effect_for_keyword(effect: &Effect) -> &Effect {
    if let Some(with_id) = effect.downcast_ref::<crate::effects::WithIdEffect>() {
        return unwrap_effect_for_keyword(&with_id.effect);
    }
    effect
}

fn tagged_create_token_effect_for_keyword(
    effect: &Effect,
) -> Option<(&TagKey, &crate::effects::CreateTokenEffect)> {
    let tagged =
        unwrap_effect_for_keyword(effect).downcast_ref::<crate::effects::TaggedEffect>()?;
    let create = unwrap_effect_for_keyword(&tagged.effect)
        .downcast_ref::<crate::effects::CreateTokenEffect>()?;
    Some((&tagged.tag, create))
}

fn is_living_weapon_germ_token(token: &CardDefinition) -> bool {
    token.card.is_token
        && token.card.name == "Phyrexian Germ"
        && token.card.colors() == crate::color::ColorSet::BLACK
        && token.card.card_types == [CardType::Creature]
        && token.card.subtypes == [Subtype::Phyrexian, Subtype::Germ]
        && matches!(
            token.card.power_toughness,
            Some(crate::card::PowerToughness {
                power: crate::card::PtValue::Fixed(0),
                toughness: crate::card::PtValue::Fixed(0),
            })
        )
        && token.abilities.is_empty()
}

fn is_for_mirrodin_rebel_token(token: &CardDefinition) -> bool {
    token.card.is_token
        && token.card.name == "Rebel"
        && token.card.colors() == crate::color::ColorSet::RED
        && token.card.card_types == [CardType::Creature]
        && token.card.subtypes == [Subtype::Rebel]
        && matches!(
            token.card.power_toughness,
            Some(crate::card::PowerToughness {
                power: crate::card::PtValue::Fixed(2),
                toughness: crate::card::PtValue::Fixed(2),
            })
        )
        && token.abilities.is_empty()
}

fn trigger_is_this_enters_battlefield(trigger: &crate::triggers::Trigger) -> bool {
    trigger
        .downcast_ref::<crate::triggers::ZoneChangeTrigger>()
        .is_some_and(|zone_change| {
            zone_change.this_object
                && zone_change.to.matches(Zone::Battlefield)
                && zone_change.cause_filter.is_none()
        })
}

fn describe_structural_evolve_keyword(ability: &Ability) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.intervening_if.is_some() || !triggered.choices.is_empty() {
        return None;
    }
    let zone_change = triggered
        .trigger
        .downcast_ref::<crate::triggers::ZoneChangeTrigger>()?;
    if zone_change.this_object
        || !zone_change.to.matches(Zone::Battlefield)
        || zone_change.object_filter.controller != Some(PlayerFilter::You)
        || !zone_change
            .object_filter
            .card_types
            .contains(&CardType::Creature)
    {
        return None;
    }
    let [effect] = triggered.effects.flattened_default_effects() else {
        return None;
    };
    effect
        .downcast_ref::<crate::effects::EvolveEffect>()
        .is_some()
        .then(|| "Evolve".to_string())
}

fn describe_structural_training_keyword(ability: &Ability) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || triggered
            .trigger
            .downcast_ref::<crate::triggers::ThisAttacksWithGreaterPowerTrigger>()
            .is_none()
    {
        return None;
    }
    let [put, emit] = triggered.effects.flattened_default_effects() else {
        return None;
    };
    let put = put.downcast_ref::<crate::effects::PutCountersEffect>()?;
    if put.counter_type != CounterType::PlusOnePlusOne
        || put.amount != Value::Fixed(1)
        || !matches!(put.target, ChooseSpec::Source)
        || put.target_count.is_some()
        || put.distributed
    {
        return None;
    }
    let emit = emit.downcast_ref::<crate::effects::EmitKeywordActionEffect>()?;
    (emit.action == ironsmith_core::KeywordActionKind::Train && emit.amount == 1)
        .then(|| {
            "Training (Whenever this creature attacks with another creature with greater power, put a +1/+1 counter on this creature.)".to_string()
        })
}

fn describe_structural_renown_keyword(ability: &Ability) -> Option<String> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || triggered.presentation_label.is_some()
        || triggered.trigger.intro_surface().is_some()
    {
        return None;
    }
    let combat_damage = triggered
        .trigger
        .downcast_ref::<crate::triggers::ThisDealsCombatDamageToPlayerTrigger>()?;
    if combat_damage.player != PlayerFilter::Any || combat_damage.source_surface.is_some() {
        return None;
    }
    let [segment] = triggered.effects.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() || segment.starts_new_source_line {
        return None;
    }
    let [effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let renown = effect.downcast_ref::<crate::effects::RenownEffect>()?;
    (renown.amount > 0).then(|| format!("Renown {}", renown.amount))
}

fn describe_structural_ingest_keyword(ability: &Ability) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || triggered
            .trigger
            .downcast_ref::<crate::triggers::ThisDealsCombatDamageToPlayerTrigger>()
            .is_none()
    {
        return None;
    }
    let [effect] = triggered.effects.flattened_default_effects() else {
        return None;
    };
    let exile_top = effect.downcast_ref::<crate::effects::ExileTopOfLibraryEffect>()?;
    (exile_top.player == PlayerFilter::DamagedPlayer
        && exile_top.count == Value::Fixed(1)
        && exile_top.accumulated_tags.is_empty())
    .then(|| "Ingest".to_string())
}

fn describe_structural_rampage_pair(first: &Ability, second: &Ability) -> Option<String> {
    let AbilityKind::Static(static_ability) = &first.kind else {
        return None;
    };
    if static_ability.id() != crate::static_abilities::StaticAbilityId::KeywordMarker {
        return None;
    }
    let display = static_ability.display();
    let lower = display.trim().trim_end_matches('.').to_ascii_lowercase();
    let amount: i32 = lower.strip_prefix("rampage ")?.parse().ok()?;
    if amount <= 0 {
        return None;
    }

    let AbilityKind::Triggered(triggered) = &second.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || triggered
            .trigger
            .downcast_ref::<crate::triggers::ThisBecomesBlockedTrigger>()
            .is_none()
    {
        return None;
    }
    let [effect] = triggered.effects.flattened_default_effects() else {
        return None;
    };
    let pump = effect.downcast_ref::<crate::effects::ModifyPowerToughnessEffect>()?;
    if !matches!(pump.target, ChooseSpec::Source)
        || !matches!(pump.duration, Until::EndOfTurn)
        || !matches!(pump.power, Value::EventValue(EventValueSpec::BlockersBeyondFirst { multiplier }) if multiplier == amount)
        || !matches!(pump.toughness, Value::EventValue(EventValueSpec::BlockersBeyondFirst { multiplier }) if multiplier == amount)
    {
        return None;
    }
    Some(format!("Rampage {amount}"))
}

fn describe_structural_graveyard_or_exile_cast_pair(
    first: &Ability,
    second: &Ability,
) -> Option<String> {
    fn play_from_source_zone(ability: &Ability) -> Option<Zone> {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let spec = static_ability.grant_spec()?;
        (matches!(spec.grantable, crate::grant::Grantable::PlayFrom)
            && spec.filter == crate::target::ObjectFilter::source())
        .then_some(spec.zone)
    }

    match (play_from_source_zone(first), play_from_source_zone(second)) {
        (Some(Zone::Graveyard), Some(Zone::Exile)) | (Some(Zone::Exile), Some(Zone::Graveyard)) => {
            Some("You may cast this card from your graveyard or from exile".to_string())
        }
        _ => None,
    }
}

fn describe_structural_play_from_with_mana_permission_pair(
    first: &Ability,
    second: &Ability,
) -> Option<String> {
    fn render(grant_ability: &Ability, mana_ability: &Ability) -> Option<String> {
        let AbilityKind::Static(grant_static) = &grant_ability.kind else {
            return None;
        };
        let grant = grant_static.grant_spec()?;
        if !matches!(grant.grantable, crate::grant::Grantable::PlayFrom)
            || grant.beneficiary != PlayerFilter::You
        {
            return None;
        }

        let AbilityKind::Static(mana_static) = &mana_ability.kind else {
            return None;
        };
        let ironsmith_core::StaticAbilityPayload::ManaSpendPermission { permission, .. } =
            &mana_static.compiled_model()?.payload
        else {
            return None;
        };
        let ironsmith_core::ManaSpendScope::CastingSpellsMatching(mana_filter) = &permission.scope
        else {
            return None;
        };
        if permission.player != PlayerFilter::You
            || *mana_filter != grant.filter
            || permission.any_color_mana_symbol.is_some()
            || permission.other_mana_only_as_colorless
        {
            return None;
        }

        let source_phrase = match &permission.mana_source_filter {
            None => "mana".to_string(),
            Some(source_filter) => {
                let mut normalized = source_filter.clone();
                if normalized.supertypes.as_slice() != [Supertype::Snow] {
                    return None;
                }
                normalized.supertypes.clear();
                if normalized != ObjectFilter::default() {
                    return None;
                }
                "mana from snow sources".to_string()
            }
        };
        let mana_clause = match permission.mode {
            ironsmith_core::value_model::ManaSpendMode::AnyColor => format!(
                "you may spend {source_phrase} as though it were mana of any color to cast those spells"
            ),
            ironsmith_core::value_model::ManaSpendMode::AnyType
                if permission.mana_source_filter.is_none() =>
            {
                "mana of any type can be spent to cast those spells".to_string()
            }
            _ => return None,
        };
        // A during-your-turn gate on the grant carries onto the fused line:
        // "During your turn, you may play ... , and mana of any type ...".
        let during_your_turn = matches!(
            &grant_static.compiled_model()?.payload,
            ironsmith_core::StaticAbilityPayload::Conditional { condition, .. }
                if matches!(
                    condition,
                    ironsmith_core::Condition::ActivationTiming(
                        ironsmith_core::ActivationTiming::DuringYourTurn
                    )
                )
        );
        if during_your_turn {
            return Some(format!(
                "During your turn, {}, and {mana_clause}",
                lowercase_first(&grant.display())
            ));
        }
        Some(format!("{}, and {mana_clause}", grant.display()))
    }

    render(first, second).or_else(|| render(second, first))
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum CounterKeywordAmount {
    Fixed(u32),
    Sunburst,
    X,
}

impl CounterKeywordAmount {
    fn render(self) -> String {
        match self {
            CounterKeywordAmount::Fixed(amount) => amount.to_string(),
            CounterKeywordAmount::Sunburst => "Sunburst".to_string(),
            CounterKeywordAmount::X => "X".to_string(),
        }
    }
}

fn static_enter_counter_amount(ability: &Ability) -> Option<(CounterType, CounterKeywordAmount)> {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    if static_ability.id() != crate::static_abilities::StaticAbilityId::EnterWithCounters {
        return None;
    }

    // Compiled counter-entry abilities retain the typed value that determines
    // their counter count. Only fixed, X, and sunburst counts belong to the
    // structural keyword bundles below. In particular, an authored
    // "a counter ... for each ..." line must not be mistaken for a fixed one
    // merely because its rendered surface starts with the article "a".
    if let Some(model) = static_ability.compiled_model()
        && let ironsmith_core::StaticAbilityPayload::EntersWithCountersValue { counter, count } =
            &model.payload
    {
        let amount = match count.unhinted() {
            Value::Fixed(amount) => CounterKeywordAmount::Fixed(u32::try_from(*amount).ok()?),
            Value::X => CounterKeywordAmount::X,
            Value::ColorsOfManaSpentToCastThisSpell => CounterKeywordAmount::Sunburst,
            _ => return None,
        };
        return Some((*counter, amount));
    }

    let display = static_ability
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    let counter_type = if display.contains("+1/+1 counter") {
        CounterType::PlusOnePlusOne
    } else if display.contains("fade counter") {
        CounterType::Fade
    } else if display.contains("time counter") {
        CounterType::Time
    } else {
        return None;
    };

    if display.contains("for each color of mana spent to cast") {
        return Some((counter_type, CounterKeywordAmount::Sunburst));
    }
    if display.contains("with x ") {
        return Some((counter_type, CounterKeywordAmount::X));
    }
    if display.contains(" for each ")
        || display.contains(" equal to ")
        || display.contains("where x is ")
    {
        return None;
    }

    let after_enters = display
        .strip_prefix("enters with ")
        .or_else(|| display.strip_prefix("enters the battlefield with "))?;
    let amount = parse_leading_counter_count(after_enters)?;
    Some((counter_type, CounterKeywordAmount::Fixed(amount)))
}

fn parse_leading_counter_count(text: &str) -> Option<u32> {
    let first = text.split_whitespace().next()?;
    ironsmith_core::parse_cardinal_word(first)
}

fn is_upkeep_remove_counter(
    triggered: &crate::ability::TriggeredAbility,
    counter: CounterType,
) -> bool {
    let valid_condition = if counter == CounterType::Time {
        matches!(
            triggered.intervening_if,
            Some(Condition::SourceHasCounterAtLeast {
                counter_type: CounterType::Time,
                count: 1,
                ..
            })
        )
    } else {
        triggered.intervening_if.is_none()
    };
    if !valid_condition
        || !triggered.choices.is_empty()
        || triggered
            .trigger
            .downcast_ref::<crate::triggers::BeginningOfUpkeepTrigger>()
            .is_none_or(|trigger| trigger.player != PlayerFilter::You)
    {
        return false;
    }
    let [segment] = triggered.effects.segments.as_slice() else {
        return false;
    };
    if !segment.self_replacements.is_empty() {
        return false;
    }
    let [effect] = segment.default_effects.as_slice() else {
        return false;
    };
    let (effect, target) =
        if let Some(each) = effect.downcast_ref::<crate::effects::ForEachObject>() {
            if each.filter != ObjectFilter::source() || each.effects.len() != 1 {
                return false;
            }
            (&each.effects[0], ChooseSpec::Iterated)
        } else {
            (effect, ChooseSpec::Source)
        };
    let Some(remove) = effect.downcast_ref::<crate::effects::RemoveCountersEffect>() else {
        return false;
    };
    remove.counter_type == counter && remove.count == Value::Fixed(1) && remove.target == target
}

fn sacrifices_source(triggered: &crate::ability::TriggeredAbility) -> bool {
    let [segment] = triggered.effects.segments.as_slice() else {
        return false;
    };
    if !segment.self_replacements.is_empty() {
        return false;
    }
    let [effect] = segment.default_effects.as_slice() else {
        return false;
    };
    let Some(sacrifice) = effect.downcast_ref::<crate::effects::SacrificeTargetEffect>() else {
        return false;
    };
    matches!(sacrifice.target, ChooseSpec::Source)
}

fn is_fading_sacrifice_trigger(
    triggered: &crate::ability::TriggeredAbility,
    counter: CounterType,
) -> bool {
    if !matches!(triggered.intervening_if, Some(Condition::SourceHasNoCounter(found)) if found == counter)
        || !triggered.choices.is_empty()
        || !sacrifices_source(triggered)
    {
        return false;
    }
    triggered
        .trigger
        .downcast_ref::<crate::triggers::CounterRemovedFromTrigger>()
        .is_some_and(|trigger| trigger.filter.source)
}

fn is_vanishing_sacrifice_trigger(triggered: &crate::ability::TriggeredAbility) -> bool {
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || !sacrifices_source(triggered)
    {
        return false;
    }
    if triggered
        .trigger
        .downcast_ref::<crate::triggers::CounterRemovedFromTrigger>()
        .is_some_and(|trigger| {
            trigger.filter == ObjectFilter::source()
                && trigger.counter_type == Some(CounterType::Time)
                && trigger.last
                && !trigger.caused_by_source
                && !trigger.one_or_more
        })
    {
        return true;
    }
    triggered
        .trigger
        .downcast_ref::<crate::triggers::CustomTrigger>()
        .is_some_and(|trigger| {
            trigger.id == "vanishing-last-time-counter-removed"
                || trigger
                    .description
                    .eq_ignore_ascii_case("when the last time counter is removed")
        })
}

fn is_graft_trigger(triggered: &crate::ability::TriggeredAbility) -> bool {
    if triggered.intervening_if.is_some() || !triggered.choices.is_empty() {
        return false;
    }
    let Some(zone_change) = triggered
        .trigger
        .downcast_ref::<crate::triggers::zone_changes::ZoneChangeTrigger>()
    else {
        return false;
    };
    if zone_change.from != crate::triggers::zone_changes::ZonePattern::Any
        || zone_change.to != crate::triggers::zone_changes::ZonePattern::Specific(Zone::Battlefield)
        || zone_change.player != crate::triggers::zone_changes::PlayerRelation::Any
        || zone_change.count_mode != crate::triggers::zone_changes::CountMode::Each
        || !zone_change.object_filter.other
        || !zone_change
            .object_filter
            .card_types
            .contains(&CardType::Creature)
    {
        return false;
    }

    let [segment] = triggered.effects.segments.as_slice() else {
        return false;
    };
    if !segment.self_replacements.is_empty() {
        return false;
    }
    let [tag_effect, may_effect] = segment.default_effects.as_slice() else {
        return false;
    };
    let Some(tag) = tag_effect.downcast_ref::<crate::effects::TagTriggeringObjectEffect>() else {
        return false;
    };
    let Some(may) = may_effect.downcast_ref::<crate::effects::MayEffect>() else {
        return false;
    };
    let [move_effect] = may.effects.as_slice() else {
        return false;
    };
    let Some(move_counters) = move_effect.downcast_ref::<crate::effects::MoveCountersEffect>()
    else {
        return false;
    };
    move_counters.counter_type == CounterType::PlusOnePlusOne
        && move_counters.count == Value::Fixed(1)
        && matches!(move_counters.from, ChooseSpec::Source)
        && matches!(&move_counters.to, ChooseSpec::Tagged(found) if found == &tag.tag)
}

/// Preserve the causative land-entry surface and payer-relative effect cost
/// for the reusable Chant pattern. The executable model deliberately stores
/// the entered land's controller as an aliased player and stores the counter
/// placement as an effect-backed cost relative to that payer; rendering those
/// pieces independently would lose both authored relationships.
fn describe_land_entry_damage_unless_creature_counter(
    ability: &Ability,
    subject: &str,
) -> Option<String> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || triggered.presentation_label.is_some()
    {
        return None;
    }
    let zone_change = triggered
        .trigger
        .downcast_ref::<crate::triggers::zone_changes::ZoneChangeTrigger>()?;
    if zone_change.from != crate::triggers::zone_changes::ZonePattern::Any
        || zone_change.to != crate::triggers::zone_changes::ZonePattern::Specific(Zone::Battlefield)
        || zone_change.player != crate::triggers::zone_changes::PlayerRelation::Any
        || zone_change.count_mode != crate::triggers::zone_changes::CountMode::Each
        || zone_change.this_object
        || zone_change.cause_filter.is_some()
        || zone_change.during_turn.is_some()
        || zone_change.timing.is_some()
        || zone_change.origin_condition.is_some()
    {
        return None;
    }
    let [land_type] = zone_change.object_filter.subtypes.as_slice() else {
        return None;
    };
    if !land_type.is_basic_land_type() {
        return None;
    }
    let mut land_filter = zone_change.object_filter.clone();
    land_filter.subtypes.clear();
    if land_filter != ObjectFilter::default() {
        return None;
    }

    let [segment] = triggered.effects.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() || segment.starts_new_source_line {
        return None;
    }
    let [effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let unless = effect.downcast_ref::<crate::effects::UnlessPaysEffect>()?;
    if unless.leading_surface || unless.before_delayed_step {
        return None;
    }
    let PlayerFilter::AliasedControllerOf(crate::target::ObjectRef::Tagged(payer_tag)) =
        &unless.player
    else {
        return None;
    };
    if payer_tag.as_str() != "triggering" {
        return None;
    }
    let [damage_effect] = unless.effects.as_slice() else {
        return None;
    };
    let damage = damage_effect.downcast_ref::<crate::effects::DealDamageEffect>()?;
    if damage.amount.unhinted() != &Value::Fixed(3)
        || damage.source_is_combat
        || damage.unpreventable
        || !matches!(damage.target.base(), ChooseSpec::Player(player) if player == &unless.player)
    {
        return None;
    }

    let [cost] = unless.cost.as_all()? else {
        return None;
    };
    let put = cost
        .effect_ref()?
        .downcast_ref::<crate::effects::PutCountersEffect>()?;
    let ChooseSpec::Object(counter_filter) = put.target.base() else {
        return None;
    };
    let mut semantic_counter_filter = counter_filter.clone();
    semantic_counter_filter.union_surface = Default::default();
    if put.counter_type != CounterType::MinusOneMinusOne
        || put.amount.unhinted() != &Value::Fixed(1)
        || put.target.count() != ChoiceCount::exactly(1)
        || put
            .target_count
            .as_ref()
            .is_some_and(|target_count| target_count != &put.target.count())
        || put.distributed
        || semantic_counter_filter != ObjectFilter::creature().controlled_by(PlayerFilter::You)
    {
        return None;
    }

    Some(format!(
        "Whenever a player puts a {land_type} onto the battlefield, {subject} deals 3 damage to that player unless they put a -1/-1 counter on a creature they control"
    ))
}

#[cfg(test)]
mod land_entry_damage_unless_creature_counter_tests {
    use super::*;

    #[test]
    fn explicit_effect_cost_cardinality_keeps_the_entering_players_choice() {
        for (name, land_type, mana) in [
            ("Thelon's Chant", "Swamp", "{G}"),
            ("Tourach's Chant", "Forest", "{B}"),
        ] {
            let text = format!(
                "At the beginning of your upkeep, sacrifice this enchantment unless you pay {mana}.\nWhenever a player puts a {land_type} onto the battlefield, this enchantment deals 3 damage to that player unless they put a -1/-1 counter on a creature they control."
            );
            let definition = crate::CardDefinitionBuilder::new(crate::ids::CardId::new(), name)
                .card_types(vec![CardType::Enchantment])
                .parse_text(&text)
                .unwrap_or_else(|error| panic!("{name} should compile: {error}"));

            assert_eq!(
                crate::compiled_text::compiled_text_lines(&definition).join("\n"),
                text,
                "{definition:#?}"
            );
        }
    }
}

fn is_modular_trigger(triggered: &crate::ability::TriggeredAbility) -> bool {
    if triggered.intervening_if.is_some() || triggered.choices.is_empty() {
        return false;
    }
    let Some(zone_change) = triggered
        .trigger
        .downcast_ref::<crate::triggers::zone_changes::ZoneChangeTrigger>()
    else {
        return false;
    };
    if zone_change.from != crate::triggers::zone_changes::ZonePattern::Specific(Zone::Battlefield)
        || zone_change.to != crate::triggers::zone_changes::ZonePattern::Specific(Zone::Graveyard)
        || !zone_change.this_object
    {
        return false;
    }

    let [segment] = triggered.effects.segments.as_slice() else {
        return false;
    };
    if !segment.self_replacements.is_empty() {
        return false;
    }
    let [tag_effect, may_effect] = segment.default_effects.as_slice() else {
        return false;
    };
    if tag_effect
        .downcast_ref::<crate::effects::TagTriggeringObjectEffect>()
        .is_none()
    {
        return false;
    }
    let Some(may) = may_effect.downcast_ref::<crate::effects::MayEffect>() else {
        return false;
    };
    let [put_effect] = may.effects.as_slice() else {
        return false;
    };
    let Some(put) = put_effect.downcast_ref::<crate::effects::PutCountersEffect>() else {
        return false;
    };
    put.counter_type == CounterType::PlusOnePlusOne
        && matches!(
            &put.amount,
            Value::CountersOn(_, Some(CounterType::PlusOnePlusOne))
        )
}

fn is_ravenous_draw_trigger(triggered: &crate::ability::TriggeredAbility) -> bool {
    if !matches!(triggered.intervening_if, Some(Condition::XValueAtLeast(5)))
        || !triggered.choices.is_empty()
        || !trigger_is_this_enters_battlefield(&triggered.trigger)
    {
        return false;
    }

    let [effect] = triggered.effects.flattened_default_effects() else {
        return false;
    };
    let Some(draw) = effect.downcast_ref::<crate::effects::DrawCardsEffect>() else {
        return false;
    };
    draw.player == PlayerFilter::You && draw.count == Value::Fixed(1)
}

fn is_suspend_helper_ability(ability: &Ability) -> bool {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return false;
    };
    ability.functional_zones == [Zone::Exile]
        && (is_suspend_remove_time_counter_trigger(triggered)
            || is_suspend_cast_when_last_counter_removed_trigger(triggered))
}

fn is_conspire_helper_ability(ability: &Ability) -> bool {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return false;
    };
    if ability.functional_zones != [Zone::Stack]
        || !matches!(
            triggered.intervening_if.as_ref(),
            Some(Condition::ThisSpellPaidLabel(label))
                if label.display_label().eq_ignore_ascii_case("Conspire")
        )
        || !triggered.choices.is_empty()
        || triggered
            .trigger
            .downcast_ref::<crate::triggers::YouCastThisSpellTrigger>()
            .is_none()
    {
        return false;
    }

    let [copy_effect, choose_targets] = triggered.effects.flattened_default_effects() else {
        return false;
    };
    let Some(with_id) = copy_effect.downcast_ref::<crate::effects::WithIdEffect>() else {
        return false;
    };
    let Some(copy) = with_id
        .effect
        .downcast_ref::<crate::effects::CopySpellEffect>()
    else {
        return false;
    };
    let Some(retarget) = choose_targets.downcast_ref::<crate::effects::ChooseNewTargetsEffect>()
    else {
        return false;
    };
    matches!(copy.target.unhinted(), ChooseSpec::Source)
        && copy.count == Value::Fixed(1)
        && copy.copier == PlayerFilter::You
        && retarget.from_effect == with_id.id
        && retarget.may
}

fn describe_repeatable_sacrifice_copy_followup(
    cost: &crate::cost::OptionalCost,
    ability: &Ability,
) -> Option<String> {
    if cost.kind != crate::cost::OptionalCostKind::Additional || !cost.repeatable {
        return None;
    }
    let [cost_component] = cost.cost.as_all()? else {
        return None;
    };
    let sacrifice = unwrap_basic_render_wrapper(cost_component.effect_ref()?)
        .downcast_ref::<crate::effects::SacrificeEffect>()?;
    if sacrifice.player != PlayerFilter::You || sacrifice.count != Value::Fixed(1) {
        return None;
    }

    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if ability.functional_zones != [Zone::Stack]
        || !triggered.choices.is_empty()
        || triggered
            .trigger
            .downcast_ref::<crate::triggers::YouCastThisSpellTrigger>()
            .is_none()
        || !matches!(
            triggered.intervening_if.as_ref(),
            Some(Condition::ThisSpellPaidLabel(label)) if label == &cost.reference
        )
    {
        return None;
    }
    let [copy_effect] = triggered.effects.flattened_default_effects() else {
        return None;
    };
    let copy = unwrap_basic_render_wrapper(copy_effect)
        .downcast_ref::<crate::effects::CopySpellEffect>()?;
    if !matches!(copy.target.unhinted(), ChooseSpec::Source)
        || copy.copier != PlayerFilter::You
        || !matches!(
            &copy.count,
            Value::TimesPaidLabel(label) if label == &cost.reference
        )
    {
        return None;
    }

    let mut noun_filter = sacrifice.filter.clone();
    noun_filter.zone = None;
    noun_filter.controller = None;
    noun_filter.owner = None;
    let noun_description = noun_filter.description();
    let noun = strip_indefinite_article(&noun_description);
    Some(format!(
        "When you do, copy this spell for each {noun} sacrificed this way"
    ))
}

fn is_repeatable_sacrifice_copy_helper(def: &CardDefinition, ability: &Ability) -> bool {
    def.optional_costs
        .iter()
        .any(|cost| describe_repeatable_sacrifice_copy_followup(cost, ability).is_some())
}

fn is_squad_helper_ability(ability: &Ability) -> bool {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return false;
    };
    if !triggered.choices.is_empty()
        || triggered.intervening_if.is_some()
        || !trigger_is_this_enters_battlefield(&triggered.trigger)
    {
        return false;
    }
    let [effect] = triggered.effects.flattened_default_effects() else {
        return false;
    };
    let Some(copy) = effect.downcast_ref::<crate::effects::CreateTokenCopyEffect>() else {
        return false;
    };
    matches!(copy.target.unhinted(), ChooseSpec::Source)
        && copy.controller == PlayerFilter::You
        && matches!(
            &copy.count,
            Value::TimesPaidLabel(label)
                if matches!(label.kind, crate::cost::OptionalCostKind::Squad)
        )
}

/// Evoke is compiled as an alternative casting method plus its executable
/// enter-the-battlefield sacrifice helper. The helper is rules machinery for
/// the keyword, not a separately authored ability line.
fn is_evoke_helper_ability(def: &CardDefinition, ability: &Ability) -> bool {
    if def
        .alternative_casts
        .iter()
        .filter(|method| method.is_composed_cost() && method.name().eq_ignore_ascii_case("Evoke"))
        .count()
        != 1
    {
        return false;
    }
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return false;
    };
    if !triggered.choices.is_empty()
        || !trigger_is_this_enters_battlefield(&triggered.trigger)
        || !matches!(
            triggered.intervening_if.as_ref(),
            Some(Condition::ThisSpellPaidLabel(label))
                if label.display_label().eq_ignore_ascii_case("Evoke")
        )
    {
        return false;
    }
    let [tag, sacrifice] = triggered.effects.flattened_default_effects() else {
        return false;
    };
    tag.downcast_ref::<crate::effects::TagTriggeringObjectEffect>()
        .is_some()
        && sacrifice
            .downcast_ref::<crate::effects::SacrificeTargetEffect>()
            .is_some_and(|sacrifice| matches!(sacrifice.target.unhinted(), ChooseSpec::Source))
}

fn is_suspend_remove_time_counter_trigger(triggered: &crate::ability::TriggeredAbility) -> bool {
    if !matches!(
        triggered.intervening_if,
        Some(Condition::SourceHasCounterAtLeast {
            counter_type: CounterType::Time,
            count: 1,
            ..
        })
    ) || !triggered.choices.is_empty()
        || triggered
            .trigger
            .downcast_ref::<crate::triggers::BeginningOfUpkeepTrigger>()
            .is_none_or(|trigger| trigger.player != PlayerFilter::You)
    {
        return false;
    }

    let [effect] = triggered.effects.flattened_default_effects() else {
        return false;
    };
    let Some(remove) = effect.downcast_ref::<crate::effects::RemoveCountersEffect>() else {
        return false;
    };
    remove.counter_type == CounterType::Time
        && remove.count == Value::Fixed(1)
        && matches!(remove.target, ChooseSpec::Source)
}

fn is_suspend_cast_when_last_counter_removed_trigger(
    triggered: &crate::ability::TriggeredAbility,
) -> bool {
    if !matches!(
        triggered.intervening_if,
        Some(Condition::SourceHasNoCounter(CounterType::Time))
    ) || !triggered.choices.is_empty()
        || triggered
            .trigger
            .downcast_ref::<crate::triggers::CounterRemovedFromTrigger>()
            .is_none_or(|trigger| !trigger.filter.source)
    {
        return false;
    }

    let [effect] = triggered.effects.flattened_default_effects() else {
        return false;
    };
    let Some(may) = effect.downcast_ref::<crate::effects::MayEffect>() else {
        return false;
    };
    let [cast] = may.effects.as_slice() else {
        return false;
    };
    let Some(cast) = cast.downcast_ref::<crate::effects::CastSourceEffect>() else {
        return false;
    };
    cast.without_paying_mana_cost && cast.require_exile
}

fn describe_structural_counter_keyword_bundle(abilities: &[Ability]) -> Option<(String, usize)> {
    if let (
        Some(Ability {
            kind: AbilityKind::Triggered(remove_triggered),
            ..
        }),
        Some(Ability {
            kind: AbilityKind::Triggered(sacrifice_triggered),
            ..
        }),
    ) = (abilities.first(), abilities.get(1))
        && is_upkeep_remove_counter(remove_triggered, CounterType::Time)
        && is_vanishing_sacrifice_trigger(sacrifice_triggered)
    {
        return Some(("Vanishing".to_string(), 2));
    }

    let (counter, amount) = static_enter_counter_amount(abilities.first()?)?;

    if counter == CounterType::PlusOnePlusOne
        && amount == CounterKeywordAmount::X
        && let Some(Ability {
            kind: AbilityKind::Triggered(triggered),
            ..
        }) = abilities.get(1)
        && is_ravenous_draw_trigger(triggered)
    {
        return Some(("Ravenous".to_string(), 2));
    }

    if counter == CounterType::PlusOnePlusOne
        && let Some(Ability {
            kind: AbilityKind::Triggered(triggered),
            ..
        }) = abilities.get(1)
    {
        if is_modular_trigger(triggered) {
            return Some((format!("Modular {}", amount.render()), 2));
        }
        if matches!(amount, CounterKeywordAmount::Fixed(_)) && is_graft_trigger(triggered) {
            return Some((format!("Graft {}", amount.render()), 2));
        }
    }

    if (counter == CounterType::Fade || counter == CounterType::Time)
        && let (
            Some(Ability {
                kind: AbilityKind::Triggered(remove_triggered),
                ..
            }),
            Some(Ability {
                kind: AbilityKind::Triggered(sacrifice_triggered),
                ..
            }),
        ) = (abilities.get(1), abilities.get(2))
        && is_upkeep_remove_counter(remove_triggered, counter)
    {
        if counter == CounterType::Fade
            && is_fading_sacrifice_trigger(sacrifice_triggered, CounterType::Fade)
        {
            return Some((format!("Fading {}", amount.render()), 3));
        }
        if counter == CounterType::Time && is_vanishing_sacrifice_trigger(sacrifice_triggered) {
            return Some((format!("Vanishing {}", amount.render()), 3));
        }
    }

    None
}

fn is_ascend_condition(condition: &Condition) -> bool {
    let Condition::And(left, right) = condition else {
        return false;
    };

    fn controls_ten_permanents(condition: &Condition) -> bool {
        matches!(
            condition,
            Condition::PlayerHasAtLeast {
                player: PlayerFilter::You,
                filter,
                count: 10,
            } if filter.controller == Some(PlayerFilter::You)
                && filter.card_types.is_empty()
                && filter.subtypes.is_empty()
                && filter.zone == Some(Zone::Battlefield)
        )
    }

    fn lacks_citys_blessing(condition: &Condition) -> bool {
        matches!(
            condition,
            Condition::Not(inner)
                if matches!(
                    inner.as_ref(),
                    Condition::PlayerHasCitysBlessing {
                        player: PlayerFilter::You
                    }
                )
        )
    }

    (controls_ten_permanents(left) && lacks_citys_blessing(right))
        || (controls_ten_permanents(right) && lacks_citys_blessing(left))
}

fn describe_structural_ascend_ability(ability: &Ability) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if !triggered.choices.is_empty()
        || !triggered
            .intervening_if
            .as_ref()
            .is_some_and(is_ascend_condition)
    {
        return None;
    }

    let zone_change = triggered
        .trigger
        .downcast_ref::<crate::triggers::zone_changes::ZoneChangeTrigger>()?;
    if zone_change.from != crate::triggers::zone_changes::ZonePattern::Any
        || zone_change.to != crate::triggers::zone_changes::ZonePattern::Specific(Zone::Battlefield)
        || zone_change.player != crate::triggers::zone_changes::PlayerRelation::Any
        || zone_change.count_mode != crate::triggers::zone_changes::CountMode::Each
        || zone_change.object_filter.controller != Some(PlayerFilter::You)
    {
        return None;
    }

    let [segment] = triggered.effects.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let [effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let create_emblem = effect.downcast_ref::<crate::effects::CreateEmblemEffect>()?;
    if !create_emblem
        .emblem
        .name
        .eq_ignore_ascii_case("City's Blessing")
    {
        return None;
    }

    Some("Ascend".to_string())
}

pub(super) fn describe_alternative_cast_line(
    method: &AlternativeCastingMethod,
    idx: usize,
) -> String {
    match method {
        method if method.is_composed_cost() && method.name().eq_ignore_ascii_case("Evoke") => {
            let mana_cost = method.mana_cost();
            let costs = method.non_mana_costs();
            match (mana_cost, costs.is_empty()) {
                (Some(cost), true) => format!("Evoke {}", cost.to_oracle()),
                (Some(cost), false) => format!(
                    "Evoke—{}, {}",
                    cost.to_oracle(),
                    capitalize_first(&describe_alternative_costs(&costs))
                ),
                (None, false) => format!(
                    "Evoke—{}",
                    capitalize_first(&describe_alternative_costs(&costs))
                ),
                (None, true) => "Evoke".to_string(),
            }
        }
        method if method.is_composed_cost() && method.name().eq_ignore_ascii_case("Spectacle") => {
            method
                .mana_cost()
                .map(|cost| format!("Spectacle {}", cost.to_oracle()))
                .unwrap_or_else(|| "Spectacle".to_string())
        }
        method if method.is_composed_cost() && method.name().eq_ignore_ascii_case("Emerge") => {
            method
                .mana_cost()
                .map(|cost| format!("Emerge {}", cost.to_oracle()))
                .unwrap_or_else(|| "Emerge".to_string())
        }
        method if method.is_composed_cost() && method.name().eq_ignore_ascii_case("Surge") => {
            method
                .mana_cost()
                .map(|cost| format!("Surge {}", cost.to_oracle()))
                .unwrap_or_else(|| "Surge".to_string())
        }
        method if method.is_composed_cost() && method.name().eq_ignore_ascii_case("Freerunning") => {
            method
                .mana_cost()
                .map(|cost| {
                    format!(
                        "Freerunning {} (You may cast this spell for its freerunning cost if you dealt combat damage to a player this turn with an Assassin or commander.)",
                        cost.to_oracle()
                    )
                })
                .unwrap_or_else(|| "Freerunning".to_string())
        }
        method if method.is_composed_cost() && method.name().eq_ignore_ascii_case("Sneak") => {
            method
                .mana_cost()
                .map(|cost| {
                    format!(
                        "Sneak {} (You may cast this spell for {} if you also return an unblocked attacker you control to hand during the declare blockers step.)",
                        cost.to_oracle(),
                        cost.to_oracle()
                    )
                })
                .unwrap_or_else(|| "Sneak".to_string())
        }
        method if method.is_composed_cost() && method.name().eq_ignore_ascii_case("Prowl") => {
            method
                .mana_cost()
                .map(|cost| format!("Prowl {}", cost.to_oracle()))
                .unwrap_or_else(|| "Prowl".to_string())
        }
        method if method.is_composed_cost() => {
            let name = method.name();
            let mana_cost = method.mana_cost();
            let costs = method.non_mana_costs();
            let cast_condition = method.cast_condition();
            if name.eq_ignore_ascii_case("Prototype")
                && let (Some(cost), Some(power_toughness)) =
                    (mana_cost, method.prototype_power_toughness())
            {
                return format!(
                    "Prototype {} — {}/{}",
                    cost.to_oracle(),
                    power_toughness.power,
                    power_toughness.toughness
                );
            }
            // Named keyword costs keep their oracle keyword surface
            // ("Evoke {2}{U}"), like the dedicated variants above.
            if !name.is_empty()
                && !name.eq_ignore_ascii_case("Parsed alternative cost")
                && cast_condition.is_none()
                && costs.is_empty()
                && let Some(cost) = mana_cost
            {
                return format!("{name} {}", cost.to_oracle());
            }
            let mut parts = Vec::new();
            if let Some(cost) = mana_cost {
                parts.push(format!("pay {}", cost.to_oracle()));
            }
            if !costs.is_empty() {
                parts.push(describe_alternative_costs(&costs));
            }
            let clause = if parts.is_empty() {
                "cast this spell without paying its mana cost".to_string()
            } else {
                parts.join(" and ")
            };
            let mut line = if parts.is_empty() {
                "You may cast this spell without paying its mana cost".to_string()
            } else {
                format!("You may {clause} rather than pay this spell's mana cost")
            };
            if !name.is_empty() {
                line.push_str(&format!(" ({name})"));
            }
            if let Some(condition) = cast_condition
                && let Some(condition_text) =
                    crate::static_abilities::describe_this_spell_cost_condition(condition)
            {
                line = format!("If {condition_text}, {}", lowercase_first(&line));
            }
            line
        }
        AlternativeCastingMethod::Madness { total_cost } => {
            let costs = method.non_mana_costs();
            if costs.is_empty() {
                return total_cost.mana_cost().map(render_madness_cost).unwrap_or_else(|| "Madness {0}".into());
            }
            let extra = capitalize_first(&describe_alternative_costs(&costs));
            match total_cost.mana_cost() {
                Some(mana) => format!("Madness—{}, {extra}", mana.to_oracle()),
                None => format!("Madness—{extra}"),
            }
        },
        AlternativeCastingMethod::Miracle { cost } => format!("Miracle {}", cost.to_oracle()),
        AlternativeCastingMethod::FlashWithAdditionalCost {
            additional_cost, ..
        } => format!(
            "You may cast this spell as though it had flash if you pay {} more to cast it",
            additional_cost.to_oracle()
        ),
        AlternativeCastingMethod::Plot { cost } => format!("Plot {}", cost.to_oracle()),
        AlternativeCastingMethod::Warp { cost } => format!("Warp {}", cost.to_oracle()),
        AlternativeCastingMethod::Suspend { cost, time } => {
            format!("Suspend {time}—{}", cost.to_oracle())
        }
        AlternativeCastingMethod::Disturb { cost } => format!("Disturb {}", cost.to_oracle()),
        AlternativeCastingMethod::Overload { cost, .. } => {
            format!("Overload {}", cost.to_oracle())
        }
        AlternativeCastingMethod::Cleave { cost, .. } => {
            format!("Cleave {}", cost.to_oracle())
        }
        AlternativeCastingMethod::Awaken { amount, cost, .. } => {
            format!("Awaken {amount}—{}", cost.to_oracle())
        }
        AlternativeCastingMethod::Flashback { total_cost } => {
            let costs = method.non_mana_costs();
            let mana_cost = total_cost.mana_cost().map(|cost| cost.to_oracle());
            if costs.is_empty() {
                format!(
                    "Flashback—{}",
                    mana_cost.unwrap_or_else(|| "{0}".to_string())
                )
            } else {
                let extra = capitalize_first(&describe_alternative_costs(&costs));
                if let Some(mana_cost) = mana_cost {
                    format!("Flashback—{mana_cost}, {extra}")
                } else {
                    format!("Flashback—{extra}")
                }
            }
        }
        AlternativeCastingMethod::Harmonize { total_cost } => {
            let costs = method.non_mana_costs();
            let mana_cost = total_cost
                .mana_cost()
                .map(|cost| cost.to_oracle())
                .unwrap_or_else(|| "{0}".to_string());
            if costs.is_empty() {
                format!("Harmonize {mana_cost}")
            } else {
                let extra = capitalize_first(&describe_alternative_costs(&costs));
                format!("Harmonize {mana_cost}, {extra}")
            }
        }
        AlternativeCastingMethod::Retrace { .. } => "Retrace".to_string(),
        AlternativeCastingMethod::JumpStart { .. } => "Jump-start".to_string(),
        AlternativeCastingMethod::Escape {
            cost, exile_count, ..
        } => {
            let count_text =
                small_number_word(*exile_count).unwrap_or_else(|| exile_count.to_string());
            if let Some(cost) = cost {
                format!(
                    "Escape—{}, Exile {count_text} other cards from your graveyard",
                    cost.to_oracle()
                )
            } else {
                format!("Escape—Exile {count_text} other cards from your graveyard")
            }
        }
        AlternativeCastingMethod::Dash { cost } => format!("Dash {}", cost.to_oracle()),
        AlternativeCastingMethod::Blitz { total_cost } => {
            let costs = method.non_mana_costs();
            let mana_cost = total_cost
                .mana_cost()
                .map(|cost| cost.to_oracle())
                .unwrap_or_else(|| "{0}".to_string());
            if costs.is_empty() {
                format!("Blitz {mana_cost}")
            } else {
                let extra = capitalize_first(&describe_alternative_costs(&costs));
                format!("Blitz—{mana_cost}, {extra}")
            }
        }
        AlternativeCastingMethod::Bestow { total_cost } => {
            let costs = method.non_mana_costs();
            let mana_cost = total_cost
                .mana_cost()
                .map(|cost| cost.to_oracle())
                .unwrap_or_else(|| "{0}".to_string());
            if costs.is_empty() {
                format!("Bestow {mana_cost}")
            } else {
                let extra = capitalize_first(&describe_alternative_costs(&costs));
                format!("Bestow {mana_cost}, {extra}")
            }
        }
        other => {
            if other.name().eq_ignore_ascii_case("Parsed alternative cost") {
                if let Some(cost) = other.mana_cost() {
                    format!(
                        "You may pay {} rather than pay this spell's mana cost",
                        cost.to_oracle()
                    )
                } else {
                    "You may cast this spell rather than pay its mana cost".to_string()
                }
            } else if let Some(cost) = other.mana_cost() {
                format!(
                    "Alternative cast {}: {} {}",
                    idx + 1,
                    other.name(),
                    cost.to_oracle()
                )
            } else {
                format!("Alternative cast {}: {}", idx + 1, other.name())
            }
        }
    }
}

fn alternative_cast_method_matches_kind(
    method: &AlternativeCastingMethod,
    kind: crate::filter::AlternativeCastKind,
) -> bool {
    use crate::filter::AlternativeCastKind;

    matches!(
        (kind, method),
        (
            AlternativeCastKind::Blitz,
            AlternativeCastingMethod::Blitz { .. }
        ) | (
            AlternativeCastKind::Dash,
            AlternativeCastingMethod::Dash { .. }
        ) | (
            AlternativeCastKind::Flashback,
            AlternativeCastingMethod::Flashback { .. }
        ) | (
            AlternativeCastKind::JumpStart,
            AlternativeCastingMethod::JumpStart { .. }
        ) | (
            AlternativeCastKind::Escape,
            AlternativeCastingMethod::Escape { .. }
        ) | (
            AlternativeCastKind::Madness,
            AlternativeCastingMethod::Madness { .. }
        ) | (
            AlternativeCastKind::Miracle,
            AlternativeCastingMethod::Miracle { .. }
        ) | (
            AlternativeCastKind::Suspend,
            AlternativeCastingMethod::Suspend { .. }
        )
    )
}

fn qualified_cost_reduction_ability_for_method<'a>(
    def: &'a CardDefinition,
    method: &AlternativeCastingMethod,
) -> Option<&'a crate::static_abilities::StaticAbility> {
    let mut matches = def.abilities.iter().filter_map(|ability| {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let reduction = static_ability.this_spell_cost_reduction()?;
        reduction
            .alternative_cast
            .filter(|kind| alternative_cast_method_matches_kind(method, *kind))?;
        Some(static_ability)
    });
    let ability = matches.next()?;
    matches.next().is_none().then_some(ability)
}

fn describe_alternative_cast_with_qualified_reduction(
    def: &CardDefinition,
    method: &AlternativeCastingMethod,
    idx: usize,
) -> Option<String> {
    let reduction = qualified_cost_reduction_ability_for_method(def, method)?;
    let mut method_line = describe_alternative_cast_line(method, idx);
    if matches!(method, AlternativeCastingMethod::Flashback { .. }) {
        method_line = method_line.replacen("Flashback—", "Flashback ", 1);
    }
    Some(format!(
        "{}. {}.",
        method_line.trim_end_matches('.'),
        reduction.display().trim_end_matches('.')
    ))
}

fn ability_is_rendered_with_alternative_cast(def: &CardDefinition, ability: &Ability) -> bool {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return false;
    };
    if static_ability
        .this_spell_cost_reduction()
        .and_then(|reduction| reduction.alternative_cast)
        .is_none()
    {
        return false;
    }
    def.alternative_casts
        .iter()
        .filter_map(|method| qualified_cost_reduction_ability_for_method(def, method))
        .any(|rendered| std::ptr::eq(rendered, static_ability))
}

fn render_madness_cost(cost: &crate::mana::ManaCost) -> String {
    let pips = cost.pips();
    if !pips.is_empty()
        && pips
            .iter()
            .all(|pip| matches!(pip.as_slice(), [crate::mana::ManaSymbol::Colorless]))
    {
        let amount = small_number_word(pips.len() as u32).unwrap_or_else(|| pips.len().to_string());
        return format!("Madness—Pay {amount} {{C}}");
    }
    format!("Madness {}", cost.to_oracle())
}

fn aura_attaches_to_plain_creature(
    attachment: Option<&crate::object::AuraAttachmentFilter>,
) -> bool {
    let Some(crate::object::AuraAttachmentFilter::Object(filter)) = attachment else {
        return false;
    };
    if !matches!(filter.zone, None | Some(Zone::Battlefield)) {
        return false;
    }
    let mut normalized = filter.clone();
    // `ObjectFilter::creature()` carries the battlefield zone by default.
    // Treat an omitted zone as that same ordinary Aura attachment domain.
    normalized.zone = Some(Zone::Battlefield);
    normalized == ObjectFilter::creature()
}

fn aura_attaches_to_creature_or_planeswalker(
    attachment: Option<&crate::object::AuraAttachmentFilter>,
) -> bool {
    let Some(crate::object::AuraAttachmentFilter::Object(filter)) = attachment else {
        return false;
    };
    let mut normalized = filter.clone();
    normalized.zone = Some(Zone::Battlefield);
    let mut expected = ObjectFilter::default().in_zone(Zone::Battlefield);
    expected.card_types = vec![CardType::Creature, CardType::Planeswalker];
    normalized == expected
}

fn aura_attaches_to_plain_permanent_type_union(
    attachment: Option<&crate::object::AuraAttachmentFilter>,
) -> bool {
    let Some(crate::object::AuraAttachmentFilter::Object(filter)) = attachment else {
        return false;
    };
    if filter.card_types.len() < 2
        || filter.card_types.iter().any(|card_type| {
            !matches!(
                card_type,
                CardType::Artifact
                    | CardType::Battle
                    | CardType::Creature
                    | CardType::Enchantment
                    | CardType::Land
                    | CardType::Planeswalker
            )
        })
    {
        return false;
    }
    let mut normalized = filter.clone();
    normalized.zone = Some(Zone::Battlefield);
    let mut expected = ObjectFilter::default().in_zone(Zone::Battlefield);
    expected.card_types = filter.card_types.clone();
    normalized == expected
}

fn rewrite_plain_creature_aura_subject(
    attachment: Option<&crate::object::AuraAttachmentFilter>,
    line: &str,
) -> String {
    if aura_attaches_to_plain_creature(attachment) {
        return line
            .replace("Enchanted permanent", "Enchanted creature")
            .replace("enchanted permanent", "enchanted creature");
    }
    if aura_attaches_to_creature_or_planeswalker(attachment)
        || aura_attaches_to_plain_permanent_type_union(attachment)
    {
        return line
            .replace("Enchanted creature", "Enchanted permanent")
            .replace("enchanted creature", "enchanted permanent");
    }
    line.to_string()
}

/// The `Enchant ...` line and the spell-resolution `AttachToEffect` are two
/// typed views of the same Aura attachment operation. Keep the executable
/// effect, but remove that one encoded operation from the program used for
/// compiled-text rendering so it cannot surface as a second
/// "Attach this source to target ..." sentence when the Aura also has other
/// cast-time setup such as gift.
fn spell_program_without_encoded_aura_attachment(
    program: &crate::resolution::ResolutionProgram,
    attachment: &crate::object::AuraAttachmentFilter,
) -> crate::resolution::ResolutionProgram {
    let expected_target = attachment.target_spec();
    let mut removed = false;
    let mut segments = Vec::with_capacity(program.segments.len());
    for segment in &program.segments {
        let mut default_effects = Vec::with_capacity(segment.default_effects.len());
        for effect in &segment.default_effects {
            let is_encoded_attachment = !removed
                && effect
                    .downcast_ref::<crate::effects::AttachToEffect>()
                    .is_some_and(|attach| attach.target == expected_target);
            if is_encoded_attachment {
                removed = true;
            } else {
                default_effects.push(effect.clone());
            }
        }
        if !default_effects.is_empty() || !segment.self_replacements.is_empty() {
            segments.push(ironsmith_core::ResolutionSegment {
                default_effects,
                self_replacements: segment.self_replacements.clone(),
                starts_new_source_line: segment.starts_new_source_line,
            });
        }
    }
    crate::resolution::ResolutionProgram::new(segments)
}

fn is_exact_discard_source_cost(cost: &crate::costs::Cost) -> bool {
    let Some(effect) = cost.effect_ref() else {
        return false;
    };
    let Some(discard) = effect.downcast_ref::<crate::effects::DiscardEffect>() else {
        return false;
    };
    discard.count == Value::Fixed(1)
        && discard.player == PlayerFilter::You
        && !discard.random
        && !discard.any_number
        && discard.card_filter.as_ref() == Some(&ObjectFilter::source().in_zone(Zone::Hand))
        && discard.tag.is_none()
}

/// Recover Reinforce only from its complete executable shape. This deliberately
/// ignores activation display text: the hand zone, exact mana/discard cost,
/// and sole fixed +1/+1-counter effect prove the keyword and its amount.
fn describe_structural_reinforce_ability(ability: &Ability) -> Option<String> {
    if ability.functional_zones.as_slice() != [Zone::Hand] {
        return None;
    }
    let AbilityKind::Activated(activated) = &ability.kind else {
        return None;
    };
    if activated.timing != ActivationTiming::AnyTime
        || activated.is_loyalty_ability
        || !activated.additional_restrictions.is_empty()
        || !activated.activation_restrictions.is_empty()
        || activated.mana_output.is_some()
        || activated.activation_condition.is_some()
        || !activated.mana_usage_restrictions.is_empty()
    {
        return None;
    }

    let costs = activated.mana_cost.as_all()?;
    if costs.len() != 2 {
        return None;
    }
    let mut mana_cost = None;
    let mut saw_discard_source = false;
    for cost in costs {
        if let Some(mana) = cost.mana_cost_ref() {
            if mana_cost.replace(mana).is_some() {
                return None;
            }
        } else if is_exact_discard_source_cost(cost) {
            if saw_discard_source {
                return None;
            }
            saw_discard_source = true;
        } else {
            return None;
        }
    }
    let mana_cost = mana_cost?;
    if !saw_discard_source {
        return None;
    }

    let [segment] = activated.effects.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let [effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let put = effect.downcast_ref::<crate::effects::PutCountersEffect>()?;
    if put.counter_type != crate::object::CounterType::PlusOnePlusOne
        || put.target_count.is_some()
        || put.distributed
        || put.target
            != ChooseSpec::target(ChooseSpec::Object(
                ObjectFilter::creature().in_zone(Zone::Battlefield),
            ))
    {
        return None;
    }
    // Parser lowering indexes the target requirement on the activated
    // ability as well as retaining it on the executable effect. Hand-built
    // definitions may omit that redundant index, but no additional or
    // divergent choice can still prove the Reinforce keyword shape.
    if !activated.choices.is_empty() && activated.choices.as_slice() != [put.target.clone()] {
        return None;
    }
    let Value::Fixed(amount) = &put.amount else {
        return None;
    };
    if *amount <= 0 {
        return None;
    }

    Some(format!("Reinforce {amount}—{}", mana_cost.to_oracle()))
}

/// Render the typed counter-removal prevention family as its authored inline
/// replacement sentence. The generic conditional-static renderer otherwise
/// presents this model as an `As long as` ability grant, even though the
/// condition is part of the damage replacement event itself.
fn structural_source_counter_threshold(condition: &Condition) -> Option<CounterType> {
    match condition {
        Condition::SourceHasCounterAtLeast {
            counter_type,
            count: 1,
            ..
        } => Some(*counter_type),
        Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::MatchingFilter(filter),
            comparison: Comparison::GreaterThanOrEqual(1),
            ..
        } => {
            let Some(ironsmith_core::CounterConstraint::Typed(counter_type)) = filter.with_counter
            else {
                return None;
            };
            (filter == &ObjectFilter::source().with_counter_type(counter_type))
                .then_some(counter_type)
        }
        Condition::CountComparison {
            count:
                ironsmith_core::AnthemCountExpression::CountersOnSource(counter_type)
                | ironsmith_core::AnthemCountExpression::CountersOnSourceWithSurface {
                    counter_type, ..
                }
                | ironsmith_core::AnthemCountExpression::CountersOnSourceWithPronoun {
                    counter_type, ..
                },
            comparison: Comparison::GreaterThanOrEqual(1),
            ..
        } => Some(*counter_type),
        _ => None,
    }
}

fn describe_structural_counter_removal_damage_prevention(
    ability: &Ability,
    subject: &str,
) -> Option<String> {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: prevention,
        condition,
    } = &model.payload
    else {
        return None;
    };
    let condition_counter = structural_source_counter_threshold(condition)?;
    let ironsmith_core::StaticAbilityPayload::PreventDamageToSelfRemoveCounter {
        counter_type,
        amount,
        follow_up,
        one_damage_per_counter,
        surface,
    } = &prevention.payload
    else {
        return None;
    };
    if condition_counter != *counter_type {
        return None;
    }

    if *one_damage_per_counter {
        if !matches!(amount.unhinted(), Value::EventValue(EventValueSpec::Amount))
            || follow_up.is_some()
        {
            return None;
        }
        let counter = counter_type.description();
        return Some(format!(
            "For each 1 damage that would be dealt to {subject}, if it has a {counter} counter on it, remove a {counter} counter from it and prevent that 1 damage"
        ));
    }

    let (amount_text, plural) = match amount.unhinted() {
        Value::Fixed(1) => ("a".to_string(), false),
        Value::Fixed(amount) if *amount > 1 => (
            u32::try_from(*amount)
                .ok()
                .and_then(small_number_word)
                .unwrap_or_else(|| amount.to_string()),
            true,
        ),
        Value::EventValue(EventValueSpec::Amount) => ("that many".to_string(), true),
        _ => return None,
    };
    let counter = counter_type.description();
    let counter_noun = if plural { "counters" } else { "counter" };
    let head = format!(
        "If damage would be dealt to {subject} while it has a {counter} counter on it, prevent that damage"
    );
    let removal = format!("remove {amount_text} {counter} {counter_noun} from it");

    match follow_up {
        None => Some(match surface {
            ironsmith_core::CounterRemovalPreventionSurface::Conjoined => {
                format!("{head} and {removal}")
            }
            ironsmith_core::CounterRemovalPreventionSurface::SeparateSentences => {
                format!("{head}. {}", capitalize_first(&removal))
            }
        }),
        Some(ironsmith_core::CounterRemovalFollowUp::EachPlayerGetsCounters {
            counter_type: gained_counter,
            counters_per_removed,
        }) => {
            let (gained_amount, gained_plural) = if *counters_per_removed == 1 {
                ("a".to_string(), "")
            } else {
                (
                    small_number_word(*counters_per_removed)
                        .unwrap_or_else(|| counters_per_removed.to_string()),
                    "s",
                )
            };
            Some(format!(
                "{head}, {removal}, then give each player {gained_amount} {} counter{gained_plural} for each {counter} counter removed this way",
                gained_counter.description()
            ))
        }
    }
}

/// Preserve the authored pronoun and event-derived amount for the reusable
/// "counters removed this way" damage trigger. Generic permanent damage
/// normalization changes an effect-leading `it deals` into the source type,
/// which loses the surface relationship carried by this exact typed shape.
fn describe_structural_counter_removed_this_way_damage_trigger(
    ability: &Ability,
) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || triggered.choices.as_slice() != [ChooseSpec::AnyTarget].as_slice()
        || triggered.presentation_label.is_some()
    {
        return None;
    }
    let counter_removed = triggered
        .trigger
        .downcast_ref::<crate::triggers::CounterRemovedFromTrigger>()?;
    let mut trigger_filter = counter_removed.filter.clone();
    trigger_filter.source_surface = None;
    if trigger_filter != ObjectFilter::source()
        || !counter_removed.one_or_more
        || !counter_removed.caused_by_source
    {
        return None;
    }
    let [segment] = triggered.effects.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() {
        return None;
    }
    let [tag_effect, execute_effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let triggering = tag_effect.downcast_ref::<crate::effects::TagTriggeringObjectEffect>()?;
    let execute = execute_effect.downcast_ref::<crate::effects::ExecuteWithSourceEffect>()?;
    if !matches!(
        execute.source.unhinted(),
        ChooseSpec::Tagged(tag) if *tag == triggering.tag
    ) {
        return None;
    }
    let damage = execute
        .effect
        .downcast_ref::<crate::effects::DealDamageEffect>()?;
    if damage.source_is_combat
        || damage.unpreventable
        || !matches!(damage.target.base(), ChooseSpec::AnyTarget)
        || !matches!(
            damage.amount.unhinted(),
            Value::EventValue(EventValueSpec::Amount)
        )
        || !damage
            .amount
            .has_surface_hint(ironsmith_core::ValueSurfaceHint::CountersRemovedThisWay)
    {
        return None;
    }

    Some(format!(
        "{}, it deals that much damage to any target",
        triggered.trigger.display()
    ))
}

fn describe_attachment_qualified_triggering_object_return(ability: &Ability) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if !triggered.choices.is_empty() || triggered.presentation_label.is_some() {
        return None;
    }
    let condition = triggered.intervening_if.as_ref()?;
    let reference_tag =
        super::normalize_common::attachment_state_disjunction_reference_tag(condition)?;
    let effects = triggered.effects.flattened_default_effects();
    let return_effect = match effects {
        [effect] => effect,
        [first_tag, second_tag, effect]
            if first_tag
                .downcast_ref::<crate::effects::TagAttachedToSourceEffect>()
                .is_some()
                && second_tag
                    .downcast_ref::<crate::effects::TagAttachedToSourceEffect>()
                    .is_some() =>
        {
            effect
        }
        _ => return None,
    };
    let returned = structural_unwrap_render_wrappers(return_effect)
        .downcast_ref::<crate::effects::ReturnToHandEffect>()?;
    if !matches!(returned.spec.base(), ChooseSpec::Tagged(tag) if tag == reference_tag)
        || returned.actor_surface.is_some()
        || returned.destination_player_surface.is_some()
        || returned.exiled_with_source_surface.is_some()
        || returned.set_quantifier_surface.is_some()
        || returned.set_reference_surface.is_some()
    {
        return None;
    }
    Some(format!(
        "{}, if {}, return it to its owner's hand",
        triggered.trigger.display(),
        super::normalize_common::describe_condition(condition),
    ))
}

fn describe_attachment_qualified_return_and_reattach(ability: &Ability) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.presentation_label.is_some() {
        return None;
    }
    let condition = triggered.intervening_if.as_ref()?;
    let reference_tag =
        super::normalize_common::attachment_state_disjunction_reference_tag(condition)?;
    let flattened = triggered.effects.flattened_default_effects();
    let [snapshot_one, snapshot_two, sequence_effect] = flattened else {
        return None;
    };
    let first_snapshot =
        snapshot_one.downcast_ref::<crate::effects::TagAttachedToSourceEffect>()?;
    if &first_snapshot.tag != reference_tag
        || snapshot_two
            .downcast_ref::<crate::effects::TagAttachedToSourceEffect>()
            .is_none()
    {
        return None;
    }
    let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::CommaThen
        || sequence.result_label.is_some()
    {
        return None;
    }
    let [move_effect, attach_returned_effect, attach_equipment_effect] =
        sequence.effects.as_slice()
    else {
        return None;
    };
    let tagged_move = move_effect.downcast_ref::<crate::effects::TaggedEffect>()?;
    let movement = tagged_move
        .effect
        .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    let ChooseSpec::WithCount(aura_spec, aura_count) = movement.target.unhinted() else {
        return None;
    };
    let ChooseSpec::Object(aura_filter) = aura_spec.unhinted() else {
        return None;
    };
    let [aura_link] = aura_filter.tagged_constraints.as_slice() else {
        return None;
    };
    let mut plain_aura = aura_filter.clone();
    plain_aura.zone = None;
    plain_aura.owner = None;
    plain_aura.subtypes.clear();
    plain_aura.tagged_constraints.clear();
    plain_aura.union_surface = Default::default();
    if !aura_count.is_any_number()
        || aura_filter.zone != Some(Zone::Graveyard)
        || aura_filter.owner != Some(PlayerFilter::You)
        || aura_filter.subtypes.as_slice() != [Subtype::Aura]
        || aura_link.tag != *reference_tag
        || aura_link.relation != crate::filter::TaggedOpbjectRelation::WasAttachedToTaggedObject
        || plain_aura != ObjectFilter::default()
        || movement.zone != Zone::Battlefield
        || movement.to_top
        || movement.library_order.is_some()
        || movement.verb_surface != ironsmith_core::MoveToZoneVerbSurface::Return
    {
        return None;
    }

    let attach_returned =
        attach_returned_effect.downcast_ref::<crate::effects::AttachObjectsEffect>()?;
    let returned_filter = match attach_returned.objects.base() {
        ChooseSpec::Object(filter) | ChooseSpec::All(filter) => filter,
        _ => return None,
    };
    let [returned_link] = returned_filter.tagged_constraints.as_slice() else {
        return None;
    };
    let ChooseSpec::Target(creature_target) = attach_returned.target.unhinted() else {
        return None;
    };
    let ChooseSpec::Object(creature_filter) = creature_target.unhinted() else {
        return None;
    };
    let mut semantic_creature_filter = creature_filter.clone();
    semantic_creature_filter.union_surface = Default::default();
    if returned_link.tag != tagged_move.tag
        || returned_link.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
        || semantic_creature_filter != ObjectFilter::creature().in_zone(Zone::Battlefield)
        || attach_returned.individual_targets
        || !matches!(triggered.choices.as_slice(), [choice] if target_specs_select_same_objects(choice, &attach_returned.target))
    {
        return None;
    }

    let attach_equipment =
        attach_equipment_effect.downcast_ref::<crate::effects::AttachObjectsEffect>()?;
    let ChooseSpec::WithCount(equipment_spec, equipment_count) =
        attach_equipment.objects.unhinted()
    else {
        return None;
    };
    let ChooseSpec::Object(equipment_filter) = equipment_spec.unhinted() else {
        return None;
    };
    let [equipment_link] = equipment_filter.tagged_constraints.as_slice() else {
        return None;
    };
    let mut plain_equipment = equipment_filter.clone();
    plain_equipment.zone = None;
    plain_equipment.subtypes.clear();
    plain_equipment.tagged_constraints.clear();
    plain_equipment.union_surface = Default::default();
    if !equipment_count.is_any_number()
        || equipment_filter.zone != Some(Zone::Battlefield)
        || equipment_filter.subtypes.as_slice() != [Subtype::Equipment]
        || equipment_link.tag != *reference_tag
        || equipment_link.relation
            != crate::filter::TaggedOpbjectRelation::WasAttachedToTaggedObject
        || plain_equipment != ObjectFilter::default()
        || !matches!(attach_equipment.target.base(), ChooseSpec::Tagged(tag) if tag == &tagged_move.tag)
        || attach_equipment.individual_targets
    {
        return None;
    }

    Some(format!(
        "{}, if {}, return any number of Aura cards that were attached to it from your graveyard to the battlefield attached to target creature, then attach any number of Equipment that were attached to it to that creature",
        triggered.trigger.display(),
        super::normalize_common::describe_condition(condition),
    ))
}

/// Preserve a singular choice from the source's persistent exile collection
/// across the result-dependent investigate instruction. The source-exile tag
/// proves the relative "with it" reference; the shared effect id proves the
/// investigate happens only when that exact card move succeeds.
fn describe_source_exiled_card_move_then_investigate(
    ability: &Ability,
    subject: &str,
) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || triggered.presentation_label.is_some()
        || triggered
            .trigger
            .downcast_ref::<crate::triggers::ThisAttacksTrigger>()
            .is_none()
    {
        return None;
    }
    let effects = triggered.effects.flattened_default_effects();
    let [move_root, result_root] = effects else {
        return None;
    };
    let with_id = move_root.downcast_ref::<crate::effects::WithIdEffect>()?;
    let movement = with_id
        .effect
        .downcast_ref::<crate::effects::MoveToZoneEffect>()?;
    let ChooseSpec::Object(filter) = movement.target.unhinted() else {
        return None;
    };
    let [constraint] = filter.tagged_constraints.as_slice() else {
        return None;
    };
    let mut remainder = filter.clone();
    remainder.zone = None;
    remainder.tagged_constraints.clear();
    remainder.union_surface = Default::default();
    if filter.zone != Some(Zone::Exile)
        || constraint.tag.as_str() != crate::tag::SOURCE_EXILED_TAG
        || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
        || remainder != ObjectFilter::default()
        || movement.zone != Zone::Graveyard
        || movement.to_top
        || movement.library_order.is_some()
        || movement.verb_surface != ironsmith_core::MoveToZoneVerbSurface::Put
        || movement.target_plural_surface
    {
        return None;
    }
    let result = result_root.downcast_ref::<crate::effects::IfEffect>()?;
    if result.condition != with_id.id
        || result.predicate != crate::effect::EffectPredicate::Happened
        || !result.else_.is_empty()
        || result.per_player_result
        || result.prior_result_replacement_surface
    {
        return None;
    }
    let [investigate] = result.then.as_slice() else {
        return None;
    };
    let investigate = investigate.downcast_ref::<crate::effects::InvestigateEffect>()?;
    if investigate.count.unhinted() != &Value::Fixed(1) {
        return None;
    }
    let trigger = triggered
        .trigger
        .display()
        .replace("this creature", subject);
    Some(format!(
        "{trigger}, put a card exiled with it into its owner's graveyard. If you do, investigate"
    ))
}

fn describe_triggering_card_type_exile_then_cast_permission(ability: &Ability) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || triggered.presentation_label.is_some()
    {
        return None;
    }
    let [exile_segment, permission_segment] = triggered.effects.segments.as_slice() else {
        return None;
    };
    if [exile_segment, permission_segment]
        .iter()
        .any(|segment| !segment.self_replacements.is_empty())
    {
        return None;
    }
    let [tag_effect, conditional_effect] = exile_segment.default_effects.as_slice() else {
        return None;
    };
    let tagged = tag_effect.downcast_ref::<crate::effects::TagTriggeringObjectEffect>()?;
    let conditional = conditional_effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    let exact_type_branch = |condition: &Condition, card_type: CardType| {
        let Condition::TaggedObjectMatches(tag, filter) = condition else {
            return false;
        };
        if tag != &tagged.tag || filter.card_types.as_slice() != [card_type] {
            return false;
        }
        let mut remainder = filter.clone();
        remainder.card_types.clear();
        remainder == ObjectFilter::default()
    };
    let Condition::Or(left, right) = &conditional.condition else {
        return None;
    };
    if !((exact_type_branch(left, CardType::Instant)
        && exact_type_branch(right, CardType::Sorcery))
        || (exact_type_branch(left, CardType::Sorcery)
            && exact_type_branch(right, CardType::Instant)))
        || conditional.surface != ironsmith_core::ConditionalSurface::TrailingIf
        || !conditional.if_false.is_empty()
    {
        return None;
    }
    let [move_effect] = conditional.if_true.as_slice() else {
        return None;
    };
    let movement = move_to_zone_surface_view(move_effect)?;
    if movement.as_ref()
        != &crate::effects::MoveToZoneEffect::new(
            ChooseSpec::Tagged(tagged.tag.clone()),
            Zone::Exile,
            true,
        )
    {
        return None;
    }

    let [permission_effect] = permission_segment.default_effects.as_slice() else {
        return None;
    };
    let permission = permission_effect.downcast_ref::<crate::effects::GrantPlayTaggedEffect>()?;
    let expected_permission = crate::effects::GrantPlayTaggedEffect::new(
        tagged.tag.clone(),
        PlayerFilter::You,
        crate::effects::GrantPlayTaggedDuration::UntilYourNextTurnEnd,
        false,
        ironsmith_core::value_model::ManaSpendMode::Normal,
    );
    if permission != &expected_permission {
        return None;
    }

    Some(format!(
        "{}, exile it if it's an instant or sorcery card. You may cast that card until the end of your next turn",
        triggered.trigger.display()
    ))
}

#[cfg(test)]
mod triggering_card_type_exile_permission_tests {
    use super::*;

    fn exact_ability() -> Ability {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Triggering Card Permission Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text("Whenever a face-down creature you control dies, exile it if it's an instant or sorcery card. You may cast that card until the end of your next turn.")
        .expect("typed triggering-card permission should compile");
        definition.abilities.into_iter().next().expect("ability")
    }

    #[test]
    fn renders_the_triggering_card_without_exposing_its_tag() {
        let ability = exact_ability();
        assert_eq!(
            describe_triggering_card_type_exile_then_cast_permission(&ability),
            Some("Whenever a face-down creature you control dies, exile it if it's an instant or sorcery card. You may cast that card until the end of your next turn".to_string()),
            "{ability:#?}"
        );
    }

    #[test]
    fn rejects_a_permission_for_a_different_tag() {
        let mut ability = exact_ability();
        let AbilityKind::Triggered(triggered) = &mut ability.kind else {
            panic!("expected triggered ability");
        };
        let mut permission = triggered.effects.segments[1].default_effects[0]
            .downcast_ref::<crate::effects::GrantPlayTaggedEffect>()
            .expect("permission")
            .clone();
        permission.tag = TagKey::from("unrelated_card");
        triggered.effects.segments[1].default_effects[0] = Effect::new(permission);
        assert_eq!(
            describe_triggering_card_type_exile_then_cast_permission(&ability),
            None
        );
    }
}

fn describe_voting_allied_players_shared_optional_scry(ability: &Ability) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if triggered.intervening_if.is_some()
        || !triggered.choices.is_empty()
        || triggered.presentation_label.is_some()
    {
        return None;
    }
    let vote = triggered
        .trigger
        .downcast_ref::<crate::triggers::KeywordActionTrigger>()?;
    if vote.action != crate::events::KeywordActionKind::Vote
        || vote.player != PlayerFilter::Any
        || vote.source_must_match
        || vote.source_filter.is_some()
        || vote.tagged_object_filter.is_some()
        || vote.during_your_turn
        || vote.during_your_main_phase
    {
        return None;
    }
    let flattened = triggered.effects.flattened_default_effects();
    let (controller_may_effect, allies_effect) = match flattened {
        [controller_may, allies] => (controller_may, allies),
        [sequence_effect] => {
            let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
            if sequence.surface != ironsmith_core::SequenceSurface::Coordinated
                || sequence.result_label.is_some()
            {
                return None;
            }
            let [controller_may, allies] = sequence.effects.as_slice() else {
                return None;
            };
            (controller_may, allies)
        }
        _ => return None,
    };
    let controller_may = controller_may_effect.downcast_ref::<crate::effects::MayEffect>()?;
    let allies = allies_effect.downcast_ref::<crate::effects::ForEachTaggedPlayerEffect>()?;
    let [controller_scry_effect] = controller_may.effects.as_slice() else {
        return None;
    };
    let [ally_may_effect] = allies.effects.as_slice() else {
        return None;
    };
    let ally_may = ally_may_effect.downcast_ref::<crate::effects::MayEffect>()?;
    let [ally_scry_effect] = ally_may.effects.as_slice() else {
        return None;
    };
    let controller_scry = controller_scry_effect.downcast_ref::<crate::effects::ScryEffect>()?;
    let ally_scry = ally_scry_effect.downcast_ref::<crate::effects::ScryEffect>()?;
    if controller_may.decider.is_some()
        || controller_may.fallback != crate::decision::FallbackStrategy::Decline
        || allies.tag.as_str() != "voted_with_you"
        || ally_may.decider != Some(PlayerFilter::IteratedPlayer)
        || ally_may.fallback != crate::decision::FallbackStrategy::Decline
        || controller_scry.player != PlayerFilter::You
        || ally_scry.player != PlayerFilter::IteratedPlayer
        || controller_scry.count != ally_scry.count
    {
        return None;
    }
    Some(format!(
        "Whenever players finish voting, you and each opponent who voted for a choice you voted for may scry {}",
        describe_value(&controller_scry.count),
    ))
}

fn describe_condition_collection_choice_trigger(ability: &Ability) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if !triggered.choices.is_empty() || triggered.presentation_label.is_some() {
        return None;
    }
    let condition = triggered.intervening_if.as_ref()?;
    let Condition::PlayerControls {
        player: PlayerFilter::DamagedPlayer,
        filter: condition_filter,
    } = condition
    else {
        return None;
    };
    let [tag_effect, sequence_effect] = triggered.effects.flattened_default_effects() else {
        return None;
    };
    let sequence = sequence_effect.downcast_ref::<crate::effects::SequenceEffect>()?;
    if sequence.surface != ironsmith_core::SequenceSurface::Coordinated
        || sequence.result_label.is_some()
    {
        return None;
    }
    let [choose_effect, gain_effect, untap_effect] = sequence.effects.as_slice() else {
        return None;
    };
    let choose = choose_effect.downcast_ref::<crate::effects::ChooseObjectsEffect>()?;
    let mut choice_basis = choose.filter.clone();
    choice_basis.controller = None;
    choice_basis.union_surface = Default::default();
    let mut condition_basis = condition_filter.clone();
    condition_basis.union_surface = Default::default();
    if choose.filter.controller != Some(PlayerFilter::DamagedPlayer)
        || choice_basis != condition_basis
    {
        return None;
    }
    let payload = [
        tag_effect.clone(),
        choose_effect.clone(),
        gain_effect.clone(),
        untap_effect.clone(),
    ];
    let action =
        super::render_effects::describe_condition_collection_choice_gain_control_then_untap(
            &payload,
        )?;
    Some(format!(
        "{}, if {}, {}",
        triggered.trigger.display(),
        super::normalize_common::describe_condition(condition),
        lowercase_first(&action),
    ))
}

#[cfg(test)]
#[test]
fn allied_voting_scry_requires_the_runtime_vote_result_tag() {
    let ability_for_tag = |tag: &str| {
        Ability::triggered(
            crate::triggers::Trigger::players_finish_voting(),
            vec![
                Effect::may(vec![Effect::scry(2)]),
                Effect::for_each_tagged_player(
                    tag,
                    vec![Effect::may_player(
                        PlayerFilter::IteratedPlayer,
                        vec![Effect::scry_player(2, PlayerFilter::IteratedPlayer)],
                    )],
                ),
            ],
        )
    };

    assert_eq!(
        describe_voting_allied_players_shared_optional_scry(&ability_for_tag("voted_with_you"))
            .as_deref(),
        Some(
            "Whenever players finish voting, you and each opponent who voted for a choice you voted for may scry 2"
        ),
    );
    assert!(
        describe_voting_allied_players_shared_optional_scry(&ability_for_tag("other_voters"))
            .is_none()
    );
}

#[cfg(all(test, ironsmith_runtime_parser_tests))]
#[test]
fn attachment_qualified_return_requires_the_condition_reference_tag() {
    let oracle = "Whenever another nontoken creature you control dies, if it was enchanted or equipped, return it to its owner's hand.";
    let definition = crate::cards::builders::CardDefinitionBuilder::new(
        crate::ids::CardId::new(),
        "Attachment Return Probe",
    )
    .card_types(vec![CardType::Creature])
    .parse_text(oracle)
    .expect("attachment-qualified return should parse");
    let ability = definition.abilities.first().expect("triggered ability");
    assert_eq!(
        describe_attachment_qualified_triggering_object_return(ability).as_deref(),
        Some(oracle.trim_end_matches('.')),
    );

    let mut changed_tag = ability.clone();
    let AbilityKind::Triggered(triggered) = &mut changed_tag.kind else {
        panic!("expected triggered ability");
    };
    triggered.effects = crate::resolution::ResolutionProgram::new(vec![
        crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(
            crate::effects::ReturnToHandEffect::with_spec(ChooseSpec::Tagged(TagKey::from(
                "other_attachment",
            ))),
        )]),
    ]);
    assert!(describe_attachment_qualified_triggering_object_return(&changed_tag).is_none());
}

fn conditioned_combat_keyword_application(
    effect: &Effect,
) -> Option<(
    &crate::effects::ApplyContinuousEffect,
    &crate::ConditionExpr,
)> {
    if let Some(apply) = effect.downcast_ref::<crate::effects::ApplyContinuousEffect>() {
        return Some((apply, apply.condition.as_ref()?));
    }
    let conditional = effect.downcast_ref::<crate::effects::ConditionalEffect>()?;
    if !matches!(
        conditional.surface,
        ironsmith_core::ConditionalSurface::LeadingIf
            | ironsmith_core::ConditionalSurface::TrailingIf
    ) || !conditional.if_false.is_empty()
    {
        return None;
    }
    let [inner] = conditional.if_true.as_slice() else {
        return None;
    };
    let apply = inner.downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    apply
        .condition
        .is_none()
        .then_some((apply, &conditional.condition))
}

fn modeled_each_combat_matching_keyword_grant(
    ability: &Ability,
) -> Option<(&ObjectFilter, String)> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if !triggered.choices.is_empty()
        || triggered.intervening_if.is_some()
        || triggered.presentation_label.is_some()
    {
        return None;
    }
    let combat = triggered
        .trigger
        .downcast_ref::<crate::triggers::BeginningOfCombatTrigger>()?;
    if combat.player != PlayerFilter::Any {
        return None;
    }
    let [segment] = triggered.effects.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() || segment.starts_new_source_line {
        return None;
    }
    let [effect] = segment.default_effects.as_slice() else {
        return None;
    };
    let (apply, condition) = conditioned_combat_keyword_application(effect)?;
    if apply.target_spec.is_some()
        || !apply.additional_modifications.is_empty()
        || !apply.runtime_modifications.is_empty()
        || apply.until != Until::EndOfTurn
        || apply.source_type.is_some()
        || apply.source_reference_surface.is_some()
        || apply.set_quantifier_surface.is_some()
        || apply.type_retention_surface.is_some()
        || apply.animation_pt_surface.is_some()
        || apply.animation_duration_surface.is_some()
        || !apply.lock_filter_at_resolution
        || apply.resolve_set_pt_values_at_resolution
        || apply.require_creature_target
    {
        return None;
    }
    let crate::continuous::EffectTarget::Filter(target_filter) = &apply.target else {
        return None;
    };
    let crate::continuous::Modification::AddAbility(granted) = apply.modification.as_ref()? else {
        return None;
    };
    if !granted.is_keyword() {
        return None;
    }
    let keyword_id = granted.id();
    let keyword = granted
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    let Condition::PlayerControls {
        player: PlayerFilter::You,
        filter: condition_filter,
    } = condition
    else {
        return None;
    };
    if condition_filter.static_abilities.as_slice() != [keyword_id]
        || !condition_filter.excluded_static_abilities.is_empty()
        || !condition_filter.ability_markers.is_empty()
        || !condition_filter.excluded_ability_markers.is_empty()
    {
        return None;
    }
    let mut condition_basis = condition_filter.clone();
    condition_basis.static_abilities.clear();
    if &condition_basis != target_filter {
        return None;
    }
    Some((target_filter, keyword))
}

#[derive(Clone, Copy, PartialEq)]
enum MatchingKeywordGrantTarget<'a> {
    Filter(&'a ObjectFilter),
    Source(&'a crate::target::SourceReferenceSurface),
    Triggering(&'a TagKey),
}

fn modeled_matching_keyword_grant(
    ability: &Ability,
) -> Option<(
    &crate::triggers::Trigger,
    MatchingKeywordGrantTarget<'_>,
    ObjectFilter,
    String,
)> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    if !triggered.choices.is_empty()
        || triggered.intervening_if.is_some()
        || triggered.presentation_label.is_some()
    {
        return None;
    }
    let [segment] = triggered.effects.segments.as_slice() else {
        return None;
    };
    if !segment.self_replacements.is_empty() || segment.starts_new_source_line {
        return None;
    }
    let effect = match segment.default_effects.as_slice() {
        [effect] => effect,
        [tag_effect, effect]
            if tag_effect
                .downcast_ref::<crate::effects::TagTriggeringObjectEffect>()
                .is_some_and(|tag| tag.tag.as_str() == "triggering") =>
        {
            effect
        }
        _ => return None,
    };
    let (apply, condition) = conditioned_combat_keyword_application(effect)?;
    if !apply.additional_modifications.is_empty()
        || !apply.runtime_modifications.is_empty()
        || apply.until != Until::EndOfTurn
        || apply.source_type.is_some()
        || apply.set_quantifier_surface.is_some()
        || apply.type_retention_surface.is_some()
        || apply.animation_pt_surface.is_some()
        || apply.animation_duration_surface.is_some()
        || apply.resolve_set_pt_values_at_resolution
        || apply.require_creature_target
    {
        return None;
    }
    let target = match (&apply.target, &apply.target_spec) {
        (crate::continuous::EffectTarget::Filter(filter), None)
            if apply.lock_filter_at_resolution && apply.source_reference_surface.is_none() =>
        {
            MatchingKeywordGrantTarget::Filter(filter)
        }
        (crate::continuous::EffectTarget::Source, Some(spec))
            if !apply.lock_filter_at_resolution
                && matches!(spec.unhinted(), ChooseSpec::Source) =>
        {
            let surface = apply.source_reference_surface.as_ref()?;
            if !matches!(surface.display_text().as_str(), "this creature" | "it") {
                return None;
            }
            MatchingKeywordGrantTarget::Source(surface)
        }
        (crate::continuous::EffectTarget::Source, Some(spec))
            if !apply.lock_filter_at_resolution
                && apply.source_reference_surface.is_none()
                && matches!(spec.unhinted(), ChooseSpec::Tagged(tag) if tag.as_str() == "triggering") =>
        {
            let ChooseSpec::Tagged(tag) = spec.unhinted() else {
                unreachable!();
            };
            MatchingKeywordGrantTarget::Triggering(tag)
        }
        _ => return None,
    };
    let crate::continuous::Modification::AddAbility(granted) = apply.modification.as_ref()? else {
        return None;
    };
    if !granted.is_keyword() {
        return None;
    }
    let keyword_id = granted.id();
    let keyword = granted
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    let Condition::PlayerControls {
        player: PlayerFilter::You,
        filter: condition_filter,
    } = condition
    else {
        return None;
    };
    if condition_filter.static_abilities.as_slice() != [keyword_id]
        || !condition_filter.excluded_static_abilities.is_empty()
        || !condition_filter.ability_markers.is_empty()
        || !condition_filter.excluded_ability_markers.is_empty()
    {
        return None;
    }
    let mut condition_basis = condition_filter.clone();
    condition_basis.static_abilities.clear();
    Some((&triggered.trigger, target, condition_basis, keyword))
}

/// Rejoin adjacent keyword-grant triggers whose typed condition asks whether
/// the same candidate set has the exact keyword granted by that branch. This
/// covers attack, each-combat, and your-combat ladders without relying on
/// card names or rendered-line coincidence.
fn describe_structural_matching_keyword_grant_ladder(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let mut shared_trigger = None;
    let mut shared_target = None;
    let mut shared_condition = None;
    let mut keywords = Vec::new();
    let mut consumed = 0usize;
    for ability in abilities {
        let Some((trigger, target, condition, keyword)) = modeled_matching_keyword_grant(ability)
        else {
            break;
        };
        if let Some(shared) = shared_trigger {
            if shared != trigger {
                break;
            }
        } else {
            shared_trigger = Some(trigger);
        }
        if let Some(shared) = shared_target {
            if shared != target {
                break;
            }
        } else {
            shared_target = Some(target);
        }
        if let Some(shared) = &shared_condition {
            if shared != &condition {
                break;
            }
        } else {
            shared_condition = Some(condition);
        }
        if keywords.contains(&keyword) {
            break;
        }
        keywords.push(keyword);
        consumed += 1;
    }
    if consumed < 3 {
        return None;
    }
    let trigger = shared_trigger?;
    let target = shared_target?;
    let condition = shared_condition?;
    let (subject, singular) = match target {
        MatchingKeywordGrantTarget::Filter(target) => {
            crate::static_abilities::grant_subject_with_set_quantifier(target, None)
        }
        MatchingKeywordGrantTarget::Source(surface) => (surface.display_text(), true),
        MatchingKeywordGrantTarget::Triggering(_) => ("it".to_string(), true),
    };
    let verb = if singular { "gains" } else { "gain" };
    let condition_clause = if condition.zone == Some(Zone::Battlefield)
        && condition.controller == Some(PlayerFilter::You)
    {
        let mut controlled = condition.clone();
        controlled.zone = None;
        controlled.controller = None;
        format!(
            "you control {} with {}",
            with_indefinite_article(strip_leading_article(&controlled.description())),
            keywords[0]
        )
    } else {
        format!(
            "{} has {}",
            with_indefinite_article(strip_leading_article(&condition.description())),
            keywords[0]
        )
    };
    Some((
        format!(
            "{}, {subject} {verb} {} until end of turn if {condition_clause}. The same is true for {}",
            trigger.display(),
            keywords[0],
            render_keyword_list(&keywords[1..], false),
        ),
        consumed,
    ))
}

/// Rejoin a run of independent "at the beginning of each combat" keyword
/// grants when every typed condition asks whether the exact same affected set
/// already has that branch's keyword. This preserves all executable grants,
/// including branches lowered through an explicit conditional wrapper.
fn describe_structural_each_combat_keyword_grant_ladder(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let mut target_filter = None;
    let mut keywords = Vec::new();
    let mut consumed = 0usize;
    for ability in abilities {
        let Some((target, keyword)) = modeled_each_combat_matching_keyword_grant(ability) else {
            break;
        };
        if let Some(shared) = target_filter {
            if shared != target {
                break;
            }
        } else {
            target_filter = Some(target);
        }
        if keywords.contains(&keyword) {
            break;
        }
        keywords.push(keyword);
        consumed += 1;
    }
    if consumed < 3 {
        return None;
    }
    let target_filter = target_filter?;
    let (subject, singular) =
        crate::static_abilities::grant_subject_with_set_quantifier(target_filter, None);
    if singular {
        return None;
    }
    let condition_noun = target_filter.description();
    let condition_noun = with_indefinite_article(strip_leading_article(&condition_noun));
    Some((
        format!(
            "At the beginning of each combat, {subject} gain {} until end of turn if {condition_noun} has {}. The same is true for {}",
            keywords[0],
            keywords[0],
            render_keyword_list(&keywords[1..], false),
        ),
        consumed,
    ))
}

fn describe_structural_attached_zero_life_rule(ability: &Ability) -> Option<String> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::Conditional { ability, condition } = &model.payload
    else {
        return None;
    };
    let Condition::AttachedToSourceMatches(filter) = condition else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::RuleRestriction {
        restriction,
        additional_restrictions,
        display,
    } = &ability.payload
    else {
        return None;
    };
    if filter != &ObjectFilter::creature()
        || restriction != &ironsmith_core::Restriction::LoseGame(PlayerFilter::You)
        || !additional_restrictions.is_empty()
        || display != "you don't lose the game for having 0 or less life"
    {
        return None;
    }
    Some(
        "As long as this Equipment is attached to a creature, you don't lose the game for having 0 or less life"
            .to_string(),
    )
}

fn described_mana_value_tag(value: &Value) -> Option<&crate::tag::TagKey> {
    let Value::ManaValueOf(spec) = value.unhinted() else {
        return None;
    };
    let ChooseSpec::Tagged(tag) = spec.base() else {
        return None;
    };
    Some(tag)
}

fn describe_structural_equipped_draw_reveal_mana_value_pump(ability: &Ability) -> Option<String> {
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    let attacks = triggered
        .trigger
        .downcast_ref::<crate::triggers::combat::AttacksTrigger>()?;
    let filter = &attacks.filter;
    let mut equipped_creature = ObjectFilter::creature();
    equipped_creature.set_explicit_card_type_noun(Some(CardType::Creature));
    equipped_creature = equipped_creature
        .match_tagged(
            "equipped",
            crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        )
        .in_zone(Zone::Battlefield);
    if filter != &equipped_creature
        || attacks.one_or_more
        || attacks.min_total_attackers != 1
        || attacks.max_total_attackers.is_some()
        || !triggered.choices.is_empty()
        || triggered.intervening_if.is_some()
    {
        return None;
    }
    let [draw_segment, result_segment] = triggered.effects.segments.as_slice() else {
        return None;
    };
    if !draw_segment.self_replacements.is_empty()
        || draw_segment.starts_new_source_line
        || !result_segment.self_replacements.is_empty()
    {
        return None;
    }
    let [tag_triggering, draw_sequence] = draw_segment.default_effects.as_slice() else {
        return None;
    };
    let tag_triggering =
        tag_triggering.downcast_ref::<crate::effects::TagTriggeringObjectEffect>()?;
    let draw_sequence = draw_sequence.downcast_ref::<crate::effects::SequenceEffect>()?;
    let [draw_root, reveal_root] = draw_sequence.effects.as_slice() else {
        return None;
    };
    let draw_tagged = draw_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let draw = draw_tagged
        .effect
        .downcast_ref::<crate::effects::DrawCardsEffect>()?;
    let reveal = reveal_root.downcast_ref::<crate::effects::RevealTaggedEffect>()?;
    if draw.count != Value::Fixed(1)
        || draw.player != PlayerFilter::You
        || reveal.tag != draw_tagged.tag
    {
        return None;
    }
    let [result_sequence] = result_segment.default_effects.as_slice() else {
        return None;
    };
    let result_sequence = result_sequence.downcast_ref::<crate::effects::SequenceEffect>()?;
    let [pump_root, lose_root] = result_sequence.effects.as_slice() else {
        return None;
    };
    let pump_tagged = pump_root.downcast_ref::<crate::effects::TaggedEffect>()?;
    let apply = pump_tagged
        .effect
        .downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
    let lose = lose_root.downcast_ref::<crate::effects::LoseLifeEffect>()?;
    let [
        crate::effects::continuous::RuntimeModification::ModifyPowerToughness { power, toughness },
    ] = apply.runtime_modifications.as_slice()
    else {
        return None;
    };
    let Some(power_tag) = described_mana_value_tag(power) else {
        return None;
    };
    if described_mana_value_tag(toughness) != Some(power_tag)
        || described_mana_value_tag(&lose.amount) != Some(power_tag)
        || power_tag != &draw_tagged.tag
        || !matches!(
            apply.target_spec.as_ref().map(ChooseSpec::base),
            Some(ChooseSpec::Tagged(tag)) if tag == &tag_triggering.tag
        )
        || apply.until != crate::effect::Until::EndOfTurn
        || apply.modification.is_some()
        || !apply.additional_modifications.is_empty()
        || apply.condition.is_some()
        || lose.player != ChooseSpec::Player(PlayerFilter::You)
    {
        return None;
    }
    Some(
        "Whenever equipped creature attacks, draw a card and reveal it. The creature gets +X/+X until end of turn and you lose X life, where X is that card's mana value"
            .to_string(),
    )
}

fn describe_structural_discard_equip(ability: &Ability) -> Option<String> {
    let AbilityKind::Activated(activated) = &ability.kind else {
        return None;
    };
    let [cost] = activated.mana_cost.as_all()? else {
        return None;
    };
    let discard = cost
        .effect_ref()?
        .downcast_ref::<crate::effects::DiscardEffect>()?;
    let [segment] = activated.effects.segments.as_slice() else {
        return None;
    };
    let target = super::render_effects::structural_equip_target(activated)?;
    if discard.count != Value::Fixed(1)
        || discard.player != PlayerFilter::You
        || discard.random
        || discard.any_number
        || discard.card_filter.is_some()
        || activated.timing != crate::ability::ActivationTiming::SorcerySpeed
        || !(activated.choices.is_empty() || activated.choices.as_slice() == [target.clone()])
        || !segment.self_replacements.is_empty()
        || target
            != ChooseSpec::target(ChooseSpec::Object(
                ObjectFilter::creature().controlled_by(PlayerFilter::You),
            ))
    {
        return None;
    }
    Some("Equip—Discard a card".to_string())
}

fn describe_structural_each_combat_keyword_pt_ladder(ability: &Ability) -> Option<String> {
    const KEYWORDS: &[crate::static_abilities::StaticAbilityId] = &[
        crate::static_abilities::StaticAbilityId::Flying,
        crate::static_abilities::StaticAbilityId::FirstStrike,
        crate::static_abilities::StaticAbilityId::DoubleStrike,
        crate::static_abilities::StaticAbilityId::Deathtouch,
        crate::static_abilities::StaticAbilityId::Haste,
        crate::static_abilities::StaticAbilityId::Hexproof,
        crate::static_abilities::StaticAbilityId::Indestructible,
        crate::static_abilities::StaticAbilityId::Lifelink,
        crate::static_abilities::StaticAbilityId::Menace,
        crate::static_abilities::StaticAbilityId::Protection,
        crate::static_abilities::StaticAbilityId::Reach,
        crate::static_abilities::StaticAbilityId::Trample,
        crate::static_abilities::StaticAbilityId::Vigilance,
        crate::static_abilities::StaticAbilityId::Partner,
    ];
    let AbilityKind::Triggered(triggered) = &ability.kind else {
        return None;
    };
    let combat = triggered
        .trigger
        .downcast_ref::<crate::triggers::BeginningOfCombatTrigger>()?;
    if combat.player != PlayerFilter::Any
        || !triggered.choices.is_empty()
        || triggered.intervening_if.is_some()
    {
        return None;
    }
    let effects = triggered.effects.flattened_default_effects();
    if effects.len() != KEYWORDS.len() {
        return None;
    }
    for (effect, keyword) in effects.iter().zip(KEYWORDS) {
        let effect = effect
            .downcast_ref::<crate::effects::TaggedEffect>()
            .map(|tagged| tagged.effect.as_ref())
            .unwrap_or(effect);
        let apply = effect.downcast_ref::<crate::effects::ApplyContinuousEffect>()?;
        let crate::continuous::EffectTarget::Filter(filter) = &apply.target else {
            return None;
        };
        let [
            crate::effects::continuous::RuntimeModification::ModifyPowerToughness {
                power,
                toughness,
            },
        ] = apply.runtime_modifications.as_slice()
        else {
            return None;
        };
        let mut basis = filter.clone();
        if basis.static_abilities.as_slice() != [*keyword] {
            return None;
        }
        basis.static_abilities.clear();
        let mut expected = ObjectFilter::creature().controlled_by(PlayerFilter::You);
        expected.other = true;
        expected.set_explicit_card_type_noun(Some(CardType::Creature));
        if basis != expected
            || power != &Value::Fixed(1)
            || toughness != &Value::Fixed(1)
            || apply.until != crate::effect::Until::EndOfTurn
            || apply.target_spec.is_some()
            || apply.modification.is_some()
            || !apply.additional_modifications.is_empty()
            || apply.condition.is_some()
        {
            return None;
        }
    }
    Some("At the beginning of each combat, until end of turn, each other creature you control gets +1/+1 if it has flying, +1/+1 if it has first strike, and so on for double strike, deathtouch, haste, hexproof, indestructible, lifelink, menace, protection, reach, trample, vigilance, and partner".to_string())
}

#[cfg(test)]
mod pact_and_keyword_pt_ladder_tests {
    use super::*;

    #[test]
    fn keyword_pt_ladder_requires_every_typed_branch() {
        let oracle = "At the beginning of each combat, until end of turn, each other creature you control gets +1/+1 if it has flying, +1/+1 if it has first strike, and so on for double strike, deathtouch, haste, hexproof, indestructible, lifelink, menace, protection, reach, trample, vigilance, and partner.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Keyword P/T Ladder Probe",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(oracle)
        .expect("keyword P/T ladder should parse");
        let ability = definition.abilities.first().expect("triggered ability");
        assert_eq!(
            describe_structural_each_combat_keyword_pt_ladder(ability).as_deref(),
            Some(oracle.trim_end_matches('.')),
        );

        let mut missing_partner = ability.clone();
        let AbilityKind::Triggered(triggered) = &mut missing_partner.kind else {
            panic!("expected triggered ability");
        };
        triggered
            .effects
            .segments
            .iter_mut()
            .rev()
            .find(|segment| !segment.default_effects.is_empty())
            .expect("keyword ladder should retain a final effect segment")
            .default_effects
            .pop();
        triggered.effects =
            crate::resolution::ResolutionProgram::new(triggered.effects.segments.clone());
        assert_eq!(
            describe_structural_each_combat_keyword_pt_ladder(&missing_partner),
            None,
            "{missing_partner:#?}"
        );
    }

    #[test]
    fn pact_surfaces_require_the_complete_typed_program() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Pact Surface Probe",
        )
        .card_types(vec![CardType::Artifact])
        .subtypes(vec![crate::types::Subtype::Equipment])
        .parse_text("As long as this Equipment is attached to a creature, you don't lose the game for having 0 or less life.\nWhenever equipped creature attacks, draw a card and reveal it. The creature gets +X/+X until end of turn and you lose X life, where X is that card's mana value.\nEquip—Discard a card.")
        .expect("Pact surface probe should parse");
        assert!(describe_structural_attached_zero_life_rule(&definition.abilities[0]).is_some());
        assert!(
            describe_structural_equipped_draw_reveal_mana_value_pump(&definition.abilities[1])
                .is_some(),
            "{:#?}",
            definition.abilities[1]
        );
        assert!(
            describe_structural_discard_equip(&definition.abilities[2]).is_some(),
            "{:#?}",
            definition.abilities[2]
        );
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition).join("\n"),
            "As long as this Equipment is attached to a creature, you don't lose the game for having 0 or less life.\nWhenever equipped creature attacks, draw a card and reveal it. The creature gets +X/+X until end of turn and you lose X life, where X is that card's mana value.\nEquip—Discard a card.",
            "the strict Pact surfaces must dispatch through the public renderer"
        );

        let mut wrong_draw_count = definition.abilities[1].clone();
        let AbilityKind::Triggered(triggered) = &mut wrong_draw_count.kind else {
            panic!("expected Pact trigger");
        };
        let sequence = triggered.effects.segments[0].default_effects[1]
            .downcast_ref::<crate::effects::SequenceEffect>()
            .expect("draw sequence")
            .clone();
        let mut altered = sequence;
        altered.effects[0] = crate::effect::Effect::new(crate::effects::DrawCardsEffect::you(2))
            .tag("__drawn_revealed_card__");
        triggered.effects.segments[0].default_effects[1] = crate::effect::Effect::new(altered);
        assert_eq!(
            describe_structural_equipped_draw_reveal_mana_value_pump(&wrong_draw_count),
            None,
        );

        let mut wrong_life_player = definition.abilities[1].clone();
        let AbilityKind::Triggered(triggered) = &mut wrong_life_player.kind else {
            panic!("expected Pact trigger");
        };
        let mut result = triggered.effects.segments[1].default_effects[0]
            .downcast_ref::<crate::effects::SequenceEffect>()
            .expect("result sequence")
            .clone();
        let mut lose = result.effects[1]
            .downcast_ref::<crate::effects::LoseLifeEffect>()
            .expect("life-loss result")
            .clone();
        lose.player = ChooseSpec::Player(PlayerFilter::Opponent);
        result.effects[1] = crate::effect::Effect::new(lose);
        triggered.effects.segments[1].default_effects[0] = crate::effect::Effect::new(result);
        assert_eq!(
            describe_structural_equipped_draw_reveal_mana_value_pump(&wrong_life_player),
            None,
        );
    }
}

#[cfg(test)]
mod each_combat_keyword_grant_ladder_tests {
    use super::*;

    #[test]
    fn all_matching_keyword_branches_rejoin_across_direct_and_wrapped_conditions() {
        let oracle = "At the beginning of each combat, creatures you control gain first strike until end of turn if a creature you control has first strike. The same is true for flying, deathtouch, double strike, haste, hexproof, indestructible, lifelink, menace, reach, skulk, trample, and vigilance.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Combat Keyword Ladder Probe",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(oracle)
        .expect("matching combat keyword ladder should compile");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![oracle.to_string()]
        );
    }
}

#[cfg(test)]
mod matching_keyword_grant_ladder_tests {
    use super::*;

    #[test]
    fn typed_keyword_conditions_rejoin_across_combat_trigger_families() {
        for (name, card_type, oracle) in [
            (
                "Bleeding Effect",
                CardType::Enchantment,
                "At the beginning of combat on your turn, creatures you control gain flying until end of turn if a creature card in your graveyard has flying. The same is true for first strike, double strike, deathtouch, hexproof, indestructible, lifelink, menace, reach, trample, and vigilance.",
            ),
            (
                "Majestic Myriarch",
                CardType::Creature,
                "At the beginning of each combat, this creature gains flying until end of turn if you control a creature with flying. The same is true for first strike, double strike, deathtouch, haste, hexproof, indestructible, lifelink, menace, reach, trample, and vigilance.",
            ),
            (
                "Thunderous Orator",
                CardType::Creature,
                "Whenever this creature attacks, it gains flying until end of turn if you control a creature with flying. The same is true for first strike, double strike, deathtouch, indestructible, lifelink, menace, and trample.",
            ),
        ] {
            let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
                crate::ids::CardId::new(),
                name,
            )
            .card_types(vec![card_type])
            .parse_text(oracle)
            .expect("matching keyword ladder should compile");

            for (index, ability) in definition.abilities.iter().enumerate() {
                assert!(
                    modeled_matching_keyword_grant(ability).is_some(),
                    "{name} branch {index} must retain the typed keyword/condition correlation"
                );
            }

            assert_eq!(
                crate::compiled_text::compiled_text_lines(&definition),
                vec![oracle.to_string()],
                "{name}"
            );

            let mut changed_trigger = definition.abilities.clone();
            let AbilityKind::Triggered(triggered) = &mut changed_trigger[1].kind else {
                panic!("expected triggered keyword branch");
            };
            triggered.trigger = crate::triggers::Trigger::beginning_of_upkeep(PlayerFilter::You);
            assert_eq!(
                describe_structural_matching_keyword_grant_ladder(&changed_trigger),
                None,
                "a changed trigger must not merge unrelated abilities"
            );
        }
    }
}

fn cast_as_flash_cleanup_sacrifice_surface() -> &'static str {
    "You may cast this spell as though it had flash. If you cast it any time a sorcery couldn't have been cast, the controller of the permanent it becomes sacrifices it at the beginning of the next cleanup step."
}

fn is_cast_as_flash_cleanup_sacrifice_program(program: &crate::ResolutionProgram) -> bool {
    let [effect] = program.flattened_default_effects() else {
        return false;
    };
    let Some(conditional) = effect.downcast_ref::<crate::effects::ConditionalEffect>() else {
        return false;
    };
    let cast_outside_sorcery_timing = |cast: &Condition, timing: &Condition| {
        matches!(cast, Condition::SourceWasCast)
            && matches!(timing, Condition::Not(inner)
                if matches!(inner.as_ref(), Condition::ThisSpellWasCastAtSorceryTiming))
    };
    let Condition::And(left, right) = &conditional.condition else {
        return false;
    };
    if !cast_outside_sorcery_timing(left.as_ref(), right.as_ref())
        && !cast_outside_sorcery_timing(right.as_ref(), left.as_ref())
    {
        return false;
    }
    if !conditional.if_false.is_empty() {
        return false;
    }
    let [schedule_effect] = conditional.if_true.as_slice() else {
        return false;
    };
    let Some(schedule) =
        schedule_effect.downcast_ref::<crate::effects::ScheduleDelayedTriggerEffect>()
    else {
        return false;
    };
    if !schedule.one_shot
        || !schedule
            .trigger
            .downcast_ref::<crate::triggers::BeginningOfCleanupStepTrigger>()
            .is_some_and(|trigger| trigger.next && trigger.player == PlayerFilter::Any)
    {
        return false;
    }
    let [sacrifice_effect] = schedule.effects.flattened_default_effects() else {
        return false;
    };
    sacrifice_effect
        .downcast_ref::<crate::effects::SacrificeTargetEffect>()
        .is_some_and(|sacrifice| matches!(sacrifice.target.base(), ChooseSpec::Source))
}

/// Render the typed delirium maximum-hand-size rule directly. The generic
/// conditional fallback must use a source grant when the leaf rule has no
/// native condition hook, which is executable but otherwise exposes both the
/// zero-threshold leaf and the outer four-type condition as separate prose.
fn describe_structural_delirium_maximum_hand_size(ability: &Ability) -> Option<String> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    if model.id
        != Some(crate::static_abilities::StaticAbilityId::MaximumHandSizeSevenMinusYourGraveyardCardTypes)
    {
        return None;
    }

    let exact_leaf = |ability: &crate::static_abilities::CompiledStaticAbility, minimum: u32| {
        matches!(
            &ability.payload,
            ironsmith_core::StaticAbilityPayload::MaximumHandSizeSevenMinusYourGraveyardCardTypes {
                player: PlayerFilter::Opponent,
                min_card_types,
            } if *min_card_types == minimum
        )
    };
    let exact = match &model.payload {
        ironsmith_core::StaticAbilityPayload::Conditional { ability, condition } => {
            exact_leaf(ability, 0)
                && matches!(
                    condition,
                    Condition::PlayerHasCardTypesInGraveyardOrMore {
                        player: PlayerFilter::You,
                        count: 4,
                    }
                )
        }
        ironsmith_core::StaticAbilityPayload::MaximumHandSizeSevenMinusYourGraveyardCardTypes {
            player: PlayerFilter::Opponent,
            min_card_types: 4,
        } => true,
        _ => false,
    };
    exact.then(|| {
        "Delirium — As long as there are four or more card types among cards in your graveyard, each opponent's maximum hand size is equal to seven minus the number of those card types"
            .to_string()
    })
}

fn describe_structural_conditional_additional_land_play(ability: &Ability) -> Option<String> {
    if ability.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let model = static_ability.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: inner,
        condition,
    } = &model.payload
    else {
        return None;
    };
    if !matches!(
        inner.payload,
        ironsmith_core::StaticAbilityPayload::AdditionalLandPlays(1)
    ) {
        return None;
    }
    let Condition::CountComparison {
        comparison: Comparison::GreaterThanOrEqual(1),
        display: Some(display),
        ..
    } = condition
    else {
        return None;
    };
    let display = display.trim().to_ascii_lowercase();
    display.starts_with("you control another ").then(|| {
        format!("As long as {display}, you may play an additional land on each of your turns")
    })
}

fn compiled_lines_inner(def: &CardDefinition) -> Vec<String> {
    let mut out = Vec::new();
    let mut leading_alternative_cast_lines = Vec::new();
    let mut alternative_cast_lines = Vec::new();
    let mut deferred_spell_optional_lines = Vec::new();
    let subject = subject_for_card(&def.card);
    let rewrite_it_deals = def.card.card_types.contains(&CardType::Creature)
        || def.card.card_types.contains(&CardType::Artifact)
        || def.card.card_types.contains(&CardType::Land)
        || def.card.card_types.contains(&CardType::Planeswalker)
        || def.card.card_types.contains(&CardType::Battle);
    let spell_like_card = def.card.card_types.contains(&CardType::Instant)
        || def.card.card_types.contains(&CardType::Sorcery);
    let spell_effects_for_render = def.spell_effect.as_ref().map(|effects| {
        def.aura_attach_filter
            .as_ref()
            .map(|attachment| spell_program_without_encoded_aura_attachment(effects, attachment))
            .unwrap_or_else(|| effects.clone())
    });
    let cast_as_flash_cleanup_sacrifice_ability = spell_effects_for_render
        .as_ref()
        .filter(|program| is_cast_as_flash_cleanup_sacrifice_program(program))
        .and_then(|_| {
            def.abilities.iter().position(|ability| {
                matches!(
                    &ability.kind,
                    AbilityKind::Static(static_ability)
                        if static_ability.id() == crate::static_abilities::StaticAbilityId::Flash
                            && static_ability.authored_line_surface().is_some()
                )
            })
        });
    for (idx, method) in def.alternative_casts.iter().enumerate() {
        let line = describe_alternative_cast_with_qualified_reduction(def, method, idx)
            .unwrap_or_else(|| describe_alternative_cast_line(method, idx));
        let is_prototype = method.name().eq_ignore_ascii_case("Prototype")
            && method.prototype_power_toughness().is_some();
        if is_prototype
            || method.is_mutate()
            || matches!(method, AlternativeCastingMethod::Cleave { .. })
            || matches!(method, AlternativeCastingMethod::Suspend { .. })
            || matches!(
                method,
                AlternativeCastingMethod::FlashWithAdditionalCost { .. }
            )
            || line.contains("rather than pay this spell's mana cost")
        {
            leading_alternative_cast_lines.push(line);
        } else {
            alternative_cast_lines.push(line);
        }
    }
    out.extend(leading_alternative_cast_lines);
    let kicker_x_minimum_ability = def.abilities.iter().position(|ability| {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return false;
        };
        let Some(model) = static_ability.compiled_model() else {
            return false;
        };
        matches!(
            &model.payload,
            ironsmith_core::StaticAbilityPayload::ThisSpellXMinimum {
                minimum: Value::Fixed(1),
                display,
            } if display.trim_end_matches('.') == "X can't be 0"
        ) && ability.functional_zones.contains(&Zone::Stack)
    });
    for cost in &def.optional_costs {
        let mut line = describe_optional_cost_line(cost);
        if matches!(cost.kind, crate::cost::OptionalCostKind::Kicker)
            && cost.cost.mana_cost().is_some_and(|mana| mana.has_x())
            && kicker_x_minimum_ability.is_some()
        {
            line = format!("{}. X can't be 0.", line.trim_end_matches('.'));
        }
        if let Some(followup) = def
            .abilities
            .iter()
            .find_map(|ability| describe_repeatable_sacrifice_copy_followup(cost, ability))
        {
            line = format!("{}. {followup}", line.trim_end_matches('.'));
        }
        if spell_like_card && matches!(cost.kind, crate::cost::OptionalCostKind::Conspire) {
            deferred_spell_optional_lines.push(line);
        } else {
            out.push(line);
        }
    }
    let has_visible_gift_line = def
        .optional_costs
        .iter()
        .any(|cost| matches!(cost.kind, crate::cost::OptionalCostKind::Gift));
    let leading_aura_flash_ability = def.aura_attach_filter.as_ref().and_then(|_| {
        def.abilities.iter().position(|ability| {
            matches!(
                &ability.kind,
                AbilityKind::Static(static_ability)
                    if static_ability.id()
                        == crate::static_abilities::StaticAbilityId::Flash
            )
        })
    });
    if let Some(ability_idx) = leading_aura_flash_ability {
        if cast_as_flash_cleanup_sacrifice_ability == Some(ability_idx) {
            out.push(cast_as_flash_cleanup_sacrifice_surface().to_string());
        } else {
            let authored_surface = match &def.abilities[ability_idx].kind {
                AbilityKind::Static(static_ability) => static_ability.authored_line_surface(),
                _ => None,
            };
            out.push(authored_surface.unwrap_or_else(|| "Flash".to_string()));
        }
    }
    if let Some(filter) = &def.aura_attach_filter {
        let attachment_target = describe_enchant_filter(filter);
        if def.card.subtypes.contains(&Subtype::Equipment) {
            // The shared attachment-legality field models both Aura enchant
            // restrictions and Equipment's "can be attached only to"
            // restriction. Card type/subtype distinguishes their authored
            // rules surface without changing the executable legality filter.
            let attachment_target = with_indefinite_article(&attachment_target);
            out.push(format!(
                "This Equipment can be attached only to {attachment_target}"
            ));
        } else {
            out.push(format!("Enchant {attachment_target}"));
        }
    }
    let derived_final_chapter = def
        .abilities
        .iter()
        .filter_map(|ability| {
            if let AbilityKind::Triggered(triggered) = &ability.kind {
                triggered
                    .trigger
                    .saga_chapters()
                    .and_then(|chapters| chapters.iter().copied().max())
            } else {
                None
            }
        })
        .max();
    if let Some(max_chapter) = derived_final_chapter
        && let Some(roman) = chapter_number_to_roman(max_chapter)
    {
        out.push(format!(
            "(As this Saga enters and after your draw step, add a lore counter. Sacrifice after {roman}.)"
        ));
    }

    let has_begin_on_battlefield_pregame = def
        .abilities
        .iter()
        .any(ability_has_begin_on_battlefield_pregame);
    let render_leading_spell_abilities = spell_like_card
        && def.abilities.iter().enumerate().any(|(idx, ability)| {
            source_line_repeated_cascade_group_consumed(&def.abilities[idx..]).is_some()
                || (ability_precedes_spell_resolution(ability)
                    && !ability_is_rendered_with_alternative_cast(def, ability))
        });
    let push_abilities = |output: &mut Vec<String>| {
        let has_suspend = def
            .alternative_casts
            .iter()
            .any(|method| matches!(method, AlternativeCastingMethod::Suspend { .. }));
        let has_squad = def
            .optional_costs
            .iter()
            .any(|cost| matches!(cost.kind, crate::cost::OptionalCostKind::Squad));
        let source_has_delve = def.abilities.iter().any(|ability| {
            matches!(
                &ability.kind,
                AbilityKind::Static(static_ability)
                    if static_ability.id() == crate::static_abilities::StaticAbilityId::Delve
            )
        });
        let mut ability_idx = 0usize;
        while ability_idx < def.abilities.len() {
            let ability = &def.abilities[ability_idx];
            if kicker_x_minimum_ability == Some(ability_idx) {
                ability_idx += 1;
                continue;
            }
            if cast_as_flash_cleanup_sacrifice_ability == Some(ability_idx) {
                if leading_aura_flash_ability != Some(ability_idx) {
                    output.push(format!(
                        "Static ability {}: {}",
                        ability_idx + 1,
                        cast_as_flash_cleanup_sacrifice_surface()
                    ));
                }
                ability_idx += 1;
                continue;
            }
            if render_leading_spell_abilities
                && let Some(consumed) =
                    source_line_repeated_cascade_group_consumed(&def.abilities[ability_idx..])
            {
                ability_idx += consumed;
                continue;
            }
            if let Some(keyword_count) = source_line_keyword_group_count(ability) {
                output.push(source_line_keyword_group_sentinel(keyword_count));
                ability_idx += 1;
                continue;
            }
            if let Some(member_count) = source_line_static_group_count(ability) {
                if let Some(text) = describe_source_line_static_group(
                    &def.abilities[ability_idx + 1..],
                    member_count,
                    subject,
                ) {
                    let text =
                        if let Some(label) = source_line_static_group_presentation_label(ability) {
                            format!("{label} — {text}")
                        } else {
                            text
                        };
                    output.push(format!("Static ability {}: {text}", ability_idx + 2));
                    ability_idx += member_count + 1;
                } else {
                    // Unsupported structural families retain the preexisting
                    // per-ability surfaces; only the inert marker is skipped.
                    ability_idx += 1;
                }
                continue;
            }
            if leading_aura_flash_ability == Some(ability_idx) {
                ability_idx += 1;
                continue;
            }
            if ability_is_rendered_with_alternative_cast(def, ability) {
                ability_idx += 1;
                continue;
            }
            if render_leading_spell_abilities && ability_precedes_spell_resolution(ability) {
                ability_idx += 1;
                continue;
            }
            if has_suspend && is_suspend_helper_ability(ability) {
                ability_idx += 1;
                continue;
            }
            if is_conspire_helper_ability(ability) {
                ability_idx += 1;
                continue;
            }
            if is_repeatable_sacrifice_copy_helper(def, ability) {
                ability_idx += 1;
                continue;
            }
            if is_evoke_helper_ability(def, ability) {
                ability_idx += 1;
                continue;
            }
            if has_squad && is_squad_helper_ability(ability) {
                ability_idx += 1;
                continue;
            }
            if has_visible_gift_line && is_hidden_gift_etb_ability(ability) {
                ability_idx += 1;
                continue;
            }
            if ability_level_range_prefix(ability).is_some() {
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_structural_delirium_maximum_hand_size(ability) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_structural_conditional_additional_land_play(ability) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_structural_attached_zero_life_rule(ability) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_structural_equipped_draw_reveal_mana_value_pump(ability) {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_structural_discard_equip(ability) {
                output.push(format!("Activated ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_structural_each_combat_keyword_pt_ladder(ability) {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_each_combat_keyword_grant_ladder(&def.abilities[ability_idx..])
            {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_matching_keyword_grant_ladder(&def.abilities[ability_idx..])
            {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some(text) = describe_structural_counter_removed_this_way_damage_trigger(ability)
            {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_land_entry_damage_unless_creature_counter(ability, subject)
            {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_attachment_qualified_triggering_object_return(ability) {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_attachment_qualified_return_and_reattach(ability) {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_source_exiled_card_move_then_investigate(ability, subject)
            {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_triggering_card_type_exile_then_cast_permission(ability) {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_voting_allied_players_shared_optional_scry(ability) {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_condition_collection_choice_trigger(ability) {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) =
                describe_structural_counter_removal_damage_prevention(ability, subject)
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some((text, consumed)) = describe_structural_threshold_source_modifier_bundle(
                &def.abilities[ability_idx..],
                subject,
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_source_counter_threshold_static_bundle(
                    &def.abilities[ability_idx..],
                    subject,
                )
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some(text) = describe_structural_noncombat_source_anthem(ability, subject) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) =
                describe_structural_opponent_graveyard_source_anthem(ability, subject)
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_structural_starting_life_threshold_anthem(ability) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_structural_opening_hand_reveal(ability) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_conditioned_source_anthem_keyword_bundle(
                    &def.abilities[ability_idx..],
                    subject,
                )
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_attached_legendary_anthem_ward_bundle(
                    &def.abilities[ability_idx..],
                )
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) = describe_structural_threshold_anthem_must_attack_bundle(
                &def.abilities[ability_idx..],
                subject,
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) = describe_structural_labeled_animation_grant_bundle(
                &def.abilities[ability_idx..],
                subject,
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some(text) = describe_structural_threshold_source_keyword_grant(ability, subject)
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some((text, consumed)) = describe_structural_threshold_color_grant_bundle(
                &def.abilities[ability_idx..],
                subject,
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_conditioned_source_animation_annihilator_bundle(
                    &def.abilities[ability_idx..],
                )
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_repeated_cascade_grant_pair(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_quoted_static_grant_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_attached_subtype_base_pt_keyword_loss_bundle(
                    &def.abilities[ability_idx..],
                )
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_attached_anthem_condition_chain_bundle(
                    &def.abilities[ability_idx..],
                )
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_attached_anthem_otherwise_base_block_bundle(
                    &def.abilities[ability_idx..],
                )
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some(text) = describe_source_line_attached_keyword_otherwise_prevention_group(
                def.abilities
                    .get(ability_idx..ability_idx + 2)
                    .unwrap_or_default(),
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 2;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_attached_turtle_transform_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_attached_mixed_grant_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_keyword_maximum_blocker_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) = describe_structural_shared_subject_block_attack_bundle(
                &def.abilities[ability_idx..],
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_remove_all_abilities_untap_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_enchanted_combat_activation_restriction_bundle(
                    &def.abilities[ability_idx..],
                )
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) = describe_structural_conditional_anthem_otherwise_bundle(
                &def.abilities[ability_idx..],
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_dynamic_animation_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_source_fixed_animation_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((lines, consumed)) =
                describe_structural_anthem_then_color_subtype_bundle(&def.abilities[ability_idx..])
            {
                for (offset, text) in lines.into_iter().enumerate() {
                    output.push(format!(
                        "Static ability {}: {text}",
                        ability_idx + offset + 1
                    ));
                }
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_modifier_type_addition_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_authored_attached_transform_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_all_subtypes_scope_ladder(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) = describe_structural_delve_keyword_variant_ladder(
                &def.abilities[ability_idx..],
                subject,
                source_has_delve,
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) = describe_structural_removed_draft_keyword_ladder(
                &def.abilities[ability_idx..],
                subject,
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) = describe_structural_keyword_same_is_true_ladder(
                &def.abilities[ability_idx..],
                subject,
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) = describe_structural_five_basic_land_modifier_ladder(
                &def.abilities[ability_idx..],
                subject,
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_conditioned_no_defender_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_labeled_static_bundle(&def.abilities[ability_idx..], subject)
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some(text) =
                describe_structural_source_combat_presence_restriction(ability, subject)
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) =
                describe_structural_conditional_source_no_defender_permission(ability, subject)
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) =
                describe_structural_conditional_source_keyword_grant(ability, subject)
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some((text, consumed)) = describe_structural_anthem_remove_all_abilities_bundle(
                &def.abilities[ability_idx..],
            ) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_anthem_set_colors_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some(text) = describe_structural_can_block_additional_grant(ability) {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(keyword) = describe_structural_ascend_ability(ability) {
                output.push(format!("Keyword ability {}: {keyword}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(keyword) = describe_structural_reinforce_ability(ability) {
                output.push(format!("Keyword ability {}: {keyword}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(keyword) = describe_structural_equipment_token_keyword(ability) {
                output.push(format!("Keyword ability {}: {keyword}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(keyword) = describe_structural_evolve_keyword(ability) {
                output.push(format!("Keyword ability {}: {keyword}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(keyword) = describe_structural_training_keyword(ability) {
                output.push(format!("Keyword ability {}: {keyword}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(keyword) = describe_structural_renown_keyword(ability) {
                output.push(format!("Keyword ability {}: {keyword}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(keyword) = describe_structural_ingest_keyword(ability) {
                output.push(format!("Keyword ability {}: {keyword}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(keyword) = describe_structural_station_keyword(ability) {
                output.push(format!("Keyword ability {}: {keyword}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if let Some(text) = describe_graveyard_cast_entry_counter_trigger(ability) {
                output.push(format!("Triggered ability {}: {text}", ability_idx + 1));
                ability_idx += 1;
                continue;
            }
            if ability_idx + 1 < def.abilities.len()
                && let Some(partner_with) =
                    describe_structural_partner_with_pair(ability, &def.abilities[ability_idx + 1])
            {
                output.push(format!(
                    "Keyword ability {}: {partner_with}",
                    ability_idx + 1
                ));
                ability_idx += 2;
                continue;
            }
            if ability_idx + 1 < def.abilities.len()
                && let Some(keyword) =
                    describe_structural_rampage_pair(ability, &def.abilities[ability_idx + 1])
            {
                output.push(format!("Keyword ability {}: {keyword}", ability_idx + 1));
                ability_idx += 2;
                continue;
            }
            if ability_idx + 1 < def.abilities.len()
                && let Some(permission) = describe_structural_play_from_with_mana_permission_pair(
                    ability,
                    &def.abilities[ability_idx + 1],
                )
            {
                output.push(format!("Static ability {}: {permission}", ability_idx + 1));
                ability_idx += 2;
                continue;
            }
            if ability_idx + 1 < def.abilities.len()
                && let Some(permission) = describe_structural_graveyard_or_exile_cast_pair(
                    ability,
                    &def.abilities[ability_idx + 1],
                )
            {
                output.push(format!("Static ability {}: {permission}", ability_idx + 1));
                ability_idx += 2;
                continue;
            }
            if let Some((keyword, consumed)) =
                describe_structural_counter_keyword_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Keyword ability {}: {keyword}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_attached_land_type_setting_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_type_base_pt_addition_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if let Some((text, consumed)) =
                describe_structural_pt_color_type_addition_bundle(&def.abilities[ability_idx..])
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += consumed;
                continue;
            }
            if ability_idx + 1 < def.abilities.len()
                && let Some(text) = describe_as_enters_source_exile_counter_pair(
                    ability,
                    &def.abilities[ability_idx + 1],
                )
            {
                output.push(format!("Static ability {}: {text}", ability_idx + 1));
                ability_idx += 2;
                continue;
            }
            if ability_idx + 1 < def.abilities.len()
                && let Some(echo) =
                    describe_structural_echo_pair(ability, &def.abilities[ability_idx + 1])
            {
                output.push(format!("Keyword ability {}: {echo}", ability_idx + 1));
                ability_idx += 2;
                continue;
            }
            if let AbilityKind::Activated(first) = &ability.kind
                && first.is_mana_ability()
                && first.effects.is_empty()
                && first.activation_condition.is_none()
                && first.additional_restrictions.is_empty()
                && first.mana_usage_restrictions.is_empty()
                && first.mana_symbols().len() == 1
            {
                let mut symbols = vec![first.mana_symbols()[0]];
                let mut consumed = 1usize;
                while ability_idx + consumed < def.abilities.len() {
                    let next = &def.abilities[ability_idx + consumed];
                    let AbilityKind::Activated(next_mana) = &next.kind else {
                        break;
                    };
                    if !next_mana.is_mana_ability()
                        || !next_mana.effects.is_empty()
                        || next_mana.activation_condition.is_some()
                        || !next_mana.additional_restrictions.is_empty()
                        || !next_mana.mana_usage_restrictions.is_empty()
                        || next_mana.mana_symbols().len() != 1
                        || next_mana.mana_cost != first.mana_cost
                    {
                        break;
                    }
                    symbols.push(next_mana.mana_symbols()[0]);
                    consumed += 1;
                }
                if consumed > 1 {
                    let mut line = format!("Mana ability {}", ability_idx + 1);
                    let add = format!("Add {}", describe_mana_alternatives(&symbols));
                    if !first.mana_cost.costs().is_empty() {
                        let cost = describe_cost_list(first.mana_cost.costs());
                        line.push_str(": ");
                        line.push_str(&cost);
                        line.push_str(": ");
                        line.push_str(&add);
                    } else {
                        line.push_str(": ");
                        line.push_str(&add);
                    }
                    line = normalize_ability_self_reference_surface(&line, subject);
                    output.push(line);
                    ability_idx += consumed;
                    continue;
                }
            }
            let ability_subject = if ability_prefers_card_name_subject(ability) {
                def.card.name.as_str()
            } else {
                subject
            };
            let mut ability_lines =
                describe_ability(ability_idx + 1, ability, ability_subject, rewrite_it_deals);
            if ability_has_level_tiers(ability) {
                ability_lines = interleave_level_range_activations(
                    ability_lines,
                    &def.abilities,
                    ability_subject,
                    rewrite_it_deals,
                );
            }
            if ability_has_begin_on_battlefield_pregame(ability) {
                for line in &mut ability_lines {
                    *line = substitute_pregame_self_reference(line, &def.card.name);
                }
            }
            if has_begin_on_battlefield_pregame {
                for line in &mut ability_lines {
                    *line = substitute_pregame_card_self_reference(line, subject, &def.card.name);
                }
            }
            let oracle_short = oracle_short_self_name(def);
            for line in &mut ability_lines {
                *line = substitute_legendary_source_reference(
                    line,
                    &def.card,
                    subject,
                    oracle_short.as_deref(),
                );
                *line = rewrite_plain_creature_aura_subject(def.aura_attach_filter.as_ref(), line);
            }
            output.extend(ability_lines);
            ability_idx += 1;
        }
    };

    let additional_costs = def.additional_non_mana_costs();
    if !additional_costs.is_empty() {
        let additional_cost_text = describe_additional_costs(&additional_costs);
        out.push(format!(
            "As an additional cost to cast this spell, {}",
            lowercase_first(&additional_cost_text)
        ));
    }
    if !spell_like_card {
        push_abilities(&mut out);
    }
    if render_leading_spell_abilities {
        for (idx, ability) in def.abilities.iter().enumerate() {
            if source_line_repeated_cascade_group_consumed(&def.abilities[idx..]).is_some() {
                out.push(source_line_keyword_group_sentinel(2));
                continue;
            }
            let belongs_to_renderable_static_group =
                def.abilities[..idx]
                    .iter()
                    .enumerate()
                    .any(|(marker_idx, marker)| {
                        source_line_static_group_count(marker).is_some_and(|member_count| {
                            idx <= marker_idx + member_count
                                && describe_source_line_static_group(
                                    &def.abilities[marker_idx + 1..],
                                    member_count,
                                    subject,
                                )
                                .is_some()
                        })
                    });
            if belongs_to_renderable_static_group {
                continue;
            }
            if !ability_precedes_spell_resolution(ability) {
                continue;
            }
            if ability_is_rendered_with_alternative_cast(def, ability) {
                continue;
            }
            let ability_subject = if ability_prefers_card_name_subject(ability) {
                def.card.name.as_str()
            } else {
                subject
            };
            let mut ability_lines =
                describe_ability(idx + 1, ability, ability_subject, rewrite_it_deals);
            let oracle_short = oracle_short_self_name(def);
            for line in &mut ability_lines {
                *line = substitute_legendary_source_reference(
                    line,
                    &def.card,
                    subject,
                    oracle_short.as_deref(),
                );
                *line = rewrite_plain_creature_aura_subject(def.aura_attach_filter.as_ref(), line);
            }
            out.extend(ability_lines);
        }
    }
    if let Some(spell_effects) = &spell_effects_for_render
        && !spell_effects.is_empty()
        && cast_as_flash_cleanup_sacrifice_ability.is_none()
    {
        if is_choose_background_spell_effect(spell_effects) {
            out.push("Keyword ability 0: Choose a Background".to_string());
        } else {
            let rendered = describe_resolution_program_for_card(def, spell_effects);
            let rendered = preserve_cleave_bracket_surface(def, &rendered).unwrap_or(rendered);
            let spell_text = rewrite_additional_sacrifice_reference_surface(def, &rendered);
            if !spell_text.trim().is_empty() {
                if let Some(mut source_lines) =
                    typed_resolution_source_lines(spell_effects, &spell_text)
                        .or_else(|| typed_spree_source_lines(spell_effects, &spell_text))
                {
                    let header = source_lines.remove(0);
                    out.push(format!("Spell effects: {header}"));
                    out.extend(source_lines);
                } else {
                    out.push(format!("Spell effects: {}", spell_text));
                }
            }
        }
    }
    out.extend(deferred_spell_optional_lines);
    if spell_like_card {
        push_abilities(&mut out);
    }
    if def.has_fuse {
        out.push("Fuse".to_string());
    }
    out.extend(alternative_cast_lines);
    let oracle_short = oracle_short_self_name(def);
    merge_adjacent_keyword_surface_lines(out)
        .into_iter()
        .map(|line| {
            substitute_legendary_source_reference(
                &line,
                &def.card,
                subject,
                oracle_short.as_deref(),
            )
        })
        .collect()
}

fn ability_prefers_card_name_subject(ability: &Ability) -> bool {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return false;
    };
    if static_ability.prefers_card_name_subject() {
        return true;
    }
    if static_ability.id() == crate::static_abilities::StaticAbilityId::CanBeCommander {
        return true;
    }
    static_ability
        .enter_as_copy_as_enters()
        .is_some_and(|spec| spec.linked_exile_pair.is_some())
}

fn static_display_with_id(
    ability: &Ability,
    expected_id: crate::static_abilities::StaticAbilityId,
) -> Option<String> {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    (static_ability.id() == expected_id).then(|| {
        static_ability
            .display()
            .trim()
            .trim_end_matches('.')
            .to_string()
    })
}

fn structural_static_effect_filter(
    ability: &Ability,
    expected_id: crate::static_abilities::StaticAbilityId,
) -> Option<&ObjectFilter> {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    if static_ability.id() != expected_id {
        return None;
    }
    if let Some(filter) = static_ability.structural_effect_filter() {
        return Some(filter);
    }
    match &static_ability.compiled_model()?.payload {
        ironsmith_core::StaticAbilityPayload::Anthem(anthem)
            if expected_id == crate::static_abilities::StaticAbilityId::Anthem =>
        {
            anthem.filter.as_ref()
        }
        ironsmith_core::StaticAbilityPayload::RemoveAllAbilities(filter)
            if expected_id
                == crate::static_abilities::StaticAbilityId::RemoveAllAbilitiesForFilter =>
        {
            Some(filter)
        }
        ironsmith_core::StaticAbilityPayload::SetColors { filter, .. }
            if expected_id == crate::static_abilities::StaticAbilityId::SetColors =>
        {
            Some(filter)
        }
        ironsmith_core::StaticAbilityPayload::AddSubtypes { filter, .. }
            if expected_id == crate::static_abilities::StaticAbilityId::AddSubtypes =>
        {
            Some(filter)
        }
        _ => None,
    }
}

fn static_bundle_subjects_match(first: &str, next: &str) -> bool {
    if first.eq_ignore_ascii_case(next) {
        return true;
    }
    let normalize_each = |subject: &str| {
        let lower = subject.trim().to_ascii_lowercase();
        let Some(rest) = lower.strip_prefix("each ") else {
            return lower;
        };
        let Some((noun, tail)) = rest.split_once(' ') else {
            return format!("{rest}s");
        };
        let plural = match noun {
            "creature" => "creatures".to_string(),
            "artifact" => "artifacts".to_string(),
            "enchantment" => "enchantments".to_string(),
            "permanent" => "permanents".to_string(),
            "land" => "lands".to_string(),
            "token" => "tokens".to_string(),
            other => format!("{other}s"),
        };
        format!("{plural} {tail}")
    };
    normalize_each(first) == normalize_each(next)
}

fn render_static_bundle_list(items: &[String]) -> String {
    match items {
        [] => String::new(),
        [only] => only.clone(),
        [left, right] => format!("{left} and {right}"),
        _ => {
            let (last, leading) = items.split_last().expect("nonempty static bundle list");
            format!("{}, and {last}", leading.join(", "))
        }
    }
}

fn split_static_ability_loss_predicate(display: &str) -> Option<(&str, &str, &'static str)> {
    if let Some((subject, tail)) = split_static_predicate(display, " loses ") {
        return Some((subject, tail, "loses"));
    }
    split_static_predicate(display, " lose ").map(|(subject, tail)| (subject, tail, "lose"))
}

/// Recombine a typed list of keyword-loss effects emitted from one authored
/// static source-line chunk. The source marker proves the line boundary; the
/// payload checks prove every member has the same affected set and loss mode.
fn describe_source_line_static_ability_loss_group(abilities: &[Ability]) -> Option<String> {
    let first = abilities.first()?;
    let AbilityKind::Static(first_static) = &first.kind else {
        return None;
    };
    let first_model = first_static.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::RemoveAbilityForFilter {
        filter: first_filter,
        mode: first_mode,
        ..
    } = &first_model.payload
    else {
        return None;
    };
    if *first_mode != ironsmith_core::AbilityLossMode::Lose {
        return None;
    }
    let first_display = first_static.display();
    let first_display = first_display.trim().trim_end_matches('.');
    let (subject, first_tail, verb) = split_static_ability_loss_predicate(first_display)?;
    let mut tails = vec![first_tail.to_string()];

    for ability in &abilities[1..] {
        if ability.functional_zones != first.functional_zones {
            return None;
        }
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let model = static_ability.compiled_model()?;
        let ironsmith_core::StaticAbilityPayload::RemoveAbilityForFilter { filter, mode, .. } =
            &model.payload
        else {
            return None;
        };
        if filter != first_filter || mode != first_mode {
            return None;
        }
        let display = static_ability.display();
        let display = display.trim().trim_end_matches('.');
        let (next_subject, tail, _) = split_static_ability_loss_predicate(display)?;
        if !static_bundle_subjects_match(subject, next_subject) {
            return None;
        }
        tails.push(tail.to_string());
    }

    Some(format!(
        "{} {verb} {}",
        capitalize_first(subject),
        render_static_bundle_list(&tails)
    ))
}

/// Recombine a continuous P/T modifier and a keyword-loss effect emitted from
/// one authored static line, such as "Equipped creature gets +10/+10 and loses
/// flying." The source-line marker proves adjacency; matching typed filters
/// prove that both layer effects apply to the same set of objects.
fn describe_source_line_anthem_keyword_loss_group(abilities: &[Ability]) -> Option<String> {
    let [first, second] = abilities else {
        return None;
    };
    if first.functional_zones != second.functional_zones {
        return None;
    }

    let mut anthem = None;
    let mut removal = None;
    for ability in abilities {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let (model, wrapper_condition) = unwrapped_static_model(static_ability)?;
        match &model.payload {
            ironsmith_core::StaticAbilityPayload::Anthem(spec)
                if anthem.is_none() && wrapper_condition.is_none() =>
            {
                anthem = Some((
                    static_ability,
                    spec.filter.as_ref()?,
                    spec.condition.as_ref(),
                ));
            }
            ironsmith_core::StaticAbilityPayload::RemoveAbilityForFilter {
                filter,
                mode: ironsmith_core::AbilityLossMode::Lose,
                ..
            } if removal.is_none() => {
                removal = Some((static_ability, model, filter, wrapper_condition));
            }
            _ => return None,
        }
    }
    let (anthem, anthem_filter, anthem_condition) = anthem?;
    let (removal, removal_model, removal_filter, removal_condition) = removal?;
    if anthem_filter != removal_filter || anthem_condition != removal_condition {
        return None;
    }

    let anthem_display = anthem.display();
    let anthem_display = anthem_display.trim().trim_end_matches('.');
    let condition_text = anthem_condition.map(super::normalize_common::describe_condition);
    let anthem_display = condition_text
        .as_ref()
        .and_then(|condition| anthem_display.strip_suffix(&format!(" as long as {condition}")))
        .unwrap_or(anthem_display);
    let (subject, anthem_tail, anthem_verb) =
        split_static_predicate_with_verb(anthem_display, &[" gets ", " get "])?;
    let removal_display = if removal_condition.is_some() {
        crate::static_abilities::StaticAbility::from_model(removal_model.clone()).display()
    } else {
        removal.display()
    };
    let removal_display = removal_display.trim().trim_end_matches('.');
    let (removal_subject, removal_tail, removal_verb) =
        split_static_ability_loss_predicate(removal_display)?;
    if !static_bundle_subjects_match(subject, removal_subject) {
        return None;
    }

    if let Some(condition) = condition_text {
        let repeated_subject = condition
            .to_ascii_lowercase()
            .starts_with(&format!("{} ", subject.to_ascii_lowercase()));
        let conditioned_subject = if repeated_subject {
            if anthem_verb == "gets" { "it" } else { "they" }
        } else {
            subject
        };
        Some(format!(
            "As long as {condition}, {conditioned_subject} {anthem_verb} {anthem_tail} and {removal_verb} {removal_tail}"
        ))
    } else {
        Some(format!(
            "{} {anthem_verb} {anthem_tail} and {removal_verb} {removal_tail}",
            capitalize_first(subject)
        ))
    }
}

/// Recombine one granted static keyword and one removed static keyword from a
/// single authored clause. The source-line marker supplies provenance; the
/// typed payload checks prove that both continuous effects are unconditional
/// and affect the exact same objects.
fn describe_source_line_grant_keyword_loss_group(abilities: &[Ability]) -> Option<String> {
    let [first, second] = abilities else {
        return None;
    };
    if first.functional_zones != second.functional_zones {
        return None;
    }

    let mut grant = None;
    let mut removal = None;
    for ability in abilities {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let model = static_ability.compiled_model()?;
        match &model.payload {
            ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(spec)
                if grant.is_none()
                    && spec.condition.is_none()
                    && spec.additional_abilities.is_empty()
                    && spec.set_quantifier_surface.is_none()
                    && matches!(&spec.ability.kind, ironsmith_core::AbilityKind::Static(_)) =>
            {
                grant = Some((static_ability, &spec.filter));
            }
            ironsmith_core::StaticAbilityPayload::RemoveAbilityForFilter {
                filter,
                mode: ironsmith_core::AbilityLossMode::Lose,
                ..
            } if removal.is_none() => {
                removal = Some((static_ability, filter));
            }
            _ => return None,
        }
    }
    let (grant, grant_filter) = grant?;
    let (removal, removal_filter) = removal?;
    if grant_filter != removal_filter {
        return None;
    }

    let grant_display = grant.display();
    let grant_display = grant_display.trim().trim_end_matches('.');
    let (subject, grant_tail, grant_verb) =
        split_static_predicate_with_verb(grant_display, &[" has ", " have "])?;
    let removal_display = removal.display();
    let removal_display = removal_display.trim().trim_end_matches('.');
    let (removal_subject, removal_tail, removal_verb) =
        split_static_ability_loss_predicate(removal_display)?;
    if !static_bundle_subjects_match(subject, removal_subject) {
        return None;
    }

    Some(format!(
        "{} {grant_verb} {grant_tail} and {removal_verb} {removal_tail}",
        capitalize_first(subject)
    ))
}

#[cfg(test)]
mod source_line_grant_keyword_loss_tests {
    use super::*;

    const LINE: &str = "Enchanted creature has defender and loses flying.";

    fn parsed_definition() -> crate::CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Attached Keyword Grant-Loss Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(format!("Enchant creature\n{LINE}"))
        .expect("attached keyword grant-and-loss text should parse")
    }

    fn parsed_members() -> Vec<Ability> {
        parsed_definition()
            .abilities
            .into_iter()
            .filter(|ability| {
                !matches!(
                    &ability.kind,
                    AbilityKind::Static(static_ability)
                        if static_ability.id()
                            == crate::static_abilities::StaticAbilityId::SourceLineStaticGroup
                )
            })
            .collect()
    }

    #[test]
    fn exact_equal_filter_group_rejoins_but_zone_near_miss_does_not() {
        let members = parsed_members();
        assert_eq!(members.len(), 2, "{members:#?}");
        assert_eq!(
            describe_source_line_grant_keyword_loss_group(&members).as_deref(),
            Some(LINE.trim_end_matches('.'))
        );

        let mut different_zone = members;
        different_zone[1].functional_zones = vec![Zone::Graveyard];
        assert!(describe_source_line_grant_keyword_loss_group(&different_zone).is_none());
    }

    #[test]
    fn full_source_line_dispatch_restores_the_authored_conjunction() {
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&parsed_definition()),
            ["Enchant creature", LINE]
        );
    }
}

/// Recombine the paired player restrictions represented by Hand to Hand's
/// static model family. Matching typed player filters and conditions establish
/// the shared subject; the two prohibited actions use Oracle's `or` surface.
fn describe_source_line_cast_activation_restriction_group(abilities: &[Ability]) -> Option<String> {
    let [first, second] = abilities else {
        return None;
    };
    if first.functional_zones != second.functional_zones {
        return None;
    }
    let AbilityKind::Static(first_static) = &first.kind else {
        return None;
    };
    let AbilityKind::Static(second_static) = &second.kind else {
        return None;
    };
    let (first_restriction, first_display, first_condition) =
        first_static.rule_restriction_parts()?;
    let (second_restriction, second_display, second_condition) =
        second_static.rule_restriction_parts()?;
    if first_condition != second_condition || first_condition.is_none() {
        return None;
    }

    let matching_players = match (first_restriction, second_restriction) {
        (
            crate::effect::Restriction::CastSpellsMatching(cast_player, _),
            crate::effect::Restriction::ActivateNonManaAbilities(activation_player),
        )
        | (
            crate::effect::Restriction::ActivateNonManaAbilities(activation_player),
            crate::effect::Restriction::CastSpellsMatching(cast_player, _),
        ) => cast_player == activation_player,
        _ => false,
    };
    if !matching_players {
        return None;
    }

    let first_display = first_display.trim().trim_end_matches('.');
    let second_display = second_display.trim().trim_end_matches('.');
    let (subject, first_action) = split_static_predicate(first_display, " can't ")?;
    let (second_subject, second_action) = split_static_predicate(second_display, " can't ")?;
    if !static_bundle_subjects_match(subject, second_subject) {
        return None;
    }

    let first_full = first_static.display();
    let second_full = second_static.display();
    let first_full = first_full.trim().trim_end_matches('.');
    let second_full = second_full.trim().trim_end_matches('.');
    let prefix = first_full.strip_suffix(first_display)?;
    if second_full.strip_suffix(second_display)? != prefix {
        return None;
    }
    let rendered_subject = if prefix.is_empty() {
        capitalize_first(subject)
    } else {
        subject.to_string()
    };

    Some(format!(
        "{prefix}{rendered_subject} can't {first_action} or {second_action}"
    ))
}

/// Recombine a base-P/T setter, one granted static keyword, and ability loss
/// when all three typed models affect the exact same object and came from one
/// authored source-line chunk.
fn describe_source_line_base_pt_grant_loss_group(abilities: &[Ability]) -> Option<String> {
    let [first, second, third] = abilities else {
        return None;
    };
    if first.functional_zones != second.functional_zones
        || first.functional_zones != third.functional_zones
    {
        return None;
    }

    let mut base = None;
    let mut removal = None;
    let mut grant = None;
    for ability in abilities {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let model = static_ability.compiled_model()?;
        match &model.payload {
            ironsmith_core::StaticAbilityPayload::SetBasePowerToughness { filter, .. }
                if base.is_none() =>
            {
                base = Some((static_ability, filter));
            }
            ironsmith_core::StaticAbilityPayload::RemoveAllAbilities(filter)
                if removal.is_none() =>
            {
                removal = Some((static_ability, filter));
            }
            ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(spec)
                if grant.is_none()
                    && spec.condition.is_none()
                    && spec.additional_abilities.is_empty()
                    && spec.set_quantifier_surface.is_none()
                    && matches!(&spec.ability.kind, ironsmith_core::AbilityKind::Static(_)) =>
            {
                grant = Some((static_ability, &spec.filter));
            }
            _ => return None,
        }
    }
    let (base, base_filter) = base?;
    let (removal, removal_filter) = removal?;
    let (grant, grant_filter) = grant?;
    if base_filter != removal_filter || base_filter != grant_filter {
        return None;
    }

    let base_display = base.display();
    let base_display = base_display.trim().trim_end_matches('.');
    let (subject, base_tail, base_verb) =
        split_static_predicate_with_verb(base_display, &[" has ", " have "])?;
    let grant_display = grant.display();
    let grant_display = grant_display.trim().trim_end_matches('.');
    let (grant_subject, grant_tail, grant_verb) =
        split_static_predicate_with_verb(grant_display, &[" has ", " have "])?;
    let removal_display = removal.display();
    let removal_display = removal_display.trim().trim_end_matches('.');
    let (removal_subject, removal_tail, removal_verb) =
        split_static_ability_loss_predicate(removal_display)?;
    if removal_tail != "all abilities"
        || !static_bundle_subjects_match(subject, grant_subject)
        || !static_bundle_subjects_match(subject, removal_subject)
    {
        return None;
    }

    Some(format!(
        "{} {base_verb} {base_tail}, {grant_verb} {grant_tail}, and {removal_verb} all other abilities",
        capitalize_first(subject)
    ))
}

/// Recombine a granted ability and an all-abilities removal emitted from one
/// authored static source line. Matching typed filters prove the two layer-6
/// pieces share a subject; the source marker proves that `other` refers to the
/// granted ability from this same clause.
fn describe_source_line_grant_all_other_loss_group(abilities: &[Ability]) -> Option<String> {
    let [first, second] = abilities else {
        return None;
    };
    if first.functional_zones != second.functional_zones {
        return None;
    }

    let mut removal = None;
    let mut grant = None;
    let removal_precedes_grant = matches!(
        &first.kind,
        AbilityKind::Static(static_ability)
            if matches!(
                &static_ability.compiled_model()?.payload,
                ironsmith_core::StaticAbilityPayload::RemoveAllAbilities(_)
            )
    );
    for ability in [first, second] {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let model = static_ability.compiled_model()?;
        match &model.payload {
            ironsmith_core::StaticAbilityPayload::RemoveAllAbilities(filter)
                if removal.is_none() =>
            {
                removal = Some((static_ability, filter));
            }
            ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(spec)
                if grant.is_none()
                    && spec.condition.is_none()
                    && spec.additional_abilities.is_empty() =>
            {
                grant = Some((static_ability, &spec.filter, spec.display.as_str()));
            }
            _ => return None,
        }
    }
    let (removal, removal_filter) = removal?;
    let (grant, grant_filter, authored_grant_tail) = grant?;
    if removal_filter != grant_filter {
        return None;
    }

    let grant_display = grant.display();
    let grant_display = grant_display.trim().trim_end_matches('.');
    let (subject, grant_tail, grant_verb) =
        split_static_predicate_with_verb(grant_display, &[" has ", " have "])?;
    let removal_display = removal.display();
    let removal_display = removal_display.trim().trim_end_matches('.');
    let (removal_subject, removal_tail, removal_verb) =
        split_static_ability_loss_predicate(removal_display)?;
    if removal_tail != "all abilities" || !static_bundle_subjects_match(subject, removal_subject) {
        return None;
    }
    let grant_tail = if removal_precedes_grant {
        let body = authored_grant_tail
            .trim()
            .trim_matches('"')
            .trim_end_matches('.');
        format!("\"{}.\"", capitalize_first(body))
    } else {
        grant_tail
            .strip_prefix('"')
            .and_then(|body| body.strip_suffix('"'))
            .map(|body| format!("\"{}\"", body.trim_end_matches('.')))
            .unwrap_or_else(|| grant_tail.to_string())
    };

    if removal_precedes_grant {
        Some(format!(
            "{} {removal_verb} all abilities and {grant_verb} {grant_tail}",
            capitalize_first(subject)
        ))
    } else {
        Some(format!(
            "{} {grant_verb} {grant_tail} and {removal_verb} all other abilities",
            capitalize_first(subject)
        ))
    }
}

/// A two-member source-line marker records one authored grant/loss clause, but
/// the executable abilities are stored in layer order, with ability removal
/// before ability granting. Restore the clause's canonical grant-first surface
/// before the order-sensitive helper renders `all other abilities`.
fn describe_source_line_grant_then_all_other_loss_group(abilities: &[Ability]) -> Option<String> {
    let [first, second] = abilities else {
        return None;
    };
    let removal_is_first = matches!(
        &first.kind,
        AbilityKind::Static(static_ability)
            if matches!(
                &static_ability.compiled_model()?.payload,
                ironsmith_core::StaticAbilityPayload::RemoveAllAbilities(_)
            )
    );
    if removal_is_first {
        describe_source_line_grant_all_other_loss_group(&[second.clone(), first.clone()])
    } else {
        describe_source_line_grant_all_other_loss_group(abilities)
    }
}

enum TypeAdditionGrantSurface {
    Quoted(String),
    Equip(String),
}

fn render_type_addition_grant_surfaces(items: &[TypeAdditionGrantSurface]) -> Option<String> {
    if items.len() < 2
        || !matches!(items.first(), Some(TypeAdditionGrantSurface::Quoted(_)))
        || !items
            .iter()
            .any(|item| matches!(item, TypeAdditionGrantSurface::Equip(_)))
    {
        return None;
    }

    let last = items.len() - 1;
    let rendered = items
        .iter()
        .enumerate()
        .map(|(idx, item)| match item {
            TypeAdditionGrantSurface::Quoted(text) => {
                let punctuation = if idx < last { "," } else { "." };
                format!("\"{}{punctuation}\"", text.trim_end_matches(['.', ',']))
            }
            TypeAdditionGrantSurface::Equip(text) if idx < last && items.len() > 2 => {
                format!("{},", lowercase_first(text.trim_end_matches('.')))
            }
            TypeAdditionGrantSurface::Equip(text) => lowercase_first(text.trim_end_matches('.')),
        })
        .collect::<Vec<_>>();
    let (final_item, leading) = rendered.split_last()?;
    Some(format!("{} and {final_item}", leading.join(" ")))
}

/// Recombine a type-addition clause and its mixed quoted/equip grants when
/// lowering emitted one continuous ability per executable layer component.
fn describe_source_line_type_addition_grant_group(abilities: &[Ability]) -> Option<String> {
    let [first, grants @ ..] = abilities else {
        return None;
    };
    if first.functional_zones.as_slice() != [Zone::Battlefield] || grants.is_empty() {
        return None;
    }
    let AbilityKind::Static(first_static) = &first.kind else {
        return None;
    };
    let first_model = first_static.compiled_model()?;
    let affected_filter = match &first_model.payload {
        ironsmith_core::StaticAbilityPayload::AddCardTypes { filter, card_types }
            if !card_types.is_empty() =>
        {
            filter
        }
        ironsmith_core::StaticAbilityPayload::AddSubtypes { filter, subtypes }
            if !subtypes.is_empty() =>
        {
            filter
        }
        _ => return None,
    };
    let type_display = first_static.display();
    let type_display = type_display.trim().trim_end_matches('.');
    let (_, addition, type_verb) =
        split_static_predicate_with_verb(type_display, &[" is ", " are "])?;
    if !addition.contains("in addition to")
        || !addition.ends_with("other types")
        || type_display.starts_with("During ")
    {
        return None;
    }

    let mut surfaces = Vec::with_capacity(grants.len());
    for ability in grants {
        if ability.functional_zones != first.functional_zones {
            return None;
        }
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let model = static_ability.compiled_model()?;
        match &model.payload {
            ironsmith_core::StaticAbilityPayload::GrantAbility(grant)
                if &grant.filter == affected_filter
                    && grant.condition.is_none()
                    && grant.set_quantifier_surface.is_none()
                    && grant.ability.functional_zones.as_slice() == [Zone::Battlefield] =>
            {
                let ironsmith_core::AbilityKind::Static(granted_model) = &grant.ability.kind else {
                    return None;
                };
                let granted =
                    crate::static_abilities::StaticAbility::from_model(granted_model.clone());
                if granted.is_keyword() {
                    return None;
                }
                let text = describe_static_ability_with_subject(
                    &granted,
                    granted_ability_self_subject_for_filter(affected_filter),
                );
                let text = capitalize_first(text.trim().trim_end_matches('.'));
                if text.is_empty() {
                    return None;
                }
                surfaces.push(TypeAdditionGrantSurface::Quoted(text));
            }
            ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant)
                if &grant.filter == affected_filter
                    && grant.condition.is_none()
                    && grant.additional_abilities.is_empty()
                    && grant.set_quantifier_surface.is_none() =>
            {
                let granted =
                    crate::static_abilities::StaticAbilityModelInterpreter::ability_from_model(
                        &grant.ability,
                    );
                let AbilityKind::Activated(activated) = &granted.kind else {
                    return None;
                };
                let equip = super::render_effects::describe_structural_equip_keyword(activated)?;
                surfaces.push(TypeAdditionGrantSurface::Equip(equip));
            }
            _ => return None,
        }
    }

    let verb = match type_verb {
        "are" => "have",
        "is" => "has",
        _ => return None,
    };
    Some(format!(
        "{type_display} and {verb} {}",
        render_type_addition_grant_surfaces(&surfaces)?
    ))
}

/// Rejoin a leading turn condition shared by a type addition and its quoted
/// activated-ability grant. Both static abilities remain independently
/// executable; the source-line marker and these guards prove the authored
/// common condition and subject.
fn describe_source_line_conditional_type_addition_activated_grant_group(
    abilities: &[Ability],
) -> Option<String> {
    let [type_ability, grant_ability] = abilities else {
        return None;
    };
    if type_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || grant_ability.functional_zones != type_ability.functional_zones
    {
        return None;
    }
    let AbilityKind::Static(type_static) = &type_ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: type_inner,
        condition,
    } = &type_static.compiled_model()?.payload
    else {
        return None;
    };
    if condition != &Condition::ActivationTiming(crate::ability::ActivationTiming::DuringYourTurn) {
        return None;
    }
    let ironsmith_core::StaticAbilityPayload::AddSubtypes { filter, subtypes } =
        &type_inner.payload
    else {
        return None;
    };
    let [subtype] = subtypes.as_slice() else {
        return None;
    };
    let expected_filter = ObjectFilter::artifact()
        .controlled_by(PlayerFilter::You)
        .in_zone(Zone::Battlefield);
    if filter != &expected_filter {
        return None;
    }

    let AbilityKind::Static(grant_static) = &grant_ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) =
        &grant_static.compiled_model()?.payload
    else {
        return None;
    };
    if &grant.filter != filter
        || grant.condition.as_ref() != Some(condition)
        || !grant.additional_abilities.is_empty()
        || grant.set_quantifier_surface.is_some()
    {
        return None;
    }
    let granted =
        crate::static_abilities::StaticAbilityModelInterpreter::ability_from_model(&grant.ability);
    if !matches!(&granted.kind, AbilityKind::Activated(_)) {
        return None;
    }
    let mut granted_text =
        super::render_effects::describe_inline_ability_with_self_subject(&granted, "this artifact")
            .trim()
            .trim_end_matches('.')
            .to_string();
    if granted_text.is_empty() {
        return None;
    }
    granted_text.push('.');
    let subtype = super::render_effects::pluralize_word(&subtype.display_name());
    Some(format!(
        "During your turn, artifacts you control are {subtype} in addition to their other types and have \"{granted_text}\""
    ))
}

fn describe_source_line_static_group(
    abilities: &[Ability],
    member_count: usize,
    subject: &str,
) -> Option<String> {
    if member_count < 2 {
        return None;
    }
    let members = abilities.get(..member_count)?;
    describe_structural_attached_subtype_base_pt_keyword_loss_bundle(members)
        .and_then(|(text, consumed)| (consumed == member_count).then_some(text))
        .or_else(|| {
            describe_structural_labeled_animation_grant_bundle(members, subject)
                .and_then(|(text, consumed)| (consumed == member_count).then_some(text))
        })
        .or_else(|| describe_source_line_blitz_from_card_mana_cost_group(members))
        .or_else(|| describe_source_line_chosen_object_anthem_keyword_grant_group(members))
        .or_else(|| describe_carried_attached_additional_grant(members))
        .or_else(|| describe_source_line_attached_anthem_reach_shadow_permission_group(members))
        .or_else(|| describe_source_line_first_spell_cost_reduction_and_flash_group(members))
        .or_else(|| describe_source_line_attached_land_reset_group(members))
        .or_else(|| describe_source_line_keyword_attack_dynamic_anthem_group(members))
        .or_else(|| describe_source_line_attached_restriction_loss_grant_group(members))
        .or_else(|| {
            describe_structural_attached_mixed_grant_bundle(members)
                .and_then(|(text, consumed)| (consumed == member_count).then_some(text))
        })
        .or_else(|| describe_source_line_static_ability_loss_group(members))
        .or_else(|| describe_source_line_anthem_keyword_loss_group(members))
        .or_else(|| describe_source_line_grant_keyword_loss_group(members))
        .or_else(|| {
            describe_structural_attached_anthem_condition_chain_bundle(members)
                .and_then(|(text, consumed)| (consumed == member_count).then_some(text))
        })
        .or_else(|| {
            describe_structural_attached_anthem_otherwise_base_block_bundle(members)
                .and_then(|(text, consumed)| (consumed == member_count).then_some(text))
        })
        .or_else(|| describe_source_line_attached_keyword_otherwise_prevention_group(members))
        .or_else(|| describe_source_line_conditional_spell_protection_group(members))
        .or_else(|| describe_source_line_cast_activation_restriction_group(members))
        .or_else(|| describe_source_line_spell_cost_reduction_counter_protection_group(members))
        .or_else(|| describe_source_line_base_pt_grant_loss_group(members))
        .or_else(|| describe_source_line_grant_then_all_other_loss_group(members))
        .or_else(|| describe_source_line_conditional_type_addition_activated_grant_group(members))
        .or_else(|| describe_source_line_type_addition_grant_group(members))
        .or_else(|| describe_source_line_conditioned_prevention_and_keyword(members, subject))
        .or_else(|| describe_source_line_conditioned_source_mixed_grants_group(members, subject))
        .or_else(|| describe_source_line_conditioned_source_anthem_trait_group(members, subject))
        .or_else(|| {
            describe_source_line_conditional_self_and_other_anthem_keyword_grant_group(members)
        })
        .or_else(|| describe_source_line_conditional_self_anthem_keyword_grant_group(members))
        .or_else(|| describe_source_line_graveyard_permission_dynamic_surcharge_group(members))
        .or_else(|| describe_source_line_any_player_ignore_search_group(members))
        .or_else(|| {
            describe_source_line_off_battlefield_creature_characteristic_group(members, subject)
        })
}

/// Rejoin two differently shaped abilities granted to the source under one
/// typed condition.  This covers an authored keyword plus quoted activated
/// ability without weakening either executable grant or relying on its display
/// string for identity.
fn describe_source_line_conditioned_prevention_and_keyword(
    abilities: &[Ability],
    subject: &str,
) -> Option<String> {
    let [prevention, grant] = abilities else {
        return None;
    };
    if prevention.functional_zones.as_slice() != [Zone::Battlefield] {
        return None;
    }
    let AbilityKind::Static(prevention) = &prevention.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::Conditional { ability, condition } =
        &prevention.compiled_model()?.payload
    else {
        return None;
    };
    if ability.id
        != Some(
            crate::static_abilities::StaticAbilityId::PreventAllCombatDamageDealtByThisPermanent,
        )
    {
        return None;
    }
    let (_, keyword, grant_condition) = modeled_source_keyword_grant(grant)?;
    if condition != grant_condition {
        return None;
    }
    let condition_text = match condition {
        Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::CountersOnSource(counter),
            comparison: crate::effect::Comparison::GreaterThanOrEqual(1),
            ..
        } => format!(
            "{} has {} on it",
            lowercase_first(subject),
            with_indefinite_article(&format!("{} counter", counter.description()))
        ),
        _ => describe_condition(condition),
    };
    Some(format!(
        "As long as {condition_text}, prevent all combat damage it would deal and it has {keyword}"
    ))
}

fn describe_source_line_conditioned_source_mixed_grants_group(
    abilities: &[Ability],
    subject: &str,
) -> Option<String> {
    let [keyword_grant, activated_grant] = abilities else {
        return None;
    };
    let modeled_grant = |ability: &Ability| {
        if ability.functional_zones.as_slice() != [Zone::Battlefield] {
            return None;
        }
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) =
            &static_ability.compiled_model()?.payload
        else {
            return None;
        };
        if grant.filter != ObjectFilter::source()
            || !grant.additional_abilities.is_empty()
            || grant.set_quantifier_surface.is_some()
            || grant.ability.functional_zones.as_slice() != [Zone::Battlefield]
        {
            return None;
        }
        Some((
            grant.condition.as_ref()?.clone(),
            crate::static_abilities::StaticAbilityModelInterpreter::ability_from_model(
                &grant.ability,
            ),
        ))
    };

    let (condition, keyword_ability) = modeled_grant(keyword_grant)?;
    let (activated_condition, activated_ability) = modeled_grant(activated_grant)?;
    if activated_condition != condition || condition != Condition::SourceIsRenowned {
        return None;
    }
    let AbilityKind::Static(keyword_static) = &keyword_ability.kind else {
        return None;
    };
    if !keyword_static.is_keyword() {
        return None;
    }
    let keyword = keyword_static
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    if keyword.is_empty() || keyword.contains(',') || keyword.contains('"') {
        return None;
    }
    if !matches!(&activated_ability.kind, AbilityKind::Activated(_)) {
        return None;
    }
    let activated = super::render_effects::describe_inline_ability_with_self_subject(
        &activated_ability,
        subject,
    );
    let activated = activated.trim().trim_end_matches('.');
    if activated.is_empty() || activated.contains('\n') {
        return None;
    }
    Some(format!(
        "As long as {}, it has {keyword} and \"{activated}.\"",
        describe_condition(&condition)
    ))
}

#[cfg(test)]
mod conditioned_source_mixed_grants_tests {
    use super::*;

    const LINE: &str = "As long as this creature is renowned, it has vigilance and \"{T}: Add one mana of any color.\"";

    fn definition() -> crate::CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Honored Hierarch",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(format!("Renown 1\n{LINE}"))
        .expect("renown and its conditioned grants should parse")
    }

    #[test]
    fn renown_keyword_and_mixed_grants_keep_their_authored_surfaces() {
        let definition = definition();
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            ["Renown 1".to_string(), LINE.to_string()]
        );

        let mut members = definition.abilities[2..].to_vec();
        assert_eq!(
            describe_source_line_conditioned_source_mixed_grants_group(&members, "this creature")
                .as_deref(),
            Some(LINE)
        );
        members[1].functional_zones = vec![Zone::Graveyard];
        assert!(
            describe_source_line_conditioned_source_mixed_grants_group(&members, "this creature")
                .is_none()
        );
    }
}

fn describe_source_line_blitz_from_card_mana_cost_group(abilities: &[Ability]) -> Option<String> {
    if abilities.len() != 5 {
        return None;
    }
    let mut specs = Vec::new();
    for ability in abilities {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let spec = static_ability.grant_spec()?;
        if !matches!(
            spec.grantable,
            crate::grant::Grantable::DerivedAlternativeCast(
                crate::grant::DerivedAlternativeCast::BlitzFromCardManaCost
            )
        ) || spec.beneficiary != PlayerFilter::You
        {
            return None;
        }
        specs.push(spec);
    }
    let normalized_filter = |spec: &crate::grant::GrantSpec| {
        let mut filter = spec.filter.clone();
        filter.zone = None;
        filter
    };
    let shared_filter = normalized_filter(&specs[0]);
    if specs
        .iter()
        .any(|spec| normalized_filter(spec) != shared_filter)
    {
        return None;
    }
    let mut zones = specs
        .iter()
        .map(|spec| spec.filter.zone.unwrap_or(spec.zone))
        .collect::<Vec<_>>();
    zones.sort_by_key(|zone| *zone as u8);
    let mut expected = vec![
        Zone::Hand,
        Zone::Graveyard,
        Zone::Exile,
        Zone::Library,
        Zone::Command,
    ];
    expected.sort_by_key(|zone| *zone as u8);
    if zones != expected {
        return None;
    }

    let mut line = specs[0].display();
    for zone in [
        " in hand",
        " in exile",
        " in a graveyard",
        " in your library",
        " in the command zone",
    ] {
        line = line.replace(zone, "");
    }
    line = line.replace(
        "Each creature spell with",
        "Each creature spell you cast with",
    );
    Some(format!(
        "{line}. {STANDARD_REMINDER_OPEN_SENTINEL}You may choose to cast that spell for its blitz cost. If you do, it gains haste and \"When this creature dies, draw a card.\" Sacrifice it at the beginning of the next end step.{STANDARD_REMINDER_CLOSE_SENTINEL}"
    ))
}

fn describe_source_line_attached_anthem_reach_shadow_permission_group(
    abilities: &[Ability],
) -> Option<String> {
    let [anthem_ability, reach_ability, shadow_ability] = abilities else {
        return None;
    };
    if abilities
        .iter()
        .any(|ability| ability.functional_zones.as_slice() != [Zone::Battlefield])
    {
        return None;
    }
    let AbilityKind::Static(anthem_static) = &anthem_ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::Anthem(anthem) =
        &anthem_static.compiled_model()?.payload
    else {
        return None;
    };
    let expected_filter = ObjectFilter::creature().match_tagged(
        "enchanted",
        crate::target::TaggedOpbjectRelation::IsTaggedObject,
    );
    let (ironsmith_core::AnthemValue::Fixed(1), ironsmith_core::AnthemValue::Fixed(1)) =
        (&anthem.power, &anthem.toughness)
    else {
        return None;
    };
    if anthem.filter.as_ref() != Some(&expected_filter)
        || anthem.condition.is_some()
        || anthem.set_quantifier_surface.is_some()
        || anthem.count_uses_where_x
        || anthem.additional_surface
        || anthem.replacement_surface.is_some()
    {
        return None;
    }
    let (reach_filter, reach_condition, reach) =
        modeled_unquantified_object_static_grant(reach_ability)?;
    let (shadow_filter, shadow_condition, shadow) =
        modeled_unquantified_object_static_grant(shadow_ability)?;
    if reach_filter != &expected_filter
        || shadow_filter != &expected_filter
        || reach_condition.is_some()
        || shadow_condition.is_some()
        || reach.id() != crate::static_abilities::StaticAbilityId::Reach
        || shadow.id() != crate::static_abilities::StaticAbilityId::CanBlockAsThoughNoShadow
    {
        return None;
    }
    Some("Enchanted creature gets +1/+1, has reach, and can block creatures with shadow as though they didn't have shadow".to_string())
}

fn describe_source_line_first_spell_cost_reduction_and_flash_group(
    abilities: &[Ability],
) -> Option<String> {
    let [reduction_ability, flash_ability] = abilities else {
        return None;
    };
    if reduction_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || flash_ability.functional_zones != reduction_ability.functional_zones
    {
        return None;
    }
    let AbilityKind::Static(reduction_static) = &reduction_ability.kind else {
        return None;
    };
    let reduction = reduction_static.cost_reduction()?;
    if !reduction.filter.first_spell_cast_each_turn
        || reduction.filter.cast_by != Some(PlayerFilter::You)
        || reduction.condition.is_some()
        || reduction.per_target
        || reduction.characteristic_intersection.is_some()
    {
        return None;
    }
    let AbilityKind::Static(flash_static) = &flash_ability.kind else {
        return None;
    };
    let flash = flash_static.grant_spec()?;
    if flash != crate::grant::GrantSpec::flash_to_spells_matching(reduction.filter.clone()) {
        return None;
    }
    Some(format!(
        "{} and can be cast as though it had flash",
        reduction_static.display().trim_end_matches('.')
    ))
}

fn describe_source_line_chosen_object_anthem_keyword_grant_group(
    abilities: &[Ability],
) -> Option<String> {
    let [anthem_ability, grant_ability] = abilities else {
        return None;
    };
    if anthem_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || grant_ability.functional_zones != anthem_ability.functional_zones
    {
        return None;
    }
    let AbilityKind::Static(anthem_static) = &anthem_ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::Anthem(anthem) =
        &anthem_static.compiled_model()?.payload
    else {
        return None;
    };
    let Some(anthem_filter) = anthem.filter.as_ref() else {
        return None;
    };
    let (grant_filter, grant_condition, granted) = modeled_object_static_grant(grant_ability)?;
    let [constraint] = anthem_filter.tagged_constraints.as_slice() else {
        return None;
    };
    let mut semantic_filter = anthem_filter.clone();
    semantic_filter.tagged_constraints.clear();
    semantic_filter.union_surface = Default::default();
    let (ironsmith_core::AnthemValue::Fixed(power), ironsmith_core::AnthemValue::Fixed(toughness)) =
        (&anthem.power, &anthem.toughness)
    else {
        return None;
    };
    if grant_filter != anthem_filter
        || grant_condition.is_some()
        || constraint.tag.as_str() != ironsmith_core::CHOSEN_OBJECTS_TAG
        || constraint.relation != crate::filter::TaggedOpbjectRelation::IsTaggedObject
        || semantic_filter != ObjectFilter::creature().in_zone(Zone::Battlefield)
        || anthem.condition.is_some()
        || anthem.set_quantifier_surface.is_some()
        || anthem.count_uses_where_x
        || anthem.additional_surface
        || anthem.replacement_surface.is_some()
        || !granted.is_keyword()
    {
        return None;
    }
    let keyword = granted
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    if keyword.is_empty() || keyword.contains(',') {
        return None;
    }
    Some(format!(
        "The chosen creature gets {} and has {keyword}",
        describe_fixed_anthem_modifier(*power, *toughness)
    ))
}

fn describe_source_line_spell_cost_reduction_counter_protection_group(
    abilities: &[Ability],
) -> Option<String> {
    let [reduction_ability, protection_ability] = abilities else {
        return None;
    };
    if reduction_ability.functional_zones != protection_ability.functional_zones {
        return None;
    }
    let AbilityKind::Static(reduction_static) = &reduction_ability.kind else {
        return None;
    };
    let AbilityKind::Static(protection_static) = &protection_ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::CostReduction(reduction) =
        &reduction_static.compiled_model()?.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::RuleRestriction {
        restriction: crate::effect::Restriction::BeCountered(protected),
        additional_restrictions,
        ..
    } = &protection_static.compiled_model()?.payload
    else {
        return None;
    };
    if !additional_restrictions.is_empty() || protected != &reduction.filter {
        return None;
    }
    let rendered =
        super::render_effects::describe_ability(0, reduction_ability, "this card", false);
    let [rendered] = rendered.as_slice() else {
        return None;
    };
    let reduction_text = rendered.strip_prefix("Static ability 0: ")?;
    Some(format!("{reduction_text} and can't be countered"))
}

fn permanent_cards_in_your_graveyard_threshold(condition: &Condition) -> Option<i32> {
    let Condition::CountComparison {
        count: ironsmith_core::AnthemCountExpression::MatchingFilter(filter),
        comparison: Comparison::GreaterThanOrEqual(threshold),
        ..
    } = condition
    else {
        return None;
    };
    if *threshold < 1
        || filter.zone != Some(Zone::Graveyard)
        || filter.owner != Some(PlayerFilter::You)
        || filter.card_types.as_slice()
            != [
                CardType::Artifact,
                CardType::Creature,
                CardType::Enchantment,
                CardType::Land,
                CardType::Planeswalker,
                CardType::Battle,
            ]
    {
        return None;
    }
    let mut remainder = filter.clone();
    remainder.zone = None;
    remainder.owner = None;
    remainder.card_types.clear();
    (remainder == ObjectFilter::default()).then_some(*threshold)
}

/// Rejoin an authored conditioned source anthem with its same-condition
/// source trait. The line marker proves source adjacency; these guards prove
/// Battlefield scope, the complete permanent-card graveyard threshold, the
/// source-only modifier, and the exact companion payload.
fn describe_source_line_conditioned_source_anthem_trait_group(
    abilities: &[Ability],
    subject: &str,
) -> Option<String> {
    let [anthem_ability, companion] = abilities else {
        return None;
    };
    if anthem_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || companion.functional_zones != anthem_ability.functional_zones
    {
        return None;
    }
    let AbilityKind::Static(anthem_static) = &anthem_ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::Anthem(anthem) =
        &anthem_static.compiled_model()?.payload
    else {
        return None;
    };
    if anthem.filter.is_some()
        || anthem.set_quantifier_surface.is_some()
        || anthem.count_uses_where_x
        || anthem.replacement_surface.is_some()
    {
        return None;
    }
    let (ironsmith_core::AnthemValue::Fixed(power), ironsmith_core::AnthemValue::Fixed(toughness)) =
        (&anthem.power, &anthem.toughness)
    else {
        return None;
    };
    if (*power, *toughness) == (0, 0) {
        return None;
    }
    let condition = anthem.condition.as_ref()?;
    let threshold = permanent_cards_in_your_graveyard_threshold(condition)?;
    let modifier = format!(
        "{}{modifier}",
        if anthem.additional_surface {
            "an additional "
        } else {
            ""
        },
        modifier = describe_fixed_anthem_modifier(*power, *toughness),
    );
    let threshold = u32::try_from(threshold)
        .ok()
        .and_then(small_number_word)
        .unwrap_or_else(|| threshold.to_string());

    let companion_surface = if let Some((_, keyword, companion_condition)) =
        modeled_source_keyword_grant(companion)
        && companion_condition == condition
    {
        format!("and has {keyword}")
    } else {
        let AbilityKind::Static(companion_static) = &companion.kind else {
            return None;
        };
        let ironsmith_core::StaticAbilityPayload::Conditional {
            ability,
            condition: companion_condition,
        } = &companion_static.compiled_model()?.payload
        else {
            return None;
        };
        let ironsmith_core::StaticAbilityPayload::CantBeBlockedByMoreThan(maximum) =
            &ability.payload
        else {
            return None;
        };
        if companion_condition != condition || *maximum == 0 {
            return None;
        }
        let maximum = u32::try_from(*maximum)
            .ok()
            .and_then(small_number_word)
            .unwrap_or_else(|| maximum.to_string());
        let creature = if maximum == "one" {
            "creature"
        } else {
            "creatures"
        };
        format!("and can't be blocked by more than {maximum} {creature}")
    };

    Some(format!(
        "As long as there are {threshold} or more permanent cards in your graveyard, {subject} gets {modifier} {companion_surface}"
    ))
}

#[cfg(test)]
mod conditioned_source_anthem_trait_group_tests {
    use super::*;

    fn threshold_condition(zone: Zone, threshold: i32) -> Condition {
        let mut filter = ObjectFilter::default().in_zone(zone);
        filter.owner = Some(PlayerFilter::You);
        filter.card_types = vec![
            CardType::Artifact,
            CardType::Creature,
            CardType::Enchantment,
            CardType::Land,
            CardType::Planeswalker,
            CardType::Battle,
        ];
        Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::MatchingFilter(filter),
            comparison: Comparison::GreaterThanOrEqual(threshold),
            display: None,
        }
    }

    fn anthem(condition: Condition) -> Ability {
        let model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::new(
                ironsmith_core::Anthem::for_source(2, 2).with_condition(condition),
            );
        Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
    }

    fn trample(condition: Condition) -> Ability {
        let granted = ironsmith_core::Ability {
            kind: ironsmith_core::AbilityKind::Static(ironsmith_core::StaticAbility {
                id: Some(crate::static_abilities::StaticAbilityId::Trample),
                label: "trample".to_string(),
                payload: ironsmith_core::StaticAbilityPayload::<
                    crate::triggers::Trigger,
                    crate::effect::Effect,
                    crate::costs::Cost,
                    crate::static_abilities::ThisSpellCostCondition,
                >::None,
            }),
            functional_zones: vec![Zone::Battlefield],
        };
        let grant = ironsmith_core::GrantObjectAbilityForFilter::new(
            ObjectFilter::source(),
            granted,
            "Trample",
        )
        .with_condition(condition);
        let model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::new(grant);
        Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
    }

    #[test]
    fn compacts_only_matching_battlefield_graveyard_threshold_siblings() {
        let condition = threshold_condition(Zone::Graveyard, 4);
        let abilities = [anthem(condition.clone()), trample(condition.clone())];
        assert_eq!(
            describe_source_line_conditioned_source_anthem_trait_group(&abilities, "Probe"),
            Some(
                "As long as there are four or more permanent cards in your graveyard, Probe gets +2/+2 and has trample"
                    .to_string()
            )
        );

        let changed_condition = [
            anthem(condition),
            trample(threshold_condition(Zone::Graveyard, 5)),
        ];
        assert!(
            describe_source_line_conditioned_source_anthem_trait_group(&changed_condition, "Probe")
                .is_none()
        );

        let hand_condition = threshold_condition(Zone::Hand, 4);
        let changed_zone = [anthem(hand_condition.clone()), trample(hand_condition)];
        assert!(
            describe_source_line_conditioned_source_anthem_trait_group(&changed_zone, "Probe")
                .is_none()
        );
    }
}

/// Rejoin the layer-correct pieces of an off-battlefield characteristic line.
/// The source-line marker proves authored adjacency; the strict typed guards
/// below prove the shared condition, source filter, and complete one-subtype
/// creature characteristic before presentation is compacted.
fn describe_source_line_off_battlefield_creature_characteristic_group(
    abilities: &[Ability],
    subject: &str,
) -> Option<String> {
    let [type_ability, pt_ability, subtype_ability] = abilities else {
        return None;
    };
    if type_ability.functional_zones != pt_ability.functional_zones
        || type_ability.functional_zones != subtype_ability.functional_zones
        || type_ability.functional_zones.contains(&Zone::Battlefield)
    {
        return None;
    }

    let AbilityKind::Static(type_static) = &type_ability.kind else {
        return None;
    };
    let AbilityKind::Static(pt_static) = &pt_ability.kind else {
        return None;
    };
    let AbilityKind::Static(subtype_static) = &subtype_ability.kind else {
        return None;
    };
    let (type_model, type_condition) = unwrapped_static_model(type_static)?;
    let (pt_model, pt_condition) = unwrapped_static_model(pt_static)?;
    let (subtype_model, subtype_condition) = unwrapped_static_model(subtype_static)?;
    let condition = type_condition?;
    if pt_condition != Some(condition)
        || subtype_condition != Some(condition)
        || !matches!(
            condition,
            Condition::Not(inner)
                if matches!(inner.as_ref(), Condition::SourceIsInZone(Zone::Battlefield))
        )
    {
        return None;
    }

    let ironsmith_core::StaticAbilityPayload::AddCardTypes {
        filter: type_filter,
        card_types,
    } = &type_model.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::SetBasePowerToughness {
        filter: pt_filter,
        power,
        toughness,
    } = &pt_model.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::AddSubtypes {
        filter: subtype_filter,
        subtypes,
    } = &subtype_model.payload
    else {
        return None;
    };
    let [subtype] = subtypes.as_slice() else {
        return None;
    };
    if card_types.as_slice() != [CardType::Creature]
        || type_filter != pt_filter
        || type_filter != subtype_filter
        || !type_filter.source
    {
        return None;
    }

    Some(format!(
        "As long as {subject} isn't on the battlefield, it's {power}/{toughness} {subtype:?} creature in addition to its other types"
    ))
}

/// Rejoin a self P/T bonus and a filtered keyword grant that share one typed
/// condition. Both effects remain independently executable; the marker only
/// proves they came from one authored static source line.
fn describe_source_line_conditional_self_anthem_keyword_grant_group(
    abilities: &[Ability],
) -> Option<String> {
    let [anthem_ability, grant_ability] = abilities else {
        return None;
    };
    if anthem_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || grant_ability.functional_zones != anthem_ability.functional_zones
    {
        return None;
    }
    let AbilityKind::Static(anthem_static) = &anthem_ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::Conditional {
        ability: inner_anthem,
        condition,
    } = &anthem_static.compiled_model()?.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::Anthem(anthem) = &inner_anthem.payload else {
        return None;
    };
    let (ironsmith_core::AnthemValue::Fixed(power), ironsmith_core::AnthemValue::Fixed(toughness)) =
        (&anthem.power, &anthem.toughness)
    else {
        return None;
    };
    if anthem.filter.is_some()
        || anthem.condition.is_some()
        || anthem.set_quantifier_surface.is_some()
        || anthem.count_uses_where_x
        || anthem.additional_surface
        || anthem.replacement_surface.is_some()
        || (*power, *toughness) == (0, 0)
    {
        return None;
    }

    let (grant_filter, grant_condition, granted) = modeled_object_static_grant(grant_ability)?;
    if grant_condition != Some(condition) || !granted.is_keyword() {
        return None;
    }
    let mut normalized_filter = grant_filter.clone();
    normalized_filter.union_surface = Default::default();
    if normalized_filter != ObjectFilter::creature().you_control() {
        return None;
    }
    let keyword = granted
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    if keyword.is_empty() || keyword.contains(',') {
        return None;
    }
    Some(format!(
        "As long as {}, this creature gets {power:+}/{toughness:+} and creatures you control have {keyword}",
        describe_condition(condition)
    ))
}

/// Rejoin the three independently executable pieces of a Lieutenant-style
/// line: the source bonus, the same bonus for other creatures, and their
/// keyword grant. The marker proves one authored line; these guards prove the
/// shared condition, filter, modifier, and granted keyword.
fn describe_source_line_conditional_self_and_other_anthem_keyword_grant_group(
    abilities: &[Ability],
) -> Option<String> {
    let [self_anthem_ability, other_anthem_ability, grant_ability] = abilities else {
        return None;
    };
    if abilities
        .iter()
        .any(|ability| ability.functional_zones.as_slice() != [Zone::Battlefield])
    {
        return None;
    }
    let AbilityKind::Static(self_static) = &self_anthem_ability.kind else {
        return None;
    };
    let AbilityKind::Static(other_static) = &other_anthem_ability.kind else {
        return None;
    };
    let (self_model, self_condition) = unwrapped_static_model(self_static)?;
    let (other_model, other_condition) = unwrapped_static_model(other_static)?;
    let condition = self_condition?;
    if other_condition != Some(condition) {
        return None;
    }
    let ironsmith_core::StaticAbilityPayload::Anthem(self_anthem) = &self_model.payload else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::Anthem(other_anthem) = &other_model.payload else {
        return None;
    };
    let (
        ironsmith_core::AnthemValue::Fixed(self_power),
        ironsmith_core::AnthemValue::Fixed(self_toughness),
        ironsmith_core::AnthemValue::Fixed(other_power),
        ironsmith_core::AnthemValue::Fixed(other_toughness),
    ) = (
        &self_anthem.power,
        &self_anthem.toughness,
        &other_anthem.power,
        &other_anthem.toughness,
    )
    else {
        return None;
    };
    if self_anthem.filter.is_some()
        || self_anthem.condition.is_some()
        || other_anthem.condition.is_some()
        || self_anthem.set_quantifier_surface.is_some()
        || other_anthem.set_quantifier_surface.is_some()
        || self_anthem.count_uses_where_x
        || other_anthem.count_uses_where_x
        || self_anthem.additional_surface
        || other_anthem.additional_surface
        || self_anthem.replacement_surface.is_some()
        || other_anthem.replacement_surface.is_some()
        || (*self_power, *self_toughness) == (0, 0)
        || (*self_power, *self_toughness) != (*other_power, *other_toughness)
    {
        return None;
    }

    let other_filter = other_anthem.filter.as_ref()?;
    let (grant_filter, grant_condition, granted) = modeled_object_static_grant(grant_ability)?;
    if grant_condition != Some(condition) || grant_filter != other_filter || !granted.is_keyword() {
        return None;
    }
    let mut normalized_filter = other_filter.clone();
    normalized_filter.union_surface = Default::default();
    let mut expected_filter = ObjectFilter::creature().you_control();
    expected_filter.other = true;
    if normalized_filter != expected_filter {
        return None;
    }
    let keyword = granted
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    if keyword.is_empty() || keyword.contains(',') {
        return None;
    }
    Some(format!(
        "As long as {}, this creature gets {self_power:+}/{self_toughness:+} and other creatures you control get {other_power:+}/{other_toughness:+} and have {keyword}",
        describe_condition(condition)
    ))
}

/// Rejoin a shared-subject keyword grant, attack requirement, and dynamic
/// anthem emitted as three independent continuous effects. The source-line
/// marker proves authored adjacency; exact typed filters and model payloads
/// prove all three predicates affect the same set.
fn describe_source_line_keyword_attack_dynamic_anthem_group(
    abilities: &[Ability],
) -> Option<String> {
    let [keyword_ability, attack_ability, anthem_ability] = abilities else {
        return None;
    };
    if keyword_ability.functional_zones != attack_ability.functional_zones
        || keyword_ability.functional_zones != anthem_ability.functional_zones
    {
        return None;
    }
    let (keyword_filter, keyword_condition, keyword) =
        modeled_unquantified_object_static_grant(keyword_ability)?;
    let (attack_filter, attack_condition, attack) =
        modeled_unquantified_object_static_grant(attack_ability)?;
    if keyword_condition.is_some()
        || attack_condition.is_some()
        || keyword.id() != crate::static_abilities::StaticAbilityId::Trample
        || attack.id() != crate::static_abilities::StaticAbilityId::MustAttack
        || keyword_filter != attack_filter
    {
        return None;
    }

    let AbilityKind::Static(anthem_static) = &anthem_ability.kind else {
        return None;
    };
    let anthem_model = anthem_static.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::Anthem(anthem) = &anthem_model.payload else {
        return None;
    };
    if anthem.filter.as_ref()? != keyword_filter
        || anthem.condition.is_some()
        || anthem.set_quantifier_surface != Some(ironsmith_core::SetQuantifierSurface::Each)
        || !anthem.count_uses_where_x
        || anthem.additional_surface
        || anthem.replacement_surface.is_some()
        || !matches!(
            (&anthem.power, &anthem.toughness),
            (
                ironsmith_core::AnthemValue::PerCount { multiplier: 1, .. },
                ironsmith_core::AnthemValue::Fixed(0)
            )
        )
    {
        return None;
    }
    let anthem_display = anthem_static.display();
    let anthem_display = anthem_display.trim().trim_end_matches('.');
    let (subject, anthem_tail, anthem_verb) =
        split_static_predicate_with_verb(anthem_display, &[" gets ", " get "])?;
    if anthem_verb != "gets" {
        return None;
    }
    Some(format!(
        "{} has trample, attacks each combat if able, and gets {anthem_tail}",
        capitalize_first(subject)
    ))
}

#[cfg(test)]
mod source_line_keyword_attack_dynamic_anthem_tests {
    use super::*;

    const LINE: &str = "Each Skeleton you control has trample, attacks each combat if able, and gets +X/+0, where X is the number of other Skeletons you control.";

    fn parsed_definition() -> crate::CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Shared Skeleton Static Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(LINE)
        .expect("shared-subject static line should parse")
    }

    #[test]
    fn exact_typed_trio_rejoins_and_zone_near_miss_does_not() {
        let mut members = parsed_definition()
            .abilities
            .into_iter()
            .filter(|ability| {
                !matches!(
                    &ability.kind,
                    AbilityKind::Static(static_ability)
                        if static_ability.id()
                            == crate::static_abilities::StaticAbilityId::SourceLineStaticGroup
                )
            })
            .collect::<Vec<_>>();
        assert_eq!(members.len(), 3, "{members:#?}");
        assert_eq!(
            describe_source_line_keyword_attack_dynamic_anthem_group(&members).as_deref(),
            Some(LINE.trim_end_matches('.')),
            "{members:#?}"
        );

        members[1].functional_zones = vec![Zone::Graveyard];
        assert!(describe_source_line_keyword_attack_dynamic_anthem_group(&members).is_none());
    }

    #[test]
    fn full_source_line_dispatch_preserves_all_three_predicates() {
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&parsed_definition()),
            [LINE]
        );
    }

    #[test]
    fn public_enchantment_keeps_shared_static_subject_and_token_count_replacement() {
        let oracle = "Each Skeleton you control has trample, attacks each combat if able, and gets +X/+0, where X is the number of other Skeletons you control.\nAt the beginning of your end step, create a tapped 1/1 black Skeleton creature token. If a creature died this turn, create two of those tokens instead.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Skeleton Swarm Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(oracle)
        .expect("shared Skeleton rules and replacement should parse");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            oracle.lines().map(str::to_string).collect::<Vec<_>>()
        );
        let replacement = definition.abilities.iter().find_map(|ability| {
            let AbilityKind::Triggered(triggered) = &ability.kind else {
                return None;
            };
            triggered
                .effects
                .segments
                .iter()
                .find_map(|segment| segment.self_replacements.first())
        });
        let replacement = replacement.expect("end-step token count replacement");
        let (_, create) = exact_tagged_create_token(&replacement.replacement_effects)
            .expect("replacement keeps the same tagged token payload");
        assert_eq!(create.count.unhinted(), &Value::Fixed(2));
    }
}

/// Rejoin the four layer-correct pieces of an authored attached-land reset.
/// The group marker carries only source-line provenance; this function proves
/// the exact affected object and both executable mana abilities without using
/// either grant's stored display string.
fn describe_source_line_attached_land_reset_group(abilities: &[Ability]) -> Option<String> {
    let [subtype_reset, ability_loss, first_grant, second_grant] = abilities else {
        return None;
    };
    if abilities
        .iter()
        .any(|ability| ability.functional_zones.as_slice() != [Zone::Battlefield])
    {
        return None;
    }

    let AbilityKind::Static(subtype_reset) = &subtype_reset.kind else {
        return None;
    };
    let subtype_model = subtype_reset.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::SetLandSubtypes {
        filter: subtype_filter,
        subtypes,
    } = &subtype_model.payload
    else {
        return None;
    };
    let AbilityKind::Static(ability_loss) = &ability_loss.kind else {
        return None;
    };
    let ability_loss_model = ability_loss.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::RemoveAllAbilities(ability_filter) =
        &ability_loss_model.payload
    else {
        return None;
    };
    let expected_filter = ObjectFilter::land().match_tagged(
        "enchanted",
        crate::target::TaggedOpbjectRelation::IsTaggedObject,
    );
    if !subtypes.is_empty()
        || subtype_filter != ability_filter
        || subtype_filter != &expected_filter
    {
        return None;
    }

    let render_grant = |ability: &Ability| {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let model = static_ability.compiled_model()?;
        let ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(grant) = &model.payload
        else {
            return None;
        };
        if grant.condition.is_some()
            || !grant.additional_abilities.is_empty()
            || grant.ability.functional_zones.as_slice() != [Zone::Battlefield]
        {
            return None;
        }
        let granted = crate::static_abilities::StaticAbilityModelInterpreter::ability_from_model(
            &grant.ability,
        );
        granted.is_mana_ability().then(|| {
            super::render_effects::describe_inline_ability_with_self_subject(&granted, "this land")
                .trim()
                .trim_end_matches('.')
                .to_string()
        })
    };
    let first = render_grant(first_grant)?;
    let second = render_grant(second_grant)?;
    if first != "{T}: Add {C}" || second != "{T}, Pay 1 life: Add one mana of any color" {
        return None;
    }

    Some(format!(
        "Enchanted land loses all land types and abilities and has \"{first}\" and \"{second}.\""
    ))
}

#[cfg(test)]
mod attached_land_reset_source_group_tests {
    use super::*;

    const LINE: &str = "Enchanted land loses all land types and abilities and has \"{T}: Add {C}\" and \"{T}, Pay 1 life: Add one mana of any color.\"";

    fn parsed_members() -> Vec<Ability> {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Attached Land Reset Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(LINE)
        .expect("attached land reset source line should parse");
        definition
            .abilities
            .iter()
            .filter(|ability| {
                !matches!(
                    &ability.kind,
                    AbilityKind::Static(static_ability)
                        if static_ability.id()
                            == crate::static_abilities::StaticAbilityId::SourceLineStaticGroup
                )
            })
            .cloned()
            .collect()
    }

    #[test]
    fn parsed_land_reset_rejoins_only_the_exact_typed_grants() {
        let members = parsed_members();
        assert_eq!(
            describe_source_line_attached_land_reset_group(&members).as_deref(),
            Some(LINE)
        );

        let mut wrong_zone = members.clone();
        wrong_zone[3].functional_zones = vec![Zone::Graveyard];
        assert!(describe_source_line_attached_land_reset_group(&wrong_zone).is_none());
    }

    #[test]
    fn parsed_land_reset_dispatches_as_one_exact_source_line() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Attached Land Reset Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(LINE)
        .expect("attached land reset source line should parse");
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![LINE],
            "{:#?}",
            definition.abilities
        );
    }
}

fn describe_source_line_graveyard_permission_dynamic_surcharge_group(
    abilities: &[Ability],
) -> Option<String> {
    let [permission, surcharge] = abilities else {
        return None;
    };
    if permission.functional_zones != surcharge.functional_zones
        || !permission.functional_zones.contains(&Zone::Graveyard)
        || permission.functional_zones.contains(&Zone::Battlefield)
    {
        return None;
    }
    let AbilityKind::Static(permission) = &permission.kind else {
        return None;
    };
    let AbilityKind::Static(surcharge) = &surcharge.kind else {
        return None;
    };
    let grant = permission.grant_spec()?;
    if !matches!(&grant.grantable, ironsmith_core::Grantable::PlayFrom)
        || grant.zone != Zone::Graveyard
        || grant.beneficiary != PlayerFilter::You
        || grant.usage_limit.is_some()
        || !grant.cast_this_way_grants.is_empty()
        || grant.cast_this_way_filter.is_some()
        || grant.source_exiled_surface.is_some()
        || !grant.filter.source
    {
        return None;
    }
    let source_surface = match grant.filter.source_surface.as_ref()? {
        ironsmith_core::SourceReferenceSurface::ThisPermanentType(surface)
            if surface.starts_with("this ") =>
        {
            surface.as_str()
        }
        _ => return None,
    };
    let mut permission_residual = grant.filter.clone();
    permission_residual.source = false;
    permission_residual.source_surface = None;
    if permission_residual != ObjectFilter::default() {
        return None;
    }

    let increase = surcharge.cost_increase()?;
    if increase.condition.is_some()
        || increase.per_target
        || increase.filter != ObjectFilter::source()
    {
        return None;
    }
    let (counted, multiplier) = match increase.increase.unhinted() {
        Value::Count(filter) => (filter, 1),
        Value::CountScaled(filter, multiplier) if *multiplier > 0 => (filter, *multiplier),
        _ => return None,
    };
    let mut counted_residual = counted.clone();
    if counted_residual.zone != Some(Zone::Graveyard)
        || counted_residual.owner != Some(PlayerFilter::You)
        || counted_residual.card_types.as_slice() != [CardType::Creature]
        || !counted_residual.other
    {
        return None;
    }
    counted_residual.zone = None;
    counted_residual.owner = None;
    counted_residual.card_types.clear();
    counted_residual.other = false;
    counted_residual.set_explicit_card_type_noun(None);
    counted_residual.set_explicit_card_noun(false);
    if counted_residual != ObjectFilter::default() {
        return None;
    }

    Some(format!(
        "You may cast {source_surface} from your graveyard if you pay {{{multiplier}}} more to cast it for each other creature card in your graveyard"
    ))
}

#[cfg(test)]
mod source_graveyard_dynamic_surcharge_render_tests {
    use super::*;

    fn fixture(owner: PlayerFilter) -> Vec<Ability> {
        let source = ObjectFilter::source_with_surface(
            ironsmith_core::SourceReferenceSurface::ThisPermanentType("this creature".to_string()),
        );
        let permission =
            crate::static_abilities::StaticAbility::grants(crate::grant::GrantSpec::new(
                crate::grant::Grantable::play_from(),
                source,
                Zone::Graveyard,
            ));
        let mut counted = ObjectFilter::creature();
        counted.zone = Some(Zone::Graveyard);
        counted.owner = Some(owner);
        counted.other = true;
        counted.set_explicit_card_type_noun(Some(CardType::Creature));
        counted.set_explicit_card_noun(true);
        let surcharge = crate::static_abilities::StaticAbility::new(
            crate::static_abilities::CostIncrease::new(
                ObjectFilter::source(),
                Value::Count(counted).with_surface_hint(ironsmith_core::ValueSurfaceHint::ForEach),
            ),
        );
        vec![
            Ability::static_ability(permission).in_zones(vec![Zone::Graveyard]),
            Ability::static_ability(surcharge).in_zones(vec![Zone::Graveyard]),
        ]
    }

    #[test]
    fn renders_only_matching_source_graveyard_permission_and_counted_tax() {
        let exact_fixture = fixture(PlayerFilter::You);
        assert_eq!(
            describe_source_line_graveyard_permission_dynamic_surcharge_group(&exact_fixture)
                .as_deref(),
            Some(
                "You may cast this creature from your graveyard if you pay {1} more to cast it for each other creature card in your graveyard"
            ),
            "{exact_fixture:#?}"
        );
        assert!(
            describe_source_line_graveyard_permission_dynamic_surcharge_group(&fixture(
                PlayerFilter::Opponent
            ))
            .is_none()
        );
    }
}

fn describe_source_line_conditional_spell_protection_group(
    abilities: &[Ability],
) -> Option<String> {
    let [first, second] = abilities else {
        return None;
    };
    if first.functional_zones != second.functional_zones
        || !first.functional_zones.contains(&Zone::Stack)
        || first.functional_zones.contains(&Zone::Battlefield)
    {
        return None;
    }
    let AbilityKind::Static(first) = &first.kind else {
        return None;
    };
    let AbilityKind::Static(second) = &second.kind else {
        return None;
    };
    let Some((first_model, Some(first_condition))) = unwrapped_static_model(first) else {
        return None;
    };
    let Some((second_model, Some(second_condition))) = unwrapped_static_model(second) else {
        return None;
    };
    if first_condition != second_condition {
        return None;
    }
    let uncounterable = |model: &crate::static_abilities::CompiledStaticAbility| {
        model.id == Some(crate::static_abilities::StaticAbilityId::CantBeCountered)
    };
    let unpreventable = |model: &crate::static_abilities::CompiledStaticAbility| {
        matches!(
            &model.payload,
            ironsmith_core::StaticAbilityPayload::RuleRestriction {
                restriction: crate::effect::Restriction::PreventDamage,
                additional_restrictions,
                ..
            } if additional_restrictions.is_empty()
        )
    };
    if !((uncounterable(first_model) && unpreventable(second_model))
        || (uncounterable(second_model) && unpreventable(first_model)))
    {
        return None;
    }
    Some(format!(
        "If {}, this spell can't be countered and the damage can't be prevented",
        super::normalize_common::describe_condition(first_condition)
    ))
}

#[cfg(test)]
mod conditional_spell_protection_source_group_tests {
    use super::*;

    const BANEFIRE_LINE: &str =
        "If X is 5 or more, this spell can't be countered and the damage can't be prevented.";
    const DEMONFIRE_LINE: &str = "Hellbent — If you have no cards in hand, this spell can't be countered and the damage can't be prevented.";

    fn definition(name: &str, line: &str) -> crate::CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(crate::ids::CardId::new(), name)
            .card_types(vec![CardType::Sorcery])
            .parse_text(line)
            .expect("conditional spell-protection line should compile")
    }

    fn protection_members(definition: &crate::CardDefinition) -> Vec<Ability> {
        definition
            .abilities
            .iter()
            .filter(|ability| source_line_static_group_count(ability).is_none())
            .cloned()
            .collect()
    }

    #[test]
    fn public_conditional_spell_protection_lines_rejoin_exactly() {
        for (name, line) in [
            ("Banefire Probe", BANEFIRE_LINE),
            ("Demonfire Probe", DEMONFIRE_LINE),
        ] {
            let definition = definition(name, line);
            assert_eq!(
                crate::compiled_text::compiled_text_lines(&definition),
                [line],
                "abilities={:#?}\ndebug={:#?}",
                definition.abilities,
                crate::compiled_text::debug_compiled_lines(&definition),
            );
            let members = protection_members(&definition);
            assert_eq!(members.len(), 2, "{members:#?}");
            assert!(members.iter().all(|ability| {
                ability.functional_zones.contains(&Zone::Stack)
                    && !ability.functional_zones.contains(&Zone::Battlefield)
            }));
        }
    }

    #[test]
    fn coalescer_rejects_a_different_condition_or_extra_restriction() {
        let definition = definition("Protection Near Miss", BANEFIRE_LINE);
        let members = protection_members(&definition);

        let mut different_condition = members.clone();
        let AbilityKind::Static(second) = &different_condition[1].kind else {
            panic!("expected a static restriction")
        };
        let mut model = second
            .compiled_model()
            .expect("compiled static model")
            .clone();
        let ironsmith_core::StaticAbilityPayload::Conditional { condition, .. } =
            &mut model.payload
        else {
            panic!("expected a conditional restriction")
        };
        *condition = Condition::XValueAtLeast(6);
        different_condition[1] =
            Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
                .in_zones(spell_only_functional_zones_for_test());
        assert!(
            describe_source_line_conditional_spell_protection_group(&different_condition).is_none()
        );

        let mut extra_restriction = members;
        let AbilityKind::Static(second) = &extra_restriction[1].kind else {
            panic!("expected a static restriction")
        };
        let mut model = second
            .compiled_model()
            .expect("compiled static model")
            .clone();
        let ironsmith_core::StaticAbilityPayload::Conditional { ability, .. } = &mut model.payload
        else {
            panic!("expected a conditional restriction")
        };
        let ironsmith_core::StaticAbilityPayload::RuleRestriction {
            additional_restrictions,
            ..
        } = &mut ability.payload
        else {
            panic!("expected a rule restriction")
        };
        additional_restrictions.push(crate::effect::Restriction::PreventDamage);
        extra_restriction[1] =
            Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
                .in_zones(spell_only_functional_zones_for_test());
        assert!(
            describe_source_line_conditional_spell_protection_group(&extra_restriction).is_none()
        );
    }

    fn spell_only_functional_zones_for_test() -> Vec<Zone> {
        vec![
            Zone::Hand,
            Zone::Stack,
            Zone::Graveyard,
            Zone::Exile,
            Zone::Library,
            Zone::Command,
        ]
    }
}

fn describe_source_line_any_player_ignore_search_group(abilities: &[Ability]) -> Option<String> {
    let [restriction, permission] = abilities else {
        return None;
    };
    if restriction.functional_zones.as_slice() != [Zone::Battlefield]
        || permission.functional_zones != restriction.functional_zones
    {
        return None;
    }
    let AbilityKind::Static(restriction) = &restriction.kind else {
        return None;
    };
    if restriction.id() != crate::static_abilities::StaticAbilityId::PlayersCantSearch {
        return None;
    }
    let AbilityKind::Static(permission) = &permission.kind else {
        return None;
    };
    let model = permission.compiled_model()?;
    if model.id
        != Some(
            crate::static_abilities::StaticAbilityId::AnyPlayerMayPayManaToIgnoreSourceEffectUntilEndOfTurn,
        )
    {
        return None;
    }
    let ironsmith_core::StaticAbilityPayload::AnyPlayerMayPayManaToIgnoreSourceEffectUntilEndOfTurn {
        cost,
        ..
    } = &model.payload
    else {
        return None;
    };
    Some(format!(
        "Players can't search libraries. Any player may pay {} for that player to ignore this effect until end of turn",
        cost.to_oracle()
    ))
}

fn describe_source_line_attached_restriction_loss_grant_group(
    abilities: &[Ability],
) -> Option<String> {
    let [restriction, loss, grant] = abilities else {
        return None;
    };
    let AbilityKind::Static(restriction_static) = &restriction.kind else {
        return None;
    };
    let restriction_display = restriction_static.display();
    let restriction_display = restriction_display.trim().trim_end_matches('.');
    let restriction_subject = restriction_display.strip_suffix(" can't attack or block")?;
    if !matches!(
        restriction_subject.to_ascii_lowercase().as_str(),
        "enchanted creature" | "equipped creature"
    ) {
        return None;
    }

    // An attached ability reset followed by a quoted grant retains its body
    // structurally; the cached static display predates source-name binding.
    if let (AbilityKind::Static(loss_static), AbilityKind::Static(grant_static)) =
        (&loss.kind, &grant.kind)
        && let Some(loss_model) = loss_static.compiled_model()
        && let ironsmith_core::StaticAbilityPayload::RemoveAllAbilities(filter) =
            &loss_model.payload
        && let Some(subject) = exact_attached_creature_subject(filter)
        && static_bundle_subjects_match(subject, restriction_subject)
        && let Some(grant_model) = grant_static.compiled_model()
        && let ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(granted) =
            &grant_model.payload
        && granted.condition.is_none()
        && granted.additional_abilities.is_empty()
        && !granted.protection_does_not_remove_controlled_attachments
        && granted.ability.functional_zones.as_slice() == [Zone::Battlefield]
        && abilities
            .iter()
            .all(|a| a.functional_zones.as_slice() == [Zone::Battlefield])
        && let Some(restriction_model) = restriction_static.compiled_model()
        && let ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(restriction_grant) =
            &restriction_model.payload
        && restriction_grant.condition.is_none()
        && restriction_grant.additional_abilities.is_empty()
        && let ironsmith_core::AbilityKind::Static(rule) = &restriction_grant.ability.kind
        && let ironsmith_core::StaticAbilityPayload::RuleRestriction {
            restriction: crate::effect::Restriction::AttackOrBlock(restricted),
            additional_restrictions,
            ..
        } = &rule.payload
        && additional_restrictions.is_empty()
        && restricted == &ObjectFilter::source()
    {
        let ability = crate::static_abilities::StaticAbilityModelInterpreter::ability_from_model(
            &granted.ability,
        );
        let body = super::render_effects::describe_inline_ability_with_self_subject(
            &ability,
            "this creature",
        );
        return Some(format!(
            "{restriction_display}. It loses all abilities and has \"{}.\"",
            body.trim_end_matches('.')
        ));
    }

    let loss_grant =
        describe_source_line_grant_all_other_loss_group(&[loss.clone(), grant.clone()])?;
    let (loss_subject, _, _) =
        split_static_predicate_with_verb(&loss_grant, &[" loses ", " lose "])?;
    if !static_bundle_subjects_match(restriction_subject, loss_subject) {
        return None;
    }
    Some(format!("{restriction_display}. {loss_grant}"))
}

#[cfg(test)]
mod source_line_grant_loss_tests {
    use super::*;

    #[test]
    fn labeled_grant_and_all_other_loss_rejoin_one_authored_static_line() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Static Grant And Loss Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(
            "Hypertoxic Miasma — All lands have \"{T}: Add one mana of any color\" and lose all other abilities.",
        )
        .expect("grant and loss static line should compile");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![
                "Hypertoxic Miasma — All lands have \"{T}: Add one mana of any color\" and lose all other abilities."
                    .to_string()
            ],
            "{:#?}",
            definition.abilities
        );
    }
}

fn unwrapped_static_model(
    ability: &crate::static_abilities::StaticAbility,
) -> Option<(
    &crate::static_abilities::CompiledStaticAbility,
    Option<&Condition>,
)> {
    let model = ability.compiled_model()?;
    match &model.payload {
        ironsmith_core::StaticAbilityPayload::Conditional {
            ability: inner,
            condition,
        } => Some((inner, Some(condition))),
        _ => Some((model, None)),
    }
}

fn describe_iterated_animation_value(value: &Value) -> Option<String> {
    match value.unhinted() {
        Value::ManaValueOf(value_spec) if matches!(value_spec.base(), ChooseSpec::Iterated) => {
            Some("its mana value".to_string())
        }
        Value::CountersOn(value_spec, Some(counter_type))
            if matches!(value_spec.base(), ChooseSpec::Iterated) =>
        {
            Some(format!(
                "the number of {} counters on it",
                counter_type.description()
            ))
        }
        _ => None,
    }
}

/// Recombine the layer-correct pieces of a permanent type animation. The
/// compiler intentionally lowers type-setting, dynamic base P/T, and optional
/// ability removal separately; this renderer rejoins them only when their
/// typed filters, conditions, and per-object values prove they are one clause.
fn describe_structural_dynamic_animation_bundle(abilities: &[Ability]) -> Option<(String, usize)> {
    let first = abilities.first()?;
    let AbilityKind::Static(first_static) = &first.kind else {
        return None;
    };
    let has_remove =
        first_static.id() == crate::static_abilities::StaticAbilityId::RemoveAllAbilitiesForFilter;
    let (remove_ability, set_types_ability, base_pt_ability, consumed) = if has_remove {
        (Some(first), abilities.get(1)?, abilities.get(2)?, 3usize)
    } else {
        (None, first, abilities.get(1)?, 2usize)
    };
    if first.functional_zones.as_slice() != [Zone::Battlefield]
        || set_types_ability.functional_zones != first.functional_zones
        || base_pt_ability.functional_zones != first.functional_zones
    {
        return None;
    }

    let AbilityKind::Static(set_types_static) = &set_types_ability.kind else {
        return None;
    };
    let AbilityKind::Static(base_pt_static) = &base_pt_ability.kind else {
        return None;
    };
    if set_types_static.id() != crate::static_abilities::StaticAbilityId::SetCardTypes
        || base_pt_static.id()
            != crate::static_abilities::StaticAbilityId::SetBasePowerToughnessForFilter
    {
        return None;
    }

    let (set_types_model, set_condition) = unwrapped_static_model(set_types_static)?;
    let (base_pt_model, base_pt_condition) = unwrapped_static_model(base_pt_static)?;
    let ironsmith_core::StaticAbilityPayload::SetCardTypes {
        filter: set_types_filter,
        card_types,
    } = &set_types_model.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::SetBasePowerToughnessValue {
        filter: base_pt_filter,
        power,
        toughness,
    } = &base_pt_model.payload
    else {
        return None;
    };
    if set_types_filter != base_pt_filter
        || set_condition != base_pt_condition
        || power != toughness
        || card_types.is_empty()
        || !card_types.contains(&CardType::Creature)
    {
        return None;
    }
    let value = describe_iterated_animation_value(power)?;

    let set_types_display =
        crate::static_abilities::StaticAbility::from_model(set_types_model.clone()).display();
    let (subject, type_phrase, type_verb) =
        split_static_predicate_with_verb(&set_types_display, &[" is ", " are "])?;
    if type_verb != "is" {
        return None;
    }

    let remove_subject = if let Some(remove_ability) = remove_ability {
        let AbilityKind::Static(remove_static) = &remove_ability.kind else {
            return None;
        };
        let (remove_model, remove_condition) = unwrapped_static_model(remove_static)?;
        let ironsmith_core::StaticAbilityPayload::RemoveAllAbilities(remove_filter) =
            &remove_model.payload
        else {
            return None;
        };
        if remove_filter != set_types_filter || remove_condition != set_condition {
            return None;
        }
        let remove_display =
            crate::static_abilities::StaticAbility::from_model(remove_model.clone()).display();
        let remove_subject = remove_display
            .strip_suffix(" lose all abilities")
            .or_else(|| remove_display.strip_suffix(" loses all abilities"))?;
        static_bundle_subjects_match(subject, remove_subject).then(|| remove_subject.to_string())?
    } else {
        subject.to_string()
    };

    let predicate =
        format!("{type_verb} {type_phrase} with power and toughness each equal to {value}");
    let text = match set_condition {
        Some(Condition::Not(inner)) => {
            let Condition::AttachedToSourceMatches(match_filter) = inner.as_ref() else {
                return None;
            };
            if has_remove {
                return None;
            }
            let excluded = describe_simple_attached_match(match_filter)?;
            format!(
                "As long as {} isn't {excluded}, it's {type_phrase} with power and toughness each equal to {value}",
                lowercase_first(subject),
            )
        }
        Some(_) => return None,
        None if has_remove => {
            format!("{remove_subject} loses all abilities and {predicate}")
        }
        None => format!("{subject} {predicate}"),
    };
    Some((text, consumed))
}

#[cfg(test)]
mod dynamic_animation_bundle_tests {
    use super::*;

    fn set_each_surface(filter: &mut ObjectFilter) {
        filter.set_set_quantifier_surface(Some(ironsmith_core::SetQuantifierSurface::Each));
    }

    fn modeled(model: crate::static_abilities::CompiledStaticAbility) -> Ability {
        Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
    }

    #[test]
    fn animate_artifact_rejoins_conditional_type_and_dynamic_pt_layers() {
        let filter = ObjectFilter::artifact().match_tagged(
            "enchanted",
            crate::target::TaggedOpbjectRelation::IsTaggedObject,
        );
        let condition = Condition::Not(Box::new(Condition::AttachedToSourceMatches(
            ObjectFilter::creature(),
        )));
        let mana_value = Value::ManaValueOf(Box::new(ChooseSpec::Iterated));
        let set_types: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::set_card_types(
                filter.clone(),
                vec![CardType::Artifact, CardType::Creature],
            )
            .with_condition(condition.clone());
        let set_pt: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::set_base_power_toughness_value(
                filter,
                mana_value.clone(),
                mana_value,
            )
            .with_condition(condition);
        let abilities = vec![modeled(set_types), modeled(set_pt)];

        assert_eq!(
            describe_structural_dynamic_animation_bundle(&abilities),
            Some((
                "As long as enchanted artifact isn't a creature, it's an artifact creature with power and toughness each equal to its mana value"
                    .to_string(),
                2,
            )),
        );
    }

    #[test]
    fn march_rejoins_type_and_per_artifact_mana_value_layers() {
        let mut filter = ObjectFilter::artifact().without_type(CardType::Creature);
        set_each_surface(&mut filter);
        let mana_value = Value::ManaValueOf(Box::new(ChooseSpec::Iterated));
        let set_types: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::set_card_types(
                filter.clone(),
                vec![CardType::Artifact, CardType::Creature],
            );
        let set_pt: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::set_base_power_toughness_value(
                filter,
                mana_value.clone(),
                mana_value,
            );
        let abilities = vec![modeled(set_types), modeled(set_pt)];

        assert_eq!(
            describe_structural_dynamic_animation_bundle(&abilities),
            Some((
                "Each noncreature artifact is an artifact creature with power and toughness each equal to its mana value"
                    .to_string(),
                2,
            )),
        );
    }

    #[test]
    fn spark_rupture_rejoins_ability_loss_type_and_counter_value_layers() {
        let mut filter = ObjectFilter::planeswalker().with_counter_type(CounterType::Loyalty);
        set_each_surface(&mut filter);
        filter.set_counter_requirement_surface(true, true, false);
        let loyalty = Value::CountersOn(Box::new(ChooseSpec::Iterated), Some(CounterType::Loyalty));
        let remove: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::remove_all_abilities(filter.clone());
        let set_types: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::set_card_types(filter.clone(), vec![CardType::Creature]);
        let set_pt: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::set_base_power_toughness_value(
                filter,
                loyalty.clone(),
                loyalty,
            );
        let abilities = vec![modeled(remove), modeled(set_types), modeled(set_pt)];

        assert_eq!(
            describe_structural_dynamic_animation_bundle(&abilities),
            Some((
                "Each planeswalker with one or more loyalty counters on it loses all abilities and is a creature with power and toughness each equal to the number of loyalty counters on it"
                    .to_string(),
                3,
            )),
        );
    }
}

/// Recombine the layer-correct pieces of a fixed-stat source animation
/// ("During your turn, this creature is a Bear with base power and toughness
/// 4/2."). The compiler lowers the creature type set, the subtype
/// replacement, and the fixed base P/T separately; rejoin them only when
/// their shared source filter and identical conditions prove they are one
/// authored clause.
fn describe_structural_source_fixed_animation_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [set_types_ability, set_subtypes_ability, base_pt_ability, ..] = abilities else {
        return None;
    };
    for ability in [set_types_ability, set_subtypes_ability, base_pt_ability] {
        if ability.functional_zones.as_slice() != [Zone::Battlefield] {
            return None;
        }
    }
    let AbilityKind::Static(set_types_static) = &set_types_ability.kind else {
        return None;
    };
    let AbilityKind::Static(set_subtypes_static) = &set_subtypes_ability.kind else {
        return None;
    };
    let AbilityKind::Static(base_pt_static) = &base_pt_ability.kind else {
        return None;
    };
    let (set_types_model, set_condition) = unwrapped_static_model(set_types_static)?;
    let (set_subtypes_model, subtypes_condition) = unwrapped_static_model(set_subtypes_static)?;
    let (base_pt_model, base_pt_condition) = unwrapped_static_model(base_pt_static)?;
    let ironsmith_core::StaticAbilityPayload::SetCardTypes { filter, card_types } =
        &set_types_model.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::SetCreatureSubtypes {
        filter: subtypes_filter,
        subtypes,
    } = &set_subtypes_model.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::SetBasePowerToughnessValue {
        filter: base_pt_filter,
        power,
        toughness,
    } = &base_pt_model.payload
    else {
        return None;
    };
    if card_types.as_slice() != [CardType::Creature]
        || subtypes.is_empty()
        || !filter.source
        || filter != subtypes_filter
        || filter != base_pt_filter
        || set_condition != subtypes_condition
        || set_condition != base_pt_condition
    {
        return None;
    }
    let (Value::Fixed(power), Value::Fixed(toughness)) = (power.unhinted(), toughness.unhinted())
    else {
        return None;
    };
    let condition_prefix = match set_condition {
        None => "",
        Some(Condition::ActivationTiming(crate::ability::ActivationTiming::DuringYourTurn))
        | Some(Condition::YourTurn) => "During your turn, ",
        Some(_) => return None,
    };
    let subtype_phrase = subtypes
        .iter()
        .map(ToString::to_string)
        .collect::<Vec<_>>()
        .join(" ");
    let text = format!(
        "{condition_prefix}this creature is {} with base power and toughness {power}/{toughness}",
        with_indefinite_article(&subtype_phrase),
    );
    Some((capitalize_first(&text), 3))
}

/// Recombine layer-correct static pieces from one authored modifier clause.
/// The terminal type addition is the structural anchor: every consumed piece
/// must affect the same subject, and only P/T, granted-ability, and additive
/// type predicates are accepted.
fn describe_structural_modifier_type_addition_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let AbilityKind::Static(first) = &abilities.first()?.kind else {
        return None;
    };
    let first_display = first.display();
    let (subject, first_tail, first_verb) = match first.id() {
        crate::static_abilities::StaticAbilityId::Anthem => {
            split_static_predicate_with_verb(&first_display, &[" gets ", " get "])?
        }
        crate::static_abilities::StaticAbilityId::SetBasePowerToughnessForFilter => {
            split_static_predicate_with_verb(&first_display, &[" has ", " have "])?
        }
        _ => return None,
    };
    let singular = matches!(first_verb, "gets" | "has");
    let first_predicate = format!("{first_verb} {first_tail}");
    let mut grant_items = Vec::new();
    let mut has_quoted_grant = false;
    let mut removes_all_other_abilities = false;
    let mut color_descriptors = Vec::new();
    let mut colors_are_additive = false;
    let mut type_descriptors = Vec::new();
    let mut consumed = 1usize;
    let mut saw_type_addition = false;

    while let Some(Ability {
        kind: AbilityKind::Static(next),
        ..
    }) = abilities.get(consumed)
    {
        let display = next.display();
        match next.id() {
            crate::static_abilities::StaticAbilityId::RemoveAllAbilitiesForFilter => {
                if saw_type_addition || removes_all_other_abilities {
                    break;
                }
                let next_subject = display
                    .strip_suffix(" lose all abilities")
                    .or_else(|| display.strip_suffix(" loses all abilities"))?;
                if !static_bundle_subjects_match(subject, next_subject) {
                    break;
                }
                removes_all_other_abilities = true;
            }
            crate::static_abilities::StaticAbilityId::GrantAbility
            | crate::static_abilities::StaticAbilityId::GrantObjectAbilityForFilter => {
                if saw_type_addition {
                    break;
                }
                let (next_subject, tail, _) =
                    split_static_predicate_with_verb(&display, &[" has ", " have "])?;
                if !static_bundle_subjects_match(subject, next_subject) {
                    break;
                }
                grant_items.push(tail.to_string());
            }
            crate::static_abilities::StaticAbilityId::AttachedAbilityGrant => {
                if saw_type_addition {
                    break;
                }
                let (next_subject, tail, _) =
                    split_static_predicate_with_verb(&display, &[" has ", " have "])?;
                if !static_bundle_subjects_match(subject, next_subject) {
                    break;
                }
                let body = tail.trim().trim_matches('"').trim_end_matches('.');
                let terminal = if body.ends_with('?') || body.ends_with('!') {
                    ""
                } else {
                    "."
                };
                grant_items.push(format!("\"{body}{terminal}\""));
                has_quoted_grant = true;
            }
            crate::static_abilities::StaticAbilityId::SetColors
            | crate::static_abilities::StaticAbilityId::AddColors => {
                if saw_type_addition {
                    break;
                }
                let (next_subject, tail, _) =
                    split_static_predicate_with_verb(&display, &[" is ", " are "])?;
                if !static_bundle_subjects_match(subject, next_subject) {
                    break;
                }
                let (descriptor, additive) = if next.id()
                    == crate::static_abilities::StaticAbilityId::AddColors
                {
                    (
                        tail.strip_suffix(" in addition to its other colors")
                            .or_else(|| tail.strip_suffix(" in addition to their other colors"))?,
                        true,
                    )
                } else {
                    (tail, false)
                };
                color_descriptors.push(descriptor.trim().to_string());
                colors_are_additive |= additive;
            }
            crate::static_abilities::StaticAbilityId::AddCardTypes
            | crate::static_abilities::StaticAbilityId::AddSubtypes => {
                let (next_subject, tail, type_verb) =
                    split_static_predicate_with_verb(&display, &[" is ", " are "])?;
                if !static_bundle_subjects_match(subject, next_subject) {
                    break;
                }
                let descriptor = tail
                    .strip_suffix(" in addition to its other types")
                    .or_else(|| tail.strip_suffix(" in addition to their other types"))?;
                let descriptor = descriptor
                    .strip_prefix("a ")
                    .or_else(|| descriptor.strip_prefix("an "))
                    .unwrap_or(descriptor)
                    .trim();
                let descriptor = if singular && type_verb == "are" {
                    let mut words = descriptor
                        .split_whitespace()
                        .map(str::to_string)
                        .collect::<Vec<_>>();
                    if let Some(last) = words.last_mut() {
                        *last = singular_subtype_word(last);
                    }
                    words.join(" ")
                } else {
                    descriptor.to_string()
                };
                type_descriptors.push(descriptor);
                saw_type_addition = true;
            }
            _ => break,
        }
        consumed += 1;
    }

    if !saw_type_addition || type_descriptors.is_empty() {
        return None;
    }
    let mut descriptors = color_descriptors;
    descriptors.extend(type_descriptors);
    let descriptor = descriptors.join(" ");
    let descriptor = if singular {
        with_indefinite_article(&descriptor)
    } else {
        descriptor
    };
    let scopes = if colors_are_additive {
        if singular {
            "its other colors and types"
        } else {
            "their other colors and types"
        }
    } else if singular {
        "its other types"
    } else {
        "their other types"
    };
    let type_predicate = format!(
        "{} {descriptor} in addition to {scopes}",
        if singular { "is" } else { "are" },
    );

    let text = if grant_items.is_empty() && removes_all_other_abilities {
        format!("{subject} {first_predicate}, loses all other abilities, and {type_predicate}")
    } else if grant_items.is_empty() {
        format!("{subject} {first_predicate} and {type_predicate}")
    } else {
        let grant_predicate = format!("has {}", render_static_bundle_list(&grant_items));
        if removes_all_other_abilities {
            format!(
                "{subject} {first_predicate}, {grant_predicate}, loses all other abilities, and {type_predicate}"
            )
        } else if has_quoted_grant && singular {
            format!(
                "{subject} {first_predicate} and {grant_predicate}. It's {descriptor} in addition to its other types"
            )
        } else {
            format!("{subject} {first_predicate}, {grant_predicate}, and {type_predicate}")
        }
    };
    Some((text, consumed))
}

fn static_subject_for_attached_transform_piece(ability: &Ability) -> Option<String> {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return None;
    };
    let display = static_ability.display();
    match static_ability.id() {
        crate::static_abilities::StaticAbilityId::AddSubtypes
        | crate::static_abilities::StaticAbilityId::SetLandSubtypes
        | crate::static_abilities::StaticAbilityId::SetColors
        | crate::static_abilities::StaticAbilityId::MakeColorless => {
            split_static_predicate_with_verb(&display, &[" is ", " are "])
                .map(|(subject, _, _)| subject.to_string())
        }
        crate::static_abilities::StaticAbilityId::RemoveAllAbilitiesForFilter => display
            .strip_suffix(" lose all abilities")
            .or_else(|| display.strip_suffix(" loses all abilities"))
            .map(str::to_string),
        _ => None,
    }
}

/// Recombine a layer-correct attached-creature transformation consisting of
/// a creature-type reset, one creature subtype, fixed base P/T, one granted
/// keyword, and removal of every other ability. Every executable component
/// remains independent; this helper restores the two authored sentences only
/// after their typed filters and attachment ownership agree exactly.
fn describe_structural_attached_subtype_base_pt_keyword_loss_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [
        types_ability,
        subtype_ability,
        pt_ability,
        grant_ability,
        loss_ability,
        ..,
    ] = abilities
    else {
        return None;
    };
    let members = [
        types_ability,
        subtype_ability,
        pt_ability,
        grant_ability,
        loss_ability,
    ];
    if members
        .iter()
        .any(|ability| ability.functional_zones.as_slice() != [Zone::Battlefield])
    {
        return None;
    }
    fn model(ability: &Ability) -> Option<&crate::static_abilities::CompiledStaticAbility> {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        static_ability.compiled_model()
    }
    let types = model(types_ability)?;
    let subtype = model(subtype_ability)?;
    let pt = model(pt_ability)?;
    let grant = model(grant_ability)?;
    let loss = model(loss_ability)?;

    let ironsmith_core::StaticAbilityPayload::SetCardTypes {
        filter: types_filter,
        card_types,
    } = &types.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::SetCreatureSubtypes {
        filter: subtype_filter,
        subtypes,
    } = &subtype.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::SetBasePowerToughnessValue {
        filter: pt_filter,
        power,
        toughness,
    } = &pt.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(grant) = &grant.payload else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::RemoveAllAbilities(loss_filter) = &loss.payload
    else {
        return None;
    };
    let [subtype] = subtypes.as_slice() else {
        return None;
    };
    let (Value::Fixed(power), Value::Fixed(toughness)) = (power.unhinted(), toughness.unhinted())
    else {
        return None;
    };
    if card_types.as_slice() != [CardType::Creature]
        || grant.condition.is_some()
        || !grant.additional_abilities.is_empty()
        || grant.protection_does_not_remove_controlled_attachments
        || grant.ability.functional_zones.as_slice() != [Zone::Battlefield]
    {
        return None;
    }
    let ironsmith_core::AbilityKind::Static(granted_model) = &grant.ability.kind else {
        return None;
    };
    let granted = crate::static_abilities::StaticAbility::from_model(granted_model.clone());
    if !granted.is_keyword() {
        return None;
    }
    let keyword = granted
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    if keyword.is_empty() || keyword.contains(',') || keyword.contains('"') {
        return None;
    }

    let normalized = |filter: &ObjectFilter| {
        let mut filter = filter.clone();
        filter.set_explicit_card_type_noun(None);
        filter.source_surface = None;
        filter
    };
    let expected = ObjectFilter::creature().match_tagged(
        "enchanted",
        crate::target::TaggedOpbjectRelation::IsTaggedObject,
    );
    if normalized(types_filter) != expected
        || normalized(subtype_filter) != expected
        || normalized(pt_filter) != expected
        || normalized(loss_filter) != expected
    {
        return None;
    }
    let display = grant.display.trim().trim_end_matches('.');
    let (subject, display_keyword) = split_static_predicate(display, " has ")?;
    if !subject.eq_ignore_ascii_case("enchanted creature")
        || !display_keyword.eq_ignore_ascii_case(&keyword)
    {
        return None;
    }

    Some((
        format!(
            "Enchanted creature is {} with base power and toughness {power}/{toughness}. It has {keyword} and loses all other abilities",
            with_indefinite_article(&subtype.to_string()),
        ),
        5,
    ))
}

#[cfg(test)]
mod attached_subtype_base_pt_keyword_loss_bundle_tests {
    use super::*;

    const LINE: &str = "Enchanted creature is a Citizen with base power and toughness 1/1. It has defender and loses all other abilities.";

    fn definition() -> crate::CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Attached Subtype Transform Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(format!("Enchant creature\n{LINE}"))
        .expect("attached subtype transform should parse")
    }

    fn members() -> Vec<Ability> {
        definition()
            .abilities
            .into_iter()
            .filter(|ability| {
                !matches!(
                    &ability.kind,
                    AbilityKind::Static(static_ability)
                        if static_ability.id()
                            == crate::static_abilities::StaticAbilityId::SourceLineStaticGroup
                )
            })
            .collect()
    }

    #[test]
    fn exact_typed_bundle_rejoins_and_changed_zone_does_not() {
        let mut members = members();
        assert_eq!(members.len(), 5, "{members:#?}");
        assert_eq!(
            describe_structural_attached_subtype_base_pt_keyword_loss_bundle(&members),
            Some((LINE.trim_end_matches('.').to_string(), 5)),
        );

        members[4].functional_zones = vec![Zone::Graveyard];
        assert!(
            describe_structural_attached_subtype_base_pt_keyword_loss_bundle(&members).is_none()
        );
    }

    #[test]
    fn full_source_line_dispatch_restores_both_sentences() {
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition()),
            ["Enchant creature".to_string(), LINE.to_string()]
        );
    }
}

/// Recombine the five independently executable continuous pieces of an
/// attached 0/1 Turtle transformation. Each layer stays modeled separately;
/// the strict shared-filter and restriction checks only restore the authored
/// two-sentence surface (including its rules reminder).
fn describe_structural_attached_turtle_transform_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [
        types_ability,
        subtype_ability,
        pt_ability,
        attack_ability,
        loss_ability,
        ..,
    ] = abilities
    else {
        return None;
    };
    if [
        types_ability,
        subtype_ability,
        pt_ability,
        attack_ability,
        loss_ability,
    ]
    .iter()
    .any(|ability| ability.functional_zones.as_slice() != [Zone::Battlefield])
    {
        return None;
    }
    fn static_model(ability: &Ability) -> Option<&crate::static_abilities::CompiledStaticAbility> {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        static_ability.compiled_model()
    }
    let types = static_model(types_ability)?;
    let subtypes = static_model(subtype_ability)?;
    let pt = static_model(pt_ability)?;
    let attack = static_model(attack_ability)?;
    let loss = static_model(loss_ability)?;

    let ironsmith_core::StaticAbilityPayload::SetCardTypes {
        filter: types_filter,
        card_types,
    } = &types.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::SetCreatureSubtypes {
        filter: subtype_filter,
        subtypes,
    } = &subtypes.payload
    else {
        return None;
    };
    let pt_filter = match &pt.payload {
        ironsmith_core::StaticAbilityPayload::SetBasePowerToughness {
            filter,
            power: 0,
            toughness: 1,
        } => filter,
        ironsmith_core::StaticAbilityPayload::SetBasePowerToughnessValue {
            filter,
            power,
            toughness,
        } if power.unhinted() == &Value::Fixed(0) && toughness.unhinted() == &Value::Fixed(1) => {
            filter
        }
        _ => return None,
    };
    let ironsmith_core::StaticAbilityPayload::RemoveAllAbilities(loss_filter) = &loss.payload
    else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(grant) = &attack.payload else {
        return None;
    };
    if card_types.as_slice() != [CardType::Creature]
        || subtypes.as_slice() != [crate::types::Subtype::Turtle]
        || grant.condition.is_some()
        || !grant.additional_abilities.is_empty()
        || grant.ability.functional_zones.as_slice() != [Zone::Battlefield]
    {
        return None;
    }
    let ironsmith_core::AbilityKind::Static(granted_attack) = &grant.ability.kind else {
        return None;
    };
    let ironsmith_core::StaticAbilityPayload::RuleRestriction {
        restriction: crate::effect::Restriction::Attack(attack_filter),
        additional_restrictions,
        ..
    } = &granted_attack.payload
    else {
        return None;
    };
    if !additional_restrictions.is_empty() || attack_filter != &ObjectFilter::source() {
        return None;
    }

    let normalized = |filter: &ObjectFilter| {
        let mut filter = filter.clone();
        filter.set_explicit_card_type_noun(None);
        filter.source_surface = None;
        filter
    };
    let expected = ObjectFilter::creature().match_tagged(
        "enchanted",
        crate::target::TaggedOpbjectRelation::IsTaggedObject,
    );
    if normalized(types_filter) != expected
        || normalized(subtype_filter) != expected
        || normalized(pt_filter) != expected
        || normalized(loss_filter) != expected
    {
        return None;
    }

    Some((
        format!(
            "Enchanted creature is a Turtle with base power and toughness 0/1. It can't attack and loses all abilities. {STANDARD_REMINDER_OPEN_SENTINEL}It also loses all other creature types.{STANDARD_REMINDER_CLOSE_SENTINEL}"
        ),
        5,
    ))
}

#[cfg(test)]
mod attached_turtle_transform_bundle_tests {
    use super::*;

    const LINE: &str = "Enchanted creature is a Turtle with base power and toughness 0/1. It can't attack and loses all abilities. (It also loses all other creature types.)";

    fn bundle_members() -> Vec<Ability> {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Attached Turtle Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(LINE)
        .expect("attached Turtle line should parse");
        definition
            .abilities
            .into_iter()
            .filter(|ability| {
                matches!(
                    &ability.kind,
                    AbilityKind::Static(static_ability)
                        if matches!(
                            static_ability.id(),
                            crate::static_abilities::StaticAbilityId::SetCardTypes
                                | crate::static_abilities::StaticAbilityId::SetCreatureSubtypes
                                | crate::static_abilities::StaticAbilityId::SetBasePowerToughnessForFilter
                                | crate::static_abilities::StaticAbilityId::AttachedAbilityGrant
                                | crate::static_abilities::StaticAbilityId::RemoveAllAbilitiesForFilter
                        )
                )
            })
            .collect()
    }

    #[test]
    fn exact_five_piece_transform_rejoins_and_zone_near_miss_does_not() {
        let mut members = bundle_members();
        assert_eq!(members.len(), 5, "{members:#?}");
        let AbilityKind::Static(pt) = &members[2].kind else {
            panic!("base-P/T member should remain a typed static ability");
        };
        assert!(matches!(
            pt.compiled_model().map(|model| &model.payload),
            Some(ironsmith_core::StaticAbilityPayload::SetBasePowerToughnessValue {
                power,
                toughness,
                ..
            }) if power.unhinted() == &Value::Fixed(0)
                && toughness.unhinted() == &Value::Fixed(1)
        ));
        let (rendered, consumed) = describe_structural_attached_turtle_transform_bundle(&members)
            .expect("exact typed transform should compact");
        assert_eq!(consumed, 5);
        assert!(rendered.contains("Enchanted creature is a Turtle"));
        assert!(rendered.contains("It can't attack and loses all abilities"));

        members[4].functional_zones = vec![Zone::Graveyard];
        assert!(describe_structural_attached_turtle_transform_bundle(&members).is_none());
    }
}

/// Attached characteristic-setting lines lower into independent layer-correct
/// static abilities. Preserve the authored line on SetCardTypes and consume the
/// adjacent pieces only when their subjects prove they belong to that line.
fn describe_authored_attached_transform_bundle(abilities: &[Ability]) -> Option<(String, usize)> {
    let first_static = match &abilities.first()?.kind {
        AbilityKind::Static(static_ability) => static_ability,
        _ => return None,
    };
    let (set_idx, grant_tail) =
        if first_static.id() == crate::static_abilities::StaticAbilityId::AttachedAbilityGrant {
            let display = first_static.display();
            let (_, tail) = split_static_predicate(&display, " has ")?;
            (1usize, Some(tail.to_string()))
        } else {
            (0usize, None)
        };

    let AbilityKind::Static(set_types) = &abilities.get(set_idx)?.kind else {
        return None;
    };
    if set_types.id() != crate::static_abilities::StaticAbilityId::SetCardTypes {
        return None;
    }
    let surface = set_types.authored_line_surface()?;
    if let Some(grant_tail) = grant_tail {
        let surface_lower = surface.to_ascii_lowercase();
        let grant_lower = grant_tail.to_ascii_lowercase();
        if !surface_lower.contains(&grant_lower) {
            return None;
        }
    } else if surface.to_ascii_lowercase().contains(" with ") {
        return None;
    }

    let set_display = set_types.display();
    let (subject, _, _) = split_static_predicate_with_verb(&set_display, &[" is ", " are "])?;
    let mut consumed = set_idx + 1;
    while let Some(next) = abilities.get(consumed) {
        let Some(next_subject) = static_subject_for_attached_transform_piece(next) else {
            break;
        };
        if next_subject != subject {
            break;
        }
        consumed += 1;
    }

    Some((surface.trim().trim_end_matches('.').to_string(), consumed))
}

fn exact_enchanted_restriction_subject(filter: &ObjectFilter) -> Option<&'static str> {
    let tagged = |filter: ObjectFilter| {
        filter.match_tagged(
            "enchanted",
            crate::target::TaggedOpbjectRelation::IsTaggedObject,
        )
    };
    [
        ("Enchanted creature", tagged(ObjectFilter::creature())),
        (
            "Enchanted permanent",
            tagged(ObjectFilter::permanent_card().in_zone(Zone::Battlefield)),
        ),
        ("Enchanted artifact", tagged(ObjectFilter::artifact())),
    ]
    .into_iter()
    .find_map(|(subject, expected)| (filter == &expected).then_some(subject))
}

/// Recombine the independent layer-six ability removal and untap restriction
/// produced from one shared-subject attached-permanent clause. Both executable
/// models must identify the exact same enchanted object before their predicates
/// can share one rendered subject.
fn describe_structural_remove_all_abilities_untap_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [remove_ability, untap_ability, ..] = abilities else {
        return None;
    };
    if remove_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || untap_ability.functional_zones != remove_ability.functional_zones
    {
        return None;
    }

    let remove_filter = structural_static_effect_filter(
        remove_ability,
        crate::static_abilities::StaticAbilityId::RemoveAllAbilitiesForFilter,
    )?;
    let remove_display = static_display_with_id(
        remove_ability,
        crate::static_abilities::StaticAbilityId::RemoveAllAbilitiesForFilter,
    )?;

    let AbilityKind::Static(untap_static) = &untap_ability.kind else {
        return None;
    };
    let (untap_restriction, untap_display, untap_condition) =
        untap_static.rule_restriction_parts()?;
    let crate::effect::Restriction::Untap(untap_filter) = untap_restriction else {
        return None;
    };
    if untap_condition.is_some() || remove_filter != untap_filter {
        return None;
    }

    let subject = exact_enchanted_restriction_subject(remove_filter)?;
    let subject_lower = subject.to_ascii_lowercase();
    if !restriction_display_matches(
        &remove_display,
        &format!("{subject_lower} loses all abilities"),
    ) {
        return None;
    }

    let untap_surface = [
        "doesn't untap during its controller's untap step",
        "can't become untapped",
    ]
    .into_iter()
    .find(|surface| {
        restriction_display_matches(untap_display, &format!("{subject_lower} {surface}"))
    })?;

    Some((
        format!("{subject} loses all abilities and {untap_surface}"),
        2,
    ))
}

fn restriction_display_matches(display: &str, expected: &str) -> bool {
    display
        .trim()
        .trim_end_matches('.')
        .eq_ignore_ascii_case(expected)
}

/// Recombine adjacent block and attack-target restrictions that were authored
/// with one shared object subject. The two runtime restrictions remain
/// independent; this changes only their rendered sentence surface.
fn describe_structural_shared_subject_block_attack_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [block_ability, attack_ability, ..] = abilities else {
        return None;
    };
    if block_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || attack_ability.functional_zones != block_ability.functional_zones
    {
        return None;
    }

    let AbilityKind::Static(block_static) = &block_ability.kind else {
        return None;
    };
    let (block_restriction, block_display, block_condition) =
        block_static.rule_restriction_parts()?;
    let crate::effect::Restriction::Block(block_filter) = block_restriction else {
        return None;
    };
    if block_condition.is_some() {
        return None;
    }

    let AbilityKind::Static(attack_static) = &attack_ability.kind else {
        return None;
    };
    let (attack_restriction, attack_display, attack_condition) =
        attack_static.rule_restriction_parts()?;
    let crate::effect::Restriction::AttackPlayerOrPlaneswalkersControlledBy {
        attackers,
        player: PlayerFilter::You,
    } = attack_restriction
    else {
        return None;
    };
    if attack_condition.is_some() || attackers != block_filter {
        return None;
    }

    let block_display = block_display.trim().trim_end_matches('.');
    let marker = " can't block";
    let block_display_lower = block_display.to_ascii_lowercase();
    if !block_display_lower.ends_with(marker) {
        return None;
    }
    let subject = block_display[..block_display.len() - marker.len()].trim();
    if subject.is_empty()
        || !restriction_display_matches(
            attack_display,
            &format!("{subject} can't attack you or planeswalkers you control"),
        )
    {
        return None;
    }

    Some((
        format!(
            "{} can't block, and they can't attack you or planeswalkers you control",
            capitalize_first(subject)
        ),
        2,
    ))
}

/// Recombine the two adjacent typed restrictions produced from Oracle's
/// compound enchanted-object sentence. This intentionally accepts only the
/// exact no-exception restrictions for the exact same enchanted object filter.
fn describe_structural_enchanted_combat_activation_restriction_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [combat_ability, activation_ability, ..] = abilities else {
        return None;
    };
    if combat_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || activation_ability.functional_zones != combat_ability.functional_zones
    {
        return None;
    }

    let AbilityKind::Static(combat_static) = &combat_ability.kind else {
        return None;
    };
    let (combat_restriction, combat_display, combat_condition) =
        combat_static.rule_restriction_parts()?;
    if combat_condition.is_some() {
        return None;
    }
    let (combat_filter, combat_predicate, connector) = match combat_restriction {
        crate::effect::Restriction::AttackOrBlock(filter) => {
            (filter, "can't attack or block", ", and")
        }
        crate::effect::Restriction::Block(filter) => (filter, "can't block", ", and"),
        crate::effect::Restriction::Untap(filter) => (
            filter,
            "doesn't untap during its controller's untap step",
            " and",
        ),
        _ => return None,
    };

    let AbilityKind::Static(activation_static) = &activation_ability.kind else {
        return None;
    };
    let (activation_restriction, activation_display, activation_condition) =
        activation_static.rule_restriction_parts()?;
    if activation_condition.is_some() {
        return None;
    }
    let crate::effect::Restriction::ActivateAbilitiesOf(activation_filter) = activation_restriction
    else {
        return None;
    };
    if combat_filter != activation_filter {
        return None;
    }

    let subject = exact_enchanted_restriction_subject(combat_filter)?;
    let subject_lower = subject.to_ascii_lowercase();
    if !restriction_display_matches(
        combat_display,
        &format!("{subject_lower} {combat_predicate}"),
    ) || !restriction_display_matches(
        activation_display,
        &format!("{subject_lower} activated abilities can't be activated"),
    ) {
        return None;
    }

    let mut text = format!(
        "{subject} {combat_predicate}{connector} its activated abilities can't be activated"
    );
    let mut consumed = 2;
    if let Some(ignore_ability) = abilities.get(2)
        && ignore_ability.functional_zones.as_slice() == [Zone::Battlefield]
        && let AbilityKind::Static(ignore_static) = &ignore_ability.kind
        && ignore_static.id()
            == crate::static_abilities::StaticAbilityId::AttachedControllerMaySacrificePermanentToIgnoreSourceEffectUntilEndOfTurn
    {
        let attached_noun = subject
            .strip_prefix("Enchanted ")
            .map(str::to_ascii_lowercase)?;
        text.push_str(&format!(
            ". That {attached_noun}'s controller may sacrifice a permanent of their choice for that player to ignore this effect until end of turn"
        ));
        consumed = 3;
    }

    Some((text, consumed))
}

fn split_static_predicate<'a>(display: &'a str, marker: &str) -> Option<(&'a str, &'a str)> {
    let (subject, predicate) = display.split_once(marker)?;
    (!subject.trim().is_empty() && !predicate.trim().is_empty())
        .then_some((subject.trim(), predicate.trim()))
}

fn exact_enchanted_creature_anthem_filter(filter: &ObjectFilter) -> bool {
    let expected = ObjectFilter::creature().match_tagged(
        "enchanted",
        crate::target::TaggedOpbjectRelation::IsTaggedObject,
    );
    filter == &expected
}

/// Describe the simple characteristic predicates used by a mutually-exclusive
/// attached-creature anthem pair. Removing the one rendered characteristic
/// must leave the default filter, so this cannot silently omit extra facts.
fn describe_simple_attached_match(filter: &ObjectFilter) -> Option<String> {
    let mut normalized = filter.clone();
    if normalized.zone == Some(Zone::Battlefield) {
        normalized.zone = None;
    }
    let filter = &normalized;

    if let [subtype] = filter.subtypes.as_slice()
        && filter.controller == Some(PlayerFilter::You)
    {
        let mut remainder = filter.clone();
        remainder.subtypes.clear();
        remainder.controller = None;
        if remainder == ObjectFilter::default() {
            return Some(format!(
                "{} you control",
                with_indefinite_article(&subtype.to_string())
            ));
        }
    }

    if let [subtype] = filter.subtypes.as_slice() {
        let mut remainder = filter.clone();
        remainder.subtypes.clear();
        if remainder == ObjectFilter::default() {
            return Some(with_indefinite_article(&subtype.to_string()));
        }
    }

    if let [card_type] = filter.card_types.as_slice() {
        let mut remainder = filter.clone();
        remainder.card_types.clear();
        if remainder == ObjectFilter::default() {
            return Some(with_indefinite_article(card_type.name()));
        }
    }

    if let [supertype] = filter.supertypes.as_slice() {
        let mut remainder = filter.clone();
        remainder.supertypes.clear();
        if remainder == ObjectFilter::default() {
            return Some(supertype.name().to_ascii_lowercase());
        }
    }

    if let Some(colors) = filter.colors
        && colors.count() == 1
    {
        let mut remainder = filter.clone();
        remainder.colors = None;
        if remainder == ObjectFilter::default() {
            let color = crate::color::Color::ALL
                .into_iter()
                .find(|color| colors.contains(*color))?;
            return Some(color.name().to_string());
        }
    }

    if filter.attacking {
        let mut remainder = filter.clone();
        remainder.attacking = false;
        if remainder == ObjectFilter::default() {
            return Some("attacking".to_string());
        }
    }

    None
}

fn describe_simple_attached_match_for_subject(
    filter: &ObjectFilter,
    subject: &str,
) -> Option<String> {
    let expected_tag = match subject {
        "Enchanted creature" => "enchanted",
        "Equipped creature" => "equipped",
        _ => return None,
    };
    let mut normalized = filter.clone();
    if let [constraint] = normalized.tagged_constraints.as_slice() {
        if constraint.tag.as_str() != expected_tag
            || constraint.relation != crate::target::TaggedOpbjectRelation::IsTaggedObject
        {
            return None;
        }
        normalized.tagged_constraints.clear();
    }
    describe_simple_attached_match(&normalized)
}

fn attached_match_filter(condition: &Condition) -> Option<&ObjectFilter> {
    match condition {
        Condition::TargetMatches(filter) | Condition::AttachedToSourceMatches(filter) => {
            Some(filter)
        }
        _ => None,
    }
}

fn exact_attached_creature_subject(filter: &ObjectFilter) -> Option<&'static str> {
    let mut normalized = filter.clone();
    if normalized.zone == Some(Zone::Battlefield) {
        normalized.zone = None;
    }
    if normalized.card_types.as_slice() != [CardType::Creature]
        || normalized.tagged_constraints.len() != 1
    {
        return None;
    }
    let constraint = normalized.tagged_constraints[0].clone();
    if constraint.relation != crate::target::TaggedOpbjectRelation::IsTaggedObject {
        return None;
    }
    let subject = match constraint.tag.as_str() {
        "enchanted" => "Enchanted creature",
        "equipped" => "Equipped creature",
        _ => return None,
    };
    normalized.card_types.clear();
    normalized.tagged_constraints.clear();
    normalized.set_explicit_card_type_noun(None);
    (normalized == ObjectFilter::default()).then_some(subject)
}

/// Rejoin an attached-creature base anthem followed by independently gated
/// modifier and keyword sentences. The grammar carries the explicit attached
/// subject into both leading-condition continuations; this helper restores
/// their authored pronouns only when all executable members retain the same
/// attachment owner and each condition is a simple target characteristic.
/// A carried attached subject can share one condition across its extra stats
/// and granted keyword. Identity and condition equality keep all three
/// executable static abilities in scope.
fn describe_carried_attached_additional_grant(abilities: &[Ability]) -> Option<String> {
    let [base_ability, extra_ability, grant_ability] = abilities else {
        return None;
    };
    if abilities
        .iter()
        .any(|a| a.functional_zones.as_slice() != [Zone::Battlefield])
    {
        return None;
    }
    let AbilityKind::Static(base_static) = &base_ability.kind else {
        return None;
    };
    let AbilityKind::Static(extra_static) = &extra_ability.kind else {
        return None;
    };
    let base = base_static.anthem_payload()?;
    let extra = extra_static.anthem_payload()?;
    let filter = base.filter.as_ref()?;
    let subject = exact_attached_creature_subject(filter)?;
    let (grant_filter, grant_condition, granted) = modeled_object_static_grant(grant_ability)?;
    if extra.filter.as_ref() != Some(filter)
        || grant_filter != filter
        || base.condition.is_some()
        || extra.condition.is_none()
        || grant_condition != extra.condition.as_ref()
        || !extra.additional_surface
        || base.count_uses_where_x
        || extra.count_uses_where_x
        || base.replacement_surface.is_some()
        || extra.replacement_surface.is_some()
        || base.set_quantifier_surface.is_some()
        || extra.set_quantifier_surface.is_some()
        || !granted.is_keyword()
    {
        return None;
    }
    let (
        ironsmith_core::AnthemValue::Fixed(bp),
        ironsmith_core::AnthemValue::Fixed(bt),
        ironsmith_core::AnthemValue::Fixed(ep),
        ironsmith_core::AnthemValue::Fixed(et),
    ) = (&base.power, &base.toughness, &extra.power, &extra.toughness)
    else {
        return None;
    };
    Some(format!(
        "{subject} gets {}. It gets an additional {} and has {} as long as {}",
        describe_fixed_anthem_modifier(*bp, *bt),
        describe_fixed_anthem_modifier(*ep, *et),
        granted
            .display()
            .trim()
            .trim_end_matches('.')
            .to_ascii_lowercase(),
        describe_condition(extra.condition.as_ref()?)
    ))
}

fn describe_structural_attached_anthem_condition_chain_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [base_ability, additional_ability, keyword_ability, ..] = abilities else {
        return None;
    };
    if [base_ability, additional_ability, keyword_ability]
        .into_iter()
        .any(|ability| ability.functional_zones.as_slice() != [Zone::Battlefield])
    {
        return None;
    }
    let AbilityKind::Static(base_static) = &base_ability.kind else {
        return None;
    };
    let AbilityKind::Static(additional_static) = &additional_ability.kind else {
        return None;
    };
    let base = base_static.anthem_payload()?;
    let additional = additional_static.anthem_payload()?;
    let filter = base.filter.as_ref()?;
    let subject = exact_attached_creature_subject(filter)?;
    if additional.filter.as_ref() != Some(filter)
        || base.condition.is_some()
        || base.additional_surface
        || base.set_quantifier_surface.is_some()
        || base.count_uses_where_x
        || base.replacement_surface.is_some()
        || !additional.additional_surface
        || additional.set_quantifier_surface.is_some()
        || additional.count_uses_where_x
        || additional.replacement_surface.is_some()
    {
        return None;
    }
    let additional_match = attached_match_filter(additional.condition.as_ref()?)?;
    let additional_condition =
        describe_simple_attached_match_for_subject(additional_match, subject)?;

    let AbilityKind::Static(keyword_static) = &keyword_ability.kind else {
        return None;
    };
    let model = keyword_static.compiled_model()?;
    let ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(grant) = &model.payload else {
        return None;
    };
    if !grant.additional_abilities.is_empty()
        || grant.protection_does_not_remove_controlled_attachments
        || grant.ability.functional_zones.as_slice() != [Zone::Battlefield]
    {
        return None;
    }
    let keyword_match = attached_match_filter(grant.condition.as_ref()?)?;
    let keyword_condition = describe_simple_attached_match_for_subject(keyword_match, subject)?;
    let ironsmith_core::AbilityKind::Static(granted_model) = &grant.ability.kind else {
        return None;
    };
    let granted = crate::static_abilities::StaticAbility::from_model(granted_model.clone());
    if !granted.is_keyword() {
        return None;
    }
    let keyword = granted
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    if keyword.is_empty()
        || keyword.contains([',', '"'])
        || !grant
            .display
            .trim()
            .trim_end_matches('.')
            .eq_ignore_ascii_case(&format!("{subject} has {keyword}"))
    {
        return None;
    }

    let (
        ironsmith_core::AnthemValue::Fixed(base_power),
        ironsmith_core::AnthemValue::Fixed(base_toughness),
    ) = (&base.power, &base.toughness)
    else {
        return None;
    };
    let (
        ironsmith_core::AnthemValue::Fixed(additional_power),
        ironsmith_core::AnthemValue::Fixed(additional_toughness),
    ) = (&additional.power, &additional.toughness)
    else {
        return None;
    };
    Some((
        format!(
            "{subject} gets {}. As long as it's {additional_condition}, it gets an additional {}. As long as it's {keyword_condition}, it has {keyword}",
            describe_fixed_anthem_modifier(*base_power, *base_toughness),
            describe_fixed_anthem_modifier(*additional_power, *additional_toughness),
        ),
        3,
    ))
}

/// Rejoin a conditional attached anthem with an inverse `Otherwise` branch
/// that both sets the attached creature's base P/T and restricts which
/// creatures it can block. All three independently executable static
/// abilities must retain the same attached-object filter and the two fallback
/// abilities must carry the exact negation of the anthem condition.
fn describe_structural_attached_anthem_otherwise_base_block_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [anthem_ability, base_ability, block_ability, ..] = abilities else {
        return None;
    };
    if [anthem_ability, base_ability, block_ability]
        .into_iter()
        .any(|ability| ability.functional_zones.as_slice() != [Zone::Battlefield])
    {
        return None;
    }

    let AbilityKind::Static(anthem_static) = &anthem_ability.kind else {
        return None;
    };
    let anthem = anthem_static.anthem_payload()?;
    let target = anthem.filter.as_ref()?;
    let subject = exact_attached_creature_subject(target)?;
    if anthem.additional_surface
        || anthem.replacement_surface.is_some()
        || anthem.set_quantifier_surface.is_some()
        || anthem.count_uses_where_x
    {
        return None;
    }
    let match_filter = attached_match_filter(anthem.condition.as_ref()?)?;
    let predicate = describe_simple_attached_match_for_subject(match_filter, subject)?;
    let inverse = Condition::Not(Box::new(anthem.condition.clone()?));

    let AbilityKind::Static(base_static) = &base_ability.kind else {
        return None;
    };
    let (base_model, base_condition) = unwrapped_static_model(base_static)?;
    if base_condition != Some(&inverse) {
        return None;
    }
    let ironsmith_core::StaticAbilityPayload::SetBasePowerToughness {
        filter: base_filter,
        power,
        toughness,
    } = &base_model.payload
    else {
        return None;
    };
    if base_filter != target {
        return None;
    }

    let AbilityKind::Static(block_static) = &block_ability.kind else {
        return None;
    };
    let (block_model, block_condition) = unwrapped_static_model(block_static)?;
    if block_condition != Some(&inverse) {
        return None;
    }
    let ironsmith_core::StaticAbilityPayload::RuleRestriction {
        restriction: crate::effect::Restriction::BlockSpecificAttacker { blockers, attacker },
        additional_restrictions,
        display,
    } = &block_model.payload
    else {
        return None;
    };
    if blockers != target || !additional_restrictions.is_empty() {
        return None;
    }
    let [blocked_subtype] = attacker.subtypes.as_slice() else {
        return None;
    };
    let mut attacker_remainder = attacker.clone();
    attacker_remainder.subtypes.clear();
    attacker_remainder.set_plural_object_noun_surface(false);
    if attacker_remainder != ObjectFilter::default() {
        return None;
    }
    let blocked = super::render_effects::pluralize_word(&blocked_subtype.to_string());
    let expected_display = format!("{subject} can't block {blocked}");
    if !display
        .trim()
        .trim_end_matches('.')
        .eq_ignore_ascii_case(&expected_display)
    {
        return None;
    }

    let (
        ironsmith_core::AnthemValue::Fixed(anthem_power),
        ironsmith_core::AnthemValue::Fixed(anthem_toughness),
    ) = (&anthem.power, &anthem.toughness)
    else {
        return None;
    };
    Some((
        format!(
            "{subject} gets {} as long as it's {predicate}. Otherwise, it has base power and toughness {power}/{toughness} and can't block {blocked}",
            describe_fixed_anthem_modifier(*anthem_power, *anthem_toughness),
        ),
        3,
    ))
}

#[cfg(test)]
mod attached_anthem_otherwise_base_block_tests {
    use super::*;

    const LINE: &str = "Enchanted creature gets +2/+2 as long as it's a Detective you control. Otherwise, it has base power and toughness 1/1 and can't block Detectives.";

    fn definition() -> crate::CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Attached Otherwise Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .subtypes(vec![Subtype::Aura])
        .parse_text(format!("Enchant creature\n{LINE}"))
        .expect("attached otherwise program should compile")
    }

    #[test]
    fn exact_inverse_program_rejoins_the_otherwise_surface() {
        let definition = definition();
        assert_eq!(
            describe_structural_attached_anthem_otherwise_base_block_bundle(
                &definition.abilities[1..]
            ),
            Some((LINE.trim_end_matches('.').to_string(), 3))
        );
    }

    #[test]
    fn noninverse_block_condition_is_not_rejoined() {
        let definition = definition();
        let mut members = definition.abilities[1..].to_vec();
        let AbilityKind::Static(anthem_static) = &members[0].kind else {
            panic!("expected anthem");
        };
        let positive_condition = anthem_static
            .anthem_payload()
            .and_then(|anthem| anthem.condition.clone())
            .expect("positive anthem condition");
        let AbilityKind::Static(block_static) = &members[2].kind else {
            panic!("expected block restriction");
        };
        let mut block_model = block_static
            .compiled_model()
            .expect("block restriction model")
            .clone();
        let ironsmith_core::StaticAbilityPayload::Conditional { condition, .. } =
            &mut block_model.payload
        else {
            panic!("expected conditional block restriction");
        };
        *condition = positive_condition;
        members[2].kind = AbilityKind::Static(crate::static_abilities::StaticAbility::from_model(
            block_model,
        ));

        assert!(
            describe_structural_attached_anthem_otherwise_base_block_bundle(&members).is_none()
        );
    }
}

#[cfg(test)]
mod attached_anthem_condition_chain_tests {
    use super::*;

    const LINE: &str = "Equipped creature gets +1/+1. As long as it's legendary, it gets an additional +2/+2. As long as it's red, it has trample.";

    fn definition() -> crate::CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Attached Anthem Chain Probe",
        )
        .card_types(vec![CardType::Artifact])
        .subtypes(vec![Subtype::Equipment])
        .parse_text(format!("{LINE}\nEquip {{1}}"))
        .expect("attached anthem condition chain should compile")
    }

    #[test]
    fn public_surface_rejoins_exact_attached_condition_chain() {
        let definition = definition();
        let members = definition
            .abilities
            .iter()
            .filter(|ability| source_line_static_group_count(ability).is_none())
            .take(3)
            .cloned()
            .collect::<Vec<_>>();
        assert_eq!(
            describe_structural_attached_anthem_condition_chain_bundle(&members),
            Some((LINE.trim_end_matches('.').to_string(), 3)),
            "{members:#?}",
        );
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            [LINE.to_string(), "Equip {1}".to_string()]
        );
    }

    #[test]
    fn changed_attachment_filter_is_not_rejoined() {
        let definition = definition();
        let members = definition
            .abilities
            .iter()
            .filter(|ability| source_line_static_group_count(ability).is_none())
            .take(3)
            .cloned()
            .collect::<Vec<_>>();
        let mut changed = members.clone();
        let AbilityKind::Static(additional_static) = &changed[1].kind else {
            panic!("expected additional anthem");
        };
        let mut model = additional_static
            .compiled_model()
            .expect("anthem model")
            .clone();
        let ironsmith_core::StaticAbilityPayload::Anthem(anthem) = &mut model.payload else {
            panic!("expected anthem payload");
        };
        anthem.filter = Some(ObjectFilter::creature().match_tagged(
            "enchanted",
            crate::target::TaggedOpbjectRelation::IsTaggedObject,
        ));
        changed[1].kind =
            AbilityKind::Static(crate::static_abilities::StaticAbility::from_model(model));
        assert!(describe_structural_attached_anthem_condition_chain_bundle(&changed).is_none());
    }

    #[test]
    fn attached_controller_control_condition_keeps_affected_object_binding() {
        const DESTINY_LINE: &str =
            "Enchanted creature has shroud as long as its controller controls another creature.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Attached Controller Condition Probe",
        )
        .card_types(vec![CardType::Enchantment])
        .subtypes(vec![Subtype::Aura])
        .parse_text(format!("Enchant creature\n{DESTINY_LINE}"))
        .expect("attached-controller condition should compile");
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            ["Enchant creature".to_string(), DESTINY_LINE.to_string()]
        );

        let grant = definition.abilities.iter().find_map(|ability| {
            let AbilityKind::Static(static_ability) = &ability.kind else {
                return None;
            };
            let model = static_ability.compiled_model()?;
            let ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(grant) = &model.payload
            else {
                return None;
            };
            Some(grant)
        });
        let Some(Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::MatchingFilter(filter),
            comparison: crate::effect::Comparison::GreaterThanOrEqual(1),
            ..
        }) = grant.and_then(|grant| grant.condition.as_ref())
        else {
            panic!("expected a typed affected-controller count condition");
        };
        assert_eq!(
            filter.controller,
            Some(PlayerFilter::ControllerOf(crate::target::ObjectRef::Target))
        );
        assert!(filter.other);
    }
}

fn describe_fixed_anthem_modifier(power: i32, toughness: i32) -> String {
    let signed = |value: i32| {
        if value >= 0 {
            format!("+{value}")
        } else {
            value.to_string()
        }
    };
    let toughness = if power < 0 && toughness == 0 {
        "-0".to_string()
    } else {
        signed(toughness)
    };
    format!("{}/{toughness}", signed(power))
}

/// Recombine an attached-creature keyword grant and the inverse damage-
/// prevention rule produced from an authored `Otherwise` clause. The two
/// runtime grants stay independent so their conditions remain executable;
/// the source-line marker plus exact inverse predicates prove the shared
/// surface.
fn describe_source_line_attached_keyword_otherwise_prevention_group(
    abilities: &[Ability],
) -> Option<String> {
    let [keyword_ability, prevention_ability] = abilities else {
        return None;
    };
    if keyword_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || prevention_ability.functional_zones != keyword_ability.functional_zones
    {
        return None;
    }

    let grant = |ability: &Ability| {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            return None;
        };
        let model = static_ability.compiled_model()?;
        let ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(grant) = &model.payload
        else {
            return None;
        };
        if !grant.additional_abilities.is_empty()
            || grant.protection_does_not_remove_controlled_attachments
            || grant.ability.functional_zones.as_slice() != [Zone::Battlefield]
        {
            return None;
        }
        let ironsmith_core::AbilityKind::Static(granted) = &grant.ability.kind else {
            return None;
        };
        Some((
            grant.condition.clone(),
            grant.display.clone(),
            crate::static_abilities::StaticAbility::from_model(granted.clone()),
        ))
    };
    let (keyword_condition, keyword_display, keyword) = grant(keyword_ability)?;
    let (prevention_condition, prevention_display, prevention) = grant(prevention_ability)?;
    if !keyword.is_keyword()
        || prevention.id()
            != crate::static_abilities::StaticAbilityId::PreventAllDamageDealtByThisPermanent
    {
        return None;
    }

    let match_filter = attached_match_filter(keyword_condition.as_ref()?)?;
    let Some(Condition::Not(prevention_inner)) = prevention_condition.as_ref() else {
        return None;
    };
    if prevention_inner.as_ref() != keyword_condition.as_ref()? {
        return None;
    }
    if describe_simple_attached_match_for_subject(match_filter, "Enchanted creature").as_deref()
        != Some("an enchantment")
    {
        return None;
    }

    let keyword = keyword
        .display()
        .trim()
        .trim_end_matches('.')
        .to_ascii_lowercase();
    if keyword.is_empty()
        || keyword.contains([',', '"'])
        || keyword_display.trim().trim_end_matches('.')
            != format!("enchanted creature has {keyword}")
        || prevention_display.trim().trim_end_matches('.')
            != "prevent all damage that would be dealt by enchanted creature"
    {
        return None;
    }

    Some(format!(
        "Enchanted creature has {keyword} as long as it's an enchantment. Otherwise, prevent all damage that would be dealt by enchanted creature"
    ))
}

#[cfg(test)]
mod attached_keyword_otherwise_prevention_tests {
    use super::*;

    const LINE: &str = "Enchanted creature has double strike as long as it's an enchantment. Otherwise, prevent all damage that would be dealt by enchanted creature.";

    fn definition() -> crate::CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Armament of Nyx",
        )
        .card_types(vec![CardType::Enchantment])
        .subtypes(vec![Subtype::Aura])
        .parse_text(format!("Enchant creature\n{LINE}"))
        .expect("conditional attached prevention should compile")
    }

    #[test]
    fn public_surface_rejoins_exact_inverse_attached_grants() {
        let definition = definition();
        let members = definition
            .abilities
            .iter()
            .filter(|ability| source_line_static_group_count(ability).is_none())
            .take(2)
            .cloned()
            .collect::<Vec<_>>();
        assert_eq!(
            describe_source_line_attached_keyword_otherwise_prevention_group(&members).as_deref(),
            Some(LINE.trim_end_matches('.')),
            "{members:#?}",
        );
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            ["Enchant creature", LINE]
        );
        assert_eq!(
            describe_source_line_attached_keyword_otherwise_prevention_group(
                &definition.abilities[1..3]
            )
            .as_deref(),
            Some(LINE.trim_end_matches('.'))
        );
    }

    #[test]
    fn noninverse_prevention_condition_is_not_rejoined() {
        let definition = definition();
        let mut members = definition.abilities[1..3].to_vec();
        let AbilityKind::Static(keyword_static) = &members[0].kind else {
            panic!("expected keyword static grant");
        };
        let ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(keyword_grant) =
            &keyword_static
                .compiled_model()
                .expect("keyword model")
                .payload
        else {
            panic!("expected attached keyword grant");
        };
        let positive_condition = keyword_grant.condition.clone();

        let AbilityKind::Static(prevention_static) = &members[1].kind else {
            panic!("expected prevention static grant");
        };
        let mut prevention_model = prevention_static
            .compiled_model()
            .expect("prevention model")
            .clone();
        let ironsmith_core::StaticAbilityPayload::AttachedAbilityGrant(prevention_grant) =
            &mut prevention_model.payload
        else {
            panic!("expected attached prevention grant");
        };
        prevention_grant.condition = positive_condition;
        members[1].kind = AbilityKind::Static(crate::static_abilities::StaticAbility::from_model(
            prevention_model,
        ));

        assert!(
            describe_source_line_attached_keyword_otherwise_prevention_group(&members).is_none()
        );
    }
}

/// Recombine adjacent typed anthems produced from
/// "... as long as it's X. Otherwise, ...". Both branches must affect the
/// exact same enchanted creature, use fixed P/T-only payloads, and carry exact
/// inverse attachment predicates.
fn describe_structural_conditional_anthem_otherwise_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [positive_ability, negative_ability, ..] = abilities else {
        return None;
    };
    if positive_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || negative_ability.functional_zones != positive_ability.functional_zones
    {
        return None;
    }

    let AbilityKind::Static(positive_static) = &positive_ability.kind else {
        return None;
    };
    let AbilityKind::Static(negative_static) = &negative_ability.kind else {
        return None;
    };
    let positive = positive_static.anthem_payload()?;
    let negative = negative_static.anthem_payload()?;

    let target = positive.filter.as_ref()?;
    if !exact_enchanted_creature_anthem_filter(target)
        || negative.filter.as_ref() != Some(target)
        || positive.additional_surface
        || negative.additional_surface
        || positive.replacement_surface.is_some()
        || negative.replacement_surface.is_some()
        || positive.set_quantifier_surface.is_some()
        || negative.set_quantifier_surface.is_some()
        || positive.count_uses_where_x
        || negative.count_uses_where_x
    {
        return None;
    }

    let crate::ConditionExpr::AttachedToSourceMatches(match_filter) =
        positive.condition.as_ref()?
    else {
        return None;
    };
    let crate::ConditionExpr::Not(negative_inner) = negative.condition.as_ref()? else {
        return None;
    };
    if negative_inner.as_ref() != positive.condition.as_ref()? {
        return None;
    }

    let (
        ironsmith_core::AnthemValue::Fixed(positive_power),
        ironsmith_core::AnthemValue::Fixed(positive_toughness),
    ) = (&positive.power, &positive.toughness)
    else {
        return None;
    };
    let (
        ironsmith_core::AnthemValue::Fixed(negative_power),
        ironsmith_core::AnthemValue::Fixed(negative_toughness),
    ) = (&negative.power, &negative.toughness)
    else {
        return None;
    };
    if (*positive_power < 0 || *positive_toughness < 0)
        || (*positive_power == 0 && *positive_toughness == 0)
        || *negative_power >= 0
        || *negative_toughness > 0
    {
        return None;
    }

    let predicate = describe_simple_attached_match(match_filter)?;
    Some((
        format!(
            "Enchanted creature gets {} as long as it's {predicate}. Otherwise, it gets {}",
            describe_fixed_anthem_modifier(*positive_power, *positive_toughness),
            describe_fixed_anthem_modifier(*negative_power, *negative_toughness),
        ),
        2,
    ))
}

fn describe_structural_anthem_remove_all_abilities_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [anthem_ability, remove_abilities_ability, ..] = abilities else {
        return None;
    };
    if anthem_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || remove_abilities_ability.functional_zones != anthem_ability.functional_zones
    {
        return None;
    }
    let anthem_filter = structural_static_effect_filter(
        anthem_ability,
        crate::static_abilities::StaticAbilityId::Anthem,
    )?;
    let remove_filter = structural_static_effect_filter(
        remove_abilities_ability,
        crate::static_abilities::StaticAbilityId::RemoveAllAbilitiesForFilter,
    )?;
    if anthem_filter != remove_filter {
        return None;
    }
    let anthem = static_display_with_id(
        anthem_ability,
        crate::static_abilities::StaticAbilityId::Anthem,
    )?;
    let remove_abilities = static_display_with_id(
        remove_abilities_ability,
        crate::static_abilities::StaticAbilityId::RemoveAllAbilitiesForFilter,
    )?;
    let (subject, modifier) = split_static_predicate(&anthem, " gets ")?;
    let remove_subject = remove_abilities
        .strip_suffix(" loses all abilities")
        .or_else(|| remove_abilities.strip_suffix(" lose all abilities"))?
        .trim();
    if !static_bundle_subjects_match(subject, remove_subject) {
        return None;
    }

    Some((
        capitalize_first(&format!(
            "{subject} gets {modifier} and loses all abilities"
        )),
        2,
    ))
}

fn describe_structural_anthem_set_colors_bundle(abilities: &[Ability]) -> Option<(String, usize)> {
    let [anthem_ability, set_colors_ability, ..] = abilities else {
        return None;
    };
    if anthem_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || set_colors_ability.functional_zones != anthem_ability.functional_zones
    {
        return None;
    }
    let anthem_filter = structural_static_effect_filter(
        anthem_ability,
        crate::static_abilities::StaticAbilityId::Anthem,
    )?;
    let color_filter = structural_static_effect_filter(
        set_colors_ability,
        crate::static_abilities::StaticAbilityId::SetColors,
    )?;
    if anthem_filter != color_filter {
        return None;
    }

    let anthem = static_display_with_id(
        anthem_ability,
        crate::static_abilities::StaticAbilityId::Anthem,
    )?;
    let set_colors = static_display_with_id(
        set_colors_ability,
        crate::static_abilities::StaticAbilityId::SetColors,
    )?;
    let (subject, modifier, get_verb) =
        split_static_predicate_with_verb(&anthem, &[" gets ", " get "])?;
    let (color_subject, color, is_verb) =
        split_static_predicate_with_verb(&set_colors, &[" is ", " are "])?;
    if !static_bundle_subjects_match(subject, color_subject) {
        return None;
    }

    Some((
        capitalize_first(&format!(
            "{subject} {get_verb} {modifier} and {is_verb} {color}"
        )),
        2,
    ))
}

/// Preserve an authored sentence boundary when a P/T modifier is followed by
/// a separate characteristic-setting clause. The color and additive subtype
/// pieces belong together by layer semantics, while the preceding anthem does
/// not. All three typed pieces must affect the exact same object set.
fn describe_structural_anthem_then_color_subtype_bundle(
    abilities: &[Ability],
) -> Option<(Vec<String>, usize)> {
    let [anthem_ability, set_colors_ability, add_subtypes_ability, ..] = abilities else {
        return None;
    };
    if anthem_ability.functional_zones.as_slice() != [Zone::Battlefield]
        || set_colors_ability.functional_zones != anthem_ability.functional_zones
        || add_subtypes_ability.functional_zones != anthem_ability.functional_zones
    {
        return None;
    }
    let anthem_filter = structural_static_effect_filter(
        anthem_ability,
        crate::static_abilities::StaticAbilityId::Anthem,
    )?;
    let color_filter = structural_static_effect_filter(
        set_colors_ability,
        crate::static_abilities::StaticAbilityId::SetColors,
    )?;
    let subtype_filter = structural_static_effect_filter(
        add_subtypes_ability,
        crate::static_abilities::StaticAbilityId::AddSubtypes,
    )?;
    if anthem_filter != color_filter || anthem_filter != subtype_filter {
        return None;
    }

    let anthem = static_display_with_id(
        anthem_ability,
        crate::static_abilities::StaticAbilityId::Anthem,
    )?;
    let set_colors = static_display_with_id(
        set_colors_ability,
        crate::static_abilities::StaticAbilityId::SetColors,
    )?;
    let add_subtypes = static_display_with_id(
        add_subtypes_ability,
        crate::static_abilities::StaticAbilityId::AddSubtypes,
    )?;
    let (subject, modifier, get_verb) =
        split_static_predicate_with_verb(&anthem, &[" gets ", " get "])?;
    let (color_subject, color, is_verb) =
        split_static_predicate_with_verb(&set_colors, &[" is ", " are "])?;
    let (subtype_subject, subtype, subtype_verb) =
        split_static_predicate_with_verb(&add_subtypes, &[" is ", " are "])?;
    if !static_bundle_subjects_match(subject, color_subject)
        || !static_bundle_subjects_match(subject, subtype_subject)
    {
        return None;
    }

    Some((
        vec![
            capitalize_first(&format!("{subject} {get_verb} {modifier}")),
            capitalize_first(&format!(
                "{color_subject} {is_verb} {color} and {subtype_verb} {subtype}"
            )),
        ],
        3,
    ))
}

fn render_all_union_subject(subject: &str) -> String {
    let pieces = subject
        .split(" or ")
        .map(str::trim)
        .filter(|piece| !piece.is_empty())
        .collect::<Vec<_>>();
    if pieces.len() < 2 {
        return subject.to_string();
    }
    let rendered = pieces
        .iter()
        .map(|piece| {
            if piece.starts_with("all ") || piece.starts_with("All ") {
                piece.to_string()
            } else {
                format!("all {piece}")
            }
        })
        .collect::<Vec<_>>()
        .join(" and ");
    capitalize_first(&rendered)
}

fn singular_subtype_word(word: &str) -> String {
    let trimmed = word.trim_matches(|ch: char| !ch.is_ascii_alphabetic());
    if trimmed.eq_ignore_ascii_case("plains") {
        "Plains".to_string()
    } else if trimmed.ends_with('s') && trimmed.len() > 1 {
        trimmed.trim_end_matches('s').to_string()
    } else {
        trimmed.to_string()
    }
}

fn is_land_subtype_surface(word: &str) -> bool {
    matches!(
        word.to_ascii_lowercase().as_str(),
        "plains" | "island" | "swamp" | "mountain" | "forest"
    )
}

fn render_pt_color_type_addition_descriptor(
    color_text: &str,
    card_type_text: &str,
    subtype_text: &str,
    pt_text: &str,
) -> Option<String> {
    let card_type_words = card_type_text
        .split(" and ")
        .map(str::trim)
        .filter(|word| !word.is_empty())
        .collect::<Vec<_>>();
    let has_creatures = card_type_words
        .iter()
        .any(|word| word.eq_ignore_ascii_case("creatures"));
    let has_lands = card_type_words
        .iter()
        .any(|word| word.eq_ignore_ascii_case("lands"));

    let mut creature_subtypes = Vec::new();
    let mut land_subtypes = Vec::new();
    for word in subtype_text.split_whitespace() {
        let subtype = singular_subtype_word(word);
        if subtype.is_empty() {
            continue;
        }
        if is_land_subtype_surface(&subtype) {
            land_subtypes.push(subtype);
        } else {
            creature_subtypes.push(subtype);
        }
    }

    let mut type_phrases = Vec::new();
    if has_creatures {
        if creature_subtypes.is_empty() {
            type_phrases.push("creatures".to_string());
        } else {
            type_phrases.push(format!("{} creatures", creature_subtypes.join(" ")));
        }
    }
    if has_lands {
        if land_subtypes.is_empty() {
            type_phrases.push("lands".to_string());
        } else {
            type_phrases.push(format!("{} lands", land_subtypes.join(" ")));
        }
    }
    for card_type in card_type_words {
        if (card_type.eq_ignore_ascii_case("creatures") && has_creatures)
            || (card_type.eq_ignore_ascii_case("lands") && has_lands)
        {
            continue;
        }
        type_phrases.push(card_type.to_string());
    }
    if type_phrases.is_empty() {
        return None;
    }

    Some(format!(
        "{pt_text} {color_text} {}",
        type_phrases.join(" and ")
    ))
}

fn describe_structural_pt_color_type_addition_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [
        color_ability,
        card_type_ability,
        subtype_ability,
        pt_ability,
        ..,
    ] = abilities
    else {
        return None;
    };
    let color_display = static_display_with_id(
        color_ability,
        crate::static_abilities::StaticAbilityId::SetColors,
    )?;
    let card_type_display = static_display_with_id(
        card_type_ability,
        crate::static_abilities::StaticAbilityId::AddCardTypes,
    )?;
    let subtype_display = static_display_with_id(
        subtype_ability,
        crate::static_abilities::StaticAbilityId::AddSubtypes,
    )?;
    let pt_display = static_display_with_id(
        pt_ability,
        crate::static_abilities::StaticAbilityId::SetBasePowerToughnessForFilter,
    )?;

    let (subject, color_text) = split_static_predicate(&color_display, " are ")?;
    let (card_type_subject, card_type_tail) = split_static_predicate(&card_type_display, " are ")?;
    let (subtype_subject, subtype_tail) = split_static_predicate(&subtype_display, " are ")?;
    let (pt_subject, pt_text) =
        split_static_predicate(&pt_display, " have base power and toughness ")?;
    if card_type_subject != subject || subtype_subject != subject || pt_subject != subject {
        return None;
    }

    let card_type_text = card_type_tail.strip_suffix(" in addition to their other types")?;
    let subtype_text = subtype_tail.strip_suffix(" in addition to their other types")?;
    let descriptor = render_pt_color_type_addition_descriptor(
        color_text,
        card_type_text,
        subtype_text,
        pt_text,
    )?;
    Some((
        format!(
            "{} are {descriptor} in addition to their other types",
            render_all_union_subject(subject)
        ),
        4,
    ))
}

fn split_static_predicate_with_verb<'a>(
    display: &'a str,
    verbs: &[&str],
) -> Option<(&'a str, &'a str, &'static str)> {
    for verb in verbs {
        if let Some((subject, tail)) = split_static_predicate(display, verb) {
            let normalized = match *verb {
                " are " => "are",
                " is " => "is",
                " have " => "have",
                " has " => "has",
                " get " => "get",
                " gets " => "gets",
                _ => continue,
            };
            return Some((subject, tail, normalized));
        }
    }
    None
}

/// Recombine the structural pieces of effects such as "Enchanted permanent is
/// a colorless Forest land." The individual static abilities deliberately keep
/// card-type replacement, land-subtype replacement, and color replacement
/// separate so the layer system can apply them correctly; their oracle surface
/// is one predicate.
fn describe_structural_attached_land_type_setting_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [card_type_ability, subtype_ability, rest @ ..] = abilities else {
        return None;
    };
    let card_type_display = static_display_with_id(
        card_type_ability,
        crate::static_abilities::StaticAbilityId::SetCardTypes,
    )?;
    let subtype_display = static_display_with_id(
        subtype_ability,
        crate::static_abilities::StaticAbilityId::SetLandSubtypes,
    )?;
    let (subject, card_type_tail, type_verb) =
        split_static_predicate_with_verb(&card_type_display, &[" is ", " are "])?;
    let (subtype_subject, subtype_tail, subtype_verb) =
        split_static_predicate_with_verb(&subtype_display, &[" is ", " are "])?;
    if subtype_subject != subject || subtype_verb != type_verb {
        return None;
    }
    let expected_land_type = if type_verb == "is" { "land" } else { "lands" };
    if !card_type_tail.eq_ignore_ascii_case(expected_land_type) {
        return None;
    }

    let subtype_tail = subtype_tail
        .strip_prefix("an ")
        .or_else(|| subtype_tail.strip_prefix("a "))
        .unwrap_or(subtype_tail);
    let mut consumed = 2usize;
    let mut descriptor = subtype_tail.to_string();
    if let Some(color_ability) = rest.first()
        && let Some(color_display) = static_display_with_id(
            color_ability,
            crate::static_abilities::StaticAbilityId::MakeColorless,
        )
        && let Some((color_subject, color_tail, color_verb)) =
            split_static_predicate_with_verb(&color_display, &[" is ", " are "])
        && color_subject == subject
        && color_verb == type_verb
        && color_tail.eq_ignore_ascii_case("colorless")
    {
        descriptor = format!("colorless {descriptor}");
        consumed += 1;
    }

    let type_phrase = format!("{descriptor} {expected_land_type}");
    let type_phrase = if type_verb == "is" {
        with_indefinite_article(&type_phrase)
    } else {
        type_phrase
    };
    Some((format!("{subject} {type_verb} {type_phrase}"), consumed))
}

fn static_subject_is_land_kind(subject: &str) -> bool {
    let lower = subject.trim().to_ascii_lowercase();
    let lower = lower
        .strip_prefix("all ")
        .or_else(|| lower.strip_prefix("other "))
        .unwrap_or(lower.as_str());
    matches!(
        lower,
        "lands"
            | "forests"
            | "islands"
            | "mountains"
            | "plains"
            | "swamps"
            | "deserts"
            | "gates"
            | "lairs"
            | "locuses"
            | "loci"
            | "mines"
            | "power-plants"
            | "towers"
            | "urza's lands"
    )
}

fn describe_structural_type_base_pt_addition_bundle(
    abilities: &[Ability],
) -> Option<(String, usize)> {
    let [card_type_ability, pt_ability, ..] = abilities else {
        return None;
    };
    let card_type_display = static_display_with_id(
        card_type_ability,
        crate::static_abilities::StaticAbilityId::AddCardTypes,
    )?;
    let pt_display = static_display_with_id(
        pt_ability,
        crate::static_abilities::StaticAbilityId::SetBasePowerToughnessForFilter,
    )?;

    let (subject, card_type_tail, type_verb) =
        split_static_predicate_with_verb(&card_type_display, &[" are ", " is "])?;
    let (pt_subject, pt_tail, pt_verb) =
        split_static_predicate_with_verb(&pt_display, &[" have ", " has "])?;
    if pt_subject != subject {
        return None;
    }

    let (type_text, other_types) = card_type_tail
        .strip_suffix(" in addition to their other types")
        .map(|text| (text, "their"))
        .or_else(|| {
            card_type_tail
                .strip_suffix(" in addition to its other types")
                .map(|text| (text, "its"))
        })?;
    let base_prefix = if pt_tail.starts_with("base power and base toughness ") {
        "base power and base toughness "
    } else if pt_tail.starts_with("base power and toughness ") {
        "base power and toughness "
    } else {
        return None;
    };
    let pt_text = pt_tail.strip_prefix(base_prefix)?;
    if static_subject_is_land_kind(subject) {
        let still_land_tail = if type_verb == "is" {
            "that's still a land"
        } else {
            "that are still lands"
        };
        return Some((
            format!("{subject} {type_verb} {pt_text} {type_text} {still_land_tail}"),
            2,
        ));
    }
    Some((
        format!(
            "{subject} {type_verb} {type_text} in addition to {other_types} other types and {pt_verb} base power and base toughness {pt_text}"
        ),
        2,
    ))
}

fn describe_structural_station_keyword(ability: &Ability) -> Option<&'static str> {
    let AbilityKind::Activated(activated) = &ability.kind else {
        return None;
    };
    if !matches!(activated.timing, ActivationTiming::SorcerySpeed)
        || activated.activation_condition.is_some()
        || !activated.choices.is_empty()
        || !activated.mana_usage_restrictions.is_empty()
    {
        return None;
    }
    if !activated
        .additional_restrictions
        .iter()
        .any(|restriction| restriction.eq_ignore_ascii_case("Activate only as a sorcery"))
    {
        return None;
    }

    let costs = activated.mana_cost.as_all()?;
    if !costs.iter().any(station_cost_chooses_tap_cost_creature)
        || !costs.iter().any(station_cost_taps_chosen_creature)
    {
        return None;
    }

    let [effect] = activated.effects.flattened_default_effects() else {
        return None;
    };
    let put = effect.downcast_ref::<crate::effects::PutCountersEffect>()?;
    if put.counter_type != crate::CounterType::Charge
        || !matches!(put.target.unhinted(), ChooseSpec::Source)
    {
        return None;
    }
    let Value::PowerOf(source) = put.amount.unhinted() else {
        return None;
    };
    match source.unhinted() {
        ChooseSpec::Tagged(tag) if tag.as_str() == "tap_cost_0" => Some("Station"),
        _ => None,
    }
}

fn station_cost_chooses_tap_cost_creature(cost: &crate::costs::Cost) -> bool {
    let Some(effect) = cost.effect_ref() else {
        return false;
    };
    let Some(choose) = effect.downcast_ref::<crate::effects::ChooseObjectsEffect>() else {
        return false;
    };
    choose.tag.as_str() == "tap_cost_0"
        && choose.filter.controller == Some(PlayerFilter::You)
        && choose.filter.card_types.contains(&CardType::Creature)
        && choose.filter.other
        && choose.filter.untapped
}

fn station_cost_taps_chosen_creature(cost: &crate::costs::Cost) -> bool {
    let Some(effect) = cost.effect_ref() else {
        return false;
    };
    let Some(tap) = effect.downcast_ref::<crate::effects::TapEffect>() else {
        return false;
    };
    matches!(tap.target.unhinted(), ChooseSpec::Tagged(tag) if tag.as_str() == "tap_cost_0")
}

fn rewrite_additional_sacrifice_reference_surface(def: &CardDefinition, text: &str) -> String {
    if !has_additional_creature_sacrifice_cost(def) {
        return text.to_string();
    }

    let mut normalized = text
        .replace(
            "You draw X cards, where X is that creature's power",
            "Draw cards equal to the sacrificed creature's power",
        )
        .replace(
            "you draw X cards, where X is that creature's power",
            "draw cards equal to the sacrificed creature's power",
        )
        .replace("that creature's power", "the sacrificed creature's power")
        .replace(
            "that creature's toughness",
            "the sacrificed creature's toughness",
        )
        .replace(
            "that creature's mana value",
            "the sacrificed creature's mana value",
        )
        .replace(
            "the total power of those creatures",
            "the total power of the sacrificed creatures",
        )
        .replace(
            "the total toughness of those creatures",
            "the total toughness of the sacrificed creatures",
        );

    if !normalized.contains("sacrificed creature's") {
        normalized = normalized
            .replace("its power", "the sacrificed creature's power")
            .replace("its toughness", "the sacrificed creature's toughness")
            .replace("its mana value", "the sacrificed creature's mana value");
    }

    normalized
}

fn has_additional_creature_sacrifice_cost(def: &CardDefinition) -> bool {
    def.additional_non_mana_costs().iter().any(|cost| {
        let Some(effect) = cost.effect_ref() else {
            return false;
        };
        let effect = effect
            .downcast_ref::<crate::effects::WithIdEffect>()
            .map(|with_id| with_id.effect.as_ref())
            .unwrap_or(effect);
        if effect
            .downcast_ref::<crate::effects::ChooseObjectsEffect>()
            .is_some_and(|choose| {
                choose.tag.as_str().starts_with("sacrificed_")
                    && choose.filter.card_types.contains(&CardType::Creature)
            })
        {
            return true;
        }
        effect
            .downcast_ref::<crate::effects::zones::SacrificePlayerEffect>()
            .is_some_and(|sacrifice| sacrifice.filter.card_types.contains(&CardType::Creature))
    })
}

fn ability_is_this_spell_cost_modifier(ability: &Ability) -> bool {
    let AbilityKind::Static(static_ability) = &ability.kind else {
        return false;
    };
    static_ability.this_spell_cost_reduction().is_some()
        || static_ability
            .this_spell_cost_reduction_mana_cost()
            .is_some()
}

fn ability_precedes_spell_resolution(ability: &Ability) -> bool {
    if ability_is_this_spell_cost_modifier(ability) {
        return true;
    }
    if describe_conditional_spell_uncounterability(ability).is_some() {
        return true;
    }
    if let AbilityKind::Triggered(triggered) = &ability.kind
        && triggered.choices.is_empty()
        && triggered.intervening_if.is_none()
        && triggered
            .trigger
            .downcast_ref::<crate::triggers::YouCastThisSpellTrigger>()
            .is_some()
    {
        return true;
    }
    if let AbilityKind::Triggered(triggered) = &ability.kind
        && triggered.choices.is_empty()
        && triggered.intervening_if.is_none()
        && triggered
            .trigger
            .downcast_ref::<crate::triggers::ConditionQualifiedTrigger>()
            .is_some_and(|qualified| {
                qualified
                    .trigger
                    .downcast_ref::<crate::triggers::YouCastThisSpellTrigger>()
                    .is_some()
            })
    {
        return true;
    }
    if let AbilityKind::Triggered(triggered) = &ability.kind
        && triggered.choices.is_empty()
        && triggered.intervening_if.is_none()
        && triggered
            .trigger
            .downcast_ref::<crate::triggers::SpellCastTrigger>()
            .is_some_and(|trigger| {
                trigger.caster == PlayerFilter::You
                    && trigger.same_name_card_in_zone.is_none()
                    && trigger.mana_source_filter.is_none()
                    && trigger.timing.is_none()
                    && trigger.during_turn.is_none()
                    && trigger.min_spells_this_turn.is_none()
                    && trigger.exact_spells_this_turn.is_none()
                    && !trigger.count_all_spells_this_turn
                    && !trigger.from_not_hand
                    && !trigger.first_spell_of_game
                    && trigger.filter.as_ref().is_some_and(|filter| {
                        filter.source
                            && filter.source_surface
                                == Some(ironsmith_core::SourceReferenceSurface::ThisPermanentType(
                                    "this spell".to_string(),
                                ))
                    })
            })
    {
        return true;
    }
    matches!(
        &ability.kind,
        AbilityKind::Static(static_ability)
            if matches!(
                static_ability.id(),
                crate::static_abilities::StaticAbilityId::MakeColorless
                    | crate::static_abilities::StaticAbilityId::CantBeCountered
                    | crate::static_abilities::StaticAbilityId::Cascade
                    | crate::static_abilities::StaticAbilityId::Convoke
                    | crate::static_abilities::StaticAbilityId::DraftRuleText
                    | crate::static_abilities::StaticAbilityId::ThisSpellCastRestriction
            )
    )
}

fn is_choose_background_spell_effect(spell_effects: &crate::ResolutionProgram) -> bool {
    let [effect] = spell_effects.flattened_default_effects() else {
        return false;
    };
    let Some(choose) = effect.downcast_ref::<crate::effects::ChooseObjectsEffect>() else {
        return false;
    };

    choose.filter.zone == Some(Zone::Battlefield)
        && matches!(choose.filter.controller, None | Some(PlayerFilter::You))
        && choose.filter.card_types.is_empty()
        && choose.filter.subtypes == [Subtype::Background]
        && choose.filter.supertypes.is_empty()
        && choose.count == ChoiceCount::exactly(1)
        && choose.chooser == PlayerFilter::You
        && choose.zone == Some(Zone::Battlefield)
        && choose.additional_zones.is_empty()
        && !choose.is_search
        && !choose.reveal
        && !choose.top_only
        && !choose.replace_tagged_objects
}

#[cfg(test)]
mod counter_linked_cross_segment_rendering_tests {
    use super::*;

    fn granted_sacrifice_trigger() -> crate::ability::Ability {
        crate::ability::Ability::triggered(
            crate::triggers::Trigger::becomes_targeted(),
            vec![Effect::new(crate::effects::SacrificeTargetEffect::new(
                ChooseSpec::Source,
            ))],
        )
    }

    #[test]
    fn adjacent_counter_and_grant_segments_retain_the_typed_object_subject() {
        for (filter, object_kind) in [
            (ObjectFilter::land(), "land"),
            (ObjectFilter::creature(), "creature"),
        ] {
            let target_tag = TagKey::from("targeted_0");
            let put = Effect::new(crate::effects::PutCountersEffect::new(
                CounterType::Named("test".into()),
                Value::Fixed(1),
                ChooseSpec::target(ChooseSpec::Object(filter)),
            ))
            .tag(target_tag.clone());
            let granted_trigger = crate::ability::Ability::triggered(
                crate::triggers::Trigger::beginning_of_upkeep(PlayerFilter::You),
                vec![Effect::deal_damage(
                    Value::Fixed(1),
                    ChooseSpec::Player(PlayerFilter::You),
                )],
            );
            let grant = Effect::new(crate::effects::ApplyContinuousEffect::with_spec(
                ChooseSpec::Tagged(target_tag),
                crate::continuous::Modification::AddAbilityGeneric(granted_trigger),
                Until::ForAsLongAs(
                    ironsmith_core::ContinuousDurationPredicate::affected_object_has_counter(
                        CounterType::Named("test".into()),
                    ),
                ),
            ))
            .tag("granted_0");
            let program = crate::resolution::ResolutionProgram::new(vec![
                crate::resolution::ResolutionSegment::from_effects(vec![put]),
                crate::resolution::ResolutionSegment::from_effects(vec![grant]),
            ]);

            let rendered = describe_resolution_program(&program);
            assert!(
                rendered.contains(&format!(
                    "For as long as that {object_kind} has a test counter on it"
                )),
                "{rendered}"
            );
            assert!(
                rendered.contains(&format!("this {object_kind} deals 1 damage to you")),
                "{rendered}"
            );
            assert!(!rendered.contains("For as long as it has"), "{rendered}");
        }
    }

    #[test]
    fn returned_creature_entry_counter_and_linked_grant_retain_duration_scope() {
        let returned_tag = TagKey::from("returned_0");
        let returned = Effect::new(
            crate::effects::ReturnFromGraveyardToBattlefieldEffect::new(
                ChooseSpec::target(ChooseSpec::Object(
                    ObjectFilter::creature()
                        .owned_by(PlayerFilter::You)
                        .in_zone(Zone::Graveyard),
                )),
                false,
            )
            .with_entry_counter(
                ironsmith_core::BattlefieldEntryCounterSpec::new(
                    CounterType::Named("mannequin".into()),
                    Value::Fixed(1),
                    ironsmith_core::BattlefieldEntryCounterSurface::Inline,
                )
                .for_matching_object(ObjectFilter::creature()),
            ),
        )
        .tag(returned_tag.clone());
        let grant = Effect::new(crate::effects::ApplyContinuousEffect::with_spec(
            ChooseSpec::Tagged(returned_tag),
            crate::continuous::Modification::AddAbilityGeneric(granted_sacrifice_trigger()),
            Until::ForAsLongAs(
                ironsmith_core::ContinuousDurationPredicate::affected_object_has_counter(
                    CounterType::Named("mannequin".into()),
                ),
            ),
        ))
        .tag("granted_0");
        let program = crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![returned]),
            crate::resolution::ResolutionSegment::from_effects(vec![grant]),
        ]);

        let rendered = describe_resolution_program(&program);
        assert!(
            rendered.contains("For as long as that creature has a mannequin counter on it, it has"),
            "{rendered}"
        );
        assert!(!rendered.contains("For as long as it has"), "{rendered}");
    }
}

#[cfg(test)]
mod self_replacement_rendering_tests {
    use super::*;

    #[test]
    fn tagged_token_copy_count_replacement_refers_to_the_created_tokens() {
        let target = ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature()));
        let default = vec![
            Effect::new(crate::effects::CreateTokenCopyEffect::new(
                target.clone(),
                1,
                PlayerFilter::You,
            ))
            .tag("created_0"),
        ];
        let replacement = vec![
            Effect::new(crate::effects::CreateTokenCopyEffect::new(
                target,
                5,
                PlayerFilter::You,
            ))
            .tag("created_0"),
        ];

        assert_eq!(
            describe_same_token_count_self_replacement(
                &default,
                &replacement,
                "Create a token that's a copy of target creature",
                "this spell was kicked",
            )
            .as_deref(),
            Some(
                "Create a token that's a copy of target creature. If this spell was kicked, create five of those tokens instead"
            )
        );

        let mismatched = vec![
            Effect::new(crate::effects::CreateTokenCopyEffect::new(
                ChooseSpec::target(ChooseSpec::Object(ObjectFilter::artifact())),
                5,
                PlayerFilter::You,
            ))
            .tag("created_0"),
        ];
        assert!(
            describe_same_token_count_self_replacement(
                &default,
                &mismatched,
                "Create a token that's a copy of target creature",
                "this spell was kicked",
            )
            .is_none()
        );
    }

    fn two_recipient_landfall_damage_segment(
        replacement_creature_target: ChooseSpec,
    ) -> crate::resolution::ResolutionSegment {
        let creature_target = ChooseSpec::target(ChooseSpec::Object(
            ObjectFilter::creature().controlled_by(PlayerFilter::TargetPlayerOrControllerOfTarget),
        ));
        let damage_pair = |amount, second_target| {
            vec![Effect::new(crate::effects::SequenceEffect::coordinated(
                vec![
                    Effect::deal_damage(
                        Value::Fixed(amount),
                        ChooseSpec::PlayerOrPlaneswalker(PlayerFilter::Any),
                    ),
                    Effect::deal_damage(Value::Fixed(amount), second_target).tag("damaged_0"),
                ],
            ))]
        };
        let branch = crate::resolution::SelfReplacementBranch::new(
            Condition::PlayerHadLandEnterBattlefieldThisTurn {
                player: PlayerFilter::You,
            },
            damage_pair(3, replacement_creature_target),
        )
        .with_presentation_label(Some(crate::ability::PresentationLabel::from_ability_word(
            "Landfall",
        )))
        .with_starts_new_source_line(true);
        crate::resolution::ResolutionSegment {
            default_effects: damage_pair(1, creature_target),
            self_replacements: vec![branch],
            starts_new_source_line: false,
        }
    }

    #[test]
    fn landfall_two_recipient_damage_upgrade_requires_both_exact_targets() {
        let creature_target = ChooseSpec::target(ChooseSpec::Object(
            ObjectFilter::creature().controlled_by(PlayerFilter::TargetPlayerOrControllerOfTarget),
        ));
        let segment = two_recipient_landfall_damage_segment(creature_target);
        assert_eq!(
            describe_player_or_planeswalker_and_controlled_creature_damage_upgrade(&segment)
                .as_deref(),
            Some(
                "Deal 1 damage to target player or planeswalker and 1 damage to target creature that player or that planeswalker's controller controls. If you had a land enter the battlefield under your control this turn, deal 3 damage to that player or planeswalker and 3 damage to that creature instead"
            )
        );

        let wrong_second_target = ChooseSpec::target(ChooseSpec::Object(
            ObjectFilter::creature().controlled_by(PlayerFilter::You),
        ));
        let near_miss = two_recipient_landfall_damage_segment(wrong_second_target);
        assert_eq!(
            describe_player_or_planeswalker_and_controlled_creature_damage_upgrade(&near_miss),
            None,
            "a replacement aimed at a different creature cannot borrow the announced-target surface"
        );
    }

    fn damage_each_creature_and_player(amount: i32) -> Vec<Effect> {
        vec![
            Effect::for_each(
                ObjectFilter::creature(),
                vec![Effect::deal_damage(
                    Value::Fixed(amount),
                    ChooseSpec::Iterated,
                )],
            ),
            Effect::new(crate::effects::ForPlayersEffect::new(
                PlayerFilter::Any,
                vec![Effect::deal_damage(
                    Value::Fixed(amount),
                    ChooseSpec::Player(PlayerFilter::IteratedPlayer),
                )],
            )),
        ]
    }

    fn labeled_branch(
        label: &str,
        condition_after_replacement: bool,
    ) -> crate::resolution::SelfReplacementBranch {
        let mut branch = crate::resolution::SelfReplacementBranch::new(
            Condition::PlayerHasCardTypesInGraveyardOrMore {
                player: PlayerFilter::You,
                count: 4,
            },
            Vec::new(),
        )
        .with_presentation_label(Some(
            crate::ability::PresentationLabel::from_ability_word(label),
        ));
        branch.condition_after_replacement = condition_after_replacement;
        branch
    }

    fn searched_forest_partition_program() -> crate::resolution::ResolutionProgram {
        let searched = TagKey::from("searched_forests");
        let battlefield = TagKey::from("battlefield_forest");
        let search_filter = ObjectFilter::land()
            .with_supertype(Supertype::Basic)
            .with_subtype(Subtype::Forest)
            .in_zone(Zone::Library)
            .owned_by(PlayerFilter::You);
        let default_search = crate::effects::ChooseObjectsEffect::new(
            search_filter,
            ChoiceCount::up_to(2),
            PlayerFilter::You,
            searched.clone(),
        )
        .in_zone(Zone::Library)
        .as_optional_search();
        let choose_battlefield = crate::effects::ChooseObjectsEffect::new(
            ObjectFilter::tagged(searched.clone()).in_zone(Zone::Library),
            ChoiceCount::exactly(1),
            PlayerFilter::You,
            battlefield.clone(),
        )
        .in_zone(Zone::Library);
        let put_battlefield = crate::effects::ForEachTaggedEffect::new(
            battlefield.clone(),
            vec![Effect::new(
                crate::effects::PutOntoBattlefieldEffect::you_control(ChooseSpec::Iterated, true),
            )],
        );
        let put_remainder_in_hand = crate::effects::ForEachTaggedEffect::new(
            searched.clone(),
            vec![Effect::new(
                crate::effects::ConditionalEffect::new(
                    Condition::TaggedObjectMatches(
                        TagKey::from("__it__"),
                        ObjectFilter::default().same_stable_id_as_tagged(battlefield),
                    ),
                    Vec::new(),
                    vec![Effect::new(
                        crate::effects::MoveToZoneEffect::new(
                            ChooseSpec::Iterated,
                            Zone::Hand,
                            false,
                        )
                        .with_remainder_surface(ironsmith_core::LibraryRemainderSurface::Rest),
                    )],
                )
                .with_surface(ironsmith_core::ConditionalSurface::LeadingIf),
            )],
        );
        let default_pipeline = vec![
            Effect::new(default_search.clone()),
            Effect::new(crate::effects::RevealTaggedEffect::new(searched)),
            Effect::new(choose_battlefield),
            Effect::new(put_battlefield),
            Effect::new(put_remainder_in_hand),
        ];
        let mut replacement_search = default_search;
        replacement_search.count = ChoiceCount::up_to(3);
        let mut replacement_pipeline = default_pipeline.clone();
        replacement_pipeline[0] = Effect::new(replacement_search);
        let mut mastery_filter = ObjectFilter::default()
            .with_type(CardType::Instant)
            .with_type(CardType::Sorcery)
            .with_union_connective(crate::filter::ObjectFilterUnionConnective::AndOr)
            .in_zone(Zone::Graveyard)
            .owned_by(PlayerFilter::You);
        mastery_filter.set_explicit_card_noun(true);
        mastery_filter.set_explicit_card_type_noun(Some(CardType::Sorcery));
        let branch = crate::resolution::SelfReplacementBranch::new(
            Condition::ValueComparison {
                left: Value::Count(mastery_filter),
                operator: crate::effect::ValueComparisonOperator::GreaterThanOrEqual,
                right: Value::Fixed(2),
            },
            vec![Effect::new(crate::effects::SequenceEffect::coordinated(
                replacement_pipeline,
            ))],
        )
        .with_presentation_label(Some(
            crate::ability::PresentationLabel::from_ability_word("Spell mastery"),
        ));
        crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment {
                default_effects: vec![Effect::new(crate::effects::SequenceEffect::coordinated(
                    default_pipeline,
                ))],
                self_replacements: vec![branch],
                starts_new_source_line: false,
            },
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(
                crate::effects::SequenceEffect::sentence_leading_then(vec![Effect::new(
                    crate::effects::ShuffleLibraryEffect::new(PlayerFilter::You),
                )]),
            )]),
        ])
    }

    #[test]
    fn count_only_search_replacement_renders_shared_partition_and_shuffle_once() {
        let rendered = describe_search_partition_count_self_replacement_program(
            &searched_forest_partition_program(),
        )
        .expect("typed search partition should render");

        assert_eq!(
            rendered,
            "Search your library for up to two basic Forest cards, reveal those cards, and put one onto the battlefield tapped and the rest into your hand. Then shuffle. Spell mastery — If there are two or more instant and/or sorcery cards in your graveyard, search your library for up to three basic Forest cards instead of two"
        );
        assert_eq!(rendered.matches("reveal those cards").count(), 1);
        assert_eq!(rendered.matches("Then shuffle").count(), 1);
    }

    #[test]
    fn count_only_search_replacement_rejects_a_changed_shared_pipeline() {
        let mut program = searched_forest_partition_program();
        let replacement = program.segments[0].self_replacements[0].replacement_effects[0]
            .downcast_ref::<crate::effects::SequenceEffect>()
            .expect("replacement pipeline")
            .clone();
        let mut replacement = replacement;
        replacement.effects[1] = Effect::new(crate::effects::RevealTaggedEffect::new(
            "different_search_result",
        ));
        program.segments[0].self_replacements[0].replacement_effects =
            vec![Effect::new(replacement)];

        assert_eq!(
            describe_search_partition_count_self_replacement_program(&program),
            None,
            "the compactor must not hide a replacement that changes shared work"
        );
    }

    #[test]
    fn searched_partition_rejects_reversed_iterated_and_chosen_identity() {
        let program = searched_forest_partition_program();
        let sequence = program.segments[0].default_effects[0]
            .downcast_ref::<crate::effects::SequenceEffect>()
            .expect("default search pipeline");
        let search = sequence.effects[0]
            .downcast_ref::<crate::effects::ChooseObjectsEffect>()
            .expect("searched cards")
            .clone();
        let mut tail = sequence.effects[1..].to_vec();
        let mut remainder = tail[3]
            .downcast_ref::<crate::effects::ForEachTaggedEffect>()
            .expect("searched-card remainder")
            .clone();
        let mut conditional = remainder.effects[0]
            .downcast_ref::<crate::effects::ConditionalEffect>()
            .expect("chosen-card complement")
            .clone();
        conditional.condition = Condition::TaggedObjectMatches(
            TagKey::from("battlefield_forest"),
            ObjectFilter::default().same_stable_id_as_tagged(TagKey::from("__it__")),
        );
        remainder.effects[0] = Effect::new(conditional);
        tail[3] = Effect::new(remainder);

        assert!(
            exact_search_one_battlefield_rest_hand_tail(&tail, &search).is_none(),
            "the compactor must require the loop candidate to be compared with the chosen set"
        );
    }

    fn threshold_action(action: TargetThresholdAction, target: ChooseSpec, tag: &TagKey) -> Effect {
        match action {
            TargetThresholdAction::Destroy => {
                Effect::new(crate::effects::DestroyEffect::with_spec(target)).tag(tag.clone())
            }
            TargetThresholdAction::GainControl => Effect::new(
                crate::effects::ApplyContinuousEffect::with_spec_runtime(
                    target,
                    crate::effects::continuous::RuntimeModification::ChangeControllerToEffectController,
                    Until::Forever,
                ),
            )
            .tag(tag.clone()),
        }
    }

    fn threshold_condition(
        characteristic: TargetThresholdCharacteristic,
        tag: &TagKey,
        limit: Value,
    ) -> Condition {
        let target = Box::new(ChooseSpec::Tagged(tag.clone()));
        let left = match characteristic {
            TargetThresholdCharacteristic::Toughness => Value::ToughnessOf(target),
            TargetThresholdCharacteristic::ManaValue => Value::ManaValueOf(target),
        };
        Condition::ValueComparison {
            left,
            operator: crate::effect::ValueComparisonOperator::LessThanOrEqual,
            right: limit,
        }
    }

    fn trailing_threshold_effect(
        action: TargetThresholdAction,
        characteristic: TargetThresholdCharacteristic,
        target: ChooseSpec,
        tag: &TagKey,
        limit: Value,
    ) -> Effect {
        Effect::new(
            crate::effects::ConditionalEffect::new(
                threshold_condition(characteristic, tag, limit),
                vec![threshold_action(action, target, tag)],
                Vec::new(),
            )
            .with_surface(ironsmith_core::ConditionalSurface::TrailingIf),
        )
    }

    fn threshold_replacement_segment(
        action: TargetThresholdAction,
        characteristic: TargetThresholdCharacteristic,
        target: ChooseSpec,
        default_limit: Value,
        replacement_limit: Value,
        paid_condition: Condition,
        leading_instead: bool,
    ) -> crate::resolution::ResolutionSegment {
        let default_tag = TagKey::from("threshold_default");
        let replacement_tag = TagKey::from("threshold_replacement");
        let mut branch = crate::resolution::SelfReplacementBranch::new(
            paid_condition,
            vec![trailing_threshold_effect(
                action,
                characteristic,
                target.clone(),
                &replacement_tag,
                replacement_limit,
            )],
        );
        branch.leading_instead_surface = leading_instead;
        crate::resolution::ResolutionSegment {
            default_effects: vec![
                Effect::new(crate::effects::TargetOnlyEffect::new(target.clone())),
                trailing_threshold_effect(
                    action,
                    characteristic,
                    target,
                    &default_tag,
                    default_limit,
                ),
            ],
            self_replacements: vec![branch],
            starts_new_source_line: false,
        }
    }

    fn threshold_replacement_as_two_segments(
        mut segment: crate::resolution::ResolutionSegment,
    ) -> crate::resolution::ResolutionProgram {
        let branch = segment
            .self_replacements
            .pop()
            .expect("threshold replacement branch");
        let conditional = crate::effects::ConditionalEffect::new(
            branch.condition,
            branch.replacement_effects,
            Vec::new(),
        )
        .with_surface(ironsmith_core::ConditionalSurface::LeadingIf);
        crate::resolution::ResolutionProgram::new(vec![
            segment,
            crate::resolution::ResolutionSegment::from_effects(vec![Effect::new(conditional)]),
        ])
    }

    #[test]
    fn folds_one_creature_target_through_madness_toughness_thresholds() {
        let segment = threshold_replacement_segment(
            TargetThresholdAction::GainControl,
            TargetThresholdCharacteristic::Toughness,
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature())),
            Value::Fixed(2),
            Value::X,
            Condition::ThisSpellPaidLabel("Madness".into()),
            true,
        );

        assert_eq!(
            describe_target_threshold_paid_cost_self_replacement(&segment).as_deref(),
            Some(
                "Gain control of target creature if its toughness is 2 or less. If this spell's madness cost was paid, instead gain control of that creature if its toughness is X or less"
            )
        );
    }

    #[test]
    fn folds_cross_segment_madness_threshold_for_the_same_target() {
        let program = threshold_replacement_as_two_segments(threshold_replacement_segment(
            TargetThresholdAction::GainControl,
            TargetThresholdCharacteristic::Toughness,
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature())),
            Value::Fixed(2),
            Value::X,
            Condition::ThisSpellPaidLabel("Madness".into()),
            true,
        ));

        assert_eq!(
            describe_cross_segment_madness_target_threshold_replacement(&program).as_deref(),
            Some(
                "Gain control of target creature if its toughness is 2 or less. If this spell's madness cost was paid, instead gain control of that creature if its toughness is X or less"
            )
        );
    }

    #[test]
    fn cross_segment_madness_threshold_rejects_a_changed_target_or_cost_kind() {
        let segment = threshold_replacement_segment(
            TargetThresholdAction::GainControl,
            TargetThresholdCharacteristic::Toughness,
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature())),
            Value::Fixed(2),
            Value::X,
            Condition::ThisSpellPaidLabel("Madness".into()),
            true,
        );
        let mut changed_target = threshold_replacement_as_two_segments(segment.clone());
        let conditional = changed_target.segments[1].default_effects[0]
            .downcast_ref::<crate::effects::ConditionalEffect>()
            .expect("paid-cost conditional")
            .clone();
        let mut conditional = conditional;
        conditional.if_true = vec![trailing_threshold_effect(
            TargetThresholdAction::GainControl,
            TargetThresholdCharacteristic::Toughness,
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::artifact())),
            &TagKey::from("threshold_replacement"),
            Value::X,
        )];
        changed_target.segments[1].default_effects = vec![Effect::new(conditional)];
        assert_eq!(
            describe_cross_segment_madness_target_threshold_replacement(&changed_target),
            None,
            "a different replacement target must remain independently rendered",
        );

        let mut kicked = threshold_replacement_as_two_segments(segment);
        let conditional = kicked.segments[1].default_effects[0]
            .downcast_ref::<crate::effects::ConditionalEffect>()
            .expect("paid-cost conditional")
            .clone();
        let mut conditional = conditional;
        conditional.condition = Condition::ThisSpellWasKicked;
        kicked.segments[1].default_effects = vec![Effect::new(conditional)];
        assert_eq!(
            describe_cross_segment_madness_target_threshold_replacement(&kicked),
            None,
            "the cross-segment bridge is reserved for the grammar-proven Madness form",
        );
    }

    #[test]
    fn folds_one_artifact_target_through_kicked_mana_value_thresholds() {
        let segment = threshold_replacement_segment(
            TargetThresholdAction::Destroy,
            TargetThresholdCharacteristic::ManaValue,
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::artifact())),
            Value::Fixed(2),
            Value::Fixed(5),
            Condition::ThisSpellWasKicked,
            false,
        );

        assert_eq!(
            describe_target_threshold_paid_cost_self_replacement(&segment).as_deref(),
            Some(
                "Destroy target artifact if its mana value is 2 or less. If this spell was kicked, destroy that artifact if its mana value is 5 or less instead"
            )
        );
    }

    #[test]
    fn folds_repeated_target_and_filter_backed_replacement_threshold() {
        let target = ChooseSpec::target(ChooseSpec::Object(ObjectFilter::artifact()));
        let mut segment = threshold_replacement_segment(
            TargetThresholdAction::Destroy,
            TargetThresholdCharacteristic::ManaValue,
            target.clone(),
            Value::Fixed(2),
            Value::Fixed(5),
            Condition::TurnHistory(ironsmith_core::TurnHistoryCondition::SourceWasKicked {
                surface: crate::target::SourceReferenceSurface::ThisPermanentType(
                    "this spell".to_string(),
                ),
            }),
            false,
        );
        let replacement_tag = TagKey::from("threshold_replacement");
        let replacement = segment.self_replacements[0].replacement_effects[0]
            .downcast_ref::<crate::effects::ConditionalEffect>()
            .expect("replacement threshold")
            .clone();
        let mut replacement = replacement;
        let mut threshold_filter = ObjectFilter::default();
        threshold_filter.mana_value = Some(crate::filter::Comparison::LessThanOrEqual(5));
        replacement.condition = Condition::TaggedObjectMatches(replacement_tag, threshold_filter);
        segment.self_replacements[0].replacement_effects = vec![
            Effect::new(crate::effects::TargetOnlyEffect::new(target)),
            Effect::new(replacement),
        ];

        assert_eq!(
            describe_target_threshold_paid_cost_self_replacement(&segment).as_deref(),
            Some(
                "Destroy target artifact if its mana value is 2 or less. If this spell was kicked, destroy that artifact if its mana value is 5 or less instead"
            )
        );
    }

    #[test]
    fn threshold_replacement_does_not_fold_a_different_replacement_target() {
        let mut segment = threshold_replacement_segment(
            TargetThresholdAction::Destroy,
            TargetThresholdCharacteristic::ManaValue,
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::artifact())),
            Value::Fixed(2),
            Value::Fixed(5),
            Condition::ThisSpellWasKicked,
            false,
        );
        let replacement_tag = TagKey::from("threshold_replacement");
        segment.self_replacements[0].replacement_effects = vec![trailing_threshold_effect(
            TargetThresholdAction::Destroy,
            TargetThresholdCharacteristic::ManaValue,
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature())),
            &replacement_tag,
            Value::Fixed(5),
        )];

        assert_eq!(
            describe_target_threshold_paid_cost_self_replacement(&segment),
            None
        );
    }

    #[test]
    fn threshold_replacement_does_not_fold_an_unlinked_characteristic() {
        let mut segment = threshold_replacement_segment(
            TargetThresholdAction::GainControl,
            TargetThresholdCharacteristic::Toughness,
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature())),
            Value::Fixed(2),
            Value::X,
            Condition::ThisSpellPaidLabel("Madness".into()),
            true,
        );
        let replacement = segment.self_replacements[0].replacement_effects[0]
            .downcast_ref::<crate::effects::ConditionalEffect>()
            .expect("replacement threshold")
            .clone();
        let mut replacement = replacement;
        replacement.condition = Condition::ValueComparison {
            left: Value::ToughnessOf(Box::new(ChooseSpec::Tagged(TagKey::from(
                "unrelated_target",
            )))),
            operator: crate::effect::ValueComparisonOperator::LessThanOrEqual,
            right: Value::X,
        };
        segment.self_replacements[0].replacement_effects = vec![Effect::new(replacement)];

        assert_eq!(
            describe_target_threshold_paid_cost_self_replacement(&segment),
            None
        );
    }

    fn targeted_search_exile_shuffle_tail(count: usize, shuffle_player: PlayerFilter) -> Effect {
        let searched = TagKey::from("searched_cards");
        let owner = PlayerFilter::Target(Box::new(PlayerFilter::Any));
        let search = crate::effects::ChooseObjectsEffect::new(
            ObjectFilter::default()
                .in_zone(Zone::Library)
                .owned_by(owner),
            ChoiceCount::up_to(count),
            PlayerFilter::You,
            searched.clone(),
        )
        .in_zone(Zone::Library)
        .as_optional_search()
        .with_search_result_reference_surface(ironsmith_core::SearchResultReferenceSurface::It)
        .with_search_top_in_any_order_surface(false);
        Effect::new(crate::effects::SequenceEffect::new(vec![
            Effect::new(search),
            Effect::new(crate::effects::ForEachTaggedEffect::new(
                searched,
                vec![Effect::new(crate::effects::MoveToZoneEffect::new(
                    ChooseSpec::Iterated,
                    Zone::Exile,
                    false,
                ))],
            )),
            Effect::new(crate::effects::ShuffleLibraryEffect::new(shuffle_player)),
        ]))
    }

    fn targeted_search_count_replacement_segment(
        replacement_shuffle_player: PlayerFilter,
    ) -> crate::resolution::ResolutionSegment {
        let target = Effect::new(crate::effects::TargetOnlyEffect::new(ChooseSpec::target(
            ChooseSpec::Player(PlayerFilter::Target(Box::new(PlayerFilter::Any))),
        )));
        let default = Effect::new(crate::effects::SequenceEffect::comma_then(vec![
            target,
            targeted_search_exile_shuffle_tail(
                3,
                PlayerFilter::AliasedTarget(Box::new(PlayerFilter::Any)),
            ),
        ]));
        let branch = crate::resolution::SelfReplacementBranch::new(
            Condition::TurnHistory(ironsmith_core::TurnHistoryCondition::SourceWasKicked {
                surface: ironsmith_core::SourceReferenceSurface::ThisPermanentType(
                    "this spell".to_string(),
                ),
            }),
            vec![targeted_search_exile_shuffle_tail(
                15,
                replacement_shuffle_player,
            )],
        )
        .with_leading_instead_surface(true);
        crate::resolution::ResolutionSegment {
            default_effects: vec![default],
            self_replacements: vec![branch],
            starts_new_source_line: false,
        }
    }

    #[test]
    fn kicked_targeted_search_count_replacement_keeps_authored_punctuation() {
        let segment = targeted_search_count_replacement_segment(PlayerFilter::AliasedTarget(
            Box::new(PlayerFilter::Any),
        ));

        assert_eq!(
            describe_targeted_search_count_self_replacement(&segment).as_deref(),
            Some(
                "Search target player's library for up to three cards, exile them, then that player shuffles. If this spell was kicked, instead search that player's library for up to fifteen cards, exile them, then that player shuffles"
            )
        );
    }

    #[test]
    fn kicked_targeted_search_count_replacement_rejects_a_changed_shuffle_player() {
        let segment = targeted_search_count_replacement_segment(PlayerFilter::You);

        assert_eq!(
            describe_targeted_search_count_self_replacement(&segment),
            None,
            "the compact surface must not hide a replacement that shuffles a different library"
        );
    }

    #[test]
    fn morbid_discard_replacement_keeps_reveal_choice_discard_inline() {
        let target = ChooseSpec::Player(PlayerFilter::Any);
        let player = PlayerFilter::AliasedTarget(Box::new(PlayerFilter::Any));
        let chosen = TagKey::from("chosen_hand_cards");
        let look = Effect::new(crate::effects::LookAtHandEffect::reveal(
            ChooseSpec::Player(player.clone()),
        ));
        let choose = Effect::new(
            crate::effects::ChooseObjectsEffect::new(
                ObjectFilter::default()
                    .in_zone(Zone::Hand)
                    .owned_by(player.clone()),
                ChoiceCount::exactly(2),
                PlayerFilter::You,
                chosen.clone(),
            )
            .in_zone(Zone::Hand),
        );
        let chosen_filter = ObjectFilter::tagged(chosen.clone());
        let discard = Effect::new(crate::effects::DiscardEffect::new_with_filter(
            Value::Count(chosen_filter.clone()),
            player,
            false,
            Some(chosen_filter),
        ));
        let replacement = Effect::new(crate::effects::SequenceEffect::comma_then(vec![
            Effect::new(crate::effects::TargetOnlyEffect::new(target.clone())),
            look,
            choose,
            discard,
        ]));
        let branch = crate::resolution::SelfReplacementBranch::new(
            Condition::CreatureDiedThisTurn,
            vec![replacement],
        )
        .with_presentation_label(Some(
            crate::ability::PresentationLabel::from_ability_word("Morbid"),
        ));
        let segment = crate::resolution::ResolutionSegment {
            default_effects: vec![
                Effect::new(crate::effects::TargetOnlyEffect::new(target)),
                Effect::new(crate::effects::DiscardEffect::new(
                    Value::Fixed(2),
                    PlayerFilter::target_player(),
                    false,
                )),
            ],
            self_replacements: vec![branch],
            starts_new_source_line: false,
        };

        let rendered = describe_single_self_replacement_segment(&segment)
            .expect("typed hand replacement should render");
        let rendered =
            apply_self_replacement_presentation_label(&segment.self_replacements[0], rendered);
        assert_eq!(
            rendered,
            "Target player discards two cards. Morbid — If a creature died this turn, instead that player reveals their hand, you choose two cards from it, then that player discards those cards"
        );
    }

    #[test]
    fn conjoins_same_source_damage_in_default_and_replacement_branches() {
        assert_eq!(
            describe_conjoined_same_source_damage(&damage_each_creature_and_player(1)).as_deref(),
            Some("Deal 1 damage to each creature and each player")
        );

        let segment = crate::resolution::ResolutionSegment {
            default_effects: damage_each_creature_and_player(1),
            self_replacements: vec![crate::resolution::SelfReplacementBranch::new(
                Condition::PlayerControls {
                    player: PlayerFilter::You,
                    filter: ObjectFilter::default()
                        .you_control()
                        .with_supertype(Supertype::Snow)
                        .with_subtype(Subtype::Swamp),
                },
                damage_each_creature_and_player(2),
            )],
            starts_new_source_line: false,
        };
        let rendered = describe_single_self_replacement_segment(&segment)
            .expect("same-source damage replacement should render");
        assert_eq!(
            rendered,
            "Deal 1 damage to each creature and each player. If you control a snow Swamp, deal 2 damage to each creature and each player instead"
        );
        assert_eq!(
            rendered
                .matches("damage to each creature and each player")
                .count(),
            2,
            "{rendered}"
        );
        assert_eq!(rendered.matches(" instead").count(), 1, "{rendered}");
    }

    #[test]
    fn conjoins_distinct_amount_same_source_damage_groups_without_sequencing_them() {
        let mut attacking_filter = ObjectFilter::creature().in_zone(Zone::Battlefield);
        attacking_filter.attacking = true;
        let attacking_creatures = Effect::for_each(
            attacking_filter,
            vec![Effect::deal_damage(Value::Fixed(2), ChooseSpec::Iterated)],
        );
        let controller_and_creatures =
            Effect::new(crate::effects::SequenceEffect::coordinated(vec![
                Effect::deal_damage(Value::Fixed(1), ChooseSpec::SourceController),
                Effect::for_each(
                    ObjectFilter::creature()
                        .controlled_by(PlayerFilter::You)
                        .in_zone(Zone::Battlefield),
                    vec![Effect::deal_damage(Value::Fixed(1), ChooseSpec::Iterated)],
                ),
            ]));

        assert_eq!(
            describe_conjoined_same_source_damage(&[attacking_creatures, controller_and_creatures])
                .as_deref(),
            Some(
                "Deal 2 damage to each attacking creature and 1 damage to you and each creature you control"
            )
        );
    }

    #[test]
    fn conjoins_coordinated_mass_damage_in_self_replacement_branches() {
        let coordinated = |amount| {
            vec![Effect::new(crate::effects::SequenceEffect::coordinated(
                damage_each_creature_and_player(amount),
            ))]
        };
        let segment = crate::resolution::ResolutionSegment {
            default_effects: coordinated(1),
            self_replacements: vec![crate::resolution::SelfReplacementBranch::new(
                Condition::PlayerControls {
                    player: PlayerFilter::You,
                    filter: ObjectFilter::default()
                        .you_control()
                        .with_supertype(Supertype::Snow)
                        .with_subtype(Subtype::Swamp),
                },
                coordinated(2),
            )],
            starts_new_source_line: false,
        };

        assert_eq!(
            describe_single_self_replacement_segment(&segment).as_deref(),
            Some(
                "Deal 1 damage to each creature and each player. If you control a snow Swamp, deal 2 damage to each creature and each player instead"
            )
        );
    }

    #[test]
    fn trailing_damage_replacement_reuses_typed_source_and_target() {
        let target = ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature()));
        let replacement = Effect::new(crate::effects::ExecuteWithSourceEffect::new(
            ChooseSpec::Source,
            Effect::deal_damage(Value::Fixed(4), target.clone()),
        ));
        let mut branch = crate::resolution::SelfReplacementBranch::new(
            Condition::ThisSpellPaidLabel(crate::cost::OptionalCostKind::Additional.into()),
            vec![replacement],
        );
        branch.condition_after_replacement = true;
        let segment = crate::resolution::ResolutionSegment {
            default_effects: vec![Effect::deal_damage(Value::Fixed(2), target.clone())],
            self_replacements: vec![branch],
            starts_new_source_line: false,
        };

        assert_eq!(
            describe_single_self_replacement_segment(&segment).as_deref(),
            Some(
                "Deal 2 damage to target creature. It deals 4 damage to that creature instead if this spell's additional cost was paid"
            )
        );

        let mut mismatched_source = segment;
        mismatched_source.self_replacements[0].replacement_effects =
            vec![Effect::new(crate::effects::ExecuteWithSourceEffect::new(
                ChooseSpec::Tagged(TagKey::from("other_source")),
                Effect::deal_damage(Value::Fixed(4), target),
            ))];
        assert_eq!(
            describe_trailing_same_source_damage_self_replacement(&mismatched_source),
            None,
            "an unrelated explicit damage source must not collapse to the pronoun `It`"
        );
    }

    #[test]
    fn trailing_plural_any_target_damage_replacement_reuses_announced_set() {
        let target = ChooseSpec::WithCount(Box::new(ChooseSpec::AnyTarget), ChoiceCount::up_to(3));
        let mut branch = crate::resolution::SelfReplacementBranch::new(
            Condition::CountComparison {
                count: ironsmith_core::AnthemCountExpression::MatchingFilter(
                    ObjectFilter::default()
                        .in_zone(Zone::Graveyard)
                        .owned_by(PlayerFilter::You),
                ),
                comparison: Comparison::GreaterThanOrEqual(7),
                display: Some("there are seven or more cards in your graveyard".to_string()),
            },
            vec![Effect::deal_damage(Value::Fixed(4), target.clone())],
        );
        branch.condition_after_replacement = true;
        let segment = crate::resolution::ResolutionSegment {
            default_effects: vec![Effect::deal_damage(Value::Fixed(2), target.clone())],
            self_replacements: vec![branch],
            starts_new_source_line: false,
        };

        assert_eq!(
            describe_trailing_plural_any_target_damage_self_replacement(&segment).as_deref(),
            Some(
                "Deal 2 damage to each of up to three targets. It deals 4 damage to each of those permanents and/or players instead if there are seven or more cards in your graveyard"
            )
        );

        let mut changed_target = segment.clone();
        changed_target.self_replacements[0].replacement_effects = vec![Effect::deal_damage(
            Value::Fixed(4),
            ChooseSpec::WithCount(Box::new(ChooseSpec::AnyTarget), ChoiceCount::up_to(2)),
        )];
        assert_eq!(
            describe_trailing_plural_any_target_damage_self_replacement(&changed_target),
            None,
            "a changed target set must not borrow the prior-target surface"
        );

        let mut leading_condition = segment;
        leading_condition.self_replacements[0].condition_after_replacement = false;
        assert_eq!(
            describe_trailing_plural_any_target_damage_self_replacement(&leading_condition),
            None,
            "the trailing-condition surface must retain authored ordering"
        );
    }

    #[test]
    fn same_target_zone_sequence_uses_pronoun_then_card_referent() {
        let oracle = "{2}{W}: Return target permanent you control to its owner's hand. If it has unearth, instead exile it, then return that card to its owner's hand. Activate only during your turn.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Meticulous Excavation",
        )
        .card_types(vec![CardType::Enchantment])
        .parse_text(oracle)
        .expect("same-target sequential zone replacement should compile");
        let activated = definition
            .abilities
            .iter()
            .find_map(|ability| match &ability.kind {
                AbilityKind::Activated(activated) => Some(activated),
                _ => None,
            })
            .expect("Meticulous Excavation should have an activated ability");
        let segment = &activated.effects.segments[0];

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![oracle.to_string()]
        );

        let mut mismatched_condition = segment.clone();
        let mut filter = match &mismatched_condition.self_replacements[0].condition {
            Condition::TargetMatches(filter) => filter.clone(),
            condition => panic!("expected a target condition, found {condition:?}"),
        };
        filter.ability_markers = vec!["cycling".to_string()];
        mismatched_condition.self_replacements[0].condition = Condition::TargetMatches(filter);
        let mismatched_text = describe_single_self_replacement_segment(&mismatched_condition)
            .expect("the conservative fallback should still render");
        assert!(
            !mismatched_text.contains("If it has unearth"),
            "a changed target condition must not borrow the original surface: {mismatched_text}"
        );
        assert!(mismatched_text.contains("cycling"), "{mismatched_text}");
    }

    #[test]
    fn repeated_mass_damage_target_survives_amount_replacement() {
        let damage_each_creature = |amount| {
            vec![Effect::for_each(
                ObjectFilter::creature(),
                vec![Effect::deal_damage(
                    Value::Fixed(amount),
                    ChooseSpec::Iterated,
                )],
            )]
        };
        let segment = crate::resolution::ResolutionSegment {
            default_effects: damage_each_creature(1),
            self_replacements: vec![crate::resolution::SelfReplacementBranch::new(
                Condition::ThisSpellWasKicked,
                damage_each_creature(2),
            )],
            starts_new_source_line: false,
        };

        assert_eq!(
            describe_single_self_replacement_segment(&segment).as_deref(),
            Some(
                "Deal 1 damage to each creature. It deals 2 damage to each creature instead if this spell was kicked"
            )
        );
    }

    #[test]
    fn leading_kicked_damage_replacement_keeps_prevention_rider_branch_local() {
        let target = ChooseSpec::AnyTarget;
        let replacement = Effect::new(
            crate::effects::DealDamageEffect::new(Value::Fixed(10), target.clone())
                .with_unpreventable(true),
        );
        let branch = crate::resolution::SelfReplacementBranch::new(
            Condition::ThisSpellWasKicked,
            vec![replacement],
        )
        .with_leading_instead_surface(true);
        let segment = crate::resolution::ResolutionSegment {
            default_effects: vec![Effect::deal_damage(Value::Fixed(3), target)],
            self_replacements: vec![branch],
            starts_new_source_line: false,
        };

        assert_eq!(
            describe_shared_target_damage_self_replacement(&segment).as_deref(),
            Some(
                "Deal 3 damage to any target. If this spell was kicked, instead it deals 10 damage to that permanent or player and the damage can't be prevented"
            )
        );

        let mut unpreventable_default = segment.clone();
        unpreventable_default.default_effects = vec![Effect::new(
            crate::effects::DealDamageEffect::new(Value::Fixed(3), ChooseSpec::AnyTarget)
                .with_unpreventable(true),
        )];
        assert_eq!(
            describe_shared_target_damage_self_replacement(&unpreventable_default),
            None,
            "an independently unpreventable default action cannot borrow the replacement-only surface"
        );

        let mut mismatched_target = segment;
        mismatched_target.self_replacements[0].replacement_effects = vec![Effect::new(
            crate::effects::DealDamageEffect::new(
                Value::Fixed(10),
                ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature())),
            )
            .with_unpreventable(true),
        )];
        assert_eq!(
            describe_shared_target_damage_self_replacement(&mismatched_target),
            None,
            "unrelated damage targets must not collapse to the default target's anaphor"
        );
    }

    #[test]
    fn damaged_target_quality_conditions_reuse_the_original_target_referent() {
        for (default_text, condition, expected) in [
            (
                "Deal 2 damage to target creature",
                "a creature with toxic was dealt damage this way",
                "that creature has toxic",
            ),
            (
                "Deal 3 damage to target creature",
                "a permanent with a +1/+1 counter on it was dealt damage this way",
                "that creature has a +1/+1 counter on it",
            ),
            (
                "Deal 1 damage to target creature",
                "a white or blue permanent was dealt damage this way",
                "that creature is white or blue",
            ),
            (
                "Deal 3 damage to target creature",
                "that creature is a permanent with a +1/+1 counter on it",
                "that creature has a +1/+1 counter on it",
            ),
            (
                "Deal 1 damage to target creature",
                "that creature is a white or blue permanent",
                "that creature is white or blue",
            ),
            (
                "Deal 2 damage to target creature",
                "that creature is an artifact creature",
                "that creature is an artifact creature",
            ),
            (
                "Deal 2 damage to target creature",
                "an artifact creature was dealt damage this way",
                "it's an artifact creature",
            ),
            (
                "Deal 2 damage to target creature",
                "it's an artifact creature",
                "it's an artifact creature",
            ),
            (
                "Deal 5 damage to target creature or planeswalker",
                "a Spirit was dealt damage this way",
                "that permanent is a Spirit",
            ),
            (
                "Deal 2 damage to any target",
                "a creature was dealt damage this way",
                "it is a creature",
            ),
            (
                "Target creature gets +2/+2 until end of turn",
                "the target is a blocking permanent",
                "it's blocking",
            ),
            (
                "Target permanent gets +2/+2 until end of turn",
                "the target is a blocking permanent",
                "it's a blocking permanent",
            ),
        ] {
            assert_eq!(
                normalize_target_quality_condition(default_text, condition),
                expected,
                "{default_text}; {condition}"
            );
        }
    }

    #[test]
    fn named_spell_damage_replacements_keep_source_target_and_quality_surfaces() {
        for (name, oracle) in [
            (
                "Lightning Dart",
                "Lightning Dart deals 1 damage to target creature. If that creature is white or blue, Lightning Dart deals 4 damage to it instead.",
            ),
            (
                "Bring Low",
                "Bring Low deals 3 damage to target creature. If that creature has a +1/+1 counter on it, Bring Low deals 5 damage to it instead.",
            ),
            (
                "Electrostatic Bolt",
                "Electrostatic Bolt deals 2 damage to target creature. If it's an artifact creature, Electrostatic Bolt deals 4 damage to it instead.",
            ),
        ] {
            let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
                crate::ids::CardId::new(),
                name,
            )
            .card_types(vec![CardType::Instant])
            .parse_text(oracle)
            .expect("the typed damage self-replacement should compile");
            let raw_condition = definition
                .spell_effect
                .as_ref()
                .and_then(|program| program.segments.first())
                .and_then(|segment| segment.self_replacements.first())
                .map(|branch| super::normalize_common::describe_condition(&branch.condition));

            assert_eq!(
                crate::compiled_text::compiled_text_lines(&definition),
                vec![oracle.to_string()],
                "{name}; raw condition: {raw_condition:?}"
            );
        }
    }

    #[test]
    fn named_spell_damage_replacement_preserves_trailing_as_cast_condition() {
        let oracle = "Trail Check deals 2 damage to target attacking or blocking creature. Trail Check deals 4 damage to that creature instead if you controlled a Mount as you cast this spell.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Trail Check",
        )
        .card_types(vec![CardType::Instant])
        .parse_text(oracle)
        .expect("the typed as-cast damage replacement should compile");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![oracle.to_string()]
        );
    }

    #[test]
    fn player_or_planeswalker_damage_keeps_controller_antecedent_noun() {
        let oracle = "Mind Bolt deals 3 damage to target player or planeswalker. That player or that planeswalker's controller discards two cards.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Mind Bolt",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(oracle)
        .expect("the typed damage-discard sequence should compile");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![oracle.to_string()]
        );

        let mut changed = definition
            .spell_effect
            .clone()
            .expect("the sorcery should have a resolution program");
        let mut discard = changed.segments[1].default_effects[0]
            .downcast_ref::<crate::effects::DiscardEffect>()
            .expect("the second segment should discard")
            .clone();
        discard.player = PlayerFilter::Opponent;
        changed.segments[1].default_effects[0] = Effect::new(discard);
        assert!(
            describe_player_or_planeswalker_damage_then_controller_discard_program(&changed)
                .is_none()
        );
    }

    #[test]
    fn optional_zone_replacement_reuses_its_typed_target() {
        let oracle = "Put target creature on top of its owner's library. If that creature is red, you may put it on the bottom of its owner's library instead.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Ether Well Variant",
        )
        .card_types(vec![CardType::Instant])
        .parse_text(oracle)
        .expect("the optional same-target zone replacement should compile");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![oracle.to_string()]
        );
    }

    #[test]
    fn named_spell_damage_replacement_rejects_mismatched_target_or_source() {
        let oracle = "Shape Probe deals 1 damage to target creature. If that creature is white, Shape Probe deals 4 damage to it instead.";
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Shape Probe",
        )
        .card_types(vec![CardType::Instant])
        .parse_text(oracle)
        .expect("the typed damage self-replacement should compile");
        let mut program = definition
            .spell_effect
            .clone()
            .expect("the instant should have a spell-resolution program");
        let target = ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature()));

        program.segments[0].self_replacements[0].replacement_effects = vec![Effect::deal_damage(
            Value::Fixed(4),
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::artifact())),
        )];
        assert_eq!(
            describe_named_spell_shared_target_damage_self_replacement(&definition, &program),
            None
        );

        program.segments[0].self_replacements[0].replacement_effects =
            vec![Effect::new(crate::effects::ExecuteWithSourceEffect::new(
                ChooseSpec::Tagged(TagKey::from("other_source")),
                Effect::deal_damage(Value::Fixed(4), target),
            ))];
        assert_eq!(
            describe_named_spell_shared_target_damage_self_replacement(&definition, &program),
            None
        );
    }

    #[test]
    fn tagged_multi_target_destroy_replacement_keeps_each_creature_referent() {
        let damaged = TagKey::from("damaged_0");
        let default = Effect::deal_damage(
            Value::Fixed(2),
            ChooseSpec::target(ChooseSpec::Object(ObjectFilter::creature()))
                .with_count(ChoiceCount::exactly(2)),
        )
        .tag(damaged.clone());
        let destroy_filter = ObjectFilter::creature().match_tagged(
            damaged,
            crate::filter::TaggedOpbjectRelation::IsTaggedObject,
        );
        let replacement = Effect::destroy(ChooseSpec::Object(destroy_filter)).tag("destroyed_0");
        let segment = crate::resolution::ResolutionSegment {
            default_effects: vec![default],
            self_replacements: vec![crate::resolution::SelfReplacementBranch::new(
                Condition::Not(Box::new(Condition::CardsInHandOrMore(1))),
                vec![replacement],
            )],
            starts_new_source_line: false,
        };

        assert_eq!(
            describe_single_self_replacement_segment(&segment).as_deref(),
            Some(
                "Deal 2 damage to two target creatures. Destroy each creature instead if you have no cards in hand"
            )
        );
    }

    #[test]
    fn preserves_multi_sentence_replacement_boundaries() {
        let rendered = format_self_replacement_fallback(
            "Destroy all enchantments",
            "there are seven or more cards in your graveyard",
            "destroy all enchantments. Return all cards destroyed this way to the battlefield",
            false,
        );
        assert_eq!(
            rendered,
            "Destroy all enchantments. If there are seven or more cards in your graveyard, instead destroy all enchantments. Return all cards destroyed this way to the battlefield"
        );
        assert!(!rendered.contains("and return"));
    }

    #[test]
    fn preserves_authored_leading_instead_on_one_coordinated_clause() {
        let rendered = format_self_replacement_fallback(
            "Draw X cards",
            "X is 10 or more",
            "shuffle your graveyard into your library, draw X cards, and untap five lands",
            true,
        );
        assert_eq!(
            rendered,
            "Draw X cards. If X is 10 or more, instead shuffle your graveyard into your library, draw X cards, and untap five lands"
        );
    }

    #[test]
    fn labeled_replacement_preserves_leading_condition_surface() {
        let rendered = apply_self_replacement_presentation_label(
            &labeled_branch("Landfall", false),
            "Default action. If a land entered this turn, replacement action instead".to_string(),
        );
        assert_eq!(
            rendered,
            "Default action. Landfall — If a land entered this turn, replacement action instead"
        );
    }

    #[test]
    fn addendum_scry_then_same_draw_keeps_leading_instead_and_imperative_draw() {
        let mut branch = crate::resolution::SelfReplacementBranch::new(
            Condition::ThisSpellPaidLabel("CastDuringYourMainPhase".into()),
            vec![Effect::scry(Value::Fixed(3)), Effect::draw(Value::Fixed(3))],
        )
        .with_presentation_label(Some(
            crate::ability::PresentationLabel::from_ability_word("Addendum"),
        ));
        branch.condition_after_replacement = false;
        let segment = crate::resolution::ResolutionSegment {
            default_effects: vec![Effect::draw(Value::Fixed(3))],
            self_replacements: vec![branch],
            starts_new_source_line: false,
        };

        let rendered = describe_single_self_replacement_segment(&segment)
            .expect("typed scry/draw replacement should render");
        let rendered =
            apply_self_replacement_presentation_label(&segment.self_replacements[0], rendered);
        assert_eq!(
            rendered,
            "Draw three cards. Addendum — If you cast this spell during your main phase, instead scry 3, then draw three cards"
        );

        let mut mismatched = segment.clone();
        mismatched.self_replacements[0].replacement_effects[1] = Effect::draw(Value::Fixed(2));
        assert_eq!(
            describe_scry_then_same_draw_self_replacement(&mismatched),
            None,
            "a replacement that changes the draw count must not use the same-draw surface"
        );
    }

    #[test]
    fn labeled_replacement_preserves_trailing_condition_surface() {
        let rendered = apply_self_replacement_presentation_label(
            &labeled_branch("Metalcraft", true),
            "Default action. If you control three artifacts, that player sacrifices two creatures instead"
                .to_string(),
        );
        assert_eq!(
            rendered,
            "Default action. Metalcraft — That player sacrifices two creatures instead if you control three artifacts"
        );

        let damage = apply_self_replacement_presentation_label(
            &labeled_branch("Morbid", true),
            "Deal 3 damage to any target. It deals 5 damage instead if a creature died this turn"
                .to_string(),
        );
        assert_eq!(
            damage,
            "Deal 3 damage to any target. Morbid — It deals 5 damage instead if a creature died this turn"
        );
    }
}

#[cfg(test)]
mod keyword_surface_merge_tests {
    use super::*;

    #[test]
    fn numbered_intrinsic_keyword_abilities_preserve_ast_boundaries() {
        let merged = merge_adjacent_keyword_surface_lines(vec![
            "Keyword ability 0: Flying".to_string(),
            "Keyword ability 1: Menace".to_string(),
        ]);

        assert_eq!(
            merged,
            vec![
                "Keyword ability 0: Flying".to_string(),
                "Keyword ability 1: Menace".to_string(),
            ]
        );
    }

    #[test]
    fn authored_keyword_group_is_one_source_line_without_invented_punctuation() {
        let merged = merge_adjacent_keyword_surface_lines(vec![
            source_line_keyword_group_sentinel(2),
            "Flying".to_string(),
            "Vigilance".to_string(),
        ]);

        assert_eq!(merged, vec!["Flying, vigilance".to_string()]);
    }

    #[test]
    fn authored_repeated_cascade_group_compacts_but_separate_lines_do_not() {
        let first = "Static ability 1: Cascade".to_string();
        let second = "Static ability 2: Cascade".to_string();
        assert_eq!(
            merge_adjacent_keyword_surface_lines(vec![
                source_line_keyword_group_sentinel(2),
                first.clone(),
                second.clone(),
            ]),
            vec!["Static ability 1: Cascade, cascade".to_string()],
        );
        assert_eq!(
            merge_adjacent_keyword_surface_lines(vec![first.clone(), second.clone()]),
            vec![first, second],
            "independent Cascade source lines must not acquire grouped punctuation",
        );

        let marker = Ability::static_ability(crate::static_abilities::StaticAbility::from_model(
            ironsmith_core::StaticAbility::source_line_keyword_group(2),
        ))
        .in_zones(Vec::new());
        let cascade = || {
            Ability::static_ability(crate::static_abilities::StaticAbility::cascade())
                .in_zones(vec![Zone::Stack])
        };
        let exact = [marker.clone(), cascade(), cascade()];
        assert_eq!(source_line_repeated_cascade_group_consumed(&exact), Some(3));

        let mut changed_zone = exact.clone();
        changed_zone[2].functional_zones = vec![Zone::Battlefield];
        assert_eq!(
            source_line_repeated_cascade_group_consumed(&changed_zone),
            None,
            "the leading spell-line compactor must require two intrinsic stack abilities",
        );
        assert_eq!(
            source_line_repeated_cascade_group_consumed(&exact[1..]),
            None,
            "adjacent Cascade abilities without the authored line marker stay independent",
        );
    }

    #[test]
    fn daybound_card_keeps_each_modeled_keyword_on_its_own_oracle_line() {
        let definition = crate::cards::builders::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Daybound Surface Probe",
        )
        .card_types(vec![CardType::Creature])
        .reach()
        .daybound()
        .build();

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec!["Reach".to_string(), "Daybound".to_string()]
        );
    }

    #[test]
    fn parameterized_keyword_marker_stays_separate_from_intrinsic_keyword() {
        let merged = merge_adjacent_keyword_surface_lines(vec![
            "Static ability 0: Mutate {4}{B}".to_string(),
            "Static ability 1: flying".to_string(),
        ]);

        assert_eq!(
            merged,
            vec![
                "Static ability 0: Mutate {4}{B}".to_string(),
                "Static ability 1: flying".to_string(),
            ]
        );
    }

    #[test]
    fn partial_color_grant_run_keeps_authored_source_lines_separate() {
        let lines = vec![
            "Static ability 0: White creatures you control have protection from black".to_string(),
            "Static ability 1: Black creatures you control have protection from white".to_string(),
        ];

        assert_eq!(merge_adjacent_keyword_surface_lines(lines.clone()), lines);
    }

    #[test]
    fn full_wubrg_grant_run_keeps_the_conditional_compaction() {
        let merged = merge_adjacent_keyword_surface_lines(vec![
            "Static ability 0: White creatures you control have vigilance".to_string(),
            "Static ability 1: Blue creatures you control have hexproof".to_string(),
            "Static ability 2: Black creatures you control have lifelink".to_string(),
            "Static ability 3: Red creatures you control have first strike".to_string(),
            "Static ability 4: Green creatures you control have trample".to_string(),
        ]);

        assert_eq!(
            merged,
            vec![
                "Static ability 0: Each creature you control has vigilance if it's white, hexproof if it's blue, lifelink if it's black, first strike if it's red, and trample if it's green"
                    .to_string(),
            ]
        );
    }

    #[test]
    fn annihilator_stays_on_its_own_keyword_line() {
        let merged = merge_adjacent_keyword_surface_lines(vec![
            "Keyword ability 0: Annihilator 2".to_string(),
            "Keyword ability 1: trample".to_string(),
        ]);

        assert_eq!(
            merged,
            vec![
                "Keyword ability 0: Annihilator 2".to_string(),
                "Keyword ability 1: trample".to_string(),
            ]
        );
    }

    #[test]
    fn trailing_annihilator_preserves_its_ast_boundary() {
        let merged = merge_adjacent_keyword_surface_lines(vec![
            "Keyword ability 0: Trample".to_string(),
            "Static ability 1: Annihilator 1".to_string(),
        ]);

        assert_eq!(
            merged,
            vec![
                "Keyword ability 0: Trample".to_string(),
                "Static ability 1: Annihilator 1".to_string(),
            ]
        );
    }

    #[test]
    fn conventional_long_numbered_keyword_list_still_compacts() {
        let merged = merge_adjacent_keyword_surface_lines(vec![
            "Keyword ability 0: Flying".to_string(),
            "Keyword ability 1: First strike".to_string(),
            "Keyword ability 2: Vigilance".to_string(),
            "Keyword ability 3: Trample".to_string(),
        ]);

        assert_eq!(
            merged,
            vec!["Keyword ability 0: Flying, first strike, vigilance, trample".to_string()]
        );
    }

    #[test]
    fn decomposed_protection_pair_still_compacts_to_one_surface() {
        let merged = merge_adjacent_keyword_surface_lines(vec![
            "Keyword ability 0: Protection from black".to_string(),
            "Keyword ability 1: Protection from red".to_string(),
        ]);

        assert_eq!(
            merged,
            vec!["Keyword ability 0: Protection from black and from red".to_string()]
        );
    }

    #[test]
    fn unnumbered_keyword_fragments_still_compact_within_one_surface() {
        let merged =
            merge_adjacent_keyword_surface_lines(vec!["Flying".to_string(), "Haste".to_string()]);

        assert_eq!(merged, vec!["Flying, haste".to_string()]);
    }

    #[test]
    fn adjacent_singular_protection_grants_keep_has_and_compact_colors() {
        let merged = merge_adjacent_keyword_surface_lines(vec![
            "Static ability 0: Equipped creature has protection from green".to_string(),
            "Static ability 1: Equipped creature has protection from blue".to_string(),
        ]);

        assert_eq!(
            merged,
            vec![
                "Static ability 0: Equipped creature has protection from green and from blue"
                    .to_string()
            ]
        );
    }
}

#[cfg(test)]
mod keyword_condition_ladder_tests {
    use super::*;

    fn keyword_model(
        id: crate::static_abilities::StaticAbilityId,
        label: &str,
    ) -> crate::static_abilities::CompiledStaticAbility {
        ironsmith_core::StaticAbility {
            id: Some(id),
            label: label.to_string(),
            payload: ironsmith_core::StaticAbilityPayload::None,
        }
    }

    fn count_condition(filter: ObjectFilter, display: impl Into<String>) -> Condition {
        Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::MatchingFilter(filter),
            comparison: Comparison::GreaterThanOrEqual(1),
            display: Some(display.into()),
        }
    }

    fn modeled_keyword_grant(
        id: crate::static_abilities::StaticAbilityId,
        label: &str,
        condition: Condition,
    ) -> Ability {
        let granted = ironsmith_core::Ability {
            kind: ironsmith_core::AbilityKind::Static(keyword_model(id, label)),
            functional_zones: vec![Zone::Battlefield],
        };
        let grant = ironsmith_core::GrantObjectAbilityForFilter::new(
            ObjectFilter::source(),
            granted,
            label,
        )
        .with_condition(condition);
        let model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::new(grant);
        Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
    }

    fn keyword_condition_filter(id: crate::static_abilities::StaticAbilityId) -> ObjectFilter {
        let mut filter = ObjectFilter::default().in_zone(Zone::Exile);
        filter.static_abilities.push(id);
        filter
    }

    fn modeled_source_anthem(power: i32, toughness: i32, condition: Condition) -> Ability {
        let anthem = ironsmith_core::Anthem::for_source(power, toughness).with_condition(condition);
        let model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::new(anthem);
        Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
    }

    fn basic_land_condition(subtype: Subtype, article: &str) -> Condition {
        let mut filter = ObjectFilter::default();
        filter.controller = Some(PlayerFilter::You);
        filter.subtypes.push(subtype);
        count_condition(filter, format!("you control {article}"))
    }

    #[test]
    fn counters_among_source_keyword_condition_keeps_leading_surface() {
        use crate::static_abilities::StaticAbilityId::{Hexproof, Shroud};

        let mut sagas = ObjectFilter::default().in_zone(Zone::Battlefield);
        sagas.controller = Some(PlayerFilter::You);
        sagas.subtypes.push(Subtype::Saga);
        let counters_among = Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::CountersAmong(
                sagas,
                crate::object::CounterType::Lore,
            ),
            comparison: Comparison::GreaterThanOrEqual(4),
            display: Some(
                "there are four or more lore counters among sagas you control".to_string(),
            ),
        };
        let grant = modeled_keyword_grant(Hexproof, "hexproof", counters_among);
        assert_eq!(
            describe_structural_conditional_source_keyword_grant(&grant, "this creature"),
            Some(
                "As long as there are four or more lore counters among sagas you control, this creature has hexproof"
                    .to_string()
            ),
        );

        let threshold = Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::MatchingFilter(
                ObjectFilter::default().in_zone(Zone::Graveyard),
            ),
            comparison: Comparison::GreaterThanOrEqual(7),
            display: Some("there are seven or more cards in your graveyard".to_string()),
        };
        let trailing = modeled_keyword_grant(Shroud, "shroud", threshold);
        assert_eq!(
            describe_structural_conditional_source_keyword_grant(&trailing, "this creature"),
            Some(
                "this creature has shroud as long as there are seven or more cards in your graveyard"
                    .to_string()
            ),
            "ordinary threshold-style count conditions keep their authored trailing surface",
        );
    }

    #[test]
    fn complete_typed_keyword_ladder_uses_same_is_true_surface() {
        use crate::static_abilities::StaticAbilityId::{Flying, Haste, Vigilance};

        let abilities = vec![
            modeled_keyword_grant(
                Flying,
                "flying",
                count_condition(
                    keyword_condition_filter(Flying),
                    "there is a card exiled with it with flying",
                ),
            ),
            modeled_keyword_grant(
                Haste,
                "haste",
                count_condition(
                    keyword_condition_filter(Haste),
                    "there is a card exiled with it with haste",
                ),
            ),
            modeled_keyword_grant(
                Vigilance,
                "vigilance",
                count_condition(
                    keyword_condition_filter(Vigilance),
                    "there is a card exiled with it with vigilance",
                ),
            ),
        ];

        assert_eq!(
            describe_structural_keyword_same_is_true_ladder(&abilities, "this creature"),
            Some((
                "As long as a card exiled with it has flying, this creature has flying. The same is true for haste and vigilance"
                    .to_string(),
                3,
            )),
        );
    }

    #[test]
    fn removed_draft_keyword_ladder_requires_shared_typed_provenance() {
        use crate::static_abilities::StaticAbilityId::{Flying, Haste, Vigilance};

        fn removed_condition(
            keyword: crate::static_abilities::StaticAbilityId,
            group: &str,
        ) -> Condition {
            let mut filter = ObjectFilter::default().with_type(CardType::Creature);
            filter.set_explicit_card_noun(true);
            filter.static_abilities.push(keyword);
            Condition::PlayerRemovedDraftCardMatching {
                player: PlayerFilter::You,
                filter,
                with_cards_named: group.to_string(),
            }
        }

        let abilities = vec![
            modeled_keyword_grant(Flying, "flying", removed_condition(Flying, "Draft Mimic")),
            modeled_keyword_grant(Haste, "haste", removed_condition(Haste, "Draft Mimic")),
            modeled_keyword_grant(
                Vigilance,
                "vigilance",
                removed_condition(Vigilance, "Draft Mimic"),
            ),
        ];
        assert_eq!(
            describe_structural_removed_draft_keyword_ladder(&abilities, "this creature"),
            Some((
                "If you removed a creature card with flying from the draft with cards named Draft Mimic, this creature has flying. The same is true for haste and vigilance"
                    .to_string(),
                3,
            )),
        );

        let mut wrong_group = abilities.clone();
        wrong_group[1] = modeled_keyword_grant(
            Haste,
            "haste",
            removed_condition(Haste, "Another Draft Group"),
        );
        assert_eq!(
            describe_structural_removed_draft_keyword_ladder(&wrong_group, "this creature"),
            None,
            "the renderer must not hide a provenance mismatch",
        );
    }

    #[test]
    fn delve_linked_leading_grant_rejoins_variant_copy_ladder() {
        use crate::static_abilities::StaticAbilityId::{FirstStrike, Flying, Vigilance};
        use ironsmith_core::StaticAbilityVariantSelector::Any;

        let mut source_exiled = ObjectFilter::default()
            .in_zone(Zone::Exile)
            .with_type(CardType::Creature);
        source_exiled.set_explicit_card_noun(true);
        source_exiled
            .tagged_constraints
            .push(crate::filter::TaggedObjectConstraint {
                tag: crate::tag::SOURCE_EXILED_TAG.into(),
                relation: crate::filter::TaggedOpbjectRelation::IsTaggedObject,
            });
        let mut flying_condition = source_exiled.clone();
        flying_condition.static_abilities.push(Flying);
        let leading = modeled_keyword_grant(
            Flying,
            "flying",
            count_condition(
                flying_condition,
                "there is a creature card exiled with this creature with flying",
            ),
        );
        let copy = ironsmith_core::CopyStaticAbilityVariants::new(
            source_exiled,
            vec![Any(FirstStrike), Any(Vigilance)],
            "As long as there is a creature card exiled with this creature with first strike, this creature has first strike. The same is true for vigilance.",
        );
        let copy_model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::new(copy);
        let variants = Ability::static_ability(crate::static_abilities::StaticAbility::from_model(
            copy_model,
        ));

        assert_eq!(
            describe_structural_delve_keyword_variant_ladder(
                &[leading.clone(), variants.clone()],
                "this creature",
                true,
            ),
            Some((
                "If a creature card with flying was exiled with this creature's delve ability, this creature has flying. The same is true for first strike and vigilance"
                    .to_string(),
                2,
            )),
        );
        assert_eq!(
            describe_structural_delve_keyword_variant_ladder(
                &[leading, variants],
                "this creature",
                false,
            ),
            None,
            "the same structural pair must not invent delve provenance on a source without delve",
        );
    }

    #[test]
    fn concatenated_or_mismatched_keyword_conditions_are_not_hidden() {
        use crate::static_abilities::StaticAbilityId::{Flying, Haste, Vigilance};

        let flying = count_condition(
            keyword_condition_filter(Flying),
            "there is a card exiled with it with flying",
        );
        let malformed = Condition::And(
            Box::new(flying),
            Box::new(count_condition(
                keyword_condition_filter(Haste),
                "there is a card exiled with it with haste",
            )),
        );
        let abilities = vec![
            modeled_keyword_grant(Flying, "flying", malformed),
            modeled_keyword_grant(
                Haste,
                "haste",
                count_condition(
                    keyword_condition_filter(Haste),
                    "there is a card exiled with it with haste",
                ),
            ),
            modeled_keyword_grant(
                Vigilance,
                "vigilance",
                count_condition(
                    keyword_condition_filter(Vigilance),
                    "there is a card exiled with it with vigilance",
                ),
            ),
        ];
        assert_eq!(
            describe_structural_keyword_same_is_true_ladder(&abilities, "this creature"),
            None,
        );

        let wrong_filter = vec![
            modeled_keyword_grant(
                Flying,
                "flying",
                count_condition(
                    keyword_condition_filter(Haste),
                    "there is a card exiled with it with flying",
                ),
            ),
            abilities[1].clone(),
            abilities[2].clone(),
        ];
        assert_eq!(
            describe_structural_keyword_same_is_true_ladder(&wrong_filter, "this creature"),
            None,
        );
    }

    #[test]
    fn full_five_basic_land_modifier_ladder_compacts_to_one_sentence() {
        use crate::static_abilities::StaticAbilityId::{FirstStrike, Flying, Trample};

        let abilities = vec![
            modeled_source_anthem(0, 2, basic_land_condition(Subtype::Plains, "a plains")),
            modeled_keyword_grant(
                Flying,
                "flying",
                basic_land_condition(Subtype::Island, "an island"),
            ),
            modeled_source_anthem(2, 0, basic_land_condition(Subtype::Swamp, "a swamp")),
            modeled_keyword_grant(
                FirstStrike,
                "first strike",
                basic_land_condition(Subtype::Mountain, "a mountain"),
            ),
            modeled_keyword_grant(
                Trample,
                "trample",
                basic_land_condition(Subtype::Forest, "a forest"),
            ),
        ];

        assert_eq!(
            describe_structural_five_basic_land_modifier_ladder(&abilities, "this creature"),
            Some((
                "This creature gets +0/+2 as long as you control a plains, has flying as long as you control an island, gets +2/+0 as long as you control a swamp, has first strike as long as you control a mountain, and has trample as long as you control a forest"
                    .to_string(),
                5,
            )),
        );
    }

    #[test]
    fn incomplete_basic_land_modifier_ladder_stays_explicit() {
        let abilities = vec![modeled_source_anthem(
            0,
            2,
            basic_land_condition(Subtype::Plains, "a plains"),
        )];
        assert_eq!(
            describe_structural_five_basic_land_modifier_ladder(&abilities, "this creature"),
            None,
        );
    }
}

#[cfg(test)]
mod reinforce_structural_render_tests {
    use super::*;

    fn mana_cost(symbols: Vec<ManaSymbol>) -> crate::mana::ManaCost {
        crate::mana::ManaCost::from_symbols(symbols)
    }

    fn reinforce_ability(amount: i32, mana: crate::mana::ManaCost) -> Ability {
        let costs = crate::cost::TotalCost::from_costs(vec![
            crate::costs::Cost::mana(mana),
            crate::costs::Cost::discard_source(),
        ]);
        let target = ChooseSpec::target(ChooseSpec::Object(
            ObjectFilter::creature().in_zone(Zone::Battlefield),
        ));
        let effect =
            Effect::put_counters(crate::object::CounterType::PlusOnePlusOne, amount, target);
        let mut ability = Ability::activated(costs, vec![effect]);
        ability.functional_zones = vec![Zone::Hand];
        ability
    }

    #[test]
    fn exact_hand_activation_renders_generic_reinforce_amount() {
        let one = reinforce_ability(1, mana_cost(vec![ManaSymbol::Generic(1), ManaSymbol::Red]));
        let three = reinforce_ability(
            3,
            mana_cost(vec![ManaSymbol::Generic(3), ManaSymbol::Green]),
        );
        assert_eq!(
            describe_structural_reinforce_ability(&one).as_deref(),
            Some("Reinforce 1—{1}{R}"),
        );
        assert_eq!(
            describe_structural_reinforce_ability(&three).as_deref(),
            Some("Reinforce 3—{3}{G}"),
        );

        let mut indexed = one.clone();
        let AbilityKind::Activated(indexed_activated) = &mut indexed.kind else {
            unreachable!();
        };
        indexed_activated
            .choices
            .push(ChooseSpec::target_creature());
        assert_eq!(
            describe_structural_reinforce_ability(&indexed).as_deref(),
            Some("Reinforce 1—{1}{R}"),
        );

        let definition = crate::cards::builders::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Reinforce Surface Probe",
        )
        .card_types(vec![CardType::Creature])
        .with_ability(Ability::static_ability(
            crate::static_abilities::StaticAbility::flying(),
        ))
        .with_ability(three)
        .build();
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec!["Flying".to_string(), "Reinforce 3—{3}{G}".to_string()],
        );
    }

    #[test]
    fn near_miss_zone_cost_metadata_and_effect_shapes_stay_explicit() {
        let base = reinforce_ability(
            2,
            mana_cost(vec![ManaSymbol::Generic(2), ManaSymbol::White]),
        );

        let mut wrong_zone = base.clone();
        wrong_zone.functional_zones = vec![Zone::Battlefield];
        assert_eq!(describe_structural_reinforce_ability(&wrong_zone), None);

        let mut extra_cost = base.clone();
        let AbilityKind::Activated(extra_cost_activated) = &mut extra_cost.kind else {
            unreachable!();
        };
        extra_cost_activated.mana_cost = crate::cost::TotalCost::from_costs(vec![
            crate::costs::Cost::mana(mana_cost(vec![ManaSymbol::Generic(2), ManaSymbol::White])),
            crate::costs::Cost::discard_source(),
            crate::costs::Cost::life(1),
        ]);
        assert_eq!(describe_structural_reinforce_ability(&extra_cost), None);

        let mut extra_choice = base.clone();
        let AbilityKind::Activated(extra_choice_activated) = &mut extra_choice.kind else {
            unreachable!();
        };
        extra_choice_activated
            .choices
            .push(ChooseSpec::target_player());
        assert_eq!(describe_structural_reinforce_ability(&extra_choice), None);

        let mut extra_effect = base.clone();
        let AbilityKind::Activated(extra_effect_activated) = &mut extra_effect.kind else {
            unreachable!();
        };
        extra_effect_activated.effects.push(Effect::draw(1));
        assert_eq!(describe_structural_reinforce_ability(&extra_effect), None);

        let mut wrong_target = base.clone();
        let AbilityKind::Activated(wrong_target_activated) = &mut wrong_target.kind else {
            unreachable!();
        };
        wrong_target_activated.effects =
            crate::resolution::ResolutionProgram::from_effects(vec![Effect::put_counters(
                crate::object::CounterType::PlusOnePlusOne,
                2,
                ChooseSpec::target(ChooseSpec::Object(ObjectFilter::artifact())),
            )]);
        assert_eq!(describe_structural_reinforce_ability(&wrong_target), None);

        let mut dynamic_amount = base;
        let AbilityKind::Activated(dynamic_activated) = &mut dynamic_amount.kind else {
            unreachable!();
        };
        dynamic_activated.effects =
            crate::resolution::ResolutionProgram::from_effects(vec![Effect::put_counters(
                crate::object::CounterType::PlusOnePlusOne,
                Value::CardsInHand(PlayerFilter::You),
                ChooseSpec::target(ChooseSpec::Object(
                    ObjectFilter::creature().in_zone(Zone::Battlefield),
                )),
            )]);
        assert_eq!(describe_structural_reinforce_ability(&dynamic_amount), None,);
    }
}

#[cfg(test)]
mod attached_anthem_bundle_tests {
    use super::*;

    fn enchanted_creature_filter() -> ObjectFilter {
        ObjectFilter::creature()
            .in_zone(Zone::Battlefield)
            .match_tagged(
                "enchanted",
                crate::target::TaggedOpbjectRelation::IsTaggedObject,
            )
    }

    #[test]
    fn attached_pt_and_ability_loss_static_pieces_recombine() {
        for (power, expected) in [
            (-5, "Enchanted creature gets -5/-0 and loses all abilities"),
            (-3, "Enchanted creature gets -3/-0 and loses all abilities"),
            (-2, "Enchanted creature gets -2/-0 and loses all abilities"),
        ] {
            let filter = enchanted_creature_filter();
            let abilities = vec![
                Ability::static_ability(crate::static_abilities::StaticAbility::anthem(
                    filter.clone(),
                    power,
                    0,
                )),
                Ability::static_ability(
                    crate::static_abilities::StaticAbility::remove_all_abilities(filter),
                ),
            ];

            assert_eq!(
                describe_structural_anthem_remove_all_abilities_bundle(&abilities),
                Some((expected.to_string(), 2)),
            );
        }
    }

    #[test]
    fn attached_pt_and_set_color_static_pieces_recombine() {
        let filter = enchanted_creature_filter();
        let abilities = vec![
            Ability::static_ability(crate::static_abilities::StaticAbility::anthem(
                filter.clone(),
                3,
                1,
            )),
            Ability::static_ability(crate::static_abilities::StaticAbility::set_colors(
                filter,
                crate::color::ColorSet::BLACK,
            )),
        ];

        assert_eq!(
            describe_structural_anthem_set_colors_bundle(&abilities),
            Some(("Enchanted creature gets +3/+1 and is black".to_string(), 2,)),
        );
    }

    #[test]
    fn attached_characteristic_bundles_require_identical_filters() {
        let filter = enchanted_creature_filter();
        let other_filter = ObjectFilter::creature().in_zone(Zone::Battlefield);
        let anthem =
            Ability::static_ability(crate::static_abilities::StaticAbility::anthem(filter, 3, 1));
        let remove = Ability::static_ability(
            crate::static_abilities::StaticAbility::remove_all_abilities(other_filter.clone()),
        );
        let color = Ability::static_ability(crate::static_abilities::StaticAbility::set_colors(
            other_filter,
            crate::color::ColorSet::BLACK,
        ));

        assert_eq!(
            describe_structural_anthem_remove_all_abilities_bundle(&[anthem.clone(), remove,]),
            None,
        );
        assert_eq!(
            describe_structural_anthem_set_colors_bundle(&[anthem, color]),
            None,
        );
    }
}

#[cfg(test)]
mod anthem_color_subtype_boundary_tests {
    use super::*;

    fn modeled(model: crate::static_abilities::CompiledStaticAbility) -> Ability {
        Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
    }

    fn goblin_filter() -> ObjectFilter {
        ObjectFilter::default()
            .in_zone(Zone::Battlefield)
            .with_subtype(Subtype::Goblin)
    }

    #[test]
    fn typed_anthem_color_and_subtype_pieces_keep_authored_sentence_boundary() {
        let filter = goblin_filter();
        let anthem: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::new(
                ironsmith_core::Anthem::<ironsmith_core::Condition>::new(filter.clone(), 1, 1)
                    .with_set_quantifier_surface(Some(ironsmith_core::SetQuantifierSurface::All)),
            );
        let colors: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::set_colors(
                filter.clone(),
                crate::color::ColorSet::BLACK,
            );
        let subtypes: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::add_subtypes(filter, vec![Subtype::Zombie]);
        let abilities = vec![modeled(anthem), modeled(colors), modeled(subtypes)];

        assert_eq!(
            describe_structural_anthem_then_color_subtype_bundle(&abilities),
            Some((
                vec![
                    "All Goblins get +1/+1".to_string(),
                    "All Goblins are black and are Zombies in addition to their other creature types"
                        .to_string(),
                ],
                3,
            )),
        );
    }

    #[test]
    fn differing_typed_filters_do_not_merge_across_authored_clauses() {
        let goblins = goblin_filter();
        let zombies = ObjectFilter::default()
            .in_zone(Zone::Battlefield)
            .with_subtype(Subtype::Zombie);
        let anthem: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::new(
                ironsmith_core::Anthem::<ironsmith_core::Condition>::new(goblins.clone(), 1, 1)
                    .with_set_quantifier_surface(Some(ironsmith_core::SetQuantifierSurface::All)),
            );
        let colors: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::set_colors(goblins, crate::color::ColorSet::BLACK);
        let subtypes: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::add_subtypes(zombies, vec![Subtype::Zombie]);

        assert_eq!(
            describe_structural_anthem_then_color_subtype_bundle(&[
                modeled(anthem),
                modeled(colors),
                modeled(subtypes),
            ]),
            None,
        );
    }
}

#[cfg(test)]
mod conditional_anthem_otherwise_bundle_tests {
    use super::*;

    fn enchanted_creature_filter() -> ObjectFilter {
        ObjectFilter::creature().match_tagged(
            "enchanted",
            crate::target::TaggedOpbjectRelation::IsTaggedObject,
        )
    }

    fn modeled_anthem(payload: ironsmith_core::Anthem) -> Ability {
        let model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::new(payload);
        Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
    }

    fn otherwise_pair(
        condition_filter: ObjectFilter,
        positive: (i32, i32),
        negative: (i32, i32),
    ) -> Vec<Ability> {
        let condition = crate::ConditionExpr::AttachedToSourceMatches(condition_filter);
        vec![
            modeled_anthem(
                ironsmith_core::Anthem::new(enchanted_creature_filter(), positive.0, positive.1)
                    .with_condition(condition.clone()),
            ),
            modeled_anthem(
                ironsmith_core::Anthem::new(enchanted_creature_filter(), negative.0, negative.1)
                    .with_condition(crate::ConditionExpr::Not(Box::new(condition))),
            ),
        ]
    }

    #[test]
    fn adjacent_inverse_fixed_anthems_render_as_otherwise() {
        let mut attacking = ObjectFilter::default();
        attacking.attacking = true;
        for (condition, positive, negative, expected) in [
            (
                ObjectFilter::default().with_subtype(Subtype::Zombie),
                (3, 3),
                (-3, -3),
                "Enchanted creature gets +3/+3 as long as it's a Zombie. Otherwise, it gets -3/-3",
            ),
            (
                ObjectFilter::enchantment(),
                (2, 2),
                (-2, -2),
                "Enchanted creature gets +2/+2 as long as it's an enchantment. Otherwise, it gets -2/-2",
            ),
            (
                ObjectFilter::default().with_colors(crate::color::ColorSet::BLACK),
                (2, 1),
                (-1, -2),
                "Enchanted creature gets +2/+1 as long as it's black. Otherwise, it gets -1/-2",
            ),
            (
                attacking,
                (3, 0),
                (-2, -1),
                "Enchanted creature gets +3/+0 as long as it's attacking. Otherwise, it gets -2/-1",
            ),
        ] {
            let abilities = otherwise_pair(condition, positive, negative);
            assert_eq!(
                describe_structural_conditional_anthem_otherwise_bundle(&abilities),
                Some((expected.to_string(), 2)),
            );
        }

        let pirate = otherwise_pair(
            ObjectFilter::default().with_subtype(Subtype::Pirate),
            (0, 2),
            (-2, 0),
        );
        let mut builder = crate::cards::builders::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Otherwise Surface Probe",
        )
        .card_types(vec![CardType::Enchantment]);
        for ability in pirate {
            builder = builder.with_ability(ability);
        }
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&builder.build()),
            vec![
                "Enchanted creature gets +0/+2 as long as it's a Pirate. Otherwise, it gets -2/-0."
                    .to_string()
            ],
        );
    }

    #[test]
    fn target_predicate_and_fixed_payload_near_misses_do_not_merge() {
        let zombie = ObjectFilter::default().with_subtype(Subtype::Zombie);

        let mut different_target = otherwise_pair(zombie.clone(), (3, 3), (-3, -3));
        let equipped_target = ObjectFilter::creature().match_tagged(
            "equipped",
            crate::target::TaggedOpbjectRelation::IsTaggedObject,
        );
        different_target[1] = modeled_anthem(
            ironsmith_core::Anthem::new(equipped_target, -3, -3).with_condition(
                crate::ConditionExpr::Not(Box::new(crate::ConditionExpr::AttachedToSourceMatches(
                    zombie.clone(),
                ))),
            ),
        );
        assert_eq!(
            describe_structural_conditional_anthem_otherwise_bundle(&different_target),
            None,
        );

        let mut noninverse = otherwise_pair(zombie.clone(), (3, 3), (-3, -3));
        noninverse[1] = modeled_anthem(
            ironsmith_core::Anthem::new(enchanted_creature_filter(), -3, -3).with_condition(
                crate::ConditionExpr::Not(Box::new(crate::ConditionExpr::AttachedToSourceMatches(
                    ObjectFilter::default().with_subtype(Subtype::Vampire),
                ))),
            ),
        );
        assert_eq!(
            describe_structural_conditional_anthem_otherwise_bundle(&noninverse),
            None,
        );

        let mut dynamic = otherwise_pair(zombie.clone(), (3, 3), (-3, -3));
        dynamic[1] = modeled_anthem(
            ironsmith_core::Anthem::new(enchanted_creature_filter(), -3, -3)
                .with_values(
                    ironsmith_core::AnthemValue::Dynamic(Value::Fixed(-3)),
                    ironsmith_core::AnthemValue::Fixed(-3),
                )
                .with_condition(crate::ConditionExpr::Not(Box::new(
                    crate::ConditionExpr::AttachedToSourceMatches(zombie),
                ))),
        );
        assert_eq!(
            describe_structural_conditional_anthem_otherwise_bundle(&dynamic),
            None,
        );

        let mut non_pt = otherwise_pair(
            ObjectFilter::default().with_subtype(Subtype::Pirate),
            (0, 2),
            (-2, 0),
        );
        non_pt[1] = Ability::static_ability(crate::static_abilities::StaticAbility::flying());
        assert_eq!(
            describe_structural_conditional_anthem_otherwise_bundle(&non_pt),
            None,
        );
    }
}

#[cfg(test)]
mod enchanted_restriction_bundle_tests {
    use super::*;

    fn enchanted_filter(subject: &str) -> ObjectFilter {
        let filter = match subject {
            "Enchanted creature" => ObjectFilter::creature(),
            "Enchanted permanent" => ObjectFilter::permanent_card().in_zone(Zone::Battlefield),
            "Enchanted artifact" => ObjectFilter::artifact(),
            other => panic!("unsupported restriction test subject: {other}"),
        };
        filter.match_tagged(
            "enchanted",
            crate::target::TaggedOpbjectRelation::IsTaggedObject,
        )
    }

    fn modeled_rule_static(
        restriction: crate::effect::Restriction,
        display: impl Into<String>,
    ) -> crate::static_abilities::StaticAbility {
        let display = display.into();
        let model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::restriction(restriction, display);
        crate::static_abilities::StaticAbility::from_model(model)
    }

    fn modeled_rule_ability(
        restriction: crate::effect::Restriction,
        display: impl Into<String>,
    ) -> Ability {
        Ability::static_ability(modeled_rule_static(restriction, display))
    }

    fn restriction_pair(subject: &str) -> Vec<Ability> {
        let filter = enchanted_filter(subject);
        let subject = subject.to_ascii_lowercase();
        vec![
            modeled_rule_ability(
                crate::effect::Restriction::attack_or_block(filter.clone()),
                format!("{subject} can't attack or block"),
            ),
            modeled_rule_ability(
                crate::effect::Restriction::activate_abilities_of(filter),
                format!("{subject} activated abilities can't be activated"),
            ),
        ]
    }

    fn ignore_restriction_special_action() -> Ability {
        let model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::attached_controller_may_sacrifice_permanent_to_ignore_source_effect_until_end_of_turn(
                "That creature's controller may sacrifice a permanent of their choice for that player to ignore this effect until end of turn",
            );
        Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
    }

    #[test]
    fn exact_attached_restrictions_and_ignore_special_action_recombine() {
        let mut abilities = restriction_pair("Enchanted creature");
        abilities.push(ignore_restriction_special_action());
        let expected = "Enchanted creature can't attack or block, and its activated abilities can't be activated. That creature's controller may sacrifice a permanent of their choice for that player to ignore this effect until end of turn";
        assert_eq!(
            describe_structural_enchanted_combat_activation_restriction_bundle(&abilities),
            Some((expected.to_string(), 3)),
        );

        let mut wrong_zone = abilities;
        wrong_zone[2].functional_zones = vec![Zone::Hand];
        assert_eq!(
            describe_structural_enchanted_combat_activation_restriction_bundle(&wrong_zone),
            Some((
                "Enchanted creature can't attack or block, and its activated abilities can't be activated"
                    .to_string(),
                2,
            )),
            "an inactive-zone marker must remain separate rather than being folded"
        );
    }

    #[test]
    fn exact_ability_loss_and_untap_restriction_recombine() {
        let filter = enchanted_filter("Enchanted permanent");
        let abilities = vec![
            Ability::static_ability(
                crate::static_abilities::StaticAbility::remove_all_abilities(filter.clone()),
            ),
            modeled_rule_ability(
                crate::effect::Restriction::untap(filter),
                "enchanted permanent doesn't untap during its controller's untap step",
            ),
        ];
        let expected = "Enchanted permanent loses all abilities and doesn't untap during its controller's untap step";
        assert_eq!(
            describe_structural_remove_all_abilities_untap_bundle(&abilities),
            Some((expected.to_string(), 2)),
        );

        let mut builder = crate::cards::builders::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Restriction Surface Probe",
        )
        .card_types(vec![CardType::Enchantment]);
        for ability in abilities {
            builder = builder.with_ability(ability);
        }
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&builder.build()),
            vec![format!("{expected}.")],
        );
    }

    #[test]
    fn exact_ability_loss_and_cant_become_untapped_require_the_same_enchanted_object() {
        let creature = enchanted_filter("Enchanted creature");
        let abilities = vec![
            Ability::static_ability(
                crate::static_abilities::StaticAbility::remove_all_abilities(creature.clone()),
            ),
            modeled_rule_ability(
                crate::effect::Restriction::untap(creature),
                "enchanted creature can't become untapped",
            ),
        ];
        assert_eq!(
            describe_structural_remove_all_abilities_untap_bundle(&abilities),
            Some((
                "Enchanted creature loses all abilities and can't become untapped".to_string(),
                2,
            )),
        );

        let mut different_subject = abilities;
        different_subject[1] = modeled_rule_ability(
            crate::effect::Restriction::untap(enchanted_filter("Enchanted permanent")),
            "enchanted permanent can't become untapped",
        );
        assert_eq!(
            describe_structural_remove_all_abilities_untap_bundle(&different_subject),
            None,
            "different attached-object filters must remain separate executable rules"
        );
    }

    #[test]
    fn exact_shared_subject_block_and_attack_restrictions_recombine() {
        let filter = ObjectFilter::default()
            .with_subtype(Subtype::Cleric)
            .controlled_by(PlayerFilter::Opponent)
            .in_zone(Zone::Battlefield);
        let abilities = vec![
            modeled_rule_ability(
                crate::effect::Restriction::block(filter.clone()),
                "clerics your opponents control can't block",
            ),
            modeled_rule_ability(
                crate::effect::Restriction::attack_player_or_planeswalkers_controlled_by(
                    filter.clone(),
                    PlayerFilter::You,
                ),
                "clerics your opponents control can't attack you or planeswalkers you control",
            ),
        ];
        let expected = "Clerics your opponents control can't block, and they can't attack you or planeswalkers you control";
        assert_eq!(
            describe_structural_shared_subject_block_attack_bundle(&abilities),
            Some((expected.to_string(), 2)),
        );

        let mut builder = crate::cards::builders::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Restriction Surface Probe",
        )
        .card_types(vec![CardType::Creature]);
        for ability in abilities.clone() {
            builder = builder.with_ability(ability);
        }
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&builder.build()),
            vec![format!("{expected}.")],
        );

        let mut different_subject = abilities;
        different_subject[1] = modeled_rule_ability(
            crate::effect::Restriction::attack_player_or_planeswalkers_controlled_by(
                ObjectFilter::default()
                    .with_subtype(Subtype::Rogue)
                    .controlled_by(PlayerFilter::Opponent)
                    .in_zone(Zone::Battlefield),
                PlayerFilter::You,
            ),
            "rogues your opponents control can't attack you or planeswalkers you control",
        );
        assert_eq!(
            describe_structural_shared_subject_block_attack_bundle(&different_subject),
            None,
        );
    }

    #[test]
    fn exact_adjacent_creature_permanent_and_artifact_restrictions_recombine() {
        for subject in [
            "Enchanted creature",
            "Enchanted permanent",
            "Enchanted artifact",
        ] {
            let expected = format!(
                "{subject} can't attack or block, and its activated abilities can't be activated"
            );
            let abilities = restriction_pair(subject);
            assert_eq!(
                describe_structural_enchanted_combat_activation_restriction_bundle(&abilities),
                Some((expected.clone(), 2)),
            );

            let mut builder = crate::cards::builders::CardDefinitionBuilder::new(
                crate::ids::CardId::new(),
                "Restriction Surface Probe",
            )
            .card_types(vec![CardType::Enchantment]);
            for ability in abilities {
                builder = builder.with_ability(ability);
            }
            let definition = builder.build();
            assert_eq!(
                crate::compiled_text::compiled_text_lines(&definition),
                vec![format!("{expected}.")],
            );
        }

        let creature = enchanted_filter("Enchanted creature");
        let block_and_activation = vec![
            modeled_rule_ability(
                crate::effect::Restriction::block(creature.clone()),
                "enchanted creature can't block",
            ),
            modeled_rule_ability(
                crate::effect::Restriction::activate_abilities_of(creature),
                "enchanted creature activated abilities can't be activated",
            ),
        ];
        assert_eq!(
            describe_structural_enchanted_combat_activation_restriction_bundle(
                &block_and_activation,
            ),
            Some((
                "Enchanted creature can't block, and its activated abilities can't be activated"
                    .to_string(),
                2,
            )),
        );

        let permanent = enchanted_filter("Enchanted permanent");
        let untap_and_activation = vec![
            modeled_rule_ability(
                crate::effect::Restriction::untap(permanent.clone()),
                "enchanted permanent doesn't untap during its controller's untap step",
            ),
            modeled_rule_ability(
                crate::effect::Restriction::activate_abilities_of(permanent),
                "enchanted permanent activated abilities can't be activated",
            ),
        ];
        assert_eq!(
            describe_structural_enchanted_combat_activation_restriction_bundle(
                &untap_and_activation,
            ),
            Some((
                "Enchanted permanent doesn't untap during its controller's untap step and its activated abilities can't be activated"
                    .to_string(),
                2,
            )),
        );
    }

    #[test]
    fn nonadjacent_or_different_subject_restrictions_stay_separate() {
        let mut nonadjacent = restriction_pair("Enchanted creature");
        nonadjacent.insert(
            1,
            Ability::static_ability(crate::static_abilities::StaticAbility::flying()),
        );
        assert_eq!(
            describe_structural_enchanted_combat_activation_restriction_bundle(&nonadjacent),
            None,
        );

        let mut different_subject = restriction_pair("Enchanted creature");
        let permanent_filter = enchanted_filter("Enchanted permanent");
        different_subject[1] = modeled_rule_ability(
            crate::effect::Restriction::activate_abilities_of(permanent_filter),
            "enchanted permanent activated abilities can't be activated",
        );
        assert_eq!(
            describe_structural_enchanted_combat_activation_restriction_bundle(&different_subject,),
            None,
        );
    }

    #[test]
    fn conditioned_or_mana_exception_restrictions_stay_separate() {
        let mut conditioned = restriction_pair("Enchanted creature");
        conditioned[1] = Ability::static_ability(
            modeled_rule_static(
                crate::effect::Restriction::activate_abilities_of(enchanted_filter(
                    "Enchanted creature",
                )),
                "enchanted creature activated abilities can't be activated",
            )
            .with_condition(crate::ConditionExpr::YourTurn)
            .expect("rule restriction supports a temporal condition"),
        );
        assert_eq!(
            describe_structural_enchanted_combat_activation_restriction_bundle(&conditioned),
            None,
        );

        let mut mana_exception = restriction_pair("Enchanted permanent");
        mana_exception[1] = modeled_rule_ability(
            crate::effect::Restriction::activate_non_mana_abilities_of(enchanted_filter(
                "Enchanted permanent",
            )),
            "enchanted permanent activated abilities can't be activated unless they're mana abilities",
        );
        assert_eq!(
            describe_structural_enchanted_combat_activation_restriction_bundle(&mana_exception),
            None,
        );
    }
}

#[cfg(test)]
mod creature_aura_subject_tests {
    use super::*;

    #[test]
    fn typed_aura_attachment_is_removed_only_from_the_rendering_program() {
        let attachment = crate::object::AuraAttachmentFilter::Object(
            ObjectFilter::creature().in_zone(Zone::Battlefield),
        );
        let executable = crate::resolution::ResolutionProgram::from_effects(vec![
            Effect::attach_to(attachment.target_spec()),
            Effect::draw(1),
        ]);

        let rendered = spell_program_without_encoded_aura_attachment(&executable, &attachment);
        assert_eq!(executable.flattened_default_effects().len(), 2);
        assert_eq!(rendered.flattened_default_effects().len(), 1);
        assert!(
            rendered.flattened_default_effects()[0]
                .downcast_ref::<crate::effects::DrawCardsEffect>()
                .is_some()
        );

        let land_attachment = crate::object::AuraAttachmentFilter::Object(
            ObjectFilter::land().in_zone(Zone::Battlefield),
        );
        let untouched =
            spell_program_without_encoded_aura_attachment(&executable, &land_attachment);
        assert_eq!(untouched.flattened_default_effects().len(), 2);
    }

    #[test]
    fn plain_creature_attachment_refines_enchanted_permanent_references() {
        let creature = crate::object::AuraAttachmentFilter::Object(
            ObjectFilter::creature().in_zone(Zone::Battlefield),
        );
        assert_eq!(
            rewrite_plain_creature_aura_subject(
                Some(&creature),
                "At the beginning of the upkeep of enchanted permanent's controller"
            ),
            "At the beginning of the upkeep of enchanted creature's controller"
        );

        let land = crate::object::AuraAttachmentFilter::Object(
            ObjectFilter::land().in_zone(Zone::Battlefield),
        );
        assert_eq!(
            rewrite_plain_creature_aura_subject(
                Some(&land),
                "At the beginning of the upkeep of enchanted permanent's controller"
            ),
            "At the beginning of the upkeep of enchanted permanent's controller"
        );

        let mut creature_or_planeswalker = ObjectFilter::default().in_zone(Zone::Battlefield);
        creature_or_planeswalker.card_types = vec![CardType::Creature, CardType::Planeswalker];
        let creature_or_planeswalker =
            crate::object::AuraAttachmentFilter::Object(creature_or_planeswalker);
        assert_eq!(
            rewrite_plain_creature_aura_subject(
                Some(&creature_or_planeswalker),
                "When this Aura enters, tap enchanted creature and investigate"
            ),
            "When this Aura enters, tap enchanted permanent and investigate"
        );

        let mut artifact_creature_or_planeswalker =
            ObjectFilter::default().in_zone(Zone::Battlefield);
        artifact_creature_or_planeswalker.card_types = vec![
            CardType::Artifact,
            CardType::Creature,
            CardType::Planeswalker,
        ];
        let artifact_creature_or_planeswalker =
            crate::object::AuraAttachmentFilter::Object(artifact_creature_or_planeswalker);
        assert_eq!(
            rewrite_plain_creature_aura_subject(
                Some(&artifact_creature_or_planeswalker),
                "When this Aura enters, tap enchanted creature"
            ),
            "When this Aura enters, tap enchanted permanent"
        );
    }

    #[test]
    fn attached_tap_then_equipment_unattach_uses_one_typed_referent() {
        let enchanted = crate::TagKey::from("enchanted");
        let first = crate::resolution::ResolutionSegment {
            default_effects: vec![
                Effect::new(crate::effects::TagAttachedToSourceEffect::new(
                    enchanted.clone(),
                )),
                Effect::new(crate::effects::TapEffect::with_spec(ChooseSpec::Tagged(
                    enchanted.clone(),
                )))
                .tag("tapped_0"),
            ],
            self_replacements: vec![],
            starts_new_source_line: false,
        };
        let conditional = Effect::conditional_only(
            crate::effect::Condition::TaggedObjectMatches(
                enchanted.clone(),
                ObjectFilter::default().with_subtype(Subtype::Equipment),
            ),
            vec![Effect::new(crate::effects::UnattachObjectsEffect::new(
                ChooseSpec::Tagged(enchanted),
            ))],
        );
        let second = crate::resolution::ResolutionSegment {
            default_effects: vec![conditional],
            self_replacements: vec![],
            starts_new_source_line: false,
        };
        let program = crate::resolution::ResolutionProgram::new(vec![first, second]);

        assert_eq!(
            describe_resolution_program(&program),
            "Tap enchanted creature. If it's an Equipment, unattach it"
        );
    }
}

#[cfg(test)]
mod keyword_maximum_blocker_bundle_tests {
    use super::*;

    fn parsed_equipment_abilities(text: &str) -> Vec<Ability> {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Keyword Maximum Blocker Probe",
        )
        .card_types(vec![CardType::Artifact])
        .subtypes(vec![Subtype::Equipment])
        .parse_text(text)
        .expect("compound Equipment grant should parse")
        .abilities
        .into_iter()
        .skip(1)
        .collect()
    }

    #[test]
    fn matching_typed_grants_recombine_without_a_source_restriction() {
        let abilities = parsed_equipment_abilities(
            "Equipped creature has trample and can't be blocked by more than one creature.",
        );

        assert_eq!(
            describe_structural_keyword_maximum_blocker_bundle(&abilities),
            Some((
                "Equipped creature has trample and can't be blocked by more than one creature"
                    .to_string(),
                2,
            ))
        );
        assert_eq!(abilities.len(), 2);
    }

    #[test]
    fn an_unrelated_followup_is_not_folded_into_the_grant() {
        let mut abilities = parsed_equipment_abilities(
            "Equipped creature has trample and can't be blocked by more than one creature.",
        );
        abilities.swap(0, 1);

        assert_eq!(
            describe_structural_keyword_maximum_blocker_bundle(&abilities),
            None
        );
    }
}

#[cfg(test)]
mod threshold_source_modifier_bundle_tests {
    use super::*;

    fn threshold_abilities() -> Vec<Ability> {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Threshold Source Modifier Probe",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(
            "Threshold — As long as there are seven or more cards in your graveyard, this creature gets +1/+1, is black, and has \"{2}{B}, {T}: Destroy target blue creature.\"",
        )
        .expect("threshold source modifier should parse")
        .abilities
        .into_iter()
        .skip(1)
        .collect()
    }

    fn mutate_model(
        ability: &mut Ability,
        mutate: impl FnOnce(&mut crate::static_abilities::CompiledStaticAbility),
    ) {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            panic!("expected static ability");
        };
        let mut model = static_ability
            .compiled_model()
            .expect("compiled static model")
            .clone();
        mutate(&mut model);
        ability.kind =
            AbilityKind::Static(crate::static_abilities::StaticAbility::from_model(model));
    }

    #[test]
    fn exact_typed_threshold_siblings_recombine() {
        let abilities = threshold_abilities();
        assert_eq!(
            describe_structural_threshold_source_modifier_bundle(&abilities, "this creature"),
            Some((
                "Threshold — As long as there are seven or more cards in your graveyard, this creature gets +1/+1, is black, and has \"{2}{B}, {T}: Destroy target blue creature.\""
                    .to_string(),
                3,
            )),
        );
    }

    #[test]
    fn near_miss_threshold_siblings_stay_separate() {
        let valid = threshold_abilities();

        let mut wrong_anthem = valid.clone();
        mutate_model(&mut wrong_anthem[0], |model| {
            let ironsmith_core::StaticAbilityPayload::Anthem(anthem) = &mut model.payload else {
                panic!("expected anthem payload");
            };
            anthem.power = ironsmith_core::AnthemValue::Fixed(2);
        });
        assert_eq!(
            describe_structural_threshold_source_modifier_bundle(&wrong_anthem, "this creature"),
            None,
        );

        let mut wrong_condition = valid.clone();
        mutate_model(&mut wrong_condition[1], |model| {
            let ironsmith_core::StaticAbilityPayload::Conditional { condition, .. } =
                &mut model.payload
            else {
                panic!("expected conditional color payload");
            };
            let Condition::ValueComparison { right, .. } = condition else {
                panic!("expected threshold comparison");
            };
            *right = Value::Fixed(8);
        });
        assert_eq!(
            describe_structural_threshold_source_modifier_bundle(&wrong_condition, "this creature"),
            None,
        );

        let mut wrong_color = valid.clone();
        mutate_model(&mut wrong_color[1], |model| {
            let ironsmith_core::StaticAbilityPayload::Conditional { ability, .. } =
                &mut model.payload
            else {
                panic!("expected conditional color payload");
            };
            let ironsmith_core::StaticAbilityPayload::SetColors { colors, .. } =
                &mut ability.payload
            else {
                panic!("expected set-colors payload");
            };
            *colors = crate::color::ColorSet::RED;
        });
        assert_eq!(
            describe_structural_threshold_source_modifier_bundle(&wrong_color, "this creature"),
            None,
        );

        let mut wrong_filter = valid.clone();
        mutate_model(&mut wrong_filter[2], |model| {
            let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) =
                &mut model.payload
            else {
                panic!("expected object-ability grant payload");
            };
            grant.filter = ObjectFilter::creature();
        });
        assert_eq!(
            describe_structural_threshold_source_modifier_bundle(&wrong_filter, "this creature"),
            None,
        );

        let mut multiple_grants = valid.clone();
        mutate_model(&mut multiple_grants[2], |model| {
            let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) =
                &mut model.payload
            else {
                panic!("expected object-ability grant payload");
            };
            let additional = grant.ability.clone();
            grant.additional_abilities.push(additional);
        });
        assert_eq!(
            describe_structural_threshold_source_modifier_bundle(&multiple_grants, "this creature"),
            None,
        );

        let mut wrong_zone = valid;
        wrong_zone[1].functional_zones = vec![Zone::Hand];
        assert_eq!(
            describe_structural_threshold_source_modifier_bundle(&wrong_zone, "this creature"),
            None,
        );
    }
}

#[cfg(test)]
mod conditioned_source_anthem_keyword_bundle_tests {
    use super::*;

    fn conditioned_source_modifier_abilities() -> Vec<Ability> {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Conditioned Source Modifier Probe",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(
            "This creature gets +1/+0 and has first strike as long as there are four or more card types among cards in your graveyard.",
        )
        .expect("conditioned source modifier should parse")
        .abilities
        .into_iter()
        .skip(1)
        .collect()
    }

    fn mutate_model(
        ability: &mut Ability,
        mutate: impl FnOnce(&mut crate::static_abilities::CompiledStaticAbility),
    ) {
        let AbilityKind::Static(static_ability) = &ability.kind else {
            panic!("expected static ability");
        };
        let mut model = static_ability
            .compiled_model()
            .expect("compiled static model")
            .clone();
        mutate(&mut model);
        ability.kind =
            AbilityKind::Static(crate::static_abilities::StaticAbility::from_model(model));
    }

    #[test]
    fn matching_typed_source_modifier_siblings_recombine() {
        let abilities = conditioned_source_modifier_abilities();
        assert_eq!(abilities.len(), 2);
        assert_eq!(
            describe_structural_conditioned_source_anthem_keyword_bundle(
                &abilities,
                "this creature",
            ),
            Some((
                "this creature gets +1/+0 and has first strike as long as there are four or more card types among cards in your graveyard"
                    .to_string(),
                2,
            )),
        );
    }

    #[test]
    fn delirium_three_way_source_modifier_recombines() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Delirium Source Modifier Probe",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(
            "Delirium — As long as there are four or more card types among cards in your graveyard, this creature gets +2/+2, has flying, and attacks each combat if able.",
        )
        .expect("three-way delirium source modifier should parse");

        assert_eq!(
            describe_structural_conditioned_source_anthem_keyword_bundle(
                &definition.abilities[1..],
                "this creature",
            ),
            Some((
                "Delirium — As long as there are four or more card types among cards in your graveyard, this creature gets +2/+2, has flying, and attacks each combat if able"
                    .to_string(),
                3,
            )),
        );
    }

    #[test]
    fn near_miss_source_modifier_siblings_stay_separate() {
        let valid = conditioned_source_modifier_abilities();

        let mut wrong_condition = valid.clone();
        mutate_model(&mut wrong_condition[1], |model| {
            let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) =
                &mut model.payload
            else {
                panic!("expected grant payload");
            };
            let Some(Condition::PlayerHasCardTypesInGraveyardOrMore { count, .. }) =
                &mut grant.condition
            else {
                panic!("expected graveyard card-type condition");
            };
            *count = 5;
        });
        assert_eq!(
            describe_structural_conditioned_source_anthem_keyword_bundle(
                &wrong_condition,
                "this creature",
            ),
            None,
        );

        let mut wrong_filter = valid.clone();
        mutate_model(&mut wrong_filter[0], |model| {
            let ironsmith_core::StaticAbilityPayload::Anthem(anthem) = &mut model.payload else {
                panic!("expected anthem payload");
            };
            anthem.filter = Some(ObjectFilter::creature());
        });
        assert_eq!(
            describe_structural_conditioned_source_anthem_keyword_bundle(
                &wrong_filter,
                "this creature",
            ),
            None,
        );

        let mut multiple_grants = valid.clone();
        mutate_model(&mut multiple_grants[1], |model| {
            let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) =
                &mut model.payload
            else {
                panic!("expected grant payload");
            };
            grant.additional_abilities.push(grant.ability.clone());
        });
        assert_eq!(
            describe_structural_conditioned_source_anthem_keyword_bundle(
                &multiple_grants,
                "this creature",
            ),
            None,
        );

        let mut wrong_zone = valid;
        wrong_zone[1].functional_zones = vec![Zone::Hand];
        assert_eq!(
            describe_structural_conditioned_source_anthem_keyword_bundle(
                &wrong_zone,
                "this creature",
            ),
            None,
        );
    }
}

#[cfg(test)]
mod station_threshold_static_surface_tests {
    use super::*;

    fn station_definition(text: &str) -> CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Station Static Surface Probe",
        )
        .card_types(vec![CardType::Artifact])
        .parse_text(text)
        .expect("station threshold line should parse")
    }

    fn replace_first_station_condition(definition: &mut CardDefinition, condition: Condition) {
        let static_ability = definition
            .abilities
            .iter_mut()
            .find_map(|ability| match &mut ability.kind {
                AbilityKind::Static(static_ability)
                    if static_ability.labeled_static_condition().is_some() =>
                {
                    Some(static_ability)
                }
                _ => None,
            })
            .expect("station row should lower to a labeled static ability");
        let mut model = static_ability
            .compiled_model()
            .expect("station row should retain a compiled model")
            .clone();
        let ironsmith_core::StaticAbilityPayload::Conditional {
            condition: found, ..
        } = &mut model.payload
        else {
            panic!("station row should retain its executable condition");
        };
        *found = condition;
        *static_ability = crate::static_abilities::StaticAbility::from_model(model);
    }

    #[test]
    fn typed_station_static_rows_render_prefix_before_the_body() {
        let land = station_definition(
            "Station\n3+ | You may play an additional land on each of your turns.",
        );
        let land_members = if source_line_static_group_count(&land.abilities[1]).is_some() {
            &land.abilities[2..]
        } else {
            &land.abilities[1..]
        };
        assert_eq!(
            describe_labeled_static_bundle(land_members, "this artifact"),
            Some((
                "3+ | You may play an additional land on each of your turns".to_string(),
                1,
            )),
        );

        let keywords = station_definition("Station\n8+ | Flying, haste");
        let keyword_members = if source_line_static_group_count(&keywords.abilities[1]).is_some() {
            &keywords.abilities[2..]
        } else {
            &keywords.abilities[1..]
        };
        assert_eq!(
            describe_labeled_static_bundle(keyword_members, "this artifact"),
            Some(("8+ | Flying, haste".to_string(), 2)),
        );
    }

    #[test]
    fn station_marker_requires_the_exact_charge_threshold_condition() {
        let mut wrong_threshold = station_definition("Station\n3+ | Flying");
        replace_first_station_condition(
            &mut wrong_threshold,
            Condition::ValueComparison {
                left: Value::CountersOnSource(CounterType::Charge),
                operator: ironsmith_core::ValueComparisonOperator::GreaterThanOrEqual,
                right: Value::Fixed(4),
            },
        );
        assert_eq!(
            describe_labeled_static_bundle(&wrong_threshold.abilities[1..], "this artifact"),
            None,
        );

        let mut wrong_counter = station_definition("Station\n3+ | Flying");
        replace_first_station_condition(
            &mut wrong_counter,
            Condition::ValueComparison {
                left: Value::CountersOnSource(CounterType::Level),
                operator: ironsmith_core::ValueComparisonOperator::GreaterThanOrEqual,
                right: Value::Fixed(3),
            },
        );
        assert_eq!(
            describe_labeled_static_bundle(&wrong_counter.abilities[1..], "this artifact"),
            None,
        );
    }
}

#[cfg(test)]
mod source_counter_threshold_static_surface_tests {
    use super::*;

    fn parsed_definition(
        name: &str,
        card_types: Vec<CardType>,
        subtypes: Vec<Subtype>,
        text: &str,
    ) -> CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(crate::ids::CardId::new(), name)
            .card_types(card_types)
            .subtypes(subtypes)
            .parse_text(text)
            .expect("counter-threshold static should parse")
    }

    #[test]
    fn typed_condition_shapes_render_no_a_and_numbered_thresholds() {
        let named = Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::CountersOnSourceWithSurface {
                counter_type: CounterType::Named("shell".into()),
                surface: crate::target::SourceReferenceSurface::ThisPermanentType(
                    "this creature".to_string(),
                ),
            },
            comparison: Comparison::LessThanOrEqual(0),
            display: Some("intentionally ignored display".to_string()),
        };
        assert_eq!(
            source_counter_threshold_condition_surface(&named, "this permanent").as_deref(),
            Some("this creature has no shell counters on it"),
        );

        let total = Condition::ValueComparison {
            left: Value::CountersOn(
                Box::new(ChooseSpec::Source.with_surface_hint(
                    ironsmith_core::ChooseSpecSurfaceHint::SourceReference(
                        crate::target::SourceReferenceSurface::ThisPermanentType(
                            "this Equipment".to_string(),
                        ),
                    ),
                )),
                None,
            ),
            operator: ironsmith_core::ValueComparisonOperator::GreaterThanOrEqual,
            right: Value::Fixed(4),
        };
        assert_eq!(
            source_counter_threshold_condition_surface(&total, "this artifact").as_deref(),
            Some("this Equipment has four or more counters on it"),
        );

        let one = Condition::ValueComparison {
            left: Value::CountersOn(Box::new(ChooseSpec::Source), None),
            operator: ironsmith_core::ValueComparisonOperator::GreaterThanOrEqual,
            right: Value::Fixed(1),
        };
        assert_eq!(
            source_counter_threshold_condition_surface(&one, "this creature").as_deref(),
            Some("this creature has a counter on it"),
        );

        let masculine = Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::CountersOnSourceWithPronoun {
                counter_type: CounterType::Named("conqueror".into()),
                pronoun: ironsmith_core::SourceCounterPronounSurface::Him,
            },
            comparison: Comparison::GreaterThanOrEqual(1),
            display: None,
        };
        assert_eq!(
            source_counter_threshold_condition_surface(&masculine, "Zhao").as_deref(),
            Some("Zhao has a conqueror counter on him"),
        );
    }

    #[test]
    fn non_source_counter_predicates_do_not_claim_the_surface() {
        let condition = Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::MatchingFilter(
                ObjectFilter::creature().with_counter_type(CounterType::Charge),
            ),
            comparison: Comparison::GreaterThanOrEqual(3),
            display: Some("a creature has three charge counters".to_string()),
        };
        assert_eq!(
            source_counter_threshold_condition_surface(&condition, "this creature"),
            None,
        );
    }

    #[test]
    fn frozen_static_shapes_round_trip_with_leading_counter_conditions() {
        let cases = [
            (
                "Roc Hatchling",
                vec![CardType::Creature],
                vec![],
                "As long as this creature has no shell counters on it, it gets +3/+2 and has flying.",
                "As long as this creature has no shell counters on it, it gets +3/+2 and has flying.",
                2,
            ),
            (
                "Warden of the Inner Sky",
                vec![CardType::Creature],
                vec![],
                "As long as this creature has three or more counters on it, it has flying and vigilance.",
                "As long as this creature has three or more counters on it, it has flying and vigilance.",
                2,
            ),
            (
                "Faithbound Judge",
                vec![CardType::Creature],
                vec![],
                "As long as this creature has three or more judgment counters on it, it can attack as though it didn't have defender.",
                "As long as this creature has three or more judgment counters on it, it can attack as though it didn't have defender.",
                1,
            ),
            (
                "Gavel of the Righteous",
                vec![CardType::Artifact],
                vec![Subtype::Equipment],
                "As long as this Equipment has four or more counters on it, equipped creature has double strike.",
                "As long as this Equipment has four or more counters on it, equipped creature has double strike.",
                1,
            ),
            (
                "Demon Wall",
                vec![CardType::Artifact, CardType::Creature],
                vec![],
                "As long as this creature has a counter on it, it can attack as though it didn't have defender.",
                "As long as this creature has a counter on it, it can attack as though it didn't have defender.",
                1,
            ),
        ];

        for (name, card_types, subtypes, oracle, expected, expected_consumed) in cases {
            let definition = parsed_definition(name, card_types, subtypes, oracle);
            let subject = subject_for_card(&definition.card);
            let abilities = if definition
                .abilities
                .first()
                .and_then(source_line_static_group_count)
                .is_some()
            {
                &definition.abilities[1..]
            } else {
                &definition.abilities
            };
            let (surface, consumed) =
                describe_structural_source_counter_threshold_static_bundle(abilities, subject)
                    .unwrap_or_else(|| {
                        panic!(
                            "typed counter-threshold renderer should match {name}: selected={abilities:#?}; all={:#?}",
                            definition.abilities
                        )
                    });
            assert_eq!(consumed, expected_consumed, "{name}: {surface}");
            assert_eq!(
                crate::compiled_text::compiled_text_lines(&definition),
                vec![expected.to_string()],
                "{name}: {surface}",
            );
        }
    }
}

#[cfg(test)]
mod conditioned_source_animation_annihilator_bundle_tests {
    use super::*;

    fn animation_definition() -> CardDefinition {
        crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Conditioned Source Animation Probe",
        )
        .card_types(vec![CardType::Artifact])
        .parse_text(
            "As long as this artifact has eight or more +1/+1 counters on it, it's a 0/0 creature in addition to its other types and it has annihilator 2.",
        )
        .expect("conditioned source animation should parse")
    }

    #[test]
    fn matching_animation_and_annihilator_siblings_recombine() {
        let definition = animation_definition();
        assert_eq!(
            describe_structural_conditioned_source_animation_annihilator_bundle(
                &definition.abilities[1..],
            ),
            Some((
                "As long as this artifact has eight or more +1/+1 counters on it, it's a 0/0 creature in addition to its other types and it has annihilator 2"
                    .to_string(),
                3,
            )),
        );
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![
                "As long as this artifact has eight or more +1/+1 counters on it, it's a 0/0 creature in addition to its other types and it has annihilator 2."
                    .to_string(),
            ],
        );
    }

    #[test]
    fn mismatched_grant_condition_does_not_recombine() {
        let mut definition = animation_definition();
        let AbilityKind::Static(grant_static) = &definition.abilities[3].kind else {
            panic!("expected annihilator grant");
        };
        let mut model = grant_static
            .compiled_model()
            .expect("compiled grant model")
            .clone();
        let ironsmith_core::StaticAbilityPayload::GrantObjectAbilityForFilter(grant) =
            &mut model.payload
        else {
            panic!("expected typed grant payload");
        };
        grant.condition = Some(Condition::YourTurn);
        definition.abilities[3].kind =
            AbilityKind::Static(crate::static_abilities::StaticAbility::from_model(model));

        assert_eq!(
            describe_structural_conditioned_source_animation_annihilator_bundle(
                &definition.abilities[1..],
            ),
            None,
        );
    }
}

#[cfg(test)]
mod target_player_life_loss_then_gain_and_create_tests {
    use super::*;

    #[test]
    fn coordinated_gain_and_token_sentence_retains_actor_and_boundaries() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Life And Token Probe",
        )
        .card_types(vec![CardType::Sorcery])
        .parse_text(
            "Target player loses 3 life. You gain 3 life and create three 0/1 colorless Eldrazi Spawn creature tokens. They have \"Sacrifice this token: Add {C}.\"",
        )
        .expect("life-and-token spell should parse");
        let program = definition
            .spell_effect
            .as_ref()
            .expect("spell resolution program");

        assert_eq!(
            describe_target_player_life_loss_then_gain_and_create_program(program),
            Some(
                "Target player loses 3 life. You gain 3 life and create three 0/1 colorless Eldrazi Spawn creature tokens. They have \"Sacrifice this token: Add {C}.\""
                    .to_string(),
            ),
        );
        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![
                "Target player loses 3 life. You gain 3 life and create three 0/1 colorless Eldrazi Spawn creature tokens. They have \"Sacrifice this token: Add {C}.\""
                    .to_string(),
            ],
        );
    }
}

#[cfg(test)]
mod shared_x_cross_segment_exile_permission_tests {
    use super::*;

    #[test]
    fn reduction_and_exile_top_share_one_where_x_basis_across_source_sentences() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Shared X Exile Permission Probe",
        )
        .card_types(vec![CardType::Creature])
        .parse_text(
            "Whenever you attack, spells you cast from exile this turn cost {X} less to cast, where X is the number of players being attacked. Exile the top X cards of your library. Until end of turn, you may cast spells from among those exiled cards.",
        )
        .expect("shared-X exile permission trigger should parse");

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec![
                "Whenever you attack, spells you cast from exile this turn cost {X} less to cast, where X is the number of players being attacked. Exile the top X cards of your library. Until end of turn, you may cast spells from among those exiled cards."
                    .to_string(),
            ],
        );
    }
}

#[cfg(test)]
mod prototype_surface_tests {
    use super::*;

    #[test]
    fn prototype_frame_line_keeps_separator_and_precedes_other_abilities() {
        let definition = crate::compiler_test_support::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Prototype Surface Probe",
        )
        .card_types(vec![CardType::Artifact, CardType::Creature])
        .parse_text("Prototype {2}{R} — 3/2\nHaste")
        .expect("Prototype surface probe should parse");

        let lines = compiled_lines_inner(&definition);
        assert_eq!(
            lines.first().map(String::as_str),
            Some("Prototype {2}{R} — 3/2")
        );
        assert!(
            lines.iter().skip(1).any(|line| line.ends_with("Haste")),
            "non-Prototype abilities should follow the Prototype frame line: {lines:?}"
        );
    }
}

#[cfg(test)]
mod counter_removal_prevention_surface_tests {
    use super::*;

    fn prevention_ability(
        amount: Value,
        follow_up: Option<ironsmith_core::CounterRemovalFollowUp>,
    ) -> Ability {
        let model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::prevent_damage_to_self_remove_counter_with_follow_up(
                CounterType::PlusOnePlusOne,
                amount,
                follow_up,
            )
            .with_condition(Condition::CountComparison {
                count: ironsmith_core::AnthemCountExpression::CountersOnSource(
                    CounterType::PlusOnePlusOne,
                ),
                comparison: Comparison::GreaterThanOrEqual(1),
                display: Some("it has a +1/+1 counter on it".to_string()),
            });
        Ability::static_ability(crate::static_abilities::StaticAbility::from_model(model))
    }

    #[test]
    fn five_card_family_renders_inline_replacement_surface() {
        let fixed = "If damage would be dealt to this creature while it has a +1/+1 counter on it, prevent that damage and remove a +1/+1 counter from it.";
        let dynamic = "If damage would be dealt to this creature while it has a +1/+1 counter on it, prevent that damage and remove that many +1/+1 counters from it.";
        let bloatfly = "If damage would be dealt to this creature while it has a +1/+1 counter on it, prevent that damage, remove that many +1/+1 counters from it, then give each player a rad counter for each +1/+1 counter removed this way.";
        let cases = [
            ("Oathsworn Knight", Value::Fixed(1), None, fixed),
            ("Undergrowth Champion", Value::Fixed(1), None, fixed),
            (
                "Ugin's Conjurant",
                Value::EventValue(EventValueSpec::Amount),
                None,
                dynamic,
            ),
            (
                "Magma Pummeler",
                Value::EventValue(EventValueSpec::Amount),
                None,
                dynamic,
            ),
            (
                "Bloatfly Swarm",
                Value::EventValue(EventValueSpec::Amount),
                Some(
                    ironsmith_core::CounterRemovalFollowUp::EachPlayerGetsCounters {
                        counter_type: CounterType::Rad,
                        counters_per_removed: 1,
                    },
                ),
                bloatfly,
            ),
        ];

        for (name, amount, follow_up, expected) in cases {
            let definition =
                crate::cards::builders::CardDefinitionBuilder::new(crate::ids::CardId::new(), name)
                    .card_types(vec![CardType::Creature])
                    .with_ability(prevention_ability(amount, follow_up))
                    .build();
            assert_eq!(
                crate::compiled_text::compiled_text_lines(&definition),
                vec![expected.to_string()],
                "{name}"
            );
        }
    }

    #[test]
    fn near_miss_condition_and_bare_payload_do_not_get_inline_surface() {
        let mut mismatched = prevention_ability(Value::Fixed(1), None);
        let AbilityKind::Static(static_ability) = &mut mismatched.kind else {
            unreachable!();
        };
        let mut model = static_ability
            .compiled_model()
            .expect("compiled model")
            .clone();
        let ironsmith_core::StaticAbilityPayload::Conditional { condition, .. } =
            &mut model.payload
        else {
            unreachable!();
        };
        *condition = Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::MatchingFilter(
                ObjectFilter::source().with_counter_type(CounterType::Shield),
            ),
            comparison: Comparison::GreaterThanOrEqual(1),
            display: Some("it has a shield counter on it".to_string()),
        };
        mismatched.kind =
            AbilityKind::Static(crate::static_abilities::StaticAbility::from_model(model));
        assert_eq!(
            describe_structural_counter_removal_damage_prevention(&mismatched, "this creature"),
            None
        );

        let mut non_source = prevention_ability(Value::Fixed(1), None);
        let AbilityKind::Static(static_ability) = &mut non_source.kind else {
            unreachable!();
        };
        let mut model = static_ability
            .compiled_model()
            .expect("compiled model")
            .clone();
        let ironsmith_core::StaticAbilityPayload::Conditional { condition, .. } =
            &mut model.payload
        else {
            unreachable!();
        };
        *condition = Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::MatchingFilter(
                ObjectFilter::creature().with_counter_type(CounterType::PlusOnePlusOne),
            ),
            comparison: Comparison::GreaterThanOrEqual(1),
            display: Some("a creature has a +1/+1 counter on it".to_string()),
        };
        non_source.kind =
            AbilityKind::Static(crate::static_abilities::StaticAbility::from_model(model));
        assert_eq!(
            describe_structural_counter_removal_damage_prevention(&non_source, "this creature"),
            None
        );

        let bare_model: crate::static_abilities::CompiledStaticAbility =
            ironsmith_core::StaticAbility::prevent_damage_to_self_remove_counter(
                CounterType::PlusOnePlusOne,
                1,
            );
        let bare = Ability::static_ability(crate::static_abilities::StaticAbility::from_model(
            bare_model,
        ));
        assert_eq!(
            describe_structural_counter_removal_damage_prevention(&bare, "this creature"),
            None
        );
    }

    #[test]
    fn source_surface_counter_count_is_the_same_typed_threshold() {
        let mut ability = prevention_ability(Value::Fixed(1), None);
        let AbilityKind::Static(static_ability) = &mut ability.kind else {
            unreachable!();
        };
        let mut model = static_ability
            .compiled_model()
            .expect("compiled model")
            .clone();
        let ironsmith_core::StaticAbilityPayload::Conditional { condition, .. } =
            &mut model.payload
        else {
            unreachable!();
        };
        *condition = Condition::CountComparison {
            count: ironsmith_core::AnthemCountExpression::CountersOnSourceWithSurface {
                counter_type: CounterType::PlusOnePlusOne,
                surface: crate::target::SourceReferenceSurface::ThisPermanentType(
                    "this creature".to_string(),
                ),
            },
            comparison: Comparison::GreaterThanOrEqual(1),
            display: Some("this creature has a +1/+1 counter on it".to_string()),
        };
        ability.kind =
            AbilityKind::Static(crate::static_abilities::StaticAbility::from_model(model));

        assert_eq!(
            describe_structural_counter_removal_damage_prevention(&ability, "this creature")
                .as_deref(),
            Some(
                "If damage would be dealt to this creature while it has a +1/+1 counter on it, prevent that damage and remove a +1/+1 counter from it"
            )
        );
    }

    fn removed_this_way_damage_ability(triggering_tag: &str, damage_source_tag: &str) -> Ability {
        let trigger = crate::triggers::Trigger::new(
            crate::triggers::CounterRemovedFromTrigger::new(ObjectFilter::source_with_surface(
                crate::target::SourceReferenceSurface::ThisPermanentType(
                    "this creature".to_string(),
                ),
            ))
            .one_or_more()
            .caused_by_source(),
        )
        .with_intro_surface(crate::triggers::TriggerIntroSurface::When);
        let amount = Value::EventValue(EventValueSpec::Amount)
            .with_surface_hint(ironsmith_core::ValueSurfaceHint::CountersRemovedThisWay);
        let effects = vec![
            Effect::tag_triggering_object(TagKey::from(triggering_tag)),
            Effect::new(crate::effects::ExecuteWithSourceEffect::new(
                ChooseSpec::Tagged(TagKey::from(damage_source_tag)),
                Effect::deal_damage(amount, ChooseSpec::AnyTarget),
            )),
        ];
        let mut ability = Ability::triggered(trigger, effects);
        let AbilityKind::Triggered(triggered) = &mut ability.kind else {
            unreachable!();
        };
        triggered.choices = vec![ChooseSpec::AnyTarget];
        ability
    }

    #[test]
    fn removed_this_way_trigger_renders_event_amount_damage_to_any_target() {
        let ability = removed_this_way_damage_ability("triggering", "triggering");
        let definition = crate::cards::builders::CardDefinitionBuilder::new(
            crate::ids::CardId::new(),
            "Magma Pummeler Surface Probe",
        )
        .card_types(vec![CardType::Creature])
        .with_ability(ability)
        .build();

        assert_eq!(
            crate::compiled_text::compiled_text_lines(&definition),
            vec!["When one or more counters are removed from this creature this way, it deals that much damage to any target.".to_string()]
        );
    }

    #[test]
    fn removed_this_way_trigger_requires_the_tagged_triggering_object_as_damage_source() {
        let mismatched = removed_this_way_damage_ability("triggering", "other");
        assert_eq!(
            describe_structural_counter_removed_this_way_damage_trigger(&mismatched),
            None
        );
    }
}

#[cfg(test)]
mod flashback_non_mana_cost_surface_tests {
    use super::*;

    fn sacrifice_creature_cost() -> crate::costs::Cost {
        crate::costs::Cost::sacrifice(crate::filter::ObjectFilter::creature().you_control())
    }

    #[test]
    fn effect_only_flashback_omits_invented_zero_mana_cost() {
        let effect_only = AlternativeCastingMethod::Flashback {
            total_cost: crate::cost::TotalCost::from_cost(sacrifice_creature_cost()),
        };
        assert_eq!(
            describe_alternative_cast_line(&effect_only, 0),
            "Flashback—Sacrifice a creature"
        );
    }

    #[test]
    fn explicit_zero_and_mixed_flashback_costs_keep_their_mana_surface() {
        let explicit_zero = AlternativeCastingMethod::Flashback {
            total_cost: crate::cost::TotalCost::free(),
        };
        assert_eq!(
            describe_alternative_cast_line(&explicit_zero, 0),
            "Flashback—{0}"
        );

        let mixed = AlternativeCastingMethod::Flashback {
            total_cost: crate::cost::TotalCost::from_costs(vec![
                crate::costs::Cost::mana(crate::mana::ManaCost::from_symbols(vec![
                    crate::mana::ManaSymbol::Generic(1),
                ])),
                sacrifice_creature_cost(),
            ]),
        };
        assert_eq!(
            describe_alternative_cast_line(&mixed, 0),
            "Flashback—{1}, Sacrifice a creature"
        );
    }
}

#[cfg(test)]
mod countered_set_cross_segment_surface_tests {
    use super::*;

    fn tagged_counter_each(tag: &TagKey, filter: ObjectFilter) -> Effect {
        Effect::new(crate::effects::ForEachObject::new(
            filter,
            vec![Effect::new(crate::effects::PutCountersEffect::new(
                CounterType::PlusOnePlusOne,
                Value::Fixed(1),
                ChooseSpec::Iterated,
            ))],
        ))
        .tag(tag.clone())
    }

    #[test]
    fn countered_set_untap_back_reference_survives_segment_boundary() {
        let tag = TagKey::from("counters_0");
        let counters = tagged_counter_each(
            &tag,
            ObjectFilter::creature().controlled_by(PlayerFilter::You),
        );
        let untap = Effect::new(crate::effects::UntapEffect::with_spec(ChooseSpec::All(
            ObjectFilter::creature()
                .match_tagged(tag, crate::filter::TaggedOpbjectRelation::IsTaggedObject),
        )));
        let program = crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![counters]),
            crate::resolution::ResolutionSegment::from_effects(vec![untap]),
        ]);

        assert_eq!(
            describe_resolution_program(&program),
            "Put a +1/+1 counter on each creature you control. Untap those creatures"
        );
    }

    #[test]
    fn countered_set_count_back_reference_survives_segment_boundary() {
        let tag = TagKey::from("counters_0");
        let counters = tagged_counter_each(
            &tag,
            ObjectFilter::creature().controlled_by(PlayerFilter::You),
        );
        let counted = ObjectFilter::creature()
            .match_tagged(tag, crate::filter::TaggedOpbjectRelation::IsTaggedObject);
        let gain = Effect::new(crate::effects::GainLifeEffect::you(
            Value::Count(counted).with_surface_hint(ironsmith_core::ValueSurfaceHint::ForEach),
        ));
        let program = crate::resolution::ResolutionProgram::new(vec![
            crate::resolution::ResolutionSegment::from_effects(vec![counters]),
            crate::resolution::ResolutionSegment::from_effects(vec![gain]),
        ]);

        assert_eq!(
            describe_resolution_program(&program),
            "Put a +1/+1 counter on each creature you control. You gain 1 life for each of those creatures"
        );
    }
}
